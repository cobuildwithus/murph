{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "murph-hosted",
  "main": "src/index.ts",
  "compatibility_date": "2026-03-27",
  "compatibility_flags": ["nodejs_compat"],
  "containers": [
    {
      "class_name": "RunnerContainer",
      "image": "../../Dockerfile.cloudflare-hosted-runner",
      "instance_type": "standard-1",
      "max_instances": 50
    }
  ],
  "durable_objects": {
    "bindings": [
      {
        "name": "USER_RUNNER",
        "class_name": "UserRunnerDurableObject"
      },
      {
        "name": "RUNNER_CONTAINER",
        "class_name": "RunnerContainer"
      }
    ]
  },
  "migrations": [
    {
      "tag": "v1",
      "new_sqlite_classes": ["UserRunnerDurableObject"]
    },
    {
      "tag": "v2",
      "new_sqlite_classes": ["RunnerContainer"]
    }
  ],
  "r2_buckets": [
    {
      "binding": "BUNDLES",
      "bucket_name": "murph-hosted-bundles",
      "preview_bucket_name": "murph-hosted-bundles-preview"
    }
  ],
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1,
    "traces": {
      "enabled": true,
      "head_sampling_rate": 0.1
    }
  },
  "secrets": {
    "required": [
      "HOSTED_EXECUTION_PLATFORM_ENVELOPE_KEY",
      "HOSTED_EXECUTION_AUTOMATION_RECIPIENT_PRIVATE_JWK",
      "HOSTED_EXECUTION_AUTOMATION_RECIPIENT_PUBLIC_JWK",
      "HOSTED_EXECUTION_RECOVERY_RECIPIENT_PUBLIC_JWK",
      "HOSTED_WEB_CALLBACK_SIGNING_PRIVATE_JWK"
    ]
  },
  "vars": {
    "HOSTED_EXECUTION_PLATFORM_ENVELOPE_KEY_ID": "v1",
    "HOSTED_EXECUTION_RECOVERY_RECIPIENT_KEY_ID": "recovery:v1",
    "HOSTED_EXECUTION_DEFAULT_ALARM_DELAY_MS": "21600000",
    "HOSTED_EXECUTION_MAX_EVENT_ATTEMPTS": "3",
    "HOSTED_EXECUTION_RETRY_DELAY_MS": "30000",
    "HOSTED_EXECUTION_RUNNER_COMMIT_TIMEOUT_MS": "30000",
    "HOSTED_EXECUTION_RUNNER_TIMEOUT_MS": "120000",
    "HOSTED_EXECUTION_VERCEL_OIDC_ENVIRONMENT": "production"
  },
  // Configure these outside source control for real deployments:
  // - HOSTED_EXECUTION_PLATFORM_ENVELOPE_KEY
  // - HOSTED_EXECUTION_AUTOMATION_RECIPIENT_PRIVATE_JWK
  // - HOSTED_EXECUTION_AUTOMATION_RECIPIENT_PUBLIC_JWK
  // - HOSTED_EXECUTION_RECOVERY_RECIPIENT_PUBLIC_JWK
  // - HOSTED_EXECUTION_VERCEL_OIDC_PROJECT_NAME
  // - HOSTED_EXECUTION_VERCEL_OIDC_TEAM_SLUG
  // - HOSTED_WEB_BASE_URL
  // - HOSTED_WEB_CALLBACK_SIGNING_PRIVATE_JWK
  // - HOSTED_EXECUTION_PLATFORM_ENVELOPE_KEYRING_JSON (optional JSON keyring for decrypting older platform envelope key ids)
  // - HOSTED_EXECUTION_TEE_AUTOMATION_RECIPIENT_PUBLIC_JWK (optional public JWK for future enclave-only automation unwrap)
  // Wrangler's `secrets.required` support is experimental; keep the required list tight.
  // Optional provider/toolchain vars and secrets must be configured on the Worker so the
  // Durable Object can forward them into the native container at runtime.
}
