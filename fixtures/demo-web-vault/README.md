# Demo Web Vault

This fixture is a small but populated vault for richer manual review and QA flows.

It is richer than `fixtures/minimal-vault` and includes enough canonical content to
exercise memory, knowledge, preferences, metric, timeline, sample-summary, and
search-oriented scenarios.
