# Demo Web Vault

This fixture is a small but populated vault intended for the local web observatory.

It is richer than `fixtures/minimal-vault` and includes enough canonical content to
exercise the profile, metric, timeline, sample-summary, and search surfaces.
