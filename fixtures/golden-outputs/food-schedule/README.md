# `vault-cli food schedule`

- Returns the remembered `foodId`, stable path, requested daily time, and paired assistant-cron job metadata.
- Persists `autoLogDaily.time` on the food record and creates a matching internal `foodAutoLog` cron job.
