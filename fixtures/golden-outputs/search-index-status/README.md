# Search Index Status Golden Output

Scaffold for the `vault-cli search index-status` smoke scenario. Package runtime tests own behavioral assertions; this directory exists so the smoke harness can keep the documented command surface indexed.
