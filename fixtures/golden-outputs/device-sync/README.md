# Device Sync Golden Output

Representative smoke-output scaffold for the local device control plane exposed through `vault-cli device ...`.

Use this directory for scenarios that describe provider discovery, browser-based connect handoff, and device account inspection/reconcile/disconnect results backed by the local `@murphai/device-syncd` daemon.
