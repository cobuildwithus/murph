# `vault-cli samples csv profile`

Current smoke expectation:

- returns CSV shape and row planning details without writing canonical samples
- can include pre-write numeric summaries for supported summary profiles
- reports skipped rows and candidate stream metadata before import
