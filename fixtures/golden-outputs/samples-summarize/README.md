# `vault-cli samples summarize`

Current smoke expectation:

- reads stored canonical samples for a single stream and timestamp window
- returns the same summary model used by CSV profile pre-write summaries
- reports threshold and gap observations without making clinical claims
