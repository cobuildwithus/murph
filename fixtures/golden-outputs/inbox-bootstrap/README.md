# `vault-cli inbox bootstrap`

Current smoke expectation:

- initializes the local inbox runtime state under `.runtime/inboxd`
- writes parser toolchain config under `.runtime/parsers/toolchain.json`
