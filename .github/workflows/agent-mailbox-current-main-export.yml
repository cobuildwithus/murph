name: Agent mailbox current main export

on:
  push:
    branches:
      - agent/mailbox-current-main-source-v2
    paths:
      - .github/workflows/agent-mailbox-current-main-export.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Archive tracked source worktree
        shell: bash
        run: |
          set -euo pipefail
          git archive --format=tar.gz --output=murph-source.tar.gz HEAD
      - name: Upload source worktree
        uses: actions/upload-artifact@v4
        with:
          name: murph-mailbox-current-main-v2
          path: murph-source.tar.gz
          if-no-files-found: error
          retention-days: 1
