name: Native Android Hosted E2E

on:
  schedule:
    - cron: "47 */6 * * *"

# This trusted default-branch controller spends native-runner capacity only
# after protected main advances beyond the latest successful scheduled proof.
# A failed canary remains eligible at the next slot; an explicit workflow rerun
# also retries the same revision immediately.
permissions:
  actions: read
  contents: read

concurrency:
  group: native-android-production-canary
  cancel-in-progress: false

jobs:
  select-main:
    name: Select changed protected main
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    outputs:
      should_run: ${{ steps.select.outputs.should_run }}
      web_sha: ${{ steps.select.outputs.web_sha }}
    steps:
      - name: Compare main with the latest successful scheduled proof
        id: select
        env:
          CURRENT_SHA: ${{ github.sha }}
          GH_TOKEN: ${{ github.token }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
        shell: bash
        run: |
          set -euo pipefail
          [[ "${CURRENT_SHA}" =~ ^[0-9a-f]{40}$ ]] || { echo "::error::Schedule must identify an exact protected-main SHA."; exit 1; }
          previous_sha=""
          if [[ "${RUN_ATTEMPT}" == 1 ]]; then
            previous_sha="$(gh api "repos/${GITHUB_REPOSITORY}/actions/workflows/native-android-hosted-e2e.yml/runs?event=schedule&status=success&per_page=1" --jq '.workflow_runs[0].head_sha // ""')"
            if [[ -n "${previous_sha}" && ! "${previous_sha}" =~ ^[0-9a-f]{40}$ ]]; then
              echo "::error::Latest successful native Android schedule did not expose an exact SHA."
              exit 1
            fi
          fi
          should_run=true
          if [[ "${RUN_ATTEMPT}" == 1 && "${previous_sha}" == "${CURRENT_SHA}" ]]; then
            should_run=false
          fi
          {
            echo "should_run=${should_run}"
            echo "web_sha=${CURRENT_SHA}"
          } >> "${GITHUB_OUTPUT}"
          echo "::notice::native-android-e2e stage=schedule_admission result=${should_run}"

  production-canary:
    name: Non-destructive production native Android canary
    needs: select-main
    if: ${{ needs.select-main.outputs.should_run == 'true' }}
    environment: native-android-production-canary
    runs-on: ubuntu-24.04
    timeout-minutes: 110
    steps:
      - name: Checkout trusted default branch
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ needs.select-main.outputs.web_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: 24.14.1
          cache: pnpm

      - name: Install trusted control dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Android dispatcher contracts
        run: node --test scripts/native-android-hosted-e2e.test.mjs

      - name: Verify scheduled SHA is protected-main history
        env:
          DEPLOYED_SHA: ${{ needs.select-main.outputs.web_sha }}
        run: |
          set -euo pipefail
          [[ "${DEPLOYED_SHA}" =~ ^[0-9a-f]{40}$ ]] || { echo "::error::Production canary must identify an exact Git SHA."; exit 1; }
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor "${DEPLOYED_SHA}" origin/main || { echo "::error::Production canary refuses a revision outside main history."; exit 1; }

      - name: Verify production alias and dispatch non-destructive canary
        env:
          DEPLOYED_SHA: ${{ needs.select-main.outputs.web_sha }}
          HOSTED_WEB_PRODUCTION_BASE_URL: ${{ vars.HOSTED_WEB_PRODUCTION_BASE_URL }}
          HOSTED_WEB_VERCEL_PROJECT_ID: ${{ vars.HOSTED_WEB_VERCEL_PROJECT_ID }}
          HOSTED_WEB_VERCEL_TEAM_ID: ${{ vars.HOSTED_WEB_VERCEL_TEAM_ID }}
          HOSTED_WEB_VERCEL_TOKEN: ${{ secrets.HOSTED_WEB_VERCEL_TOKEN }}
          NATIVE_ANDROID_E2E_ANDROID_EXPECTED_SHA: ${{ vars.NATIVE_ANDROID_E2E_ANDROID_EXPECTED_SHA }}
          NATIVE_ANDROID_E2E_ANDROID_REF: ${{ vars.NATIVE_ANDROID_E2E_ANDROID_REF }}
          NATIVE_ANDROID_E2E_ANDROID_REPOSITORY: ${{ vars.NATIVE_ANDROID_E2E_ANDROID_REPOSITORY_OWNER }}/${{ vars.NATIVE_ANDROID_E2E_ANDROID_REPOSITORY_NAME }}
          NATIVE_ANDROID_E2E_ANDROID_WORKFLOW: ${{ vars.NATIVE_ANDROID_E2E_ANDROID_WORKFLOW }}
          NATIVE_ANDROID_E2E_GITHUB_APP_ID: ${{ vars.NATIVE_ANDROID_E2E_GITHUB_APP_ID }}
          NATIVE_ANDROID_E2E_GITHUB_APP_PRIVATE_KEY: ${{ secrets.NATIVE_ANDROID_E2E_GITHUB_APP_PRIVATE_KEY }}
          PRODUCTION_BASE_URL: ${{ vars.HOSTED_WEB_PRODUCTION_BASE_URL }}
        shell: bash
        run: |
          set -euo pipefail
          node scripts/native-android-hosted-e2e.mjs canary \
            --web-sha "${DEPLOYED_SHA}" \
            --web-base-url "${PRODUCTION_BASE_URL}" \
            --correlation-id "murph-production-${DEPLOYED_SHA}-${{ github.run_id }}-${{ github.run_attempt }}"
