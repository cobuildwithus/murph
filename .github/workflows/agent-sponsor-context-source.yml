name: Prepare sponsor-context WIP

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  waiting-for-payload:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Waiting for the complete WIP payload."
