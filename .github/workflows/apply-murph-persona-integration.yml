name: Temporary Murph persona repair helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary helper disabled; source repairs are committed directly."
