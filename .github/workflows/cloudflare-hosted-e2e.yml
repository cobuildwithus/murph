name: Cloudflare Hosted E2E

on:
  pull_request:
    paths:
      - ".github/workflows/cloudflare-hosted-e2e.yml"
      - "package.json"
      - "apps/cloudflare/**"
      - "apps/web/src/lib/hosted-orchestration/**"
      - "apps/web/test/hosted-orchestration-*.test.ts"
      - "apps/web/test/support/**"
      - "packages/assistant-engine/**"
      - "packages/assistant-runtime/**"
      - "packages/cloudflare-hosted-control/**"
      - "packages/hosted-execution/**"
      - "packages/hosted-local-harness/**"
      - "packages/hosted-orchestrator-temporal/**"
      - "render.yaml"
      - "scripts/dev-hosted-local.ts"
      - "scripts/hosted-local.ts"
      - "scripts/setup-temporal-cli.sh"
      - "scripts/temporal-dev-server.sh"
  workflow_dispatch:

permissions:
  contents: read

env:
  # The hosted local E2E harness runs `prisma db push` during setup, so these
  # jobs need a real loopback Postgres target rather than the unreachable
  # build-only placeholder URL used by release/build verification lanes.
  DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/murph_test
  HOSTED_DEVICE_ROUTING_INDEX_KEY: 0101010101010101010101010101010101010101010101010101010101010101
  HOSTED_CONTACT_PRIVACY_KEYS: v1:BwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwc
  HOSTED_MAILBOX_FINGERPRINT_KEY: BwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwc
  NEXT_PUBLIC_PRIVY_APP_ID: ${{ vars.HOSTED_WEB_VERIFY_PRIVY_APP_ID }}
  PRIVY_VERIFICATION_KEY: ci-hosted-web-verification-key

jobs:
  device-connect:
    name: Device connect E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-device-connect-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run device connect hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e device-connect 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/device-connect.log

      - name: Upload device connect E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-device-connect-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/device-connect.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  linq-delivery:
    name: Linq delivery E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-linq-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run Linq delivery hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e linq-delivery 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/linq-delivery.log

      - name: Upload Linq delivery E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-linq-delivery-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/linq-delivery.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  linq-scheduled-reminder:
    name: Linq scheduled reminder E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-linq-scheduled-reminder-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run Linq scheduled reminder hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e linq-scheduled-reminder 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/linq-scheduled-reminder.log

      - name: Upload Linq scheduled reminder E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-linq-scheduled-reminder-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/linq-scheduled-reminder.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  idle-checkpoint-deferred-progress:
    name: Idle checkpoint deferred progress E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-idle-checkpoint-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run idle checkpoint deferred progress hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e idle-checkpoint-deferred-progress 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/idle-checkpoint-deferred-progress.log

      - name: Run direct R2 presigned PUT hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e direct-r2-presigned-put 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/direct-r2-presigned-put.log

      - name: Upload idle checkpoint deferred progress E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-idle-checkpoint-deferred-progress-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/idle-checkpoint-deferred-progress.log
            .artifacts/cloudflare-hosted-e2e/direct-r2-presigned-put.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  temporal-orchestration:
    name: Temporal orchestration E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-temporal-orchestration-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run Temporal orchestration hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e temporal-orchestration 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/temporal-orchestration.log

      - name: Upload Temporal orchestration E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-temporal-orchestration-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/temporal-orchestration.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  telegram-delivery:
    name: Telegram delivery E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-telegram-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Run Telegram delivery hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e telegram 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/telegram-delivery.log

      - name: Upload Telegram delivery E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-telegram-delivery-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/telegram-delivery.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn
