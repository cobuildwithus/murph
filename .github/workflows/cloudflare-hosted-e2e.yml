name: Cloudflare Hosted E2E

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

env:
  # The hosted local E2E harness runs `prisma db push` during setup, so these
  # jobs need a real loopback Postgres target rather than the unreachable
  # build-only placeholder URL used by release/build verification lanes.
  DATABASE_URL: postgresql://postgres:postgres@127.0.0.1:5432/murph_test
  HOSTED_DEVICE_ROUTING_INDEX_KEY: 0101010101010101010101010101010101010101010101010101010101010101
  HOSTED_CONTACT_PRIVACY_KEYS: v1:BwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwc
  HOSTED_MAILBOX_FINGERPRINT_KEY: BwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwcHBwc
  NEXT_PUBLIC_PRIVY_APP_ID: ${{ vars.HOSTED_WEB_VERIFY_PRIVY_APP_ID }}
  PRIVY_VERIFICATION_KEY: ci-hosted-web-verification-key
  # The runner-bundle workspace build defaults to a fully serial
  # `--workspace-concurrency=1`; CI runners have 4 vCPUs, so build in parallel.
  MURPH_RUNNER_BUNDLE_BUILD_CONCURRENCY: "4"

jobs:
  runner-bundle:
    name: Build hosted runner bundle
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    concurrency:
      group: cloudflare-hosted-e2e-runner-bundle-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      # Build the hosted-local runner bundle once and share it with every
      # scenario job below; the scenario jobs run with `--no-bundle` so they
      # skip the duplicated full-workspace build.
      - name: Assemble hosted-local runner bundle
        env:
          MURPH_RUNNER_BUNDLE_SKIP_PACK_PREFLIGHTS: "1"
          # The linq-webhook scenario needs the bundle-local e2e parser
          # toolchain stub (ffmpeg); other scenarios ignore the extra files.
          MURPH_RUNNER_BUNDLE_TEST_PARSER_TOOLCHAIN: "1"
        run: pnpm --dir apps/cloudflare runner:bundle:hosted-local

      # The tar includes the workspace dist outputs because the Temporal
      # worker's workflow webpack bundle resolves @murphai/* package exports
      # to dist/, unlike vitest and tsx which alias workspace sources.
      - name: Pack runner bundle artifact
        run: tar --zstd -cf runner-bundle.tar.zst apps/cloudflare/.deploy/runner-bundle packages/*/dist apps/cloudflare/dist

      - name: Upload runner bundle artifact
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: hosted-local-runner-bundle
          path: runner-bundle.tar.zst
          retention-days: 1
          compression-level: 0
          if-no-files-found: error

  device-connect:
    name: Device connect E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-device-connect-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run device connect hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e device-connect --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/device-connect.log

      - name: Upload device connect E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-device-connect-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/device-connect.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  linq-delivery:
    name: Linq delivery E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-linq-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run Linq delivery hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e linq-delivery --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/linq-delivery.log

      - name: Upload Linq delivery E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-linq-delivery-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/linq-delivery.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  linq-webhook-media:
    name: Linq webhook media E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-linq-webhook-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      # Gates the Worker-mediated Workers AI transcription path: audio media
      # flows through the container parser drain, the remote-transcription
      # provider, and the murph-transcribe.worker egress handler with a
      # deterministic fake AI binding from the hosted-local test entrypoint.
      - name: Run Linq webhook media hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e linq-webhook --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/linq-webhook.log

      - name: Upload Linq webhook media E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-linq-webhook-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/linq-webhook.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  codex-image-media-delivery:
    name: Codex image media delivery E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-codex-image-media-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run Codex image media delivery hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e codex-image-media-delivery --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/codex-image-media-delivery.log

      - name: Upload Codex image media delivery E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-codex-image-media-delivery-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/codex-image-media-delivery.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  linq-scheduled-reminder:
    name: Linq scheduled reminder E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-linq-scheduled-reminder-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run Linq scheduled reminder hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e linq-scheduled-reminder --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/linq-scheduled-reminder.log

      - name: Upload Linq scheduled reminder E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-linq-scheduled-reminder-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/linq-scheduled-reminder.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  idle-checkpoint-deferred-progress:
    name: Idle checkpoint deferred progress E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-idle-checkpoint-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run idle checkpoint deferred progress hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e idle-checkpoint-deferred-progress --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/idle-checkpoint-deferred-progress.log

      - name: Run direct R2 presigned PUT hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e direct-r2-presigned-put --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/direct-r2-presigned-put.log

      - name: Upload idle checkpoint deferred progress E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-idle-checkpoint-deferred-progress-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/idle-checkpoint-deferred-progress.log
            .artifacts/cloudflare-hosted-e2e/direct-r2-presigned-put.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  temporal-orchestration:
    name: Temporal orchestration E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-temporal-orchestration-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run Temporal orchestration hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e temporal-orchestration --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/temporal-orchestration.log

      - name: Upload Temporal orchestration E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-temporal-orchestration-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/temporal-orchestration.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn

  telegram-delivery:
    name: Telegram delivery E2E
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    needs: runner-bundle
    services:
      postgres:
        image: public.ecr.aws/docker/library/postgres:17
        env:
          POSTGRES_DB: murph_test
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        options: >-
          --health-cmd "pg_isready -U postgres -d murph_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
        ports:
          - 5432:5432
    concurrency:
      group: cloudflare-hosted-e2e-telegram-${{ github.event.pull_request.number || github.ref || github.run_id }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: .nvmrc

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Install/check Temporal CLI
        run: pnpm temporal:cli:setup

      - name: Download runner bundle
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: hosted-local-runner-bundle

      - name: Unpack runner bundle
        run: |
          tar --zstd -xf runner-bundle.tar.zst
          rm runner-bundle.tar.zst

      - name: Run Telegram delivery hosted E2E
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .artifacts/cloudflare-hosted-e2e
          pnpm hosted-local e2e telegram --no-bundle 2>&1 \
            | tee .artifacts/cloudflare-hosted-e2e/telegram-delivery.log

      - name: Upload Telegram delivery E2E logs
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: cloudflare-hosted-e2e-telegram-delivery-logs
          path: |
            .artifacts/cloudflare-hosted-e2e/telegram-delivery.log
            .artifacts/hosted-local/**/state.json
          if-no-files-found: warn
