name: Agent mailbox transaction census

on:
  push:
    branches:
      - agent/mailbox-prepared-append
    paths:
      - .github/workflows/agent-mailbox-census.yml

permissions:
  contents: read

jobs:
  census:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Archive source worktree
        shell: bash
        run: |
          set -euo pipefail
          tar --exclude=.git --exclude=murph-source.tar.gz -czf murph-source.tar.gz .
      - name: Upload source worktree
        uses: actions/upload-artifact@v4
        with:
          name: murph-source
          path: murph-source.tar.gz
          if-no-files-found: error
          retention-days: 1
