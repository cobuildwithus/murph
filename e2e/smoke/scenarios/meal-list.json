{
  "id": "meal-list",
  "command": "vault-cli meal list --vault <path> [--from <date>] [--to <date>] [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/meals/meal-photo-placeholder.txt",
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers meal follow-up reads over a date range so imported meals are navigable without falling back to generic list/show."
}
