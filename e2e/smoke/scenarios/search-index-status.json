{
  "id": "search-index-status",
  "command": "vault-cli search index-status --vault <path> [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md",
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/search-index-status",
  "purpose": "Covers the documented SQLite search index status command without asserting runtime details that already live in package tests."
}
