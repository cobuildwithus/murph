{
  "id": "samples-import-csv",
  "command": "vault-cli samples import-csv <file> --vault <path> [--preset <id>] [--stream <stream>] [--ts-column <name>] [--value-column <name>] [--unit <unit>] [--delimiter <char>] [--metadata-columns <name> ...] [--source <source>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/samples/heart-rate.csv",
    "fixtures/sample-imports/samples/glucose.csv"
  ],
  "goldenOutput": "fixtures/golden-outputs/samples-import-csv",
  "purpose": "Covers CSV sample imports where transformId is an import-batch id, lookupIds point at sample records, and one or more sample ledgers are written."
}
