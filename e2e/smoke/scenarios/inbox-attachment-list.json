{
  "id": "inbox-attachment-list",
  "command": "vault-cli inbox attachment list <captureId> --vault <path> [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Documents listing stored attachments for one inbox capture from the runtime index."
}
