{
  "id": "meal-show",
  "command": "vault-cli meal show <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/meals/meal-photo-placeholder.txt",
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Covers meal follow-up reads where operators can resolve either the returned meal_* id or the queryable evt_* lookup id."
}
