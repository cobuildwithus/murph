{
  "id": "document-list",
  "command": "vault-cli document list --vault <path> [--from <date>] [--to <date>] [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/documents/visit-summary.md",
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers document follow-up reads where operators list imported document events over a date range without converting ids manually."
}
