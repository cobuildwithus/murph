{
  "id": "assistant-cron-disable",
  "command": "vault-cli assistant cron disable <job> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents temporarily disabling a scheduled assistant cron job without deleting it."
}
