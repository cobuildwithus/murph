{
  "id": "measurement-import-json",
  "command": "vault-cli measurement import-json --vault <path> --input @file.json|- [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/measurements/measurement.json"
  ],
  "goldenOutput": "fixtures/golden-outputs/measurement-add",
  "purpose": "Documents the structured measurement import path for nested metadata and grouped measurement payloads."
}
