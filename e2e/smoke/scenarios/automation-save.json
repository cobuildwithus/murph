{
  "id": "automation-save",
  "command": "vault-cli automation save <title> --vault <path> --instructions <text> --schedule-kind <kind> [--channel <channel>] [...]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/init",
  "purpose": "Documents the frozen command surface entry for release fixture coverage.",
  "coverageNotes": [
    "First-session prep reminders use automation save rather than experiment followup due.",
    "One-shot first-session prep reminders use scheduleKind=at with a concrete scheduleAt timestamp.",
    "First-session prep reminder slugs are stable so re-saving updates the same automation."
  ]
}
