{
  "id": "memory-upsert",
  "command": "vault-cli memory upsert <text> --vault <path> --section <section>",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Documents canonical memory writes into bank/memory.md."
}
