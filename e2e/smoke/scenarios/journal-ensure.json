{
  "id": "journal-ensure",
  "command": "vault-cli journal ensure <date> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/journal/2026/2026-03-10.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/journal-ensure",
  "purpose": "Covers journal bootstrap semantics where the command returns a queryable journal:<date> lookupId."
}
