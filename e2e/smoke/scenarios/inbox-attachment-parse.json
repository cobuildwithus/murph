{
  "id": "inbox-attachment-parse",
  "command": "vault-cli inbox attachment parse <attachmentId> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/journal-ensure",
  "purpose": "Documents single-attachment parser queue drains without processing the entire inbox queue."
}
