{
  "id": "vault-stats",
  "command": "vault-cli vault stats --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/ledger/events/README.md",
    "fixtures/minimal-vault/ledger/samples/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers read-only vault statistics so operators can summarize record counts and major ledger families without custom scripting."
}
