{
  "id": "event-import-jsonl",
  "command": "vault-cli event import-jsonl --vault <path> --input @file.jsonl|- [--conflict-policy supersede|reject] [--apply] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/events/events.jsonl"
  ],
  "goldenOutput": "fixtures/golden-outputs/event-import-jsonl",
  "purpose": "Documents the bulk canonical event import path: one JSONL line per import-json-shaped payload without explicit ids, externalRef as re-import identity, dry-run by default with --apply to write."
}
