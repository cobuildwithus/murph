{
  "id": "inbox-model-route",
  "command": "vault-cli inbox model route <captureId> --vault <path> --model <model> [--baseUrl <url>] [--apiKey <key>] [--apiKeyEnv <name>] [--providerName <name>] [--headersJson <json>] [--apply] [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/inbox-model",
  "purpose": "Documents the audited model-routing plan surface for one inbox capture without treating the external model backend as part of smoke execution."
}
