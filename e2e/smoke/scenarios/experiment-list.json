{
  "id": "experiment-list",
  "command": "vault-cli experiment list --vault <path> [--status <status>] [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/bank/experiments/baseline-observation.md",
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers experiment follow-up reads by status so the create command has a natural list surface."
}
