{
  "id": "assistant-cron-preset-list",
  "command": "vault-cli assistant cron preset list --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents listing the built-in assistant cron presets available for later installation."
}
