{
  "id": "vault-show",
  "command": "vault-cli vault show --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md",
    "fixtures/minimal-vault/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Covers explicit vault metadata reads so operators do not need the generic show command just to inspect vault.json and CORE state."
}
