{
  "id": "device-daemon-stop",
  "command": "vault-cli device daemon stop --vault <path> [--baseUrl <url>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/device-sync",
  "purpose": "Documents stopping the Murph-managed local device-sync daemon for one vault."
}
