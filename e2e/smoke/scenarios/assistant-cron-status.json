{
  "id": "assistant-cron-status",
  "command": "vault-cli assistant cron status --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents assistant scheduler counts plus the next upcoming cron run for one vault."
}
