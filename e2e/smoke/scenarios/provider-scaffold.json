{
  "id": "provider-scaffold",
  "command": "vault-cli provider scaffold --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/bank/providers/README.md",
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Documents provider payload scaffolding so operators can start bank/providers records from the CLI instead of hand-authoring frontmatter."
}
