{
  "id": "assistant-doctor",
  "command": "vault-cli assistant doctor --vault <path> [--repair] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents tolerant assistant runtime diagnostics for transcripts, receipts, outbox intents, and runtime automation state."
}
