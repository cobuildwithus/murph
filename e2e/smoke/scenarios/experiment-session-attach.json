{
  "id": "experiment-session-attach",
  "command": "vault-cli experiment session attach <lookup> <eventId> --vault <path> [--replace] [--allow-out-of-window] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/experiment-create",
  "purpose": "Documents the repair surface for attaching an existing intervention session event to an experiment."
}
