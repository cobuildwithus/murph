{
  "id": "samples-batch-list",
  "command": "vault-cli samples batch list --vault <path> [--stream <stream>] [--from <date>] [--to <date>] [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/samples/heart-rate.csv",
    "fixtures/minimal-vault/ledger/samples/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Covers import-batch inspection over xfm_* sample batches so operators can review ingestion history by stream and date."
}
