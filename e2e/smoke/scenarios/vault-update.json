{
  "id": "vault-update",
  "command": "vault-cli vault update --vault <path> [--title <title>] [--timezone <tz>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md",
    "fixtures/minimal-vault/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/journal-ensure",
  "purpose": "Documents explicit vault metadata updates so operators can keep CORE.md and vault.json aligned without using init or generic file edits."
}
