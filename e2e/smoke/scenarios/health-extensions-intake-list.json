{
  "id": "health-extensions-intake-list",
  "command": "vault-cli intake list --vault <path> [--date-from <date>] [--date-to <date>] [--cursor <cursor>] [--limit <n>] [--format json|md] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/health-extensions/vault-overlay/ledger/assessments/2026/2026-03.jsonl"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Documents noun-specific assessment listing through `intake list` with the health assessment ledger as the source of truth."
}
