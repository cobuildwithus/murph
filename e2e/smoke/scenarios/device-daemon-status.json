{
  "id": "device-daemon-status",
  "command": "vault-cli device daemon status --vault <path> [--baseUrl <url>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/device-sync",
  "purpose": "Documents inspecting the Murph-managed local device-sync daemon for one vault."
}
