{
  "id": "capture-manifest",
  "command": "vault-cli capture manifest <id-or-label> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/captures/captures.json"
  ],
  "goldenOutput": "fixtures/golden-outputs/capture-add",
  "purpose": "Documents immutable raw-manifest inspection for imported capture media using either event id or stable label."
}
