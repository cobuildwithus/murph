{
  "id": "inbox-promote-experiment-note",
  "command": "vault-cli inbox promote experiment-note <captureId> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md",
    "fixtures/minimal-vault/bank/experiments/baseline-observation.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/journal-ensure",
  "purpose": "Documents promoting a deterministic inbox capture into an idempotent experiment-note block when the vault resolves a single target experiment."
}
