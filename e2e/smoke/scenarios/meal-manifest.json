{
  "id": "meal-manifest",
  "command": "vault-cli meal manifest <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/meals/meal-photo-placeholder.txt",
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/meal-add",
  "purpose": "Covers immutable raw-manifest inspection for meal imports using either the returned meal id or the follow-up event lookup id."
}
