{
  "id": "measurement-manifest",
  "command": "vault-cli measurement manifest <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/measurements/measurement.json"
  ],
  "goldenOutput": "fixtures/golden-outputs/measurement-add",
  "purpose": "Documents immutable raw-manifest inspection for imported measurement events."
}
