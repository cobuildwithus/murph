{
  "id": "samples-show",
  "command": "vault-cli samples show <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/samples/glucose.csv",
    "fixtures/minimal-vault/ledger/samples/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Covers direct sample follow-up reads where lookup ids point at canonical sample records."
}
