{
  "id": "document-show",
  "command": "vault-cli document show <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/documents/visit-summary.md",
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/show",
  "purpose": "Covers document follow-up reads where operators can resolve either the returned doc_* id or the queryable evt_* lookup id."
}
