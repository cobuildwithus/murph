{
  "id": "assistant-cron-enable",
  "command": "vault-cli assistant cron enable <job> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents re-enabling a scheduled assistant cron job after it was disabled."
}
