{
  "id": "capture-import-json",
  "command": "vault-cli capture import-json --vault <path> --input @file.json|- [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/captures/captures.json"
  ],
  "goldenOutput": "fixtures/golden-outputs/capture-add",
  "purpose": "Documents the structured capture import path for batch media-capture payloads."
}
