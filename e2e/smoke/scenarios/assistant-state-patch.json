{
  "id": "assistant-state-patch",
  "command": "vault-cli assistant state patch <doc> --vault <path> --input @patch.json|-",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents incrementally patching an assistant scratch-state document."
}
