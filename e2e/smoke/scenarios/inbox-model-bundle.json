{
  "id": "inbox-model-bundle",
  "command": "vault-cli inbox model bundle <captureId> --vault <path> [--sensitive] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/inbox-model",
  "purpose": "Documents the audit-only normalized capture bundle emitted for one inbox capture."
}
