{
  "id": "assistant-cron-remove",
  "command": "vault-cli assistant cron remove <job> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents removing one assistant cron job from local scheduler state."
}
