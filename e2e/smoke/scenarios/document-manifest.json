{
  "id": "document-manifest",
  "command": "vault-cli document manifest <id> --vault <path> [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/documents/care-plan.txt",
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/document-import",
  "purpose": "Covers immutable raw-manifest inspection for imported documents using either the returned document id or the follow-up event lookup id."
}
