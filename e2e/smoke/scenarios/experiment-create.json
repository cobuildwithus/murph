{
  "id": "experiment-create",
  "command": "vault-cli experiment create <slug> --vault <path> [--title <title>] [--hypothesis <text>] [--started-on <date>] [--status <status>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/bank/experiments/baseline-observation.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/experiment-create",
  "purpose": "Covers experiment creation semantics where repeated requests may return created=false while preserving the same experimentId and lookupId."
}
