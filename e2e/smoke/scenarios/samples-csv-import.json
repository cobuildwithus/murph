{
  "id": "samples-csv-import",
  "command": "vault-cli samples csv import <file> --vault <path> [--preset <id>] [--stream <stream>] [--ts-column <name>] [--value-column <name>] [--unit <unit>] [--delimiter <char>] [--metadata-columns <name> ...] [--source <source>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/sample-imports/samples/heart-rate.csv",
    "fixtures/sample-imports/samples/glucose.csv"
  ],
  "goldenOutput": "fixtures/golden-outputs/samples-import-csv",
  "purpose": "Covers the composable CSV import spelling that shares the same import-batch output contract as the compatibility wrapper."
}
