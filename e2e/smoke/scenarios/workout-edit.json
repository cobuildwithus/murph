{
  "id": "workout-edit",
  "command": "vault-cli workout edit <id> --vault <path> [--input @patch.json] [--set <path=value> ...] [--clear <path> ...] [--day-key-policy keep|recompute] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/workout-add",
  "purpose": "Documents patching an existing workout event through the canonical edit lane."
}
