{
  "id": "assistant-cron-runs",
  "command": "vault-cli assistant cron runs <job> --vault <path> [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/CORE.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/assistant",
  "purpose": "Documents inspecting recent run history for one assistant cron job."
}
