{
  "id": "inbox-source-list",
  "command": "vault-cli inbox source list --vault <path> [--limit <n>] [--request-id <id>]",
  "vaultFixture": "fixtures/minimal-vault",
  "inputs": [
    "fixtures/minimal-vault/raw/README.md"
  ],
  "goldenOutput": "fixtures/golden-outputs/list",
  "purpose": "Documents listing configured inbox connectors from the local runtime config."
}
