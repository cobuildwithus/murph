# Resolve PR #1887 ReviewGPT round 3 findings

Status: active
Created: 2026-08-18
Updated: 2026-08-18

## Goal

- Preserve ordinary foreground auto-replies when retained delivery history
  exceeds the optional context window, while keeping filesystem work bounded
  and finishing PR #1887 through exact-head CI and ReviewGPT.

## Success criteria

- The production auto-reply path can start the provider and advance accepted
  input with 100 recent ordinary deliveries plus retained generated-image
  delivery evidence on the same route.
- Exact provider-message reply anchors remain available even when optional
  route history fills the bounded context window.
- Route-history reads canonical-validate at most 100 intents and never create a
  permanent retry-only overflow outcome.
- Focused Assistant Engine, state-cardinality, type, and diff-aware verification
  pass on the final authored head.
- The PR body is truthful, required CI is green, final ReviewGPT returns
  `ROUND_OUTCOME: PASS`, and the current base merges cleanly.

## Scope

- In scope: the existing Assistant Engine outbox route/provider-tag reader,
  its store and production-path tests, the PR body discrepancy, and the
  PR-specific verification/review loop.
- Out of scope: new state owners, schemas, services, queues, locks, retention
  policies, timing thresholds, or unrelated runtime cleanup.

## Constraints

- Technical constraints: delete the candidate-101 exception; preserve exact
  provider-anchor ambiguity detection; cap the combined canonical read at 100;
  keep projections rebuildable and optional rather than admission authority.
- Product/process constraints: preserve unrelated work, do not weaken the
  shared cardinality contract, keep the PR draft until review and relevant CI
  pass, and treat native-iOS failures as waived only when unrelated to this
  Assistant Engine change.

## Risks and mitigations

1. Risk: prioritizing route history can evict the exact provider anchor needed
   for a native reply.
   Mitigation: order bounded provider-tag candidates first, deduplicate, then
   fill the remaining window with newest route history and add a regression.
2. Risk: fixing the overflow with a larger scan weakens the cardinality gate.
   Mitigation: retain one 100-intent combined cap and rerun the production
   state-cardinality adapter.
3. Risk: current main's newer route-state work changes the owning path or makes
   the branch correction obsolete.
   Mitigation: compare both sides before editing and use a normal inspected
   reconciliation rather than preserving superseded branch logic.

## Tasks

1. Reproduce and trace ReviewGPT's candidate-101 failure through the current
   branch and compare the affected owner with current `origin/main`.
2. Implement the smallest bounded route/provider candidate ordering and replace
   the overflow-expectation coverage with focused production-path regressions.
3. Run focused Assistant Engine, cardinality, type, diff, whitespace, and
   privacy checks; inspect the complete final diff.
4. Commit and push the correction, update the PR body, and run exact-head CI
   with the next final ReviewGPT round.
5. Resolve actionable results until ReviewGPT passes and relevant CI is green,
   then perform parent final review and prove current-base mergeability.

## Decisions

- This is an internal reliability correction; no changelog item is expected
  unless the implementation broadens into a member-visible change.
- Coverage and sensitive final-review lenses apply. Frontend and prompt lenses
  do not apply. Product UX remains limited to preserving the already-defined
  foreground-reply recovery contract.
- ReviewGPT's round-3 finding reproduced against the real production path and
  current base. The candidate-101 exception blocked a valid route, and native
  reply resolution also supplied the incoming message ID instead of its exact
  provider reply target to the new priority query.
- Current main absorbed the branch's duplicate session-routing production
  owner. The inspected reconciliation retained the outbox projection, pending
  hint, and executable probes while reducing this PR's production-source diff.
- Provider-message lookup is route-scoped in the existing foreground-tag
  projection. Compound Linq groups reserve up to two candidates for every
  distinct exact anchor before optional route history fills the same 100-intent
  canonical-read cap.

## Progress

- Reconciled the branch with current `origin/main`, resolved all seven
  overlapping files, and pushed a mergeable exact-head candidate.
- Replaced auto-reply route overflow with a 100-intent optional-context window:
  up to two exact provider candidates lead, followed by newest route history.
- Propagated every authoritative Linq reply target into route-scoped
  provider-tag selection.
- Added store, ordinary production-pass, and older native-anchor regressions.
- Added cross-route provider-id collision, compound exact-anchor, distinct
  self-echo/history lookup, and bounded interrupted-rebuild-slot regressions.
- The first reconciled exact head passed the dedicated cardinality workflow and
  all but one Assistant Engine release test; that failure was the superseded
  single-read assertion now corrected here. ReviewGPT round 4 was stopped
  without a result once CI proved the reviewed head required a PR-specific
  change. The correction passes Assistant Engine typecheck, six focused
  production/store regressions, and the full opt-in cardinality suite (19/19).
  A new exact head, CI, and round 4 remain.

## Verification

- Commands to run: focused outbox/runtime Vitest cases, the Assistant Engine
  state-cardinality adapter and shared meter harness, affected typecheck or
  `pnpm test:diff`, `git diff --check`, exact-head GitHub Actions, the next
  ReviewGPT final round, and `git merge-tree --write-tree`.
- Expected outcomes: no overflow exception on valid retained history, exact
  anchor continuity, identical filesystem metrics at 128 and 256, no unresolved
  accepted review finding, green relevant CI, and a clean current-base merge.
