# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Plan | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Codex | Close the verified JSONL import-lock race and agent-session token exposure, and only touch overlapping hosted security-followup files if residual gaps remain after inspection | `agent-docs/exec-plans/active/2026-04-20-import-lock-and-agent-session-hardening.md` | `packages/core/src/mutations.ts`, focused `packages/core/test/**`, `apps/web/app/api/device-sync/agent/connections/[connectionId]/{export-token-bundle,refresh-token-bundle}/route.ts`, `apps/web/src/lib/device-sync/{agent-session-service.ts,agent-session-token-bundle.ts}`, focused `apps/web/test/{agent-session-service,prisma-store-agent-session}.test.ts`, overlapping dirty hosted security-followup files only if needed | `buildJsonlAppendPlan`, `importSamples`, `importDeviceBatch`, `HostedDeviceSyncAgentSessionService.exportTokenBundle`, `HostedDeviceSyncAgentSessionService.refreshTokenBundle`, agent-session bearer rotation/export shape | in_progress | Primary ownership is issue 1 and issue 3. Coordinate carefully with the existing `2026-04-20-thread-security-followups.md` row before editing hosted-run/email/smoke files that are already dirty in this worktree. Preserve unrelated dirty-tree edits. |
| Codex | Fix the three review-reported hosted execution correctness bugs around email access gating, device-sync wake coalescing, and stale non-idempotent outbox retries | `agent-docs/exec-plans/active/2026-04-20-hosted-ingress-outbox-fixes.md` | `apps/web/app/api/internal/hosted-execution/email/resolve-route/route.ts`, focused hosted email route tests, focused `apps/cloudflare` hosted email ingress tests, `apps/web/src/lib/hosted-ingress/{queue.ts,store-append.ts}`, `apps/web/test/hosted-ingress-queue.test.ts`, `packages/assistant-runtime/src/hosted-device-sync-runtime.ts`, `packages/assistant-runtime/test/hosted-device-sync-runtime.test.ts`, `packages/assistant-engine/src/assistant/outbox/{retry-policy.ts,dispatch-state.ts}`, `packages/assistant-engine/src/assistant/outbox.ts`, focused `packages/assistant-engine/test/**` | hosted email route entitlement gate, device-sync wake coalescing/merge semantics, stale non-idempotent outbox resend policy | in_progress | High-risk narrow fix lane from the current review only. Preserve overlapping dirty-tree hosted-run/onboarding/runtime work and avoid unrelated schema or protocol refactors unless a minimal safe fix is impossible without them. |
| Codex | Land the actionable security follow-ups from the exported ChatGPT thread: sanitize hosted-run logs, redact hosted-email routing logs, and stop printing smoke transcripts | `agent-docs/exec-plans/active/2026-04-20-thread-security-followups.md` | `apps/web/app/api/internal/hosted-run/log/route.ts`, `apps/web/src/lib/hosted-run/store.ts`, `apps/web/test/hosted-run-store.test.ts`, `packages/hosted-execution/src/{contracts.ts,parsers/run-control.ts}`, `packages/hosted-execution/test/hosted-wake-parsers.test.ts`, `apps/cloudflare/src/hosted-email/{routes.ts,worker-ingress.ts}`, focused hosted-email tests, `apps/cloudflare/scripts/runner-docker-smoke.ts`, `apps/cloudflare/src/hosted-runner-smoke-{child,contract}.ts`, focused smoke tests | hosted-run log request/token enforcement, hosted-run log sanitization, hosted-email log detail redaction, hosted runner smoke output | in_progress | Narrow security follow-up lane only. Do not broaden into device-sync bearer redesign or core import-lock architecture unless a minimal safe fix becomes obvious. Preserve unrelated dirty-tree edits and the active hosted onboarding / Cloudflare restoration work. |
| Codex | Remove the remaining durable DO bundle-ref/version cache so greenfield Cloudflare runs restore from web-owned snapshot refs only | `agent-docs/exec-plans/active/2026-04-20-cloudflare-greenfield-bundle-cache-removal.md` | `apps/cloudflare/src/user-runner/{runner-state-schema,runner-state-store,runner-state-helpers,runner-bundle-sync,types}.ts`, `apps/cloudflare/src/user-runner.ts`, focused `apps/cloudflare/test/**`, current-state docs if needed | DO-local `bundle_ref_json` / `bundle_version`, bundle restore/apply path, hosted-run snapshot ref handoff | in_progress | Greenfield removal lane only. Preserve overlapping dirty-tree Cloudflare/runtime work and do not reintroduce a renamed durable cache unless removal turns out to be impossible in the current architecture. |
| Codex | Rename `HostedRun.wakeIdsJson` to `ingressEventIdsJson` so the run-owned field stops using wake-shaped terminology |  | `apps/web/prisma/schema.prisma`, `apps/web/src/lib/hosted-run/store.ts`, `apps/web/test/hosted-run-store.test.ts` | `HostedRun.ingressEventIdsJson`, hosted-run store projection/read helpers | completed | Narrow hosted-run naming cleanup only. Landed without a migration; the underlying `wake_ids_json` column mapping stays unchanged. Focused hosted-web diff verification passed, while repo-wide `pnpm typecheck` remains blocked by an unrelated pre-existing syntax error in `apps/cloudflare/src/user-runner/runner-run-processor.ts`. |
| Codex | Rename the hosted runtime pre-web-commit phase from `committed` to `prepared` so the run lifecycle names match the actual ownership boundary |  | `packages/assistant-runtime/src/{hosted-runtime,models}.ts`, `packages/assistant-runtime/test/hosted-runtime-runner.test.ts`, `apps/cloudflare/src/{node-runner-isolated,user-runner/runner-run-processor}.ts`, directly coupled Cloudflare worker/node-runner tests only if required | `HostedAssistantRuntimeJobPreparedResult`, hosted runtime pre-web-commit phase discriminant, prepared-snapshot guards | completed | Working-tree rename landed across assistant-runtime and the active Cloudflare split files, with focused `assistant-runtime` and Cloudflare node-runner tests passing. No scoped commit was created because the tracked Cloudflare processor/tests are being replaced by overlapping dirty-tree add/delete work, so committing only this rename would not have represented the real current consumer path. |
| Codex | Make hosted run-drain commits always take the explicit finalize path instead of inferring finalize only from assistant-delivery effects |  | `apps/cloudflare/src/user-runner/runner-wake-processor.ts`, focused `apps/cloudflare/test/runner-wake-processor.test.ts` | `RunnerWakeProcessor.executeRunDrain`, `finalizeRequired` | completed | Committed-phase run drains now always request explicit finalize, and focused Cloudflare proof passed (`runner-wake-processor` Vitest slice plus repo `pnpm typecheck`). The diff-aware `apps/cloudflare verify` wrapper still hit unrelated lock/process issues in this dirty tree after the relevant Node lane passed. |
| Codex | Restore the focused Cloudflare hosted-local e2e lanes after the run-centric hard cut removed the files but left the public commands and CI contract alive | `agent-docs/exec-plans/active/2026-04-20-cloudflare-e2e-hard-cut-restoration.md` | `apps/cloudflare/package.json`, `apps/cloudflare/scripts/run-hosted-local-e2e.ts`, `apps/cloudflare/test/hosted-local-*.test.ts`, `apps/cloudflare/test/helpers/hosted-local-*.ts` | hosted-local Linq/Telegram/duplicate-commit e2e commands, `runtime.timer` duplicate-commit coverage | in_progress | Narrow Cloudflare test restoration lane only. Preserve overlapping dirty-tree Cloudflare runtime work and keep the duplicate-commit lane aligned with the run-centric protocol rather than reintroducing `assistant.cron.tick`. |
| Codex | Remove narrow wake-shaped residue in Cloudflare `user-runner` where the write scope stays local to run-drain cleanup |  | `apps/cloudflare/src/user-runner.ts`, directly coupled `apps/cloudflare/test/**` only if required | `cleanupCommittedHostedWakesLocally`, local run-drain wake residue, run-centric helper naming | completed | Narrow Cloudflare cleanup landed in `user-runner.ts` only. Focused verification was attempted, but broader `apps/cloudflare` checks remain blocked by pre-existing `node-runner` type drift and the current dirty-tree finalize-release seam in `user-runner` tests. |
| Codex | Fix the Cloudflare-side hosted-run finalize ownership bypass and make `nudgeHostedRun` a real immediate-alarm seam |  | `apps/cloudflare/src/{user-runner,index,hosted-email/worker-ingress}.ts`, focused `apps/cloudflare/test/{user-runner-resume-finalize,index,hosted-email-worker-ingress}.test.ts` | `prepareAndCommitAcquiredHostedRun`, `finalizeAcquiredHostedRun`, `nudgeHostedRun`, `handleRunRoute`, hosted email post-ingress nudge | completed | Cloudflare now reacquires before finalize, releases retryable finalize failures back to web-owned recovery, and `nudgeHostedRun` remains the immediate-alarm seam. Focused finalize/nudge verification passed; broader `apps/cloudflare` typecheck is still blocked by pre-existing `test/node-runner.test.ts` runtime-timer typing drift. |
| Codex | Remove live `assistant.cron.tick` support from shared hosted-execution parsing and source-of-truth docs | `agent-docs/exec-plans/active/2026-04-20-remove-hosted-cron-tick.md` | `agent-docs/references/hosted-run-protocol.md`, `docs/hosted-hard-cut-migration-guide.md`, `apps/web/src/lib/hosted-ingress/queue.ts`, `apps/web/test/hosted-ingress-queue.test.ts`, `packages/hosted-execution/src/parsers.ts`, directly coupled `packages/hosted-execution/test/**` | `parseHostedRuntimeEvent`, hosted ingress rejection wording, hosted run protocol cron-tick wording | in_progress | Narrow shared-contract/docs lane only. Preserve overlapping Cloudflare hosted-run work and do not edit immutable completed plans even though they still mention historical `assistant.cron.tick` state. |
| Codex | Fence resumable hosted-run finalize acquisition so overlapping acquires cannot stomp the finalize token | `agent-docs/exec-plans/active/2026-04-20-hosted-run-resume-finalize-fencing.md` | `apps/web/src/lib/hosted-run/store.ts`, focused `apps/web` hosted-run tests, directly coupled shared hosted-run status contract/parsers only if required | `acquireHostedRunTx`, `finalizeHostedRunTx`, resumable finalize claim/recovery state | completed | Web-owned finalize recovery is fenced in two stages now: resumable acquires claim `committed_needs_finalize -> finalizing`, and retryable finalize failures can release `finalizing -> committed_needs_finalize` through the new release-finalize contract/route. Focused `apps/web` store verification and shared contract typecheck/tests passed. |
| Codex | Verify and fix issue 3 plus the assistant-runtime side of issue 6 by removing downstream run-drain residue, narrowing wake resolution inputs, and extracting assistant-runtime env category maps | `agent-docs/exec-plans/active/2026-04-20-assistant-runtime-rundrain-cleanup.md` | `packages/assistant-runtime/src/hosted-runtime.ts`, `packages/assistant-runtime/src/hosted-runtime/{execution,models,utils}.ts`, directly coupled `packages/assistant-runtime/test/**` only if required | parser-only `runDrain` validation, `resolveHostedWake`, `buildHostedRuntimeStartDetails` env categories | in_progress | Narrow assistant-runtime cleanup lane only. Overlaps existing naming-only and run-drain-budget rows in the same files; preserve those edits and avoid unrelated behavior or Cloudflare changes. |
| Codex | Add coverage proofs for new assistant-runtime hosted logging instrumentation |  | `packages/assistant-runtime/test/**` | hosted runtime execution, typing, and callback log assertions | in_progress | Narrow test-only lane. Do not touch production files or overlap the hosted runtime behavior patches. |
| Codex | Fix inbox attachment parsing/inspection clarity plus explicit recurring-food unschedule path |  | `packages/inboxd/**`, `packages/assistant-engine/src/inbox-*`, `packages/assistant-engine/test/**`, `packages/cli/src/commands/{food,inbox}.ts`, `packages/cli/test/**`, `docs/contracts/03-command-surface.md` | `processCapture`, `enqueueAttachmentParseJobs`, inbox attachment commands, `registerFoodCommands` | in_progress | Cross-package operator/runtime lane. Avoid touching assistant CLI bootstrap refresh work owned separately by this task exclusion. |
| Codex | Land supplied hosted final-shape cleanup patch for hosted execution/webhook dispatch seams |  | `apps/cloudflare/src/{dispatch-payload-store,user-runner}.ts`, `apps/web/src/lib/hosted-execution/{outbox,outbox-payload}.ts`, `apps/web/src/lib/hosted-onboarding/{webhook-dispatch-payload,webhook-receipt-store}.ts` | `HostedDispatchPayloadStore`, `HostedUserRunner`, hosted execution outbox payload helpers, hosted webhook dispatch payload helpers | completed | Narrow supplied patch landing. Avoid unrelated hosted web or Cloudflare edits, and preserve the pre-existing `apps/web/prisma.config.ts` modification. |
| Codex | Suppress phone-only Privy restart copy when an alternate hosted auth method is active |  | `apps/web/src/components/hosted-onboarding/{hosted-auth-panel,hosted-phone-auth,hosted-phone-auth-controller}.tsx`, `apps/web/test/{hosted-auth-panel,hosted-phone-auth}.test.ts` | `HostedAuthPanel`, `HostedPhoneAuth`, `useHostedPhoneAuthController` | completed | Narrow apps/web UX fix for Telegram/email auth. Preserve existing Privy server identity rules and avoid unrelated onboarding copy changes. |
| Codex | Reconcile hosted Privy auth completion so email, Telegram, and SMS share one client-session contract and SMS wallet provisioning uses fresh user state | `agent-docs/exec-plans/active/2026-04-16-phone-wallet-refresh.md` | `apps/web/src/components/hosted-onboarding/{hosted-auth-completion,hosted-email-auth-button,hosted-telegram-auth-button,hosted-phone-auth-controller,hosted-phone-auth-support}.ts*`, `apps/web/test/{hosted-phone-auth,homepage-privy-auth}.test.ts` | `completeHostedPrivyAuth`, `HostedPrivyClientSessionInput`, `useHostedPhoneAuthController`, `finalizeHostedPrivyVerification` | in_progress | Narrow hosted onboarding auth-architecture cleanup. Avoid unrelated onboarding copy, routing, and server-side identity changes. |
| Codex | Fix hosted assistant rapid-turn continuity by bundling session indexes, hydrating hosted default targets before first-contact delivery, and resolving direct-thread restores across rapid turns |  | `packages/runtime-state/src/assistant-local-state-descriptors.ts`, `packages/runtime-state/test/hosted-bundle.test.ts`, `packages/runtime-state/README.md`, `packages/assistant-engine/src/assistant/{conversation-policy,execution-context,first-contact-welcome-delivery,local-service,notification-turn,store,turn-plan}.ts`, `packages/assistant-engine/src/assistant/store/{paths,persistence}.ts`, `packages/assistant-engine/test/**`, `packages/assistant-runtime/src/hosted-runtime/{context,events,execution}.ts`, `packages/assistant-runtime/test/**`, `apps/cloudflare/test/hosted-local-*.test.ts` | `assistantLocalStateDescriptors`, `readAssistantIndexStore`, `resolveAssistantExecutionOperatorDefaults`, `resolveAssistantSession`, `buildAssistantConversationPolicy`, `hydrateHostedExecutionDefaultTarget`, hosted local rapid-turn E2E | completed | Hosted assistant continuity lane. Root cause was same-activation hosted dispatches entering first-contact delivery before hosted default-target bootstrap completed; keep restore fallback for older bundles and preserve local behavior outside hosted execution. |
| Codex | Make hosted invite-status polling single-flight so slow refreshes stop overlapping and amplifying DB load |  | `apps/web/src/components/hosted-onboarding/invite-status-client.ts`, focused `apps/web/test/join-invite-status-client.test.ts`, `apps/web/test/route-loading.test.tsx` | `useHostedInviteStatusRefresh`, `fetchHostedInviteStatus` | completed | Narrow polling-only lane plus stale loading-test cleanup so scoped verification is not blocked by the removed join loading route. Do not change auth, invite-service, or broader onboarding flow semantics in this slice. |
| Codex | Restore hosted web Vercel builds by committing the Prisma client link helper referenced by the build script |  | `apps/web/scripts/ensure-prisma-client-link.ts` | `ensureHostedWebPrismaClientLink`, `resolveHostedWebPrismaClientLinkPaths` | in_progress | Narrow deploy-fix lane. Do not touch unrelated hosted onboarding, release, or webhook work. |
| Codex | Fix apps/web hosted hard-cut test fallout from dispatch-boundary removal |  | `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts`, directly related hosted wake web tests/helpers only | hosted onboarding webhook idempotency, hosted wake test fixtures/builders | in_progress | Test-only apps/web lane for wake-first hard-cut fallout. Do not touch packages/assistant-runtime, packages/hosted-execution, or apps/cloudflare. Preserve unrelated apps/web edits already in flight. |
| Codex | Add a stable Privy logo asset under hosted web public files |  | `apps/web/public/privy-logo.png` | static hosted logo asset | completed | Narrow asset-only lane for Privy login branding. Avoid overlapping existing homepage/onboarding work in `apps/web/src/**`. |
| Codex | Ignore generated `.next-smoke*` directories in review-gpt and repo hygiene tooling so hosted smoke residue does not break audit packaging |  | `scripts/check-no-js.ts`, `scripts/check-no-js.test.ts`, `scripts/verify-workspace-boundaries.mjs` | `shouldSkipSourceArtifactDirectory`, `isBlockedTrackedArtifactPath`, `isWorkspaceBuildArtifactPath`, `shouldSkipDirectory` | completed | Narrow tooling-only lane. Avoid unrelated hosted app/runtime changes and preserve pre-existing `apps/web/**` worktree edits. |
| Codex | Delete unused review-gpt preset prompt files for incentives and grief vectors |  | `scripts/chatgpt-review-presets/{grief-vectors,incentives}.md` | review-gpt preset prompt files | completed | Narrow docs/tooling cleanup lane. Avoid changing preset registry behavior or other prompt files. |
| Codex | Update review-gpt preset prompts to stop requesting returned patch attachments |  | `scripts/review-gpt-delay.sh`, `scripts/chatgpt-review-presets/*.md` | delayed review-gpt follow-up prompt defaults, preset final response contracts | completed | Narrow review-gpt tooling/doc wording lane. Avoid changing preset registry behavior or unrelated review-gpt runtime flows. |
| Codex | Finish hosted Linq active-member receipt cutover and collapse hosted conversation wake capture-builder re-entry |  | `apps/web/src/lib/hosted-onboarding/{webhook-service,webhook-provider-linq,webhook-transport}.ts`, `apps/web/test/hosted-onboarding-linq*.test.ts`, `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts`, `packages/assistant-runtime/src/hosted-runtime/events/{conversation,email,linq,telegram}.ts`, `packages/assistant-runtime/test/**` | `handleHostedOnboardingLinqWebhook`, active-member Linq direct planning/drain, `ingestHostedConversationMessageWake` | in_progress | Cross-slice hosted hard-cut lane. Preserve unrelated hosted onboarding auth/pricing work in `apps/web` and avoid Cloudflare/bundle recovery changes reserved for later discussion. |
| Codex | Remove webhook-shaped runtime parsing from hosted conversation.message Linq ingestion | `agent-docs/exec-plans/active/2026-04-18-hosted-conversation-wake-cleanup.md` | `packages/assistant-runtime/src/hosted-runtime/events/**`, `packages/inboxd/src/connectors/linq/**`, focused `packages/{assistant-runtime,inboxd}/test/**` | hosted conversation wake Linq adapter, `ingestHostedConversationMessageWake` | in_progress | Narrow package-only lane. Do not touch `apps/web/**` or `apps/cloudflare/**`; preserve the broader hosted Linq/webhook lane already in flight. |
| Codex | Finish the remaining local wake-to-run naming hard cut in Cloudflare and assistant-runtime internals | `agent-docs/exec-plans/active/2026-04-20-wake-to-run-naming-hard-cut.md` | `apps/cloudflare/src/user-runner.ts`, `apps/cloudflare/src/user-runner/{runner-run-processor,runner-runtime-alarm-scheduler}.ts`, directly coupled Cloudflare tests only if required, `packages/assistant-runtime/src/hosted-runtime{,.ts}/{callbacks,context,events,execution,models,typing}.ts`, directly coupled assistant-runtime tests only if required | `RunnerRunProcessor`, `RunnerRuntimeAlarmScheduler`, `HostedIngressEffect`, `HostedIngressExecutionMetrics`, `HostedIngressLane`, `startHostedRunTypingIndicator`, `resolveHostedRunExecutionContext` | in_progress | Narrow naming-only hard cut. Keep behavior unchanged, preserve overlapping hosted-wake/run work, and do not broaden into protocol or schema changes. |
| Codex | Replace weak `{}` assistant-delivery test stub types with the real hosted delivery record type |  | `packages/assistant-runtime/test/{hosted-runtime-environment,hosted-runtime-entry-execution,hosted-runtime-execution,hosted-email-subject,hosted-runtime-email-event}.test.ts` | `writeAssistantDeliveryRecord`, hosted runtime effects-port test stubs | completed | Narrow assistant-runtime test-only cleanup. Avoid production runtime changes and preserve unrelated in-flight hosted/runtime work. |
| Codex | Prove the hosted-local Linq later-reply drain path persists the follow-up assistant turn after inbound message wake processing |  | `apps/cloudflare/test/user-runner.test.ts`, targeted hosted-local Linq e2e verification only | `HostedUserRunner.wakeHostedWakesInternal`, hosted-local Linq later-reply regression proof | completed | Added a focused regression test for between-round assistant wake rematerialization and verified fresh hosted-local Linq e2e reruns now persist the second session turn/outbox intent. No further runtime edits in this lane. |
| Codex | Replace hosted wake drain RPC semantics with a fire-and-forget nudge contract while preserving DO-local drain, alarm, and repair correctness |  | `apps/web/src/lib/hosted-{wake/control,onboarding/webhook-service-wake}.ts`, `apps/web/app/api/internal/hosted-wake/repair/route.ts`, `apps/web/app/api/hosted-onboarding/linq/webhook/route.ts`, `apps/cloudflare/src/{index,hosted-email/worker-ingress,worker-routes/shared,user-runner}.ts`, `packages/{hosted-execution,cloudflare-hosted-control}/src/**`, focused hosted wake tests under `apps/{web,cloudflare}/test/**` and package tests | `triggerHostedWakeUser*`, `maybeHandoffHostedExecutionWebhookWake`, `/internal/users/:userId/wake`, `UserRunnerDurableObject.wakeHostedWakes`, hosted wake nudge result contracts | completed | Cross-surface hosted wake control refactor. Focused `apps/web` and `apps/cloudflare` typecheck/test slices passed; preserve unrelated dirty `apps/cloudflare/src/container-entrypoint.ts` and its tests plus other hosted-wake lanes still in flight. |
| Codex | Drain hosted runtime-local assistant and device-sync follow-up work in-process until idle or bounded budget before snapshot | `agent-docs/exec-plans/active/2026-04-20-hosted-run-drain-budget.md` | `packages/assistant-runtime/src/hosted-runtime/execution.ts`, direct `packages/assistant-runtime/test/**` coverage for run-drain execution only | `executeHostedRunDrainForCommit`, hosted immediate maintenance drain budget, assistant/device-sync due-state draining | in_progress | Narrow assistant-runtime execution lane only. Do not broaden into Cloudflare/web acquire-commit flow or reintroduce any web-visible internal wake materialization. Preserve overlapping in-flight assistant-runtime test/runtime edits outside the direct run-drain coverage surface. |
