# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Plan | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Codex | Align hosted URL normalization so build-time and runtime accept the same explicit public-base values and IPv6 loopback forms without widening beyond the shared hosted-execution/public-url/auth seam | `agent-docs/exec-plans/active/2026-04-23-hosted-url-normalization-alignment.md` | `packages/hosted-execution/src/env.ts`, `apps/web/{src/lib/hosted-web/public-url.ts,test/{public-url.test.ts,env.test.ts,hosted-onboarding-landing.test.ts}}`, `apps/cloudflare/{src/{auth-adapter.ts,hosted-execution-worker-env.ts},test/{env.test.ts,auth-adapter.test.ts}}`, `agent-docs/exec-plans/active/{2026-04-23-hosted-url-normalization-alignment.md,COORDINATION_LEDGER.md}` | shared hosted base-URL normalization, scheme-less explicit URL acceptance, IPv6 loopback hostname handling, Cloudflare dev JWKS override validation | in_progress | Narrow high-risk hosted URL-contract lane. Overlaps the active hosted-auth-binding row only on `apps/web/src/lib/hosted-web/public-url.ts`; keep that overlap minimal and additive, preserve the dirty tree, and do not widen into onboarding flow or broader runner env changes. |
| Codex | Align the documented command-taxonomy section with the shared contracts registry and make tolerant frontmatter parsing actually tolerate malformed quoted scalars without widening beyond the contracts/doc/test seam | `agent-docs/exec-plans/active/2026-04-23-contract-taxonomy-frontmatter-tolerance.md` | `docs/contracts/03-command-surface.md`, `packages/contracts/src/{command-capabilities.ts,frontmatter.ts}`, directly coupled `packages/contracts/test/**`, `agent-docs/exec-plans/active/{2026-04-23-contract-taxonomy-frontmatter-tolerance.md,COORDINATION_LEDGER.md}` | command taxonomy bundles/nouns/aliases, tolerant quoted-scalar frontmatter fallback | completed | Narrow `packages/contracts` lane completed. Focused contracts Vitest proof and `pnpm test:smoke` are green; broader `pnpm typecheck`, `test:diff`, package typecheck, and contracts artifact verification are still red for unrelated pre-existing `packages/core`, `packages/assistantd`, `packages/contracts/test/schema-catalog-examples.test.ts`, and missing `generated/frontmatter-automation.schema.json` issues. No scoped commit was created because the shared ledger file already carried unrelated concurrent churn, so `scripts/finish-task` would have absorbed other active rows. |
| Codex | Preserve specialized event evidence across rewrites and stop standalone attachment cleanup from deleting shared raw directories without widening beyond the `packages/core` event/attachment seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-event-evidence-retention.md` | `packages/core/src/{domains/events.ts,event-attachments.ts}`, directly coupled `packages/core/test/{workout-primitives,event-attachments,core-event-thresholds}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-event-evidence-retention.md,COORDINATION_LEDGER.md}` | specialized attachment-backed event rehydration, retained evidence paths, standalone attachment cleanup exact-path deletion | in_progress | High-risk storage seam in a dirty tree. Keep the diff narrow to `packages/core`, preserve unrelated edits, and do not widen into generic event upsert semantics or broader raw-owner redesign. |
| Codex | Restore raw-manifest retry idempotency and explicit history-event id uniqueness without widening beyond the `packages/core` raw/history seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-core-raw-manifest-and-history-idempotency.md` | `packages/core/src/{domains/events.ts,event-attachments.ts,raw.ts,operations/raw-manifests.ts,history/api.ts,vault.ts}`, directly coupled `packages/core/test/{operations-thresholds,event-attachments,workout-primitives,core,core-event-thresholds,health-history-family}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-core-raw-manifest-and-history-idempotency.md,COORDINATION_LEDGER.md}` | immutable raw manifest naming, manifest directory validation, attachment retry idempotency, duplicate explicit history id rejection | in_progress | Narrow `packages/core` storage/reliability lane in a dirty tree. Overlaps the active event-evidence row on `domains/events.ts` and `event-attachments.ts`; keep this additive, preserve unrelated edits, and do not widen into generic event upsert semantics, shared raw-owner layout redesign, or a new history update API. |
| Codex | Decouple `WriteBatch.commit()` rollback from metadata persist failures without widening beyond the narrow rollback-status seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-write-batch-rollback-persist-failure.md` | `packages/core/src/operations/write-batch.ts`, directly coupled `packages/core/test/core.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-write-batch-rollback-persist-failure.md,COORDINATION_LEDGER.md}` | `WriteBatch.commit`, rollback-after-persist-failure handling, post-mutation persist regression coverage | in_progress | Narrow high-risk `packages/core` reliability lane. Overlaps the active core lock/write-batch refactor row on `packages/core/src/operations/write-batch.ts`; keep this additive on the current clean commit/rollback path, avoid `write-policy.ts` or lock-order work, and keep test coverage in `core.test.ts` rather than the other row's `operations-thresholds` lane. |
| Codex | Harden write-batch rollback journaling and case-insensitive reserved-path enforcement without widening beyond the reported `packages/core` integrity gaps | `agent-docs/exec-plans/active/2026-04-23-core-write-batch-rollback-and-path-case-hardening.md` | `packages/core/src/{operations/write-batch.ts,operations/canonical-resource-lock.ts,path-safety.ts,write-policy.ts}`, directly coupled `packages/core/test/{core.test.ts,core-utilities.test.ts,canonical-resource-lock.test.ts,operations-thresholds.test.ts}`, `agent-docs/exec-plans/active/{2026-04-23-core-write-batch-rollback-and-path-case-hardening.md,COORDINATION_LEDGER.md}` | pre-mutation rollback prerequisites, case-insensitive reserved roots, protected canonical path checks, canonical path lock-key normalization | in_progress | Narrow high-risk `packages/core` durability/path-safety lane. Overlaps the active core lock/write-batch refactor row on `write-batch.ts`, `canonical-resource-lock.ts`, and `write-policy.ts`; keep this row limited to the reported rollback-window and case-insensitive alias defects, avoid helper-extraction churn, and stop if overlapping dirty edits appear in those files. Focused `packages/core` proof and package-local typecheck are green, `pnpm test:smoke` is green, scoped `test:diff` is currently blocked by unrelated `packages/assistantd` typecheck failures, package-local coverage is currently blocked by unrelated existing `event-attachments` failures/threshold debt, and required spawned audit passes are pending because the environment hit a subagent usage limit. |
| Codex | Clean up low-risk `@murphai/contracts` owner drift so preferences and automation literals have one canonical owner and the package treats the root barrel as the primary JS API without widening beyond directly coupled contracts/Health Commons seams |  | `packages/contracts/{package.json,README.md,scripts/verify.ts,src/{automation,constants,index,preferences,vault-families}.ts,test/public-entrypoints.test.ts}`, directly coupled `apps/web/src/lib/health-commons/{catalog,biomarker-detail,experiment-detail}.ts`, `apps/web/test/health-commons-biomarker-detail-page.test.ts`, `packages/health-commons/vitest.config.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | deprecated preferences alias ownership, automation doc-type literal owner, contracts root-barrel package surface | completed | Scoped commit `f21278441777` landed the owner cleanup. Shared-file and overlapping-contracts churn remains in the working tree on `COORDINATION_LEDGER.md`, `packages/contracts/test/public-entrypoints.test.ts`, and the active schema-metadata/schema-catalog lanes, so those edits were left out of the commit on purpose. |
| Codex | Support WHOOP body measurements for real, keep the body scope least-privilege, and avoid widening beyond the WHOOP provider/importer descriptor + directly coupled proof/docs slice | `agent-docs/exec-plans/active/2026-04-23-whoop-body-measurement-support.md` | `packages/device-syncd/src/providers/whoop.ts`, `packages/device-syncd/test/{whoop-provider,provider-descriptor-integration}.test.ts`, `packages/importers/src/device-providers/{provider-descriptors,whoop}.ts`, `packages/importers/test/device-providers.test.ts`, `docs/device-provider-compatibility-matrix.md`, `agent-docs/exec-plans/active/{2026-04-23-whoop-body-measurement-support.md,COORDINATION_LEDGER.md}` | WHOOP body-measurement scope override, scheduled body snapshot fetch, body observation normalization | in_progress | Narrow WHOOP privacy/normalization lane. Overlaps the active WHOOP webhook/refresh rows on `packages/device-syncd/src/providers/whoop.ts`; keep edits additive, preserve unrelated dirty-tree work, and do not widen into Oura/Garmin/hosted device-sync changes. |
| Codex | Align WHOOP/importer activity metrics with the canonical/query seam so WHOOP energy and workout metrics stop dropping on import/read without widening beyond the shared wearables metric mapping slice | `agent-docs/exec-plans/active/2026-04-23-whoop-wearable-metric-alignment.md` | `packages/importers/src/device-providers/{metric-catalog.ts,canonical-wearable-records.ts}`, directly coupled `packages/importers/test/**`, `packages/query/src/{wearables.ts,wearables/{candidates,types}.ts,browser-replica/{build,shared,views}.ts}`, directly coupled `packages/query/test/**`, `docs/device-provider-compatibility-matrix.md`, `agent-docs/exec-plans/active/{2026-04-23-whoop-wearable-metric-alignment.md,COORDINATION_LEDGER.md}` | WHOOP `energy-burned` to `activeCalories` conversion, canonical wearable metric key coverage, activity metric query surfacing, dropped workout metric preservation | in_progress | Narrow wearables importer/query lane only. Preserve unrelated dirty-tree changes, especially the active browser-replica split row, and keep the fix scoped to the cited metric-alignment seam rather than a broad wearable redesign. |
| Codex | Prevent WHOOP `*.updated` webhook follow-up 404s from tombstoning live records while preserving explicit delete events | `agent-docs/exec-plans/completed/2026-04-23-whoop-update-404-tombstone.md` | `packages/device-syncd/src/providers/whoop.ts`, `packages/device-syncd/test/whoop-provider.test.ts`, `agent-docs/exec-plans/{completed/2026-04-23-whoop-update-404-tombstone.md,active/COORDINATION_LEDGER.md}` | WHOOP resource-job missing-fetch handling, explicit delete snapshot boundary, updated-vs-deleted webhook semantics | completed | Narrow `packages/device-syncd` webhook/reliability lane completed. Focused WHOOP regression proof plus package-local typecheck and coverage are green, required `coverage-write` and `task-finish-review` audits found no blockers, and no scoped commit was created because the same WHOOP files already carry unrelated in-progress refresh-token-rotation edits and the shared ledger file has unrelated concurrent churn. |
| Codex | Hard-cut `LINQ_WEBHOOK_SECRET` out of Linq-enabled hosted execution so the webhook HMAC secret stays ingress-only without widening beyond the hosted env-policy/runtime-boundary seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-linq-webhook-secret-boundary.md` | `packages/assistant-runtime/src/{hosted-env-categories.ts,hosted-runtime/environment.ts,hosted-runtime/context.ts}`, `apps/cloudflare/src/hosted-env-policy.ts`, focused `apps/cloudflare/test/runner-env.test.ts`, optional focused `packages/assistant-runtime/test/hosted-runtime-environment.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-linq-webhook-secret-boundary.md,COORDINATION_LEDGER.md}` | Linq webhook secret trust boundary, hosted runner env forwarding, child runtime env denylist | in_progress | High-risk secret-boundary lane. Preserve overlapping dirty-tree edits in `hosted-runtime/environment.ts` and the active hosted child-env/runner rows; keep Linq API token/base URL forwarding intact and do not widen into hosted onboarding or outbound Linq redesign. |
| Codex | Backstop hosted AI usage idempotency so one logical hosted turn attempt cannot persist or meter twice when a producer emits multiple `usageId` values, while keeping malformed pending usage files from stalling later valid exports | `agent-docs/exec-plans/active/2026-04-23-hosted-ai-usage-idempotency.md` | `apps/web/src/lib/hosted-execution/usage.ts`, `apps/web/prisma/{schema.prisma,migrations/**}`, `packages/runtime-state/src/assistant-usage.ts`, `packages/assistant-runtime/src/hosted-runtime/usage.ts`, directly coupled `apps/web/test/**`, `packages/runtime-state/test/**`, and `packages/assistant-runtime/test/**`, `agent-docs/exec-plans/active/{2026-04-23-hosted-ai-usage-idempotency.md,COORDINATION_LEDGER.md}` | hosted usage logical uniqueness, canonical assistant usage id derivation, duplicate metering backstop, malformed pending-usage isolation | in_progress | High-risk billing/storage seam. Preserve overlapping dirty edits in `usage.ts`, `schema.prisma`, and `assistant-usage.ts`; coordinate carefully with the active hosted Stripe and assistant-provider rows and keep this change limited to the duplicate-logical-record path plus the directly coupled hosted-export resilience fix. |
| Codex | Retain the hosted runner pending cleanup sidecar when Telegram cleanup cannot be confirmed so transient provider/env failures do not erase finalize retry inputs | `agent-docs/exec-plans/completed/2026-04-23-runner-telegram-cleanup-retry-state.md` | `apps/cloudflare/src/user-runner/{runner-cleanup.ts,runner-run-processor.ts,runner-state-store.ts,types.ts}`, focused `apps/cloudflare/test/{runner-run-processor,runner-state-store.bundle-slots}.test.ts`, `agent-docs/exec-plans/{completed/2026-04-23-runner-telegram-cleanup-retry-state.md,active/COORDINATION_LEDGER.md}` | pending cleanup sidecar retention, Telegram cleanup failure handling, finalize retry inputs | completed | Telegram cleanup failures now retain durable retry inputs, including outcome-derived Telegram cleanup refs, and focused direct proof covers later clear-on-success retry behavior. Required `coverage-write` found no gap; `task-finish-review` found and closed the missing outcome-persistence hole. No scoped commit was created because the shared Cloudflare test file, the untracked adjacent `runner-cleanup.ts` helper, and the ledger file all carry overlapping active dirty-tree work. |
| Codex | Retain the hosted runner pending cleanup sidecar when Linq cleanup cannot be confirmed so transient provider/env failures do not erase finalize retry inputs | `agent-docs/exec-plans/active/2026-04-23-runner-linq-cleanup-retry-state.md` | `apps/cloudflare/src/user-runner/{runner-cleanup.ts,runner-run-processor.ts}`, focused `apps/cloudflare/test/runner-run-processor.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-runner-linq-cleanup-retry-state.md,COORDINATION_LEDGER.md}` | pending cleanup sidecar retention, Linq cleanup failure handling, finalize retry inputs | completed | Focused Linq regression proof is green and the dirty-tree cleanup implementation already retains the sidecar on Linq cleanup failures. No scoped commit was created because the shared ledger file and `runner-run-processor.test.ts` both carry overlapping unrelated dirty-tree edits that would have been absorbed by a path-level commit. |
| Codex | Land the unclaimed Linq cleanup, retry, webhook-freshness, liveness-probe, and occurred-at hardening gaps without widening beyond the named runner, shared parser, operator-config, inboxd connector, and hosted control-plane seams | `agent-docs/exec-plans/active/2026-04-23-linq-unclaimed-hardening.md` | `apps/cloudflare/src/{user-runner.ts,user-runner/types.ts,user-runner/runner-cleanup.ts,user-runner/runner-run-processor.ts,user-runner/runner-state-store.ts}`, focused `apps/cloudflare/test/{runner-run-processor.test.ts,user-runner-resume-finalize.test.ts,runner-state-store.bundle-slots.test.ts}`, `packages/operator-config/src/linq-runtime.ts`, focused `packages/operator-config/test/http-linq-device-runtime.test.ts`, `packages/messaging-ingress/src/linq-webhook.ts`, focused `packages/messaging-ingress/test/linq-webhook.test.ts`, `packages/inboxd/src/connectors/linq/connector.ts`, focused `packages/inboxd/test/linq-webhook-connector.test.ts`, `apps/web/src/lib/linq/control-plane.ts`, focused `apps/web/test/linq-control-plane.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-linq-unclaimed-hardening.md,COORDINATION_LEDGER.md}` | durable Linq cleanup ids across finalize/resume, Linq DELETE retryability, shared Linq occurredAt canonicalization, inboxd timestamp freshness, inboxd Linq GET probe minimization, hosted control-plane occurredAt parity | in_progress | High-risk overlap with the active Linq cleanup-retention, sparse-recipient fallback, and local-Linq-removal rows, plus the recently completed shared recipient-preservation row on `packages/messaging-ingress/src/linq-webhook.ts`. Keep the runner change additive on top of the current sidecar-retention work, avoid recipient-routing redesign in `apps/web/src/lib/linq/control-plane.ts`, preserve the shared parser's explicit recipient fallbacks, and keep the inboxd connector hardening narrow enough that the separate local-removal lane can still delete the surface later. |
| Codex | Hard-cut hosted Telegram platform env out of user-executable runtime env without widening beyond the Telegram env-category/policy seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-telegram-bot-token-boundary.md` | `packages/assistant-runtime/src/{hosted-env-categories.ts,hosted-runtime/environment.ts,hosted-runtime.ts,hosted-runtime/events/telegram.ts,hosted-assistant-env-constants.ts}`, `apps/cloudflare/src/{hosted-env-policy.ts,runner-env.ts,node-runner-isolated.ts,user-runner/runner-run-processor.ts}`, `apps/cloudflare/test/runner-env.test.ts`, `packages/assistant-runtime/test/{hosted-runtime-environment,package-entrypoints}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-telegram-bot-token-boundary.md,COORDINATION_LEDGER.md}` | hosted Telegram env profile, child runtime env forwarding, Telegram platform env trust boundary, package export surface | completed | Focused Cloudflare and assistant-runtime proof are green, required `coverage-write` plus `task-finish-review` both landed, and a follow-up `coverage-write` rerun added explicit `platformEnv` proof in `apps/cloudflare/test/node-runner.test.ts`. `task-finish-review` surfaced and we fixed the missing container platform-URL rewrite plus the source-level assistant-runtime export gap; local ignored `dist` artifacts were refreshed only to prove the package subpath behavior. `pnpm verify:acceptance` still fails for unrelated pre-existing `packages/core/test/canonical-resource-lock.test.ts(226,30)` typecheck, and the live tree now also has unrelated `apps/cloudflare typecheck` failures in `src/user-runner/runner-run-processor.ts` and `packages/contracts/src/vault-families.ts`. No scoped commit was created because exact staging would require partial commits across overlapping active dirty edits in `apps/cloudflare/src/user-runner/runner-run-processor.ts`, `apps/cloudflare/src/hosted-env-policy.ts`, and `packages/assistant-runtime/src/hosted-env-categories.ts`. |
| Codex | Harden hosted onboarding auth binding, sign-in-only enforcement, stale Privy member metadata handling, and hosted mutation-origin checks without widening beyond the hosted auth/onboarding seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-hosted-auth-binding-hardening.md` | `apps/web/app/auth-controls.tsx`, `apps/web/src/components/hosted-onboarding/{hosted-existing-account-sign-in-dialog.tsx,hosted-auth-completion.ts,hosted-phone-auth-support.ts,hosted-email-auth-button.tsx,hosted-telegram-auth-button.tsx}`, `apps/web/src/lib/hosted-onboarding/{authentication-service,member-identity-service,member-identity-lookup,privy,privy-user,request-auth,request-auth-types,authentication-intent,csrf,env,public-url,invite-service}.ts`, directly coupled `apps/web/test/**`, `agent-docs/exec-plans/active/{2026-04-23-hosted-auth-binding-hardening.md,COORDINATION_LEDGER.md}` | existing-account sign-in contract, hosted identity binding keys, stale `murph_member_id` rejection, canonical-origin enforcement | in_progress | High-risk hosted auth lane. Preserve unrelated dirty-tree work, especially the active hosted billing and hosted-ingress rows, and keep invite/new-member creation semantics unchanged outside the explicit sign-in-only path. |
| Codex | Harden hosted Telegram routing so contact-privacy key rotation cannot create cross-version duplicate bindings or nondeterministic inbound lookup without widening beyond the Telegram routing seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-telegram-routing-rotation-hardening.md` | `apps/web/src/lib/hosted-onboarding/{hosted-member-routing-store,hosted-member-routing-telegram,webhook-provider-telegram}.ts`, directly coupled `apps/web/test/{hosted-onboarding-member-store,hosted-onboarding-telegram-dispatch}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-telegram-routing-rotation-hardening.md,COORDINATION_LEDGER.md}` | hosted Telegram blind-index rotation seam, cross-version routing uniqueness, fail-closed inbound Telegram member lookup | completed | Focused Telegram rotation hardening is implemented and directly coupled proof is green. Required `coverage-write` and `task-finish-review` audit-agent attempts were blocked by account usage limits, and no scoped commit was created because the touched routing files and tests overlap active dirty-tree edits from the broader blind-index and Telegram thread-target lanes. |
| Codex | Redact raw Telegram chat ids, business connection ids, and topic-composite targets from hosted/operator failure logs without widening beyond the Telegram runtime and hosted observability seams | `agent-docs/exec-plans/active/2026-04-23-telegram-log-redaction.md` | `packages/{operator-config/src/telegram-runtime.ts,hosted-execution/src/observability.ts}`, focused `packages/{operator-config/test/runtime-helpers.test.ts,hosted-execution/test/hosted-execution-observability-side-effects.test.ts}`, `agent-docs/exec-plans/active/{2026-04-23-telegram-log-redaction.md,COORDINATION_LEDGER.md}` | Telegram error-context redaction, hosted structured-log Telegram sanitization, privacy-bounded cleanup failure logs | in_progress | Narrow privacy/logging lane only. Coordinate with the active operator-config boundary row plus the hosted observability and runner split rows; avoid delivery-behavior changes or direct edits inside dirty Cloudflare cleanup files unless a focused proof gap forces it. |
| Codex | Harden assistant provider identity, API-key resolution, hosted delegation guards, failover boundaries, and runtime-target precedence without widening beyond the cited operator-config, assistant-engine, assistant-runtime, and runtime-state seams | `agent-docs/exec-plans/active/2026-04-23-assistant-provider-hardening.md` | `packages/operator-config/src/assistant/{openai-compatible-provider-presets.ts,provider-config.ts,shared.ts,target-runtime.ts}`, `packages/operator-config/src/{assistant-backend.ts,assistant-cli-contracts.ts}`, `packages/operator-config/test/**`, `packages/assistant-engine/src/{model-harness.ts}`, `packages/assistant-engine/src/assistant/{failover.ts,provider-config.ts,provider-turn-runner.ts}`, `packages/assistant-engine/src/assistant/providers/openai-compatible.ts`, `packages/assistant-engine/test/**`, `packages/assistant-runtime/src/hosted-runtime.ts`, `packages/assistant-runtime/test/**`, `packages/runtime-state/src/assistant-usage.ts`, `packages/runtime-state/test/**`, `agent-docs/exec-plans/active/{2026-04-23-assistant-provider-hardening.md,COORDINATION_LEDGER.md}` | provider preset host identity, gateway-only delegation gating, assistant API-key resolution parity, provider failover retry boundary, runtime-target precedence | in_progress | High-risk cross-package security/billing lane. Preserve the existing dirty hard-cut edit in `packages/runtime-state/src/assistant-usage.ts`, work carefully around the active operator-config boundary lane plus the active assistant-runtime drain lane, and avoid widening into unrelated assistant-codex, hosted-run, or cloudflare execution work. |
| Codex | Harden hosted ingress alias invariants so one current alias per wake is DB-enforced and replacement chains cannot dangle, without widening beyond the alias seam and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-hosted-ingress-alias-invariant.md` | `apps/web/prisma/{schema.prisma,migrations/2026040600_init/migration.sql}`, `apps/web/src/lib/hosted-ingress/{store-data.ts,store-append.ts}`, directly coupled `apps/web/test/{hosted-ingress-store-data,hosted-onboarding-privacy-foundation-migration}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-hosted-ingress-alias-invariant.md,COORDINATION_LEDGER.md}` | hosted ingress alias current-row uniqueness, alias replacement-chain FK, fail-closed current-alias replacement | in_progress | High-risk hosted-ingress storage lane. Preserve the existing dirty rename/hard-cut edits in the same schema/store files, avoid widening into unrelated hosted billing or hosted-run work, and treat overlapping alias-file changes as additive-only unless coordination is required. |
| Codex | Split the five user-requested giant files along the documented seam boundaries without changing behavior or widening beyond the named file families and directly coupled proof | `agent-docs/exec-plans/active/2026-04-23-giant-file-seam-splits.md` | `packages/assistant-engine/src/{assistant/cron.ts,assistant/cron/**,assistant-codex.ts,assistant-codex/**}`, `apps/web/src/lib/hosted-run/{store.ts,**}`, `apps/cloudflare/src/user-runner/{runner-run-processor.ts,**}`, `packages/query/src/{browser-replica.ts,browser-replica/**}`, directly coupled tests/callers, `agent-docs/exec-plans/active/{2026-04-23-giant-file-seam-splits.md,COORDINATION_LEDGER.md}` | assistant cron canonical/execution split, hosted-run acquire/lifecycle/status split, runner web-observability/cleanup split, browser-replica parser split, assistant-codex config/images split | completed | Completed with the required `simplify`, `coverage-write`, and `task-finish-review` passes. Focused assistant-engine, CLI, query coverage, apps/web, apps/cloudflare, smoke, `git diff --check`, and one direct facade smoke all passed; repo-wide `pnpm typecheck` and scoped `test:diff` remain blocked by unrelated branch-level workspace-boundary / app typecheck failures. No scoped commit was created because the checkout is too dirty for exact staging without absorbing unrelated active-row churn. |
| Codex | Hard-cut the remaining hosted ingress/wake compatibility fallbacks, the hosted-run Prisma wake-column alias, the singular vault-sync redacted summary field, and the local assistant-usage legacy pending filename cleanup without widening beyond those named seams and directly coupled proof | `agent-docs/exec-plans/completed/2026-04-23-hosted-hard-cut-cleanups.md` | `packages/hosted-execution/src/{parsers.ts,parsers/run-control.ts}`, `apps/web/prisma/{schema.prisma,migrations/2026040600_init/migration.sql}`, `apps/web/src/lib/{hosted-ingress/store-append.ts,hosted-run/store.ts,vault-sync/session-service.ts}`, `apps/cloudflare/src/{runner-outbound/turn-input.ts,user-runner.ts}`, `packages/assistant-runtime/src/hosted-runtime/execution.ts`, `packages/runtime-state/src/assistant-usage.ts`, directly coupled tests only if needed, `agent-docs/exec-plans/{completed/2026-04-23-hosted-hard-cut-cleanups.md,active/COORDINATION_LEDGER.md}` | canonical ingress-event parser keys, hosted-run ingress column baseline, plural vault-sync redacted summaries, assistant-usage pending filename hard cut | completed | Hard cuts landed with focused proof plus the required `simplify`, `coverage-write`, and `task-finish-review` audits. The simplify pass found dual-key parser acceptance and a dead singular runtime metric slot; both were fixed before the final reruns. `pnpm typecheck`, focused owner checks, direct runtime proof, and `git diff --check` all passed; scoped `pnpm test:diff ...` is still blocked by the unrelated pre-existing `packages/assistant-engine/test/assistant-wrapper-exports.test.ts` failure expecting `executeCodexPrompt`. No scoped commit was created because the touched files overlap earlier uncommitted work in this dirty tree and the shared ledger file carries unrelated concurrent churn, so an exact task-only commit would have absorbed work outside this task. |
| Codex | Land the requested cron, hosted-run store, hosted-ingress naming, and Cloudflare runner lifecycle cleanup follow-ups without widening beyond the named refactor seams and directly coupled proof | `agent-docs/exec-plans/completed/2026-04-23-cron-runner-store-followups.md` | `packages/assistant-engine/src/assistant/cron.ts`, directly coupled `packages/assistant-engine/test/{assistant-cron-runtime,assistant-cron-thresholds,food-recurring-cron}.test.ts`, `packages/cli/test/assistant-cron.test.ts`, `apps/web/src/lib/{hosted-run/store.ts,hosted-ingress/store-data.ts}`, directly coupled `apps/web/test/{hosted-run-store,hosted-onboarding-member-activation}.test.ts` and naming-coupled callers/tests only if needed, `apps/cloudflare/src/user-runner/runner-run-processor.ts`, directly coupled `apps/cloudflare/test/runner-run-processor.test.ts`, `agent-docs/exec-plans/{completed/2026-04-23-cron-runner-store-followups.md,active/COORDINATION_LEDGER.md}` | assistant cron food-auto-log adapter split, resolved cron mutation helper, hosted-run locked cursor/update helpers, hosted-ingress store helper naming, shared run-drain lifecycle helper | completed | Cross-cutting operational cleanup lane completed with required `simplify`, `coverage-write`, and `task-finish-review` passes. Focused assistant-engine/CLI/apps-web/apps-cloudflare proof plus a direct cron scenario passed after the raw-vs-visible local resolver regression test was added. No scoped commit was created because the shared ledger file already carries unrelated concurrent churn, and both `pnpm typecheck` and the owner `pnpm test:diff ...` lane are currently blocked by unrelated `packages/device-syncd` type failures in this branch. |
| Codex | Land the requested hosted/runtime/CLI cleanup follow-ups without widening beyond the named compatibility cleanup seams, shared helper extraction, and dead local helper deletions | `agent-docs/exec-plans/completed/2026-04-23-hosted-cleanup-followups.md` | `packages/assistant-runtime/src/hosted-runtime/execution.ts`, `apps/web/src/lib/{vault-sync/session-service.ts,hosted-ingress/encryption.ts,hosted-web/encryption.ts,hosted-encryption-shared.ts}`, `packages/assistant-engine/src/model-harness.ts`, `packages/hosted-execution/src/{builders.ts,parsers.ts}`, `packages/cli/src/commands/{capture.ts,measurement.ts,workout.ts,command-factory-primitives.ts}`, `packages/cli/test/list-command-factory-coverage.test.ts`, `packages/{inboxd/src/connectors/linq/normalize.ts,inbox-services/src/inbox-app/runtime.ts,device-syncd/src/config/provider-manifests.ts,device-syncd/src/providers/whoop.ts,assistant-engine/src/assistant/provider-turn-runner.ts,assistant-engine/src/assistant/providers/openai-compatible.ts}`, `apps/cloudflare/src/{container-entrypoint.ts,user-runner/runner-run-processor.ts}`, directly coupled tests only if needed, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | hosted vault-sync singular cleanup, shared hosted encryption helper, validated assistant model spec helper, hosted-execution member/user naming seam, CLI common list command adoption, dead helper removal | completed | Cleanup landed in the shared worktree. Required `simplify`, `coverage-write`, and `task-finish-review` passes all ran; the follow-up read-path proof for plural vault-sync summaries was added before the final green focused reruns. No scoped commit was created because `packages/inboxd/src/connectors/linq/normalize.ts` overlaps another active row and the shared ledger file carries unrelated concurrent churn, so commit helpers would risk absorbing work outside this task. |
| Codex | Minimize duplicated Linq raw persistence in local and hosted inbox captures without regressing assistant UX, reply routing, or attachment handling | `agent-docs/exec-plans/active/2026-04-23-linq-raw-minimization.md` | `packages/messaging-ingress/src/linq-webhook.ts`, `packages/inboxd/src/connectors/linq/normalize.ts`, directly coupled `packages/{messaging-ingress,inboxd}/test/**`, `apps/web/test/hosted-onboarding-linq-dispatch.test.ts` only if needed for hosted capture proof, `agent-docs/exec-plans/active/{2026-04-23-linq-raw-minimization.md,COORDINATION_LEDGER.md}` | Linq minimal raw schema, hosted/local Linq capture raw parity, Linq reply-linkage preservation | in_progress | High-sensitivity privacy lane. Keep normalized text and attachment hydration unchanged, avoid the active hosted-local Linq voice memo downloader lane unless additive proof is required, and do not broaden into gateway-local or hosted wake-contract redesigns without a reviewed blocker. |
| Codex | Preserve explicit Linq recipient fields for legacy/non-canonical message payloads without widening beyond the shared parser and directly coupled hosted regression proof |  | `packages/messaging-ingress/src/linq-webhook.ts`, directly coupled `packages/messaging-ingress/test/linq-webhook.test.ts`, `apps/web/test/hosted-onboarding-linq-webhook.test.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | legacy Linq recipient_handle/recipient_phone preservation, hosted recipient lookup fallback | completed | Narrow overlap with the active Linq raw-minimization row on `packages/messaging-ingress/src/linq-webhook.ts`. Landed in commit `482f2b067` with focused messaging-ingress typecheck plus package/app Vitest proof. The shared worktree still has unrelated overlapping edits in `packages/messaging-ingress/{src/linq-webhook.ts,test/linq-webhook.test.ts}` outside this commit, so leave those for the owning lane. |
| Codex | Clarify shared Linq raw-vs-canonical parser naming without widening beyond the parser API, direct callers, and coupled proof | `agent-docs/exec-plans/active/2026-04-23-linq-parser-naming-clarity.md` | `packages/messaging-ingress/src/linq-webhook.ts`, directly coupled `packages/messaging-ingress/test/linq-webhook.test.ts`, `apps/web/src/lib/{hosted-onboarding/linq-webhook.ts,linq/control-plane.ts}`, directly coupled `apps/web/test/{hosted-onboarding-linq-webhook.test.ts,linq-control-plane.test.ts}`, `packages/inboxd/src/connectors/linq/normalize.ts`, directly coupled `packages/inboxd/test/linq-connector.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-linq-parser-naming-clarity.md,COORDINATION_LEDGER.md}` | Linq raw webhook parser naming, shared normalization entrypoint naming, direct caller/mock terminology | in_progress | Narrow naming-only follow-up to commit `482f2b067`. Overlaps the active Linq raw-minimization and unclaimed-hardening rows on `packages/messaging-ingress/src/linq-webhook.ts` plus direct consumer files; keep behavior unchanged, preserve unrelated dirty edits, and do not widen into routing/fallback redesign. |
| Codex | Reopen the hosted snapshot cleanup lane to fail closed on unreadable authoritative bundles, reclaim losing candidate bundle/browser-vault objects, reconcile cleanup after commit/finalize response loss, and preserve finalize cleanup inputs across sidecar write failures without widening beyond the directly coupled Cloudflare/query seams and focused regression coverage | `agent-docs/exec-plans/active/2026-04-23-hosted-snapshot-cleanup.md` | `apps/cloudflare/src/{bundle-store.ts,bundle-gc.ts,browser-vault-store.ts,user-runner.ts,user-runner/runner-bundle-sync.ts,user-runner/runner-run-processor.ts,user-runner/runner-state-store.ts,user-runner/types.ts}`, focused `apps/cloudflare/test/{runner-bundle-helpers,browser-vault-store,runner-run-processor,user-runner-resume-finalize}.test.ts`, `packages/query/test/browser-vault-replica.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-hosted-snapshot-cleanup.md,COORDINATION_LEDGER.md}` | fail-closed bundle GC, losing candidate cleanup, authoritative cursor transition reconciliation, pending finalize cleanup recovery, browser replica dataVersion coverage | in_progress | Implementation plus focused and diff-scoped Cloudflare verification are green again, including `apps/cloudflare` typecheck and `workspace-verify test:diff ...`. Required `coverage-write` and `task-finish-review` reruns are now in progress for the latest finalize/restore follow-up diff. Preserve unrelated dirty-tree work and keep this lane scoped to the hosted run cleanup seam plus the requested query regression tests. |
| Codex | Port only the applicable app-side slice of the downloaded SpO₂ review-improvements patch without recreating the missing content/source landing baseline | `agent-docs/exec-plans/active/2026-04-23-spo2-review-app-slice.md` | `apps/web/app/biomarkers/[biomarkerId]/biomarker-page-client.tsx`, focused `apps/web/test/**`, `agent-docs/exec-plans/active/{2026-04-23-spo2-review-app-slice.md,COORDINATION_LEDGER.md}` | percent-unit trend delta copy, raw-delta near-flat classification, isolated app proof for percent biomarkers | completed | Narrow wake-up lane completed. Focused Vitest, diff-aware `pnpm test:diff`, `apps/web verify`, and `git diff --check` all passed; required `frontend-review`, `coverage-write`, and `task-finish-review` found no blocking issues. No scoped commit was created because the shared ledger file carries unrelated concurrent churn, and the downloaded patch's authored-content/change-log hunks remain inapplicable on this checkout without the missing SpO₂ source baseline. |
| Codex | Add hosted-local Linq voice memo attachment proof without widening beyond the local downloader override, shared Linq stub helper, and webhook E2E coverage | `agent-docs/exec-plans/active/2026-04-23-linq-voice-memo-local-e2e.md` | `packages/assistant-runtime/src/hosted-runtime/events/linq.ts`, `packages/assistant-runtime/test/hosted-runtime-linq-event.test.ts`, `apps/cloudflare/test/helpers/hosted-local-linq-support.ts`, `apps/cloudflare/test/hosted-local-linq-webhook-e2e.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-linq-voice-memo-local-e2e.md,COORDINATION_LEDGER.md}` | hosted Linq attachment CDN override, local attachment metadata/download stub, hosted-local Linq voice memo webhook proof | in_progress | Narrow hosted-local proof lane. Overlaps the active Linq typing row on the shared helper; keep helper edits additive, avoid `apps/cloudflare/test/hosted-local-linq-first-contact-e2e.test.ts`, and preserve unrelated dirty-tree work. |
| Codex | Land the supplied SpO₂ review-improvements patch without widening beyond the authored SpO₂ source slice, percent-delta formatter seam, and directly coupled tests/generated outputs | `agent-docs/exec-plans/active/2026-04-23-spo2-review-improvements.md` | `packages/health-commons/content/{biomarkers/blood-oxygen-spo2.md,sources/spo2/**,changes/2026-04.jsonl}`, directly required `packages/health-commons/generated/**`, `apps/web/app/biomarkers/[biomarkerId]/biomarker-page-client.tsx`, `apps/web/test/{health-commons-biomarker-detail-page,health-commons-biomarker-page-client}.test.ts`, `agent-docs/exec-plans/active/{2026-04-23-spo2-review-improvements.md,COORDINATION_LEDGER.md}` | SpO₂ evidence claims, percentage-point trend copy, raw-delta flat classification, SpO₂ authored-source coverage | completed | Follow-up patch landed in the shared working tree. Verified with Health Commons generate/verify, focused biomarker Vitest, `apps/web` lint/typecheck, repo `typecheck`, scoped `workspace-verify test:diff`, `git diff --check`, and the required `coverage-write` / `frontend-review` / `task-finish-review` audits; the final review's page-boundary claim coverage gap was fixed before the green reruns. No scoped commit was created because `changes/2026-04.jsonl` plus `packages/health-commons/generated/**` still overlap other active biomarker rows, so staging this slice would absorb unrelated in-flight work. |
| Codex | Land the supplied deep sleep biomarker patch without widening beyond the authored Health Commons biomarker/source slice, biomarker evidence rendering, and directly required generated artifacts | `agent-docs/exec-plans/active/2026-04-23-deep-sleep-biomarker-landing.md` | `apps/web/app/biomarkers/[biomarkerId]/biomarker-page-client.tsx`, `apps/web/src/lib/health-commons/biomarker-detail.ts`, `packages/health-commons/content/{biomarkers/deep-sleep-minutes.md,sources/deep-sleep/**,changes/2026-04.jsonl}`, directly required `packages/health-commons/generated/**`, `agent-docs/exec-plans/active/{2026-04-23-deep-sleep-biomarker-landing.md,COORDINATION_LEDGER.md}` | deep sleep biomarker page, evidence-section claim rendering, source highlight hydration, Health Commons generated catalog artifacts | completed | Landed with scoped shared-file staging by rebuilding a temporary HEAD-exported Health Commons baseline plus only the deep-sleep overlay. Focused health-commons/web tests, `apps/web typecheck`, `pnpm test:smoke`, and `git diff --check` passed; `pnpm typecheck` and the scoped `workspace-verify test:diff` wrapper still report unrelated `apps/cloudflare/test/hosted-local-linq-cold-start-repro.e2e.test.ts` boundary violations, and `pnpm typecheck` also fails in unrelated `packages/vault-usecases` module-resolution checks. |
| Codex | Add assistant conversation-onboarding lifecycle state and CLI completion commands without widening into provider failover or app-server execution work |  | `packages/assistant-engine/src/assistant/{onboarding-state,turn-plan,system-prompt}.ts`, `packages/assistant-engine/src/assistant-state.ts`, `packages/assistant-cli/src/commands/assistant.ts`, `packages/operator-config/src/assistant-cli-contracts.ts`, `packages/runtime-state/src/assistant-local-state-descriptors.ts`, focused assistant CLI/engine tests only | assistant onboarding open/completed state, `assistant onboarding status/complete/reopen`, onboarding prompt eligibility | completed | Narrow lifecycle/prompt lane completed. Preserved the active Codex app-server edits in assistant CLI/provider files, avoided `provider-turn-runner.ts`, passed focused assistant CLI/engine/runtime-state verification plus `pnpm typecheck`, and the required `coverage-write` / `task-finish-review` audits found no additional changes. Scoped `test:diff` broadened into unrelated pre-existing assistant-engine failures outside this lane. |
| Codex | Prevent hosted assistant device-connect attempts for unsupported providers and surface dynamic supported-provider guidance |  | `packages/assistant-engine/src/assistant/{execution-context,provider-turn-runner,system-prompt}.ts`, `packages/assistant-engine/src/assistant-cli-tools/definitions/outward-side-effects.ts`, `packages/assistant-runtime/src/hosted-runtime.ts`, focused assistant/runtime tests only | hosted `murph.device.connect`, supported hosted device provider prompt guidance, hosted device connect issuer | completed | Unsupported providers now fail before hosted API calls; prompts list configured providers dynamically. Verified focused tests, `pnpm typecheck`, and `scripts/workspace-verify.sh test:diff`; finish review found no issues. |
| Codex | Restore linked release checks by covering hosted assistant turn-input refresh behavior without production runtime changes |  | `packages/assistant-runtime/test/hosted-runtime-turn-input.test.ts` | `createHostedAssistantTurnInputPort`, hosted turn-input refresh/import coverage | completed | Narrow test-only lane for Actions run `24814241499`. Added focused coverage for hosted refresh absence, event import/dedupe/afterSeq handling, base-port delegation, and refresh failure logging/rethrow. Preserve unrelated dirty research scripts and the separate Vercel gateway row. |
| Worker A | Make `packages/contracts` scheduled-log coverage green for the current repo-checks-green task without weakening thresholds or widening outside directly coupled generated artifacts |  | `packages/contracts/test/scheduled-log.test.ts`, `packages/contracts/scripts/verify.ts`, `packages/contracts/generated/{event-record.schema.json,frontmatter-experiment.schema.json}` | scheduled-log schema/action/schedule issue coverage, contracts generated schema freshness, event-record schema variant count | completed | Narrow contracts package failure lane completed. Added focused scheduled-log tests, regenerated stale contracts schemas, updated the verifier's event-record variant count to match the generated contract, and verified with green contracts coverage/typecheck plus diff whitespace checks. Preserved unrelated dirty work, including `packages/contracts/src/examples.ts`. |
| Codex | Fix prepared-runtime acceptance build graph so package declaration artifacts are emitted before built-runtime/package-shape consumers inspect them |  | `scripts/build-test-runtime-prepared.mjs` | `pnpm build:test-runtime:prepared`, TypeScript project references, prepared runtime artifact checks | completed | Narrow acceptance-build fix completed. Prepared helper now validates declaration artifacts plus `device-syncd/client`, and force-retries when the initial incremental project-reference build exits nonzero. Verified stale-artifact simulation with `pnpm build:test-runtime:prepared`, `pnpm typecheck`, and `bash scripts/workspace-verify.sh test:diff scripts/build-test-runtime-prepared.mjs`. `pnpm --dir packages/cli verify:package-shape` now reaches a separate stale CLI config-schema failure tied to protected dirty CLI command work; do not touch that lane here. No commit requested. |
| Codex | Add a Biomarkers entry to the shared dashboard sidebar and keep `/biomarkers/*` inside the same dashboard shell without widening beyond the shared `apps/web` nav/layout seam |  | `apps/web/app/{(dashboard)/layout.tsx,biomarkers/layout.tsx}`, `apps/web/src/components/dashboard/{sidebar.tsx,dashboard-shell.tsx}`, `apps/web/test/{dashboard-sidebar.test.ts,biomarker-layout.test.ts,health-commons-biomarker-page-client.test.ts}` | dashboard sidebar nav items, biomarker section route match, shared dashboard shell | completed | Narrow `apps/web` navigation/layout lane completed. Added the Biomarkers sidebar entry for the live resting-heart-rate page, rendered biomarker routes inside the shared dashboard shell with route-owned spacing, aligned in-shell breadcrumb navigation to `/overview`, preserved unrelated dirty-tree edits, and ran the required `gpt-5.4` high completion-audit passes plus focused Vitest/diff proof. Broader `apps/web` typecheck and scoped `test:diff` remain blocked by the unrelated pre-existing `packages/contracts/src/examples.ts` type mismatch. |
| Worker 1 | Add focused CLI experiment test coverage for the landing progress/session/context/outcome command surfaces without editing runtime implementation |  | `packages/cli/test/{cli-expansion-experiment-journal-vault-phase2.test.ts,cli-expansion-experiment-journal-vault.test.ts}` | experiment CLI progress summary, session/context logging, deterministic outcome analysis, outcome-write coverage only if the command already lands in this checkout | completed | Added a focused phase-2 experiment CLI scenario covering update + session/context logging, experiment-linked event readback, deterministic `experiment progress`, and deterministic `experiment outcome analyze` payloads. Verified with a focused Vitest run on `packages/cli/test/cli-expansion-experiment-journal-vault-phase2.test.ts`; `pnpm --dir packages/cli typecheck` remains blocked by overlapping in-flight source changes under `packages/cli/src/commands/experiment.ts`, `packages/core/src/domains/events.ts`, and an unrelated `packages/assistant-engine/src/assistant-cli-tools/definitions/vault-write.ts` type mismatch. The `experiment outcome write` command is now present in the dirty source lane but was intentionally left untested here because this worker stayed test-only and did not guess the still-shifting write contract. |
| Codex | Land the supplied raw-envelope and canonical-wearables patch without widening beyond the importers/query wearable seams and directly coupled tests |  | `packages/importers/{package.json,src/{core-port.ts,device-providers/{canonical-wearable-records.ts,import-device-provider-snapshot.ts,index.ts,metric-catalog.ts,raw-ingest-envelope.ts,types.ts}},test/**}`, `packages/query/src/{index.ts,wearables.ts,wearables/{canonical-records.ts,provider-policy.ts,types.ts}}`, directly coupled focused tests only if required | wearable raw-ingest envelope, canonical wearable record schema, query canonical dataset bridge, importer-owned metric tolerance catalog | done | Narrow supplied patch landing only. Preserve unrelated dirty-tree edits, especially the existing `apps/web/next-env.d.ts` modification and the unrelated active ledger rows above and below this entry. |
| Codex | Land the missing Bryan Johnson sauna experiment-onboarding patch from the supplied trace without widening beyond the protocol page, directly coupled generated Health Commons artifacts, and assistant experiment-onboarding instructions |  | `packages/health-commons/content/{protocols/dry-sauna/bryan-johnson-blueprint.md,changes/2026-04.jsonl}`, directly coupled `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}`, `packages/assistant-engine/src/assistant/{system-prompt.ts}`, `packages/assistant-engine/test/system-prompt.test.ts`, `packages/openclaw-plugin/skills/murph/SKILL.md`, `packages/health-commons/test/bryan-johnson-onboarding.test.ts` | Bryan Johnson sauna `experimentOnboarding`, source-attributed onboarding guidance, generated Health Commons onboarding projection | completed | Narrow supplied-trace landing completed. Verified with `pnpm typecheck`, scoped `test:diff`, `pnpm test:smoke`, `pnpm --dir packages/health-commons generate:check`, focused `assistant-engine` prompt coverage, direct generated-catalog readback, required `coverage-write`, and clean final review. Preserved unrelated dirty-tree edits, including `apps/web/next-env.d.ts` and separate wearables work under `packages/{cli,importers,query}`. |
| Codex | Regenerate the stale CLI generated artifacts only if needed to clear current package-shape verification for the shared branch landing |  | `packages/cli/{config.schema.json,src/incur.generated.ts}` | CLI generated artifact freshness | completed | Acceptance-only cleanup lane completed. Refreshed the generated CLI artifacts, removed the stray untracked `packages/cli/src/config.schema.json` residue from the generator, and reran `pnpm --dir packages/cli verify:package-shape` successfully while leaving the unrelated scheduled-log acceptance failures untouched. |
| Codex | Clean up the capture add result contract so exact-event lookup and stable-label lookup are separate again without widening beyond the capture result surface |  | `packages/vault-usecases/src/usecases/capture.ts`, `packages/vault-usecases/test/capture.test.ts`, `packages/operator-config/src/vault-cli-contracts.ts` | capture add `lookupId`, stable capture lookup result field, capture result schema | completed | Narrow follow-up completed. Restored exact-event `lookupId`, added nullable `stableLookupId`, updated the capture result schema and focused tests, and kept the broader capture behavior unchanged. Verified with final green reruns of `pnpm typecheck`, `pnpm test:diff packages/vault-usecases/src/usecases/capture.ts packages/vault-usecases/test/capture.test.ts packages/operator-config/src/vault-cli-contracts.ts`, `pnpm test:smoke`, focused `packages/vault-usecases` Vitest scenario checks, `pnpm build:test-runtime:prepared`, and direct built-CLI proof showing labeled output returns exact `lookupId` plus `stableLookupId`, while unlabeled output returns exact `lookupId` plus `stableLookupId: null`. Required `coverage-write` and `task-finish-review` audit passes both ran with no additional changes needed. |
| Codex | Land the supplied capture cleanup patch against current `vault-usecases` HEAD without widening beyond the capture usecase, its existing tests, and directly coupled package coverage config |  | `packages/vault-usecases/src/usecases/capture.ts`, `packages/vault-usecases/test/capture.test.ts`, `packages/vault-usecases/vitest.config.ts` | capture batch default merging, duplicate-label guard, capture add `lookupId`, deterministic capture list ordering, capture structured-input validation, capture package coverage include | completed | Narrow supplied patch landing completed. Merged the stale add-file patch into the existing tracked capture test file, kept the requested label-based `lookupId` behavior intentionally, restored the pre-existing direct `captures: []` fallback after final review, and added focused proof for structured root defaults plus direct-empty-array fallback. Verified with final green reruns of `pnpm typecheck`, `pnpm test:diff packages/vault-usecases/src/usecases/capture.ts packages/vault-usecases/test/capture.test.ts packages/vault-usecases/vitest.config.ts`, `pnpm test:smoke`, and focused `packages/vault-usecases` Vitest scenario checks. Required `coverage-write` and `task-finish-review` audit passes both ran before handoff. |
| Codex | Tighten agent workflow docs so agents avoid git worktrees unless explicitly requested and treat completion audit subagents as standing user-approved when the workflow requires them |  | `AGENTS.md`, `agent-docs/operations/{agent-workflow-routing,completion-workflow}.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | git worktree policy, completion-workflow audit subagent authorization | completed | Docs/process-only lane. Updated policy wording only. Verified with `git diff --check`, `pnpm typecheck`, and `bash scripts/workspace-verify.sh test:diff AGENTS.md agent-docs/operations/agent-workflow-routing.md agent-docs/operations/completion-workflow.md agent-docs/exec-plans/active/COORDINATION_LEDGER.md`. |
| Codex | Land the supplied assistant onboarding resume fix without widening beyond the assistant-engine onboarding/native-resume slice and the directly coupled architecture note |  | `packages/assistant-engine/src/assistant/{provider-turn-runner,providers/{codex-cli,helpers},system-prompt,turn-plan}.ts`, focused `packages/assistant-engine/test/{onboarding-injection,provider-turn-runner,provider-registry-helpers,assistant-store-runtime}.test.ts`, directly coupled `packages/cli/test/assistant-provider.test.ts`, `ARCHITECTURE.md` only | early-session onboarding eligibility, provider-native resume preservation, Codex flat-prompt resume composition | completed | Watch-only Pro patch landing completed. Preserved unrelated dirty-tree edits, including the pre-existing `apps/web/next-env.d.ts` change and unrelated Health Commons content churn. Verified with `pnpm typecheck`, `pnpm test:diff packages/assistant-engine/src/assistant/provider-turn-runner.ts packages/assistant-engine/src/assistant/providers/codex-cli.ts packages/assistant-engine/src/assistant/providers/helpers.ts packages/assistant-engine/src/assistant/system-prompt.ts packages/assistant-engine/src/assistant/turn-plan.ts packages/assistant-engine/test/onboarding-injection.test.ts packages/assistant-engine/test/provider-turn-runner.test.ts packages/assistant-engine/test/provider-registry-helpers.test.ts packages/assistant-engine/test/assistant-store-runtime.test.ts packages/cli/test/assistant-provider.test.ts ARCHITECTURE.md`, and a post-review focused `pnpm --dir packages/assistant-engine exec vitest run test/onboarding-injection.test.ts --config vitest.config.ts --no-coverage`. Recursive same-thread follow-up send succeeded and armed the depth-0 wake. |
| Codex | Land the supplied Norwegian 4x4 Health Commons research-gap patch without widening beyond the returned evidence/source corpus update |  | `packages/health-commons/content/{artifacts/norwegian-4x4/research-artifacts.json,changes/2026-04.jsonl,families/norwegian-4x4.md,protocols/norwegian-4x4/norwegian-4x4.md,sources/norwegian-4x4/**}`, directly coupled `packages/health-commons/generated/**` only | Norwegian 4x4 bibliography expansion, protocol/family evidence guardrails, generated Health Commons catalog artifacts | completed | Watch-only Pro patch landing. Landed the Norwegian 4x4 evidence/source additions, regenerated clean Norwegian-only catalog outputs from a clean worktree to avoid overlapping Bryan Johnson source edits, and updated the directly coupled hosted-web research-stats expectation. Verified with `pnpm typecheck`, `pnpm --dir packages/health-commons verify`, `pnpm test:smoke`, focused hosted-web Vitest, and diff whitespace checks. Scoped `pnpm test:diff ...` remains blocked only by the unrelated pre-existing `apps/web/test/experiment-header.test.ts` baseline/protocol expectation. |
| Codex | Tighten Bryan Johnson sauna protocol copy and dedupe repeated study-card metadata in the narrow Health Commons and hosted experiment-detail slice |  | `packages/health-commons/content/protocols/dry-sauna/bryan-johnson-blueprint.md`, directly coupled `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}`, `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, directly coupled `apps/web/test/study-card.test.ts` only if required | Bryan Johnson sauna redundant step framing, `StudyCard` metadata dedupe | completed | Tightened the redundant Bryan Johnson sauna step/tip/test-plan phrasing, regenerated the coupled Health Commons artifacts, deduped repeated study-card metadata labels, and added focused `StudyCard` coverage including case/whitespace normalization. Verified with `pnpm --dir packages/health-commons verify`, `pnpm --dir apps/web typecheck`, focused `StudyCard` Vitest, and a direct render proof. `pnpm --dir apps/web verify` remains red on the unrelated pre-existing `apps/web/test/experiment-header.test.ts` expectation. |
| Codex | Land the supplied red-light-glasses evidence metadata patch without widening into UI or unrelated Health Commons sources |  | `packages/health-commons/content/sources/red-light-glasses-before-bed/*.md`, directly coupled `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson}` only | red-light `researchEvidence` metadata, generated participant rollup artifacts | completed | Supplied patch landing only. Applied the source-content slice, regenerated the coupled Health Commons artifacts because the supplied generated hunks no longer matched HEAD, and verified with `pnpm --dir packages/health-commons verify`, `git diff --check`, plus a direct catalog readback confirming the deduped red-light primary participant total is 390. Completion review flagged that the supplied `primary` set includes adjacent/safety cohorts, which is preserved here because it matches the requested aggregation intent. |
| Codex | Land supplied mechanism-first Health Commons `whyItWorks` patch for the four experiment pages |  | `packages/health-commons/content/protocols/{dry-sauna,norwegian-4x4,red-light-glasses-before-bed}/**`, `packages/health-commons/content/changes/2026-04.jsonl`, `packages/health-commons/generated/**`, `apps/web/test/health-commons-experiment-detail-page.test.ts` | protocol `whyItWorks` arrays, generated Health Commons catalog artifacts, app detail-page contract copy expectation | completed | Supplied patch landing only. Landed mechanism-first copy, regenerated Health Commons catalog outputs, updated the stale hosted-web expectation, and preserved overlapping source metadata plus hosted web work. Verified `pnpm --dir packages/health-commons verify`, `pnpm --dir apps/web verify`, and diff whitespace checks. |
| Codex | Swap experiment source-card year/sample placement and sort source cards by participant count |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, `apps/web/src/lib/health-commons/experiment-detail.ts`, focused `apps/web/test/{study-card,health-commons-experiment-detail-page}.test.ts` only if required | `StudyCard` participant/year pills, study source ordering | completed | Moved participant counts into the leading badge, rendered included-study counts as a smaller sub-pill, moved years to the title row, sorted study rows by participant count, and verified hosted web plus desktop/mobile browser checks. Preserved overlapping experiment-detail/source metadata edits. |
| Codex | Move experiment source-card sample counts to the upper right and restore year badges |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, focused `apps/web/test/study-card.test.ts` only if required | `StudyCard` year badge, sample-count placement | completed | Restored visible year badges, moved sample counts to the title row's upper-right position, kept year out of metadata, updated focused render coverage, and verified the live Norwegian 4x4 page on desktop/mobile screenshots. Preserved active Health Commons evidence metadata rows. |
| Codex | Show study years in experiment source-card badges and remove duplicate year metadata from the source subtitle |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, focused `apps/web/test/**` only if required | `StudyCard` source badge and metadata formatting | completed | Moved publication years into the source-card badge with type fallback, removed year duplication from metadata, added focused render coverage, and verified the live Norwegian 4x4 page on desktop/mobile screenshots. Preserved active Health Commons source-label/protocol rows. |
| Codex | Make experiment source cards show platform-specific source labels and replace ID-only Bryan Johnson X source titles | `agent-docs/exec-plans/active/2026-04-21-experiment-source-labels.md` | `apps/web/src/lib/health-commons/experiment-detail.ts`, focused `apps/web/test/**` only if required, `packages/health-commons/content/sources/sauna/{x-bryan-johnson-*.md,bryan-johnson-*.md,linkedin-bryan-johnson-*.md}`, `packages/health-commons/generated/**` | source card label formatting, Bryan Johnson X source titles, Health Commons generated source metadata | completed | Added platform-aware source-card labels, replaced ID-only Bryan Johnson X source titles, regenerated catalog outputs, and verified with Health Commons checks plus scoped web experiment-detail tests. Preserved overlapping Health Commons/generated dirty work. |
| Codex | Land the supplied Bryan Johnson sauna source-findings wake patch without widening beyond the cited sauna source pages and directly coupled generated artifacts |  | `packages/health-commons/content/changes/2026-04.jsonl`, `packages/health-commons/content/sources/sauna/{bryan-johnson-*,linkedin-bryan-johnson-*,x-bryan-johnson-*}.md`, directly coupled `packages/health-commons/generated/**` only | Bryan Johnson sauna source findings paragraphs, change log row, generated Health Commons catalog artifacts | completed | Watch-only Pro patch landing completed. Verified with `pnpm --dir packages/health-commons verify` and `pnpm typecheck`. The live branch index already carried unrelated staged Health Commons work, so the scoped commit was created cleanly on helper branch `codex/bryan-sauna-source-findings-clean` at `691c89e8` instead of mixing lanes in this checkout. |
| Codex | Land the supplied Finnish dry-sauna second-batch research-landscape patch without widening beyond the cited sauna source pages, protocol landscape block, and directly coupled generated artifacts |  | `packages/health-commons/content/changes/2026-04.jsonl`, `packages/health-commons/content/protocols/dry-sauna/murph-finnish-standard-3x-week.md`, `packages/health-commons/content/sources/sauna/{doi-10-1016-j-aimed-2024-09-009,doi-10-3389-fcvm-2025-1537194,doi-10.1080-23328940.2026.2645467,doi-10.3390-app151910762,pmid-24304490,pmid-24899780,pmid-30800676,pmid-31102597,pmid-31293098,pmid-31331560,pmid-31490429,pmid-31869820,pmid-31950931,pmid-3218892,pmid-3218894,pmid-3218897,pmid-32615263,pmid-32951736,pmid-33211153,pmid-33513711,pmid-34199101,pmid-34297227,pmid-34363927,pmid-34622026,pmid-34727008,pmid-35710395,pmid-35785965,pmid-35908583,pmid-36078656,pmid-36255556,pmid-36813265,pmid-37029766,pmid-37270272,pmid-37650138,pmid-38011189,pmid-38344040,pmid-38410962,pmid-38577299,pmid-38836690,pmid-39446139,pmid-40202605,pmid-40611569,pmid-41049507,pmid-41340471,pmid-41426898,pmid-41461792,pmid-41831305}.md`, directly coupled `packages/health-commons/generated/**` only | Finnish dry-sauna research landscape, second-batch sauna `protocolEvidence`, generated Health Commons catalog artifacts | completed | Watch-only Pro patch landing completed. Applied the sauna content slice, regenerated the coupled Health Commons outputs, added focused Finnish sauna route proof, and tightened the app projection so grouped research mode only activates when every displayed study has protocol appraisals. Verified with `pnpm typecheck`, `pnpm --dir packages/health-commons test:coverage`, `pnpm --dir packages/health-commons generate:check`, `pnpm test:smoke`, focused `apps/web` Vitest for `health-commons-experiment-detail-page` and `experiment-detail-protocol-tab`, and `git diff --check`. |
| Codex | Land the supplied experiment biomarker-prominence copy patch in the detail projection without widening into source metadata or protocol layout work |  | `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/experiment-detail-protocol-tab.test.ts` only if required | `PROTOCOL_BIOMARKER_DISPLAY_HINT_OVERRIDES`, red-light-glasses signal prominence, sauna/Norwegian biomarker hint copy | completed | Landed the supplied override copy/prominence patch, added focused protocol-tab coverage for Bryan Johnson sauna, Norwegian 4x4, and red-light glasses, and verified with `pnpm test:diff ...` plus `pnpm typecheck`. Kept the unrelated participant-stat label hunk out of scope for this lane. |
| Codex | Rename stale assistant-engine onboarding seams to match early-session onboarding while keeping first-contact dedupe state separate |  | `packages/assistant-engine/src/assistant/{service-contracts,turn-plan,provider-turn-runner,system-prompt,delivery-service,local-service,notification-turn}.ts`, `packages/assistant-engine/src/assistant/automation/reply.ts`, `packages/assistantd/src/http-protocol.ts`, directly coupled `packages/{assistant-engine,assistantd}/test/{onboarding-injection,assistant-store-runtime,provider-turn-runner,system-prompt,assistant-local-service-runtime,assistant-service-runtime,assistant-notification-turn-runtime,model-behavior,http-protocol-subject}.test.ts` only | `firstTurnCheckIn*` terminology cleanup, `includeEarlySessionOnboarding`, `firstContactStateDocIds`, assistantd message-parser flag validation | completed | Narrow cleanup only. Coordinated with overlapping assistant-engine notification/sanitizer work, edited only onboarding-specific lines plus the assistantd parser follow-up found in final review, and preserved unrelated dirty-tree edits. |
| Codex | Land the supplied assistant-engine onboarding first-session patch in the narrow onboarding slice only |  | `packages/assistant-engine/src/assistant/{turn-plan,provider-turn-runner,system-prompt}.ts`, directly coupled `packages/assistant-engine/test/{onboarding-injection,assistant-store-runtime,provider-turn-runner,system-prompt}.test.ts` only | first-session onboarding eligibility, onboarding bootstrap injection plan, early-session onboarding guidance | completed | Narrow supplied patch landing only. Coordinated with the overlapping assistant-engine system-prompt row, preserved unrelated dirty-tree edits, and kept the diff scoped to onboarding behavior plus directly coupled tests. |
| Codex | Land the supplied assistant-engine no-reply-sanitizer patch and repair the directly coupled test slice only |  | `packages/assistant-engine/src/assistant/{delivery-service,issue-reporting,local-service,notification-turn,reply-sanitizer,system-prompt}.ts`, directly coupled `packages/assistant-engine/test/**` only if required | reply sanitizer removal, direct provider response persistence/delivery, notification `send_message.text` validation, dev-note visibility policy removal | completed | Narrow supplied patch landing only. Preserved overlapping onboarding edits, removed the sanitizer/runtime-policy surface, and verified with `pnpm typecheck` plus the truthful `test:diff` lane for the touched assistant-engine slice. |
| Codex | Remove redundant CLI/runtime JSON-boundary `as unknown` casts only within the user-approved setup/assistant/operator-config/runtime-state slice | `agent-docs/exec-plans/active/2026-04-21-cli-runtime-json-boundary-cleanup.md` | `packages/{setup-cli,assistant-cli,assistantd,operator-config,runtime-state}/src/**`, directly coupled tests only if required | `JSON.parse(... as unknown)` cleanup, assistant daemon/client payload parsing, versioned JSON state reads | in_progress | Narrow mechanical cleanup only. Do not widen into parser behavior changes or unrelated active cleanup rows, and preserve all pre-existing dirty-tree edits. |
| Codex | Remove the now-unused `defer` branch from hosted run nudging and trim the dead pass-through fields from its clean caller slice only |  | `apps/web/src/lib/hosted-ingress/control.ts`, clean directly coupled callers under `apps/web/{app,src}/**`, directly coupled `apps/web/test/hosted-execution-handoff.test.ts` plus the clean `apps/web/test/hosted-onboarding-stripe-webhook-service.test.ts` typecheck follow-up only if required | `nudgeHostedRunBestEffort`, dead `defer`/`eventId`/`prisma` nudge inputs | completed | Narrow cleanup only. Kept webhook route-level `after()` behavior intact, avoided dirty `apps/web/src/lib/hosted-run/store.ts` and `apps/web/test/hosted-run-store.test.ts`, and preserved unrelated dirty-tree edits. |
| Codex | Remove redundant `JSON.parse(... ) as unknown` casts only in the owned storage/loader slice without changing behavior |  | `packages/core/src/{fs.ts,assessment/storage.ts,operations/write-batch.ts}`, `packages/health-commons/src/load.ts`, `packages/device-syncd/src/client.ts`, directly coupled tests only if required | `readJsonFile`, `parseAssessmentResponse`, `readStoredWriteOperation`, `readRecoverableStoredWriteOperation`, Health Commons JSON loaders, `parseJsonPayload` | completed | Narrow JSON-boundary cleanup only. Avoided `packages/device-syncd/src/store.ts`, preserved overlapping dirty-tree edits, and verified with package-local typecheck/coverage plus `pnpm test:smoke`; root `pnpm typecheck` is still red on an unrelated `packages/assistant-runtime/test/hosted-runtime-run-drain-coverage.test.ts` dirty-tree mismatch. |
| Codex | Remove high-signal unsafe TypeScript assertions in non-overlapping package-only slices covering typed SQLite reads, connector/runtime API boundaries, assistantd HTTP test fixtures, trivial typed initializer cleanups, and redundant `JSON.parse(... ) as unknown` seams |  | `packages/{gateway-local,query,device-syncd,inboxd,assistantd,importers,inbox-services,operator-config,assistant-engine,assistant-cli,setup-cli,runtime-state,core,health-commons}/**`, directly coupled tests only if required | sqlite row result typing helpers, typed connector/runtime boundaries, assistantd service/gateway test fixtures, typed array/object initializers, schema-parse JSON boundaries | in_progress | Broad cleanup split into disjoint owned slices. Avoid overlapping active hosted-runtime and hosted-run rows already dirty in this tree, and do not widen into unrelated apps/web or cloudflare work. |
| Codex | Ignore local `review-stage/` scratch artifacts and remove the current staged additions from the index without deleting local files |  | `.gitignore`, `review-stage/**` | `review-stage` ignore rule, staged scratch-artifact cleanup | completed | Narrow repo-config lane completed. Added the root ignore rule and removed only the current `review-stage/**` staged additions from the index while preserving local files and all other staged or dirty work. |
| Codex | Land the supplied Norwegian 4x4 Health Commons content patch and verify the touched `health-commons` slice only |  | `packages/health-commons/content/{protocols/norwegian-4x4,families/norwegian-4x4.md,biomarkers/estimated-vo2max.md,sources/norwegian-4x4,artifacts/norwegian-4x4,research-artifacts},packages/health-commons/scripts/download-norwegian-4x4-artifacts.sh,packages/health-commons/generated/**` | norwegian-4x4 protocol landing, estimated-vo2max biomarker, source-artifact/source manifest additions | in_progress | Narrow supplied patch landing only. Preserve unrelated dirty-tree edits, including existing staged `review-stage/**` additions and other active rows. |
| Codex | Land the supplied Norwegian 4x4 evidence metadata patch and verify the cited-source projection |  | `apps/web/src/lib/health-commons/experiment-detail.ts`, `apps/web/test/health-commons-experiment-detail-page.test.ts`, `packages/health-commons/content/sources/norwegian-4x4/**`, `packages/health-commons/src/**`, `packages/health-commons/generated/**` | Health Commons `researchEvidence`, Norwegian 4x4 evidence badges, coded participant rollup | completed | Applied and verified in the working tree. Left uncommitted because generated catalog/test diffs overlap active sauna/Bryan Johnson evidence edits in the same files; avoid staging another lane's work. |
| Codex | Fix assistant-engine acceptance fallout by updating only owned test expectations for signup notification and delivery-source metadata changes |  | `packages/assistant-engine/test/{assistant-notification-turn-runtime,assistant-service-runtime,outbound-channel-runtime,channel-helpers,assistant-channels-runtime}.test.ts` | signup notification send inputs, delivery-source transport metadata, assistant-engine test expectations | completed | Test-only lane for current assistant-engine fallout. Stayed within the five owned test files, preserved overlapping dirty edits in `assistant-service-runtime.test.ts`, and avoided production changes. |
| Codex | Raise `packages/query` browser snapshot coverage by adding only focused parser proof in the owned browser snapshot test file |  | `packages/query/test/browser-vault-snapshot.test.ts` | `parseBrowserVaultSnapshot`, browser snapshot field-level parsers | completed | Test-only `packages/query` lane for current browser snapshot parser fallout. Do not touch production query code unless the user explicitly widens scope; preserve overlapping dirty-tree edits. |
| Codex | Restore clean workspace build resolution for assistant runtime packages so `tsc -b` stops requiring prebuilt sibling `dist` declarations after a clean |  | `packages/{assistant-runtime,assistantd}/tsconfig.json` | `disableSourceOfProjectReferenceRedirect`, clean project-reference build | completed | Config-only fix landed and `pnpm build:workspace:clean` plus `pnpm release:check` now pass without requiring prebuilt sibling `dist` declarations. Preserve unrelated dirty-tree edits. |
| Codex | Restore apps/cloudflare verify-time workspace source resolution for `@murphai/vault-usecases/runtime` so release lanes stop failing on transitive inbox-services imports |  | `apps/cloudflare/vitest.shared.ts` | `cloudflareVitestAliases`, `@murphai/vault-usecases/runtime` | completed | Narrow alias fix landed and `pnpm --dir apps/cloudflare verify` plus `pnpm release:check` now pass. Preserve unrelated dirty-tree edits. |
| Codex | Fix the `@murphai/health-commons` public barrel and clean package build so release builds stop failing on duplicate CLI helper exports and missing clean emits |  | `packages/health-commons/{package.json,src/index.ts}` | `parseCliOptions` export collision, health-commons public barrel, clean build emit | completed | Narrow package fix landed; `pnpm --dir packages/health-commons build`, its coverage lane, and `pnpm release:check` now pass. Preserve unrelated dirty-tree edits. |
| Codex | Land the supplied red-light-glasses-before-bed Health Commons content patch and verify the touched `health-commons` slice only |  | `packages/health-commons/content/{protocols/red-light-glasses-before-bed,families/evening-light-reduction.md,biomarkers/sleep-onset-latency.md,sources/red-light-glasses-before-bed,artifacts/red-light-glasses-before-bed,research-artifacts},packages/health-commons/scripts/download-red-light-glasses-artifacts.sh,packages/health-commons/content/changes/2026-04.jsonl` | red-light-glasses-before-bed protocol landing, evening-light-reduction family, sleep-onset-latency biomarker, source-artifact/source manifest additions | completed | Narrow patch-landing lane landed with regenerated Health Commons catalogs. Preserved unrelated dirty-tree edits, including existing `review-stage/**` additions and other untouched files. |
| Codex | Dedupe the supplied red-light-glasses-before-bed pass-4 landing against the already-landed Health Commons slice and keep only net-new additions |  | `packages/health-commons/content/{protocols/red-light-glasses-before-bed/red-light-glasses-before-bed.md,families/evening-light-reduction.md,biomarkers/sleep-onset-latency.md,sources/red-light-glasses-before-bed,artifacts/red-light-glasses-before-bed/research-artifacts.json,changes/2026-04.jsonl},packages/health-commons/generated/*` | red-light-glasses-before-bed pass-4 dedupe, adjacent source-artifact additions, artifact-manifest/source-page reconciliation | completed | Dedupe landed as a narrow follow-up to the existing slice: kept only net-new source/artifact coverage, regenerated catalogs, and intentionally excluded ignored `output-packages/research/**`. Preserved unrelated dirty-tree edits, especially existing `review-stage/**` additions and untouched Health Commons work outside the red-light-glasses slice. |
| Codex | Land the supplied red-light-glasses research-landscape patch without widening beyond the cited red-light source pages, protocol landscape block, and directly coupled generated artifacts |  | `packages/health-commons/content/changes/2026-04.jsonl`, `packages/health-commons/content/protocols/red-light-glasses-before-bed/red-light-glasses-before-bed.md`, `packages/health-commons/content/sources/red-light-glasses-before-bed/*.md`, directly coupled `packages/health-commons/generated/**` only | red-light-glasses `researchLandscape`, source-level `protocolEvidence`, generated Health Commons catalog artifacts | in_progress | Watch-only Pro patch landing. The Norwegian schema/UI seam is already present; keep this lane content-only, preserve unrelated dirty-tree edits, and regenerate the coupled Health Commons outputs locally because the exported generated hunks no longer apply cleanly to HEAD. |
| Codex | Finish the assistant-cli hard cut by removing deprecated deep-import tolerance from workspace config and fixing the remaining `packages/cli/test` consumers | `agent-docs/exec-plans/active/2026-04-20-assistant-cli-hard-cut-tests.md` | `tsconfig.base.json`, `scripts/workspace-boundaries/import-policy-rules.mjs`, affected `packages/cli/test/**`, `packages/assistant-cli/test/**` only if needed | `@murphai/assistant-cli/assistant/*` path alias/tolerance, remaining CLI test imports | in_progress | Narrow hard-cut cleanup only. Do not widen assistant-cli exports, do not import sibling package src/dist paths from CLI tests, and preserve unrelated dirty-tree edits. |
| Codex | Add the still-missing local assistant runtime retention reductions in the owned assistant-engine write scope only | `agent-docs/exec-plans/active/2026-04-20-assistant-runtime-retention-reductions.md` | `packages/assistant-engine/src/assistant/runtime-budgets.ts`, `packages/assistant-engine/src/assistant/store/persistence.ts`, `packages/assistant-engine/src/assistant/outbox/store.ts`, `packages/assistant-engine/src/assistant/cron/store.ts`, directly coupled `packages/assistant-engine/test/**` only if required, `packages/operator-config/src/assistant-cli-contracts.ts` only if required | assistant transcript retention window, terminal outbox retention, cron run response/history retention | in_progress | Narrow maintenance-only lane. Do not touch `packages/assistant-engine/src/assistant/cron.ts` unless a minimal safe seam is unavoidable, and preserve overlapping dirty-tree edits in the broader privacy audit row. |
| Codex | Implement the still-missing device connection metadata minimization only within the owned provider slice |  | `packages/device-syncd/src/providers/{strava,oura,whoop}.ts`, `packages/device-syncd/src/provider-label.ts`, directly coupled `packages/device-syncd/test/**` only if required | provider connection `displayName` minimization, provider metadata minimization | in_progress | User-scoped `device-syncd` lane only. Preserve overlapping dirty-tree edits elsewhere in `packages/device-syncd/**`; keep reconciliation and webhook identifiers intact while removing profile PII retention that current package surfaces do not need. |
| Codex | Land only the still-missing hosted Cloudflare retention/privacy fixes for raw hosted email cleanup, lifecycle backstop, and browser-vault sidecar deletion | `agent-docs/exec-plans/active/2026-04-20-cloudflare-retention-slice.md` | `apps/cloudflare/src/hosted-email/worker-ingress.ts`, `apps/cloudflare/src/hosted-email.ts` if needed, `apps/cloudflare/src/browser-vault-store.ts`, `apps/cloudflare/src/user-runner/runner-run-processor.ts`, directly coupled `apps/cloudflare/test/**` only if required, `apps/cloudflare/r2-bundles-lifecycle.json`, directly coupled `apps/cloudflare/{README.md,DEPLOY.md}` only if required | hosted email append-failure raw cleanup, hosted-email R2 retention backstop, browser-vault snapshot sidecar deletion | in_progress | Narrow Cloudflare-only lane requested by the user. Preserve overlapping dirty-tree edits in hosted-email log redaction and other active runner work; do not broaden into the browser-vault projection redesign or unrelated wake/runtime refactors. |
| Codex | Bump `@cobuild/review-gpt` to `0.5.72` in the workspace root and add only the version-scoped pnpm release-age exception needed for that hotfix |  | `pnpm-workspace.yaml`, `package.json`, `pnpm-lock.yaml` | `minimumReleaseAgeExclude`, root `@cobuild/review-gpt` devDependency | completed | Dependency-update lane completed with the published `0.5.72` release while preserving the large hosted/runtime dirty-tree work already in flight elsewhere. |
| Codex | Verify whether the deprecated hosted-local proxy env scrub is already gone, and if not, remove only the stale deletes for the two removed env vars plus the matching focused test expectations |  | `scripts/dev-hosted-local/environment.ts`, `scripts/dev-hosted-local/environment.test.ts` | `stripDeprecatedHostedLocalProxyEnv`, `HOSTED_EXECUTION_INTERNAL_PROXY_UPSTREAM_BASE_URL`, `HOSTED_EXECUTION_LOCAL_LOOPBACK_PROXY_TOKEN` | completed | Worker-4 narrow lane. Preserved the existing `HOSTED_EXECUTION_RUNNER_HOST_ALIAS` behavior and avoided broader hosted-local stack changes. Coordinated with the overlapping hard-cut audit row already covering the same owner files. |
| Codex | Close the verified JSONL import-lock race and agent-session token exposure, and only touch overlapping hosted security-followup files if residual gaps remain after inspection | `agent-docs/exec-plans/active/2026-04-20-import-lock-and-agent-session-hardening.md` | `packages/core/src/mutations.ts`, focused `packages/core/test/**`, `apps/web/app/api/device-sync/agent/connections/[connectionId]/{export-token-bundle,refresh-token-bundle}/route.ts`, `apps/web/src/lib/device-sync/{agent-session-service.ts,agent-session-token-bundle.ts}`, focused `apps/web/test/{agent-session-service,prisma-store-agent-session}.test.ts`, overlapping dirty hosted security-followup files only if needed | `buildJsonlAppendPlan`, `importSamples`, `importDeviceBatch`, `HostedDeviceSyncAgentSessionService.exportTokenBundle`, `HostedDeviceSyncAgentSessionService.refreshTokenBundle`, agent-session bearer rotation/export shape | in_progress | Primary ownership is issue 1 and issue 3. Coordinate carefully with the existing `2026-04-20-thread-security-followups.md` row before editing hosted-run/email/smoke files that are already dirty in this worktree. Preserve unrelated dirty-tree edits. |
| Codex | Rename `HostedRun.wakeIdsJson` to `ingressEventIdsJson` so the run-owned field stops using wake-shaped terminology |  | `apps/web/prisma/schema.prisma`, `apps/web/src/lib/hosted-run/store.ts`, `apps/web/test/hosted-run-store.test.ts` | `HostedRun.ingressEventIdsJson`, hosted-run store projection/read helpers | completed | Narrow hosted-run naming cleanup only. Landed without a migration; the underlying `wake_ids_json` column mapping stays unchanged. Focused hosted-web diff verification passed, while repo-wide `pnpm typecheck` remains blocked by an unrelated pre-existing syntax error in `apps/cloudflare/src/user-runner/runner-run-processor.ts`. |
| Codex | Rename the hosted runtime pre-web-commit phase from `committed` to `prepared` so the run lifecycle names match the actual ownership boundary |  | `packages/assistant-runtime/src/{hosted-runtime,models}.ts`, `packages/assistant-runtime/test/hosted-runtime-runner.test.ts`, `apps/cloudflare/src/{node-runner-isolated,user-runner/runner-run-processor}.ts`, directly coupled Cloudflare worker/node-runner tests only if required | `HostedAssistantRuntimeJobPreparedResult`, hosted runtime pre-web-commit phase discriminant, prepared-snapshot guards | completed | Working-tree rename landed across assistant-runtime and the active Cloudflare split files, with focused `assistant-runtime` and Cloudflare node-runner tests passing. No scoped commit was created because the tracked Cloudflare processor/tests are being replaced by overlapping dirty-tree add/delete work, so committing only this rename would not have represented the real current consumer path. |
| Codex | Make hosted run-drain commits always take the explicit finalize path instead of inferring finalize only from assistant-delivery effects |  | `apps/cloudflare/src/user-runner/runner-wake-processor.ts`, focused `apps/cloudflare/test/runner-wake-processor.test.ts` | `RunnerWakeProcessor.executeRunDrain`, `finalizeRequired` | completed | Committed-phase run drains now always request explicit finalize, and focused Cloudflare proof passed (`runner-wake-processor` Vitest slice plus repo `pnpm typecheck`). The diff-aware `apps/cloudflare verify` wrapper still hit unrelated lock/process issues in this dirty tree after the relevant Node lane passed. |
| Codex | Remove narrow wake-shaped residue in Cloudflare `user-runner` where the write scope stays local to run-drain cleanup |  | `apps/cloudflare/src/user-runner.ts`, directly coupled `apps/cloudflare/test/**` only if required | `cleanupCommittedHostedWakesLocally`, local run-drain wake residue, run-centric helper naming | completed | Narrow Cloudflare cleanup landed in `user-runner.ts` only. Focused verification was attempted, but broader `apps/cloudflare` checks remain blocked by pre-existing `node-runner` type drift and the current dirty-tree finalize-release seam in `user-runner` tests. |
| Codex | Fix the Cloudflare-side hosted-run finalize ownership bypass and make `nudgeHostedRun` a real immediate-alarm seam |  | `apps/cloudflare/src/{user-runner,index,hosted-email/worker-ingress}.ts`, focused `apps/cloudflare/test/{user-runner-resume-finalize,index,hosted-email-worker-ingress}.test.ts` | `prepareAndCommitAcquiredHostedRun`, `finalizeAcquiredHostedRun`, `nudgeHostedRun`, `handleRunRoute`, hosted email post-ingress nudge | completed | Cloudflare now reacquires before finalize, releases retryable finalize failures back to web-owned recovery, and `nudgeHostedRun` remains the immediate-alarm seam. Focused finalize/nudge verification passed; broader `apps/cloudflare` typecheck is still blocked by pre-existing `test/node-runner.test.ts` runtime-timer typing drift. |
| Codex | Remove live `assistant.cron.tick` support from shared hosted-execution parsing and source-of-truth docs | `agent-docs/exec-plans/active/2026-04-20-remove-hosted-cron-tick.md` | `agent-docs/references/hosted-run-protocol.md`, `docs/hosted-hard-cut-migration-guide.md`, `apps/web/src/lib/hosted-ingress/queue.ts`, `apps/web/test/hosted-ingress-queue.test.ts`, `packages/hosted-execution/src/parsers.ts`, directly coupled `packages/hosted-execution/test/**` | `parseHostedRuntimeEvent`, hosted ingress rejection wording, hosted run protocol cron-tick wording | in_progress | Narrow shared-contract/docs lane only. Preserve overlapping Cloudflare hosted-run work and do not edit immutable completed plans even though they still mention historical `assistant.cron.tick` state. |
| Codex | Fence resumable hosted-run finalize acquisition so overlapping acquires cannot stomp the finalize token | `agent-docs/exec-plans/active/2026-04-20-hosted-run-resume-finalize-fencing.md` | `apps/web/src/lib/hosted-run/store.ts`, focused `apps/web` hosted-run tests, directly coupled shared hosted-run status contract/parsers only if required | `acquireHostedRunTx`, `finalizeHostedRunTx`, resumable finalize claim/recovery state | completed | Web-owned finalize recovery is fenced in two stages now: resumable acquires claim `committed_needs_finalize -> finalizing`, and retryable finalize failures can release `finalizing -> committed_needs_finalize` through the new release-finalize contract/route. Focused `apps/web` store verification and shared contract typecheck/tests passed. |
| Codex | Add coverage proofs for new assistant-runtime hosted logging instrumentation |  | `packages/assistant-runtime/test/**` | hosted runtime execution, typing, and callback log assertions | completed | Focused hosted-runtime logging proof landed in `packages/assistant-runtime/test/hosted-runtime-runner.test.ts`; owner coverage and `pnpm release:check` now pass. Do not touch production files or overlap the hosted runtime behavior patches. |
| Codex | Fix inbox attachment parsing/inspection clarity plus explicit recurring-food unschedule path |  | `packages/inboxd/**`, `packages/assistant-engine/src/inbox-*`, `packages/assistant-engine/test/**`, `packages/cli/src/commands/{food,inbox}.ts`, `packages/cli/test/**`, `docs/contracts/03-command-surface.md` | `processCapture`, `enqueueAttachmentParseJobs`, inbox attachment commands, `registerFoodCommands` | in_progress | Cross-package operator/runtime lane. Avoid touching assistant CLI bootstrap refresh work owned separately by this task exclusion. |
| Codex | Land supplied hosted final-shape cleanup patch for hosted execution/webhook dispatch seams |  | `apps/cloudflare/src/{dispatch-payload-store,user-runner}.ts`, `apps/web/src/lib/hosted-execution/{outbox,outbox-payload}.ts`, `apps/web/src/lib/hosted-onboarding/{webhook-dispatch-payload,webhook-receipt-store}.ts` | `HostedDispatchPayloadStore`, `HostedUserRunner`, hosted execution outbox payload helpers, hosted webhook dispatch payload helpers | completed | Narrow supplied patch landing. Avoid unrelated hosted web or Cloudflare edits, and preserve the pre-existing `apps/web/prisma.config.ts` modification. |
| Codex | Suppress phone-only Privy restart copy when an alternate hosted auth method is active |  | `apps/web/src/components/hosted-onboarding/{hosted-auth-panel,hosted-phone-auth,hosted-phone-auth-controller}.tsx`, `apps/web/test/{hosted-auth-panel,hosted-phone-auth}.test.ts` | `HostedAuthPanel`, `HostedPhoneAuth`, `useHostedPhoneAuthController` | completed | Narrow apps/web UX fix for Telegram/email auth. Preserve existing Privy server identity rules and avoid unrelated onboarding copy changes. |
| Codex | Reconcile hosted Privy auth completion so email, Telegram, and SMS share one client-session contract and SMS wallet provisioning uses fresh user state | `agent-docs/exec-plans/active/2026-04-16-phone-wallet-refresh.md` | `apps/web/src/components/hosted-onboarding/{hosted-auth-completion,hosted-email-auth-button,hosted-telegram-auth-button,hosted-phone-auth-controller,hosted-phone-auth-support}.ts*`, `apps/web/test/{hosted-phone-auth,homepage-privy-auth}.test.ts` | `completeHostedPrivyAuth`, `HostedPrivyClientSessionInput`, `useHostedPhoneAuthController`, `finalizeHostedPrivyVerification` | in_progress | Narrow hosted onboarding auth-architecture cleanup. Avoid unrelated onboarding copy, routing, and server-side identity changes. |
| Codex | Fix hosted assistant rapid-turn continuity by bundling session indexes, hydrating hosted default targets before first-contact delivery, and resolving direct-thread restores across rapid turns |  | `packages/runtime-state/src/assistant-local-state-descriptors.ts`, `packages/runtime-state/test/hosted-bundle.test.ts`, `packages/runtime-state/README.md`, `packages/assistant-engine/src/assistant/{conversation-policy,execution-context,first-contact-welcome-delivery,local-service,notification-turn,store,turn-plan}.ts`, `packages/assistant-engine/src/assistant/store/{paths,persistence}.ts`, `packages/assistant-engine/test/**`, `packages/assistant-runtime/src/hosted-runtime/{context,events,execution}.ts`, `packages/assistant-runtime/test/**`, `apps/cloudflare/test/hosted-local-*.test.ts` | `assistantLocalStateDescriptors`, `readAssistantIndexStore`, `resolveAssistantExecutionOperatorDefaults`, `resolveAssistantSession`, `buildAssistantConversationPolicy`, `hydrateHostedExecutionDefaultTarget`, hosted local rapid-turn E2E | completed | Hosted assistant continuity lane. Root cause was same-activation hosted dispatches entering first-contact delivery before hosted default-target bootstrap completed; keep restore fallback for older bundles and preserve local behavior outside hosted execution. |
| Codex | Make hosted invite-status polling single-flight so slow refreshes stop overlapping and amplifying DB load |  | `apps/web/src/components/hosted-onboarding/invite-status-client.ts`, focused `apps/web/test/join-invite-status-client.test.ts`, `apps/web/test/route-loading.test.tsx` | `useHostedInviteStatusRefresh`, `fetchHostedInviteStatus` | completed | Narrow polling-only lane plus stale loading-test cleanup so scoped verification is not blocked by the removed join loading route. Do not change auth, invite-service, or broader onboarding flow semantics in this slice. |
| Codex | Remove stale hosted-web test expectations for deleted loading routes and fire-and-forget hosted run nudges so acceptance matches the current less-coupled wake contract |  | `apps/web/test/{route-loading,hosted-onboarding-billing-success-service,device-sync-hosted-wake,hosted-share-service,settings-email-sync-route,settings-phone-sync-route,settings-telegram-sync-route}.test.ts*` | removed loading-route expectation, `nudgeHostedRunBestEffort` test doubles, hosted wake assertions | in_progress | Test-only acceptance cleanup. Do not reintroduce loading pages or restore event-id/prisma nudge inputs in production code. Preserve unrelated dirty-tree edits. |
| Codex | Restore hosted web Vercel builds by committing the Prisma client link helper referenced by the build script |  | `apps/web/scripts/ensure-prisma-client-link.ts` | `ensureHostedWebPrismaClientLink`, `resolveHostedWebPrismaClientLinkPaths` | in_progress | Narrow deploy-fix lane. Do not touch unrelated hosted onboarding, release, or webhook work. |
| Codex | Fix apps/web hosted hard-cut test fallout from dispatch-boundary removal |  | `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts`, directly related hosted wake web tests/helpers only | hosted onboarding webhook idempotency, hosted wake test fixtures/builders | in_progress | Test-only apps/web lane for wake-first hard-cut fallout. Do not touch packages/assistant-runtime, packages/hosted-execution, or apps/cloudflare. Preserve unrelated apps/web edits already in flight. |
| Codex | Add a stable Privy logo asset under hosted web public files |  | `apps/web/public/privy-logo.png` | static hosted logo asset | completed | Narrow asset-only lane for Privy login branding. Avoid overlapping existing homepage/onboarding work in `apps/web/src/**`. |
| Codex | Ignore generated `.next-smoke*` directories in review-gpt and repo hygiene tooling so hosted smoke residue does not break audit packaging |  | `scripts/check-no-js.ts`, `scripts/check-no-js.test.ts`, `scripts/verify-workspace-boundaries.mjs` | `shouldSkipSourceArtifactDirectory`, `isBlockedTrackedArtifactPath`, `isWorkspaceBuildArtifactPath`, `shouldSkipDirectory` | completed | Narrow tooling-only lane. Avoid unrelated hosted app/runtime changes and preserve pre-existing `apps/web/**` worktree edits. |
| Codex | Delete unused review-gpt preset prompt files for incentives and grief vectors |  | `scripts/chatgpt-review-presets/{grief-vectors,incentives}.md` | review-gpt preset prompt files | completed | Narrow docs/tooling cleanup lane. Avoid changing preset registry behavior or other prompt files. |
| Codex | Update review-gpt preset prompts to stop requesting returned patch attachments |  | `scripts/review-gpt-delay.sh`, `scripts/chatgpt-review-presets/*.md` | delayed review-gpt follow-up prompt defaults, preset final response contracts | completed | Narrow review-gpt tooling/doc wording lane. Avoid changing preset registry behavior or unrelated review-gpt runtime flows. |
| Codex | Finish hosted Linq active-member receipt cutover and collapse hosted conversation wake capture-builder re-entry |  | `apps/web/src/lib/hosted-onboarding/{webhook-service,webhook-provider-linq,webhook-transport}.ts`, `apps/web/test/hosted-onboarding-linq*.test.ts`, `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts`, `packages/assistant-runtime/src/hosted-runtime/events/{conversation,email,linq,telegram}.ts`, `packages/assistant-runtime/test/**` | `handleHostedOnboardingLinqWebhook`, active-member Linq direct planning/drain, `ingestHostedConversationMessageWake` | in_progress | Cross-slice hosted hard-cut lane. Preserve unrelated hosted onboarding auth/pricing work in `apps/web` and avoid Cloudflare/bundle recovery changes reserved for later discussion. |
| Codex | Remove webhook-shaped runtime parsing from hosted conversation.message Linq ingestion | `agent-docs/exec-plans/active/2026-04-18-hosted-conversation-wake-cleanup.md` | `packages/assistant-runtime/src/hosted-runtime/events/**`, `packages/inboxd/src/connectors/linq/**`, focused `packages/{assistant-runtime,inboxd}/test/**` | hosted conversation wake Linq adapter, `ingestHostedConversationMessageWake` | in_progress | Narrow package-only lane. Do not touch `apps/web/**` or `apps/cloudflare/**`; preserve the broader hosted Linq/webhook lane already in flight. |
| Codex | Replace weak `{}` assistant-delivery test stub types with the real hosted delivery record type |  | `packages/assistant-runtime/test/{hosted-runtime-environment,hosted-runtime-entry-execution,hosted-runtime-execution,hosted-email-subject,hosted-runtime-email-event}.test.ts` | `writeAssistantDeliveryRecord`, hosted runtime effects-port test stubs | completed | Narrow assistant-runtime test-only cleanup. Avoid production runtime changes and preserve unrelated in-flight hosted/runtime work. |
| Codex | Prove the hosted-local Linq later-reply drain path persists the follow-up assistant turn after inbound message wake processing |  | `apps/cloudflare/test/user-runner.test.ts`, targeted hosted-local Linq e2e verification only | `HostedUserRunner.wakeHostedWakesInternal`, hosted-local Linq later-reply regression proof | completed | Added a focused regression test for between-round assistant wake rematerialization and verified fresh hosted-local Linq e2e reruns now persist the second session turn/outbox intent. No further runtime edits in this lane. |
| Codex | Replace hosted wake drain RPC semantics with a fire-and-forget nudge contract while preserving DO-local drain, alarm, and repair correctness |  | `apps/web/src/lib/hosted-{wake/control,onboarding/webhook-service-wake}.ts`, `apps/web/app/api/internal/hosted-wake/repair/route.ts`, `apps/web/app/api/hosted-onboarding/linq/webhook/route.ts`, `apps/cloudflare/src/{index,hosted-email/worker-ingress,worker-routes/shared,user-runner}.ts`, `packages/{hosted-execution,cloudflare-hosted-control}/src/**`, focused hosted wake tests under `apps/{web,cloudflare}/test/**` and package tests | `triggerHostedWakeUser*`, `maybeHandoffHostedExecutionWebhookWake`, `/internal/users/:userId/wake`, `UserRunnerDurableObject.wakeHostedWakes`, hosted wake nudge result contracts | completed | Cross-surface hosted wake control refactor. Focused `apps/web` and `apps/cloudflare` typecheck/test slices passed; preserve unrelated dirty `apps/cloudflare/src/container-entrypoint.ts` and its tests plus other hosted-wake lanes still in flight. |
| Codex | Simplify the experiment protocol card layout on the hosted experiment detail page without changing protocol data or schema surfaces |  | `apps/web/src/components/experiments/experiment-detail/protocol-tab.tsx`, directly coupled `apps/web/test/**` only if required | protocol summary layout hierarchy, experiment detail protocol card readability | completed | Landed the clearer two-column protocol layout, removed duplicate lead copy and generic `STEP N` labels, and verified the live `localhost:3000` experiment routes plus focused `apps/web` tests. Preserved overlapping dirty protocol data/schema work. |
| Codex | Land the supplied experiment protocol signal hierarchy patch without disturbing the active source-label or protocol-layout work |  | `apps/web/src/components/experiments/experiment-detail/protocol-tab.tsx`, `apps/web/src/lib/health-commons/experiment-detail.ts`, `apps/web/src/types/experiments.ts`, directly coupled `apps/web/test/experiment-detail-protocol-tab.test.tsx` only if required | expected signal prominence, protocol-tab "What could change" hierarchy, red-light-glasses signal emphasis | in_progress | Narrow supplied patch landing only. Preserve overlapping active experiment-detail rows, keep unrelated dirty-tree edits intact, and avoid broader Health Commons schema/content changes. |
| Codex | Tighten Norwegian 4x4 protocol steps so they describe real-world session actions instead of repeated plan metadata |  | `packages/health-commons/content/protocols/norwegian-4x4/norwegian-4x4.md`, `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}` | Norwegian 4x4 `protocol.steps`, cleaner-read spacing guidance | completed | Narrow copy cleanup requested by the user. Reworked the step list into session actions, moved 48-hour spacing to cleaner-read guidance, preserved test-plan/frequency/duration fields, regenerated catalog outputs, and avoided unrelated Norwegian 4x4 source corpus changes. |
| Codex | Tighten remaining Health Commons experiment protocol steps and descriptions so they describe real-world actions instead of repeated plan metadata |  | `packages/health-commons/content/protocols/{dry-sauna/bryan-johnson-blueprint.md,dry-sauna/murph-finnish-standard-3x-week.md,norwegian-4x4/norwegian-4x4.md,red-light-glasses-before-bed/red-light-glasses-before-bed.md,sauna-protocol.md}`, `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}` | protocol summaries, remaining protocol `steps`, sauna chooser option copy, generated Health Commons catalog copy | completed | Narrow copy-only cleanup requested by the user. Reworked summaries and remaining step lists into plain-language actions, kept dose/frequency/test-plan fields as the structured source of schedule metadata, preserved safety guidance, regenerated catalog outputs, and avoided unrelated source/evidence corpus edits. |
| Codex | Remove redundant expected-signal helper copy from experiment protocol tab |  | `apps/web/src/components/experiments/experiment-detail/protocol-tab.tsx`, directly coupled `apps/web/test/**` only if required | protocol tab expected-signal intro copy | completed | Removed only the helper paragraph under "What could change"; preserved the expected-signal cards and context pills. Typecheck passed; app verify remains blocked by unrelated dirty `StudyCard` expectation fallout. |
| Codex | Remove redundant baseline-stability copy from Murph-owned experiment protocol steps |  | `packages/health-commons/content/protocols/{dry-sauna/murph-finnish-standard-3x-week.md,norwegian-4x4/norwegian-4x4.md,red-light-glasses-before-bed/red-light-glasses-before-bed.md}`, `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}` | protocol `steps` baseline-stability copy, generated Health Commons catalog step text | completed | Narrow copy-only cleanup requested by the user. Removed the redundant baseline-stability step from the three Murph-owned protocols, regenerated the catalog, preserved baseline/test-plan fields plus analysis notes, and left overlapping generated catalog changes uncommitted for the active Health Commons rows. |
| Codex | Rename the Bryan Johnson sauna protocol title to the shorter user-facing experiment name without widening the protocol-content slice |  | `packages/health-commons/content/protocols/dry-sauna/bryan-johnson-blueprint.md`, `packages/health-commons/content/protocols/sauna-protocol.md`, directly coupled `packages/health-commons/generated/{catalog.hash,catalog.json,entities.ndjson,recent-changes.json}`, directly coupled `apps/web/test/health-commons-experiment-detail-page.test.ts` only if required | Bryan Johnson sauna title copy, protocol chooser label, generated catalog title surfaces | completed | Renamed the protocol and chooser label to `Bryan Johnson Sauna`, regenerated only the intended catalog outputs from a temporary HEAD-based content snapshot to avoid unrelated dirty Bryan Johnson source edits, and added standalone title coverage in `apps/web/test/health-commons-bryan-johnson-protocol.test.ts`. Preserved slug/key/route ids. |
| Codex | Pin dry sauna and Norwegian 4x4 as the `/experiments` featured cards instead of featuring the Bryan Johnson sauna protocol | `agent-docs/exec-plans/completed/2026-04-21-experiments-featured-cards.md` | `apps/web/app/(dashboard)/experiments/experiments-page-client.tsx`, `apps/web/test/browser-vault-dashboard-pages.test.tsx`, plan/ledger only | `selectFeaturedCards`, `/experiments` featured section copy and assertions | completed | Pinned Finnish Dry Sauna and Norwegian 4x4 by route id, excluded Bryan Johnson from featured fallback cards, updated featured copy, and added a focused rendered-page assertion. Preserved unrelated dirty Health Commons content/generated files and existing experiment-detail page edits. |
| Codex | Remove decorative quote punctuation from experiment expert cards in the narrow hosted experiment-detail UI slice only |  | `apps/web/src/components/experiments/experiment-detail/expert-card.tsx`, directly coupled `apps/web/test/{expert-card,health-commons-experiment-experts}.test.ts*` only if required | expert-card quote punctuation removal | completed | No additional code change was required in this pass because the current branch head already rendered expert quotes without decorative punctuation. Confirmed on the live Bryan Johnson experiment route at `localhost:3000`. |
| Codex | Sort experiment research sources by year as the secondary key after participant count in the narrow hosted experiment-detail projection only |  | `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/health-commons-experiment-detail-page.test.ts` only if required | `sortStudySourcesForDisplay`, experiment-detail research source ordering | completed | Participant count remains primary, publication year now wins equal-`n` ties, and the focused Bryan Johnson page coverage plus scoped `apps/web` verification/typecheck are green. Preserved overlapping experiment-detail quote/profile-image work in the same production file. |
| Codex | Show experiment study descriptions before author metadata in the narrow hosted experiment-detail study-card slice only |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, directly coupled `apps/web/test/study-card.test.ts` only if required | `StudyCard` finding/metadata hierarchy | completed | Rendered the study finding before the author/journal line, promoted the finding to body-like text, strengthened focused `StudyCard` assertions for order plus class hierarchy, and verified with `pnpm --dir apps/web typecheck`, focused Vitest, and an isolated `localhost:3001` render check. The broader `pnpm test:diff ...` lane is still red on unrelated `health-commons-experiment-detail-page` and `vault-sync-session-service` expectations already dirty in this tree. |
| Codex | Remove approximate-marker participant badges from the narrow hosted experiment-detail study-card slice only |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, directly coupled `apps/web/test/study-card.test.ts` only if required | `StudyCard` participant label formatting | completed | Restored `StudyCard`, kept the description-first hierarchy intact, changed participant badges to plain `n=` formatting for approximate counts too, and verified with focused Vitest, `pnpm --dir apps/web typecheck`, and an isolated `localhost:3001/experiments/finnish-sauna` render showing `n=2,138`. The broader `pnpm test:diff ...` lane is still red on the unrelated pre-existing `apps/web/test/experiment-header.test.ts` baseline/protocol expectation. |
| Codex | Enable hosted-web Turbopack dev filesystem cache by default while preserving an explicit opt-out seam |  | `apps/web/next-artifacts.ts`, directly coupled `apps/web/test/next-config.test.ts` only if required | `isHostedWebDevFileSystemCacheEnabled`, `buildHostedWebNextConfig`, Turbopack dev cache default | completed | Defaulted the hosted-web Turbopack dev filesystem cache on while preserving explicit `0`/`false`/`no` opt-out handling, added focused config coverage, and verified with focused Vitest, `pnpm --dir apps/web typecheck`, plus an isolated dev boot that created `apps/web/.next-dev-codex-cache-enabled-2/dev/cache/turbopack`. The broader `pnpm test:diff ...` lane is still red on the unrelated pre-existing `apps/web/test/experiment-header.test.ts` baseline/protocol expectation. |
| Codex | Remove low-value counter copy from the `/experiments` page header and tighten the browser-vault intro sentence |  | `apps/web/app/(dashboard)/experiments/experiments-page-client.tsx`, directly coupled `apps/web/test/browser-vault-dashboard-pages.test.tsx` only | `ExperimentsPageContent`, `/experiments` header and featured copy | in_progress | Narrow hosted-web copy/UI cleanup only. Preserve overlapping dirty-tree work elsewhere in `apps/web` and avoid changing experiment card behavior or data wiring. |
| Codex | Swap the Norwegian 4x4 experiment artwork to the newest Downloads image without widening into page copy or protocol-layout work |  | `apps/web/public/design-assets/hero-norwegian-4x4.jpeg`, `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/**` only if required | Norwegian 4x4 experiment image mapping | completed | Narrow hosted-web asset and mapping lane only. Added the dedicated Norwegian 4x4 asset, pinned the protocol image resolver to it, added focused metadata coverage, and verified with `pnpm --dir apps/web typecheck`, focused `vitest` for `health-commons-bryan-johnson-protocol.test.ts`, and `git diff --check`. The broader `pnpm test:diff ...` lane remains red on the unrelated pre-existing `apps/web/test/experiment-header.test.ts` baseline/protocol expectation. |
| Codex | Swap the red-light-glasses-before-bed experiment artwork to the newest Downloads image without widening into page copy or protocol-layout work |  | `apps/web/public/design-assets/hero-red-light-glasses-before-bed.jpeg`, `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/**` only if required | red-light-glasses experiment image mapping | completed | Narrow hosted-web asset and mapping lane only. Added the dedicated red-light asset, pinned the protocol image resolver to it, added focused metadata coverage, and verified with `pnpm --dir apps/web typecheck`, focused `vitest` for `health-commons-bryan-johnson-protocol.test.ts`, and `git diff --check`. |
| Codex | Swap the Bryan Johnson sauna experiment artwork to the newest Downloads image without widening into page copy or protocol-layout work |  | `apps/web/public/design-assets/hero-bryan-johnson-sauna.jpg`, `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/**` only if required | Bryan Johnson sauna experiment image mapping | completed | Narrow hosted-web asset and mapping lane only. Added the dedicated Bryan Johnson asset, pinned the protocol image resolver to it, added focused metadata coverage, and verified with `pnpm --dir apps/web typecheck`, focused `vitest` for `health-commons-bryan-johnson-protocol.test.ts`, and `git diff --check`. |
| Codex | Swap the Finnish dry sauna experiment artwork to the newest Downloads image without widening into page copy or protocol-layout work |  | `apps/web/public/design-assets/hero-finnish-sauna.jpeg`, `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/**` only if required | Finnish dry sauna experiment image mapping | completed | Narrow hosted-web asset and mapping lane only. Added the dedicated Finnish sauna asset, pinned the protocol image resolver to it while keeping the generic sauna fallback intact, added focused metadata coverage, and verified with `pnpm --dir apps/web typecheck`, focused `vitest` for `health-commons-bryan-johnson-protocol.test.ts`, and `git diff --check`. |
| Codex | Replace the experiment artwork resolver `if` chain with a single route-id mapping while keeping app-local artwork ownership and existing fallbacks intact |  | `apps/web/src/lib/health-commons/experiment-detail.ts`, directly coupled `apps/web/test/health-commons-bryan-johnson-protocol.test.ts` only if required | experiment artwork route-id mapping, fallback image inference | completed | Narrow hosted-web architecture cleanup only. Replaced the growing route-specific branch chain with one app-local route-id image map plus the existing category-based fallback helper, and verified with `pnpm --dir apps/web typecheck`, focused `vitest` for `health-commons-bryan-johnson-protocol.test.ts`, and `git diff --check`. |
| Codex | Land the downloaded hosted AI usage billing wake patch without widening beyond the hosted onboarding billing/env/runtime/test slice |  | `apps/web/.env.example`, `apps/web/README.md`, `apps/web/src/lib/hosted-onboarding/{billing-plans,billing-service,env,invite-service,runtime}.ts`, directly coupled `apps/web/test/{hosted-onboarding-billing-plans,hosted-onboarding-billing-service,hosted-onboarding-env,hosted-onboarding-privy-invite-status,hosted-onboarding-csrf}.test.ts` only | hosted billing plan Stripe price pairing, usage-meter readiness gating, checkout line items | completed | Wake-thread patch landing only. Added the metered usage-price/env gating and checkout line-item pairing, preserved the existing pricing tests while extending coverage, verified with `pnpm --dir apps/web typecheck`, direct `pnpm exec vitest run ... --config apps/web/vitest.config.ts --no-coverage` for the affected hosted-onboarding tests, and `git diff --check`. The truthful `bash scripts/workspace-verify.sh test:diff ...` lane remains blocked only by the unrelated pre-existing `apps/web/test/experiment-header.test.ts` expectation from the separate in-progress experiments-header row. Recursive same-thread follow-up send succeeded and armed the depth-0 wake. |
| Codex | Drain hosted runtime-local assistant and device-sync follow-up work in-process until idle or bounded budget before snapshot | `agent-docs/exec-plans/active/2026-04-20-hosted-run-drain-budget.md` | `packages/assistant-runtime/src/hosted-runtime/execution.ts`, direct `packages/assistant-runtime/test/**` coverage for run-drain execution only | `executeHostedRunDrainForCommit`, hosted immediate maintenance drain budget, assistant/device-sync due-state draining | in_progress | Narrow assistant-runtime execution lane only. Do not broaden into Cloudflare/web acquire-commit flow or reintroduce any web-visible internal wake materialization. Preserve overlapping in-flight assistant-runtime test/runtime edits outside the direct run-drain coverage surface. |
| Codex | Stop the Cloudflare Telegram attachment hydration test from treating the hosted typing-indicator request as an unexpected fetch |  | `apps/cloudflare/test/node-runner.test.ts` | hosted Telegram attachment hydration fetch stub, typing-indicator test seam | completed | Narrow Cloudflare test-only lane completed. Added the missing `sendChatAction` fetch stub/assertion, kept production behavior unchanged, and verified with `pnpm --dir apps/cloudflare test -- --runInBand test/node-runner.test.ts -t "hydrates hosted Telegram attachment bytes when runner Telegram env is present"` plus `pnpm --dir apps/cloudflare verify`. The scoped `bash scripts/workspace-verify.sh test:diff apps/cloudflare/test/node-runner.test.ts` wrapper still ends with a pre-existing shell quote parse error after the Cloudflare verify step succeeds. |
| Codex | Land the supplied hosted cold-start typing and nudge ordering patch without widening beyond the current assistant-runtime, assistant-engine channel typing, and directly coupled Cloudflare run-ingress slices |  | `packages/assistant-runtime/src/hosted-runtime/{events,typing}.ts`, `packages/assistant-runtime/test/hosted-runtime-{typing,notification}.test.ts`, `packages/assistant-engine/src/assistant/channels/{runtime,types}.ts`, `apps/cloudflare/src/{hosted-email/worker-ingress,index,user-runner}.ts`, directly coupled focused tests only if required | hosted typing ownership confirmation, Linq typing refresh, first-contact notification skip semantics, nudge-before-drain ordering | in_progress | Supplied patch landing only. Preserve unrelated dirty-tree work, especially active Health Commons and hosted-web rows. Port onto current file layout instead of replaying stale hunks where adjacent code has moved. |
| Codex | Land the hosted typing follow-up review patch so Telegram typing start respects the hosted abort deadline and Linq refresh cleanup stops leaking linked abort listeners |  | `packages/assistant-runtime/src/hosted-runtime/typing.ts`, `packages/assistant-engine/src/assistant/channels/runtime.ts`, `packages/operator-config/src/telegram-runtime.ts`, directly coupled focused tests only if required | hosted typing abort propagation, Telegram start abortability, Linq typing cleanup | in_progress | Narrow supplied follow-up patch lane. Overlaps the active hosted typing row on `typing.ts` and channel runtime wiring; preserve unrelated nudge-ordering work and commit only the abort/cleanup slice. |
| Codex | Land the stale Norwegian 4x4 research-improvements patch against current HEAD without widening beyond the requested study-card and Norwegian evidence/source copy refresh |  | `apps/web/src/components/experiments/experiment-detail/study-card.tsx`, `apps/web/src/lib/health-commons/experiment-detail.ts`, `packages/health-commons/content/{families/norwegian-4x4.md,protocols/norwegian-4x4/norwegian-4x4.md,sources/norwegian-4x4/**}` | Norwegian 4x4 study-card context/fallback copy, family/protocol evidence framing, source-card readability refresh | completed | Narrow supplied-patch lane completed. Preserved the overlapping Norwegian onboarding and broader data rows by staging only the non-overlapping study-card, fallback, and authored Norwegian source/protocol/family copy slice; left shared generated catalog artifacts and unrelated shared-file churn for the coordinated data landing. Verified with `pnpm --dir apps/web typecheck`, focused `apps/web/test/study-card.test.ts`, `pnpm test:smoke`, and `git diff --check`. `pnpm --dir packages/health-commons verify` now fails only on the expected stale generated artifacts because the scoped landing intentionally excludes overlapping generated churn. |
| Codex | Forward privacy-bounded hosted assistant-notification lifecycle/failure metadata into the web-owned hosted run log sink without persisting raw provider or delivery payloads |  | `packages/hosted-execution/src/{contracts,parsers}.ts`, `packages/assistant-runtime/src/hosted-runtime/{events,execution,models}.ts`, `apps/cloudflare/src/user-runner/runner-run-processor.ts`, directly coupled focused tests only | hosted runner `redactedLogEntries`, assistant-notification web log forwarding, hosted run log component override | in_progress | Narrow hosted observability lane only. Keep raw request bodies and local harness dumps local-only, and preserve overlapping assistant-runtime/Cloudflare edits outside this safe log-forwarding slice. |
| Codex | Set up a scoped Health Commons research workspace for a creatine monohydrate experiment without widening into source landing or generated Health Commons outputs | `agent-docs/exec-plans/active/2026-04-23-creatine-monohydrate-research-setup.md` | `output-packages/research/creatine-monohydrate/**`, `agent-docs/exec-plans/active/{2026-04-23-creatine-monohydrate-research-setup.md,COORDINATION_LEDGER.md}` | creatine monohydrate charter scope, creatine family boundary, research workspace materialization | completed | Workspace scaffolded and materialized. The charter response was recovered from the landed thread export after the managed-browser wake loop failed to backfill `responses/01-charter.md`; no commit was created because the actual research workspace lives under ignored `output-packages/**` and the shared ledger file already carries unrelated concurrent churn. |
| Codex | Set up a scoped Health Commons research workspace for a morning outdoor light exposure experiment without widening into source landing or generated Health Commons outputs | `agent-docs/exec-plans/active/2026-04-23-morning-outdoor-light-exposure-research-setup.md` | `output-packages/research/morning-outdoor-light-exposure/**`, `agent-docs/exec-plans/active/{2026-04-23-morning-outdoor-light-exposure-research-setup.md,COORDINATION_LEDGER.md}` | morning outdoor light charter scope, morning-light family boundary, research workspace setup | completed | Workspace scaffolded, charter recovered from the exported ChatGPT thread, and post-charter prompts/commands materialized. Verified with `git diff --check` and `pnpm typecheck`; `pnpm verify:acceptance` is red for unrelated pre-existing assistant-engine wrapper-export and assistant-runtime coverage failures. No scoped commit was created because the actual workspace lives under ignored `output-packages/**` and the shared ledger file already carries unrelated concurrent churn. |
| Codex | Add a repo-local GitHub task-management skill under `.agents/skills` so future GitHub issue/project routing work triggers automatically in this repo |  | `.gitignore`, `.agents/skills/github-project-issues/SKILL.md` | repo-local GitHub task skill, narrow `.agents` git include | completed | Added `.agents/skills/github-project-issues/SKILL.md` plus a narrow `.gitignore` exception so the repo can carry this one auto-discovered skill without unignoring the rest of `.agents`. Verified with `pnpm typecheck`, `pnpm test:smoke`, and `git diff --check` on the tracked diff. |
| Codex | Add a repo-local Health Commons research workflow skill so future protocol-research runs trigger with the right charter/discovery/recovery heuristics |  | `.gitignore`, `.agents/skills/health-commons-research/SKILL.md` | Health Commons research workflow, `review:gpt` research runner, thread-wake recovery, repomix/package rules | completed | Added `.agents/skills/health-commons-research/SKILL.md` plus the narrow `.gitignore` exception needed to track it. The skill now captures end-to-end protocol research flow, recovery rules, packaging expectations, and the autonomy contract for charter through final reducer. Verified with `pnpm typecheck`, direct skill readback, and `git diff --check` on the touched files. |
| Codex | Harden assistant runtime issue pending-path resolution so crafted issue ids cannot escape `issues/pending` through exported path resolution or deletion | `agent-docs/exec-plans/completed/2026-04-23-runtime-state-issue-path-hardening.md` | `packages/runtime-state/src/assistant-runtime-issues.ts`, directly coupled `packages/runtime-state/test/assistant-runtime-issues.test.ts`, `agent-docs/exec-plans/{completed/2026-04-23-runtime-state-issue-path-hardening.md,active/COORDINATION_LEDGER.md}` | `resolvePendingAssistantRuntimeIssuePath`, `deletePendingAssistantRuntimeIssueRecord`, assistant runtime issue id validation | completed | High-risk runtime-state trust-boundary lane completed. Package-local typecheck, coverage, focused Vitest, `pnpm test:smoke`, `git diff --check`, and the direct malicious-id scenario proof all passed; repo-wide `pnpm typecheck` and diff-aware verification remain red only on unrelated pre-existing branch churn. No scoped commit was created because the shared dirty `COORDINATION_LEDGER.md` would have absorbed unrelated concurrent edits. |
| Codex | Land the supplied scheduled-log follow-up review patch without widening beyond the existing scheduled-log contract/core/query/CLI slice |  | `packages/{contracts,core,query}/src/scheduled-logs.ts`, `packages/core/src/mutations.ts`, `packages/cli/src/commands/scheduled-log.ts`, directly coupled scheduled-log tests only if required | scheduled-log union validation, shared schedule parsing, scheduled meal tags, paused/archived execution skip semantics, CLI input parsing | completed | Supplied patch landing completed. Kept schedule parsing contract-owned while restoring stable top-level schedule error text, preserved meal action tags, made paused/archived logs skip cleanly without losing duplicate-occurrence idempotency, and removed the unnecessary CLI input assertion. Verified with `pnpm typecheck`, `bash scripts/workspace-verify.sh test:diff packages/contracts/src/scheduled-log.ts packages/core/src/mutations.ts packages/core/src/scheduled-logs.ts packages/core/test/scheduled-logs.test.ts packages/query/src/scheduled-logs.ts packages/query/test/scheduled-logs.test.ts packages/cli/src/commands/scheduled-log.ts`, `pnpm test:smoke`, focused scheduled-log Vitest, and direct TSX proofs for meal-tag forwarding plus idempotent retry after a later pause. Required `coverage-write` and `task-finish-review` audit passes both ran; final review findings were fixed before the final reruns. |
| Codex | Fix query overview vault-source coverage acceptance failure without widening beyond the experiment overview summary contract and directly coupled test |  | `packages/query/test/overview-vault-source-coverage.test.ts` | `summarizeOverviewExperiments`, experiment overview nullable metadata fields | completed | Test expectation updated to match the normalized `OverviewExperiment` contract: absent protocol/run/outcome/onboarding/support metadata remains explicit `null`. Verified with package-local typecheck, focused overview test, and full query tests. `pnpm --dir packages/query test:coverage` runs all 150 tests but remains red on pre-existing `src/experiments.ts` coverage thresholds unrelated to this test-only diff. Do not commit. |
| Codex | Wire hosted AI usage reporting secret through the Cloudflare deploy workflow and set the GitHub production environment secret without exposing the generated value | `agent-docs/exec-plans/active/2026-04-23-hosted-ai-usage-reporting-secret.md` | `.github/workflows/deploy-cloudflare-hosted.yml`, `agent-docs/exec-plans/active/{2026-04-23-hosted-ai-usage-reporting-secret.md,COORDINATION_LEDGER.md}` | `HOSTED_AI_USAGE_REPORTING_SECRET`, Cloudflare deploy job env pass-through | completed | Completed in commit `5d8477d7`. The workflow now forwards `HOSTED_AI_USAGE_REPORTING_SECRET`, the GitHub `production` environment secret exists by name, and the focused workflow/test diff verification passed before the user committed. |
| Codex | Run the HBOT charter, upgrade Murph to the fixed review-gpt export release, and update the repo-local Health Commons research skill without widening into HBOT evidence landing or further research phases | `agent-docs/exec-plans/active/2026-04-23-hbot-charter-and-research-skill.md` | `.agents/skills/health-commons-research/SKILL.md`, `{package.json,pnpm-lock.yaml,pnpm-workspace.yaml}`, `output-packages/research/hyperbaric-oxygen-therapy-20260423-093246Z/**`, `agent-docs/exec-plans/active/{2026-04-23-hbot-charter-and-research-skill.md,COORDINATION_LEDGER.md}` | research skill export guidance, `@cobuild/review-gpt` 0.5.76 rollout, HBOT charter scope, clinical-vs-mild HBOT split | in_progress | Narrow research-tooling/docs lane only. Preserve unrelated ledger churn and stop before post-charter materialization or any Health Commons content landing. |
