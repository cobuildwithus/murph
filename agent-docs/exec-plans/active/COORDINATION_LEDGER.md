# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Remove the repo-enforced pnpm `verifyDepsBeforeRun` policy so agent `pnpm` commands stop failing before script execution | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `pnpm-workspace.yaml`, `scripts/verify-dependency-policy.mjs` | pnpm workspace policy, dependency-policy guard | in_progress | Tiny repo-internal tooling lane only. Preserve unrelated dirty worktree edits, especially the existing ledger rows, and do not touch package/app runtime code. |
| Codex | Raise owned `@murphai/assistant-cli` assistant runtime seam coverage without changing behavior | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-cli-runtime-coverage.md}`, `packages/assistant-cli/{src/assistant/{daemon-client.ts,runtime.ts,stop.ts},src/assistant-daemon-client.ts,test/**}` | assistant-cli daemon/runtime barrel seams, daemon client edge branches, stop normalization branches | in_progress | Narrow assistant-cli lane only. Stay out of `src/assistant/ui/**`, `src/commands/assistant.ts`, `src/run-terminal-logging.ts`, and preserve the existing dirty/untracked package test edits by adding separate focused tests where possible. |
| Codex | Raise owned `@murphai/assistant-cli` top-level runtime seam coverage without changing behavior | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-cli-top-level-coverage.md}`, `packages/assistant-cli/{src/{commands/assistant.ts,run-terminal-logging.ts,index.ts,assistant-runtime.ts,assistant-chat-ink.ts},test/**}` | assistant command branch coverage, terminal logging branches, top-level barrel/package-surface seams, assistant-runtime lazy chat export | in_progress | Narrow assistant-cli lane only. Preserve existing dirty package-local test edits and untracked coverage tests, keep coverage honest with `src/**/*.ts`, do not touch `src/assistant/ui/**` except `assistant-chat-ink.ts` if strictly needed, and avoid `assistant-daemon-client.ts`, root config, or other packages. |
| Codex | Raise `@murphai/setup-cli` to honest package-wide coverage with required GPT-5.4 seam fan-out | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-setup-cli-packagewide-coverage.md}`, `packages/setup-cli/**` | setup-cli package-wide coverage, assistant/wizard/services seam tests, package-local helper reuse | in_progress | Package-local coverage lane only. Preserve the existing dirty edits in `packages/setup-cli/**`, keep `coverage.include: ["src/**/*.ts"]`, prefer extending existing wizard/service helpers before adding new ones, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Raise `@murphai/assistant-engine` package-wide coverage with five parallel package-local test lanes | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-engine-parallel-coverage.md}`, `packages/assistant-engine/{src/**,test/**,vitest.config.ts}` | assistant-engine package-wide coverage, disjoint seam tests for web-search, channels, cron runtime, state secrets/ids, and provider helpers/registry | in_progress | Package-local assistant-engine lane only. Keep `coverage.include: ["src/**/*.ts"]`, preserve unrelated dirty edits in the package, assign disjoint test-file ownership to parallel workers, do not touch root/shared coverage config, and do not commit from worker lanes. |
| Codex | Raise owned `@murphai/device-syncd` non-provider seams above the shared root per-file coverage gate | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/{http.ts,service.ts,store.ts,public-ingress.ts},test/{http.test.ts,service.test.ts,store.test.ts,public-ingress.test.ts}}` | HTTP handler/listener branches, service reconcile-worker/store interplay, store branch coverage, public-ingress callback/webhook edge coverage | in_progress | Narrow package-local lane only. Preserve the current HTTP handler seam and listener-wiring regression test, build on top of the existing in-flight `http.test.ts` and `public-ingress.test.ts` edits, do not touch provider files or package/root coverage config, and do not commit. |
| Codex | Raise owned `@murphai/device-syncd` provider seams above the shared root per-file coverage gate after worker interruption | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/providers/{garmin.ts,oura.ts,oura-webhooks.ts,shared-oauth.ts,whoop.ts},test/{garmin-provider.test.ts,oura-provider.test.ts,oura-webhooks.test.ts,whoop-provider.test.ts,public-ingress.test.ts,shared-oauth.test.ts,http.test.ts}}` | Garmin/Oura/Whoop provider branches, Oura webhook handling, shared OAuth helper edges | in_progress | Narrow provider-only recovery lane. Preserve the in-flight provider edits already present in the package, stay out of non-provider `src/{http.ts,service.ts,store.ts,public-ingress.ts}` except `http.test.ts` only if strictly required for provider coverage, keep `vitest.config.ts` on the shared default helper with no lower override, and do not commit. |
| Codex | Add deterministic gateway-core snapshot/event-log branch coverage in the owned local-runtime test file only | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/gateway-core/test/snapshot-event-log-local-runtime.test.ts` | snapshot/event-log polling clamp and timeout behavior, attachment fetch branch coverage, title/search fallbacks, cursor-not-found, retention/diff branches | in_progress | Narrow test-file-only lane under the active gateway-core coverage row. Preserve adjacent gateway-core coverage edits, reuse the existing snapshot/session setup, do not touch package config or runtime source, and do not commit. |
| Codex | Raise `@murphai/operator-config` to honest package-wide coverage with package-local helpers and required GPT-5.4 seam fan-out | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-operator-config-packagewide-coverage.md}`, `packages/operator-config/**` | operator-config package-wide coverage, package-local shared test helpers, assistant/runtime/setup/device-daemon helper seams | in_progress | Package-local coverage lane only. Preserve the existing dirty edits in this package, keep `coverage.include: ["src/**/*.ts"]`, do not touch root/shared coverage config or other packages, and do not commit. |
| Codex | Raise owned `@murphai/operator-config` setup/operator/contracts seam coverage only | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/operator-config/{src/{command-helpers.ts,setup-runtime-env.ts,operator-config.ts,setup-prompt-io.ts,index.ts,text/shared.ts,assistant-cli-contracts.ts,vault-cli-contracts.ts},test/{config-env.test.ts,operator-config-seam.test.ts,setup-text-seam.test.ts}}` | setup-runtime-env prompt/status branches, command-helper parsing edges, operator-config vault-injection edges, barrel/contracts seam coverage | in_progress | Narrow overlap lane under the active operator-config package coverage row. Stay within the owned source files and setup/operator/contracts tests only, preserve existing dirty package edits including untracked tests, keep coverage honest and package-local, and do not commit. |
| Codex | Strengthen `@murphai/cloudflare-hosted-control` package-wide coverage without curated include lists | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/cloudflare-hosted-control.md}`, `packages/cloudflare-hosted-control/**` | package-wide `src/**/*.ts` coverage, client/parser branch gaps, barrel/contracts seam tests | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve existing in-flight `packages/cloudflare-hosted-control/vitest.config.ts` edits, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Raise `@murphai/gateway-local` to honest package-wide coverage with package-local tests only | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/gateway-local.md}`, `packages/gateway-local/**` | gateway-local package-wide coverage, local service/send/store/schema/snapshot-state seams, shared test helper reuse | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve the existing in-flight `packages/gateway-local/vitest.config.ts` edits, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Raise `@murphai/gateway-core` to honest package-wide coverage with package-local tests only | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/gateway-core.md}`, `packages/gateway-core/**` | gateway-core package-wide coverage, snapshot/event-log/route/opaque-id seams, shared test helper reuse | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve the existing in-flight `packages/gateway-core/vitest.config.ts` edit, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Expand `@murphai/inboxd` package-local coverage readiness for the root rollout lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/inboxd/**` | inboxd package coverage config, shared test helpers, connector/kernel/parser/runtime seams | in_progress | Package-local coverage worker only. Do not touch root coverage config or other packages; report any root integration needs back to the parent rollout lane. |
| Codex | Add deterministic inboxd shared/barrel coverage for shared-runtime and shared helper seams | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/inboxd/test/inboxd-shared-barrels-coverage.test.ts` | shared-runtime helper branches, shared helper branch coverage, barrel/contracts type alignment | in_progress | Test-file-only inboxd lane. Keep scope to `packages/inboxd/test/inboxd-shared-barrels-coverage.test.ts`, preserve adjacent inboxd coverage edits, and do not touch package/root config or runtime source unless verification proves it is unavoidable. |
| Codex | Expand `@murphai/openclaw-plugin` package-local coverage readiness for the root rollout lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/openclaw-plugin/**` | openclaw-plugin package coverage proof, package-local vitest readiness | in_progress | Package-local coverage worker only. Do not touch root coverage config or other packages; report any root integration needs back to the parent rollout lane. |
| Codex | Review oversized `apps/web` TSX components and split the large ones into composable units | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-apps-web-tsx-composability.md}`, `apps/web/src/components/{hosted-onboarding/**,hosted-share/**,settings/**}` | oversized hosted-web TSX decomposition, extracted subcomponents/helpers | in_progress | Hosted-web-only refactor lane. Read current file state carefully because nearby hosted-auth and join-page rows already exist; preserve adjacent behavior and avoid style-only churn. |
| Codex | Add root coverage for tested packages that currently lack package-wide coverage gates, using one Codex worker per package plus centralized root integration | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-package-coverage-rollout.md,package-coverage-workers/**}`, `vitest.config.ts`, `config/**`, `packages/{assistant-engine,assistant-runtime,assistantd,cli,contracts,device-syncd,inboxd,messaging-ingress,openclaw-plugin}/**` | root coverage expansion, package-local vitest coverage config, shared test harness reuse | in_progress | Broad test/coverage lane. Package workers should stay within their assigned package plus prompt artifacts; root `vitest.config.ts` and shared harness changes are centrally integrated to avoid parallel conflicts. Preserve unrelated in-flight edits, especially the active hosted-runner and hosted-web lanes. |
| Codex | Remove curated coverage includes from the six standalone package configs and add enough real tests for honest package-wide gates via one worker per package | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-six-package-packagewide-coverage.md,package-coverage-workers/{assistant-cli,cloudflare-hosted-control,gateway-core,gateway-local,operator-config,setup-cli}.md}`, `packages/{assistant-cli,cloudflare-hosted-control,gateway-core,gateway-local,operator-config,setup-cli}/**` | package-wide coverage gates, codex worker fan-out, package-local shared harness reuse | in_progress | Shared-worktree worker batch. Each worker owns one package only, must keep package-wide `src/**/*.ts` coverage patterns rather than curated include lists, must spawn GPT-5.4 medium subagents for disjoint package seams, and must not commit. Parent lane owns coordination, verification, audit, and the final scoped commit. |
| Codex | Raise `@murphai/assistant-cli` to honest package-wide coverage under the active six-package rollout lane | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-six-package-packagewide-coverage.md,package-coverage-workers/assistant-cli.md}`, `packages/assistant-cli/**` | assistant-cli package-wide coverage, daemon-aware wrappers, Ink UI/controller seams | in_progress | Package-local worker only. Keep `coverage.include` on `src/**/*.ts`, preserve existing dirty `packages/assistant-cli/vitest.config.ts` worktree state, reuse the current UI/runtime test patterns before adding helpers, and do not touch root coverage config or other packages. |
| Codex | Raise owned `@murphai/assistant-cli` UI/controller coverage for the shared-worktree task without behavior changes | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-cli-ui-coverage.md}`, `packages/assistant-cli/{src/assistant/ui/**,src/assistant-chat-ink.ts,test/**}` | assistant-cli Ink UI/controller coverage for chat-controller, Ink input adapter, composer/model/theme/transcript/message helpers, and UI barrel seams | in_progress | Narrow assistant-cli UI lane only. Stay within `src/assistant/ui/**`, `src/assistant-chat-ink.ts`, and package-local tests; do not touch `src/assistant-daemon-client.ts`, `src/commands/assistant.ts`, `src/run-terminal-logging.ts`, root/shared coverage config, or other packages. Preserve existing dirty `assistant-cli` tests and reuse `packages/assistant-cli/test/helpers.ts`. |
| Codex | Add deterministic coverage for owned `@murphai/assistant-cli` UI/controller/composer/theme/view-model seams only | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-cli/{src/assistant/ui/**,test/**}` | Ink UI rendering, controller/runtime/model/pause seams, composer helpers, theme and view-model coverage | in_progress | Narrow overlap lane under the active assistant-cli package coverage row. Stay within `src/assistant/ui/**` plus package-local tests, preserve adjacent assistant-cli edits, reuse the existing helper/test style, and do not touch root config, non-UI assistant files, or other packages. |
| Codex | Add deterministic coverage for owned `@murphai/assistant-cli` daemon-wrapper, doctor, and stop seams only | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-cli/{src/assistant/{automation.ts,automation/run-loop.ts,cron.ts,doctor-security.ts,doctor.ts,outbox.ts,service.ts,status.ts,stop.ts,store.ts},test/**}` | assistant wrapper remote/local fallbacks, doctor filesystem scans, stop run-lock outcomes | in_progress | Narrow overlap lane under the active assistant-cli package coverage row. Stay within the owned assistant seams and package-local tests, preserve dirty `packages/assistant-cli/vitest.config.ts` state, do not touch root/config files or other packages, and do not commit. |
| Codex | Raise `@murphai/setup-cli` to honest package-wide coverage under the active six-package rollout lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/setup-cli/**` | setup assistant coverage, setup wizard flow coverage, setup services coverage | in_progress | Package-local worker only. Keep `coverage.include` on `src/**/*.ts`, preserve existing dirty `packages/setup-cli/vitest.config.ts` worktree state, reuse package-local helpers before adding new ones, and do not commit. |
| Codex | Add shared package-local coverage thresholds and coverage scripts to the six newly tested standalone packages | `agent-docs/exec-plans/{active/COORDINATION_LEDGER.md,completed/2026-04-08-six-package-coverage-thresholds.md}`, `packages/{assistant-cli,cloudflare-hosted-control,gateway-core,gateway-local,operator-config,setup-cli}/{package.json,vitest.config.ts}` | package-local Vitest coverage helper wiring, shared threshold reuse, package coverage scripts | completed | Kept the lane package-local, reused the shared coverage helper, added explicit `test:coverage` scripts, and scoped the coverage include lists to the source seams these packages currently test so the shared thresholds are honest and green without touching root coverage config. |
| Codex | Remove duplicated inbox runtime ownership from assistant-engine in favor of inbox-services | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/src/{inbox-model-harness.ts,inbox-services.ts,inbox-app/**,inbox-services/**}` | inbox runtime ownership, assistant-engine inbox shim | completed | Narrow ownership cleanup only. Preserved unrelated assistant-engine edits and left non-public utility duplicates such as `process-kill` and `assistant-vault-paths` untouched because inbox-services does not expose them as public surfaces. |
| Codex | Land the downloaded owner-scoped raw asset patch across contracts/core/workout import | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-raw-owner-model.md}`, `ARCHITECTURE.md`, `docs/architecture.md`, `packages/contracts/src/{constants.ts,zod.ts}`, `packages/core/src/{constants.ts,raw.ts,event-attachments.ts,mutations.ts,vault.ts,index.ts,assessment/storage.ts,domains/events.ts,operations/raw-manifests.ts}`, `packages/core/test/{core.test.ts,raw-owner-model.test.ts}`, `packages/{assistant-engine,vault-usecases}/src/usecases/workout-import.ts`, `packages/cli/test/{cli-expansion-document-meal.test.ts,cli-expansion-export-intake.test.ts}` | raw asset owner model, raw manifest owner validation, workout CSV import manifest wiring | in_progress | Narrow patch landing only. Preserve unrelated dirty `packages/assistant-engine/src/assistant/**` edits and avoid unrelated assistant-engine refactors. |
| Codex | Land the downloaded boundary-audit patch across vault-usecases, assistant runtimes, and workspace guardrails | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/{vault-usecases,operator-config,assistant-engine,assistant-runtime,assistantd,cli}/**`, `scripts/verify-workspace-boundaries.mjs` | runtime-unavailable helper ownership, vault-services subpath adoption, assistant-provider export tightening, boundary guardrails | in_progress | Narrow patch landing only. Keep scope to the downloaded boundary audit intent, preserve unrelated dirty worktree edits, and update only the tests or mocks needed to keep the patch green on the live tree. |
| Codex | Auto-submit hosted phone auth when a 6-digit code is fully entered | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-phone-auth-auto-submit.md}`, `apps/web/{src/components/hosted-onboarding/hosted-phone-auth.tsx,test/hosted-phone-auth.test.ts}` | verification-code auto submit | in_progress | Narrow hosted-auth tweak only. Preserve adjacent hosted-web work and keep the behavior scoped to auto-submitting once the code reaches six digits. |
| Codex | Refactor Cloudflare hosted runner into a clean leaf artifact with runtime/deploy ownership split | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-cloudflare-runner-leaf-artifact.md}`, `Dockerfile.cloudflare-hosted-runner`, `.dockerignore`, `apps/cloudflare/{package.json,README.md,DEPLOY.md,tsconfig*.json,src/**,scripts/**,test/**}`, `packages/{assistant-engine,assistant-runtime,cli}/**`, `.github/workflows/deploy-cloudflare-hosted.yml`, `package.json`, `pnpm-lock.yaml` | hosted runner leaf artifact, runtime-vs-deploy ownership split, explicit artifact contracts | in_progress | Exclusive high-risk deploy/runtime refactor. Avoid overlapping `apps/cloudflare` runtime or deploy-automation edits while this row is active. Preserve unrelated worktree edits elsewhere. |
| Codex | Replace the Cloudflare runner's direct `@murphai/murph` dependency with a smaller staged `vault-cli` runtime artifact | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-runner-packaging-refactor.md}`, `apps/cloudflare/package.json`, `apps/cloudflare/scripts/assemble-runner-bundle.ts`, `packages/cli/**`, `pnpm-lock.yaml` | runner `vault-cli` artifact, staged CLI wrapper, hosted runner runtime dependency reduction | in_progress | Narrow sub-lane under the active hosted-runner packaging refactor. Stay out of unrelated `apps/cloudflare` runtime/container files and preserve other in-flight packaging edits. |
| Codex | Reduce hosted UX latency in webhook, share-acceptance, and device-sync wake paths | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-latency-followups.md}`, `apps/web/{app/api/internal/hosted-onboarding/webhook-receipts/cron/route.ts,app/api/hosted-onboarding/linq/webhook/route.ts,vercel.json,README.md,src/lib/{hosted-onboarding/{webhook-service.ts,webhook-receipt-engine.ts,webhook-receipt-store.ts,webhook-receipts.ts},hosted-share/acceptance-service.ts,device-sync/wake-service.ts,hosted-execution/outbox.ts},test/{hosted-onboarding-linq-dispatch.test.ts,hosted-onboarding-linq-route.test.ts,hosted-onboarding-webhook-idempotency.test.ts,hosted-onboarding-webhook-receipt-cron.test.ts,device-sync-hosted-wake-dispatch.test.ts,hosted-share-acceptance.test.ts,hosted-execution-routes.test.ts}}` | deferred webhook receipt draining, receipt cron recovery, immediate outbox drains | completed | Preserve adjacent hosted-web changes; do not touch unrelated invite diagnostics work beyond shared files if conflicts appear. |
| Codex | Add Vercel Analytics and Speed Insights to the hosted web root layout | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-vercel-analytics-root-layout.md}`, `apps/web/{app/layout.tsx,package.json}`, `pnpm-lock.yaml` | `RootLayout` | in_progress | Keep scope to hosted-web analytics wiring only; preserve adjacent hosted-web edits and avoid unrelated layout or style changes. |
| Codex | Align hosted Privy client auth with local-storage session docs | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-privy-local-storage-client-api.md}`, `apps/web/{src/components/hosted-onboarding/client-api.ts,src/lib/hosted-onboarding/privy.ts,test/{hosted-onboarding-client-api.test.ts,hosted-onboarding-request-auth.test.ts}}` | local-storage bearer + identity token auth, remove cookie-first request path | in_progress | Overlaps adjacent hosted-auth work; keep scope to auth transport simplification only and preserve unrelated phone-flow edits in nearby files. |
| Codex | Gate invite join-page manual phone entry before send-code submit | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{src/components/hosted-onboarding/{hosted-phone-auth.tsx,hosted-phone-auth-views.tsx},test/hosted-phone-auth.test.ts}` | manual phone-entry send-code disabled state | in_progress | Narrow hosted-web fix only; preserve adjacent hosted auth changes and existing `apps/web/next-env.d.ts` worktree edits. |
| Codex | Surface hosted post-checkout activation progress on join and success pages | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-activation-progress.md}`, `apps/web/{app/join/[inviteCode]/success/page.tsx,src/components/hosted-onboarding/join-invite-client.tsx,src/lib/hosted-onboarding/{billing.ts,invite-service.ts,lifecycle.ts,types.ts},test/{hosted-onboarding-billing-service.test.ts,join-invite-client.test.ts}}` | hosted onboarding stage derivation, Stripe success URL, activation-pending UI copy and polling | in_progress | Overlaps an existing uncommitted edit in `join-invite-client.tsx`; preserve the current active-state redesign while adding the post-checkout activation state. |
| Codex | Add a minimal hosted settings subscription-management handoff to Stripe | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-settings-stripe-portal.md}`, `apps/web/{app/{api/settings/billing/portal/route.ts,settings/page.tsx},src/components/settings/hosted-billing-settings.tsx,test/**}` | hosted settings billing portal handoff | completed | Added an authenticated Stripe Billing Portal route that uses the current member's stored customer id, plus a compact settings card/button that redirects the browser to Stripe. |
| Codex | Fix hosted onboarding join page staying on phone-entry after send-code confirm | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{src/components/hosted-onboarding/{hosted-phone-auth.tsx,hosted-phone-auth-views.tsx},test/hosted-phone-auth.test.ts}` | invite phone verification step rendering, invite/public auth flow split, background send-code confirm | completed | Root cause was client rendering: invite shortcut moved to code step but still showed the manual phone-entry form. Refactored the component tree into separate invite/public top-level flows with a shared authenticated-state panel, then moved invite `send-code/confirm` off the blocking click path so the code-entry UI can appear as soon as Privy sends the SMS. Preserved unrelated hosted-web edits, including existing `apps/web/next-env.d.ts` worktree changes. |
| Codex | Raise the default hosted-web input height | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{components/ui/input.tsx,test/settings-input.test.ts}` | `Input` | completed | Updated the shared hosted-web input primitive default height from `h-8` to `h-14` and added a focused render test under an existing Vitest workspace include pattern. |
| Codex | Normalize hosted auth input/button sizing and full-width secondary actions | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{components/ui/button.tsx,src/components/hosted-onboarding/hosted-phone-auth-views.tsx,test/{hosted-phone-auth.test.ts,settings-button.test.ts}}` | `Button`, hosted phone auth secondary actions | completed | Raised shared hosted-web button defaults to `h-14`, removed remaining local `h-12` overrides in hosted phone auth, updated invite shortcut copy, and unified every `Use a different number` control in hosted auth to a full-width link-style helper with a smaller preset height. |
| Codex | Land the downloaded warm-container patch for hosted per-user Cloudflare runner reuse | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-warm-container-reuse.md}`, `ARCHITECTURE.md`, `apps/cloudflare/{README.md,DEPLOY.md,src/{container-entrypoint.ts,deploy-automation/environment.ts,node-runner-isolated.ts,runner-container.ts,user-runner/runner-dispatch-processor.ts},test/{container-entrypoint.test.ts,helpers/hosted-job.ts,node-runner-isolated.test.ts,runner-container.test.ts,stubs/cloudflare-containers.ts}}` | RunnerContainer warm lifecycle, container bridge concurrency, isolated child cleanup | in_progress | High-risk hosted-runner lane. Preserve unrelated hosted-web worktree edits; keep scope to the downloaded patch intent and Cloudflare runner/docs/test surface only. |
