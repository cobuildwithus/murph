# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Land the residual data-model review patch on the current tree | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-data-model-review-residuals.md}`, `agent-docs/references/data-model-seams.md`, `packages/{query,assistant-engine,assistant-runtime,cli,hosted-execution}/**`, `apps/{cloudflare,web}/**` | knowledge contract ownership, hosted assistant-delivery journals, hosted webhook receipt JSON detail shell | in_progress | Keep this lane scoped to the patch files only, preserve the unfinished hosted-legal lane, and verify with repo acceptance plus the required final audit pass. |
| Codex | Align Docker and CI Node selection to the repo baseline | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-node-version-uniformity.md}`, `.github/workflows/{deploy-cloudflare-hosted.yml,host-support.yml,release.yml,repo-hygiene.yml}`, `Dockerfile.cloudflare-hosted-runner`, `.nvmrc`, `apps/cloudflare/{README.md,test/container-image-contract.test.ts}`, `agent-docs/operations/verification-and-runtime.md` | hosted runner Node pin, shared CI Node pin | in_progress | Keep this lane narrow to Node-version uniformity. Preserve unrelated in-flight Cloudflare and docs worktree edits, verify with focused Cloudflare coverage plus repo typecheck, then run the required final audit pass. |
| Codex | Fix current release-check regressions in setup wizard/service coverage and CLI journal date expectations | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-release-check-regressions.md}`, `packages/{setup-cli,cli,core}/**` | setup wizard TTY rendering, Linux apt fallback, experiment endedOn day derivation | in_progress | Preserve unrelated dirty worktree edits. Keep this lane scoped to the failing CI surfaces, verify with focused tests plus required repo checks, then run the required final audit pass. |
| Codex | Add first-class Vercel AI Gateway defaults plus optional Gateway ZDR for Murph assistants | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-vercel-ai-gateway-support.md}`, `packages/{operator-config,assistant-engine,setup-cli}/**`, `apps/cloudflare/{src,test}/**` | vercel gateway preset, openai-compatible zeroDataRetention seam, hosted runner env allowlist | in_progress | Keep this lane scoped to assistant provider config/setup/runtime support. Preserve unrelated in-flight operator-config/setup-cli edits and avoid reopening unrelated hosted execution policy. |
| Codex | Land the next bounded parallel review extractions across device-syncd, outbox, inboxd, and operator-config | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-next-review-parallel-extractions.md}`, `packages/{device-syncd/src/store.ts,assistant-engine/src/assistant/outbox.ts,inboxd/src/kernel/sqlite.ts,operator-config/src/operator-config.ts}`, matching new sibling modules, matching focused tests | device-sync OAuth-state seam, assistant outbox summary/store seam, inboxd parse-job seam, operator-config storage seam | in_progress | Narrow seam-only pass. Preserve unrelated dirty worktree edits and avoid widening into neighboring active assistant-engine/operator-config work. |
| Codex | Extract the first bounded assistant outbox summary/store seams behind the stable facade | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-outbox-summary-store-seams.md}`, `packages/assistant-engine/src/assistant/outbox.ts`, `packages/assistant-engine/src/assistant/outbox/{store.ts,summary.ts}`, focused `packages/assistant-engine/test/**` if needed | `buildAssistantOutboxSummary`, outbox inventory/quarantine helpers, stable outbox facade | in_progress | Worker-owned narrow seam only. Preserve overlapping assistant-engine edits, re-read live file state before patching, and do not widen into dispatch/drain extraction. |
| Codex | Extract the inboxd attachment parse-job seam into a sibling sqlite module | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-next-review-parallel-extractions.md}`, `packages/inboxd/src/kernel/{sqlite.ts,sqlite/parse-jobs.ts}`, `packages/inboxd/test/**` only if needed | attachment parse-job read/list/claim/complete/fail/requeue helpers | in_progress | Worker lane under the shared parallel-extractions plan. Keep `sqlite.ts` as the assembly/search root, do not widen into search extraction, and preserve overlapping inboxd edits outside this seam. |
| Codex | Extract the operator-config storage/path/file-I/O seam behind the stable facade | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-next-review-parallel-extractions.md}`, `packages/operator-config/src/{operator-config.ts,operator-config/storage.ts}`, `packages/operator-config/test/**` only if needed | operator config home/path resolution, config read/write helpers, path existence checks | in_progress | Worker-scoped seam only. Preserve overlapping operator-config edits, keep `operator-config.ts` as the public facade, and avoid widening into assistant-defaults cleanup. |
