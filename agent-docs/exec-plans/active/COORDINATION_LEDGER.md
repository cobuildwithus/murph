# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| codex-main | Batch 2 orchestration + review for managed-hosted cutover | `agent-docs/exec-plans/active/2026-04-05-managed-hosted-cutover-batch2-workers.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `.codex-worker-prompts/managed-hosted-cutover-batch2/**`, `.codex-runs/**` | Batch 2 worker prompts, run review, verification notes | in_progress | Shared live worktree on top of Batch 1 commit `7b6a5ce2`. Preserve unrelated dirty edits already present on the branch. |
| codex-worker-5 | Hosted email raw-body root-key cutover | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-05-hosted-email-root-key-cutover.md}`, `apps/cloudflare/src/{hosted-email.ts,index.ts,runner-outbound.ts,user-runner.ts,user-env.ts}`, focused Cloudflare tests | hosted email root-key storage, user-root-key authorization reads | in_progress | Shared live worktree. Keep this lane within hosted email and the bound-user storage path only. |
| codex-worker-6 | Device-sync escrow cutover to Cloudflare | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-05-device-sync-escrow-cutover.md}`, `apps/cloudflare/src/{device-sync-runtime-store.ts,index.ts,user-runner.ts,worker-contracts.ts}`, `packages/hosted-execution/src/{client.ts,routes.ts,web-control-plane.ts,contracts.ts,parsers.ts}`, `apps/web/src/lib/device-sync/**`, `apps/web/app/api/device-sync/**`, `apps/web/prisma/schema.prisma`, migrations, focused tests | device-sync token escrow ownership, signed apply/read path, Prisma metadata-only cleanup | in_progress | Shared live worktree. Reuse the existing runtime-store seam; avoid Batch 2 share/onboarding files unless a contract forces it. |
| codex-worker-7 | Hosted share + remaining onboarding decrypt-domain cutover | `apps/web/src/lib/hosted-share/**`, `apps/web/app/api/hosted-share/**`, `apps/web/src/lib/hosted-onboarding/webhook-*`, any new Cloudflare share/webhook storage helpers, `apps/web/prisma/schema.prisma`, migrations, focused tests | hosted share payload storage, webhook encrypted retention deletion or cutover | queued | Shared live worktree. Prefer deleting unnecessary encrypted retention over re-homing it. Avoid device-sync escrow files unless a shared contract forces it. |
