# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Remove greenfield-incompatible compatibility and rollout baggage while preserving future key-rotation seams | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-greenfield-compatibility-cleanup.md}`, `apps/{web,cloudflare}/**`, `packages/{core,contracts,query,operator-config}/**`, `docs/**`, `ARCHITECTURE.md`, `agent-docs/index.md` | hosted contact-privacy posture, hosted email verified-sender schema bridge, hosted Prisma baseline story, canonical event/query compatibility projections, vault-upgrade copy, rollout docs cleanup | in_progress | Exclusive cross-cutting cleanup lane for this task; preserve adjacent edits and port carefully onto the current tree. |
| Codex | Land the residual data-model review patch on the current tree | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-data-model-review-residuals.md}`, `agent-docs/references/data-model-seams.md`, `packages/{query,assistant-engine,assistant-runtime,cli,hosted-execution}/**`, `apps/{cloudflare,web}/**` | knowledge contract ownership, hosted assistant-delivery journals, hosted webhook receipt JSON detail shell | in_progress | Keep this lane scoped to the patch files only, preserve the unfinished hosted-legal lane, and verify with repo acceptance plus the required final audit pass. |
| Codex | Align Docker and CI Node selection to the repo baseline | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-node-version-uniformity.md}`, `.github/workflows/{deploy-cloudflare-hosted.yml,host-support.yml,release.yml,repo-hygiene.yml}`, `Dockerfile.cloudflare-hosted-runner`, `.nvmrc`, `apps/cloudflare/{README.md,test/container-image-contract.test.ts}`, `agent-docs/operations/verification-and-runtime.md` | hosted runner Node pin, shared CI Node pin | in_progress | Keep this lane narrow to Node-version uniformity. Preserve unrelated in-flight Cloudflare and docs worktree edits, verify with focused Cloudflare coverage plus repo typecheck, then run the required final audit pass. |
| Codex | Tighten Vercel AI Gateway assistant support to one env var and hosted-default ZDR | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-vercel-ai-gateway-followup.md}`, `packages/{operator-config,assistant-engine,setup-cli,cli}/**`, `apps/cloudflare/{src,test}/**` | VERCEL_AI_API_KEY standardization, hosted gateway ZDR default, local gateway ZDR opt-in | in_progress | Keep this lane scoped to the Vercel Gateway follow-up only. Preserve unrelated in-flight operator-config/setup-cli/cloudflare edits. |
| Codex | Self-heal stale Codex resume sessions instead of surfacing `EPIPE` | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-codex-stale-resume-recovery.md}`, `packages/{assistant-engine,cli}/**` | Codex stale-resume detection, same-turn fresh fallback, provider error classification | in_progress | Keep this lane narrow to Codex resume recovery. Preserve unrelated in-flight assistant-engine and CLI edits. |
| Codex | Restore faster local package-coverage throughput without reintroducing oversubscription regressions | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-coverage-parallelism-followup.md}`, `agent-docs/operations/verification-and-runtime.md`, `scripts/workspace-verify.sh` | workspace coverage orchestration, package-coverage fanout, coverage-lane Vitest worker defaults | in_progress | Keep this lane scoped to the root coverage harness/doc contract only. Preserve unrelated in-flight assistant-engine and docs edits while re-verifying `pnpm typecheck` and `pnpm test:coverage`. |
| Codex | Trim duplicate hosted app verification work from `test:apps` | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-app-verify-speedup.md}`, `apps/web/{package.json,scripts/verify-fast.sh,scripts/dev-smoke.ts}`, `agent-docs/operations/verification-and-runtime.md` | hosted web verify sequencing, app-lane duplicate proof, dev-smoke request duplication | in_progress | Keep this lane limited to app verification tooling/docs only. Preserve unrelated in-flight web and assistant-engine edits while re-benchmarking `pnpm test:apps`. |
| Codex | Aggregate package coverage failures instead of aborting the lane on the first red package | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-coverage-failure-aggregation.md}`, `scripts/workspace-verify.sh`, `agent-docs/operations/verification-and-runtime.md` | package coverage batch waits, failure aggregation, coverage-lane reporting | in_progress | Keep this lane scoped to coverage-harness behavior only. Preserve unrelated in-flight assistant-engine and web edits while re-running focused coverage tooling checks. |
| Codex | Hard-cut the profile surface into memory/wiki/preferences | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-profile-hard-cut.md}`, `packages/{contracts,core,query,vault-usecases,cli,assistant-engine}/**`, `docs/**`, `README.md`, `ARCHITECTURE.md`, `fixtures/**` | profile removal, canonical preferences singleton, workout unit defaults, assistant prompt/read-model rewiring | in_progress | Exclusive cross-cutting refactor lane for this task. Remove `profile` end-to-end with no compatibility shim, preserve unrelated edits, and verify with package tests, smoke, direct CLI proof, and the required final audit pass. |
| Codex | Hard-cut query history and keep blood-test as a user-facing projected event view | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-history-blood-test-hard-cut.md}`, `packages/{query,vault-usecases,cli}/**`, `docs/contracts/**`, `ARCHITECTURE.md` | query event-vs-history family split, blood-test projected view, history CLI removal, export/timeline/search rewiring | in_progress | Cross-cutting read-model lane overlapping the active profile hard cut. Preserve current in-flight edits, port carefully on top of live CLI/usecase changes, and avoid touching unrelated profile-removal code unless required for this task. |
| Codex | Restore complete package/app TypeScript coverage in local typecheck without reintroducing build-shaped rootDir failures | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-09-typecheck-coverage-tsconfigs.md}`, `packages/*/{package.json,tsconfig*.json}`, `apps/{web,cloudflare}/{package.json,tsconfig*.json}` | package/app typecheck coverage, typecheck rootDir/baseUrl shape, vitest config inclusion | in_progress | Keep this lane limited to typecheck config/script wiring. Preserve unrelated in-flight package/app edits and verify with `pnpm typecheck` plus a direct coverage audit before the required final review pass. |
