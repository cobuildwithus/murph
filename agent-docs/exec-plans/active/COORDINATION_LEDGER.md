# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Debug the local web observatory demo-vault read path and simplify the overview UI so canonical fixture data is obvious on first load. | `packages/web/**`, `packages/query/**` (read-only inspection unless required), `agent-docs/exec-plans/active/2026-03-17-web-overview-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | overview loader/view-model symbols, homepage layout/components, web tests | in_progress | Narrow web-only lane. Do not touch the active CLI/docs assistant-runtime slice. |
| Codex | Integrate the provided Telegram local-first inbox setup into the current tree. | `packages/inboxd/src/connectors/chat/**`, `packages/inboxd/src/connectors/telegram/**`, `packages/inboxd/src/connectors/imessage/connector.ts`, `packages/inboxd/src/connectors/types.ts`, `packages/inboxd/src/contracts/capture.ts`, `packages/inboxd/src/indexing/persist.ts`, `packages/inboxd/src/kernel/daemon.ts`, `packages/inboxd/src/index.ts`, `packages/inboxd/test/*telegram*`, `packages/inboxd/test/connectors-daemon.test.ts`, `packages/inboxd/test/inboxd.test.ts`, `packages/cli/src/commands/inbox.ts`, `packages/cli/src/inbox-cli-contracts.ts`, `packages/cli/src/inbox-services.ts`, `packages/cli/src/incur.generated.ts`, `packages/inboxd/README.md`, `ARCHITECTURE.md`, `docs/architecture.md`, `docs/telegram-local-first-migration.md`, `agent-docs/exec-plans/active/2026-03-17-telegram-local-first.md` | add shared chat poll seam, source-native checkpoint persistence, Telegram poll driver/normalizer symbols, Telegram CLI source registration | in_progress | Non-exclusive lane. Preserve unrelated dirty CLI/docs edits and merge on top of current generated/type changes instead of reverting them. |
