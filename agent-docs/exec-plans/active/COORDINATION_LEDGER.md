# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Integrate the assistant iMessage delivery and Ink chat patch onto the current assistant runtime. | `packages/cli/src/assistant-*`, `packages/cli/src/commands/assistant.ts`, `packages/cli/src/incur.generated.ts`, `packages/cli/package.json`, assistant-related `packages/cli/test/*`, `docs/contracts/03-command-surface.md`, `agent-docs/exec-plans/active/2026-03-17-assistant-imessage-ink.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add outbound assistant channel/delivery symbols, Ink chat UI symbols, assistant deliver command/options/result metadata | in_progress | Non-exclusive lane. Preserve current inbox CLI/generated edits from the Telegram lane and merge on top of the already-landed assistant runtime slice. |
| Codex | Replace duplicated CLI raw import manifest schemas with a shared contracts-owned manifest contract and adopt it in core/CLI/tests. | `packages/contracts/src/{constants,zod,index}.ts`, `packages/core/src/{operations/raw-manifests,vault}.ts`, `packages/cli/src/commands/{document-meal-read-helpers,export-intake-read-helpers,intake}.ts`, raw-manifest-related tests under `packages/cli/test/*` and `packages/core/test/*`, `agent-docs/exec-plans/active/2026-03-17-raw-import-manifest-contract.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared `rawImportManifest*` contract symbols, remove CLI-local manifest schemas, tighten core raw-manifest validation around the shared contract | in_progress | Non-exclusive lane. Preserve unrelated assistant/runtime edits if they land while this contract cleanup is in flight. |
| Codex | Replace CLI-local query read-model/runtime mirrors with a single query-owned typed boundary. | `packages/query/src/index.ts`, `packages/cli/src/query-runtime.ts`, `packages/cli/src/usecases/types.ts`, `packages/cli/src/commands/query-record-command-helpers.ts`, `packages/cli/src/commands/document-meal-read-helpers.ts`, `packages/cli/src/commands/export-intake-read-helpers.ts`, `packages/cli/src/usecases/provider-event.ts`, `packages/cli/src/usecases/experiment-journal-vault.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/2026-03-17-query-runtime-typed-boundary.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared CLI query runtime type aliases/facade; delete local `VaultRecord`/`VaultReadModel`/`QueryRuntimeModule` mirrors; tighten `readVault*`, `listRecords`, `lookup*`, `buildTimeline`, `buildExportPack`, and search filter/result typing | in_progress | Non-exclusive lane. Overlap risk is limited to shared CLI/query type surfaces; preserve adjacent edits and do not widen types to `any`. |
| Codex | Simplify duplicated pure query helpers and remove one dead private converter only if it remains unused. | `packages/query/src/{canonical-entities,export-pack-health,model}.ts`, `packages/query/src/health/{assessments,history}.ts`, `agent-docs/exec-plans/active/2026-03-17-query-helper-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared internal sort/string-array helper symbols; update assessment/history ordering callers; delete `recordToCanonicalEntity` if still unreferenced | in_progress | Non-exclusive lane. Preserve exact sort order, trimming/filtering/dedupe behavior, and exported query behavior; stop instead of deleting the converter if a new local reference appears during the task. |
| Codex | Simplify the internal CLI health command factory without changing the surfaced commands. | `packages/cli/src/commands/health-command-factory.ts`, downstream `packages/cli/src/commands/{provider,event,document,meal,experiment,health-entity-command-registry}.ts` as needed, `packages/cli/test/**` if behavior coverage needs updates, `agent-docs/exec-plans/active/2026-03-17-health-command-factory-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | refactor `registerHealthCrudCommands`, `createFactoryCommandGroup`, `createRegistryDocEntityGroup`, `createLedgerEventEntityGroup`, `createArtifactBackedEntityGroup`, `createLifecycleEntityGroup`, and supporting typed helper symbols | in_progress | Non-exclusive lane. Preserve adjacent `packages/cli` work; this change is internal-only and should not alter command names, args/options, outputs, examples, or hints. |
| Codex | Consolidate exact small CLI helper duplicates and remove one stale private wrapper without changing public behavior. | `packages/cli/src/command-helpers.ts`, `packages/cli/src/commands/{assistant,inbox,document-meal-read-helpers,export-intake-read-helpers,health-command-factory}.ts`, `packages/cli/src/{setup-services,operator-config}.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/2026-03-17-cli-helper-cleanup.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared `parseHeadersJsonOption`, `resolveEffectiveTopLevelToken`, `ROOT_OPTIONS_WITH_VALUES`, and `firstString` helper symbols; delete duplicate local helper symbols and unused `registerFactoryCommandGroup` | in_progress | Non-exclusive lane. Overlaps existing CLI cleanup lanes in `health-command-factory.ts` and the read-helper modules; preserve any adjacent edits already present and keep all validation strings, argv behavior, and return shapes byte-for-byte equivalent. |
| Codex | Extract the duplicated provider/event and experiment/journal CLI helper block into a CLI-private shared module without changing current semantics. | `packages/cli/src/usecases/{provider-event,experiment-journal-vault}.ts`, new CLI-private helper module under `packages/cli/src/usecases/`, targeted `packages/cli/test/{cli-expansion-provider-event-samples,cli-expansion-experiment-journal-vault,cli-expansion-experiment-journal-vault-phase2,runtime}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-cli-vault-helper-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared helper symbols for link-kind/queryable checks, string/object/path normalization, and local id generation; preserve per-call-site `prov_`/`current` semantics explicitly | in_progress | Non-exclusive lane. Overlaps the CLI/query-runtime lane in the same two usecases; preserve current typed-boundary edits and keep behavior exact, especially `invalid_path` errors and link classification/queryability. |
| Codex | Simplify duplicated current-profile fallback/loading logic in the query health layer without changing strict/tolerant semantics. | `packages/query/src/health/canonical-collector.ts`, `packages/query/src/health/profile-snapshots.ts`, `packages/query/src/health/current-profile-resolution.ts`, `packages/query/test/health-tail.test.ts`, `agent-docs/exec-plans/active/2026-03-17-current-profile-fallback-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared latest-snapshot/stale-current/fallback helpers; update `readCurrentProfileStrict`, `readCurrentProfileTolerant`, `readCurrentProfileTolerantSync`, `readCurrentProfile`, and `fallbackCurrent` wiring | in_progress | Non-exclusive lane. Preserve adjacent query work, especially the typed-boundary lane touching `packages/query/src/index.ts`; keep fallback semantics for missing, malformed, and stale `bank/profile/current.md` unchanged. |
