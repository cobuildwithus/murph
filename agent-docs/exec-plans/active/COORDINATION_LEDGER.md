# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Land the downloaded data-model composability patch for shared query ownership | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/references/data-model-seams.md`, `packages/{query/src/index.ts,vault-usecases/src/query-runtime.ts,vault-usecases/test/query-runtime.test.ts}` | shared query search type exports, vault-usecases query runtime compatibility seam | in_progress | Narrow patch-landing lane only. Preserve adjacent `packages/query/**` and `packages/vault-usecases/**` coverage/test-cleanup edits, and keep scope to the downloaded patch intent rather than widening into other review findings. |
| Codex | Finalize fully uniform package coverage policy across the repo | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-package-coverage-uniformity.md}`, `vitest.config.ts`, `scripts/workspace-verify.sh`, `packages/{cli,inbox-services}/**`, `agent-docs/{operations/verification-and-runtime.md,references/testing-ci-map.md}` | canonical per-package coverage lane, CLI threshold exception removal, inbox-services shared Vitest coverage adoption, root package coverage expansion | in_progress | Root/package coverage policy lane only. Preserve unrelated in-flight package coverage tests and hosted-runner/web changes, avoid weakening thresholds, and keep changes focused on making coverage policy and repo coverage wiring fully uniform. |
| Codex | Inspect and clean the setup assistant wizard seam only if the source/test split is unjustified | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/setup-cli/{src/setup-assistant-wizard.ts,test/setup-assistant-wizard-flow.test.ts}` | setup assistant wizard prompt flow, any source-level test-only control-flow seams | in_progress | Narrow setup-cli seam only. Preserve the dirty worktree and adjacent test-cleanup lanes already touching `packages/setup-cli/test/**`; avoid widening beyond these two files, do not create commits, and prefer test cleanup unless a source seam is clearly unnecessary. |
| Codex | Get `packages/assistant-runtime` green and above package-local coverage thresholds | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-runtime-package-green.md}`, `packages/assistant-runtime/**` | package-local assistant-runtime coverage gaps in hosted runtime dispatch, context, execution, maintenance, parsers, and device-sync seams | in_progress | Narrow `packages/assistant-runtime/**` lane only. Prefer deterministic tests over source edits, preserve the existing dirty assistant-runtime worktree, keep shared/root coverage config unchanged unless an honest package-local blocker proves otherwise, and do not commit from worker sub-lanes. |
| Codex | Get `packages/cli` green and above package-local coverage thresholds | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-cli-package-green.md}`, `packages/cli/{src/**,test/**,package.json,vitest*.ts}` | package-local CLI failing tests, command/runtime branch coverage, prepared-runtime verification proof | completed | Narrow `packages/cli/**` package lane only. Overlaps the broader package-coverage cleanup row plus active hosted-runner/boundary work that also touches `packages/cli`; preserve those adjacent edits, avoid runner-packaging behavior or shared/root coverage-config churn unless a package-local fix proves it is unavoidable, prefer deterministic tests first, and commit only this lane's touched paths. Focused owned-file coverage now passes; package-wide coverage still has unrelated failures outside this lane. |
| Codex | Get `packages/core` green and above package-local coverage thresholds | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-core-package-green.md}`, `packages/core/**` | package-local core coverage gaps, failing tests, package-local verification | in_progress | Narrow `packages/core/**` lane only. Overlaps the broader package-coverage cleanup row; prefer deterministic tests before source edits, keep package/root coverage config unchanged unless required by an honest package-local fix, preserve unrelated dirty worktree edits, and commit only this lane's touched paths. |
| Codex | Get `packages/hosted-execution` green and above package-local coverage thresholds | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-execution-package-green.md}`, `packages/hosted-execution/**` | package-local hosted execution coverage for builders, dispatch refs, hosted email, observability, outbox payloads, parsers, and side effects | in_progress | Package-local lane only. Prefer deterministic tests over runtime changes, preserve the dirty shared worktree, avoid root/shared coverage-config edits unless package-local proof shows they are unavoidable, and do not commit from worker sub-lanes. |
| Codex | Get `packages/query` green and above package-local coverage thresholds | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-query-package-green.md}`, `packages/query/**` | package-local query coverage gaps, failing tests, package-local verification | in_progress | Narrow `packages/query/**` lane only. Overlaps the broader package-coverage cleanup row; prefer deterministic tests before source edits, keep package/root coverage config unchanged unless required by an honest package-local fix, preserve unrelated dirty worktree edits, and commit only this lane's touched paths. |
| Codex | Lift the remaining assistant-engine per-file coverage reds for cron/web/runtime/knowledge seams named by the user | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-engine-parallel-coverage.md}`, `packages/assistant-engine/{src/assistant/{cron.ts,cron/presets.ts,local-service.ts,outbox.ts,runtime-budgets.ts,turn-lock.ts,web-fetch.ts},src/knowledge/service.ts,test/{assistant-cron-thresholds.test.ts,assistant-runtime-thresholds.test.ts,assistant-knowledge-thresholds.test.ts}}` | assistant cron preset/render/install branches, local-service failure and typing branches, outbox dedupe/quarantine/delivery branches, runtime-budget maintenance branches, turn-lock queue/abort branches, web-fetch normalization/extraction branches, knowledge service lint/log/rebuild/search branches | completed | Landed deterministic coverage tests plus a small `turn-lock` helper refactor. `pnpm --dir packages/assistant-engine typecheck` and `pnpm --dir packages/assistant-engine test:coverage` now pass with the user-directed tail over threshold. |
| Codex | Clean up another batch of hacky import and mock patterns across package tests | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-test-import-hacks-cleanup-pass-3.md}`, `packages/{operator-config,assistantd,setup-cli,inboxd}/test/**` | repeated dynamic imports, reset/mock/import boilerplate, device-sync client seams, setup wizard imports, inbox HTTP seam | in_progress | Keep the pass scoped to the selected test files and preserve the existing dirty worktree. Prefer direct imports where mocks are stable and small helpers where lazy imports remain necessary. |
| Codex | Run `review:gpt` autosends for all eight audit presets, recover the returned ChatGPT thread URLs, and arm `work-with-pro` patch watchers | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-review-gpt-pro-eight-presets.md}`, `scripts/{review-gpt.sh,review-gpt.config.sh}`, `output-packages/review-gpt-pro-eight-presets/**` | `review:gpt` preset launcher, per-preset thread URL capture, `cobuild-review-gpt thread wake` watcher setup | in_progress | Workflow-only lane for outbound Pro orchestration. Preserve the existing dirty tree, do not touch unrelated package/app source, and treat any later returned patch as intent that must be merged narrowly on top of live files. |
| Codex | Land the downloaded legacy-removal patch for raw-import manifest and inbox iMessage compatibility branches | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/{contracts,core,inbox-services,vault-usecases}/**` | raw import manifest parsing/schema legacy removal, inbox iMessage runtime fallback removal | in_progress | Narrow external patch landing only. Preserve the dirty worktree, keep scope to the downloaded artifact intent plus necessary stale test updates, and avoid widening into other compatibility cleanups not covered by the patch summary. |
| Codex | Finish the remaining small `@murphai/assistant-engine` product/runtime coverage blockers in the owned assistant files | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{conversation-policy.ts,first-contact.ts,first-contact-welcome-delivery.ts,local-service.ts,memory/turn-context.ts,operator-authority.ts,prompt-attempts.ts,reply-sanitizer.ts,runtime-budgets.ts,diagnostics.ts,execution-plan.ts},test/assistant-product-small-seams.test.ts}` | small product/runtime branch coverage for conversation policy, first-contact helpers, local service helpers, turn context, authority/sanitizer/budget/diagnostic/execution-plan seams | in_progress | Narrow worker lane only. Prefer deterministic tests over source changes, stay inside the listed owned files plus this ledger row, preserve adjacent assistant-engine edits, keep canonical `src/**/*.ts` coverage wiring intact, do not touch root/shared coverage config, and do not commit. |
| Codex | Finish the remaining assistant-engine automation-support coverage blockers in the owned automation files | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/automation/{artifacts.ts,failure-observability.ts,grouping.ts,provider-watchdog.ts,prompt-builder.ts,runtime-lock.ts},test/assistant-automation-support.test.ts}` | automation artifacts/failure-observability/grouping/provider-watchdog/prompt-builder/runtime-lock branch coverage under the canonical `src/**/*.ts` gate | in_progress | Narrow worker lane only. Prefer deterministic tests over source changes, stay inside the listed owned files plus this ledger row, preserve adjacent assistant-engine edits, do not touch package/root coverage config, and do not commit. |
| Codex | Raise assistant-engine operations surfaces coverage in the owned bootstrap/local-service/outbox/runtime helpers | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{cli-surface-bootstrap.ts,local-service.ts},src/{cron/presets.ts,outbox.ts,outbox/retry-policy.ts,process-kill.ts,outbound-channel.ts},test/assistant-operations-final-coverage.test.ts}` | assistant operations bootstrap wiring, local-service guards, cron presets, outbox retry-policy, process-kill, outbound-channel branch coverage | in_progress | Narrow worker lane only. Prefer deterministic tests over source changes, stay inside the listed owned files plus this ledger row, preserve adjacent assistant-engine edits, keep coverage thresholds unchanged, run focused verification only, and do not commit. |
| Codex | Make `@murphai/inboxd-imessage` package-local coverage green with deterministic shared-runtime tests | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/inboxd-imessage/{src/shared-runtime.ts,test/**}` | shared-runtime raw-metadata sanitization and timestamp/text normalization branches | in_progress | Narrow `packages/inboxd-imessage/**` lane only. Prefer test-only changes, preserve unrelated dirty edits, do not touch root/shared coverage config, and do not commit from this worker lane. |
| Codex | Fix the remaining concrete package-local coverage reds, add missing package-local coverage commands, and bring `vault-usecases` onto an initial package-local test path | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-package-coverage-cleanup.md}`, `packages/{contracts,inboxd-imessage,cli,assistant-runtime,assistantd,core,hosted-execution,importers,parsers,query,runtime-state,vault-usecases}/**`, `scripts/build-test-runtime-prepared.mjs` | package-local coverage gaps in contracts/imessage shared runtime, CLI prepared-build coverage blocker, package-local `test:coverage` script rollout, initial `vault-usecases` package-local test/coverage setup | in_progress | Keep scope to package-local coverage proof and command wiring only. Preserve the existing dirty worktree, especially active assistant-engine, hosted-runner, hosted-web, and boundary-audit lanes. `inbox-services` stays follow-up only unless an honest narrow package-local coverage path becomes obvious. |
| Codex | Replace the hosted runner's synthetic staged vault-cli artifact with the real published `@murphai/murph` package | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-runner-published-cli.md}`, `ARCHITECTURE.md`, `apps/cloudflare/{package.json,README.md,DEPLOY.md,scripts/{assemble-runner-bundle.ts,runner-bundle-contract.ts},test/container-image-contract.test.ts}`, `pnpm-lock.yaml` | runner bundle package contract, direct `@murphai/murph` installation path, hosted CLI leaf dependency docs | in_progress | High-risk hosted-runner packaging lane. This intentionally reverses the older synthetic-artifact direction under the user's latest instruction. Keep scope out of unrelated `apps/cloudflare/src/**` runtime/container edits and preserve adjacent dirty `packages/cli/**` worktree changes. |
| Codex | Raise owned `@murphai/device-syncd` provider seams above the shared root per-file coverage gate | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-device-syncd-provider-coverage-worker.md}`, `packages/device-syncd/{src/providers/{garmin.ts,oura.ts,oura-webhooks.ts,shared-oauth.ts,whoop.ts},test/{garmin-provider.test.ts,oura-provider.test.ts,oura-webhooks.test.ts,shared-oauth.test.ts,whoop-provider.test.ts,public-ingress.test.ts}}` | Garmin/Oura/WHOOP provider edge branches, shared OAuth helpers, Oura webhook verification, provider-ingress callback paths | in_progress | Narrow provider-only lane. Prefer deterministic package-local tests over behavior changes, preserve adjacent in-flight `packages/device-syncd/**` edits from other lanes, do not touch package/root coverage config, and do not commit. |
| Codex | Align package Vitest coverage configs on the shared helper without changing the shared branch floor | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-vitest-threshold-helper-alignment.md}`, `config/vitest-coverage.ts`, `packages/{assistant-engine,assistant-runtime,cli,contracts,core,hosted-execution,importers,query}/vitest*.ts` | shared coverage helper wiring, helper-owned threshold constants, CLI coverage helper adoption | in_progress | Repo-internal config lane only. Preserve adjacent package-local coverage edits, keep the shared helper branch threshold at `80`, and avoid runtime/test behavior changes. |
| Codex | Split iMessage ownership out of `@murphai/inboxd` so hosted consumers can use inbox core without inheriting the iMessage closure | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-inboxd-imessage-split.md}`, `packages/{inboxd,inboxd-imessage,inbox-services,cli}/**`, and any directly required package manifests/tests | inbox core vs optional iMessage owner, inbox-services optional iMessage seam, local CLI/test seam updates | in_progress | High-risk package-boundary refactor. Avoid unrelated `packages/inboxd` and `packages/inbox-services` edits while this row is active. Preserve the existing dirty package-local coverage work and do not widen public surfaces with compatibility shims. |
| Codex | Raise `@murphai/assistant-engine` to the canonical repo coverage thresholds with another five parallel package-local test lanes | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-assistant-engine-parallel-coverage.md}`, `packages/assistant-engine/{src/**,test/**,vitest.config.ts}` | assistant-engine package-wide coverage, disjoint seam tests for codex runtime, cli-tools capability files, service orchestration files, model/inbox harness files, and store/runtime files | in_progress | Package-local assistant-engine lane only. Keep `coverage.include: ["src/**/*.ts"]` and the canonical repo threshold path, preserve unrelated dirty edits in the package, assign disjoint test-file ownership to parallel workers, do not add assistant-engine-specific thresholds, and do not commit from worker lanes. |
| Codex | Cover the owned `@murphai/assistant-engine` Codex runtime seam with focused package-local tests | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/{assistant-codex.ts,assistant-codex-events.ts},test/assistant-codex-runtime.test.ts}` | Codex CLI process execution seam, event normalization/progress shaping, stderr/stdout/session handling branches | in_progress | Narrow assistant-engine Codex lane only. Prefer tests over source edits, stay inside the listed owned files, preserve adjacent package edits from other workers, keep canonical package coverage wiring intact, and do not commit. |
| Codex | Cover the owned `@murphai/assistant-engine` channels runtime seam with focused package-local tests | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{channel-presentation.ts,channels/{runtime.ts,registry.ts,descriptors.ts,types.ts}},test/assistant-channels-runtime.test.ts}` | channels runtime delivery/typing assembly, adapter registry lookups, descriptor readiness/fallback branches, user-facing channel helper | in_progress | Narrow assistant-engine channels lane only. Prefer tests over source edits, stay inside the listed owned files, preserve adjacent package edits from other workers, keep canonical package coverage wiring intact, and do not commit. |
| Codex | Finish the remaining provider-related `@murphai/assistant-engine` coverage blockers in the owned provider/outbox seam | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{provider-binding.ts,provider-state.ts,provider-turn-recovery.ts,provider-catalog.ts,outbox.ts,outbox/retry-policy.ts},src/assistant/providers/{helpers.ts,registry.ts,codex-cli.ts},test/assistant-provider-final-coverage.test.ts}` | provider binding/state/recovery/catalog branches, provider registry and helper edge cases, Codex CLI provider branches, outbox and retry-policy near-misses under the canonical `src/**/*.ts` gate | in_progress | Narrow assistant-engine provider/outbox lane only. Prefer deterministic tests over source edits, stay inside the listed owned files plus the owned test file, preserve adjacent package edits from other workers, keep canonical package coverage wiring intact, do not touch shared coverage config, and do not commit. |
| Codex | Finish the remaining assistant-engine infrastructure and wrapper coverage blockers in the owned infra seam | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/{gateway-local-adapter.ts,outbound-channel.ts,process-kill.ts,knowledge/service.ts,assistant/{web-fetch.ts,session-resolution.ts,turn-lock.ts},assistant/store/persistence.ts},test/assistant-infra-final-coverage.test.ts}` | gateway-local adapter wrappers, outbound-channel edge branches, process kill ESRCH handling, knowledge service helpers, web-fetch near-miss helpers, session-resolution defaults, turn-lock abort/release branches, persistence near-miss branches | in_progress | Narrow assistant-engine infra lane only. Prefer deterministic tests over source edits, stay inside the listed owned files plus the owned test file, preserve adjacent package edits from other workers, keep canonical `src/**/*.ts` coverage intact, and do not commit. |
| Codex | Finish the remaining assistant-engine cron/channels/integration branch blockers in the owned cron lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{cron.ts,food-auto-log-hooks.ts},src/assistant/cron/{locking.ts,presets.ts},src/assistant/channels/runtime.ts,src/assistant/state/locking.ts,src/assistant/web-search/results.ts,test/assistant-cron-channels-branches.test.ts}` | cron orchestration branches, cron lock edge cases, preset parsing branches, channel runtime delivery/typing branches, state-lock edge cases, web-search result shaping | in_progress | Narrow assistant-engine cron/channels lane only. Prefer deterministic tests over source edits, stay inside the listed owned files plus the owned test file, preserve adjacent package edits from other workers, keep canonical package coverage wiring intact, and do not commit. |
| Codex | Raise owned `@murphai/device-syncd` non-provider seams above the shared root per-file coverage gate | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/{http.ts,service.ts,store.ts,public-ingress.ts},test/{http.test.ts,service.test.ts,store.test.ts,public-ingress.test.ts}}` | HTTP handler/listener branches, service reconcile-worker/store interplay, store branch coverage, public-ingress callback/webhook edge coverage | in_progress | Narrow package-local lane only. Preserve the current HTTP handler seam and listener-wiring regression test, build on top of the existing in-flight `http.test.ts` and `public-ingress.test.ts` edits, do not touch provider files or package/root coverage config, and do not commit. |
| Codex | Add deterministic gateway-core snapshot/event-log branch coverage in the owned local-runtime test file only | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/gateway-core/test/snapshot-event-log-local-runtime.test.ts` | snapshot/event-log polling clamp and timeout behavior, attachment fetch branch coverage, title/search fallbacks, cursor-not-found, retention/diff branches | in_progress | Narrow test-file-only lane under the active gateway-core coverage row. Preserve adjacent gateway-core coverage edits, reuse the existing snapshot/session setup, do not touch package config or runtime source, and do not commit. |
| Codex | Strengthen `@murphai/cloudflare-hosted-control` package-wide coverage without curated include lists | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/cloudflare-hosted-control.md}`, `packages/cloudflare-hosted-control/**` | package-wide `src/**/*.ts` coverage, client/parser branch gaps, barrel/contracts seam tests | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve existing in-flight `packages/cloudflare-hosted-control/vitest.config.ts` edits, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Raise `@murphai/gateway-local` to honest package-wide coverage with package-local tests only | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/gateway-local.md}`, `packages/gateway-local/**` | gateway-local package-wide coverage, local service/send/store/schema/snapshot-state seams, shared test helper reuse | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve the existing in-flight `packages/gateway-local/vitest.config.ts` edits, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Raise `@murphai/gateway-core` to honest package-wide coverage with package-local tests only | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,package-coverage-workers/gateway-core.md}`, `packages/gateway-core/**` | gateway-core package-wide coverage, snapshot/event-log/route/opaque-id seams, shared test helper reuse | in_progress | Package-local coverage lane only. Keep `coverage.include` on `src/**/*.ts`, preserve the existing in-flight `packages/gateway-core/vitest.config.ts` edit, spawn required GPT-5.4 medium subagents on disjoint seams, and do not commit. |
| Codex | Inspect and clean the `device-syncd` bin seam for test-only hacks or duplicated harness boilerplate | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/bin.ts,test/bin.test.ts}` | bin startup/shutdown wiring, mocked import harness, signal handling test seam | in_progress | Narrow seam-only cleanup. Stay inside `src/bin.ts` and `test/bin.test.ts`, preserve adjacent `packages/device-syncd/**` work, and do not commit. |
| Codex | Expand `@murphai/inboxd` package-local coverage readiness for the root rollout lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/inboxd/**` | inboxd package coverage config, shared test helpers, connector/kernel/parser/runtime seams | in_progress | Package-local coverage worker only. Do not touch root coverage config or other packages; report any root integration needs back to the parent rollout lane. |
| Codex | Add deterministic inboxd shared/barrel coverage for shared-runtime and shared helper seams | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/inboxd/test/inboxd-shared-barrels-coverage.test.ts` | shared-runtime helper branches, shared helper branch coverage, barrel/contracts type alignment | in_progress | Test-file-only inboxd lane. Keep scope to `packages/inboxd/test/inboxd-shared-barrels-coverage.test.ts`, preserve adjacent inboxd coverage edits, and do not touch package/root config or runtime source unless verification proves it is unavoidable. |
| Codex | Expand `@murphai/openclaw-plugin` package-local coverage readiness for the root rollout lane | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/openclaw-plugin/**` | openclaw-plugin package coverage proof, package-local vitest readiness | in_progress | Package-local coverage worker only. Do not touch root coverage config or other packages; report any root integration needs back to the parent rollout lane. |
| Codex | Review oversized `apps/web` TSX components and split the large ones into composable units | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-apps-web-tsx-composability.md}`, `apps/web/src/components/{hosted-onboarding/**,hosted-share/**,settings/**}` | oversized hosted-web TSX decomposition, extracted subcomponents/helpers | in_progress | Hosted-web-only refactor lane. Read current file state carefully because nearby hosted-auth and join-page rows already exist; preserve adjacent behavior and avoid style-only churn. |
| Codex | Add root coverage for tested packages that currently lack package-wide coverage gates, using one Codex worker per package plus centralized root integration | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-package-coverage-rollout.md,package-coverage-workers/**}`, `vitest.config.ts`, `config/**`, `packages/{assistant-engine,assistant-runtime,assistantd,cli,contracts,device-syncd,inboxd,messaging-ingress,openclaw-plugin}/**` | root coverage expansion, package-local vitest coverage config, shared test harness reuse | in_progress | Broad test/coverage lane. Package workers should stay within their assigned package plus prompt artifacts; root `vitest.config.ts` and shared harness changes are centrally integrated to avoid parallel conflicts. Preserve unrelated in-flight edits, especially the active hosted-runner and hosted-web lanes. |
| Codex | Cover the owned `@murphai/assistant-engine` provider seams with focused final coverage tests | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/{src/assistant/{provider-binding.ts,provider-catalog.ts,provider-state.ts,provider-turn-recovery.ts},src/providers/{helpers.ts,registry.ts,codex-cli.ts},test/assistant-provider-final-coverage.test.ts}` | provider binding/catalog/state/recovery helpers, provider registry lookups, Codex CLI env/error branches, provider helper edge cases | in_progress | Narrow provider-only lane. Prefer deterministic tests over source edits, stay inside the listed owned files plus this ledger row, preserve adjacent assistant-engine edits from other workers, keep canonical package coverage wiring intact, and do not commit. |
| Codex | Add shared package-local coverage thresholds and coverage scripts to the six newly tested standalone packages | `agent-docs/exec-plans/{active/COORDINATION_LEDGER.md,completed/2026-04-08-six-package-coverage-thresholds.md}`, `packages/{assistant-cli,cloudflare-hosted-control,gateway-core,gateway-local,operator-config,setup-cli}/{package.json,vitest.config.ts}` | package-local Vitest coverage helper wiring, shared threshold reuse, package coverage scripts | completed | Kept the lane package-local, reused the shared coverage helper, added explicit `test:coverage` scripts, and scoped the coverage include lists to the source seams these packages currently test so the shared thresholds are honest and green without touching root coverage config. |
| Codex | Remove duplicated inbox runtime ownership from assistant-engine in favor of inbox-services | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/src/{inbox-model-harness.ts,inbox-services.ts,inbox-app/**,inbox-services/**}` | inbox runtime ownership, assistant-engine inbox shim | completed | Narrow ownership cleanup only. Preserved unrelated assistant-engine edits and left non-public utility duplicates such as `process-kill` and `assistant-vault-paths` untouched because inbox-services does not expose them as public surfaces. |
| Codex | Land the downloaded owner-scoped raw asset patch across contracts/core/workout import | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-raw-owner-model.md}`, `ARCHITECTURE.md`, `docs/architecture.md`, `packages/contracts/src/{constants.ts,zod.ts}`, `packages/core/src/{constants.ts,raw.ts,event-attachments.ts,mutations.ts,vault.ts,index.ts,assessment/storage.ts,domains/events.ts,operations/raw-manifests.ts}`, `packages/core/test/{core.test.ts,raw-owner-model.test.ts}`, `packages/{assistant-engine,vault-usecases}/src/usecases/workout-import.ts`, `packages/cli/test/{cli-expansion-document-meal.test.ts,cli-expansion-export-intake.test.ts}` | raw asset owner model, raw manifest owner validation, workout CSV import manifest wiring | in_progress | Narrow patch landing only. Preserve unrelated dirty `packages/assistant-engine/src/assistant/**` edits and avoid unrelated assistant-engine refactors. |
| Codex | Land the downloaded boundary-audit patch across vault-usecases, assistant runtimes, and workspace guardrails | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/{vault-usecases,operator-config,assistant-engine,assistant-runtime,assistantd,cli}/**`, `scripts/verify-workspace-boundaries.mjs` | runtime-unavailable helper ownership, vault-services subpath adoption, assistant-provider export tightening, boundary guardrails | in_progress | Narrow patch landing only. Keep scope to the downloaded boundary audit intent, preserve unrelated dirty worktree edits, and update only the tests or mocks needed to keep the patch green on the live tree. |
| Codex | Auto-submit hosted phone auth when a 6-digit code is fully entered | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-phone-auth-auto-submit.md}`, `apps/web/{src/components/hosted-onboarding/hosted-phone-auth.tsx,test/hosted-phone-auth.test.ts}` | verification-code auto submit | in_progress | Narrow hosted-auth tweak only. Preserve adjacent hosted-web work and keep the behavior scoped to auto-submitting once the code reaches six digits. |
| Codex | Refactor Cloudflare hosted runner into a clean leaf artifact with runtime/deploy ownership split | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-cloudflare-runner-leaf-artifact.md}`, `Dockerfile.cloudflare-hosted-runner`, `.dockerignore`, `apps/cloudflare/{package.json,README.md,DEPLOY.md,tsconfig*.json,src/**,scripts/**,test/**}`, `packages/{assistant-engine,assistant-runtime,cli}/**`, `.github/workflows/deploy-cloudflare-hosted.yml`, `package.json`, `pnpm-lock.yaml` | hosted runner leaf artifact, runtime-vs-deploy ownership split, explicit artifact contracts | in_progress | Exclusive high-risk deploy/runtime refactor. Avoid overlapping `apps/cloudflare` runtime or deploy-automation edits while this row is active. Preserve unrelated worktree edits elsewhere. |
| Codex | Replace the Cloudflare runner's direct `@murphai/murph` dependency with a smaller staged `vault-cli` runtime artifact | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-runner-packaging-refactor.md}`, `apps/cloudflare/package.json`, `apps/cloudflare/scripts/assemble-runner-bundle.ts`, `packages/cli/**`, `pnpm-lock.yaml` | runner `vault-cli` artifact, staged CLI wrapper, hosted runner runtime dependency reduction | in_progress | Narrow sub-lane under the active hosted-runner packaging refactor. Stay out of unrelated `apps/cloudflare` runtime/container files and preserve other in-flight packaging edits. |
| Codex | Reduce hosted UX latency in webhook, share-acceptance, and device-sync wake paths | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-latency-followups.md}`, `apps/web/{app/api/internal/hosted-onboarding/webhook-receipts/cron/route.ts,app/api/hosted-onboarding/linq/webhook/route.ts,vercel.json,README.md,src/lib/{hosted-onboarding/{webhook-service.ts,webhook-receipt-engine.ts,webhook-receipt-store.ts,webhook-receipts.ts},hosted-share/acceptance-service.ts,device-sync/wake-service.ts,hosted-execution/outbox.ts},test/{hosted-onboarding-linq-dispatch.test.ts,hosted-onboarding-linq-route.test.ts,hosted-onboarding-webhook-idempotency.test.ts,hosted-onboarding-webhook-receipt-cron.test.ts,device-sync-hosted-wake-dispatch.test.ts,hosted-share-acceptance.test.ts,hosted-execution-routes.test.ts}}` | deferred webhook receipt draining, receipt cron recovery, immediate outbox drains | completed | Preserve adjacent hosted-web changes; do not touch unrelated invite diagnostics work beyond shared files if conflicts appear. |
| Codex | Add Vercel Analytics and Speed Insights to the hosted web root layout | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-vercel-analytics-root-layout.md}`, `apps/web/{app/layout.tsx,package.json}`, `pnpm-lock.yaml` | `RootLayout` | in_progress | Keep scope to hosted-web analytics wiring only; preserve adjacent hosted-web edits and avoid unrelated layout or style changes. |
| Codex | Align hosted Privy client auth with local-storage session docs | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-privy-local-storage-client-api.md}`, `apps/web/{src/components/hosted-onboarding/client-api.ts,src/lib/hosted-onboarding/privy.ts,test/{hosted-onboarding-client-api.test.ts,hosted-onboarding-request-auth.test.ts}}` | local-storage bearer + identity token auth, remove cookie-first request path | in_progress | Overlaps adjacent hosted-auth work; keep scope to auth transport simplification only and preserve unrelated phone-flow edits in nearby files. |
| Codex | Gate invite join-page manual phone entry before send-code submit | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{src/components/hosted-onboarding/{hosted-phone-auth.tsx,hosted-phone-auth-views.tsx},test/hosted-phone-auth.test.ts}` | manual phone-entry send-code disabled state | in_progress | Narrow hosted-web fix only; preserve adjacent hosted auth changes and existing `apps/web/next-env.d.ts` worktree edits. |
| Codex | Surface hosted post-checkout activation progress on join and success pages | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-activation-progress.md}`, `apps/web/{app/join/[inviteCode]/success/page.tsx,src/components/hosted-onboarding/join-invite-client.tsx,src/lib/hosted-onboarding/{billing.ts,invite-service.ts,lifecycle.ts,types.ts},test/{hosted-onboarding-billing-service.test.ts,join-invite-client.test.ts}}` | hosted onboarding stage derivation, Stripe success URL, activation-pending UI copy and polling | in_progress | Overlaps an existing uncommitted edit in `join-invite-client.tsx`; preserve the current active-state redesign while adding the post-checkout activation state. |
| Codex | Add a minimal hosted settings subscription-management handoff to Stripe | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-hosted-settings-stripe-portal.md}`, `apps/web/{app/{api/settings/billing/portal/route.ts,settings/page.tsx},src/components/settings/hosted-billing-settings.tsx,test/**}` | hosted settings billing portal handoff | completed | Added an authenticated Stripe Billing Portal route that uses the current member's stored customer id, plus a compact settings card/button that redirects the browser to Stripe. |
| Codex | Fix hosted onboarding join page staying on phone-entry after send-code confirm | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{src/components/hosted-onboarding/{hosted-phone-auth.tsx,hosted-phone-auth-views.tsx},test/hosted-phone-auth.test.ts}` | invite phone verification step rendering, invite/public auth flow split, background send-code confirm | completed | Root cause was client rendering: invite shortcut moved to code step but still showed the manual phone-entry form. Refactored the component tree into separate invite/public top-level flows with a shared authenticated-state panel, then moved invite `send-code/confirm` off the blocking click path so the code-entry UI can appear as soon as Privy sends the SMS. Preserved unrelated hosted-web edits, including existing `apps/web/next-env.d.ts` worktree changes. |
| Codex | Raise the default hosted-web input height | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{components/ui/input.tsx,test/settings-input.test.ts}` | `Input` | completed | Updated the shared hosted-web input primitive default height from `h-8` to `h-14` and added a focused render test under an existing Vitest workspace include pattern. |
| Codex | Normalize hosted auth input/button sizing and full-width secondary actions | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{components/ui/button.tsx,src/components/hosted-onboarding/hosted-phone-auth-views.tsx,test/{hosted-phone-auth.test.ts,settings-button.test.ts}}` | `Button`, hosted phone auth secondary actions | completed | Raised shared hosted-web button defaults to `h-14`, removed remaining local `h-12` overrides in hosted phone auth, updated invite shortcut copy, and unified every `Use a different number` control in hosted auth to a full-width link-style helper with a smaller preset height. |
| Codex | Land the downloaded warm-container patch for hosted per-user Cloudflare runner reuse | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-warm-container-reuse.md}`, `ARCHITECTURE.md`, `apps/cloudflare/{README.md,DEPLOY.md,src/{container-entrypoint.ts,deploy-automation/environment.ts,node-runner-isolated.ts,runner-container.ts,user-runner/runner-dispatch-processor.ts},test/{container-entrypoint.test.ts,helpers/hosted-job.ts,node-runner-isolated.test.ts,runner-container.test.ts,stubs/cloudflare-containers.ts}}` | RunnerContainer warm lifecycle, container bridge concurrency, isolated child cleanup | in_progress | High-risk hosted-runner lane. Preserve unrelated hosted-web worktree edits; keep scope to the downloaded patch intent and Cloudflare runner/docs/test surface only. |
| 2026-04-08 | Codex | Test import cleanup pass 4 | active | Reduce another batch of duplicated dynamic-import and reset/mock/import test patterns across selected `packages/{cli,vault-usecases}/test/**` files. |
| Codex | Final pass for source-level test hacks and remaining duplicated test seams | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-final-test-hacks-pass-5.md}`, selected clean files under `packages/{assistant-cli,device-syncd,importers,inboxd,setup-cli}/**` | source-level test-only seams, remaining mocked-import boilerplate, final narrow cleanup opportunities | active | Final cleanup lane only. Inspect source-level code for hacks that exist purely to make testing easier; remove them only when clearly unnecessary and only within clean package slices that do not overlap the current dirty worktree. |
| Codex | Review the `device-syncd` HTTP seam for test-only exports and duplicated test harness code | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-final-test-hacks-pass-5.md}`, `packages/device-syncd/{src/http.ts,test/http.test.ts}` | `createDeviceSyncHttpRequestHandler`, HTTP test harness helpers | in_progress | Narrow seam-only overlap under the active final test-hacks pass. Preserve unrelated `packages/device-syncd/**` files and avoid broader package cleanup outside this source/test pair. |
| Codex | Package-wide final pass for source-level test hacks and duplicated test patterns | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-package-wide-test-hacks-pass-6.md}`, `packages/{assistant-cli,device-syncd,importers,operator-config,setup-cli}/**` | package-wide source/test coupling review, test-only seams, duplicated test helpers and lazy imports | active | Broader package-local cleanup lane. Each worker owns one whole clean package and should inspect source for test-only hacks before changing tests; preserve unrelated dirty worktree edits outside the owned package. |
| Codex | Final package-wide cleanup pass for `packages/operator-config` source/test seams | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-operator-config-final-cleanup.md}`, `packages/operator-config/**` | operator-config source-level test seams, mocked-import cleanup, duplicated package-local test helpers | in_progress | Narrow `packages/operator-config/**` lane only. Audit source first for seams that exist mainly to make tests easier; remove them only when clearly unnecessary, otherwise keep fixes test-local and preserve all unrelated repo edits. |
| Codex | Review the operator-config device-daemon slice for test-only seams and duplicate mocked-import helpers | `agent-docs/exec-plans/active/{COORDINATION_LEDGER.md,2026-04-08-operator-config-final-cleanup.md}`, `packages/operator-config/{src/device-daemon.ts,src/device-daemon/**,test/device-daemon-runtime.test.ts,test/import-with-mocks.ts}` | device-daemon dependency seam audit, mocked-import helper reuse, package-local test boilerplate cleanup | in_progress | Narrow device-daemon-owned lane only. Preserve unrelated `packages/operator-config/**` edits, prefer test-local cleanup unless a source seam is clearly unnecessary, and do not commit. |
