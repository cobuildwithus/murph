# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Land test harness speedups for incremental runtime builds and app verify concurrency | `package.json`, `scripts/workspace-verify.sh`, `packages/cli/package.json`, `packages/cli/test/cli-test-helpers.ts`, `apps/web/package.json`, `tsconfig.test-runtime.json` | `build:test-runtime`, `build:workspace:incremental`, `run_test_apps`, `rebuildCliRuntimeArtifacts` | in_progress | Narrow supplied-patch landing. Preserve current repo acceptance surface and existing root `vitest.config.ts` behavior. |
| Codex | Safe Vercel production-domain env fallbacks for hosted web and hosted-execution readers | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/app/layout.tsx`, `apps/web/src/lib/device-sync/env.ts`, `apps/web/src/lib/hosted-onboarding/env.ts`, `apps/web/src/lib/hosted-onboarding/landing.ts`, `apps/web/test/env.test.ts`, `apps/web/test/hosted-onboarding-env.test.ts`, `apps/web/test/hosted-onboarding-landing.test.ts`, `apps/web/README.md`, `packages/hosted-execution/src/env.ts`, `packages/hosted-execution/test/hosted-execution.test.ts` | hosted web env fallback resolution, `readHostedExecutionWebControlPlaneEnvironment`, onboarding/device-sync public base URL defaults | in_progress | Narrow runtime hardening change. Keep explicit env overrides authoritative; only fall back to Vercel's canonical production domain and do not infer from request hosts. |
| Codex | Add safe Vercel production-domain fallbacks for hosted web env defaults | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/src/lib/device-sync/env.ts`, `apps/web/src/lib/hosted-onboarding/env.ts`, `apps/web/src/lib/hosted-onboarding/landing.ts`, `apps/web/app/layout.tsx`, related hosted-web env tests | hosted web base URL fallback helpers, device-sync origin defaults, hosted onboarding public URL defaults | in_progress | Narrow runtime-config hardening. Preserve explicit env override precedence and do not weaken request-bound auth or origin validation semantics. |
| Codex | Fix assistantd startup env parity so daemonized email delivery sees repo-local AgentMail credentials | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistantd/src/bin.ts`, `packages/assistantd/src/config.ts`, `packages/assistantd/test/config.test.ts`, `packages/assistantd/README.md`, `agent-docs/operations/verification-and-runtime.md`, `agent-docs/references/testing-ci-map.md` | `loadAssistantdEnvFiles`, assistantd startup env loading, assistantd env-loading coverage | in_progress | Narrow runtime fix. Keep exported shell variables authoritative while letting assistantd load `.env.local` and `.env` from its launch cwd before startup, matching the CLI behavior. |
| Codex | Add missing hosted web env placeholders and Vercel setup docs for hosted execution/device-sync secrets | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/.env.example`, `apps/web/README.md`, `apps/web/test/vercel-config.test.ts` | hosted web env template/docs coverage for Vercel-managed secrets | in_progress | Narrow docs/config patch only. Do not change runtime behavior or expose real secret material. |
