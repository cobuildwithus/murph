# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Cloudflare user-runner test fixture hard-cut | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/test/user-runner.test.ts` | required `assistantDeliveryEffects` commit payloads, no legacy `sideEffects` fallback in stale tests | completed | Verified the targeted `user-runner` commit payload sites were already aligned in the current worktree and confirmed the slice with `pnpm --dir apps/cloudflare typecheck`. Preserve unrelated dirty Cloudflare/runtime/deploy edits already in progress. |
| Codex | Device-sync hosted apply-response hard-cut | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/hosted-runtime.ts,test/hosted-runtime.test.ts,README.md}` | explicit hosted apply-response `writeUpdate` requirement, legacy backfill removal | in_progress | Narrow `device-syncd` parser/test/doc slice only. Preserve overlapping greenfield cleanup work and any unrelated dirty edits already present in the ledger or adjacent package files. |
| Codex | Hosted onboarding contact-privacy test isolation | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/test/{contact-privacy-member-lookups.test.ts,hosted-onboarding-member-store.test.ts,hosted-onboarding-member-identity-service.test.ts}` | deterministic contact-privacy keyring/version expectations under injected env | in_progress | Narrow apps/web test-only slice to stop hosted onboarding/contact-privacy tests from depending on the live injected Vercel keyring version. Preserve unrelated dirty `apps/web/**`, package metadata, and active hosted onboarding runtime lanes outside these tests. |
| Codex | Greenfield cleanup non RevNet | `agent-docs/exec-plans/active/{2026-04-13-greenfield-cleanup-non-revnet.md,COORDINATION_LEDGER.md}`, plus worker-owned slices under Cloudflare deploy/docs, hosted assistant-delivery runtime seams, inboxd persistence, and device-sync runtime apply parsing | direct-deploy-only hosted cleanup, hosted assistant-delivery canonical shape, inboxd raw-capture hard-cut, explicit hosted device-sync write results | in_progress | Parallel workers own disjoint write scopes. Preserve unrelated dirty `apps/web` hosted onboarding/auth route and test edits already in progress. RevNet is explicitly out of scope for this task. |
| Codex | Greenfield legacy audit and Prisma bootstrap collapse | `agent-docs/exec-plans/active/{2026-04-13-greenfield-legacy-audit.md,COORDINATION_LEDGER.md}`, `apps/web/prisma/{schema.prisma,migrations/**}`, and review-only inspection across `apps/**`, `packages/**`, `config/**`, `scripts/**`, and durable docs | pre-launch compatibility/legacy/v2-v3 cleanup targets, hosted Prisma bootstrap migration | completed | Refreshed the single hosted bootstrap migration so `apps/web/prisma/migrations/2026040600_init/migration.sql` matches Prisma's generated schema baseline exactly. Direct Prisma diff proof, focused migration test, and Prisma validation passed. `pnpm verify:acceptance` remains blocked by apparently unrelated pre-existing assistant-runtime/CLI/background package coverage failures. Parallel seam reviews identified likely follow-up cleanup targets in dormant RevNet paths, Cloudflare deploy-mode branching, hosted assistant-delivery compatibility aliases, inboxd raw-capture rescue logic, and device-sync legacy internal response backfill. Preserve unrelated dirty `apps/web` hosted onboarding/auth route and test edits outside this slice. |
| Codex | Delete hosted Privy-user reset helper and local skill installs | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/scripts/local-reset-hosted-onboarding.ts`, local Codex-home `skills/delete-privy-user` directories | hosted onboarding reset helper removal, local skill uninstall | completed | Removed the six local Codex-home `delete-privy-user` skill directories and deleted `apps/web/scripts/local-reset-hosted-onboarding.ts`. Verified with `pnpm typecheck`, `pnpm test:smoke`, direct absence checks for the script and installed skill directories, and `git diff --check` on the touched repo paths. Preserve unrelated dirty `apps/web/next-env.d.ts`, `package.json`, and `pnpm-lock.yaml` edits outside this slice. |
| Codex | Hosted onboarding local-dev contact-privacy fallback | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/src/lib/hosted-onboarding/env.ts`, `apps/web/test/hosted-onboarding-env.test.ts` | non-production contact-privacy keyring fallback, production env hard requirement | completed | Added a non-production fallback keyring in `readHostedContactPrivacyKeyring()` so local development/test no longer require `HOSTED_CONTACT_PRIVACY_KEYS` at startup, while production still throws when the env is absent. Verified with `pnpm --dir apps/web exec vitest run --config vitest.workspace.ts test/hosted-onboarding-env.test.ts` and direct `pnpm exec tsx -e \"...readHostedOnboardingEnvironment({ NODE_ENV: 'development' })...\"` proof returning `v1`. Preserve unrelated dirty `apps/web/**`, `package.json`, and `pnpm-lock.yaml` edits outside this env/test slice. |
| Codex | Hosted web dev command default | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{package.json,README.md}` | hosted-web default dev command via Vercel env injection | completed | `pnpm --dir apps/web dev` now routes through `vercel env run`, with `dev:local` and `dev:turbopack:local` retained as explicit raw-env bypass scripts. Direct script assertions and `git diff --check` passed. App-local `tsc --noEmit` remains blocked by unrelated pre-existing `apps/web/test/hosted-onboarding-routes.test.ts` `NODE_ENV` readonly assignments. Preserve unrelated dirty `apps/web/src/lib/hosted-onboarding/{http.ts,request-auth.ts}`, `apps/web/test/hosted-onboarding-routes.test.ts`, and `apps/web/next-env.d.ts` edits outside this slice. |
| Codex | Hosted onboarding Cloudflare pre-provision warmup | `agent-docs/exec-plans/active/{2026-04-13-hosted-preprovision-warmup.md,COORDINATION_LEDGER.md}`, `apps/web/{app/api/hosted-onboarding/{billing/checkout,privy/complete}/route.ts,src/lib/{hosted-execution/control.ts,hosted-onboarding/authentication-service.ts},test/{hosted-execution-control.test.ts,hosted-onboarding-billing-checkout-route.test.ts,hosted-onboarding-privy-complete-route.test.ts}}` | hosted execution best-effort pre-provision helper, post-verification checkout warmup, checkout-route fallback warmup | completed | Landed in commit `2c9de9a2` with focused Vitest coverage and `pnpm --dir apps/web typecheck` green. `pnpm --dir apps/web verify` still hits the unrelated pre-existing hosted contact-privacy env mismatch (`HOSTED_CONTACT_PRIVACY_CURRENT_KEY_VERSION "v2"` missing from `HOSTED_CONTACT_PRIVACY_KEYS`). Preserve unrelated dirty `package.json` and `pnpm-lock.yaml` edits outside this slice. |
| Codex | Hosted onboarding timing logs | `agent-docs/exec-plans/active/{2026-04-13-hosted-onboarding-timing-logs.md,COORDINATION_LEDGER.md}`, `apps/web/{app/api/hosted-onboarding/{billing/checkout,privy/complete}/route.ts,src/lib/{hosted-execution/control.ts,hosted-onboarding/{authentication-service.ts,billing-service.ts,logging.ts,member-activation.ts,stripe-event-reconciliation.ts}},test/{hosted-execution-control.test.ts,hosted-onboarding-billing-checkout-route.test.ts,hosted-onboarding-billing-service.test.ts,hosted-onboarding-logging.test.ts,hosted-onboarding-member-activation.test.ts,hosted-onboarding-privy-complete-route.test.ts,hosted-onboarding-privy-service.test.ts,hosted-onboarding-stripe-event-reconciliation.test.ts}}` | hosted onboarding timing helper, route/service latency instrumentation, activation and reconciliation timing breadcrumbs | completed | Added a shared sanitized timing logger plus route/service breadcrumbs for Privy completion, checkout creation, Cloudflare crypto warmup, member activation, and Stripe reconciliation. Focused Vitest passed for the touched suites and `pnpm --dir apps/web typecheck` passed. `pnpm --dir apps/web verify` still fails only in the pre-existing `dev:smoke` step because `/api/device-sync/agents/pair` requires `DATABASE_URL` during page-data collection. Preserve unrelated dirty `package.json` and `pnpm-lock.yaml` edits outside this slice. |
| Codex | Hosted onboarding Privy auth fast path | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{app/api/hosted-onboarding/privy/complete/route.ts,src/lib/hosted-onboarding/{request-auth.ts,member-identity-service.ts,page-auth.ts},test/{hosted-onboarding-routes.test.ts,hosted-session.test.ts,page-auth.test.ts}}` | session-only Privy auth context, parallel hosted member identity lookup, completion-route auth narrowing, homepage auth degradation on invalid Privy cookies | in_progress | Narrow `apps/web` hosted onboarding auth slice to avoid redundant DB lookups on completion routes and degrade invalid homepage Privy cookies to signed-out state. Preserve unrelated dirty `apps/web/src/lib/hosted-onboarding/http.ts`, `apps/web/test/hosted-onboarding-routes.test.ts`, `apps/web/next-env.d.ts`, `package.json`, and `pnpm-lock.yaml` edits outside this slice. |
| Codex | Hosted onboarding development error logging | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/src/lib/hosted-onboarding/http.ts`, `apps/web/test/hosted-onboarding-routes.test.ts` | hosted onboarding unexpected-error dev logging, sanitized message/stack details | completed | Narrow `apps/web` slice landed: unexpected hosted onboarding route errors now add development-only sanitized message/stack/details logging while production/test keep the compact payload. Fixed audit-found gaps by redacting top-level sensitive error keys, redacting `file:///...` stack paths, and adding both development and non-development regression coverage in `hosted-onboarding-routes.test.ts`. `git diff --check` passed. Local placeholder `.env.local` was created temporarily during verification triage and removed once the user clarified runtime env injection should come from `vercel env run`. `vercel env run -- pnpm verify` now provides the truthful env path; that command fails in unrelated pre-existing hosted onboarding/contact-privacy tests expecting `v1` blind-index versions under the current injected env, not in this logging slice. Preserve unrelated dirty `apps/web/**`, `package.json`, and `pnpm-lock.yaml` edits outside this wrapper/test slice. |
| Codex | Hosted web local-secret cleanup | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{README.md,package.json,prisma.config.ts}`, local untracked `apps/web/.env` and `apps/web/.env.local` | hosted-web local dev secret-injection guidance | completed | Production Vercel secret-bearing envs were converted to `sensitive` where applicable without exporting remote values. Preview was intentionally left unchanged per user instruction. Removed local `apps/web/.env` and `.env.local`, added `pnpm dev:vercel`, and stopped `prisma.config.ts` from auto-loading local env files. Direct checks passed (`tsx` import of `prisma.config.ts`, script assertion, `git diff --check`, file-removal proof). `pnpm --dir apps/web verify` was queued behind another active repo lock holder and app-local `tsc --noEmit` is currently blocked by unrelated pre-existing `apps/web/test/hosted-onboarding-routes.test.ts` `NODE_ENV` readonly errors. Preserve unrelated dirty `apps/web/src/lib/hosted-onboarding/http.ts` and `apps/web/test/hosted-onboarding-routes.test.ts` edits outside this slice. |
| Codex | Code-quality audit wake patch landing | `agent-docs/exec-plans/active/{2026-04-13-code-quality-audit-wake.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/src/hosted-email/routes.ts`, `packages/assistant-engine/src/assistant/outbox/retry-policy.ts`, `packages/hosted-execution/src/outbox-payload.ts`, targeted tests only if required by verification or audit passes | hosted-email routing-store helper extraction, explicit previous verified-email deletion target, named assistant outbox retry markers/helpers, split hosted outbox payload readers | completed | Landed the supplied patch as-is, passed truthful `pnpm test:diff apps/cloudflare packages/assistant-engine packages/hosted-execution`, `pnpm test:smoke`, and `git diff --check`, and confirmed the required audit passes returned no edits/findings. Repo-wide `pnpm typecheck` is blocked only by the unrelated dirty `apps/web/test/hosted-onboarding-webhook-receipt-codec.test.ts` `DbNull` error. The same-thread review draft staged with attachments but auto-submit failed with `send-button-disabled`, so delivery is unconfirmed; the requested depth-0 wake hop is armed and `waiting` under `output-packages/chatgpt-watch/69dc20fe-9540-83a1-b07c-14c03724401c-2026-04-13T004356Z/`. Preserve unrelated dirty `apps/web/**` work outside this slice. |
| Codex | Privacy target-area review patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/device-syncd/{src/store/webhook-traces.ts,test/{store.test.ts,webhook-traces.test.ts}}`, `apps/web/src/lib/device-sync/{prisma-store.ts,prisma-store/webhook-traces.ts}`, `apps/web/src/lib/hosted-onboarding/{webhook-provider-linq.ts,webhook-receipt-codec.ts,webhook-receipt-types.ts,webhook-transport.ts}`, `apps/web/test/{device-sync-webhook-traces.test.ts,hosted-onboarding-linq-dispatch.test.ts,hosted-onboarding-linq-transport.test.ts,hosted-onboarding-webhook-receipt-codec.test.ts,hosted-onboarding-webhook-receipt-transitions.test.ts,prisma-store-device-sync-signal.test.ts}` | minimized webhook-trace account sentinel, typed Linq side-effect payload unions, redirect fallback phone persistence | completed | Landed the supplied patch narrowly. Passed `pnpm typecheck`, `pnpm test:diff packages/device-syncd apps/web`, focused `packages/device-syncd` Vitest, focused hosted-web Vitest for the touched webhook/receipt seams, and `git diff --check`. Local final review only; audit subagents were not used because this tool session does not authorize delegation without an explicit user request. Preserve unrelated dirty `apps/cloudflare/**`, `packages/assistant-engine/**`, `packages/hosted-execution/**`, and the untracked active-plan file outside this slice. |
| Codex | Package-boundary watch follow-up patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/{assistant-engine/{src/assistant/state-ids.ts,test/assistant-state-secrets.test.ts,tsconfig.typecheck.json},cli/src/commands/search.ts,operator-config/{src/{assistant-cli-contracts.ts,vault-cli-contracts.ts},test/{assistant-cli-contracts.test.ts,assistant-seam-coverage.test.ts,vault-cli-contracts.test.ts},vitest.config.ts},query/{README.md,package.json,src/{entity-families.ts,index.ts,model.ts},test/{entity-families.test.ts,query.test.ts}},runtime-state/{README.md,package.json,src/{assistant-ids.ts,index.ts},test/{assistant-ids.test.ts,package-boundary.test.ts},tsconfig.typecheck.json},vault-usecases/src/usecases/integrated-services.ts}`, `scripts/verify-workspace-boundaries.mjs` | assistant opaque-id normalization subpath owner, query entity-family subpath owner, boundary-guard tightening, focused regression tests | completed | Landed the still-applicable follow-up from `murph-target-area-review.patch`: assistant opaque-id normalization now lives on `@murphai/runtime-state/assistant-ids`, query entity-family metadata now lives on `@murphai/query/entity-families`, root barrels no longer leak those helpers, and focused regression tests lock the trimmed-id behavior plus the dedicated subpaths. Passed `pnpm verify:workspace-boundaries`, `pnpm verify:workspace-package-cycles`, `pnpm test:smoke`, `pnpm --dir packages/runtime-state test:coverage`, `pnpm --dir packages/query test:coverage`, `pnpm --dir packages/operator-config test:coverage`, `pnpm --dir packages/vault-usecases test:coverage`, and `git diff --check`. `pnpm test:diff packages/runtime-state packages/query packages/operator-config packages/assistant-engine packages/cli packages/vault-usecases scripts/verify-workspace-boundaries.mjs` is blocked by the unrelated pre-existing `packages/device-syncd/test/store.test.ts` webhook-trace expectation failure, and full `pnpm typecheck` is blocked by unrelated dirty `apps/web/test/hosted-onboarding-webhook-receipt-transitions.test.ts` errors around `HostedWebhookLinqMessagePayload.inviteId`. Preserve the existing dirty `apps/web/**` and `packages/device-syncd/**` work outside this slice. |
| Codex | Architecture review wake follow-up patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/gateway-core/{src/{reply-routes.ts,routes.ts},test/reply-routes.test.ts}`, `packages/assistant-engine/{src/assistant/channels/{helpers.ts,descriptors.ts,registry.ts,types.ts},test/{channel-binding-delivery.test.ts,channel-helpers.test.ts}}` | gateway-owned channel reply-policy inference, assistant adapter default delivery inference, focused reply-route regressions | completed | Landed the watched-thread follow-up patch narrowly: `gateway-core` now owns channel reply-policy inference via `inferGatewayReplyRouteForChannel(...)`, assistant adapters default to that shared policy, and focused tests now also lock thread-first telegram/email behavior when both thread and participant are present. Passed `pnpm --dir packages/gateway-core build`, `pnpm typecheck`, truthful `pnpm test:diff packages/gateway-core packages/assistant-engine` twice (before and after the audit-added test coverage), `pnpm test:smoke`, and `git diff --check`. Required `coverage-write` added only test coverage, and final review returned no findings. |
| Codex | Watched security review patch landing | `agent-docs/exec-plans/active/2026-04-13-security-review-watch-patch.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{app/api/device-sync/oauth/[provider]/callback/route.ts,src/lib/{http.ts,device-sync/{http.ts,prisma-store.ts,prisma-store/oauth-sessions.ts,prisma-store/token-audits.ts,prisma-store/types.ts}},test/{device-sync-callback-route.test.ts,device-sync-http.test.ts,prisma-store-oauth-connection.test.ts,prisma-store-token-audits.test.ts}`, `packages/device-syncd/{src/{http.ts,public-ingress.ts,service.ts,store.ts,store/oauth-states.ts,types.ts},test/{http-redirects.test.ts,http.test.ts,public-ingress.test.ts,service.test.ts,store.test.ts}}` | hosted callback param decoding, safe `returnTo` parsing, provider-aware OAuth state consumption, hosted token-audit session attribution | completed | Landed the watched security patch narrowly, fixed the final-review follow-up around the hosted callback `TypeError` catch, and reran `pnpm typecheck`, `pnpm test:diff apps/web packages/device-syncd`, `pnpm test:smoke`, and `git diff --check` successfully. Direct proof confirmed hosted/local redirect helpers fail closed on `javascript:` `returnTo`. Required `coverage-write` made no changes. The required same-thread `pnpm review:gpt --send ...` attempt staged attachments but failed with `send-button-disabled`; refreshed thread export still showed no new user turn, so review delivery is unconfirmed. Detached depth-0 wake watcher armed at `output-packages/chatgpt-watch/69dc208a-f708-8399-80d6-eb93a517e662-2026-04-13T000128Z/` and is currently `waiting`. |
| Codex | Package-boundaries wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/runtime-state/{src/{assistant-ids.ts,index.ts},test/{assistant-ids.test.ts,package-boundary.test.ts}}`, `packages/operator-config/{src/assistant-cli-contracts.ts,test/assistant-seam-coverage.test.ts}`, `packages/assistant-engine/src/assistant/state-ids.ts`, `packages/vault-usecases/{src/{query-runtime.ts,runtime.ts,usecases/integrated-services.ts},test/query-runtime.test.ts}`, `packages/cli/src/commands/search.ts`, `scripts/verify-workspace-boundaries.mjs` | shared assistant opaque-id validator owner, query entity-family owner seam, focused boundary regressions | completed | Landed the watched-thread patch narrowly: `@murphai/runtime-state` now owns `isValidAssistantOpaqueId`, `@murphai/query` remains the only `ALL_QUERY_ENTITY_FAMILIES` owner, and boundary guards now fail closed on both seams. Passed `pnpm typecheck`, truthful `pnpm test:diff packages/runtime-state packages/operator-config packages/assistant-engine packages/vault-usecases packages/cli scripts/verify-workspace-boundaries.mjs`, `pnpm test:smoke`, `pnpm verify:workspace-boundaries`, `pnpm verify:workspace-package-cycles`, and `git diff --check` after refreshing the local `packages/runtime-state` build output so the package root export matched the new source seam. Required `coverage-write` and final-review audit passes returned no findings. Sent the required same-thread follow-up review request; the browser reported commit-timeout but captured a new matching user-turn signature (`repo.repomix 123` / `repo.snapshot 126`), and the detached recursive wake hop at depth 0 is armed under `output-packages/chatgpt-watch/69dc2255-8470-839c-a8d2-0c77e070bf74-2026-04-12T234922Z/`. Preserve the unrelated untracked active-plan file outside this slice. |
| Codex | Legacy removal wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/{src/runner-vault-cli-bin.ts,src/runner-vault-cli-command-manifest.ts,src/runner-vault-cli.ts,vitest.workspace.ts,test/runner-vault-cli.test.ts,test/vault-cli-wiring.test.ts}`, `packages/core/src/storage-spine.ts`, `packages/query/{src/health/health-registry-query-metadata.ts,test/health-registry-definitions.test.ts}` | runner-only CLI wrapper hard cut, stale CLI coverage exclusion, dead core/query shim removal | completed | Landed the watched-thread legacy-removal patch narrowly: removed the dead runner-only CLI wrappers and stale core/query re-export shims, moved the surviving `createVaultCli` wiring assertion onto a current-surface test, and passed `pnpm typecheck`, `bash scripts/workspace-verify.sh test:diff packages/cli packages/core packages/query`, `pnpm test:smoke`, and `git diff --check`. Required `coverage-write` and final-review audit passes returned no findings. |
| Codex | Giant-file composability wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/setup-cli/src/setup-services.ts`, `packages/setup-cli/src/setup-services/{tool-provisioning.ts,operator-defaults.ts}`, `packages/assistant-engine/src/assistant/outbox.ts`, `packages/assistant-engine/src/assistant/outbox/dispatch-state.ts`, targeted `packages/{setup-cli,assistant-engine}/test/**` if merge or verification requires it | setup host tool-provisioning split, setup operator-default persistence split, assistant outbox dispatch-state extraction | in_progress | Narrow watched-thread patch landing only. Preserve unrelated dirty-tree work; merge only the still-applicable composability splits plus direct verification or audit fixes. |
| Codex | Data-model composability wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/references/data-model-seams.md`, `packages/contracts/src/zod.ts`, `packages/vault-usecases/src/usecases/{intervention.ts,workout-measurement.ts,workout.ts}`, `packages/operator-config/{package.json,src/device-sync-client.ts}`, `packages/device-syncd/src/{client.ts,types.ts}`, `packages/cli/src/commands/{document.ts,intervention.ts,meal.ts,workout.ts}`, `packages/assistant-runtime/src/hosted-runtime{.ts,/platform.ts}`, `packages/assistant-engine/src/assistant/redaction.ts`, `apps/cloudflare/src/{index.ts,runtime-platform.ts,usage-store.ts,user-runner.ts,worker-contracts.ts,worker-routes/shared.ts}` | shared `EventSource` / `eventSourceSchema` owner, public `DeviceSyncJobRecord` owner, hosted runtime usage response parser owner, shared assistant header persistence split | completed | Landed the watched shared-owner cleanup patch narrowly, refreshed the local `packages/device-syncd` build output so the `@murphai/device-syncd/client` subpath exposed `DeviceSyncJobRecord`, and passed `pnpm typecheck`, truthful `pnpm test:diff ...` over the touched owners/reverse dependents, `pnpm test:smoke`, and `git diff --check`. Two required same-thread `pnpm review:gpt --send ...` attempts staged the files but failed browser-side with `send-button-disabled`, so the follow-up review request is unconfirmed; the refreshed thread export still showed no new user turn. The detached wake status bundle was still written at `output-packages/chatgpt-watch/69dc1dd0-07cc-83a0-b681-3d62bf897bf7-2026-04-12T231836Z/` and currently reports state `waiting`. |
| Codex | Watched Cloudflare simplify wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/{scripts/deploy-automation/container-images.ts,scripts/r2-lifecycle.ts,src/hosted-email/{route-addressing.ts,route-store.ts,worker-ingress.ts}}`, targeted `apps/cloudflare/test/**` if verification or audit fixes require it | hosted-email route candidate parsing, route-store decode boundary, ingress rejection helper reuse, script-only helper collapse | completed | Landed the watched-thread simplification patch narrowly: removed two one-use script helpers, collapsed duplicate hosted-email normalization/reject logic, and tightened the hosted-email route-store decode boundary without changing contracts. Passed `bash scripts/workspace-verify.sh test:diff apps/cloudflare/scripts/deploy-automation/container-images.ts apps/cloudflare/scripts/r2-lifecycle.ts apps/cloudflare/src/hosted-email/route-addressing.ts apps/cloudflare/src/hosted-email/route-store.ts apps/cloudflare/src/hosted-email/worker-ingress.ts`, direct `tsx` proof for cleanup/bucket/route behavior, and `git diff --check`; required `coverage-write` and final-review audit passes both returned no findings. Attempted the required same-thread `pnpm review:gpt --send ...` follow-up twice, but the managed browser kept the ChatGPT send button disabled after attachments staged (`send-button-disabled`), and direct thread exports confirmed no new user turn landed, so no wake hop was armed. Preserve unrelated dirty `apps/cloudflare/**`, `apps/web/**`, and package-boundary/env lanes outside this slice. |
| Codex | Architecture review wake patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/gateway-core/{src/{index.ts,reply-routes.ts,routes.ts},test/reply-routes.test.ts}`, `packages/assistant-engine/{src/assistant/channels/helpers.ts,test/channel-binding-delivery.test.ts}` | shared gateway-owned reply-route inference, assistant binding delivery wrappers, focused route fallback regression coverage | completed | Landed the watched-thread architecture patch narrowly: `gateway-core` now owns reply-route inference via `reply-routes.ts`, assistant binding helpers delegate to that package surface, and focused tests cover explicit, thread-first, and fallback behavior. Verification passed with `pnpm typecheck`, `pnpm test:diff packages/gateway-core packages/assistant-engine`, `pnpm test:smoke`, and `git diff --check` after refreshing the local `packages/gateway-core` build output so the generated package surface included the new export. Sent the required same-thread follow-up review request; the browser flow reported commit-timeout but captured a new matching user-turn signature (`repo.repomix 111` / `repo.snapshot 114`). Armed the detached recursive wake hop at depth 0 under `output-packages/chatgpt-watch/69dc1d90-d9e0-83a1-892a-cf0f37f9d7a5-2026-04-12T230318Z/`. |
| Codex | Cloudflare direct deploy migration-history fix | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/{wrangler.jsonc,scripts/deploy-automation/{deployment-traffic.ts,wrangler-config.ts},test/deploy-automation.test.ts}` | historical Durable Object migration tags `v1`/`v2`, gradual-deploy-safe migration allowlist, rendered deploy config parity | completed | Narrow deploy unblock only. Restored the production migration history split (`UserRunnerDurableObject` in `v1`, `RunnerContainer` in `v2`) so direct deploy no longer tries to reapply a new-sqlite-class migration to an existing depended-on class. Verified with focused deploy-automation tests, rendered config readback, and `pnpm --dir apps/cloudflare verify`. |
| Codex | Cloudflare direct deploy workflow fix | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/test/node-runner.test.ts` | ambient forwarded-env fallback expectation vs workflow-provided hosted assistant/web env keys | completed | Narrow deploy unblock only. The stale ambient-env fallback test now clears workflow-injected hosted assistant/web keys before asserting the fallback envelope. Verified with the exact failing Vitest case under workflow-like env plus `pnpm --dir apps/cloudflare verify`. |
| Codex | Release-check test fixture type fixes | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/test/{hosted-share-claim-state.test.ts,prisma-store-local-heartbeat.test.ts,hosted-execution-control.test.ts,linq-control-plane.test.ts,hosted-onboarding/webhook-receipt-privacy.test.ts}`, `apps/cloudflare/test/{node-runner.test.ts,user-runner.test.ts}`, `packages/assistantd/test/{http-coverage.test.ts,http.test.ts}`, `packages/assistant-runtime/test/{hosted-runtime-platform.test.ts,hosted-runtime-usage.test.ts}` | typed `updateMany` test doubles, heartbeat patch field alignment, hosted-web control/mock seam drift, assistant session provider-option fixture drift, pending-usage record shape drift | in_progress | Narrow release unblock only. Preserve overlapping dirty `apps/web/**`, `apps/cloudflare/**`, `packages/assistantd/**`, and `packages/assistant-runtime/**` work and keep the fix limited to test typing/runtime-shape drift. |
| Codex | Release-check CLI fixture drift | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/test/{assistant-chat-controller.test.ts,assistant-provider.test.ts,assistant-robustness.test.ts,assistant-runtime.test.ts,assistant-service.test.ts,assistant-state.test.ts,inbox-model-harness.test.ts}`, `packages/assistant-engine/src/assistant/provider-catalog.ts`, `packages/setup-cli/test/setup-assistant.test.ts` | CLI helper `providerOptions` runtime-target fields, assistant model-catalog preset-aware capabilities, setup-defaults stale zero-data-retention expectation | in_progress | Narrow release unblock only. Preserve unrelated dirty `packages/messaging-ingress/**` and existing CLI work; keep the fix limited to stale test fixtures and small helper/input contract repairs needed by release verification. |
| Codex | Repo-green assistant-cli fixture drift follow-up | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-cli/src/assistant/ui/chat-controller-models.ts`, `packages/assistant-cli/test/{assistant-command-coverage.test.ts,assistant-command-runtime.test.ts,assistant-daemon-client-more.test.ts,assistant-daemon-client-owned-coverage.test.ts,assistant-doctor-security.test.ts,assistant-doctor.test.ts,assistant-ui-controller.test.ts,assistant-ui-ink.test.ts,assistant-ui-logging.test.ts,assistant-ui-runtime.test.ts,assistant-ui-state-view-model.test.ts}` | assistant session `providerOptions` runtime-target fields in stale CLI/UI test fixtures, preset-aware model catalog state threading | in_progress | Narrow repo-green unblock only. Preserve overlapping dirty `apps/web/**`, `packages/messaging-ingress/**`, and active assistant/operator-config lanes; touch only stale assistant-cli fixtures and the minimal UI state seam needed to restore repo verification. |
| Codex | Repo-green CLI fixture drift follow-up | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `pnpm-workspace.yaml`, `packages/cli/test/{assistant-chat-controller.test.ts,assistant-provider.test.ts,assistant-robustness.test.ts,assistant-runtime.test.ts,assistant-service.test.ts,assistant-state.test.ts,inbox-model-harness.test.ts}` | CLI assistant session `providerOptions` runtime-target fields, provider-helper option shape cleanup, review-gpt release-age allowlist parity | in_progress | Narrow repo-green unblock only. Preserve unrelated dirty `apps/web/**`, `packages/messaging-ingress/**`, and active assistant/operator-config lanes; touch only stale CLI test fixtures, root policy metadata, and helper expectations needed to restore repo verification. |
| Codex | CLI wording clarity for saved model vs live runtime inspection | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/commands/model.ts`, `packages/assistant-cli/src/commands/assistant.ts` | `model` saved-default wording, `assistant status` live-runtime wording, `assistant chat` interactive-terminal wording | completed | Tiny copy-only CLI surface patch landed. `pnpm typecheck` and `pnpm test:diff packages/cli packages/assistant-cli` still fail for unrelated pre-existing `apps/web` and `packages/assistant-cli` type drift. Direct proof passed via `node --import=tsx packages/cli/src/bin.ts model --schema --format json` plus rebuilt `packages/assistant-cli`/`packages/cli` help-schema checks from `packages/cli/dist/bin.js` for `status`, `assistant status`, and `chat`. Preserve unrelated dirty `packages/messaging-ingress/**`, `package.json`, and `pnpm-lock.yaml` edits. |
| Codex | Hosted assistant-delivery identity patch landing | `agent-docs/exec-plans/active/2026-04-12-hosted-assistant-delivery-identity.md`, `agent-docs/references/data-model-seams.md`, `packages/hosted-execution/{src/side-effects.ts,test/side-effects.test.ts}`, `packages/assistant-runtime/src/hosted-runtime/{callbacks.ts,platform.ts}`, `packages/assistant-runtime/test/hosted-runtime-callbacks.test.ts`, `apps/cloudflare/src/{runtime-platform.ts,runner-outbound/results.ts,side-effect-journal.ts}`, `apps/cloudflare/test/index.test.ts` | canonical hosted assistant-delivery `effectId`, assistant-delivery-only journal port, dead `/intents/:effectId` route removal | completed | Landed the supplied hosted assistant-delivery seam patch narrowly: canonical hosted records/effects now use only `effectId`, assistant-runtime dropped the duplicate generic journal API and maps local outbox `intentId` only at the adapter edge, Cloudflare hard-cut `/intents/:effectId`, and the broader Cloudflare route tests now match the new seam. Passed hosted-execution typecheck, focused hosted-execution/assistant-runtime/Cloudflare Vitest, coverage-write found no extra changes, final review findings were fixed, and `git diff --check` passed. Broader `pnpm typecheck` still fails for unrelated pre-existing `apps/web` errors, while scoped `test:diff` and package-local `packages/assistant-runtime typecheck` are blocked only by unrelated pre-existing `AssistantUsageRecord.providerMetadataJson` test drift. |
| Codex | Assistant target-area production review patch landing | `agent-docs/exec-plans/active/2026-04-12-assistant-target-area-production-review.md`, `packages/assistant-engine/src/assistant/{provider-binding.ts,provider-state.ts,provider-turn-runner.ts,turn-finalizer.ts}`, `packages/assistant-engine/test/turn-finalizer.test.ts`, `packages/operator-config/{src/assistant-cli-contracts.ts,test/assistant-session-resume-state.test.ts}` | exact resume-route persistence, route-only resume-state hard cut, dead provider-binding helper removal | in_progress | Narrow supplied assistant-target patch landing only. Preserve overlapping assistant-target and operator-config work already in the tree; merge only the still-applicable resume-state/runtime hunks plus direct verification or audit fixes. |
| Codex | Cloudflare hosted-email code-quality patch landing | `agent-docs/exec-plans/active/2026-04-12-cloudflare-hosted-email-code-quality.md`, `apps/cloudflare/src/{worker-contracts.ts,index.ts,runner-outbound.ts,runner-outbound/results.ts,hosted-email/{route-store.ts,routes.ts,worker-ingress.ts}}` | worker string-env boundary helper, hosted-email route-store crypto/object-key naming, verified-sender config collapse | in_progress | Narrow supplied Cloudflare-only code-quality patch. Preserve overlapping dirty `apps/cloudflare/**` and `apps/web/**` lanes; merge only the still-applicable helper/naming/branch-flattening hunks plus direct verification or audit fixes. |
| Codex | Hosted target-area prod-review patch landing | `packages/cloudflare-hosted-control/{package.json,src/{client.ts,routes.ts},test/routes.test.ts}`, `packages/hosted-execution/{package.json,src/outbox-payload.ts,test/outbox-payload.test.ts}`, `packages/assistant-runtime/src/hosted-runtime/{callbacks.ts,models.ts,parsers.ts,platform.ts}`, `apps/cloudflare/{scripts/smoke-hosted-deploy.shared.ts,src/{bundle-gc.ts,dispatch-payload-store.ts,execution-journal.ts,index.ts,runner-outbound/results.ts,runtime-platform.ts,side-effect-journal.ts,user-env.ts,user-runner.ts,user-runner/runner-user-env.ts,worker-contracts.ts,worker-routes/shared.ts}}`, `apps/web/src/lib/{hosted-execution/{control.ts,dispatch.ts,outbox-payload.ts,outbox.ts,pending-usage-client.ts},hosted-share/pack-client.ts}`, `scripts/verify-workspace-boundaries.mjs` | hosted-control focused subpaths, hosted-execution outbox identity helpers, boundary guard tightening | in_progress | Narrow supplied package-boundary patch only. Preserve overlapping dirty `apps/cloudflare/**` and `apps/web/**` lanes; merge only still-applicable import-surface/helper hunks and focused verification for the new seams. |
| Codex | Hosted env rollback and share-finalize target-area patch landing | `apps/cloudflare/src/user-runner/runner-user-env.ts`, targeted `apps/cloudflare/test/**`, `apps/web/app/api/internal/hosted-execution/share-import/complete/route.ts`, `apps/web/src/lib/hosted-share/{shared.ts,pack-client.ts}`, targeted `apps/web/test/**` | canonical hosted user-env rollback ordering, verified-sender route restore gating, hosted share finalize readback ownership, idempotent pack delete | in_progress | Narrow supplied patch landing only. Preserve overlapping active `apps/cloudflare` env-narrowing work and `apps/web` hosted-share/package-boundary edits; merge only the still-applicable rollback/finalize hunks plus direct verification fixes. |
| Codex | Cloudflare string-env narrowing patch landing | `agent-docs/exec-plans/active/2026-04-12-cloudflare-string-env-patch.md`, `apps/cloudflare/src/{env.ts,hosted-email/worker-ingress.ts,hosted-env-policy.ts,index.ts,runner-outbound.ts,runner-outbound/results.ts,string-env.ts,user-runner.ts,worker-routes/internal-user.ts}` | worker string-only env seam, hosted-email ingress/header-from reuse, env-cast removal | in_progress | Narrow supplied patch landing only. Preserve unrelated dirty `apps/web/**`, `packages/messaging-ingress/**`, and other concurrent Cloudflare lanes; merge only the still-applicable env-narrowing hunks plus direct verification or audit fixes. |
| Codex | Device-sync store composability patch landing | `packages/device-syncd/src/store{.ts,/jobs.ts,/schema.ts,/webhook-traces.ts}`, targeted `packages/device-syncd/test/**` if merge or verification requires it | store owner split for jobs, webhook-trace claims, schema bootstrap | in_progress | Narrow supplied patch landing only. Preserve unrelated dirty `packages/messaging-ingress/**` and other active lanes; merge only the still-applicable `device-syncd` store extraction hunks plus direct verification fixes. |
| Codex | Hosted target-area review patch landing | `apps/web/src/lib/{device-sync/prisma-store/{types.ts,token-audits.ts},hosted-execution/{outbox-payload.ts,outbox.ts},hosted-onboarding/{linq.ts,webhook-receipt-codec.ts,webhook-receipt-transitions.ts}}`, targeted `apps/web/test/**` | inline settled-payload pruning, pruned-inline idempotent upserts, empty-body Linq send success, delivered-only Linq side-effect results, token-audit app record trim | completed | Supplied apps/web target-area patch is now fully landed: trimmed the hosted token-audit app record, pruned settled inline outbox payloads while preserving idempotent re-enqueue, hard-cut legacy Linq result payload shapes, and updated focused hosted-web regression coverage. Targeted hosted-web Vitest for the touched files passed; `pnpm --dir apps/web typecheck` remains blocked by unrelated pre-existing failures in `test/hosted-share-claim-state.test.ts` and `test/prisma-store-local-heartbeat.test.ts`. |
| Codex | Hosted device-sync runtime review patch landing | `agent-docs/exec-plans/completed/2026-04-12-device-sync-runtime-review-patch.md`, `apps/cloudflare/{src/device-sync-runtime-store.ts,test/device-sync-runtime-store.test.ts}`, `apps/web/src/lib/device-sync/{agent-session-service.ts,prisma-store/local-heartbeats.ts,wake-service.ts}`, `packages/device-syncd/{src/hosted-runtime.ts,test/hosted-runtime.test.ts}` | hosted runtime explicit write results, stale local-state/token CAS guards, hosted-web runtime apply helpers | completed | Landed the remaining still-applicable `apps/web` hunks on top of the already-present Cloudflare/device-syncd runtime-store work: hosted agent/session and local-heartbeat callers now rely on explicit `writeUpdate` apply results instead of timestamp inference, and local-heartbeat tests cover stale-write rejection. Focused Vitest passed for `apps/web/test/agent-session-service.test.ts` under `hosted-web-execution` and `apps/web/test/prisma-store-local-heartbeat.test.ts` under `hosted-web-store-config`; `pnpm typecheck` remains red for unrelated pre-existing `apps/web` test/type drift in hosted-execution/outbox, webhook receipt, and hosted-share tests. Preserve unrelated dirty `apps/web/**`, `packages/messaging-ingress/**`, and root dependency metadata edits outside this slice. |
| Codex | Assistant rich-content routing patch landing | `packages/assistant-engine/src/assistant/{local-service.ts,provider-turn-runner.ts,rich-content-routing.ts,automation/reply.ts}`, `packages/assistant-engine/test/{assistant/rich-content-routing.test.ts,assistant-automation-runtime.test.ts}` | rich-content route prioritization, route-specific `userMessageContent` filtering, auto-reply rich-content capability gating | completed | Landed the supplied assistant rich-content routing behavior: multimodal-capable routes are preferred when rich user content is present, text-only routes now drop rich `userMessageContent`, and auto-reply rich-content gating now checks route capability without injecting provider overrides. Passed `pnpm --dir packages/assistant-engine typecheck`, targeted Vitest for the new helper and updated auto-reply behavior, `pnpm --dir packages/assistant-engine test:coverage`, and `pnpm test:smoke`. Repo-required `pnpm typecheck` and scoped `bash scripts/workspace-verify.sh test:diff ...` still fail for unrelated pre-existing `packages/assistantd/**` and `packages/assistant-cli/**` typecheck drift. Preserve unrelated dirty `apps/web/**` and `packages/messaging-ingress/**` edits outside this slice. |
| Codex | Hosted Linq routing prod-hardening wake patch landing | `apps/web/src/lib/hosted-onboarding/{linq-routing-policy.ts,webhook-provider-linq.ts,linq-home-routing.ts}`, `apps/web/test/{hosted-onboarding-linq-routing.test.ts,hosted-onboarding-linq-home-routing.test.ts}` | home recipient preservation on exact-chat rebinding, stale pending Linq cleanup when durable home already exists | completed | Landed only the still-applicable prod-hardening hunks on top of the already-committed final-review work: exact-chat rebinding now preserves the saved home recipient phone, and durable-home activation clears stale pending Linq state. Passed `pnpm test:diff` for the touched `apps/web` slice, `pnpm --dir apps/web verify`, targeted `vitest` proof for the two Linq routing suites, direct `tsx` runtime proof for the helper behavior, and `git diff --check`. Repo-wide `pnpm typecheck` still fails for unrelated pre-existing `packages/assistantd/**` type errors. Preserve unrelated dirty `packages/messaging-ingress/**`, `packages/operator-config/**`, and other concurrent `apps/web` work. |
| Codex | Assistant target prod-review final patch landing | `agent-docs/exec-plans/active/2026-04-12-assistant-target-prod-review-final.md`, `packages/operator-config/{src/{assistant/target-runtime.ts,assistant/provider-config.ts,assistant-cli-contracts.ts},test/{assistant-provider-config.test.ts,assistant-runtime-contracts-coverage.test.ts,assistant-seam-coverage.test.ts,hosted-assistant-bootstrap.test.ts}}` | preset-backed runtime behavior resolver, zero-data-retention persistence pruning, conservative unlabeled-compatible persistence | completed | Landed only the still-applicable operator-config/runtime hunks from `assistant-target-prod-review-final.patch`; `packages/assistant-engine/src/assistant/local-service.ts` and its regression already matched the artifact intent, so no assistant-engine edit was needed. Focused proof passed with `pnpm --dir packages/operator-config typecheck`, `pnpm --dir packages/operator-config test:coverage`, `pnpm --dir packages/assistant-engine typecheck`, targeted `packages/assistant-engine/test/assistant-local-service-runtime.test.ts`, and direct `tsx` runtime proof. Repo-required `pnpm typecheck` and `pnpm test:diff packages/operator-config` still fail for unrelated pre-existing assistant-session fixture drift in `packages/assistantd/**` and `packages/assistant-cli/**`. Preserve unrelated dirty `packages/messaging-ingress/**` edits. |
| Codex | Watched security audit runtime-state and log-sanitization patch landing | `agent-docs/exec-plans/active/2026-04-12-security-audit-runtime-log-followup.md`, `apps/cloudflare/{src/device-sync-runtime-store.ts,src/user-key-store.ts,test/device-sync-runtime-store.test.ts}`, `apps/web/app/api/internal/hosted-execution/usage/cron/route.ts`, `apps/web/src/lib/device-sync/{agent-session-service.ts,prisma-store/local-heartbeats.ts,wake-service.ts}`, `apps/web/src/lib/hosted-execution/{control.ts,dispatch.ts,logging.ts,outbox.ts,usage.ts}`, targeted `apps/web/test/**` if direct proof requires it | hosted device-sync runtime CAS guards, hosted execution safe log formatting, outbox last-error sanitization | in_progress | Narrow watched-thread security patch only. Preserve unrelated dirty Cloudflare package-boundary work and any concurrent hosted-web edits; merge only still-applicable hunks plus direct verification or audit fixes. |
| Codex | Privacy data minimization wake patch landing | `agent-docs/exec-plans/active/2026-04-13-privacy-webhook-trace-minimization.md`, `packages/device-syncd/{src/store/webhook-traces.ts,test/store.test.ts}`, `apps/web/src/lib/{device-sync/prisma-store/webhook-traces.ts,hosted-onboarding/{webhook-provider-linq.ts,webhook-receipt-codec.ts,webhook-receipt-types.ts,webhook-transport.ts}}`, `apps/web/test/{prisma-store-device-sync-signal.test.ts,hosted-onboarding-linq-dispatch.test.ts,hosted-onboarding-webhook-receipt-codec.test.ts,hosted-onboarding-webhook-idempotency.test.ts}` | processed webhook-trace retention pruning, hashed local webhook account ids, member-backed Linq home redirect payloads | completed | Landed the narrow watched-thread privacy patch only: local and hosted webhook dedupe traces now opportunistically prune processed rows older than 30 days, local sqlite stores hashed `external_account_id` values for new webhook-trace rows, and new Linq `conversation_home_redirect` side effects persist `memberId` instead of a raw home phone while delivery resolves routing state and legacy payloads stay readable. Verification passed with `pnpm typecheck`, `pnpm --dir packages/device-syncd test:coverage -- test/store.test.ts`, focused `apps/web` Vitest for the touched files, and direct sqlite/Linq runtime proof. `pnpm test:diff packages/device-syncd apps/web` remains blocked by an unrelated reverse-dependent `packages/assistant-engine` typecheck failure. Required audit passes returned no findings, the same-thread review request recorded a matching new user-turn signature despite browser `commit-timeout`, and the detached final wake hop is armed under `output-packages/chatgpt-watch/69dc1c7b-56ec-83a1-a986-8e97bbb3f3f0-2026-04-12T233151Z/`. |
| Codex | Hosted execution journal wake bugfix landing | `agent-docs/exec-plans/active/2026-04-13-execution-journal-canonicalization.md`, `apps/cloudflare/{src/execution-journal.ts,test/execution-journal.test.ts}` | order-insensitive duplicate-commit comparison for structured journal payloads | completed | Landed the watched-thread bugfix narrowly: duplicate hosted execution commits now compare structured payloads canonically, and focused replay coverage proves reordered object keys reuse the existing durable journal entry. Passed `pnpm typecheck`, `pnpm test:diff apps/cloudflare/src/execution-journal.ts apps/cloudflare/test/execution-journal.test.ts`, targeted `vitest`, and `git diff --check`; required audit passes found no changes. Attempted the required same-thread `pnpm review:gpt --send ...` follow-up, but the browser kept the ChatGPT send button disabled after attachments staged (`send-button-disabled`), so delivery is unconfirmed and the detached wake hop at `output-packages/chatgpt-watch/69dc218c-20d8-839f-a995-7e7c11497b41-2026-04-12T232826Z/` remains in `waiting` state. |
| Codex | Package-boundary audit wake patch landing | `packages/assistant-runtime/{package.json,src/{index.ts,hosted-email-route.ts,hosted-runtime/parsers.ts}}`, `packages/cloudflare-hosted-control/src/client.ts`, `apps/cloudflare/{src/{dispatch-payload-store.ts,hosted-email/{config.ts,transport.ts,worker-ingress.ts},hosted-env-policy.ts,index.ts,runner-env.ts,runner-outbound/results.ts,runtime-platform.ts,share-store.ts,worker-routes/internal-user.ts},scripts/smoke-hosted-deploy.shared.ts}`, `apps/web/src/lib/{hosted-onboarding/webhook-dispatch-payload.ts,hosted-share/pack-client.ts}`, `scripts/verify-workspace-boundaries.mjs` | assistant-runtime hosted-email entrypoint hard cut, hosted-execution owner subpath tightening, boundary-guard regression coverage | in_progress | Narrow watched-thread patch landing only. Preserve unrelated dirty `assistant-engine`, `operator-config`, `messaging-ingress`, and other concurrent edits; merge only still-applicable package-boundary cleanup hunks plus direct verification fixes. |
| Codex | Assistant target review-fixes wake patch landing | `agent-docs/exec-plans/active/2026-04-12-assistant-target-review-fixes-wake.md`, `packages/assistant-engine/{src/assistant/{local-service.ts,provider-binding.ts},test/{assistant-local-service-runtime.test.ts,provider-seams.test.ts}}` | session-option target persistence, exact resume-route matching | completed | Landed the watched assistant-target review patch narrowly in assistant-engine, passed `packages/assistant-engine` coverage plus direct `tsx` proof, confirmed repo-wide `pnpm typecheck` and `pnpm test:diff packages/assistant-engine` are blocked only by unrelated pre-existing assistantd/assistant-cli type errors, sent the same-thread follow-up review request as `repo.repomix(93).xml` / `repo.snapshot(97).zip`, and armed the detached recursive wake hop at depth 1 under `output-packages/chatgpt-watch/69daf1be-3040-839b-9dfe-a39d68707a68-2026-04-12T023642Z/`. Preserve unrelated dirty `packages/messaging-ingress/**` edits outside this slice. |
| Codex | Hosted device-sync architecture review patch landing | `packages/device-syncd/src/hosted-runtime.ts`, `apps/web/src/lib/device-sync/{internal-runtime.ts,internal-runtime-request.ts,local-heartbeat.ts,shared.ts,wake-service.ts}`, targeted `apps/web/test/**` | hosted runtime request-parser ownership, hosted runtime error-sanitizer ownership, web compatibility re-exports | in_progress | Narrow watched-thread patch landing only. Preserve unrelated dirty `packages/messaging-ingress/**` edits and keep the merge limited to the hosted device-sync runtime seam plus direct verification fixes. |
| Codex | Hosted device-sync local-heartbeat target-area patch landing | `agent-docs/exec-plans/active/2026-04-12-device-sync-local-heartbeat-target-area.md`, `apps/web/src/lib/device-sync/{agent-session-service.ts,control-plane.ts,internal-runtime.ts,local-heartbeat.ts,prisma-store.ts,prisma-store/local-heartbeats.ts,prisma-store/types.ts}`, `apps/web/test/{device-sync/local-heartbeat.test.ts,device-sync/runtime-seams.test.ts,device-sync-internal-runtime.test.ts,hosted-contact-privacy.test.ts,prisma-store-local-heartbeat.test.ts,setup-env.ts}`, `packages/device-syncd/test/hosted-runtime.test.ts` | local-heartbeat owned patch shape, hosted runtime parser ownership hard cut, runtime local-state write-shape alignment | in_progress | Narrow supplied patch landing only. Preserve overlapping hosted device-sync runtime/security lanes and existing dirty `packages/messaging-ingress/**` work; merge only the still-applicable local-heartbeat and parser-ownership hunks plus focused verification fixes. |
| Codex | review-gpt 0.5.57 Murph bump | `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` | `@cobuild/review-gpt` devDependency pin, version-scoped `minimumReleaseAgeExclude` entry | completed | Landed the narrow dependency bump, confirmed the broader lockfile peer churn reproduces cleanly from `HEAD`, passed `pnpm typecheck`, passed the focused CLI release-audit Vitest file, and passed `pnpm --dir apps/web build` after a transient `apps/web verify` build fetch failure. Preserve the large unrelated dirty worktree and avoid touching active assistant/operator-config/messaging edits outside the root dependency metadata and lockfile slice. |
| Codex | Target-area boundary patch bundle landing | `agent-docs/exec-plans/active/2026-04-12-target-area-patch-bundle.md`, `packages/messaging-ingress/**`, `packages/query/**`, `packages/device-syncd/**`, `packages/inboxd/src/connectors/telegram/normalize.ts`, `apps/web/src/lib/hosted-onboarding/{telegram.ts,webhook-provider-linq.ts}`, `packages/operator-config/{package.json,src/index.ts,src/knowledge-contracts.ts}`, `packages/assistant-engine/src/knowledge.ts`, `agent-docs/references/data-model-seams.md`, `scripts/verify-workspace-boundaries.mjs` | messaging-ingress explicit owner subpaths, Telegram thread-target normalization, query knowledge-search ownership, device-sync public-ingress boundary, knowledge contract hard cuts | in_progress | Narrow supplied patch bundle. Preserve overlapping dirty `apps/web`, `assistant-engine`, and `operator-config` work already in flight; merge only the still-applicable target-area hunks and direct verification fixes. |
| Codex | Hosted onboarding data-model patch landing | `agent-docs/exec-plans/active/2026-04-12-hosted-data-model-patch.md`, `apps/web/src/lib/hosted-onboarding/{member-activation.ts,stripe-billing-events.ts,stripe-billing-lookup.ts,stripe-billing-policy.ts,stripe-dispatch.ts,stripe-event-reconciliation.ts,shared.ts}`, targeted `apps/web/test/**` | hosted activation owner split, shared Stripe dispatch context, hosted onboarding Prisma-client reuse, focused hosted-web seam coverage | in_progress | Narrow high-risk hosted billing/onboarding lane from a supplied patch. Merge on top of the newer Linq-routing work already in flight; preserve unrelated `apps/web` edits and keep the landing limited to the owner split plus direct verification fixes. |
| Codex | Cloudflare smoke/CLI target-area follow-up patch landing | `agent-docs/exec-plans/active/2026-04-12-cloudflare-smoke-cli-target-area-followup.md`, `apps/cloudflare/scripts/{deploy-automation/shared.ts,deploy-worker-version.shared.ts,smoke-hosted-deploy.shared.ts}`, targeted `apps/cloudflare/test/**`, `packages/assistant-engine/src/assistant-cli-tools/execution-adapters.ts`, targeted `packages/assistant-engine/test/**` | shared strict integer parsing, smoke env fallback selection, assistant CLI Windows PATH normalization | in_progress | Narrow target-area follow-up from supplied patches. Preserve unrelated active Cloudflare runtime/env work and assistant target/hardening edits; keep the landing limited to deploy/smoke helpers plus the child launcher env seam. |
| Codex | Hosted runner target-area review follow-up patch landing | `agent-docs/exec-plans/active/2026-04-12-hosted-runner-target-area-review-followup.md`, `apps/cloudflare/src/{node-runner.ts,runner-env.ts,user-runner/runner-dispatch-processor.ts}`, `apps/cloudflare/test/{node-runner.test.ts,runner-env.test.ts}` | worker-owned forwarded env authority, child control-env stripping, shared runtime config source | in_progress | Narrow high-risk hosted-runner follow-up from the supplied review patch. Preserve unrelated dirty `apps/cloudflare`, assistant, and hosted-web work; keep the landing limited to this runtime/env seam plus direct regression proof. |
| Codex | Hosted assistant env hardening simplify patch landing | `agent-docs/exec-plans/active/2026-04-12-hosted-assistant-env-hardening-simplify.md`, `packages/operator-config/src/hosted-assistant-config.ts`, `packages/assistant-runtime/src/hosted-assistant-env.ts`, `apps/cloudflare/src/hosted-env-policy.ts`, `apps/cloudflare/scripts/deploy-automation/{environment.ts,secrets.ts}`, `packages/assistant-engine/src/assistant-cli-tools/execution-adapters.ts`, targeted `apps/cloudflare/test/**` and `packages/assistant-engine/test/**` | hosted assistant API-key env allowlist, runner/deploy/CLI env filtering, referenced-secret removal | in_progress | Narrow supplied patch on a secrets/trust-boundary seam. Preserve unrelated active assistant, Cloudflare, and hosted-web edits; keep the landing limited to the shared allowlist hardening plus direct verification fixes. |
| Codex | Greenfield version cutover | `agent-docs/exec-plans/active/2026-04-12-greenfield-version-cutover.md`, `packages/{operator-config,assistant-engine,assistant-cli,assistantd,cli,setup-cli,contracts,core,gateway-core,runtime-state,hosted-execution}/**`, `apps/cloudflare/**`, and any matching durable doc updates required by the cutover | assistant session schema reset, greenfield legacy-reader removal, non-`v1` durable contract renumbering | completed | Hard-cut the touched durable contracts to `v1`, removed assistant lock/session/preferences/setup compatibility lanes that were still migration-only, aligned downstream tests including Cloudflare gateway/hosted-email expectations, and passed repo-wide `pnpm verify:acceptance`. Preserve unrelated dirty root dependency metadata and `packages/messaging-ingress/**` edits outside this slice. |
| Codex | Assistant target architecture refactor patch landing | `agent-docs/exec-plans/active/2026-04-12-assistant-target-architecture-refactor.md`, `packages/operator-config/{package.json,src/{assistant/provider-config.ts,assistant/hosted-config.ts,assistant/target-runtime.ts,assistant-backend.ts,assistant-cli-contracts.ts,hosted-assistant-config.ts,setup-cli-contracts.ts},test/**}`, `packages/assistant-engine/src/{assistant/{provider-binding.ts,provider-config.ts,provider-state.ts,provider-turn-runner.ts,providers/{openai-compatible.ts,registry.ts,types.ts},service-contracts.ts,service-turn-routes.ts,session-resolution.ts,store/{paths.ts,types.ts},turn-finalizer.ts},model-harness.ts}`, `packages/setup-cli/src/{setup-assistant-defaults.ts,setup-assistant.ts}`, targeted assistant-engine/operator-config/setup-cli tests, and any matching durable doc updates required by the landed architecture | runtime target seam, preset-backed target intent, explicit execution driver/resume contract, session v5 compatibility | completed | Landed the supplied refactor cleanly with explicit runtime-target resolution, preserved persisted v5 resume contracts on reload, recomputed inferred preset identity after prompt-edited endpoints, and passed scoped owner typecheck plus owner coverage with direct session/runtime proof. Preserve unrelated root dependency and messaging-ingress edits outside this slice. |
| Codex | Hosted Linq home-routing patch landing | `agent-docs/exec-plans/completed/2026-04-12-hosted-linq-home-routing.md`, targeted `apps/web/{src/lib/hosted-onboarding/**,test/**,prisma/**}`, `ARCHITECTURE.md` | pre-activation Linq routing split, sticky post-activation home line, pooled line soft-cap selection, redirect-vs-rebind policy, activation home-chat creation | completed | Landed the supplied hosted Linq routing patch with current-HEAD compatibility fixes: pending-vs-home routing is split, activation fails closed when no durable home line exists, sparse or unknown-home non-home inbound traffic no longer silently rebinds, and focused activation/webhook/policy coverage was added. Verification passed on `pnpm test:diff apps/web`; repo-wide `pnpm typecheck` remains blocked only by the unrelated pre-existing `packages/assistant-engine/test/assistant-store-persistence.test.ts` nullability errors. |
| Codex | Target-area port/shutdown wake patch landing | `agent-docs/exec-plans/completed/2026-04-12-target-area-port-shutdown-fix.md`, `packages/runtime-state/{src/loopback-control-plane.ts,test/loopback-control-plane.test.ts}`, `packages/assistantd/{src/{config.ts,http.ts},test/{config.test.ts,http-startup.test.ts}}`, `packages/device-syncd/{src/{bin.ts,config.ts,http.ts},test/{bin.test.ts,config.test.ts,http-startup.test.ts}}` | shared listener-port validation, fail-closed device-sync env integer parsing, device-sync shutdown cleanup coalescing | completed | Landed the watched patch intent with shared listener-port validation, fail-closed decimal parsing for the touched device-syncd env integer paths, and coalesced shutdown cleanup that still attempts every cleanup step before exit. Required verification and direct proof passed, and the required coverage-write and final-review audit passes returned no findings. |
| Codex | Security target-area wake follow-up patch landing | `agent-docs/exec-plans/active/2026-04-12-security-target-area-wake-followup.md`, `packages/runtime-state/{src/loopback-control-plane.ts,test/loopback-control-plane.test.ts}`, `packages/assistantd/{src/config.ts,test/{config.test.ts,http-startup.test.ts}}`, `packages/device-syncd/{src/{bin.ts,config.ts,http.ts},test/{bin.test.ts,config.test.ts,http-startup.test.ts}}` | loopback listener-host syntax guard, assistant/device-sync startup rollback, public listener host validation | completed | Landed the watched follow-up patch intent with shared unbracketed listener-host validation, device-sync listener rollback on startup failure, and preserved rollback-close errors via `AggregateError`. Required verification and direct proof passed, the scoped same-thread review request landed as `repo.repomix(69).xml` / `repo.snapshot(73).zip`, and the final wake hop at recursive depth 0 is armed under `output-packages/chatgpt-watch/69da4b72-eee4-8399-9a91-0f3411170f00-2026-04-11T152203Z/`. Preserve unrelated dirty knowledge-boundary work outside this slice. |
| Codex | Device-sync webhook follow-up v2 patch landing | `agent-docs/exec-plans/completed/2026-04-12-device-sync-webhook-followup-v2-patch.md`, `packages/device-syncd/{src/{types.ts,public-ingress.ts,providers/{whoop.ts,oura.ts}},test/public-ingress.test.ts}`, `apps/web/src/lib/device-sync/wake-service.ts`, targeted `apps/web/test/**` | narrowed ingress webhook contract, provider webhook summary normalization, hosted wake webhook hint typing | completed | Landed the watched artifact as a narrow contract-tightening follow-up: shared ingress summaries now carry only `resourceCategory`, hosted wake consumes the shared type directly, focused owner verification passed, and diff-aware `pnpm test:diff packages/device-syncd apps/web` remains blocked only by unrelated pre-existing `packages/assistant-engine` typecheck failures. |
| Codex | Cloudflare hosted deploy path fix | `agent-docs/exec-plans/active/2026-04-12-cloudflare-hosted-deploy-fix.md`, `packages/operator-config/src/device-daemon/paths.ts`, `packages/operator-config/test/device-daemon-runtime.test.ts` | `resolveInstalledDeviceSyncPackageEntry`, device-daemon package resolution during worker deploy validation | in_progress | Narrow high-risk deploy-surface lane. Preserve the unrelated dirty importer edit and all other concurrent work; do not broaden beyond the worker-deploy module-load failure unless a direct verification fix is required. |
| Codex | Patch release recovery and acceptance green-up | `agent-docs/exec-plans/active/2026-04-11-patch-release-recovery.md`, targeted release-check and coverage-fix files discovered during `pnpm test:coverage`, plus release artifacts only if the rerun succeeds | `release:patch`, `verify:acceptance`, package coverage thresholds blocking release | in_progress | Narrow release lane. Start from the clean post-failure tree, preserve unrelated in-flight work, and touch only the owners needed to make the patch release pass. |
| Codex | Hosted signup direct Stripe continuation | `agent-docs/exec-plans/active/2026-04-10-hosted-signup-direct-checkout.md`, `apps/web/src/components/hosted-onboarding/{client-api.ts,hosted-phone-auth-support.ts,join-invite-client.tsx}`, `apps/web/test/{hosted-phone-auth.test.ts,join-invite-client.test.ts,page.test.ts}` | post-verification hosted billing continuation, homepage signup redirect path, shared checkout client helper | completed | Simplified homepage/default signup continuation so `checkout` goes straight into Stripe, reused one shared client checkout helper, and preserved the invite page fallback path plus unrelated dirty homepage/auth edits. |
| Codex | Linq product-copy swap to iMessage | `apps/web/app/page.tsx`, `apps/web/src/components/homepage/{capabilities-grid.tsx,faq-section.tsx,trust-strip.tsx}`, `apps/web/legal/terms-of-service.md`, `agent-docs/product-marketing-context.md`, `agent-docs/exec-plans/active/2026-04-10-linq-copy-to-imessage.md` | user-facing messaging-channel copy, hosted homepage metadata, legal/product-marketing wording | completed | Narrow copy-only lane. Preserved existing dirty homepage edits and broader Linq/iMessage runtime work; did not rename technical/provider references. |
| Codex | Hosted invite auto-checkout cleanup | `apps/web/src/components/hosted-onboarding/{join-invite-client.tsx,join-invite-stage-panels.tsx}`, `apps/web/test/join-invite-client.test.ts` | invite post-verification checkout launch, one-shot auto redirect fallback state, checkout CTA copy | in_progress | Narrow hosted-onboarding lane only. Preserve unrelated `apps/web` auth/session and RSC work outside these files; do not change billing route semantics. |
| Codex | setup-cli auto-reply greenfield slice | , targeted  | setup channel reconciliation, per-channel auto-reply cursor enable/disable semantics | in_progress | Narrow setup-cli-only lane for the new auto-reply shape. Preserve overlapping iMessage decommission and broader scheduler/assistant refactor edits outside this owned surface. |
| Codex | Setup/inbox CLI iMessage surface removal | `packages/setup-cli/**`, `packages/operator-config/src/setup-cli-contracts.ts`, `packages/operator-config/src/setup-runtime-env.ts`, `packages/cli/src/commands/inbox.ts`, `packages/cli/src/incur.generated.ts`, `packages/cli/config.schema.json`, targeted setup/cli/operator-config tests | setup channel enums/defaults/options, macOS-first setup CTA, inbox source add help/schema/examples | in_progress | Narrow user-facing CLI/setup lane only. Preserve scheduler and broader iMessage decommission edits outside these owned surfaces; do not revert overlapping nearby work. |
| Codex | Inbox runtime iMessage removal in owned surfaces only | `packages/inboxd-imessage/**`, `packages/inbox-services/src/inbox-app/**`, `packages/inbox-services/src/inbox-services/connectors.ts`, `packages/inbox-services/src/inbox-services/shared.ts`, `packages/inboxd/src/indexing/persist.ts`, `packages/operator-config/src/imessage-readiness.ts`, targeted inbox/inbox-services/operator-config tests | iMessage connector loading, doctor/readiness, host gating, startup skip behavior, attachment trust/source defaults | in_progress | Narrow worker lane. Preserve scheduler and broader decommission edits already in flight; do not touch setup, CLI, manifests, lockfile, docs, or assistant channel adapters unless a direct compile fix is unavoidable. |
| Codex | Assistant iMessage runtime removal in owned surfaces only | `packages/assistant-engine/src/assistant/channels/**`, `packages/assistant-cli/src/commands/assistant.ts`, `packages/assistant-cli/src/run-terminal-logging.ts`, `packages/core/src/automation.ts`, targeted tests under `packages/assistant-engine/test/**`, `packages/assistant-cli/test/**`, `packages/core/test/**` | assistant channel adapters/runtime/types, assistant CLI help/examples, automation default channel scaffold | in_progress | Narrow worker lane. Preserve scheduler edits and unrelated decommission work outside this owned surface; historical session reads must keep working and live iMessage delivery must fail explicitly. |
| Codex | iMessage topology prep in owned build and release surfaces | `packages/cli/package.json`, `packages/operator-config/package.json`, `tsconfig.base.json`, `tsconfig.test-runtime.json`, `vitest.config.ts`, `packages/cli/vitest.config.ts`, `packages/cli/vitest.workspace.ts`, `scripts/build-test-runtime-prepared.mjs`, `scripts/clean-build-artifacts.mjs`, `scripts/workspace-verify.sh`, `packages/cli/test/assistant-core-facades.test.ts`, `packages/cli/test/release-script-coverage-audit.test.ts`, `pnpm-lock.yaml` | CLI packaging topology, root package coverage/test topology, prepared-runtime smoke imports, package-shape release expectations | in_progress | Non-destructive prep first because live runtime imports to `@murphai/inboxd-imessage` and `@photon-ai/imessage-kit` still exist elsewhere. Preserve overlapping scheduler and runtime-removal edits; defer manifest/path/lockfile deletion until those imports are gone. |
| Codex | iMessage docs/site/legal cleanup slice | `README.md`, `ARCHITECTURE.md`, `packages/cli/README.md`, `packages/contracts/src/examples.ts`, `apps/web/app/page.tsx`, `apps/web/src/components/homepage/**`, `apps/web/legal/terms-of-service.md`, `apps/web/public/legal/terms.pdf`, `agent-docs/product-marketing-context.md`, `agent-docs/operations/verification-and-runtime.md` | hosted homepage copy, legal channel list, product marketing channel claims, architecture channel references, contract examples | in_progress | Narrow docs-only lane inside the broader decommission effort. Preserve unrelated overlap and avoid runtime/package-manifest edits. |
| Codex | Event/deadline assistant scheduler patch landing | `agent-docs/exec-plans/active/2026-04-10-assistant-event-driven-scheduler-cutover.md`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/inbox-services/**`, `packages/inboxd/**`, `packages/assistant-runtime/**`, `packages/assistantd/**`, `apps/cloudflare/**`, `packages/cli/**` | `runAssistantAutomation`, `runAssistantAutomationPass`, `createAssistantAutomationWakeController`, `resolveRunnerNextWakeAt`, parser drain wake flow | in_progress | Cross-cutting refactor lane from returned external patch; treat as overlap-sensitive and preserve unrelated edits, especially pre-existing `packages/cli/test/assistant-service.test.ts` changes. |
| Codex | Assistant recovery and automation surface cleanup | `agent-docs/exec-plans/active/2026-04-10-assistant-recovery-surface-cleanup.md`, `packages/assistant-engine/src/assistant/automation/**`, `packages/assistant-runtime/src/hosted-runtime/maintenance.ts`, `packages/assistantd/{README.md,src/**,test/**}`, `packages/assistant-cli/src/commands/assistant.ts`, `packages/assistant-cli/test/**`, `packages/cli/{config.schema.json,src/incur.generated.ts,test/**}`, `ARCHITECTURE.md` | failed auto-reply recovery, ordinary pass recovery draining, one-shot vs continuous automation contract, `skipDaemon` removal | in_progress | Cross-cutting but scoped follow-up to the scheduler cutover. Preserve overlapping scheduler, iMessage, and assistant-engine prompt work outside this recovery/API lane; do not revert unrelated dirty-tree edits. |
| Codex | Scheduler follow-up CLI coverage fix | `packages/cli/test/assistant-runtime.test.ts` | automation scan result assertions, `nextWakeAt` expectations | in_progress | Narrow test-only follow-up for the event-driven scheduler cutover. Avoid touching production code or overlapping iMessage removal work. |
| Codex | Auto-reply offline catch-up simplification | `ARCHITECTURE.md`, `packages/operator-config/src/assistant-cli-contracts.ts`, `packages/assistant-engine/src/assistant/{automation/**,status.ts,store.ts,store/persistence.ts,auto-reply-channels.ts}`, `packages/assistant-engine/src/{assistant-state.ts,index.ts}`, `packages/inbox-services/src/inbox-app/{runtime.ts,environment.ts,sources.ts}`, `packages/cli/src/vault-cli.ts`, `packages/setup-cli/src/setup-services/channels.ts`, `packages/assistant-runtime/src/hosted-runtime/context.ts`, targeted assistant/setup/hosted/daemon/CLI tests | assistant automation state shape, per-channel reply cursors, backfill import wake semantics, shared auto-reply channel reconciliation, inbox source auto-reply enablement | completed | Cross-cutting but intentionally narrow refactor. Preserve overlapping iMessage decommission edits and any unrelated pre-existing changes in shared assistant/CLI test files; do not revert neighboring changes outside this auto-reply architecture lane. |
| Codex | Hosted-runtime auto-reply context slice | `packages/assistant-runtime/src/hosted-runtime/context.ts`, `packages/assistant-runtime/test/hosted-runtime-context*.test.ts` | hosted channel enable/disable reconciliation, per-channel autoReply cursors, inbox routing cursor seeding | in_progress | Narrow worker lane for the hosted-runtime context slice only. Preserve overlapping auto-reply architecture edits elsewhere and avoid setup-cli or broader scheduler changes. |
| Codex | Full iMessage decommission orchestration | `packages/inbox-services/**`, `packages/inboxd/**`, `packages/inboxd-imessage/**`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/setup-cli/**`, `packages/operator-config/**`, `packages/core/**`, `packages/cli/**`, `tsconfig*.json`, `vitest.config.ts`, `scripts/**`, `README.md`, `ARCHITECTURE.md`, `apps/web/**`, `docs/contracts/03-command-surface.md`, `agent-docs/**` | inbox connector/source removal, assistant channel removal, setup channel removal, migration-safe stale-state handling, release topology cleanup, docs/legal cleanup | in_progress | Large cross-cutting removal with subagents. Overlaps existing scheduler lane in shared packages; preserve unrelated edits, integrate carefully, and do not revert scheduler changes. |
| Codex | CLI iMessage test cleanup only | `packages/cli/test/assistant-runtime.test.ts`, `packages/cli/test/inbox-cli.test.ts`, `packages/cli/test/cli-expansion-inbox-attachments.test.ts`, `packages/cli/test/canonical-mutation-boundary.test.ts` | legacy CLI inbox/assistant iMessage assertions, supported-source replacements, migration-pruning coverage | in_progress | User-scoped test-only lane. Preserve concurrent scheduler and decommission edits outside these four files; keep migration coverage for persisted legacy iMessage state where the runtime still sanitizes it. |
| Codex | Hosted Privy cookie-session auth migration | `apps/web/app/{page.tsx,settings/page.tsx,join/**,share/**}`, `apps/web/src/components/{homepage/**,hosted-onboarding/**,hosted-share/**,settings/**}`, `apps/web/src/lib/hosted-onboarding/**`, `apps/web/test/{page.test.ts,join-*.test.ts,hosted-onboarding-*.test.ts,hosted-session.test.ts,settings-*.test.ts,hosted-billing-settings.test.tsx}`, `apps/web/README.md`, `ARCHITECTURE.md`, `agent-docs/operations/verification-and-runtime.md` | hosted onboarding/session auth, server-first hosted page auth snapshot, Privy cookie verification, same-origin client API, hosted auth docs | completed | Browser-authenticated hosted flows now verify Privy's `privy-id-token` cookie directly, and homepage/join/share/settings read the hosted session server-side before falling back to client actions. No Murph-owned auth cookie was introduced; unrelated active `apps/web` edits outside this auth lane were preserved. |
| Codex | Hosted web RSC boundary cleanup | `apps/web/app/{settings/page.tsx,join/[inviteCode]/success/page.tsx,share/[shareCode]/page.tsx}`, `apps/web/src/components/{hosted-onboarding/**,hosted-share/**,settings/**}`, `apps/web/src/lib/device-sync/settings-surface.ts`, `apps/web/test/{settings-page.test.ts,hosted-billing-settings.test.tsx,join-invite-success-client.test.ts,share-link-client.test.ts}` | hosted share/join success server shells, device-sync server-prefetched settings payload, billing portal client island, shared settings/share presentation leaves | in_progress | Narrow hosted-web refactor lane only. Preserve unrelated concurrent `apps/web` work, especially hosted auth migration follow-ons outside these files, and avoid changing route/auth semantics. |
| Codex | Hosted web client residue cleanup | `apps/web/src/components/{hosted-onboarding/**,settings/**}`, targeted `apps/web/test/**`, coordination/plan docs for this lane | dead client state, avoidable hook wrappers, tiny action-leaf cleanup, remaining low-value client residue after the server-first auth refactor | completed | Narrow hosted-web cleanup lane only. Preserve unrelated active `apps/web` and broader iMessage work; no route/auth contract changes. |
| Codex | Route estimate privacy, hiking accuracy, and ownership cleanup | `packages/cli/**`, `packages/assistant-engine/src/assistant-cli-tools/**`, `packages/assistant-engine/src/assistant/system-prompt.ts`, `packages/assistant-engine/test/**`, `README.md`, `ARCHITECTURE.md`, targeted agent docs | Mapbox route estimation, route-argv redaction, hiking geocoding/search fallback, walking entrance preference, CLI ownership seam | in_progress | Narrow route-estimation lane only. Preserve overlapping assistant/CLI scheduler and iMessage edits outside this surface; do not touch unrelated hosted-web work already in the tree. |
| Codex | Route estimate module split and composability cleanup | `packages/cli/src/mapbox-route*.ts`, `packages/cli/src/commands/route.ts`, `packages/cli/src/vault-cli-command-manifest.ts`, `packages/cli/test/mapbox-route.test.ts`, `agent-docs/exec-plans/active/2026-04-10-route-module-split.md` | CLI-owned Mapbox route module boundaries, shared schemas/contracts, point lookup helpers, elevation helpers | in_progress | Keep the external `vault-cli route estimate` API stable while splitting the oversized CLI owner module into focused internal files. Preserve unrelated CLI and scheduler edits elsewhere. |
| Codex | Assistant food ingredient capture prompt guidance | `agent-docs/exec-plans/active/2026-04-12-assistant-food-ingredient-capture.md`, `packages/assistant-engine/src/assistant/system-prompt.ts`, `packages/assistant-engine/test/system-prompt.test.ts` | explicit food/supplement ingredient lookup guidance, calories/amount best-effort capture, future-reference logging prompt wording | in_progress | Narrow prompt-and-test lane only. Preserve overlapping route-estimate and cron framing edits in `system-prompt.ts`; do not widen into parser or CLI behavior changes. |
| Codex | Assistant web-fetch composability patch landing | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/assistant-engine/src/assistant/{web-fetch.ts,web-fetch/**,web-pdf-read.ts}`, `packages/assistant-engine/src/assistant-cli-tools/capability-definitions.ts`, `packages/assistant-engine/test/{web-fetch-*.test.ts,web-pdf-read.test.ts}` | web.fetch module boundaries, shared response/network/html extraction seams, web.pdf.read URL normalization reuse | completed | Landed the watched-thread patch with narrow branch-local test integration fixes, verified the touched assistant-engine seam with package-local typecheck plus focused vitest, and armed the next same-thread wake after a review-gpt send that reported commit-timeout but also saw a matching new user-turn signature. Preserve unrelated dirty `apps/web`, device-sync/importers, `package.json`, and `pnpm-lock.yaml` edits outside this assistant-engine slice. |
| Codex | Assistant web-fetch production follow-up patch landing | `agent-docs/exec-plans/active/2026-04-11-web-fetch-production-followup.md`, `packages/assistant-engine/src/assistant/{web-fetch.ts,web-fetch/**,web-pdf-read.ts}`, `packages/assistant-engine/test/{web-fetch-content.test.ts,web-fetch-html.test.ts,web-fetch-response.test.ts,web-pdf-read.test.ts,web-fetch-runtime.test.ts}` | charset-aware response decoding, bodyless upstream handling, text-vs-binary media gating, normalized markdown href resolution, shared PDF media-type classification | completed | Landed the supplied follow-up patch intent in the assistant-engine web-fetch seam, added focused proof for charset decoding, 204/205/304 bodyless responses, relative-link normalization, unsafe-link stripping, normalized redirects, and shared `application/x-pdf` rejection, and preserved unrelated dirty `apps/web`, `package.json`, and `pnpm-lock.yaml` edits. |
| Codex | Assistant cron execution framing | `packages/assistant-engine/src/assistant/{provider-turn-runner.ts,service-contracts.ts,system-prompt.ts}`, `packages/assistant-engine/test/{provider-turn-runner.test.ts,system-prompt.test.ts,assistant-cron-runtime.test.ts}` | cron turn system prompt context, automation-run framing, prompt builder test coverage | in_progress | Narrow assistant-engine lane only. Preserve overlapping scheduler, route-estimate, and iMessage edits outside these files; no session-model or delivery-behavior changes in this slice. |
| Codex | CLI list/model payload simplification | `packages/operator-config/src/vault-cli-contracts.ts`, `packages/vault-usecases/src/usecases/shared.ts`, `packages/assistant-engine/src/assistant-cli-tools/capability-definitions.ts`, targeted `packages/cli/test/**`, `packages/vault-usecases/test/**`, `packages/assistant-engine/test/**`, coordination/plan docs for this lane | list item schema without markdown-by-default, shared list envelope markdown stripping, `vault.cli.run` parsed-json return contract | completed | Narrow CLI/model-surface lane only. Preserve overlapping route-estimate and scheduler edits in shared assistant/CLI files; avoid unrelated command-surface changes beyond list payload slimming and JSON wrapper dedupe. |
| Codex | CLI list summary architecture cleanup | `packages/vault-usecases/src/{commands/query-record-command-helpers.ts,usecases/{shared.ts,experiment-journal-vault.ts,provider-event.ts,explicit-health-family-services.ts,food.ts,recipe.ts,workout-format.ts}}`, targeted `packages/cli/test/**`, targeted `packages/vault-usecases/test/**` | centralized list excerpt derivation, helper-backed list items, supplement markdown-free compact summaries | completed | Landed the supplied regression fix plus direct proof that supplement/document/meal/provider/workout/food/recipe list items expose compact excerpts without reintroducing list-level `markdown`. Preserved unrelated dirty-tree work outside direct list-surface assertions. |
| Codex | Review-gpt bug-hunt preset | `scripts/review-gpt.config.sh`, `scripts/chatgpt-review-presets/**` | review-gpt local preset registration, high-value seam bug-hunt prompt | completed | Narrow repo-tooling lane only. Preserve unrelated review-gpt automation/runtime work and avoid touching package/app runtime code. |
| Codex | Cloudflare deploy workflow Mapbox secret-name alignment | `.github/workflows/deploy-cloudflare-hosted.yml` | GitHub Actions env secret mapping for hosted worker deploy | completed | Tiny workflow-only lane. Reverted the temporary `MAPBOX_API_TOKEN` mapping so repo/runtime stay canonical on `MAPBOX_ACCESS_TOKEN`; the remaining GitHub secret-store rename still has to happen outside the repo because the current secret value is write-only. |
| Codex | Canonical resource-lock write runtime | `agent-docs/exec-plans/active/2026-04-10-canonical-resource-lock-runtime.md`, `packages/runtime-state/**`, `packages/core/**`, `packages/assistant-engine/src/knowledge/**`, `packages/cli/src/commands/{memory,knowledge}.ts`, targeted tests, `ARCHITECTURE.md`, `docs/contracts/03-command-surface.md` | canonical mutation runner, resource lock taxonomy, memory/knowledge adoption, write concurrency regression coverage | in_progress | Overlap-sensitive runtime refactor. Preserve unrelated dirty-tree edits, especially active assistant and hosted-web lanes. Treat `packages/core/**` write-runtime files as effectively exclusive while this lane is active. |
| Codex | Canonical lock/runtime follow-up hardening | `agent-docs/exec-plans/active/2026-04-10-canonical-lock-hardening.md`, `packages/runtime-state/**`, `packages/core/**`, targeted tests, optional docs touch-ups | directory-lock publish protocol, single write-coordination regime, raw/jsonl canonical staging, raw-target lock correctness | completed | Follow-up correctness lane on the resource-lock runtime. Landed hardened directory-lock publication plus staged-write unification in core; preserved unrelated dirty-tree work in CLI, vault-usecases, assistant, and web packages. |
| Codex | Hosted runner env hardening patch landing | `agent-docs/exec-plans/active/2026-04-10-hosted-runner-env-hardening-patch.md`, `apps/cloudflare/{README.md,scripts/deploy-automation/environment.ts,src/{hosted-env-policy.ts,node-runner.ts,worker-contracts.ts}}`, `packages/assistant-engine/src/{assistant/provider-turn-runner.ts,assistant-cli-tools/execution-adapters.ts}`, targeted tests | runner env profile allowlist, hosted-email profile gating, hosted CLI child env filtering, hosted assistant turn env forwarding | completed | Landed the returned hosted runner env hardening patch, added targeted tests, fixed the hosted-email profile gate so it is real end-to-end, and plumbed zero-data-retention through deploy/config. Unrelated dirty `apps/web`, `package.json`, and `pnpm-lock.yaml` edits were preserved. |
| Codex | Cloudflare runner-bundle composability patch landing | `apps/cloudflare/scripts/assemble-runner-bundle.ts`, `apps/cloudflare/scripts/runner-bundle/**`, `apps/cloudflare/test/container-image-contract.test.ts` | runner bundle assembly phase boundaries, pnpm process seam, runtime shaping helpers, container-image contract coverage | completed | Landed the supplied runner-bundle composability patch, updated the contract test to follow the new helper-module boundaries, and kept unrelated dirty `apps/web`, `package.json`, and `pnpm-lock.yaml` edits out of scope. Scoped `apps/cloudflare` verification still hit unrelated `deploy-automation` and `node-runner` failures. |
| Codex | Hosted runner-bundle production review patch landing | `apps/cloudflare/scripts/assemble-runner-bundle.ts`, `apps/cloudflare/scripts/runner-bundle/{dependency-install.ts,runtime-shape.ts,workspace-artifacts.ts}`, `apps/cloudflare/scripts/runner-bundle-contract.ts`, `apps/cloudflare/test/{container-image-contract.test.ts,runner-bundle-*.test.ts}` | runner bundle dependency pinning, workspace closure derivation, staged/final manifest rewriting | completed | Landed the watched runner-bundle production review patch, tightened optional-dependency filtering and `.bin` regeneration in the same seam, and added focused runner-bundle proof. Focused runner-bundle Vitest coverage passed; scoped `apps/cloudflare` diff verification still hit unrelated pre-existing `node-runner` lock-path failures and the existing deploy-automation zero-data-retention expectation mismatch. Sent the same-thread follow-up review request with attached files, saw a matching new user-turn signature despite browser commit-timeout, and armed the next wake hop at depth 0. |
| Codex | Cloudflare smoke and assistant CLI simplification patch landing | `apps/cloudflare/scripts/{deploy-preflight.ts,deploy-worker-version.shared.ts,smoke-hosted-deploy.shared.ts}`, `apps/cloudflare/test/{deploy-preflight.test.ts,deploy-worker-version.test.ts,smoke-hosted-deploy.test.ts}`, `packages/assistant-engine/src/assistant-cli-tools/execution-adapters.ts`, `packages/assistant-engine/test/execution-adapters.test.ts` | hosted deploy env validation, hosted worker deploy branching, smoke control request headers, assistant CLI launcher resolution | completed | Landed the watched-thread review patch plus one test-only coverage follow-up. Preserved unrelated dirty `apps/web`, `package.json`, and `pnpm-lock.yaml` edits. Truthful `test:diff` still hit unrelated pre-existing blockers in `packages/cli/test/supplement-wearables-coverage.test.ts:190` after earlier unrelated assistantd noise, but focused Cloudflare and assistant-engine checks passed. |
| Codex | Watched security audit follow-up patch landing | `agent-docs/exec-plans/active/2026-04-10-security-audit-watch-patch-followup.md`, `packages/assistantd/src/http.ts`, `packages/assistantd/test/http.test.ts`, `packages/device-syncd/src/{hosted-hints.ts,http.ts,providers/oura.ts}`, `packages/device-syncd/test/{http-control-helpers.test.ts,http.test.ts,hosted-hints.test.ts,oura-provider.test.ts}`, `packages/importers/src/device-providers/{garmin.ts,oura.ts,shared-normalization.ts,whoop.ts}`, `packages/importers/test/device-providers/deletion-normalization.test.ts`, `packages/parsers/src/shared.ts`, `packages/parsers/test/shared.test.ts` | assistant/device-sync local control-plane header hardening, metadata-only device deletion artifacts, parser child env allowlist | completed | Landed the watched-thread patch intent, ran required package coverage plus direct proof, sent the same-thread follow-up review prompt, and armed the next wake hop. Preserved overlapping scheduler work in `packages/assistantd/**` and unrelated dirty `apps/web` / lockfile edits. |
| Codex | Security audit target-area review follow-up patch landing | `packages/assistantd/{src/http.ts,test/http.test.ts}`, `packages/device-syncd/{src/http.ts,test/http-control-helpers.test.ts}`, `packages/importers/{src/device-providers/shared-normalization.ts,test/device-providers/deletion-normalization.test.ts}`, `packages/parsers/test/shared.test.ts` | shared loopback control-plane parsing, unique deletion artifact ids, parser child env allowlist casing | completed | Landed the remaining still-applicable changes from `security-target-area-followup.patch`: removed the last assistantd/device-syncd token wrapper indirection, kept deletion artifact content owned by one descriptor path, and added focused regression proof for duplicate auth, host-error mapping, undefined deletion strings, and parser env edge cases. Required verification passed twice after the coverage-write test-only follow-up: `pnpm typecheck`, `pnpm test:diff packages/assistantd packages/device-syncd packages/importers packages/parsers`, `pnpm test:smoke`, plus direct `pnpm exec tsx --eval ...` proof. Required audit passes returned no findings after the parser-test cast fix. |
| Codex | Security audit watched pass3 patch landing | `packages/device-syncd/{src/{public-ingress.ts,service.ts,shared.ts,types.ts},test/{public-ingress.test.ts,service.test.ts,shared-ids.test.ts}}`, `packages/importers/{src/device-providers/shared-normalization.ts,test/device-providers/deletion-normalization.test.ts}` | public-ingress durable trace scoping, provider-facing trace-id preservation, duplicate deletion-marker collapse | completed | Landed the watched-thread patch plus audit-driven follow-up fixes for delimiter-safe durable ids and deletion identity collisions. Verified with focused device-syncd/importers tests, owner coverage, owner typechecks, and smoke; repo-wide `pnpm typecheck` / `pnpm test:diff packages/device-syncd packages/importers` remain blocked by the unrelated pre-existing `packages/cli/test/supplement-wearables-coverage.test.ts:190` error. Sent the required same-thread review request; the browser send hit commit-timeout but recorded a new matching user-turn signature, and the recursive wake hop at depth 0 is armed. |
| Codex | Code quality audit patch landing | `apps/cloudflare/scripts/render-worker-secrets.ts`, `packages/assistant-engine/src/assistant/shared.ts`, `packages/operator-config/src/{assistant/shared.ts,device-daemon/paths.ts,vault-cli-contracts.ts}`, `packages/runtime-state/src/local-state-files.ts`, targeted tests | assistant env/base-URL helper ownership, truthful missing-path handling, device-sync package resolution, query family contract source | completed | Landed the watched ChatGPT code-quality audit patch plus focused proof updates, tightened device-sync fallback so broken local installs are not masked, and preserved unrelated dirty `apps/web`, `package.json`, `pnpm-lock.yaml`, and concurrent package edits outside this slice. |
| Codex | Hosted runner runtime audit patch landing | `apps/cloudflare/src/{runner-env.ts,node-runner.ts}`, `apps/cloudflare/test/{runner-env.test.ts,node-runner.test.ts}` | hosted runner typed runtime envelope ownership, child-env control-key stripping, container runtime regression coverage | completed | Landed the watched-thread patch intent, preserved typed runtime ownership in the container path, added focused regression coverage, and left unrelated dirty `apps/web`, `package.json`, `pnpm-lock.yaml`, and broader cloudflare/runtime failures untouched. |
| Codex | Boundary audit patch landing | `apps/web/src/lib/hosted-onboarding/telegram.ts`, `packages/device-syncd/{README.md,src/public-ingress.ts}`, `packages/inboxd/src/connectors/telegram/normalize.ts`, `packages/messaging-ingress/{README.md,package.json,src/{telegram-types.ts,telegram-webhook.ts,telegram-webhook-payload.ts},test/telegram-webhook.test.ts}`, `packages/query/src/{index.ts,knowledge-contracts.ts,knowledge-graph.ts,knowledge-search.ts}`, `scripts/verify-workspace-boundaries.mjs` | device-sync public-ingress owner seam, Telegram payload entrypoint split, derived-knowledge search ownership guard | completed | Landed the watched-thread follow-up patch that moved shared Telegram wire types to `telegram-types.ts`, moved derived-knowledge search contracts onto `knowledge-search.ts`, kept the root/public seams clean with hardened boundary guards, ran required scoped verification with an unrelated `packages/assistantd/test/http.test.ts:354` typecheck blocker noted separately, sent the same-thread follow-up review prompt, and armed the final wake hop. Preserve unrelated dirty `apps/web`, assistant/operator-config/runtime-state, Cloudflare, `package.json`, and `pnpm-lock.yaml` edits outside this slice. |
| Codex | Knowledge-boundary wake follow-up patch landing | `agent-docs/references/data-model-seams.md`, `packages/assistant-engine/{src/{index.ts,knowledge.ts},test/{assistant-wrapper-exports.test.ts,knowledge-boundary.test.ts}}`, `packages/cli/{src/index.ts,test/knowledge-boundary.test.ts}`, `packages/query/test/knowledge-boundary.test.ts`, `scripts/verify-workspace-boundaries.mjs` | assistant-engine root/knowledge export leaks, public helper leak removal, published CLI root knowledge-contract leak, query knowledge boundary guards | completed | Landed the watched-thread follow-up patch plus coverage/review-driven helper hard-cut, removed obsolete knowledge-boundary tests that depended on leaked surfaces, verified with boundary/cycle checks, `pnpm typecheck`, `pnpm test:smoke`, and `pnpm test:diff packages/assistant-engine packages/cli packages/query`, then sent the next same-thread review request with attached files. The browser send hit commit-timeout but recorded a new matching user-turn signature (`repo.repomix 68` / `repo.snapshot 72`), and the detached recursive wake hop at depth 1 is armed. Preserve unrelated dirty device-sync work outside this knowledge-boundary slice. |
| Codex | Device-sync webhook follow-up patch landing | `agent-docs/exec-plans/completed/2026-04-11-device-sync-webhook-followup-patch.md`, `packages/device-syncd/{src/{public-ingress.ts,service.ts,types.ts},test/{public-ingress.test.ts,service.test.ts}}`, `apps/web/src/lib/device-sync/{public-ingress-service.ts,wake-service.ts}`, `apps/web/test/device-sync-hosted-wake-dispatch.test.ts` | ingress webhook contract split, durable trace handoff, hosted wake transaction ordering, missing-owner retry behavior | completed | Landed the supplied follow-up patch intent on top of the already-landed pass3 durable trace scoping, kept disconnected-account webhook dedupe intact, added retryable unknown-owner/reauthorization coverage plus hosted outbox-failure proof, and verified the touched owners with package/app typecheck, `packages/device-syncd` coverage, and `apps/web verify`. Repo-wide `pnpm typecheck` and `pnpm test:diff ...` still hit unrelated pre-existing blockers in `packages/cli` and reverse-dependent `packages/assistant-engine`. Preserve unrelated dirty `apps/web` homepage/auth work, `package.json`, `pnpm-lock.yaml`, and concurrent assistant/web-fetch edits outside this slice. |
| Codex | Device-sync webhook wake prose-patch landing | `agent-docs/exec-plans/completed/2026-04-11-device-sync-webhook-wake-prose-patch.md`, `packages/device-syncd/{src/{public-ingress.ts,service.ts,types.ts},test/public-ingress.test.ts}`, `apps/web/src/lib/{http.ts,device-sync/public-ingress-service.ts}`, `apps/web/test/device-sync-hosted-wake-dispatch.test.ts` | ingress accepted-hook completion contract, webhook-trace payload-field removal, hosted webhook body-size cap parity | completed | Prose-only wake-task follow-up already landed and closed; keep this v2 patch lane scoped to the new artifact-only contract narrowing on top. |
| Codex | Hosted Linq routing final-review patch landing | `apps/web/src/lib/hosted-onboarding/{linq-home-routing.ts,linq-routing-policy.ts}`, `apps/web/test/{hosted-onboarding-linq-home-routing.test.ts,hosted-onboarding-linq-routing.test.ts}` | activation first-contact thread return, dead pool-config export removal, Linq routing regression proof | completed | Landed the still-applicable review-patch hunks only: fresh activation-created Linq home chats now return the created chat id for first-contact dispatch, the dead pool-config export is removed, and focused hosted-Linq regression tests were updated. Focused Vitest proof passed; `pnpm typecheck` and `pnpm test:diff apps/web` remain blocked by unrelated pre-existing `apps/web` device-sync issues. Required coverage-write and final-review audit passes found no additional changes, the same-thread review request recorded a matching new user-turn signature despite browser commit-timeout, and the detached recursive wake hop at depth 0 is armed under `output-packages/chatgpt-watch/69d89e4e-310c-8399-9010-51835c5b2112-2026-04-12T015042Z/`. |
| Codex | Repo checks green and worktree cleanup | `agent-docs/exec-plans/active/2026-04-11-repo-green-and-clean.md`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, assistant/cloudflare/core/inboxd/setup/web files directly implicated by repo-wide verification, and the remaining dirty `apps/web/src/components/{homepage/{hero-section.tsx,homepage-auth-panel.tsx,integrations-bar.tsx},hosted-onboarding/hosted-phone-auth-views.tsx}` | missing `incur` workspace resolution, repo-wide typecheck/test:diff blockers, final dirty worktree cleanup | completed | Restored repo-wide `pnpm typecheck` and `pnpm test:diff` to green, added assistant lock-layout migration compatibility, fixed stale test expectations across cloudflare/setup-cli/assistant-runtime, preserved the existing web/UI edits while reverting inaccurate `iMessage` marketing copy back to `Linq`, and prepared the tree for one scoped cleanup commit. |
