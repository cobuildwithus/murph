# Coordination Ledger

Active coding work must claim ownership here before code changes begin.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| codex-raw-incur | Replace the custom CLI wrapper layer with native `incur` command definitions while preserving current command behavior and envelopes | `packages/cli/src/command-helpers.ts`; `packages/cli/src/root-middleware.ts`; `packages/cli/src/vault-cli-contracts.ts`; `packages/cli/src/commands/*.ts`; `packages/cli/test/*.test.ts`; `agent-docs/exec-plans/active/2026-03-12-raw-incur-cli.md` | `defineCommand`; `wrapCommand`; command `run` handler shapes; CLI success/failure envelope; markdown output contract | in_progress | Avoid `packages/cli/package.json` because the active workspace-cleanup lane owns it. Keep scope inside the CLI source and tests. |
| codex-workspace-cleanup | Finish the TS-first workspace cleanup by hardening source-artifact checks, splitting the contracts library/scripts builds, and removing package-local typecheck side effects | `agent-docs/exec-plans/active/2026-03-12-workspace-cleanup.md`; `agent-docs/operations/verification-and-runtime.md`; `agent-docs/references/testing-ci-map.md`; `scripts/check-no-js.ts`; `tsconfig.json`; `packages/contracts/package.json`; `packages/contracts/README.md`; `packages/contracts/tsconfig.json`; `packages/contracts/tsconfig.build.json`; `packages/contracts/tsconfig.scripts.json`; `packages/contracts/scripts/generate-json-schema.ts`; `packages/contracts/scripts/verify.ts`; `packages/core/tsconfig.json`; `packages/core/tsconfig.build.json`; `packages/importers/package.json`; `packages/cli/package.json` | no-source-artifact guard; contracts package dist layout; contracts build/typecheck split; contracts script compilation; workspace build reference path; package-local typecheck semantics; CLI incur dependency range | in_progress | Avoid root `package.json` because it is already owned by the active CLI cleanup lane. Keep this task on workspace/package plumbing only. |
| codex-health-integrator | Coordinate the health-discipline integrity pass, track ownership, and integrate subagent results without crossing active lanes | `agent-docs/exec-plans/active/2026-03-13-health-discipline-pass.md`; `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | plan state; ledger ownership | in_progress | Integration lane only. Do not edit files owned by the export-tail, raw-incur, or workspace-cleanup rows. |
| codex-health-contract-alignment | Eliminate family/genetics contract drift and persist query-visible timestamps in canonical registry docs | `packages/core/src/family/types.ts`; `packages/core/src/family/api.ts`; `packages/core/src/genetics/types.ts`; `packages/core/src/genetics/api.ts`; `packages/core/test/health-history-family.test.ts` | `FAMILY_MEMBER_SCHEMA_VERSION`; `GENETIC_VARIANT_SCHEMA_VERSION`; `VARIANT_ZYGOSITIES`; `buildAttributes`; `recordFromParts`; `upsertFamilyMember`; `upsertGeneticVariant` | in_progress | Keep scope inside family/genetics code and their dedicated tests. Do not touch vault validation, bank registries, or query export files. |
| codex-health-core-discipline | Make health mutations audited, make registry upserts merge-preserving, and extend validation across health ledgers and registries | `packages/core/src/vault.ts`; `packages/core/src/profile/storage.ts`; `packages/core/src/history/api.ts`; `packages/core/src/bank/goals.ts`; `packages/core/src/bank/conditions.ts`; `packages/core/src/bank/allergies.ts`; `packages/core/src/bank/regimens.ts`; `packages/core/src/assessment/storage.ts`; `packages/core/src/assessment/project.ts`; `packages/core/package.json`; `packages/core/test/health-bank.test.ts`; `packages/core/test/profile.test.ts`; `packages/core/test/core.test.ts` | `validateVault`; `appendProfileSnapshot`; `rebuildCurrentProfile`; `appendHistoryEvent`; `upsertGoal`; `upsertCondition`; `upsertAllergy`; `upsertRegimenItem`; `stopRegimenItem`; `importAssessmentResponse`; `projectAssessmentResponse`; package-local `test` script | in_progress | Owns audit, validation, merge semantics, and core-local runner alignment only. Do not touch family/genetics files or query package files. |
| codex-health-query-cleanup | Fix health registry query edge cases and align query-local tests with the configured runner without touching export-pack lane files | `packages/query/src/health/registries.ts`; `packages/query/package.json`; `packages/query/test/query.test.ts`; `packages/importers/test/importers.test.ts` | `listFamilyMembers`; `listGeneticVariants`; package-local `test` script | in_progress | Query cleanup lane only. Do not touch `packages/query/src/export-pack.ts`, `packages/query/src/export-pack-health.ts`, `packages/query/test/health-tail.test.ts`, or `packages/importers/package.json`. |
