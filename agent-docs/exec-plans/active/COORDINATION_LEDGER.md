# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Audit the Goal shared-entity refactor for coverage gaps and add only the highest-impact Goal regression tests without touching production behavior. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/core/test/health-bank.test.ts`, `packages/query/test/health-registry-definitions.test.ts`, `packages/cli/test/health-tail.test.ts` | Goal relation clear/canonicalization assertions; Goal shared query projection proof; Goal CLI descriptor/runtime relation proof | in_progress | Narrow non-exclusive audit lane on top of the active Goal row. Preserve adjacent edits, keep scope to the three allowed Goal test files, and do not change production code unless a tiny harness fix becomes unavoidable. |
| Codex | Land the final reviewed hosted follow-up fixes across outbox equality, replay handling, share payload auth, and shared proxy-host seams without widening into unrelated hosted runtime work. | `agent-docs/exec-plans/active/{2026-03-29-hosted-followup-fixes.md,COORDINATION_LEDGER.md}`, `apps/web/src/lib/hosted-execution/outbox.ts`, `apps/web/app/api/hosted-share/internal/[shareId]/payload/route.ts`, `apps/web/src/lib/hosted-share/{service.ts,shared.ts}`, targeted `apps/web/test/{hosted-execution-outbox.test.ts,hosted-share-*.test.ts}`, `apps/cloudflare/src/{runner-env.ts,runner-container.ts,runner-outbound.ts,user-runner/runner-queue-store.ts}`, targeted `apps/cloudflare/test/{runner-outbound.test.ts,node-runner.test.ts,user-runner.test.ts}`, `packages/hosted-execution/src/{callback-hosts.ts,index.ts}`, `packages/assistant-runtime/src/{hosted-device-sync-runtime.ts,hosted-runtime/events/share.ts}`, targeted `packages/assistant-runtime/test/{hosted-device-sync-runtime.test.ts,hosted-runtime-http.test.ts}` | semantic hosted outbox equality; non-authoritative replay Bloom filter; user-bound hosted share payload auth; share-token-only proxy auth; shared proxy-host constants; remove trivial control-plane shim | in_progress | Narrow non-exclusive hosted follow-up lane. This overlaps active hosted hardening and hosted reliability rows, so read live file state first, preserve adjacent edits, and keep this scoped to the six reviewed follow-up items plus direct regressions. |
| Codex | Add the hosted AI usage ledger end to end so successful hosted provider attempts persist immutable usage under `assistant-state/usage/pending`, export post-commit into hosted web/Postgres, and optionally drain token meter events to Stripe on a later cron. | `agent-docs/exec-plans/active/{2026-03-29-hosted-ai-usage-ledger.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `agent-docs/{index.md,references/testing-ci-map.md,operations/verification-and-runtime.md}`, `packages/runtime-state/src/{assistant-state.ts,assistant-usage.ts,index.ts}`, targeted `packages/runtime-state/test/*`, `packages/cli/src/assistant/{provider-registry.ts,service.ts,store/persistence.ts}`, targeted `packages/cli/test/{assistant-provider.test.ts,assistant-runtime.test.ts}`, `packages/hosted-execution/src/{env.ts,routes.ts,web-control-plane.ts}`, targeted `packages/hosted-execution/test/*`, `packages/assistant-runtime/src/hosted-runtime/{environment.ts,execution.ts,models.ts,usage.ts}`, targeted `packages/assistant-runtime/test/*`, `apps/web/prisma/{schema.prisma,migrations/2026032901_hosted_ai_usage/migration.sql}`, `apps/web/app/api/internal/hosted-execution/usage/{record,cron}/route.ts`, `apps/web/src/lib/hosted-execution/{internal.ts,usage.ts,stripe-metering.ts}`, targeted `apps/web/test/{hosted-execution-routes.test.ts,hosted-execution-stripe-metering.test.ts,vercel-config.test.ts}`, `apps/web/vercel.json`, `apps/web/README.md` | assistant usage record schema/path helpers; provider usage extraction for Codex and OpenAI-compatible runs; hosted post-commit usage export; hosted web AI-usage import table; optional Stripe token metering drain | in_progress | Narrow non-exclusive hosted execution/billing lane. Overlaps active hosted onboarding and `apps/web/prisma/schema.prisma` work, so read live file state first, preserve adjacent edits, and keep this scoped to the durable usage-ledger path plus direct regressions/docs. |
| Codex | Harden local assistant outbox recovery plus hosted execution queue/hydration/key-rotation reliability for malformed state and duplicate-send edges. | `agent-docs/exec-plans/active/{2026-03-29-outbox-hosted-reliability-hardening.md,COORDINATION_LEDGER.md}`, `packages/cli/src/{assistant-cli-contracts.ts,outbound-channel.ts,assistant/{outbox.ts,channel-adapters.ts,doctor.ts,status.ts}}`, targeted `packages/cli/test/{assistant-observability.test.ts,assistant-robustness.test.ts,assistant-channel.test.ts}`, `apps/web/src/lib/hosted-execution/{hydration.ts,outbox.ts}`, targeted `apps/web/test/{hosted-execution-hydration.test.ts,hosted-execution-outbox.test.ts}`, `apps/cloudflare/src/{crypto.ts,bundle-store.ts,execution-journal.ts,outbox-delivery-journal.ts,user-runner/runner-queue-store.ts,user-runner/runner-bundle-sync.ts,user-runner/runner-commit-recovery.ts}`, targeted `apps/cloudflare/test/{user-runner.test.ts,index.test.ts}` | tolerant assistant outbox inventory plus quarantine; durable assistant send-attempt/idempotency metadata; hosted hydration permanent-vs-retryable failures; malformed pending-row poisoning; malformed bundle-ref clearing; migrate-on-read hosted re-encryption | in_progress | Narrow non-exclusive reliability lane. Direct overlap exists with active CLI assistant and hosted runtime work, so read live state first, preserve adjacent edits, and keep this pass limited to the requested corruption/idempotency/rotation fixes plus direct regression tests/docs. |
| Codex | Port Goal onto one shared registry entity definition consumed by contracts, core, query, and CLI without changing the underlying markdown plus JSONL storage model. | `agent-docs/exec-plans/active/{2026-03-29-goal-entity-definition.md,COORDINATION_LEDGER.md}`, `CONTINUITY_goal-entity-definition.md`, `ARCHITECTURE.md`, `agent-docs/{index.md,references/health-entity-taxonomy-seam.md}` only if the landed seam changes docs truthfully, `packages/contracts/src/{health-entities.ts,shares.ts,zod.ts,examples.ts,schemas.ts}` as needed, `packages/core/src/{bank/{goals,types}.ts,registry/{api,markdown}.ts,index.ts,public-mutations.ts}`, targeted `packages/core/test/*`, `packages/query/src/{canonical-entities.ts,health/{canonical-collector,goals,registries}.ts,index.ts}`, targeted `packages/query/test/*`, `packages/cli/src/{health-cli-descriptors.ts,health-cli-method-types.ts,usecases/explicit-health-family-services.ts,assistant-cli-tools.ts}`, targeted `packages/cli/test/*` | shared Goal registry entity definition; Goal domain-vs-document seam; Goal link extraction/projection metadata; Goal CLI/query/core descriptor reuse | in_progress | Narrow non-exclusive Goal lane. Overlaps active health CLI descriptor and query-helper cleanup rows, so read live state first, preserve adjacent edits, and keep this limited to Goal plus the minimum shared helper seams it genuinely needs. |
| Codex | Fix the reviewed hosted Oura/device-sync webhook lifecycle, control-plane ownership, verification-contract, and job-payload issues without widening into unrelated hosted runtime work. | `agent-docs/exec-plans/active/{2026-03-29-oura-hosted-webhook-cleanup.md,COORDINATION_LEDGER.md}`, `apps/web/app/api/{device-sync/webhooks/[provider]/route.ts,internal/device-sync/runtime/snapshot/route.ts}`, `apps/web/src/lib/device-sync/{control-plane.ts,wake-service.ts,prisma-store.ts}`, `apps/web/test/{agent-route.test.ts,device-sync-hosted-wake-dispatch.test.ts,hosted-device-sync-internal-routes.test.ts}`, `packages/device-syncd/src/{http.ts,public-ingress.ts,providers/oura.ts,providers/oura-webhooks.ts}`, `packages/device-syncd/test/{http.test.ts,oura-provider.test.ts,oura-webhooks.test.ts,public-ingress.test.ts}` | hosted webhook trace close-on-null-owner; explicit Oura global webhook-admin upkeep seam; shared GET webhook verification helper; kind-specific Oura webhook job payloads | in_progress | Narrow non-exclusive lane. This overlaps active hosted device-sync work in `apps/web` and `packages/device-syncd`, so read live state first, preserve adjacent edits, and keep this pass limited to the reviewed Oura/webhook simplifications plus direct regressions. |
| Codex | Clean up Telegram hosted/local parity by sharing the canonical message model, threading hosted bot identity, internalizing `TelegramThreadTarget`, and making local poll transport explicit without widening into broader onboarding/runtime work. | `agent-docs/exec-plans/active/{2026-03-28-telegram-hosted-parity-cleanup.md,COORDINATION_LEDGER.md}`, `packages/inboxd/src/connectors/telegram/{normalize.ts,target.ts,connector.ts}`, `packages/inboxd/test/telegram-connector.test.ts`, `packages/cli/src/{telegram-runtime.ts,inbox-services/connectors.ts,inbox-app/types.ts,assistant/channel-adapters.ts}`, targeted `packages/cli/test/{assistant-channel.test.ts,inbox-cli.test.ts}`, `packages/hosted-execution/src/{contracts.ts,builders.ts,parsers.ts}`, `packages/hosted-execution/test/hosted-execution.test.ts`, `packages/assistant-runtime/src/hosted-runtime/events/telegram.ts`, `apps/web/src/lib/hosted-onboarding/{telegram.ts,webhook-provider-telegram.ts,webhook-receipt-dispatch.ts}`, targeted `apps/web/test/{hosted-onboarding-telegram-dispatch.test.ts,hosted-execution-contract-parity.test.ts,hosted-execution-hydration.test.ts}` | shared Telegram direct/self/actor helpers; hosted Telegram `botUserId` dispatch metadata; internal `TelegramThreadTarget` routing; explicit Telegram poll transport mode | in_progress | Narrow non-exclusive Telegram cleanup lane. Direct overlap exists in `apps/web` hosted-onboarding and CLI channel files, so read live state first, preserve adjacent edits, and keep this limited to the requested Telegram parity/simplification changes plus direct regressions. |
| Codex | Harden hosted onboarding Stripe receipt recovery, billing monotonicity, checkout idempotency, and RevNet activation semantics without widening into unrelated onboarding flows. | `agent-docs/exec-plans/active/{2026-03-28-hosted-onboarding-stripe-hardening.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `docs/cloudflare-hosted-idempotency-followup.md`, `apps/web/prisma/{schema.prisma,migrations/**}`, `apps/web/src/lib/hosted-onboarding/{billing-service.ts,billing.ts,runtime.ts,session.ts,stripe-billing-policy.ts,stripe-revnet-issuance.ts,webhook-provider-stripe.ts,webhook-receipt-engine.ts,webhook-receipt-store.ts,webhook-receipt-transitions.ts,webhook-receipt-types.ts,webhook-service.ts}`, targeted `apps/web/test/hosted-onboarding-*.test.ts` | hosted webhook receipt lease/planned-state recovery; Stripe freshness gating; checkout/customer idempotency; subscription access revocation; RevNet activation sequencing and fail-closed submission recording | in_progress | Narrow non-exclusive lane on top of the live onboarding tree. Preserve adjacent webhook/RevNet edits already in flight, keep Cloudflare dispatch journaling transactional with the originating mutation, and do not widen into unrelated Privy/invite UX work. |
| Codex | Refactor assistant session/provider seams so stable Murph session identity is split from last successful provider binding, provider traits become explicit, and duplicate provider execution wrappers collapse onto the registry boundary. | `agent-docs/exec-plans/active/{2026-03-28-assistant-provider-session-seams.md,COORDINATION_LEDGER.md}`, `packages/cli/src/{assistant-cli-contracts.ts,assistant-provider.ts,chat-provider.ts}`, `packages/cli/src/assistant/{failover.ts,provider-catalog.ts,provider-registry.ts,provider-state.ts,provider-turn-recovery.ts,service.ts,store.ts,store/persistence.ts,store/types.ts}`, targeted `packages/cli/test/{assistant-provider.test.ts,assistant-runtime.test.ts,assistant-service.test.ts,assistant-state.test.ts,assistant-robustness.test.ts}` | assistant session/provider-binding split; provider traits metadata; registry-owned prompt/message execution shaping; service recovery without early session mutation | in_progress | Narrow non-exclusive CLI architecture lane. Overlaps active assistant runtime/chat work, so read the live file state first, preserve adjacent edits, and avoid touching Ink UI files unless a type edge forces a tiny compatibility follow-up. |
| Codex | Implement the requested hosted runtime trust-boundary cleanup without changing hosted execution behavior. | `agent-docs/exec-plans/active/{2026-03-28-hosted-runtime-trust-boundaries.md,COORDINATION_LEDGER.md}`, `packages/{hosted-execution/**,assistant-runtime/**}`, `apps/web/src/lib/hosted-execution/**`, `apps/web/src/lib/hosted-share/shared.ts`, `apps/web/src/lib/device-sync/internal-runtime.ts`, `apps/cloudflare/src/{node-runner,runner-env,user-runner}.ts`, targeted hosted web/Cloudflare/runtime tests | shared hosted outbox payload ownership; canonical dispatch lifecycle/state mapping; hosted web control-plane adapter ownership; strict token separation for hosted internal/share/control/container paths | completed | Narrow non-exclusive lane. Completed with focused hosted-runtime/web/cloudflare suites green; repo-wide verification remains blocked by unrelated active `packages/cli`, `apps/web` device-sync, and hosted-onboarding drift already present in the worktree. |
| Codex | Orchestrate four parallel production-blocker fixes for hosted onboarding idempotency, assistant auto-reply dedupe, and Ink session state resync, then integrate the resulting worker diffs back into the main tree. | `agent-docs/exec-plans/active/{2026-03-28-four-production-blockers-parallel.md,COORDINATION_LEDGER.md}`, main-tree integration of `apps/web/src/lib/hosted-onboarding/{stripe-revnet-issuance.ts,revnet-repair-service.ts,webhook-receipt-engine.ts,webhook-receipt-transitions.ts}`, `apps/web/test/{hosted-onboarding-webhook-idempotency.test.ts,hosted-onboarding-revnet-repair-service.test.ts}`, `packages/cli/src/assistant/{automation/reply.ts,ui/ink.ts}`, targeted `packages/cli/test/{assistant-runtime.test.ts,assistant-robustness.test.ts}` | RevNet issuance replay closure; hosted webhook side-effect sent-uncertain handling; auto-reply durable dedupe marker; Ink active model/reasoning resync after session mutation | in_progress | Parallel worker orchestration lane. The requested prompts collide on `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts`, `packages/cli/test/assistant-runtime.test.ts`, and the active exclusive `packages/cli/src/assistant/ui/ink.ts` lane, so workers are running in isolated git worktrees and only integration lands in the shared tree. Preserve unrelated dirty edits in the main tree. |
| Codex | Implement the five requested production followups across hosted runtime hardening, device-sync failure isolation, assessment surface/type alignment, and the first real assistant-services ownership move. | `agent-docs/exec-plans/active/{2026-03-28-production-followups-1-5.md,COORDINATION_LEDGER.md}`, `packages/assistant-runtime/src/{hosted-device-sync-control-plane.ts,hosted-device-sync-runtime.ts,hosted-email.ts,hosted-runtime/{environment.ts,events/email.ts,events/share.ts,maintenance.ts,models.ts}}`, `packages/assistant-runtime/test/{assistant-services-boundary.test.ts,hosted-runtime-http.test.ts,hosted-runtime-maintenance.test.ts}`, `packages/cli/src/{health-cli-descriptors.ts,health-cli-method-types.ts,usecases/explicit-health-family-services.ts}`, `packages/cli/test/health-tail.test.ts`, `packages/query/src/{canonical-entities.ts,health/{assessments.ts,projections.ts}}`, `packages/query/test/health-tail.test.ts`, `packages/assistant-services/src/{operator-config.ts,store.ts}` | hosted internal HTTP helpers; maintenance best-effort control-plane reconciliation; assessment filter/type alignment; assistant-services operator-home ownership | in_progress | Narrow non-exclusive follow-up lane on top of active hosted-runtime and CLI work. Read live file state first, preserve adjacent edits, keep device-sync wake failures hard, and do not widen the assistant-services move beyond hosted-facing operator-config/store ownership in this pass. |
| Codex | Fix the remaining hosted device-sync production blockers from the final review without widening into unrelated hosted/runtime work. | `agent-docs/exec-plans/active/{2026-03-28-device-sync-production-blockers.md,COORDINATION_LEDGER.md}`, `apps/web/src/lib/device-sync/internal-runtime.ts`, `apps/web/test/device-sync-internal-runtime.test.ts`, `packages/device-syncd/src/{store,types}.ts`, `packages/device-syncd/test/service.test.ts`, `packages/assistant-runtime/src/{hosted-device-sync-runtime.ts,hosted-runtime/maintenance.ts,hosted-runtime/models.ts}`, `packages/assistant-runtime/test/hosted-runtime-maintenance.test.ts`, `apps/cloudflare/{src/user-runner.ts,test/user-runner.test.ts}` | hosted device-sync wake scheduling; token-version fencing; null-expiry no-op handling; hosted hydration/disconnect parity; monotonic hosted reconciliation | in_progress | Narrow non-exclusive follow-up lane to the completed hosted parity patch. Preserve adjacent hosted-runtime and Cloudflare edits already in flight, and keep any docs updates limited to this execution plan unless the implementation proves an architecture/runtime doc change is required. |
| Codex | Land the hosted device-sync parity bridge from the reviewed patch set on top of the current hosted control-plane and Cloudflare runner tree, preserving overlapping in-flight edits. | `agent-docs/exec-plans/active/{2026-03-28-device-sync-hosted-parity.md,COORDINATION_LEDGER.md}`, `CONTINUITY_device-sync-hosted-parity.md`, `ARCHITECTURE.md`, `docs/device-sync-hosted-control-plane.md`, `apps/web/{README.md,app/api/internal/device-sync/runtime/{snapshot,apply}/route.ts,src/lib/device-sync/{hosted-dispatch.ts,internal-runtime.ts,wake-service.ts},src/lib/hosted-execution/hydration.ts,test/{device-sync-hosted-wake-dispatch.test.ts,device-sync-internal-runtime.test.ts,hosted-execution-contract-parity.test.ts,hosted-execution-hydration.test.ts}}`, `packages/hosted-execution/src/{builders.ts,contracts.ts,parsers.ts}`, `packages/assistant-runtime/src/{hosted-device-sync-control-plane.ts,hosted-device-sync-runtime.ts,hosted-runtime.ts}`, `apps/cloudflare/src/runner-env.ts` | richer `device-sync.wake` contract payloads; hosted snapshot/apply bridge; runner hydration/reconciliation; token-version fencing; null-safe hosted reconciliation | completed | Completed as a narrow non-exclusive hosted device-sync lane. Focused hosted web tests and package typechecks passed; repo-wide wrappers are still blocked by unrelated existing `packages/cli` failures (`reply.ts` typing and `dist/assistant` cleanup). |
| Codex | Finish the shared PDF-routing fallback lane by removing the eager `@murph/hosted-execution` root re-export from `@murph/runtime-state` and rerunning repo verification. | `agent-docs/exec-plans/active/{2026-03-28-pdf-routing-fallback.md,COORDINATION_LEDGER.md}`, `packages/runtime-state/{package.json,src/index.ts,test/hosted-execution-reexport.test.ts}` | `@murph/runtime-state` export surface; hosted bundle type ownership split; CLI runtime lazy loading for non-hosted commands | completed | Completed in the shared cleanup batch. The runtime-state root export now stays lazy for non-hosted CLI entrypoints; broader repo wrappers remain blocked outside this lane by the current `packages/contracts` build/verify issue. |
| Codex | Land the hosted workspace-snapshot plus external artifact patch on top of the current Cloudflare/runtime tree, preserving the existing bootstrap/user-env split and current runner state-machine seams. | `agent-docs/exec-plans/active/{2026-03-28-cloudflare-workspace-artifacts-patch.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `packages/runtime-state/{README.md,src/{hosted-bundle.ts,hosted-bundles.ts},test/hosted-bundle.test.ts}`, `packages/assistant-runtime/src/{hosted-runtime.ts,hosted-runtime/{environment.ts,execution.ts,models.ts}}`, `apps/cloudflare/{README.md,src/{bundle-store.ts,node-runner.ts,runner-container.ts,runner-outbound.ts},test/{index.test.ts,node-runner.test.ts,runner-container.test.ts}}` | hosted bundle v2 schema; single hosted workspace snapshot in `vault`; externalized raw-artifact refs; runner artifact fetch/upload plumbing | in_progress | Narrow non-exclusive hosted-runtime lane. Preserve adjacent dirty hosted-runtime and Cloudflare edits, keep the public bundle slots stable (`vault` reused, `agentState` null for new snapshots), and do not widen into unrelated DO queue behavior beyond the artifact transport needed by this patch. |
| Codex | Restore `profile list` date-range forwarding after the explicit health adapter migration without changing any other migrated health behavior. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/usecases/explicit-health-family-services.ts`, `packages/query/src/health/shared.ts`, targeted `packages/{cli,query}/test/health-tail.test.ts` | `listProfileSnapshots`; shared health `matchesDateRange`; `profile list --from/--to` filter echo and day-range matching | completed | Narrow non-exclusive follow-up from the final audit for commit `73f250f` completed. Package-local query typecheck and `packages/query` tests passed; broader CLI-focused checks remain blocked by unrelated dirty `packages/cli/dist` and assistant automation red state. |
| Codex | Run the requested `test-coverage-audit` pass for the landed current-profile seam cleanup and add only narrow query-boundary regression proof if the duplicated snapshot-recency selectors lack direct coverage. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, targeted `packages/query/test/health-tail.test.ts` | `listProfileSnapshots`, `readCurrentProfile`, current-profile snapshot recency tie-break invariants | completed | Narrow non-exclusive audit lane completed with one focused query-boundary regression test covering equal-timestamp snapshot ordering plus stale current-profile fallback alignment. |
| Codex | Simplify the Ink assistant chat runtime by extracting an explicit controller/turn-state seam without changing transcript UX, queue replay, pause/recovery, or provider semantics. | `agent-docs/exec-plans/active/{2026-03-28-assistant-ink-runtime-simplify.md,COORDINATION_LEDGER.md}`, `packages/cli/src/assistant/ui/ink.ts`, targeted `packages/cli/test/assistant-runtime.test.ts` | `runAssistantChatWithInk`, embedded `App`, turn lifecycle helpers, queued follow-up state, busy/status suppression, model-switch orchestration | in_progress | Exclusive on `packages/cli/src/assistant/ui/ink.ts` while the controller extraction is in flight because this is a delicate cross-cutting refactor inside one large file. Preserve existing assistant runtime behavior and keep any test additions narrow and behavior-anchored. |
| Codex | Make `@murph/assistant-services` the explicit hosted assistant package boundary so `@murph/assistant-runtime` stops importing `murph/*` directly, while preserving current CLI compatibility shims and hosted/local trust boundaries. | `agent-docs/exec-plans/active/{2026-03-28-assistant-services-boundary.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `packages/{assistant-services/**,assistant-runtime/**}`, targeted `packages/cli/src/index.ts` only if needed for truthful re-export notes, `apps/cloudflare/test/node-runner.test.ts` | hosted assistant service boundary; `assistant-runtime -> assistant-services` import ownership; hosted-safe self-target helper; preserve current CLI subpath compatibility while narrowing hosted imports | in_progress | Narrow but cross-cutting boundary lane. Overlaps already-dirty hosted runtime files and the active hosted-runtime seam row; read live state first, preserve adjacent edits, and keep this focused on package ownership/import shape rather than changing hosted execution ordering or Cloudflare runner behavior. |
| Codex | Run the required `test-coverage-audit` pass for the active assistant-services boundary change, adding only narrow boundary regression tests if the new package seam lacks direct proof. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, targeted `packages/assistant-runtime/test/assistant-services-boundary.test.ts`, and `packages/assistant-runtime/vitest.config.ts` only if the existing harness cannot load the new boundary imports | direct `@murph/assistant-services` boundary proof, especially package-root/subpath exports and hosted operator-config persistence semantics | in_progress | Narrow audit lane on top of the active assistant-services boundary work. Do not touch production behavior; preserve adjacent runtime/boundary edits and keep any harness changes limited to what is required to execute the new test. |
| Codex | Decompose the hosted assistant runtime into smaller internal seams without changing hosted ordering, idempotency, explicit bootstrap rules, or resume behavior. | `agent-docs/exec-plans/active/{2026-03-28-hosted-runtime-seams.md,COORDINATION_LEDGER.md}`, `packages/assistant-runtime/src/{hosted-runtime.ts,hosted-runtime/**}`, targeted `packages/assistant-runtime/test/*.test.ts`, targeted `apps/cloudflare/test/node-runner.test.ts`, `packages/assistant-runtime/README.md` only if the seam map needs a truthful note | preserve explicit `member.activated` bootstrap, pre-commit side-effect collection, post-commit replay/finalize ordering, and resume-without-recompute semantics while thinning the top-level orchestrator | completed | Completed with the hosted-runtime split landed, focused hosted-runtime plus Cloudflare verification green, and repo-wide red state isolated to unrelated `packages/cli` build/test failures. Preserve adjacent hosted runtime edits if this lane is reopened. |
| Codex | Remove Paddle from the active PDF parser path and add raw-PDF model fallback for inbox routing when parsed PDF text is unavailable. | `agent-docs/exec-plans/active/{2026-03-28-pdf-routing-fallback.md,COORDINATION_LEDGER.md}`, `packages/parsers/src/adapters/paddleocr.ts`, `packages/cli/src/{model-harness.ts,inbox-model-harness.ts}`, targeted `packages/{parsers,cli}/test/*.test.ts`, `packages/parsers/README.md` | Paddle no longer claims PDF inputs; inbox routing builds PDF `file` parts only for parse-failed/no-text PDFs; unsupported rich-input retries fall back to text-only | completed | Completed in the shared cleanup batch. `pdftotext` remains the active first-pass PDF parser, raw PDFs only flow to supported models when parsed text is unavailable, and direct image-routing behavior stayed unchanged. |
| Codex | Hard-cut remaining greenfield-only compatibility shims across assistant state/outbox/cron plus canonical core/importer/query health entity paths. | `agent-docs/exec-plans/active/{2026-03-28-greenfield-compat-hard-cut.md,COORDINATION_LEDGER.md}`, `packages/cli/src/{assistant/outbox.ts,assistant/store/persistence.ts,assistant/cron.ts}`, targeted `packages/cli/test/{assistant-observability,assistant-state,assistant-cron}.test.ts`, `packages/core/src/{domains/experiments.ts,public-mutations.ts,storage-spine.ts,family/{types.ts,api.ts},genetics/{types.ts,api.ts},history/{types.ts,api.ts}}`, targeted `packages/core/test/{core,canonical-mutations-boundary,health-history-family}.test.ts`, `packages/importers/src/{shared.ts,core-port.ts,create-importers.ts}`, targeted `packages/importers/test/importers.test.ts`, `packages/contracts/src/health-entities.ts`, `packages/query/src/health/registries.ts`, targeted `packages/query/test/query.test.ts` | remove assistant legacy record bypass/defaulting, strict experiment status validation, canonical `importMeal` + `vaultRoot` importer/core seams, canonical family/genetics/history field names | completed | Scoped cleanup shipped with focused verification green. Full `pnpm test` / `pnpm test:coverage` still hit unrelated repo-baseline failures outside this lane; see the task plan for the recorded list. |
| Codex | Make the public site and both web app shells explicitly state that Murph is open source under GPL 3.0 and link to the GitHub repo. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/app/{layout,page}.tsx`, `apps/web/test/page.test.ts`, `packages/web/app/layout.tsx`, `packages/web/test/{layout,page}.test.ts` | shared open-source/GPL/GitHub footer copy across hosted and local web shells | in_progress | Narrow non-exclusive UI/copy lane. Preserve adjacent hosted onboarding and observability hierarchy, keep `packages/web` operator-first, and avoid unrelated settings/onboarding/control-plane behavior changes. |
| Codex | Hard-cut the remaining assistant config/session legacy compatibility so CLI state only supports provider-scoped defaults and providerState. | `agent-docs/exec-plans/active/{2026-03-28-assistant-legacy-hard-cut.md,COORDINATION_LEDGER.md}`, `packages/cli/src/{operator-config.ts,assistant/service.ts,assistant/ui/ink.ts,assistant/store/persistence.ts}`, targeted `packages/cli/test/{assistant-cli,assistant-provider,assistant-runtime,assistant-service,setup-cli}.test.ts` | remove flat `assistant.*` persisted/runtime defaults projection; require `assistant.defaultsByProvider`; require session `providerState`; update selected-provider helper callers/tests | in_progress | Narrow but overlapping CLI lane. Read live state first in `operator-config.ts` and Ink UI files because adjacent assistant/provider cleanup work is active; keep this scoped to removing compatibility-only assistant config/session shapes and matching tests. |
| Codex | Finish the hosted web artifact-ownership follow-up so source-bundle uploads include the shared config helper, `apps/web` dev smoke gets its own Next dist dir, scanner/ignore rules treat all hosted Next artifacts consistently, Vitest no longer boots through `next.config`, and clean typecheck no longer depends on leftover route stubs. | `agent-docs/exec-plans/active/{2026-03-28-apps-web-next-followup.md,COORDINATION_LEDGER.md}`, `config/workspace-source-resolution.ts`, `apps/web/{next.config.ts,package.json,README.md,scripts/dev-smoke.ts,test/*.test.ts}`, `packages/web/{next.config.ts,vitest.config.ts,test/*.test.ts,package.json}`, `scripts/{repo-tools.config.sh,verify-workspace-boundaries.mjs,check-no-js.ts,ensure-next-route-type-stubs.ts}`, root `{package.json,.gitignore}`, and directly required docs | hosted web smoke dist ownership; audit-bundle config inclusion; `.next-dev`/`.next-smoke` hygiene parity; direct helper-based Vitest aliases; route-type stub bootstrap | completed | Completed as a narrow non-exclusive tooling lane. Focused `packages/web` and `apps/web` config tests passed, `pnpm zip:src` now includes `config/workspace-source-resolution.ts` at ~2.5 MB, and a direct concurrent run kept interactive `apps/web dev` alive while `apps/web dev:smoke` created `.next-smoke` separately. Repo-wide wrappers still fail only in unrelated dirty-tree hosted-execution/onboarding and assistant-runtime files. |
| Codex | Finish the remaining hosted execution cleanup across shared contracts, Cloudflare bundle/DO seams, async outbox lifecycle, share-by-reference acceptance, and Workers-runtime coverage. | `agent-docs/exec-plans/active/{2026-03-28-hosted-execution-cleanup.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `agent-docs/{index.md,references/testing-ci-map.md,operations/verification-and-runtime.md}`, `packages/{hosted-execution/**,assistant-runtime/**,assistant-services/**}`, `apps/web/{app/api/**,src/lib/{hosted-execution/**,hosted-share/**,device-sync/**,hosted-onboarding/**},test/**}`, `apps/cloudflare/{src/**,test/**,package.json,vitest*.ts,README.md,DEPLOY.md}` | hosted execution contract parity; immutable bundle refs; event-level outbox outcomes; async share acceptance by reference; DO RPC cleanup; Cloudflare 400 mapping; async drain path; Workers-runtime seams | completed | Completed as a broad but non-exclusive hosted-execution lane. Final follow-up removed dead async-drain helper surface, deleted a no-op webhook receipt hook, made hosted-share preview fallback lazy, and collapsed the redundant `dispatchWithOutcome()` bootstrap path. Repo-wide wrapper failures remain outside this lane in unrelated CLI/core typecheck work. |
| Codex | Ignore hosted `apps/web/.next-dev` artifacts in both git hygiene and audit bundle packaging so local Next dev output stops inflating `pnpm zip:src`. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `.gitignore`, `scripts/repo-tools.config.sh` | `.next-dev` ignore/prune config only | completed | Narrow non-exclusive config lane completed. `pnpm zip:src` dropped back to 2.5 MB; remaining repo wrapper failures were unrelated dirty-tree/runtime issues in `apps/web` dev smoke and `apps/cloudflare` coverage tests. |
| Codex | Implement and integrate the repo-wide Murph -> Murph rename directly in the shared tree, covering package identity, CLI/bin surface, runtime-path migration, persisted ids, hosted external names, live docs/UI copy, test/generated cleanup, and historical exceptions without rewriting immutable completed plans. | `agent-docs/exec-plans/active/{2026-03-27-murph-rename-migration.md,COORDINATION_LEDGER.md}`, `CONTINUITY_murph-rename-migration.md`, root/package/app/docs files touched by the Murph migration across scopes A-H | Murph rename integration; compatibility policy; shared-tree execution after failed worker fanout; final verification and commit | in_progress | Broad non-exclusive direct-implementation lane after the shared-tree worker fanout produced duplicate ledger noise instead of safe scope diffs. Preserve unrelated dirty edits, keep completed plans untouched, and keep external cookie/header/env names neutral where possible while dual-reading legacy operator-home paths and durable ids where required. |
| Codex | Implement the hosted `apps/web` email-settings patch so active hosted members can add or update a Privy-verified email address from `/settings`. | `agent-docs/exec-plans/active/{2026-03-27-hosted-email-settings.md,COORDINATION_LEDGER.md}`, `apps/web/{.env.example,README.md,package.json,app/settings/page.tsx,src/components/{hosted-onboarding/join-invite-client.tsx,settings/hosted-email-settings.tsx},src/lib/hosted-onboarding/privy-shared.ts,test/{hosted-onboarding-privy-shared.test.ts,settings-page.test.ts}}` | hosted email settings page/component; Privy email linked-account parsing; active invite success settings link | in_progress | Narrow non-exclusive `apps/web` lane. Overlaps already-dirty hosted onboarding surfaces in `join-invite-client.tsx` and shared Privy helpers; read live file state first, preserve adjacent onboarding/billing edits, and keep this scoped to email settings rather than broader hosted onboarding refactors. |
| Codex | Extend hosted email settings into the hosted execution bridge so a Privy-verified email can sync into hosted user env and reconcile onto the assistant email self-target. | `agent-docs/exec-plans/active/{2026-03-27-hosted-email-privy-cloudflare-sync.md,COORDINATION_LEDGER.md}`, `apps/web/{.env.example,README.md,app/api/settings/email/sync/route.ts,src/components/settings/hosted-email-settings.tsx,src/lib/{hosted-execution/control.ts,hosted-onboarding/privy-shared.ts},test/{hosted-execution-control.test.ts,hosted-onboarding-privy-shared.test.ts,settings-email-sync-route.test.ts}}`, `packages/{hosted-execution/src/env.ts,hosted-execution/test/hosted-execution.test.ts,runtime-state/src/{hosted-user-env.ts,index.ts},runtime-state/test/hosted-user-env.test.ts,assistant-runtime/src/{hosted-email-route.ts,hosted-runtime.ts,index.ts},assistant-runtime/test/hosted-email-route.test.ts,cli/src/index.ts}` | hosted verified-email sync route; hosted execution control helper; hosted verified-email env projection; assistant email self-target reconciliation | completed | Narrow non-exclusive cross-package lane completed with focused hosted verification green and one finish-review follow-up to align stale hosted-execution test expectations with the current parser message. Preserve adjacent hosted-email and hosted bootstrap/user-env edits if this lane is reopened. |
| Codex | Finish the hosted onboarding RevNet hardening pass so checkout wallets only come from trusted server state, subscription activation waits for `invoice.paid`, and invoice issuance submission stays non-blocking/idempotent. | `agent-docs/exec-plans/active/{2026-03-27-hosted-onboarding-revnet-hardening.md,COORDINATION_LEDGER.md}`, `apps/web/{app/api/hosted-onboarding/billing/checkout/route.ts,src/lib/hosted-onboarding/{billing-service.ts,privy.ts,revnet.ts,webhook-service.ts},test/{hosted-onboarding-routes.test.ts,hosted-onboarding-billing-service.test.ts,hosted-onboarding-webhook-idempotency.test.ts}}` | hosted checkout wallet trust boundary; RevNet subscription activation timing; immutable/stuck hosted RevNet issuance handling; legacy invoice subscription fallback | completed | Completed with focused hosted-onboarding checks green, tighter Stripe metadata/duplicate-invoice coverage, and root wrapper failures still isolated to unrelated active `.ts`-extension / contracts script lanes. Preserve adjacent hosted onboarding receipt/refactor edits if this lane is reopened. |
| Codex | Restore a working default local dev path for `apps/web` after Turbopack started failing to materialize `.next/dev` manifests and turning healthy routes into local 500s. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/package.json` | `apps/web` `dev` script default runtime only | completed | Completed as a narrow local-dev fix: the verified default path for `pnpm dev` is now plain `next dev`, while later artifact-ownership follow-up work split interactive output into `.next-dev` and smoke output into `.next-smoke` so local `/`, `/api/device-sync`, and `/api/hosted-onboarding/linq/webhook` stay reachable without shared-cache deletion. |
| Codex | Close the remaining hosted “production advice” gaps by making generic dispatch URLs first-class, tightening the generic hosted side-effect story, and adding Cloudflare image cleanup ops. | `agent-docs/exec-plans/active/{2026-03-27-production-advice-followup.md,COORDINATION_LEDGER.md}`, `packages/hosted-execution/src/env.ts`, `apps/web/{README.md,src/lib/hosted-execution/dispatch.ts,test/hosted-execution-dispatch.test.ts}`, `packages/assistant-runtime/src/{contracts.ts,hosted-runtime.ts}`, `apps/cloudflare/{package.json,README.md,DEPLOY.md,src/deploy-automation.ts,scripts/cleanup-container-images.ts,test/deploy-automation.test.ts}`, `docs/cloudflare-hosted-idempotency-followup.md`, and directly required architecture/runtime/testing docs only if the documented contract meaning changes | generic-first hosted dispatch env resolution; generic hosted side-effect contract wording/helpers; Cloudflare image cleanup command/rendering | completed | Completed with generic dispatch env precedence, generic-framed hosted side-effect helpers/docs, and an explicit Cloudflare image cleanup script/docs. Focused tests passed; repo-wide wrappers remain blocked by unrelated active `packages/cli` runtime-state import/type errors and unrelated `apps/cloudflare/test/user-runner.test.ts` next-wake failures in the dirty tree. |
| Codex | Clarify when cron creation should opt into assistant state so CLI help explains `--state` / `--stateDoc` instead of leaving the choice implicit. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/commands/assistant.ts`, `packages/cli/test/incur-smoke.test.ts` | assistant cron add/install help metadata; assistant cron state option descriptions | in_progress | Narrow non-exclusive follow-up on the landed assistant-state surface. Keep this to command metadata/help text plus a focused help/schema regression; do not widen back into docs or runtime behavior. |
| Codex | Salvage the failed assistant normalization worker and close the scheduled-updates worker's missing audit-pass gap without widening either cleanup scope. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/{assistant/conversation-ref.ts,assistant/store.ts,assistant/store/paths.ts,assistant/failover.ts,outbound-channel.ts,assistant/cron/presets.ts,setup-services/scheduled-updates.ts,setup-services.ts}`, `packages/cli/test/{assistant-provider.test.ts,assistant-robustness.test.ts,assistant-state.test.ts,setup-cli.test.ts,assistant-cron.test.ts}` | conversation locator canonicalization salvage; failover hash normalization proof; scheduled-updates audit recovery | completed | Salvaged locally after the original merged assistant worker hit a usage cap. Focused assistant and scheduled-updates verification is green; one follow-up audit recheck also hit the usage cap, so the final assistant close-out relies on the added participant/null-path regressions plus local review rather than a second subagent response. |
| Codex | Split the giant hosted `apps/web` service modules by business boundary while preserving the current route-facing public APIs. | `agent-docs/exec-plans/active/{2026-03-27-hosted-service-boundary-split.md,COORDINATION_LEDGER.md}`, `apps/web/src/lib/{hosted-onboarding/**,hosted-share/**,device-sync/**}`, targeted `apps/web/test/{hosted-onboarding-*.test.ts,hosted-share-service.test.ts,device-sync-hosted-wake-dispatch.test.ts}` | hosted onboarding service façade split; hosted share acceptance/import service split; device-sync wake service extraction | completed | Non-exclusive `apps/web` refactor lane. Completed with the façade splits landed and focused `apps/web` verification green; root wrappers still fail in unrelated `packages/contracts` and `apps/cloudflare` work, and the first mandatory audit worker hit the current usage limit before the audit pass sequence could finish. |
| Codex | Harden the Cloudflare hosted-execution lane with Workers-runtime coverage, explicit versions/deployments rollout automation, and Wrangler observability config. | `agent-docs/exec-plans/active/{2026-03-27-cloudflare-lane-hardening.md,COORDINATION_LEDGER.md}`, `.github/workflows/deploy-cloudflare-hosted.yml`, `apps/cloudflare/{package.json,vitest*.ts,wrangler.jsonc,README.md,DEPLOY.md,src/deploy-automation.ts,scripts/{render-deploy-config.ts,smoke-hosted-deploy.ts},test/**}`, `agent-docs/{index.md,references/testing-ci-map.md,operations/verification-and-runtime.md}` | Workers-runtime Vitest split; Cloudflare versions upload/deploy rollout wiring; Worker observability config; deployment/trust-boundary docs | in_progress | Narrow non-exclusive lane. Overlaps active dirty `apps/cloudflare` deploy/doc surfaces and the shared hosted-execution package lane on `package.json`/`vitest` edges; read the live file state first, preserve adjacent runner/package extraction edits, and keep this scoped to runtime coverage, rollout automation, and observability. |
| Codex | Make hosted bootstrap explicit and move hosted per-user env overrides out of the broader `agent-state` bundle without disturbing the in-flight DO state-machine refactor. | `agent-docs/exec-plans/active/{2026-03-27-hosted-bootstrap-user-env-boundary.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `agent-docs/{index.md,operations/verification-and-runtime.md,references/testing-ci-map.md}`, `apps/cloudflare/{README.md,src/{bundle-store.ts,node-runner.ts,user-env.ts,user-runner.ts,user-runner/runner-bundle-sync.ts},test/{node-runner.test.ts,user-runner.test.ts}}`, `packages/assistant-runtime/src/hosted-runtime.ts`, `packages/runtime-state/{README.md,src/hosted-bundles.ts,test/hosted-bundle.test.ts}` | explicit hosted bootstrap event handling; separate encrypted hosted user-env object; runner-time user-env injection without agent-state bundle churn | in_progress | Non-exclusive Cloudflare/runtime lane, but it overlaps the active exclusive `apps/cloudflare/src/user-runner.ts` state-machine migration. Preserve the current queue/store split and adjacent routing/container edits; keep this scoped to bootstrap semantics plus user-env storage/injection boundaries. |
| Codex | Route all `apps/web` hosted-execution dispatch through one durable Postgres outbox so onboarding, hosted-share, and device-sync enqueue in the same control-plane transaction and read eventual execution state instead of dispatching inline. | `agent-docs/exec-plans/{active/COORDINATION_LEDGER.md,completed/2026-03-27-hosted-execution-outbox.md}`, `ARCHITECTURE.md`, `agent-docs/index.md`, `agent-docs/references/testing-ci-map.md`, `agent-docs/operations/verification-and-runtime.md`, `apps/web/{README.md,prisma/schema.prisma,prisma/migrations/**,src/lib/{hosted-execution/**,hosted-share/service.ts,hosted-onboarding/service.ts,device-sync/control-plane.ts,device-sync/prisma-store.ts},test/**}`, targeted `apps/cloudflare/**` docs/tests only if the read/status contract needs matching updates | hosted execution outbox schema/dispatcher symbols; enqueue-in-transaction helpers; eventual-status reconciliation for hosted share; unified retry semantics for onboarding/share/device-sync | completed | Non-exclusive lane. Landed with focused `apps/web` typecheck plus hosted-share/device-sync/onboarding tests green; repo `pnpm typecheck`, `pnpm test`, and `pnpm test:coverage` still fail in unrelated dirty `apps/cloudflare` work, and both attempts to run the required simplify/coverage/final-review audit subagents were blocked by current usage-limit failures. |
| Codex | Extract a headless `@murph/assistant-runtime` package for hosted execution, move Cloudflare onto it, and remove the current typed-hostile/env-mutation seams. | `agent-docs/exec-plans/active/{2026-03-27-assistant-runtime-extraction.md,COORDINATION_LEDGER.md}`, `CONTINUITY_assistant-runtime-extraction.md`, `packages/assistant-runtime/**`, `apps/cloudflare/{src/**,test/**,package.json,tsconfig.json,vitest.config.ts,README.md}`, `packages/cli/src/index.ts`, root `{package.json,tsconfig.json}`, and directly affected docs only | hosted assistant runtime package boundary; explicit runtime context/env; isolated hosted job execution; Cloudflare runtime import cutover | completed | Landed with focused package/runtime verification green. Repo wrappers remain blocked by unrelated `packages/hosted-execution` typing failures and the tracked `apps/web/postcss.config.mjs` hygiene issue. Preserve adjacent hosted outbox/router/env work if this lane is reopened, and avoid broad assistant-command/UI rewrites. |
| Codex | Fix the hosted root-page import drift so local/dev landing no longer crashes when Privy envs are present. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/app/page.tsx`, `apps/web/test/page.test.ts` | hosted landing Privy readiness import; hosted root-page regression proof | completed | Narrow non-exclusive bug-fix lane. Landed as a minimal import-path fix plus a page-level regression test; remaining hosted signup readiness issues are broader Privy/env drift outside this narrow crash fix. |
| Codex | Orchestrate a new codex-workers simplification batch for nine user-requested prompts, merging the overlapping assistant normalization prompts into one worker lane and launching the batch in the shared current worktree. | `agent-docs/exec-plans/active/{2026-03-27-nine-cleanup-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-nine-cleanup-batch/**` | batch prompt shaping; overlap-aware lane split; codex-workers launch orchestration | completed | Batch finished. Prompts 1 and 2 were merged into one assistant lane because both needed `packages/cli/src/assistant/store/paths.ts`; that lane later required the local salvage recorded above after the worker hit a usage cap. |
| codex-worker-assistant-session-normalization | Simplify duplicated assistant provider-option normalization and conversation/session locator normalization without changing absent-field or binding semantics. | `agent-docs/exec-plans/active/{2026-03-27-nine-cleanup-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-nine-cleanup-batch/prompt-1-2-assistant-session-normalization.md`, `packages/cli/src/{chat-provider.ts,outbound-channel.ts,assistant/store.ts,assistant/failover.ts,assistant/store/paths.ts,assistant/conversation-ref.ts,assistant-cli-contracts.ts}`, `packages/cli/test/{assistant-provider.test.ts,assistant-runtime.test.ts,assistant-service.test.ts,assistant-robustness.test.ts,assistant-state.test.ts,assistant-cli.test.ts}` | provider-option raw-field normalization; runtime-vs-persisted adapters; conversation-ref canonicalization from locator inputs; binding patch presence semantics | completed | Original worker exited on a usage-cap error. The code landed via local salvage: locator normalization now tracks explicit field presence, the delivery seam preserves absent-vs-undefined behavior, thread/participant delivery targets stay aligned on binding updates, and focused assistant verification passed. |
| codex-worker-scheduled-updates-deferral | Simplify the stale onboarding scheduled-updates recommendation path without changing its deferred-install behavior. | `agent-docs/exec-plans/active/{2026-03-27-nine-cleanup-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-nine-cleanup-batch/prompt-3-scheduled-updates-deferral.md`, `packages/cli/src/{setup-services.ts,setup-services/scheduled-updates.ts,assistant/cron/presets.ts,setup-cli-contracts.ts}`, `packages/cli/test/{setup-cli.test.ts,assistant-cron.test.ts,assistant-cli.test.ts}` | deferred scheduled-update recommendations; preset projection reuse; installer-vs-recommendation naming cleanup | completed | Worker code landed cleanly, and the local salvage closed the missing audit gap with targeted onboarding regressions for unknown presets, deferred recommendation shape, dry-run wording, and install-later guidance. |
| codex-worker-device-sync-control-client | Deduplicate the device-sync control-plane client seam shared by CLI and local web without changing edge-specific error shaping. | `agent-docs/exec-plans/active/{2026-03-27-nine-cleanup-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-nine-cleanup-batch/prompt-6-device-sync-control-client.md`, `packages/cli/src/device-sync-client.ts`, `packages/web/src/lib/device-sync.ts`, `packages/runtime-state/src/device-sync.ts`, `packages/cli/test/device-sync-client.test.ts`, `packages/web/test/{device-sync-lib.test.ts,device-sync-routes.test.ts,page.test.ts}` | shared device-sync control-plane resolution; loopback-token request seam; edge-specific error mapping boundaries | in_progress | Non-exclusive lane with overlap on the active `device-syncd` control-plane hardening row. Preserve the current caller-facing messages and keep the shared extraction minimal. |
| Codex | Refactor the hosted per-user Durable Object into a SQLite-backed runner state machine with direct DO RPC methods and no request-time `blockConcurrencyWhile()` around R2 I/O. | `agent-docs/exec-plans/active/{2026-03-27-hosted-runner-state-machine-refactor.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/src/{index.ts,user-runner.ts,user-runner/**}`, `apps/cloudflare/test/{index.test.ts,user-runner.test.ts,sql-storage.ts}`, and directly required hosted docs only if the control-flow contract changes | runner queue-store state machine; DO RPC entrypoints (`dispatch`, `commit`, `finalizeCommit`, `status`, `updateUserEnv`); R2 bundle sync outside SQLite critical sections | completed | Landed with focused `apps/cloudflare` route and runner suites green. Repo-wide wrappers remain blocked by unrelated pre-existing failures in `packages/runtime-state/src/device-sync.ts`, `apps/web/test/hosted-share-service.test.ts`, and the `apps/web/postcss.config.mjs` source-artifact guard. |
| Codex | Migrate hosted runner launch from the low-level `ctx.container` lifecycle to a companion `RunnerContainer extends Container` binding while preserving the current per-user queue/state semantics. | `agent-docs/exec-plans/active/{2026-03-27-cloudflare-container-class-migration.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/{package.json,wrangler.jsonc,README.md,DEPLOY.md,src/{index.ts,user-runner.ts,runner-container.ts,deploy-automation.ts},test/{index.test.ts,user-runner.test.ts,deploy-automation.test.ts}}` | companion `RunnerContainer` DO/container class; stub-based runner invoke/destroy handoff; container-class readiness/env injection; Wrangler binding split | completed | Completed in the shared cleanup batch. The runner/container binding split landed without widening the queue state machine, and the current Cloudflare lane now reflects the `Container`-class lifecycle shape. |
| Codex | Make repo routing explicit that required completion-workflow audit subagents should run without waiting for a user to request delegation, then execute the required audit passes for the hosted webhook idempotency change and land the audit-driven webhook follow-up fixes. | `AGENTS.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/{src/lib/hosted-onboarding/service.ts,test/hosted-onboarding-webhook-idempotency.test.ts}` | required audit-subagent routing; explicit no-user-prompt completion-workflow note; explicit webhook drain-error wrapper; Linq invite-reply retry/reclaim regressions | completed | Completed with spawned `simplify`, `test-coverage-audit`, and `task-finish-review` passes. Focused webhook suite passed. Full repo checks remain blocked by unrelated dirty-tree failures in `packages/contracts/scripts/*`, `packages/assistant-runtime/src/hosted-runtime-child.ts`, and active `apps/web` Privy test drift. |
| codex-worker-hosted-webhook-receipts | Untangle hosted webhook receipt state from raw payload JSON by introducing one typed receipt-state codec around the existing Prisma schema. | `agent-docs/exec-plans/active/{2026-03-27-code-quality-cleanups-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-code-quality-cleanups/prompt-2-hosted-webhook-receipts.md`, `apps/web/src/lib/hosted-onboarding/service.ts`, `apps/web/test/hosted-onboarding-webhook-idempotency.test.ts` | hosted webhook receipt state codec; retry/idempotency state transitions; payload/state naming cleanup | in_progress | Non-exclusive lane but overlaps active hosted-onboarding work. Read current file state first, preserve unrelated hosted onboarding edits, and keep the refactor behavior-preserving. |
| Codex | Generalize hosted onboarding webhook idempotency with a receipt-local side-effect outbox for retry-safe Cloudflare dispatches and Linq invite sends. | `agent-docs/exec-plans/active/{2026-03-27-cloudflare-hosted-idempotency-followup.md,COORDINATION_LEDGER.md}`, `apps/web/{README.md,src/lib/hosted-onboarding/service.ts,test/hosted-onboarding-webhook-idempotency.test.ts}`, `docs/cloudflare-hosted-idempotency-followup.md`, `ARCHITECTURE.md`, `agent-docs/index.md`, and any directly required hosted docs/tests only if needed | hosted webhook receipt side-effect state; deterministic hosted member-activation dispatch ids; retry-safe hosted onboarding side-effect drain | completed | Focused hosted onboarding typecheck plus webhook idempotency coverage passed. Repo-wide `pnpm test` / `pnpm test:coverage` remain blocked by unrelated active `apps/web` landing/Privy export drift (`hasHostedPrivyPhoneAuthConfig`, `parseHostedPrivyIdentityToken`) outside this lane. |
| codex-worker-provider-config | Centralize assistant provider-option normalization, merge precedence, serialization, and equality checks behind one canonical internal config layer. | `agent-docs/exec-plans/active/{2026-03-27-code-quality-cleanups-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-code-quality-cleanups/prompt-3-provider-config.md`, `packages/cli/src/{chat-provider.ts,assistant/service.ts,assistant/failover.ts,assistant-cli-contracts.ts,setup-services.ts,assistant/provider-config.ts,assistant/store/paths.ts}`, `packages/cli/test/{assistant-runtime.test.ts,assistant-service.test.ts,assistant-robustness.test.ts,assistant-state.test.ts}` | provider-config normalization; merge precedence; storage/session serialization; config equality | in_progress | Non-exclusive lane with direct overlap on already-dirty `packages/cli/src/assistant/service.ts`. `assistant/store/paths.ts` is included only as the minimal session-creation dependency for the shared provider-config layer. Preserve unrelated edits and keep ownership limited to provider-config plumbing plus direct tests. |
| codex-worker-codex-event-normalization | Add one normalized Codex event layer so progress and trace extraction share the same raw-event interpretation. | `agent-docs/exec-plans/active/{2026-03-27-code-quality-cleanups-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-code-quality-cleanups/prompt-4-codex-event-normalization.md`, `packages/cli/src/assistant-codex.ts`, `packages/cli/test/assistant-codex.test.ts` | normalized Codex event IR; shared progress/trace projections; regression proof that both projections agree on one raw payload | in_progress | Non-exclusive lane. Preserve adjacent assistant runtime edits and keep the change tightly scoped to event parsing plus direct tests. |
| codex-worker-canonical-write-guard | Tighten canonical write-guard decoding so corrupted stored metadata/payloads surface intentional guard failures instead of being silently skipped. | `agent-docs/exec-plans/active/{2026-03-27-code-quality-cleanups-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-code-quality-cleanups/prompt-5-canonical-write-guard.md`, `packages/cli/src/assistant/canonical-write-guard.ts`, `packages/core/src/operations/write-batch.ts`, `packages/cli/test/assistant-service.test.ts` | stored write-operation metadata decode; committed payload validation; guard failure context for malformed audit state | in_progress | Non-exclusive lane with direct overlap on already-dirty `packages/core/src/operations/write-batch.ts`. Preserve unrelated edits in that file and keep the scope limited to write-guard decode semantics plus direct tests. |
| codex-worker-cloudflare-router | Replace the Cloudflare worker and durable-object route if-ladders with declarative route tables and truthful parse/forward helper types. | `agent-docs/exec-plans/active/{2026-03-27-code-quality-cleanups-worker-batch.md,COORDINATION_LEDGER.md}`, `agent-docs/exec-plans/active/worker-prompts/2026-03-27-code-quality-cleanups/prompt-6-cloudflare-router.md`, `apps/cloudflare/src/index.ts`, `apps/cloudflare/test/index.test.ts` | worker route table; durable-object route table; shared auth/method helpers; control-request parse/forward cleanup | in_progress | Non-exclusive lane. Preserve adjacent hosted runner/outbox/env edits and keep the scope limited to route dispatch structure plus direct tests. |
| Codex | Replace the Cloudflare hosted runner's public worker callback loop with direct internal outbound Worker handlers from the native container, while preserving DO commit/finalize locking and hosted outbox reconciliation. | `agent-docs/exec-plans/active/{2026-03-27-cloudflare-outbound-worker-handlers.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/{src/{index,env,execution-journal,node-runner,runner-container,user-runner,deploy-automation}.ts,test/{deploy-automation,env,index,node-runner,user-runner}.test.ts,README.md,DEPLOY.md,wrangler.jsonc}`, `ARCHITECTURE.md`, `agent-docs/{index.md,operations/verification-and-runtime.md,references/testing-ci-map.md}` | internal outbound Worker bridge; remove public runner callback routes/base-url env; preserve durable commit/finalize/outbox semantics | completed | Completed with `pnpm --dir apps/cloudflare test`, `pnpm --dir apps/cloudflare test:workers`, and `pnpm typecheck` passing. Repo-wide `pnpm test` and `pnpm test:coverage` still fail in unrelated `packages/cli` suites. Required delegated audits ran: simplify and coverage found no actionable Cloudflare issues; finish-review flagged a missing smoke-helper companion file, which was fixed in this lane. |
| Codex | Hard-cut the remaining branded `HB_*` error/status-code namespace to unbranded codes and remove any dead compatibility leftovers exposed by that rename. | `agent-docs/exec-plans/active/{2026-03-27-error-code-hard-cutover.md,COORDINATION_LEDGER.md}`, `packages/contracts/{src/constants.ts,generated/**}`, `packages/core/src/**`, `packages/core/test/**`, `packages/cli/src/**`, `packages/cli/test/**`, and any directly affected docs/tests only if needed | rename canonical `HB_*` error/status codes at the source contract layer, update downstream `VaultError`/validation issue emitters and assertions, prune dead branded leftovers discovered during the cutover | in_progress | Non-exclusive but cross-cutting lane. Preserve adjacent CLI/core feature work, keep this scoped to the error/status-code contract surface plus directly required generated artifacts/tests, and do not reopen the already-completed env/runtime prefix rename beyond removing genuinely dead leftovers. |
| Codex | Audit env/runtime prefix hard-cutover coverage and add only minimal proof tests for uncovered rename/removal behavior. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/test/user-env.test.ts`, `apps/web/test/linq-control-plane.test.ts` | prove `HOSTED_USER_` allowlist behavior; prove removed branded Linq webhook-secret alias is ignored at the hosted webhook boundary | completed | Non-exclusive audit lane. Kept edits strictly to env/runtime prefix proof gaps for the hard-cutover lane without altering adjacent native-container, outbox, or unrelated logic changes. |
| Codex | Hard-cut the remaining branded runtime/env prefixes off the active config surface without touching unrelated `HB_*` error/status codes. | `agent-docs/exec-plans/active/{2026-03-27-env-prefix-hard-cutover.md,COORDINATION_LEDGER.md}`, `Dockerfile.cloudflare-hosted-runner`, `.github/workflows/deploy-cloudflare-hosted.yml`, `apps/cloudflare/{DEPLOY.md,README.md,src/{deploy-automation,env,index,node-runner,user-env}.ts,test/{deploy-automation,env,node-runner}.test.ts}`, `apps/web/src/lib/{hosted-onboarding/env,hosted-onboarding/runtime,linq/env}.ts`, `apps/web/test/{hosted-onboarding-env,linq-control-plane}.test.ts`, `packages/cli/src/{assistant-cli-tools.ts,assistant/channel-adapters.ts,assistant/fault-injection.ts,inbox-app/bootstrap-doctor-strategies.ts,linq-runtime.ts,run-terminal-logging.ts,setup-cli.ts,setup-runtime-env.ts,setup-services/channels.ts}`, targeted CLI tests/docs as needed | remove `HB_HOSTED_BUNDLE_KEY` alias; rename hosted user prefix; drop `HEALTHYBOB_LINQ_*` aliases; rename hosted member/debug env vars; rename runner internal `HB_HOSTED_*` vars | in_progress | Non-exclusive lane. Keep the cutover limited to live runtime/config env names already identified in the repo, preserve overlapping Cloudflare/web/CLI work, and do not rename the broader `HB_*` validation/error code family in this pass. |
| Codex | Hard-cut `apps/cloudflare` over to native Durable Object + Container execution and remove the external runner-service fallback from runtime, deploy, and doc surfaces. | `agent-docs/exec-plans/active/{2026-03-27-cloudflare-native-container-hard-cutover.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/{src/**,test/**,README.md,DEPLOY.md,package.json,wrangler.jsonc,.dev.vars.example}`, `packages/runtime-state/src/hosted-execution.ts`, `README.md`, `ARCHITECTURE.md`, `agent-docs/{index.md,operations/verification-and-runtime.md,references/testing-ci-map.md}`, `.github/workflows/deploy-cloudflare-hosted.yml`, `Dockerfile.cloudflare-hosted-runner`, `.dockerignore` | native DO/container invocation; remove runner base-url and standalone/local runner surfaces; hosted next-wake hints; truthful container-only deploy/docs | completed | Non-exclusive lane. Landed with focused `pnpm --dir apps/cloudflare test` passing; repo-wide `pnpm typecheck`, `pnpm test`, and `pnpm test:coverage` remain blocked by unrelated active `apps/web` RevNet type errors (`viem` missing and `Invoice.payment_intent` typing mismatch). |
| Codex | Add the narrow hosted assistant outbox follow-up so hosted runs queue replies before durable commit, then reconcile and send pending assistant intents after commit without regressing local assistant behavior. | `agent-docs/exec-plans/active/{2026-03-27-hosted-assistant-outbox-followup.md,COORDINATION_LEDGER.md}`, `apps/cloudflare/src/{index.ts,node-runner.ts,runtime-adapter.ts,user-runner.ts}`, `apps/cloudflare/test/{index.test.ts,node-runner.test.ts,user-runner.test.ts}`, `packages/runtime-state/src/hosted-execution.ts`, `packages/cli/src/{assistant-runtime.ts}`, `packages/cli/src/assistant/{automation/run-loop.ts,outbox.ts,service.ts}`, targeted CLI tests/docs as needed | hosted queue-only assistant delivery; post-commit hosted outbox drain; hosted outbox delivery journal and final bundle reconciliation; preserve local assistant delivery semantics | in_progress | Non-exclusive lane. Preserve adjacent Cloudflare deploy/runtime edits and in-flight assistant runtime work, keep this scoped to hosted assistant replies only, and do not broaden into a generalized hosted side-effect framework. |
| Codex | Repair timezone/day-key handling across canonical writes, read/query surfaces, and assistant local scheduling so local-day semantics no longer depend on UTC slicing or host-local cron math. | `agent-docs/exec-plans/active/{2026-03-27-timezone-local-day-repair.md,COORDINATION_LEDGER.md}`, `packages/contracts/src/{time.ts,zod.ts,examples.ts,schemas.ts}`, generated contract schemas if regenerated, `packages/core/src/{constants.ts,time.ts,vault.ts,mutations.ts,domains/{events.ts,experiments.ts,journal.ts},history/api.ts,bank/shared.ts}`, `packages/inboxd/src/indexing/persist.ts`, `packages/importers/src/device-providers/oura.ts`, `packages/query/src/{canonical-entities.ts,model.ts,timeline.ts,search-shared.ts,search-sqlite.ts,export-pack-health.ts,health/current-profile-resolution.ts}`, `packages/cli/src/{assistant-cli-contracts.ts,assistant/{cron.ts,cron/schedule.ts,cron/presets.ts},commands/vault.ts,usecases/{food-autolog.ts,experiment-journal-vault.ts,integrated-services.ts},inbox-services/shared.ts}`, `packages/web/{src/lib/overview.ts,app/page.tsx}`, targeted tests/docs as needed | shared local-day/timezone helper symbols; optional record timezone metadata; vault/init timezone defaults; day-key write-path normalization; query/web stored-day grouping; timezone-aware daily-local assistant schedule | completed | Non-exclusive but cross-cutting lane. Landed with focused package tests passing, `pnpm typecheck` passing, and repo-wide `pnpm test` / `pnpm test:coverage` still blocked by the unrelated `apps/cloudflare/test/node-runner.test.ts` failure (`VaultError: Food was not found.`). |
| Codex | Replace the hosted app root stub with a clean public landing page plus optional public SMS CTA wiring that reuses the existing verified signup path without changing onboarding/auth semantics. | `agent-docs/exec-plans/active/{2026-03-27-hosted-public-landing-page.md,COORDINATION_LEDGER.md}`, `apps/web/{app/page.tsx,README.md,.env.example}`, `apps/web/src/lib/hosted-onboarding/landing.ts`, `apps/web/test/hosted-onboarding-landing.test.ts` | hosted public root copy/layout; optional public signup phone parsing and `sms:` CTA; preserve `/join/*`, passkey, billing, and Linq onboarding flow behavior | completed | Completed in the shared cleanup batch. The hosted root page now carries the intended public-facing copy while preserving `/join/*` as the verified signup path and avoiding broader onboarding/auth changes. |
| Codex | Add a runtime guard that rolls back unauthorized direct canonical vault file edits during vault-operator assistant turns while preserving audited CLI mutations. | `agent-docs/exec-plans/active/{2026-03-27-assistant-canonical-write-guard.md,COORDINATION_LEDGER.md}`, `packages/cli/src/assistant/{service,canonical-write-guard}.ts`, targeted `packages/cli/test/{assistant-service,assistant-runtime}.test.ts` | snapshot protected canonical text files, allow audited path changes, roll back unauthorized direct edits, emit assistant diagnostics/receipt details for guard violations | in_progress | Non-exclusive lane. Preserve adjacent in-flight assistant provider/runtime edits, keep enforcement scoped to canonical vault data under the active vault, and do not broaden into repo-code write policy or unrelated assistant failover behavior. |
| Codex | Diagnose the transient vault metadata validation failure, stop inbox envelope captures from being falsely flagged as missing raw manifests, and harden vault-operator guidance against direct canonical file edits. | `agent-docs/exec-plans/active/{2026-03-27-vault-validation-and-metadata-hardening.md,COORDINATION_LEDGER.md}`, `packages/core/src/vault.ts`, `packages/core/test/core.test.ts`, `docs/contracts/01-vault-layout.md`, `packages/cli/src/assistant-cli-access.ts`, targeted `packages/cli/test/{assistant-cli-access.test.ts,assistant-service.test.ts}` | exempt `raw/inbox/**` from generic raw-manifest validation; add focused inbox-envelope regression coverage; narrow assistant vault-operator guidance away from direct canonical file edits | in_progress | Non-exclusive lane. Preserve adjacent dirty edits in `packages/core/src/vault.ts` and assistant prompt files, keep the validator exception limited to `raw/inbox/**`, and do not reshape unrelated vault metadata compatibility behavior. |
| Codex | Route inbox canonical envelope/event/audit persistence through audited core write operations so assistant canonical-write guard and concurrent inbox ingestion can coexist without rollbacks. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/core/src/{operations/write-batch.ts,public-mutations.ts,index.ts}`, `packages/inboxd/src/{indexing/persist.ts,kernel/pipeline.ts,index.ts}`, targeted `packages/{core,inboxd,cli}/test/*` | extend generic audited write-batch support for raw payload content; migrate inbox envelope/event/audit writes onto core write-operation surfaces; add assistant/inbox regression proof | completed | Non-exclusive lane. Preserve adjacent assistant-guard and inbox-runtime edits, keep the change scoped to canonical mutation routing rather than inbox connector behavior, and avoid broad vault-layout or assistant prompt changes. |
| Codex | Stabilize the shared CLI integration-test subprocess harness so root coverage runs do not inherit unrelated suite env drift. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/test/cli-test-helpers.ts`, any directly required CLI harness tests | narrow CLI test helper env sanitization/isolation for `runCli` / `runRawCli` child processes | in_progress | Non-exclusive lane. Preserve existing CLI env-override semantics for tests that intentionally pass env, and avoid changing production CLI runtime behavior. |
| Codex | Align the Cloudflare hosted-runner deploy/runtime scaffold docs and config with the current real shape without inventing missing deployment pieces. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/cloudflare/{README.md,package.json,wrangler.jsonc}`, `Dockerfile.cloudflare-hosted-runner`, `.dockerignore`, `README.md`, `ARCHITECTURE.md`, `agent-docs/{index.md,operations/verification-and-runtime.md,references/testing-ci-map.md}` | narrow Cloudflare worker binding/env/internal-route/container-doc alignment; keep placeholders explicit; limit package-script wiring to truthful scaffold commands only | in_progress | Non-exclusive lane. Overlaps the broader hosted-runner and verification rows; preserve adjacent dirty edits, avoid `apps/web` or runtime-state changes, and keep this scoped to docs/config truthfulness plus minimal scaffold wiring. |
| Codex | Coordinate a five-lane follow-up simplification sweep via shared-worktree Codex workers after deduping out the already-completed hosted-web JSON helper refactor. | `agent-docs/exec-plans/active/{2026-03-26-parallel-simplification-followup.md,COORDINATION_LEDGER.md,worker-prompts/**}`, `packages/core/src/{canonical-mutations.ts,domains/shared.ts,bank/providers.ts,bank/protocols.ts}`, `packages/core/test/{canonical-mutations-boundary.test.ts,core.test.ts,health-bank.test.ts}`, `packages/cli/src/usecases/{shared.ts,document-meal-read.ts,experiment-journal-vault.ts,food.ts,integrated-services.ts,provider-event.ts,recipe.ts}`, `packages/cli/test/{list-cursor-compat.test.ts,cli-expansion-document-meal.test.ts,cli-expansion-experiment-journal-vault.test.ts,cli-expansion-provider-event-samples.test.ts,health-tail.test.ts}`, `packages/importers/src/device-providers/{shared-normalization.ts,oura.ts,whoop.ts,garmin-helpers.ts,garmin.ts}`, `packages/importers/test/device-providers.test.ts` | finish remaining canonical frontmatter-reader dedupe; reuse shared provider/domain/list-envelope/device-normalization helpers; characterize risky protocol selector behavior without forcing alignment | completed | Completed in the shared cleanup batch. The follow-up prompt sweep landed the low-risk simplifications, preserved the report-first `protocols.ts` characterization stance, and kept the already-landed hosted-web helper refactor deduped out. |
| Codex | Replace the repo-local `review-gpt` patch with upstream `@cobuild/review-gpt@0.4.3`, keep the sibling update compatible with the dirty tree, and verify the Murph research writeback path with one cholesterol note saved into the vault. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `package.json`, `pnpm-lock.yaml`, `patches/@cobuild__review-gpt@0.4.2.patch`, `vault/research/**` | remove stale `patchedDependencies` entry for `@cobuild/review-gpt@0.4.2`; update workspace dependency metadata to `0.4.3`; validate the `research` command writes one Deep Research markdown note into `vault/research/` | in_progress | Non-exclusive lane. Preserve all unrelated in-flight package/CLI/web edits already present; scope this to the review-gpt dependency upgrade, lock/patch metadata, and one end-to-end vault research note save. |
| Codex | Fix Deep Research autosend so the managed ChatGPT flow starts the post-send research plan without stealing focus or relying on repeated screen-coordinate clicks. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `pnpm-lock.yaml`, `patches/@cobuild__review-gpt@0.4.2.patch` | patch `@cobuild/review-gpt` deep-research autosend flow to reach the sandbox iframe target, click `Start` inside the iframe DOM, and remove repeated `Page.bringToFront`/hotspot nudging while preserving standard-chat autosend behavior | in_progress | Non-exclusive lane. Keep this scoped to the review-gpt dependency patch and lock metadata; verify against the live managed browser flow before handoff. |
| Codex | Fix `vault-cli` help/bootstrap fragility and make assistant memory upserts more robust for natural-language identity/tone messages. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/{setup-services/shell.ts,assistant/memory/{extraction,turn-context}.ts,commands/assistant.ts}`, targeted `packages/cli/test/{assistant-cli,assistant-state,setup-cli}.test.ts` | narrow shim bootstrap gating for discovery commands; add assistant-memory source-prompt clause extraction/normalization helpers and operator-override env handling; clarify `assistant memory upsert` help text/examples for canonical wording expectations | in_progress | Non-exclusive lane. Preserve existing setup alias behavior and current assistant memory privacy/health gating; keep the change scoped to discovery/bootstrap semantics, long-term memory extraction/override UX, and focused regression coverage. |
| Codex | Add a generic `assistant state` JSON scratchpad surface and optional cron `stateDocId` binding so stateful cron jobs can persist non-canonical run context without direct file edits. | `agent-docs/exec-plans/active/{2026-03-27-assistant-state-cron-binding.md,COORDINATION_LEDGER.md}`, `ARCHITECTURE.md`, `agent-docs/{index.md,operations/verification-and-runtime.md,references/testing-ci-map.md}`, `docs/contracts/03-command-surface.md`, `packages/runtime-state/src/assistant-state.ts`, `packages/cli/src/{assistant-cli-contracts.ts,assistant-runtime.ts,commands/assistant.ts,assistant/{service.ts,store.ts,store/paths.ts,cron.ts,state*.ts,state-write-lock.ts}}`, targeted `packages/cli/test/{assistant-cli,assistant-cron,assistant-service,assistant-state,incur-smoke}.test.ts` | generic assistant-state document storage; optional cron state binding and prompt/tool guidance; typed CLI/MCP-facing command surface | in_progress | Non-exclusive lane with direct overlap on already-dirty `packages/cli/src/{assistant/service.ts,assistant-cli-contracts.ts,commands/assistant.ts,assistant/cron.ts}`. Preserve adjacent provider-config/session-normalization edits and keep this scoped to the generic state primitive, cron binding, docs, and direct regressions only. |
| Codex | Align assistant meal logging/query guidance so text-only food logs do not produce false "nothing logged" answers across Telegram/email. | `packages/cli/src/{assistant-cli-access,assistant/service}.ts`, targeted `packages/cli/test/{assistant-cli-access,assistant-service}.test.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | narrow assistant guidance/prompt copy around text-only food logging and same-day meal lookup expectations; add regression assertions for cross-surface consistency wording | in_progress | Non-exclusive lane. Preserve current vault selection/session binding behavior and keep the fix scoped to assistant guidance plus focused tests unless a minimal adjacent hook is required. |
| Codex | Integrate the assistant provider recovery/progress patch and fix the reviewed retry/session/Ink regressions. | `packages/cli/src/{assistant-codex,chat-provider}.ts`, `packages/cli/src/assistant/{automation,service,provider-turn-recovery,provider-traces}.ts`, `packages/cli/src/assistant/ui/{ink,view-model}.ts`, targeted `packages/cli/test/{assistant-codex,assistant-provider,assistant-service,assistant-runtime}.test.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared provider recovery helper + provider progress event symbols; update Codex failure classification/progress parsing; thread provider progress/recovered session handling through service/UI; keep auto-reply retry state non-terminal without duplicating prior-turn trace rows or resuming non-retryable failures | in_progress | Non-exclusive lane. Preserve existing streaming trace behavior, avoid overwriting older Ink trace rows across turns, and keep recovered provider-session persistence gated to actual reconnectable failures. |
| Codex | Integrate the assistant iMessage delivery and Ink chat patch onto the current assistant runtime. | `packages/cli/src/assistant-*`, `packages/cli/src/commands/assistant.ts`, `packages/cli/src/incur.generated.ts`, `packages/cli/package.json`, assistant-related `packages/cli/test/*`, `docs/contracts/03-command-surface.md`, `agent-docs/exec-plans/active/2026-03-17-assistant-imessage-ink.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add outbound assistant channel/delivery symbols, Ink chat UI symbols, assistant deliver command/options/result metadata | in_progress | Non-exclusive lane. Preserve current inbox CLI/generated edits from the Telegram lane and merge on top of the already-landed assistant runtime slice. |
| Codex | Simplify duplicated pure query helpers and remove one dead private converter only if it remains unused. | `packages/query/src/{canonical-entities,export-pack-health,model}.ts`, `packages/query/src/health/{assessments,history}.ts`, `agent-docs/exec-plans/active/2026-03-17-query-helper-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared internal sort/string-array helper symbols; update assessment/history ordering callers; delete `recordToCanonicalEntity` if still unreferenced | in_progress | Non-exclusive lane. Preserve exact sort order, trimming/filtering/dedupe behavior, and exported query behavior; stop instead of deleting the converter if a new local reference appears during the task. |
| Codex | Consolidate exact small CLI helper duplicates and remove one stale private wrapper without changing public behavior. | `packages/cli/src/command-helpers.ts`, `packages/cli/src/commands/{assistant,inbox,document-meal-read-helpers,export-intake-read-helpers,health-command-factory}.ts`, `packages/cli/src/{setup-services,operator-config}.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/2026-03-17-cli-helper-cleanup.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared `parseHeadersJsonOption`, `resolveEffectiveTopLevelToken`, `ROOT_OPTIONS_WITH_VALUES`, and `firstString` helper symbols; delete duplicate local helper symbols and unused `registerFactoryCommandGroup` | in_progress | Non-exclusive lane. Overlaps existing CLI cleanup lanes in `health-command-factory.ts` and the read-helper modules; preserve any adjacent edits already present and keep all validation strings, argv behavior, and return shapes byte-for-byte equivalent. |
| Codex | Tighten the Ink assistant chat status copy so busy/waiting state is shown once instead of repeated across the transcript, prompt, placeholder, and footer. | `packages/cli/src/assistant/ui/ink.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | simplify busy/wait labels and transcript seed copy; add focused helper-level assertions for chat formatting/status text | in_progress | Non-exclusive lane. Keep the fix local to the Ink chat UI and do not reshape assistant runtime/session behavior. |
| Codex | Simplify duplicated registry-style health CLI descriptor entries without changing metadata or routing behavior. | `packages/cli/src/health-cli-descriptors.ts`, `packages/cli/src/commands/health-entity-command-registry.ts`, `packages/cli/src/assistant-cli-tools.ts`, targeted `packages/cli/test/{health-tail,list-cursor-compat,incur-smoke,stdin-input}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-descriptor-matrix-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add one local helper for status-filtered registry descriptor extensions; rewrite `goal`/`condition`/`allergy`/`protocol`/`family`/`genetics` entries in `checkedHealthEntityDescriptorExtensions`; keep `buildHealthEntityDescriptor` and surfaced command/tool metadata stable | in_progress | Non-exclusive lane. Confine the refactor to descriptor composition, preserve all externally visible strings and method names byte-for-byte, and avoid reshaping the downstream command-registry or assistant tool contracts unless tests prove alignment work is required. |
| Codex | Deduplicate meal/document canonical inbox promotion flow while preserving canonical-match reuse, local promotion-state validation, and retry idempotency. | `packages/cli/src/inbox-services.ts`, targeted `packages/cli/test/{inbox-cli,inbox-model-route}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-inbox-canonical-promotion-dedupe.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared canonical attachment promotion helper; rewire `promoteMeal` and `promoteDocument`; preserve `reconcileCanonicalImportPromotion`, match-spec usage, and result envelope fields | in_progress | Non-exclusive lane. Keep `promoteJournal` and `promoteExperimentNote` out of scope unless a tiny supporting extraction is unavoidable; preserve existing error codes/messages and `created` semantics exactly. |
| Codex | Centralize the shared core id-or-slug lookup path for bank and markdown registries without changing conflict or missing behavior. | `packages/core/src/{bank/shared.ts,registry/markdown.ts,registry/id-or-slug.ts}`, targeted `packages/core/test/*`, `agent-docs/exec-plans/active/2026-03-17-core-id-or-slug-dedupe.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared internal id-or-slug match helper symbol(s); rebuild bank and markdown registry wrapper symbols on top; keep protocol selectors unchanged unless a tiny fit appears | in_progress | Non-exclusive lane. Preserve error codes/messages byte-for-byte, preserve read-vs-upsert conflict behavior, and stop instead of folding protocol group semantics into the helper. |
| Codex | Give post-setup assistant chat reliable access to the raw Murph CLI through the existing Incur-backed surface. | `packages/cli/src/{assistant-cli-access,assistant-codex,assistant/service,chat-provider}.ts`, targeted `packages/cli/test/{assistant-provider,assistant-cli-access}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-assistant-cli-access.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add assistant CLI access/env hint symbols; thread provider env through Codex exec; update assistant prompt/context with `vault-cli`/`murph` guidance | in_progress | Non-exclusive lane. Preserve current assistant session/binding semantics, keep the vault-root working directory, and avoid the actively edited Ink UI files unless a local follow-up proves that necessary. |
| Codex | Add a repo-root `pnpm chat` alias so the local assistant chat has the same ergonomics as `pnpm onboard`. | `package.json`, `README.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add root `chat` script alias and document the repo-local usage | in_progress | Non-exclusive lane. Keep this to root script/docs only; do not reshape assistant runtime behavior or the CLI command graph. |
| Codex | Lock down the `device-syncd` local control plane so control routes are loopback-only/authenticated and webhook intake can stay separately exposed. | `ARCHITECTURE.md`, `agent-docs/operations/verification-and-runtime.md`, `packages/device-syncd/README.md`, `packages/device-syncd/src/{config,http,service,types}.ts`, `packages/device-syncd/test/{config,http,service}.test.ts`, `packages/cli/src/device-sync-client.ts`, `packages/cli/test/device-cli.test.ts`, `packages/web/src/lib/device-sync.ts`, `packages/web/test/{device-sync-lib,device-sync-routes}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-device-syncd-control-plane-lockdown.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add control-plane auth/listener config + request-guard helpers; split local control routes from public webhook routing; thread authenticated client headers through CLI/web callers; add rejection/allow tests for loopback, auth, and webhooks | in_progress | Non-exclusive lane but overlaps the active `device-syncd` audit row above. Preserve the current localhost-default and job-serialization edits already in the worktree; keep this slice focused on HTTP trust boundaries and aligned caller/docs updates. |
| Codex | Add optional structured strength-session data to workout quick capture so explicit lifting notes keep machine-readable exercise details without losing the freeform note. | `packages/contracts/src/{examples,zod}.ts`, `packages/contracts/generated/event-record.schema.json`, `packages/cli/src/{commands/workout.ts,usecases/workout.ts,vault-cli-contracts.ts}`, `packages/cli/test/cli-expansion-workout.test.ts`, `docs/contracts/{02-record-schemas,03-command-surface,05-fixtures}.md`, `fixtures/golden-outputs/workout-add/README.md`, `agent-docs/exec-plans/active/2026-03-18-structured-workout-logging.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add `strengthExercises` contract/result symbols; infer exercise/set/rep/load structure from explicit strength notes; keep existing workout ids, note preservation, and cardio behavior stable | in_progress | Non-exclusive lane. Confine the change to workout/activity-session structure, preserve current device-import activity-session compatibility, and avoid overlapping assistant/device verification files already in flight. |
| Codex | Build the hosted device-sync control plane in `apps/web` with Prisma/Postgres, shared ingress reuse, and hosted browser/public/agent routes. | `pnpm-workspace.yaml`, `package.json`, `tsconfig*.json`, `ARCHITECTURE.md`, `README.md`, `docs/device-sync-hosted-control-plane.md`, `agent-docs/index.md`, `agent-docs/operations/verification-and-runtime.md`, `agent-docs/references/testing-ci-map.md`, `agent-docs/exec-plans/active/2026-03-23-hosted-device-sync-control-plane.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/**`, targeted `packages/device-syncd/**` exports/docs if needed | add hosted device-sync Prisma models and app route/lib symbols; add hosted auth/agent/token escrow helpers; keep `packages/web` local-only | in_progress | Non-exclusive lane. New app lives under `apps/web`; preserve adjacent device-syncd trust-boundary edits and only touch package exports/docs when needed for shared-ingress reuse. |
| Codex | Repair the current red repo checks by fixing the smallest build/type mismatches blocking `pnpm typecheck`, `pnpm test`, and `pnpm test:coverage`. | `agent-docs/exec-plans/active/{2026-03-25-green-checks-repair.md,COORDINATION_LEDGER.md}`, `packages/core/src/bank/providers.ts`, `packages/cli/src/{inbox-services/connectors,setup-wizard}.ts`, any directly required package/build wiring, and targeted tests/docs only if needed to keep repo verification truthful | narrow green-checks repair around provider write-batch typing, Linq inbox runtime typing/build alignment, setup wizard exhaustive returns, and any immediately adjacent verification blockers discovered while rerunning the required commands | in_progress | Non-exclusive lane. Preserve the in-flight core-domain and Linq integration edits already present; do not revert adjacent work, and keep this scoped to getting the required repo checks green. |
| Codex | Add a first-class regular-food registry/file type plus CLI/query/core wiring so repeated branded foods and recurring meals can be stored once and referenced cheaply. | `agent-docs/exec-plans/active/{2026-03-26-regular-food-registry.md,COORDINATION_LEDGER.md}`, `packages/contracts/src/{constants,zod,examples,schemas}.ts`, `packages/contracts/generated/*`, `packages/core/src/{bank/types.ts,bank/foods.ts,index.ts,public-mutations.ts,constants.ts,vault.ts}`, targeted `packages/core/test/*`, `packages/query/src/health/{foods.ts,index.ts}`, targeted `packages/query/test/*`, `packages/cli/src/{commands/{food,assistant}.ts,incur.generated.ts,operator-config.ts,assistant-cli-tools.ts,vault-cli-command-manifest.ts,usecases/{food.ts,integrated-services.ts,types.ts}}`, targeted `packages/cli/test/*`, and contract docs/fixtures as needed | add regular-food/food registry symbols, frontmatter/schema/id constants, direct CLI `food scaffold|upsert|show|list`, assistant tool exposure, focused read/write regressions, and the minimum follow-on CLI typing fix required by verification | in_progress | Non-exclusive lane. Keep the change additive, preserve existing recipe/supplement semantics, avoid reshaping meal-import behavior beyond enabling future regular-meal lookup hooks, and keep any `commands/assistant.ts` edit to the narrow verification unblock only. |
| Codex | Audit the landed CLI record edit/delete feature for logic bugs and fix any confirmed issues without widening the command surface. | `agent-docs/exec-plans/active/{2026-03-27-cli-record-edit-delete.md,COORDINATION_LEDGER.md}`, `packages/cli/src/{commands/{document,event,food,health-command-factory,intervention,meal,provider,recipe,record-mutation-command-helpers,workout}.ts,usecases/{document-meal-read,event-record-mutations,food,intervention,provider-event,recipe,record-mutations,shared,vault-usecase-helpers,workout}.ts,vault-cli-command-manifest.ts,vault-cli-contracts.ts,incur.generated.ts}`, `packages/contracts/src/zod.ts`, `packages/contracts/generated/event-record.schema.json`, `packages/core/src/{bank/{foods,providers,recipes,types}.ts,domains/events.ts,index.ts,operations/write-batch.ts,public-mutations.ts,registry/{api,markdown}.ts}`, targeted CLI/core tests | deep audit of shared patch semantics, noun-specific edit/delete adapters, and core event/registry delete behavior; fix only confirmed functional bugs and add narrow regressions | completed | Non-exclusive lane. Completed with focused edit/delete regressions green; repo wrapper failures remain in unrelated `packages/contracts` and `apps/web` work. |
| Codex | Audit workspace script/typecheck/build commands for in-place TypeScript emit risk, make the source-bundle flow self-heal from generated sidecars, and clean generated JS/declaration residue without touching active TS source edits. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-03-26-source-sidecar-guard.md`, `package.json`, `scripts/{package-audit-context.sh,check-no-js.ts}`, targeted workspace `package.json`/`tsconfig*.json` files if the audit proves a repo-owned emit path, and related verification docs | audit script/tsconfig emit-risk matrix; add source-sidecar prune/report helper symbols; preserve existing package command surface while preventing or auto-cleaning untracked `src/*.js` / `src/*.d.ts` residue | in_progress | Non-exclusive lane. Root `package.json` already has active overlap; keep this scoped to source-sidecar prevention/cleanup and do not modify or remove any in-progress `.ts` edits outside generated JS/declaration artifacts. |
