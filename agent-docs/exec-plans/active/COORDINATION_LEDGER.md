# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Restore clean-checkout `tsx` workspace resolution for the Cloudflare deploy scripts so GitHub Actions does not fall back to missing package `dist/` entrypoints | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-04-03-cloudflare-deploy-script-path-resolution.md`, `apps/cloudflare/tsconfig.scripts.json` | deploy-script workspace aliases, CI `tsx` resolution | In progress | Keep the fix limited to canonical workspace entrypoint resolution and preserve unrelated web-tools/app-page work already in flight. |
| Codex | Remove the hosted assistant automation opt-out so hosted automation is always on when the hosted assistant profile is ready | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-04-03-hosted-assistant-automation-default.md`, `packages/hosted-execution/src/env.ts`, `packages/assistant-runtime/src/hosted-runtime/{context.ts,maintenance.ts}`, `apps/cloudflare/{src,test,README.md,DEPLOY.md}`, `.github/workflows/deploy-cloudflare-hosted.yml`, `ARCHITECTURE.md` | hosted assistant automation gating, runner env filtering, hosted docs | In progress | High-risk hosted-runtime lane. Preserve adjacent hosted-email/deploy edits and do not broaden into unrelated verification-script work. |
| Codex | Land the supplied workout cutover plus measurements patch against the uploaded snapshot without disturbing unrelated dirty-tree work | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/{assistant-core,cli,contracts,core,query}/**` | workout cutover CLI surface, workout/measurement ledger events, workout media manifests, profile workout-unit preferences | In progress | Supplied patch landing only. Preserve the active hosted/runtime and app page edits already in the tree and resolve overlaps by porting intent rather than overwriting. |
| Codex | Remove stale PaddleOCR deploy/docs/config residue after the hard cut so the hosted surface no longer advertises dead knobs | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `agent-docs/exec-plans/active/2026-04-03-paddleocr-stale-cleanup.md`, `.github/workflows/deploy-cloudflare-hosted.yml`, `apps/cloudflare/{src,test,DEPLOY.md}`, `agent-docs/operations/verification-and-runtime.md` | PaddleOCR deploy env residue, Cloudflare hosted deploy docs, stale parser-toolchain toggles | In progress | Keep the change scoped to stale PaddleOCR references only. Preserve adjacent hosted assistant/deploy edits and do not broaden into unrelated hosted config cleanup. |
