# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Apply bounded Pro package-boundary patch | `apps/web/package.json`, `packages/assistant-engine/package.json`, `packages/assistant-runtime/package.json`, `packages/assistantd/package.json`, `packages/hosted-execution/package.json`, `scripts/verify-workspace-boundaries.mjs` | workspace package deps, boundary verifier | in_progress | Narrow manifest + verifier cleanup from downloaded Pro patch; avoid unrelated dirty files |
| Codex | Land downloaded Pro legacy-removal patch for dead vault-upgrade seam removal | `ARCHITECTURE.md`; `docs/architecture.md`; `packages/contracts/src/vault.ts`; `packages/contracts/src/constants.ts`; `packages/contracts/generated/audit-record.schema.json`; `packages/core/src/index.ts`; `packages/core/src/public-mutations.ts`; `packages/core/src/vault-upgrade.ts`; `packages/core/README.md`; `packages/operator-config/src/vault-cli-contracts.ts`; `packages/vault-usecases/src/usecases/integrated-services.ts`; `packages/vault-usecases/src/usecases/types.ts`; `packages/vault-usecases/src/usecases/vault-usecase-helpers.ts`; `packages/cli/src/commands/vault.ts`; `packages/cli/src/incur.generated.ts`; `packages/cli/src/vault-cli-command-manifest.ts`; focused tests under `packages/{contracts,core,query,vault-usecases,cli}/test/**`; `agent-docs/exec-plans/active/2026-04-09-legacy-vault-upgrade-removal.md` | `upgradeVault`; `vault upgrade`; `VAULT_UPGRADE_REQUIRED`; `VAULT_UPGRADE_UNSUPPORTED`; `VAULT_UNSUPPORTED_FORMAT`; `vault_upgrade` | in_progress | Bounded landing of the downloaded Pro patch intent. Keep historical changelog/release-note references and completed plans untouched; preserve unrelated dirty files. |
| Codex | Land downloaded Pro workspace-artifact lock patch for shared verification outputs | `scripts/run-with-workspace-artifact-lock.mjs`; `scripts/workspace-verify.sh`; `scripts/build-test-runtime-prepared.mjs`; `apps/web/scripts/verify-fast.sh`; `apps/cloudflare/scripts/verify-fast.sh`; `agent-docs/operations/verification-and-runtime.md`; `agent-docs/references/testing-ci-map.md`; `agent-docs/exec-plans/active/2026-04-09-workspace-artifact-lock.md` | `MURPH_WORKSPACE_ARTIFACT_LOCK_HELD`; workspace-artifact lock wrapper; verify harness lock entrypoints | in_progress | Manual landing of downloaded Pro patch intent; preserve the existing local `scripts/workspace-verify.sh` concurrency tweak and avoid unrelated dirty files. |
| Codex | Apply downloaded Pro simplify patch for Cloudflare deploy scripts | `apps/cloudflare/scripts/cleanup-container-images.ts`; `apps/cloudflare/scripts/deploy-automation/container-images.ts`; `apps/cloudflare/scripts/deploy-automation/environment.ts`; `apps/cloudflare/scripts/deploy-automation/secrets.ts`; `apps/cloudflare/scripts/deploy-automation/shared.ts`; `apps/cloudflare/scripts/deploy-worker-version.cli.ts`; `apps/cloudflare/scripts/r2-lifecycle.ts` | deploy-script JSON parsing; hosted assistant env-name reads; Wrangler config arg reuse; lifecycle bucket normalization | in_progress | Bounded wake-up landing from the downloaded simplify patch only; preserve unrelated worktree edits and keep scope limited to behavior-preserving script cleanup. |
