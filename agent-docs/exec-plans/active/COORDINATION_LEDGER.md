# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Make inbox capture the only canonical intake fact | `packages/inboxd/**`, `packages/contracts/**`, `packages/assistant-core/**`, related inbox tests/docs | `persistCanonicalInboxCapture`, `ensureStoredCaptureCanonicalEvidence`, inbox capture contract/docs | in_progress | Narrow persisted-intake lane; preserve adjacent edits from the active compatibility cleanup and avoid unrelated runtime-state refactors. |
| Codex | Hard-cut remaining compatibility logic and migration fallbacks | `packages/runtime-state/**`, `packages/gateway-local/**`, `packages/query/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/assistant-core/**`, `packages/core/**`, `packages/contracts/**`, `apps/cloudflare/**`, related tests/docs | `promoteLegacyLocalState*`, `readLocalStateTextFileWithFallback`, `legacyParseValue`, `RELATED_IDS_COMPATIBILITY_RELATION`, `validateVaultMetadataWithCompatibility`, hosted pending-usage legacy migration | completed | Exclusive broad refactor lane across persisted-state/runtime compatibility seams; cleanup landed and verification passed. |
| Codex | Remove the dead hosted `agentState` bundle lane and hard-cut to one hosted workspace bundle | `packages/runtime-state/**`, `packages/hosted-execution/**`, `packages/assistant-runtime/**`, `apps/cloudflare/**`, `apps/web/**`, related hosted tests/docs | `agentStateBundle`, `HostedExecutionBundleRefs`, hosted bundle slots, Cloudflare runner bundle refs | completed | Exclusive hosted bundle-contract refactor across runtime-state, hosted-execution, assistant-runtime, and Cloudflare queue/journal surfaces; cleanup landed and repo verification passed. |
| Codex | Land assistant capability cleanup patch and remove the remaining write-tool alias | `packages/assistant-core/src/**`, `packages/assistant-core/test/**` | `defineAssistantCapability`, provider-turn runtime/catalog binding, `includeVaultWriteTools` | in_progress | Narrow assistant-core patch lane; preserve adjacent edits and keep the host-binding/catalog behavior aligned across default, inbox-routing, and provider-turn profiles. |
| Codex | Hard-cut hosted legacy storage readers and old hosted key-path fallbacks | `apps/cloudflare/**`, related hosted tests/docs | `listHostedStorageObjectKeys`, hosted `*ObjectKeys`, pending usage dirty-user/object-path fallback reads, `migrateLegacyRunnerMetaActivatedColumn` | completed | Exclusive hosted storage-cleanup lane across Cloudflare storage readers, usage store, and runner queue schema; cleanup landed, targeted Cloudflare tests passed, `pnpm typecheck` passed, and repo `pnpm test` remains blocked by unrelated existing assistant/CLI build failures. |
| Codex | Remove residual legacy cleanup references and hosted device-sync flat-field guards | `apps/web/**`, `packages/hosted-execution/**`, `vitest.config.ts`, `ARCHITECTURE.md`, `docs/legacy-removal-audit-2026-03-31.md`, related tests | hosted device-sync apply-request flat-field rejection helpers, stale legacy audit wording, stale compat test naming | in_progress | Narrow cleanup lane; preserve adjacent hosted edits and keep behavior fail-closed while removing dead legacy-specific wording and names. |
| Worker B | Assistant-state hard cut for assistant-core runtime surfaces | `packages/assistant-core/src/assistant/runtime-state-service.ts`, `packages/assistant-core/src/assistant/system-prompt.ts`, `packages/assistant-core/src/assistant/provider-turn-runner.ts`, `packages/assistant-core/src/assistant/cli-surface-bootstrap.ts`, `packages/assistant-core/src/assistant/first-contact.ts`, `packages/assistant-core/src/assistant-runtime.ts`, optional prompt-plumbing cleanup in `packages/assistant-core/src/assistant/memory*.ts` | `RuntimeStateService`, system prompt memory wording, provider turn memory injection, bootstrap/first-contact runtime root handling | in_progress | Narrow Worker B lane for the active assistant-state hard cut. Preserve existing edits in owned files and do not touch docs, shared packages, or CLI registration. |
