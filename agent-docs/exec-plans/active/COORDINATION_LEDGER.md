# Coordination Ledger

Active coding work must register here before code changes begin.
Rows are active-work notices by default, not hard file locks.
Use `Notes` to mark a lane as exclusive when overlap is unsafe, such as a large refactor or delicate cross-cutting rewrite.

| Agent | Scope | Files | Symbols | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Codex | Fix `vault-cli` help/bootstrap fragility and make assistant memory upserts more robust for natural-language identity/tone messages. | `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/{setup-services/shell.ts,assistant/memory/{extraction,turn-context}.ts,commands/assistant.ts}`, targeted `packages/cli/test/{assistant-cli,assistant-state,setup-cli}.test.ts` | narrow shim bootstrap gating for discovery commands; add assistant-memory source-prompt clause extraction/normalization helpers and operator-override env handling; clarify `assistant memory upsert` help text/examples for canonical wording expectations | in_progress | Non-exclusive lane. Preserve existing setup alias behavior and current assistant memory privacy/health gating; keep the change scoped to discovery/bootstrap semantics, long-term memory extraction/override UX, and focused regression coverage. |
| Codex | Align assistant meal logging/query guidance so text-only food logs do not produce false "nothing logged" answers across Telegram/email. | `packages/cli/src/{assistant-cli-access,assistant/service}.ts`, targeted `packages/cli/test/{assistant-cli-access,assistant-service}.test.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | narrow assistant guidance/prompt copy around text-only food logging and same-day meal lookup expectations; add regression assertions for cross-surface consistency wording | in_progress | Non-exclusive lane. Preserve current vault selection/session binding behavior and keep the fix scoped to assistant guidance plus focused tests unless a minimal adjacent hook is required. |
| Codex | Integrate the assistant provider recovery/progress patch and fix the reviewed retry/session/Ink regressions. | `packages/cli/src/{assistant-codex,chat-provider}.ts`, `packages/cli/src/assistant/{automation,service,provider-turn-recovery,provider-traces}.ts`, `packages/cli/src/assistant/ui/{ink,view-model}.ts`, targeted `packages/cli/test/{assistant-codex,assistant-provider,assistant-service,assistant-runtime}.test.ts`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared provider recovery helper + provider progress event symbols; update Codex failure classification/progress parsing; thread provider progress/recovered session handling through service/UI; keep auto-reply retry state non-terminal without duplicating prior-turn trace rows or resuming non-retryable failures | in_progress | Non-exclusive lane. Preserve existing streaming trace behavior, avoid overwriting older Ink trace rows across turns, and keep recovered provider-session persistence gated to actual reconnectable failures. |
| Codex | Integrate the assistant iMessage delivery and Ink chat patch onto the current assistant runtime. | `packages/cli/src/assistant-*`, `packages/cli/src/commands/assistant.ts`, `packages/cli/src/incur.generated.ts`, `packages/cli/package.json`, assistant-related `packages/cli/test/*`, `docs/contracts/03-command-surface.md`, `agent-docs/exec-plans/active/2026-03-17-assistant-imessage-ink.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add outbound assistant channel/delivery symbols, Ink chat UI symbols, assistant deliver command/options/result metadata | in_progress | Non-exclusive lane. Preserve current inbox CLI/generated edits from the Telegram lane and merge on top of the already-landed assistant runtime slice. |
| Codex | Simplify duplicated pure query helpers and remove one dead private converter only if it remains unused. | `packages/query/src/{canonical-entities,export-pack-health,model}.ts`, `packages/query/src/health/{assessments,history}.ts`, `agent-docs/exec-plans/active/2026-03-17-query-helper-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared internal sort/string-array helper symbols; update assessment/history ordering callers; delete `recordToCanonicalEntity` if still unreferenced | in_progress | Non-exclusive lane. Preserve exact sort order, trimming/filtering/dedupe behavior, and exported query behavior; stop instead of deleting the converter if a new local reference appears during the task. |
| Codex | Consolidate exact small CLI helper duplicates and remove one stale private wrapper without changing public behavior. | `packages/cli/src/command-helpers.ts`, `packages/cli/src/commands/{assistant,inbox,document-meal-read-helpers,export-intake-read-helpers,health-command-factory}.ts`, `packages/cli/src/{setup-services,operator-config}.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/2026-03-17-cli-helper-cleanup.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared `parseHeadersJsonOption`, `resolveEffectiveTopLevelToken`, `ROOT_OPTIONS_WITH_VALUES`, and `firstString` helper symbols; delete duplicate local helper symbols and unused `registerFactoryCommandGroup` | in_progress | Non-exclusive lane. Overlaps existing CLI cleanup lanes in `health-command-factory.ts` and the read-helper modules; preserve any adjacent edits already present and keep all validation strings, argv behavior, and return shapes byte-for-byte equivalent. |
| Codex | Tighten the Ink assistant chat status copy so busy/waiting state is shown once instead of repeated across the transcript, prompt, placeholder, and footer. | `packages/cli/src/assistant/ui/ink.ts`, targeted `packages/cli/test/*`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | simplify busy/wait labels and transcript seed copy; add focused helper-level assertions for chat formatting/status text | in_progress | Non-exclusive lane. Keep the fix local to the Ink chat UI and do not reshape assistant runtime/session behavior. |
| Codex | Simplify duplicated registry-style health CLI descriptor entries without changing metadata or routing behavior. | `packages/cli/src/health-cli-descriptors.ts`, `packages/cli/src/commands/health-entity-command-registry.ts`, `packages/cli/src/assistant-cli-tools.ts`, targeted `packages/cli/test/{health-tail,list-cursor-compat,incur-smoke,stdin-input}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-descriptor-matrix-simplify.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add one local helper for status-filtered registry descriptor extensions; rewrite `goal`/`condition`/`allergy`/`regimen`/`family`/`genetics` entries in `checkedHealthEntityDescriptorExtensions`; keep `buildHealthEntityDescriptor` and surfaced command/tool metadata stable | in_progress | Non-exclusive lane. Confine the refactor to descriptor composition, preserve all externally visible strings and method names byte-for-byte, and avoid reshaping the downstream command-registry or assistant tool contracts unless tests prove alignment work is required. |
| Codex | Deduplicate meal/document canonical inbox promotion flow while preserving canonical-match reuse, local promotion-state validation, and retry idempotency. | `packages/cli/src/inbox-services.ts`, targeted `packages/cli/test/{inbox-cli,inbox-model-route}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-inbox-canonical-promotion-dedupe.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared canonical attachment promotion helper; rewire `promoteMeal` and `promoteDocument`; preserve `reconcileCanonicalImportPromotion`, match-spec usage, and result envelope fields | in_progress | Non-exclusive lane. Keep `promoteJournal` and `promoteExperimentNote` out of scope unless a tiny supporting extraction is unavoidable; preserve existing error codes/messages and `created` semantics exactly. |
| Codex | Centralize the shared core id-or-slug lookup path for bank and markdown registries without changing conflict or missing behavior. | `packages/core/src/{bank/shared.ts,registry/markdown.ts,registry/id-or-slug.ts}`, targeted `packages/core/test/*`, `agent-docs/exec-plans/active/2026-03-17-core-id-or-slug-dedupe.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add shared internal id-or-slug match helper symbol(s); rebuild bank and markdown registry wrapper symbols on top; keep regimen selectors unchanged unless a tiny fit appears | in_progress | Non-exclusive lane. Preserve error codes/messages byte-for-byte, preserve read-vs-upsert conflict behavior, and stop instead of folding regimen group semantics into the helper. |
| Codex | Give post-setup assistant chat reliable access to the raw Healthy Bob CLI through the existing Incur-backed surface. | `packages/cli/src/{assistant-cli-access,assistant-codex,assistant/service,chat-provider}.ts`, targeted `packages/cli/test/{assistant-provider,assistant-cli-access}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-assistant-cli-access.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add assistant CLI access/env hint symbols; thread provider env through Codex exec; update assistant prompt/context with `vault-cli`/`healthybob` guidance | in_progress | Non-exclusive lane. Preserve current assistant session/binding semantics, keep the vault-root working directory, and avoid the actively edited Ink UI files unless a local follow-up proves that necessary. |
| Codex | Add a repo-root `pnpm chat` alias so the local assistant chat has the same ergonomics as `pnpm onboard`. | `package.json`, `README.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add root `chat` script alias and document the repo-local usage | in_progress | Non-exclusive lane. Keep this to root script/docs only; do not reshape assistant runtime behavior or the CLI command graph. |
| Codex | Lock down the `device-syncd` local control plane so control routes are loopback-only/authenticated and webhook intake can stay separately exposed. | `ARCHITECTURE.md`, `agent-docs/operations/verification-and-runtime.md`, `packages/device-syncd/README.md`, `packages/device-syncd/src/{config,http,service,types}.ts`, `packages/device-syncd/test/{config,http,service}.test.ts`, `packages/cli/src/device-sync-client.ts`, `packages/cli/test/device-cli.test.ts`, `packages/web/src/lib/device-sync.ts`, `packages/web/test/{device-sync-lib,device-sync-routes}.test.ts`, `agent-docs/exec-plans/active/2026-03-17-device-syncd-control-plane-lockdown.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add control-plane auth/listener config + request-guard helpers; split local control routes from public webhook routing; thread authenticated client headers through CLI/web callers; add rejection/allow tests for loopback, auth, and webhooks | in_progress | Non-exclusive lane but overlaps the active `device-syncd` audit row above. Preserve the current localhost-default and job-serialization edits already in the worktree; keep this slice focused on HTTP trust boundaries and aligned caller/docs updates. |
| Codex | Add optional structured strength-session data to workout quick capture so explicit lifting notes keep machine-readable exercise details without losing the freeform note. | `packages/contracts/src/{examples,zod}.ts`, `packages/contracts/generated/event-record.schema.json`, `packages/cli/src/{commands/workout.ts,usecases/workout.ts,vault-cli-contracts.ts}`, `packages/cli/test/cli-expansion-workout.test.ts`, `docs/contracts/{02-record-schemas,03-command-surface,05-fixtures}.md`, `fixtures/golden-outputs/workout-add/README.md`, `agent-docs/exec-plans/active/2026-03-18-structured-workout-logging.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | add `strengthExercises` contract/result symbols; infer exercise/set/rep/load structure from explicit strength notes; keep existing workout ids, note preservation, and cardio behavior stable | in_progress | Non-exclusive lane. Confine the change to workout/activity-session structure, preserve current device-import activity-session compatibility, and avoid overlapping assistant/device verification files already in flight. |
| Codex | Build the hosted device-sync control plane in `apps/web` with Prisma/Postgres, shared ingress reuse, and hosted browser/public/agent routes. | `pnpm-workspace.yaml`, `package.json`, `tsconfig*.json`, `ARCHITECTURE.md`, `README.md`, `docs/device-sync-hosted-control-plane.md`, `agent-docs/index.md`, `agent-docs/operations/verification-and-runtime.md`, `agent-docs/references/testing-ci-map.md`, `agent-docs/exec-plans/active/2026-03-23-hosted-device-sync-control-plane.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `apps/web/**`, targeted `packages/device-syncd/**` exports/docs if needed | add hosted device-sync Prisma models and app route/lib symbols; add hosted auth/agent/token escrow helpers; keep `packages/web` local-only | in_progress | Non-exclusive lane. New app lives under `apps/web`; preserve adjacent device-syncd trust-boundary edits and only touch package exports/docs when needed for shared-ingress reuse. |
| Codex | Integrate Linq webhook/chat support into CLI assistant delivery, inbox connectors, and the onboarding/setup flow. | `ARCHITECTURE.md`, `agent-docs/operations/verification-and-runtime.md`, `agent-docs/references/testing-ci-map.md`, `agent-docs/exec-plans/active/2026-03-24-linq-integration.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `packages/cli/src/{assistant/channel-adapters,assistant/conversation-ref,commands/{assistant,inbox},inbox-cli-contracts,inbox-services,inbox-services/{connectors,shared},incur.generated,linq-runtime,outbound-channel,run-terminal-logging,setup-cli,setup-cli-contracts,setup-runtime-env,setup-services/channels,setup-wizard}.ts`, targeted `packages/cli/test/{assistant-channel,cli-test-helpers,inbox-cli,inbox-incur-smoke,inbox-service-boundaries,setup-channels,setup-cli}.test.ts`, `packages/inboxd/src/{index,connectors/linq/{connector,normalize,types}}.ts`, targeted `packages/inboxd/test/linq-connector.test.ts` | add Linq outbound/runtime/setup symbols, inbox source wiring, onboarding readiness selection, focused Linq regression coverage, and raise CLI raw-output test buffering for the expanded `--llms-full` manifest | in_progress | Non-exclusive lane. Overlaps the active assistant/channel/setup lanes; preserve adjacent in-flight edits, keep the change scoped to Linq integration plus required docs/tests, and do not revert unrelated local work. |
