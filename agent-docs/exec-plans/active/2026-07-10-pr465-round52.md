# PR 465 ReviewGPT Round 52

## Goal

Restore the narrow signed authority boundary for Telegram usage-limit notices and
keep the provider deadline active through response-body consumption.

## Constraints

- Preserve the single database transaction that owns the exact delivery attempt.
- Create the signed request only after control authentication and the exact
  attempt transition succeed.
- Bind the member, period, source event, exact attempt, target, reply, message,
  notice code, idempotency key, and freshness window in one verified envelope.
- Restore no claim/reconciliation stages, queues, retries, managers, or timeout
  configuration.
- Keep the provider/web/Temporal deadline ordering at 30/40/55/60 seconds.

## Working Set

- `packages/cloudflare-hosted-control/src/client.ts`
- `packages/cloudflare-hosted-control/test/client.test.ts`
- `apps/cloudflare/src/worker/route-handlers/telegram-send.ts`
- `apps/cloudflare/test/worker-telegram-send-route.test.ts`
- `apps/cloudflare/src/worker-contracts.ts`
- `apps/cloudflare/src/hosted-env-policy.ts`
- `apps/cloudflare/deploy-automation/worker-secret-names.ts`
- `.github/workflows/deploy-cloudflare-hosted.yml`
- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- `apps/web/test/hosted-orchestration-reconciliation-facts.test.ts`
- `packages/assistant-engine/src/assistant/channels/runtime.ts`
- focused Telegram channel tests and deployment/security documentation

## Verification Plan

- Add direct signing/tamper/freshness tests and a Worker route authorization test.
- Add a streamed Telegram response-body deadline regression test.
- Run focused owner tests, owner typechecks, diff coverage, required
  security/privacy and coverage-write audits, exact-head CI, and another Mountain
  ReviewGPT round.

## Progress

- Round 52 checked PR #465 at `865abf3900ca0562b8b530717417caf762e646a8`
  with GPT-5.6 Pro and returned two High findings.
- Static tracing reproduced both findings: the simplification pass removed the
  signed authority envelope, and the provider timeout was cleared after response
  headers rather than after the response body.

Status: active
Updated: 2026-07-10
