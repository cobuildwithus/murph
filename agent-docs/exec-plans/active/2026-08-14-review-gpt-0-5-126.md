# Update ReviewGPT dependency to 0.5.126

Status: active
Created: 2026-08-14
Updated: 2026-08-14

## Goal

- Adopt ReviewGPT 0.5.126 so Murph review runs use the corrected attachment staging and exact browser-target cleanup behavior.

## Success criteria

- The workspace catalog, root dependency range, and lockfile resolve `@cobuild/review-gpt` 0.5.126.
- Murph keeps its existing browser lanes and configuration; lifecycle ownership stays inside ReviewGPT.
- Focused dependency proof, required review gates, and exact-head CI pass.

## Scope

- In scope: dependency metadata, lockfile, exact-version coverage, and this execution plan.
- Out of scope: Murph-owned browser lifecycle logic, product runtime behavior, prompts, UI, or deployment configuration.

## Constraints

- Use the published 0.5.126 package from the public registry.
- Keep the update limited to the package version and integrity unless a focused check proves another consumer seam must change.
- Preserve the existing shared pnpm store and managed-browser configuration.

## Risks and mitigations

1. Risk: package installation changes unrelated dependency resolutions.
   Mitigation: update only ReviewGPT, inspect the complete lockfile diff, and reject unrelated churn.
2. Risk: Murph duplicates lifecycle ownership already implemented by ReviewGPT.
   Mitigation: change only the existing dependency/version seams and leave browser-target policy in the package.

## Tasks

1. Completed: update the ReviewGPT catalog/range and regenerate its lockfile resolution.
2. Completed: update exact-version and lifecycle coverage and run focused dependency checks.
3. In progress: inspect the final diff, commit, push, and open the PR while keeping the plan active for review and CI.
4. Pending: complete required ReviewGPT gates and exact-head CI.

## Decisions

- ReviewGPT remains the sole owner of attachment ordering and browser-target lifecycle behavior; Murph only consumes the fixed package.

## Verification

- Passed: frozen lockfile validation and installed-package proof (`0.5.126`).
- Passed: the focused installed-package ReviewGPT contract test, including attachment-before-prompt and handled-signal target cleanup.
- Passed: dependency policy and ignored-build review; the complete lockfile diff changes only ReviewGPT's version, integrity, and snapshot keys.
- `pnpm deps:audit` reports the repository's existing advisory set; this update changes no transitive dependency resolution.
- The full CLI release coverage file reached 41 passed and 1 skipped before the audit-bundle test exceeded its 120-second cap under host filesystem contention; the one package-contract failure was corrected and its focused rerun passed.
- The CLI typecheck remains red in unrelated assistant and vault packages on the current base; none of the reported paths are changed by this task. Exact-head CI owns the broad verification surface.
