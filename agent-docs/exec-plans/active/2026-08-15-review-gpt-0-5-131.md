# Update ReviewGPT dependency to 0.5.131

Status: active
Created: 2026-08-15
Updated: 2026-08-15

## Goal

- Adopt the latest published ReviewGPT package for subsequent repository review gates.

## Success criteria

- The root dependency, supply-chain exception, installed package, and lockfile resolve `@cobuild/review-gpt` 0.5.131.
- The exact-version regression test guards both manifest seams.
- Focused dependency verification passes and the pushed PR head is reviewed with the updated package.

## Scope

- In scope: dependency metadata, lockfile, exact-version test, and this execution plan.
- Out of scope: Murph production behavior, ReviewGPT implementation internals, browser-lane policy, prompts, UI, and deployment configuration.

## Constraints

- Use the public-registry package tagged `latest` at update time.
- Keep lockfile movement limited to ReviewGPT's direct resolution.
- Do not use the unavailable Eragon browser lane.
- This is internal review tooling, so no member-facing changelog entry applies.

## Risks and mitigations

1. Risk: installation changes unrelated dependency resolutions.
   Mitigation: update only ReviewGPT and inspect the complete manifest and lockfile diff.
2. Risk: a completed review targets the older PR head.
   Mitigation: treat the earlier review as stale and start the next required review from the exact pushed dependency-update head.

## Tasks

1. Completed: update the ReviewGPT dependency seams and regenerate the lockfile.
2. Completed: run focused package, dependency-policy, audit, ignored-build, documentation, and typecheck proof.
3. Completed: inspect the privacy-safe diff, commit, push, and update the PR while keeping this plan open for the exact-head gate.
4. In progress: run the required ReviewGPT gate on the exact pushed head without Eragon.

## Decisions

- Keep the update consumer-only; Murph does not duplicate behavior owned by the ReviewGPT package.

## Verification

- Passed: the public registry reported `0.5.131` as `latest`, and the installed CLI reports `0.5.131`.
- Passed: `pnpm install --frozen-lockfile --offline`; the lockfile changes only ReviewGPT's importer version, package integrity, and snapshot key.
- Passed: the focused ReviewGPT package-backed contract test.
- Passed before the base update: the complete CLI release coverage audit with 44 passed and 1 skipped.
- Passed after merging the latest base: the offline frozen install resolves repo-tools 0.1.17 and ReviewGPT 0.5.131; the complete CLI release coverage audit passes 45 tests with 1 skipped; the focused phone-call suites and affected runtime tests pass; and the workspace typecheck passes after adapting three test fixtures to the base's required provider-turn identity.
- Passed after the second base update: the sole release-audit conflict resolves to the current base assertion shape; the full CLI audit passes 45 tests with 1 skipped; the two long browser-config cases also pass together under their normal timeout; and the workspace typecheck and documentation drift guard pass.
- Round 12 attached the full guarded snapshot on Vonneumann and ran past the
  450-second trust floor, but the captured response reported
  `MODEL_CONFIRMATION: UNKNOWN` with response slug `gpt-5-6-pro`, so it is not
  accepted as the required Sol gate. Its one stop-reconciliation finding was
  independently confirmed and remediated; the exact corrected head still needs
  a fresh non-Eragon Sol review.
- Passed: `pnpm deps:guard`, `pnpm deps:ignored-builds`, `pnpm docs:drift`, and `pnpm typecheck`.
- `pnpm deps:audit` reports the repository's current advisory set. ReviewGPT 0.5.127 and 0.5.131 declare the same three direct dependencies, and this update changes no transitive resolution.
- Passed: `git diff --check`.
