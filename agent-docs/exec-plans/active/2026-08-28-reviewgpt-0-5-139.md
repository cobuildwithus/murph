# Install ReviewGPT 0.5.139

Status: active
Created: 2026-08-28
Updated: 2026-08-28

## Goal

- Install the verified `@cobuild/review-gpt` 0.5.139 release so Murph's Frog
  repair lane rejects a notes attachment when a response declares a missing
  patch artifact.
- Preserve Murph's dependency supply-chain controls with one exact,
  reviewable release-age exception rather than a command-line bypass.

## Scope

- In scope: the root ReviewGPT dev dependency, its lockfile resolution, and the
  existing exact ReviewGPT `minimumReleaseAgeExclude` entry.
- Out of scope: deployed product behavior, runtime dependencies, ReviewGPT
  implementation changes, other dependency updates, and general supply-chain
  policy changes.

## Constraints

- Use only the public registry release already verified from the owning source,
  tag, GitHub release workflow, npm metadata, and published tarball.
- Rotate the existing exact version exception from 0.5.138 to 0.5.139; do not
  disable or broaden `minimumReleaseAge`, trust policy, build-script controls,
  or dependency verification.
- Keep the package manifest and lockfile aligned in the same change.
- Preserve the unique committed Frog binding for issue #2388.

## Tasks

1. Install exact ReviewGPT 0.5.139 through pnpm with the narrow version
   exception committed in configuration.
2. Review the manifest, lockfile, exception, install-script inventory, and
   dependency audit; prove the installed CLI reports 0.5.139 and contains the
   mismatch guard.
3. Commit and push a draft PR with the Frog issue binding, then run the required
   preliminary coverage ReviewGPT pass, final sensitive ReviewGPT gate, and
   exact-head CI.
4. Merge only after every dependency, review, CI, privacy, and merge-tree gate
   passes; close issue #2388 and retire the clean worktree after verified merge.

## Verification

- `pnpm deps:guard`
- `pnpm deps:audit`
- `pnpm deps:ignored-builds`
- installed ReviewGPT version and published guard inspection
- focused ReviewGPT package integration tests selected by the changed paths
- `git diff --check` and dependency/privacy review
- exact-head ReviewGPT passes, required CI, and current-main merge-tree proof

## Local Evidence

- `pnpm deps:guard`: passed for all 30 package manifests.
- `pnpm deps:ignored-builds`: only the unchanged `bufferutil`,
  `utf-8-validate`, and `msw` scripts remain blocked; ReviewGPT 0.5.139 has no
  install script.
- Registry proof: the exact npm integrity matches the committed lockfile, and
  0.5.138 and 0.5.139 declare the same dependency graph.
- Focused Murph ReviewGPT package integration: 4 tests passed.
- Direct installed-package reproduction: the declared patch plus notes-only
  attachment fails with zero downloads, zero child launches, and failed status.
- `pnpm deps:audit`: remains blocked by the same pre-existing 78 advisories on
  untouched `main` and the candidate (6 low, 37 moderate, 34 high, 1 critical),
  with the exact same ReviewGPT advisory IDs and paths. The version-only
  ReviewGPT package update adds no dependency or advisory.
