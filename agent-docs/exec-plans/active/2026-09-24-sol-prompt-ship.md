# Final GPT-6 Sol prompt review and shipping

Status: active
Created: 2026-09-24
Updated: 2026-09-24

## Intent and authority

User requests a fresh prompting-guidance check, reconciliation with current main, parent and ReviewGPT prompt review, final supported adjustments, and shipping. Continue owned branch `feat/sol-prompt-guidance` at `f391a048bdc3`. No existing PR was found; create a draft and use the normal protected merge workflow.

## Plan

1. Recheck official GPT-6 prompting guidance and inspect current prompt/skill owners.
2. Merge verified main, preserve both sides' supported behavior, and resolve overlapping test changes against their actual invariants.
3. Run affected deterministic checks, typechecks, and focused GPT-6 Sol subscription journeys. Preserve earlier evidence: 369 retained live cases passed across the previous full run and focused replays, not one green full invocation.
4. Review the complete candidate, prepare accurate PR evidence, push and open a draft PR, then run final ReviewGPT against the exact pushed head concurrently with required CI.
5. Resolve supported findings within current authority, finish checks and parent review, merge through protections, and verify the resulting release workflow status.

## Guidance

Fresh official source: https://developers.openai.com/api/docs/guides/latest-model/gpt-6-astra.md#prompting-best-practices . Family-wide starting guidance is based on Astra observations and must be evaluated on Sol. Apply authorized follow-through, focused consequential clarification, explicit instruction priority, concise application-specific prose, and calibrated verification. Preserve mandatory health, consent, privacy, external-effect, and channel boundaries. Do not import coding-agent delegation or deployment examples into the member assistant.

## Progress

- Verified clean owned checkout; no PR exists for this branch.
- Fetched main with an explicit destination refspec and verified remote identity: `82aa9007d8a83ebc2ecdab69d4d7635bb0b43edd`.
- Main contains overlapping onboarding, habit follow-through, contextual-model defaults, and live fixture changes; inspect reconciliation directly.

- Resolved only live-test conflicts: retain the obsolete routing-test deletion and explicit accepted Goal dates, preserve main's real opening/stall journeys and model defaults, and combine automation availability with email-channel fixture support.
- Parent inspected the initiative, style, support-privacy, duration, onboarding, and follow-through guidance. Main's proactive support offer remains distinct from scheduling consent; the existing initiative text preserves mandatory handoffs and channel silence.
- The merged route prompt exceeded its existing limit by two characters. Shortened “a newly stated duration” to “an explicit duration”; behavior and the existing ceiling remain unchanged.
- Merged deterministic proof: 274 passed; engine/CLI typechecks, prepared runtime build, and complexity guard passed. Nineteen affected live journeys are running, including main's explicitly named Luna canary while Sol remains the default.
- Fresh complete scripted-provider captures compare the exact main prompt module with the candidate against unchanged adjacent owners: individual 151,196 to 151,892 bytes (+696, +0.4603%); group 127,865 to 128,303 (+438, +0.3425%). Only the developer-instruction item differs after path/installation normalization. Temporary baseline modules were removed. The fixture omits generated CLI guidance, verified separately through CLI rendering; exact Sol token counts are unavailable.
- Final merged live sampling exposed two incidental wording rejections. Accept “report” as well as “issue” for a saved support report, and explicit refusal to confirm a booking until approval as pending. Existing canonical effects, private-data exclusions, and completed-booking negative checks remain. All 62 deterministic live-harness tests pass after these changes.

- One live privacy replay showed that “care locations” did not reliably exclude a generic clinic setting. Revised the same prompt sentence to name care settings such as clinics and illustrate a product-only connected-source failure summary; the private-context exclusion assertion remains unchanged. All 121 prompt/feedback contracts pass.
- Kept the saved-default journey focused by supplying exact recent workout finish instants. The composed Goal journey now has an explicit accepted plan start date, and the empty-vault case accepts one relevant sleep/authorization clarifier while preserving zero canonical writes and no false completion claim.
- Support receipt checks now recognize affirmative saved-for-triage meaning without requiring one noun; a negated save remains rejected. All 62 deterministic harness tests pass; latest engine typecheck and complexity guard pass.
- Final privacy wording adds 694 bytes (+0.4590%) to the captured individual request and 436 (+0.3410%) to the group request versus main. No new production branch, tool call, state, dependency, or deploy boundary.

- Workout default and terminal support-error replays now pass, including canonical effects and truthful delivery status. A rejected workout payload may recover only if canonical results are correct; unexpected runtime errors remain failures.
- Goal replay exposed a fixture clock conflict: prompt/handler time was August while the canonical CLI used the current date. The fixture now uses the real clock and derives future accepted dates through existing timezone helpers, preserving exact schedule equality and duplicate prevention.
