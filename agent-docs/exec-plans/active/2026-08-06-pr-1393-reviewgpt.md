# Complete ReviewGPT and CI for PR #1393

Status: active
Created: 2026-08-06
Updated: 2026-08-07

## Goal

- Bring draft PR #1393 from its current pushed head through the repository's
  exact-head ReviewGPT, focused verification, GitHub Actions, and final-review
  gates without merging it.

## Success criteria

- Focused research composes over the existing compact profile contract, has
  direct regression proof for scientific Roman numerals and publication-year
  ranges, and rejects arbitrary question prose before provider work.
- The PR body matches the current completion-workflow intent, architecture,
  verification, deployment-skew, and five-category change-shape contract.
- The preliminary completion-specialists pass is substantive and all accepted
  findings are resolved.
- The final ReviewGPT loop reaches `ROUND_OUTCOME: PASS` on the resulting patch
  with zero locally accepted findings.
- Required GitHub Actions are green on the final exact PR head, and the branch
  has no unresolved merge conflict with its configured base.
- The PR remains draft and unmerged.

## Scope

- In scope: the existing Exa research-scout contract, CLI/client path,
  assistant guidance, hosted Worker canonicalization boundary, focused tests,
  PR documentation, ReviewGPT artifacts, and exact-head CI.
- Out of scope: merging the PR, adding a second research/provider stack,
  broadening the Exa API surface, persisted research state, or unrelated
  cleanup.

## Constraints

- Technical constraints: preserve legacy tag-profile wire behavior, add only
  one optional focused-mode discriminator to that object schema, reject
  arbitrary question prose before provider work, and retain Worker-owned
  credential injection plus canonical request reconstruction.
- Product/process constraints: use the isolated PR worktree, preserve unrelated
  work, run the preliminary and final ReviewGPT stages against clean pushed
  heads, keep their state independent, run ReviewGPT concurrently with CI, and
  do not merge.

## Risks and mitigations

1. Risk: a prose classifier blocks scientific phrases such as `phase I` while
   still allowing private names in alternate casing.
   Mitigation: delete the arbitrary-prose surface, synthesize the focused query
   from compact categories, and prove public scientific/date scopes plus
   arbitrary-question rejection through the canonical contract/client path.
2. Risk: ReviewGPT evaluates stale code or an incomplete PR intent contract.
   Mitigation: preflight every pass against the exact pushed head and refresh
   the PR body before establishing the immutable final baseline.
3. Risk: review remediation adds unnecessary architecture.
   Mitigation: reproduce findings first and accept only the smallest fix at an
   existing owner boundary.
4. Risk: Worker and warm runner/container bundles deploy out of sync.
   Mitigation: retain fail-closed compatibility, document the safe deployment
   order, and require focused post-deploy proof in the PR body.

## Tasks

1. Inspect the full base-to-head diff, current PR body, source paths, tests,
   generated/schema surfaces, and exact remote head.
2. Add or confirm the missing scientific-Roman-numeral regression and resolve
   command-manifest wording drift when it is part of the shipped help surface.
3. Run the smallest focused tests, typechecks, generated-artifact checks, and
   direct canonical request/rejection scenarios for the changed owners.
4. Re-read the complete candidate diff, refresh the PR body and change-shape
   evidence, commit through the scoped repository helper, push, and preflight.
5. Start the completion-specialists pass and final ReviewGPT round 1 on the
   same exact head while GitHub Actions runs.
6. Triage every finding, reproduce accepted issues, land the smallest combined
   remediation, rerun focused proof, push, and run required correction rounds.
7. Complete the parent final review, verify final-head CI and mergeability, then
   close this plan through the repository finish-task path while leaving the PR
   draft and unmerged.

## Decisions

- Treat the handoff's claimed Roman-numeral source correction as unconfirmed:
  the current remote head ends at the generic first-person regression commit,
  so the branch itself is the authority.
- The preliminary lenses are product experience, prompt, and coverage;
  frontend is not applicable.
- The final ReviewGPT gate applies because the change crosses a provider
  egress/credential trust boundary and the user explicitly requested it.
- Final round 1 and the preliminary specialist pass independently reproduced a
  provider-bound third-person privacy gap. Round 2 proved that the attempted
  capitalization-based person-name filter still allowed lowercase and all-caps
  private names, so the same unsafe mechanism remained.
- The required round-3 anomaly retrospective is recorded on the PR. Compared
  with the first-reviewed head, round-2 remediation added 70 and deleted 5
  source lines, added 348 and deleted 1 test lines, and added 16 doc lines.
  The decision is redesign by deletion: arbitrary question prose and its
  classifier are removed. One `mode: "focused"` discriminator now composes
  with the existing compact tag profile, and the canonical provider question
  is synthesized locally. No owner or compatibility path is added.
- Research selection and evidence rules now live in capability-gated turn
  context instead of the always-resident behavior prompt. Unconfigured routes
  omit both that guidance and the research CLI family.
- Candidate interpretation remains the assistant's job, but the configured
  guidance requires a real `resultIndex` mapping to usable source metadata and
  stops without raising confidence when no such source exists.

## Verification

- Commands to select after full diff inspection: focused contracts, CLI,
  assistant-engine, and Cloudflare Worker tests; owner typechecks; generated
  schema or artifact drift proof; `git diff --check`; ReviewGPT exact-head
  preflight; required GitHub Actions.
- Expected outcomes: focused compact scopes including scientific Roman
  numerals pass, arbitrary question prose and private/member-identifying input
  fail before fetch across casing variants, legacy tag requests remain
  byte-for-byte on their existing recipe, canonical Worker
  reconstruction strips caller headers and injects only the Worker credential,
  both ReviewGPT stages resolve cleanly, and final-head CI is green.
- Remediation proof completed: focused contracts, CLI, assistant, and Worker
  tests pass; the real scripted Codex app-server journey covers generalized
  provider input, visible progress, sourced candidate mapping, final delivery,
  and empty-result recovery; all four owner typechecks pass. The sanitized
  exact-provider-input measurement is +241 tokens for both direct and group
  requests (direct: 36,410 to 36,651; group: 28,127 to 28,368), remaining
  below the one-percent completion threshold.
