# Simplify the opening signup reply

Status: active
Created: 2026-09-23
Updated: 2026-09-23

## Outcome and invariants

Keep the direct opening conversational and reduce work before the aspiration question. Preserve accepted identity facts through the existing canonical child save, truthful save claims, immediate-request priority, and finite later recovery. No new state, tools, services, or scheduler.

## Evidence and smallest change

The opening instruction requires a foreground automation save in addition to identity delegation. This requires tool discovery and an additional provider continuation for an optional early nudge. Delete that recipe and its redundant references. Keep identity persistence and existing managed recovery. Separately investigate the canary exact-identity mismatch; do not relax its assertion without evidence.

## Product UX

Entry: a new direct member answers or skips the bundled identity question. Outcome: the next health aspiration question arrives without scheduling a first-minutes nudge. Supplied facts are saved through the existing leaf; skips do not fabricate facts. Immediate requests and later-stage resume keep their owners. Existing already-saved one-shots retain their normal finite execution; no production data migration is added.

## Proof and delivery

1. Update the injected owner, routed skill references, durable product spec, and deterministic assertions.
2. Keep the automation tool available in the real synthetic journey and prove zero calls, one identity child, canonical readback, and an honest reply.
3. Run focused tests and typecheck; capture full provider input where the local provider lane permits it.
4. Scoped PR, required exact-head CI and review, managed release, then production canary.

## Risks and status

Provider timing is stochastic. A local run cannot prove hosted delivery latency. Subscription reproduction currently fails before model actions; try the authorized alternate profiles, without changing model or reading credentials. The latest production canary also stops at the identity-question exact-copy check; that separate boundary remains under investigation.

## Focused evidence

- 113 deterministic prompt and skill tests passed; seven existing unrelated tests skipped. Assistant-engine project-reference typecheck passed.
- Credential-free real App Server request capture, identical synthetic direct/group fixtures with the shipped GPT-6 Sol catalog: direct input 160,733 to 158,553 UTF-8 bytes (-2,180; -1.36%); group input unchanged at 140,618 bytes. All provider fields except transport cache identity included. Exact model tokenizer unavailable, so no token count is claimed. Temporary capture fixture removed after proof.
- Local real-model journey attempted on all available subscription profiles; no successful model response. Failures were authentication, unavailable-provider, or startup timeout before action. Live UX verdict remains Hold.
- Canary failure classification is isolated in a separate diagnostic PR; it does not weaken the exact-copy assertion or change production replies.
