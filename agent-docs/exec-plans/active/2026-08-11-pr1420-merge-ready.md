# Resolve PR 1420 conflicts and complete merge gates

Status: active
Created: 2026-08-11
Updated: 2026-08-11

## Goal

- Bring PR 1420 onto current `main` without changing its intended supporter-list
  behavior, prove the resulting funding-page and sponsorship-store paths, and
  complete every required CI and ReviewGPT gate for merge readiness.

## Success criteria

- The PR head contains an ordinary, inspected conflict resolution against current
  `origin/main` and GitHub reports no merge conflict.
- Focused component/store tests and the required frontend design proof pass.
- The PR body accurately declares product, frontend, prompt, and coverage lenses,
  changelog disposition, context sensitivity, change shape, and direct evidence.
- Preliminary specialist ReviewGPT and the routed final ReviewGPT gate have no
  unresolved accepted findings.
- All required GitHub checks are green on the final pushed head and a current-base
  merge-tree proof is clean.

## Scope

- In scope: the existing PR diff, behavior-preserving conflict resolution, focused
  proof, required audit/CI remediation, and PR metadata needed by current gates.
- Out of scope: unrelated group-funding redesign, new billing behavior, deployment,
  or merging the PR without a separate explicit merge instruction.

## Constraints

- Technical constraints: preserve Web-owned funding and supporter projection
  authority; do not introduce a second store, state owner, or compatibility layer.
- Product/process constraints: preserve unrelated work, use the authorized task
  worktree, keep the first-reviewed ReviewGPT baseline immutable, run ReviewGPT
  concurrently with CI, and use only current owner docs for conflict decisions.

## Risks and mitigations

1. Risk: current `main` changed the funding page or sponsorship-store contracts.
   Mitigation: inspect both sides and affected call paths, then resolve each hunk
   against current ownership and focused tests rather than choosing a side wholesale.
2. Risk: the PR's historical WIP/audit payload obscures the real product patch.
   Mitigation: classify every file, retain only artifacts still required by a current
   repository owner, and reject speculative cleanup outside the PR's outcome.
3. Risk: a base update creates new behavior and invalidates prior review evidence.
   Mitigation: inspect the complete base-to-head diff, document any authored
   remediation, and run current required ReviewGPT stages on the exact pushed head.

## Tasks

1. Inspect the PR intent, current diff, comments, checks, and exact conflict set.
2. Merge current `origin/main` normally and resolve conflicts with the smallest
   behavior-preserving patch.
3. Run focused tests, frontend design proof, typecheck/build slices if required,
   and inspect the final diff and privacy boundary.
4. Update and push the PR contract, then launch preliminary and final ReviewGPT
   against the same exact candidate while CI runs.
5. Resolve findings and CI failures, re-run focused proof, push as needed, and
   prove current-base merge readiness with required checks green.

## Decisions

- Use a merge commit for the base update so the existing published PR history is
  preserved and no force push is needed.
- Treat the change as user-facing frontend plus payment-adjacent data projection;
  the preliminary product/frontend/coverage lenses and final cross-cutting gate apply.

## Verification

- Commands: focused funding-page and sponsorship-store tests, frontend design proof,
  applicable typecheck/build checks selected from the verification owner docs,
  ReviewGPT preflight and both required review stages, required GitHub checks, and
  a current-base merge-tree proof.
- Expected outcomes: every focused command passes, both audits return their required
  PASS markers with no accepted findings outstanding, required exact-head checks are
  green, and the current PR head merges cleanly with current `origin/main`.
