# Resolve PR 1420 conflicts and complete merge gates

Status: completed
Created: 2026-08-11
Updated: 2026-08-11

## Goal

- Bring PR 1420 onto current `main` without changing its intended supporter-list
  behavior, prove the resulting funding-page and sponsorship-store paths, and
  complete every required CI and ReviewGPT gate for merge readiness.

## Success criteria

- The PR head contains an ordinary, inspected conflict resolution against current
  `origin/main` and GitHub reports no merge conflict.
- Focused component/store tests and the required frontend design proof pass.
- The PR body accurately declares product, frontend, prompt, and coverage lenses,
  changelog disposition, context sensitivity, change shape, and direct evidence.
- Preliminary specialist ReviewGPT and the routed final ReviewGPT gate have no
  unresolved accepted findings.
- All required GitHub checks are green on the final pushed head and a current-base
  merge-tree proof is clean.

## Scope

- In scope: the existing PR diff, behavior-preserving conflict resolution, focused
  proof, required audit/CI remediation, and PR metadata needed by current gates.
- Out of scope: unrelated group-funding redesign, new billing behavior, deployment,
  or merging the PR without a separate explicit merge instruction.

## Constraints

- Technical constraints: preserve Web-owned funding and supporter projection
  authority; do not introduce a second store, state owner, or compatibility layer.
- Product/process constraints: preserve unrelated work, use the authorized task
  worktree, keep the first-reviewed ReviewGPT baseline immutable, run ReviewGPT
  concurrently with CI, and use only current owner docs for conflict decisions.

## Risks and mitigations

1. Risk: current `main` changed the funding page or sponsorship-store contracts.
   Mitigation: inspect both sides and affected call paths, then resolve each hunk
   against current ownership and focused tests rather than choosing a side wholesale.
2. Risk: the PR's historical WIP/audit payload obscures the real product patch.
   Mitigation: classify every file, retain only artifacts still required by a current
   repository owner, and reject speculative cleanup outside the PR's outcome.
3. Risk: a base update creates new behavior and invalidates prior review evidence.
   Mitigation: inspect the complete base-to-head diff, document any authored
   remediation, and run current required ReviewGPT stages on the exact pushed head.

## Tasks

1. Inspect the PR intent, current diff, comments, checks, and exact conflict set.
2. Merge current `origin/main` normally and resolve conflicts with the smallest
   behavior-preserving patch.
3. Run focused tests, frontend design proof, typecheck/build slices if required,
   and inspect the final diff and privacy boundary.
4. Update and push the PR contract, then launch preliminary and final ReviewGPT
   against the same exact candidate while CI runs.
5. Resolve findings and CI failures, re-run focused proof, push as needed, and
   prove current-base merge readiness with required checks green.

## Decisions

- Use a merge commit for the base update so the existing published PR history is
  preserved and no force push is needed.
- Treat the change as user-facing frontend plus payment-adjacent data projection;
  the preliminary product/frontend/coverage lenses and final cross-cutting gate apply.
- Preserve current privacy ownership by showing recognition only to a signed-in
  active group participant. Signed-out visitors and authenticated non-participants
  do not run or receive the supporter projection.
- Adapt the release note to the current fragment registry under
  `apps/web/changelog/entries/` instead of restoring the retired central catalog.
- Expose only the retained public alias or `Anonymous` and monthly versus
  one-time kind. Amounts, caps, payer identity, payment state, balances, charge
  timing, and refill history remain private.

## Verification

- Commands: focused funding-page and sponsorship-store tests, frontend design proof,
  applicable typecheck/build checks selected from the verification owner docs,
  ReviewGPT preflight and both required review stages, required GitHub checks, and
  a current-base merge-tree proof.
- Expected outcomes: every focused command passes, both audits return their required
  PASS markers with no accepted findings outstanding, required exact-head checks are
  green, and the current PR head merges cleanly with current `origin/main`.

## Progress

- Merged the original PR head and two current `origin/main` snapshots with
  ordinary merge commits. The changelog conflict kept the current fragment
  loader and added one isolated `2026-08-11` entry. GitHub reports the PR
  mergeable, and a current-base `git merge-tree --write-tree` proof is clean.
- Preserved recognition only for signed-in active group participants, limited
  the projection to the current monthly activation plus 20 fulfilled one-time
  purchases, and exposed only exact-consent aliases or `Anonymous` with the
  coarse contribution kind.
- Remediated review findings by separating exact alias consent and first-settlement
  publication metadata, streaming optional supporter work after the primary
  controls, retaining the bounded legacy-readable alias ciphertext and digest,
  and deleting both the incompatible JSON consent envelope and retroactive
  replay-publication branch. Preceding-Web or authority-denied settlements stay
  anonymous permanently.
- Passed 394 assertions across 10 affected Vitest files, the Hosted Web prepared
  typecheck, focused ESLint with no warnings or errors, all 10 frontend
  design-proof policy tests, and a two-viewport repository Playwright proof.
- Captured and inspected the real supporter and consent states at desktop
  1440/2x and mobile 390/3x, uploaded all four through the hosted `designproof`
  variant, and confirmed the hosted images preserve the local output.
- The required fresh Claude Fable 5 UI check was attempted after the rendered
  state stabilized and returned explicit usage-credit exhaustion. Per the
  completion workflow, no alternate Claude request or local substitute applies.
- Preliminary ReviewGPT returned findings at
  `https://chatgpt.com/c/6a7acb18-f8c0-83ea-84c0-fa0d7198a8ad` for consent,
  optional-path latency, audience coverage, and query-shape proof; all were
  accepted and remediated.
- Final ReviewGPT round 1 returned findings at
  `https://chatgpt.com/c/6a7acb35-9254-83ea-9a08-a73b459339ca` for settlement
  publication ownership and optional-path latency; both were accepted and
  remediated. Round 2 returned findings at
  `https://chatgpt.com/c/6a7adaa5-e1cc-83ea-abae-6da358e97862` for base-parser
  incompatibility and overstated timeout semantics; both were accepted and
  remediated. Round 3 required the anomaly retrospective at
  `https://chatgpt.com/c/6a7af1d0-89e8-83ea-9642-cbe3ca20445d` and exposed
  retroactive replay publication; the retrospective chose shrinking and
  continuation, and the replay branch was deleted.
- Final ReviewGPT round 4 returned `ROUND_OUTCOME: PASS` with no qualifying
  findings at `https://chatgpt.com/c/6a7af887-4cf0-83ea-b767-4f5db0d30cc8` after
  about 62 minutes. The requested `gpt-5.6-sol` model was verified as response
  slug `gpt-5-6-pro`, and the complete artifact was substantive and consistent
  with the guarded snapshot. Three description-only wording discrepancies were
  corrected without changing behavior.
- All required GitHub checks passed on the final behavior-bearing head
  `e4aeb06a0081a3a5b822f37cbea4952b801e1424`. The remaining closure is the
  plan-and-wording-only final commit, its exact-head CI confirmation, and the
  final current-base merge-tree recheck.
Completed: 2026-08-11
