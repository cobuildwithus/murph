## Goal

Get the release candidate back to a truthful green state, then prepare and execute the next patch release without trampling unrelated in-flight work.

## Scope

- `packages/hosted-execution/**`
- release-candidate root manifest changes already present in `package.json`, `pnpm-lock.yaml`, and `pnpm-workspace.yaml`
- release artifacts generated by `scripts/release.sh`

## Constraints

- Preserve unrelated dirty worktree edits, especially active plan and ledger rows owned by other lanes.
- Prefer the smallest proof-oriented test changes that restore the required coverage thresholds.
- Do not ship generated residue like transient `next-env.d.ts` rewrites or other active-lane artifacts as part of this release.

## Verification

- `pnpm --dir packages/hosted-execution test:coverage`
- `pnpm release:check`
- required completion-workflow audits for a standard repo change before release handoff
Status: completed
Updated: 2026-04-18
Completed: 2026-04-18
