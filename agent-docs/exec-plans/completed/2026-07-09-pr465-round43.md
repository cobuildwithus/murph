# PR 465 ReviewGPT Round 43

Goal: close the accepted ReviewGPT round-43 High findings for PR 465.

Constraints:
- Pre-control Telegram usage-limit notice failures must remain retryable.
- Post-control ambiguous failures must remain terminal unknown to avoid duplicate sends.
- Legacy guard reservation must not durably suppress a period notice without the current period claim.
- Keep the fix narrow; do not perform a broad delivery-protocol rewrite in this PR.

State:
- Stale Telegram provider-dispatch rows without durable provider outcome are reclaimed as pre-control attempts instead of terminalized.
- Legacy guard rows and the current period claim are created in one store transaction with duplicate-safe inserts.
- Orphan stale legacy guard rows no longer suppress a current Telegram runtime notice.

Plan:
- Rerun ReviewGPT after commit and push.

Verification:
- `pnpm --dir apps/web test -- hosted-onboarding-linq-observability-store.test.ts`
- `pnpm typecheck`
- `pnpm test:apps`
- `git diff --check`

Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
