# Upgrade runtime Codex CLI to 0.153.4

Status: completed
Created: 2026-09-05
Updated: 2026-09-05

## Goal

Pin the latest stable Codex CLI throughout the runtime and prove that Murph's existing model selection, native tools, thread continuity, and provider egress remain compatible.

## Scope and ownership

Use the existing assistant-engine dependency, native runner base, image identity, and upstream egress inventory. Preserve the current product model catalog and saved custom-inference verification profile. No new state owner or compatibility layer is required.

## Evidence and decisions

- npm latest and the official OpenAI changelog identify 0.153.4 as the stable release on 2026-09-05.
- The baseline pins 0.151.0 across package, Docker, and source-review contracts.
- The portable-responses verification profile is persisted and compared across Web/Worker consumers. Preserve that compatibility identity if protocol proof passes; changing its embedded historical version would invalidate existing verified connections during rollout.
- Upstream provider source changes only preserve response usage metadata and add realtime history events; endpoint paths and dispositions are unchanged. The reviewed tag resolves to commit `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a`, provider tree `f89292b9cf6a6f0c316c7d88a47f111a20dc0015`.
- Remove the obsolete Astra-from-Sol catalog synthesis because the native 0.153.4 catalog supplies Astra. Retain standard versus authorized Astra filtering, Flex, mixed Code Mode, and fail-closed context validation.
- Retain pnpm-generated Codex versions and integrity hashes; restore unrelated ESLint peer snapshot normalization. Frozen installation passes after that restoration.

## Risks and proof

- Native provider routes and app-server protocol drift: review source diff and run the existing real-binary egress and scripted Responses suites.
- Runner skew: update image identity with the base dependency; retain existing version-fenced container lifecycle and model filtering. No Web/Worker wire format or database migration is intended.
- Dependency drift: refresh through pnpm and inspect the lockfile for unrelated churn.
- Local proof: focused runtime/config/container contracts and relevant typechecks; compare complete individual/group provider input if affected.
- Required exact-head CI owns broad acceptance and hosted integration. Start final ReviewGPT concurrently with CI after the stable candidate is pushed.

## Tasks

1. Review upstream changes and update all executable version owners.
2. Run focused compatibility proof and typechecks; inspect the complete candidate.
3. Close this plan, commit, push, open the PR, and complete required review/CI gates.

## Verification

- Passed: 28 focused Cloudflare egress, schema, image-catalog, and custom-provider tests.
- Passed: assistant-engine, Cloudflare, and hosted-execution typechecks.
- Passed: pinned upstream-source verification and dependency policy guard.
- Passed: complexity diff with no changed-source hotspots above 20.
- Dependency audit reports 93 pre-existing vulnerabilities (1 critical, 42 high, 43 moderate, 7 low) in unchanged dependencies; Codex has no transitive runtime dependency besides its optional platform packages. Reviewed ignored build scripts without adding execution exceptions.
- Native runtime suite: 113 of 114 initially passed. The failed fixed-sleep DST withdrawal reproduced on 0.151.0; after replacing the sleep with an explicit response barrier, the same test passes on both releases. Preserve the no-write and exact final-response assertions.
- App-server source comparison: initialize, thread start/resume, turn start/steer/interrupt, compact, dynamic-tool request/response, token-usage, and structured-error types are unchanged. Additive Thread model/reasoning metadata, agent-message questions, auth-recovery notifications, and raw usage metadata are accepted or excluded at their existing owners.
- Passed: 55 focused protocol and native Astra async-question regressions; 3 runtime metadata/auth-recovery/privacy checks. No production adapter compatibility layer is needed.
- Passed: focused live Terra subscription continuity journey; one provider request recalled a synthetic detail from 60 committed messages and produced one correct reply.
- Complete first-request measurements using the production prompt builder and actual binaries against a local scripted Responses endpoint: individual 30,161 -> 29,910 estimated tokens, 137,025 -> 135,947 bytes; group 26,315 -> 26,057 estimated tokens, 118,542 -> 117,477 bytes. Captured all request fields, including generated Code Mode guidance. Used gpt-tokenizer 3.4.0 o200k_base as an explicit estimate, not server billing tokens; normalized local paths, fixture timestamps, and prompt-cache identity. Other request metadata remains included.
- Passed: final assistant-engine typecheck after protocol proof additions. Cloudflare and hosted-execution typechecks passed earlier with unchanged runtime source.
- Passed: a synthetic persisted native thread created on 0.151.0 resumed on 0.153.4, then resumed again on 0.151.0 with both prior turns intact. This exercised the real upgrade/downgrade path, not two current-version processes.
- External final ReviewGPT and required exact-head CI follow the pushed PR; deployment is outside this task.
Completed: 2026-09-05
