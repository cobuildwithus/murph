Goal (incl. success criteria):
- Fix failing CI checks on PR #77.
- Success means the current PR head passes the failing app sidebar test and the hosted Codex image/media delivery E2E or an equivalent focused reproduction.

Constraints/Assumptions:
- Preserve unrelated active work and existing PR changes.
- Keep the fix scoped to the failing checks.
- Do not expose secrets, raw identifiers, or local paths in committed artifacts.

Key decisions:
- Mock `HostedPrivyLogout` in the sidebar unit test so CI `NEXT_PUBLIC_PRIVY_APP_ID`
  does not instantiate the real Privy SDK in a non-HTTPS test DOM.
- Keep the Codex image/media E2E focused on dynamic-tool media delivery by binding
  a deterministic Linq chat directly instead of using signup-welcome notification
  delivery as a setup prerequisite.
- Renew platform activity timeout when Cloudflare reports activity expiry before
  the runner's own idle TTL has elapsed, without recording new activity.
- Derive hosted Codex E2E dynamic-tool expectations from the assistant-engine
  public Codex helper so adding a tool cannot silently stale hard-coded strings.
- Include compact hosted recent-log summaries in full-stack E2E failure messages
  so runtime/app-server failures do not collapse into opaque delivery timeouts.

State:
- Patched focused app test setup, hosted image/media E2E setup, hosted
  container early-expiry handling, and E2E dynamic-tool diagnostics.
- Required audits and final post-audit verification are complete.

Done:
- Read required repo workflow, architecture, verification, security, and reliability docs.
- Confirmed CI failures: hosted web sidebar test and hosted Codex image media delivery E2E.
- Reproduced the sidebar failure with CI-style `NEXT_PUBLIC_PRIVY_APP_ID`.
- Reproduced the hosted image/media E2E timeout locally; the welcome notification
  did not create `/chats` and logged a Codex app-server RPC failure before any
  provider request.
- After direct Linq chat binding, reproduced repeated typing indicators with no
  final `/messages` send and no provider requests, matching early container
  lifecycle interruption evidence from the parallel investigation.
- Confirmed CI app failure is the Privy embedded-wallet HTTPS guard in
  `apps/web/test/dashboard-sidebar.test.ts` under CI-style public Privy env.
- Confirmed the stale Codex dynamic-tool expectation would reject app-server
  `thread/start` before assistant provider requests, making the E2E fail late as
  missing Linq delivery.
- Added a public assistant-engine Codex helper for dynamic-tool names and moved
  E2E contract assertions ahead of hosted turns.
- Ran completion audits: security/privacy, simplify, coverage-write,
  deep-review, and task-finish-review.
- Accepted audit proof for renewal failure cleanup and test-helper
  simplification; rejected re-adding a user-id filter to idle destroy logs
  because idle cleanup runs outside invocation log context.
- Re-ran `pnpm typecheck` after audit-driven test edits.
- Re-ran final `pnpm test:apps` after audit-driven test edits.

Now:
- Close the active plan with the scoped commit.

Next:
- Push the PR branch and check GitHub CI.

Open questions (UNCONFIRMED if needed):
- Whether broader repo typecheck exposes unrelated pre-existing failures.

Working set (files/ids/commands):
- PR #77 branch: `codex/gpt-image-2-dynamic-tool`
- CI jobs: `Release app verification (ubuntu)`, `Codex image media delivery E2E`
- `pnpm typecheck`
- `pnpm test:apps`
- `apps/web/test/dashboard-sidebar.test.ts`
- `apps/cloudflare/test/hosted-local-codex-image-media-delivery-e2e.test.ts`
- `apps/cloudflare/test/hosted-local-linq-first-contact-e2e.test.ts`
- `apps/cloudflare/test/helpers/hosted-local-full-stack-scenario.ts`
- `packages/assistant-engine/src/assistant-codex.ts`
- `packages/assistant-engine/src/assistant-codex/dynamic-tools.ts`
Status: completed
Updated: 2026-06-09
Completed: 2026-06-09
