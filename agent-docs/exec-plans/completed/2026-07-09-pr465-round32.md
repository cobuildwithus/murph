Goal (incl. success criteria):
- Resolve PR 465 ReviewGPT round-32 accepted findings.
- Success means stale `provider_dispatch_started` reclaim is transport/source-matched, spend crossing candidates are not suppressed by `limitNoticeSentAt`, focused tests/typecheck/verify pass, ReviewGPT has no accepted findings, and CI stays green.

Constraints/Assumptions:
- `hosted_linq_delivery` owns quota-notice delivery idempotency; `limitNoticeSentAt` is only a post-send marker.
- Stale `attempted` rows remain pre-provider and may be reclaimed by existing policy.
- Stale `provider_dispatch_started` rows may only be retried by the same source because only the same provider idempotency key makes ambiguous dispatch safe.

Key decisions:
- Split stale reclaim predicates for `attempted` and `provider_dispatch_started`.
- Source-match stale dispatch-started reclaim.
- Remove `limitNoticeSentAt` from the crossing-candidate predicate.

State:
- Round-32 fixes implemented and locally verified; ready to archive/commit.

Done:
- Inspected ReviewGPT artifact and current predicates.
- Source-matched stale `provider_dispatch_started` reclaim.
- Removed `limitNoticeSentAt` from the crossing-candidate predicate.
- Added regression coverage for cross-transport dispatch-start reclaim and marker-owned dedupe.
- Passed focused usage/Linq tests, broader PR-focused usage/Linq suite, `pnpm --dir apps/web typecheck`, `git diff --check`, and `pnpm --dir apps/web verify`.

Now:
- Archive the plan and create the scoped commit.

Next:
- Push, rerun ReviewGPT and CI, then merge when gates are clean.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `apps/web/src/lib/hosted-execution/usage-allowance.ts`
- `apps/web/test/hosted-onboarding-linq-observability-store.test.ts`
- `apps/web/test/hosted-execution-usage-allowance.test.ts`
- `audit-packages/pr-465-round-32.md`
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
