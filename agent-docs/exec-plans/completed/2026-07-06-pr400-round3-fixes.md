Goal (incl. success criteria):
- Land PR 400 round 3 fixes without committing: unify default requested group projections on create/reuse and make Murph replies in newsletter group email threads fan out to current authorized group recipients.
- Success means each accepted fix has a production-faithful failing test first, the minimal implementation, passing focused tests, typecheck/build verification for touched surfaces, and no new durable state, managers, queues, or migrations.

Constraints/Assumptions:
- Work only in the current group newsletter worktree on `feat/group-health-newsletter`.
- Do not commit.
- Requests only for group-email defaults on reuse: no auto-grant and no re-grant after revocation.
- Serialized group email thread targets must carry group identity without serializing member email addresses; legacy/direct targets remain compatible.

Key decisions:
- Reuse existing join-policy requested projection merge helper for group default requests.
- Re-resolve group email recipients through the existing web callback at thread-reply send time.

State:
- Active.

Done:
- Read repo routing, architecture, invariants, verification, security, reliability, and group newsletter docs.
- Confirmed branch and clean worktree before starting.
- Added and proved the Fix A failing web test, then merged the default requested projection set on the existing-group reuse path.
- Added and proved the Fix B failing runtime-state/Cloudflare tests, then extended serialized thread targets with group identity and re-resolved group replies through the existing group-recipients callback.

Now:
- Run required verification for touched app/package surfaces.

Next:
- Review the final diff for scope, privacy, and deployment-skew notes; leave the diff uncommitted.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `apps/web/src/lib/hosted-groups/group-store.ts`
- `apps/web/test/hosted-group-store.test.ts`
- `apps/cloudflare/src/hosted-email/transport.ts`
- `apps/cloudflare/test/hosted-email.test.ts`
- `packages/runtime-state/src/hosted-email.ts`
- `packages/runtime-state/test/hosted-email.test.ts`
