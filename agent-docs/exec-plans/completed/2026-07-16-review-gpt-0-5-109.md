# Ingest ReviewGPT 0.5.109 and rerun PR 712 audit

Status: completed
Created: 2026-07-16
Updated: 2026-07-16

## Goal

- Consume the published `@cobuild/review-gpt@0.5.109` fail-closed review-duration guard in Murph, align the durable PR-review workflow, and rerun the discarded PR 712 review against its exact head with the released package.

## Success criteria

- Murph's manifest and lockfile resolve `@cobuild/review-gpt` from npm at `0.5.109`.
- The durable ReviewGPT workflow states that marked concrete-model reviews completing under 10 minutes fail and do not count.
- The dependency/doc update is verified, committed, pushed, and landed through a scoped PR without touching the conflicted shared checkout.
- A fresh ReviewGPT run uses `0.5.109`, targets PR 712's exact head, and the earlier 37-second response is not reused.
- Any fresh findings are triaged from repository evidence before follow-up changes.

## Scope

- In scope: root package metadata, `pnpm-lock.yaml`, the PR ReviewGPT workflow doc and index entry, the CLI release-flow ratchet test, release-ingestion verification, and a fresh exact-head PR 712 audit.
- Out of scope: changes to PR 712 solely to recreate an open PR, unrelated Murph source changes, and any mutation of the shared conflicted checkout.

## Constraints

- Technical constraints: use the npm-published package; preserve pnpm dependency verification; run ReviewGPT from a clean exact-head checkout.
- Product/process constraints: treat the prior Mountain result as invalid, do not claim a review pass until the new duration gate and normal evidence checks pass, and preserve unrelated browser lanes/processes.

## Risks and mitigations

1. Risk: the release helper mutates the conflicted shared Murph checkout.
   Mitigation: release with upstream sync disabled and ingest from this isolated worktree.
2. Risk: a dependency-bump checkout cannot represent PR 712's exact head.
   Mitigation: invoke the installed `0.5.109` executable from a separate clean detached checkout at the PR head.
3. Risk: another quick response is accidentally accepted.
   Mitigation: require the new tool version, marked concrete-model completion, exact-turn/model evidence, and a nonzero failure for sub-10-minute completion.

## Tasks

1. Update `@cobuild/review-gpt` to `0.5.109` from npm and refresh the lockfile.
2. Align the durable ReviewGPT workflow with the fail-closed 10-minute marked-review floor.
3. Run scoped dependency/doc verification and inspect the final diff.
4. Finish the plan-bearing commit, open the ingestion PR, and land it once its required gates pass.
5. Create a clean detached PR 712 head checkout, prove it uses `0.5.109`, and run a new ReviewGPT round.
6. Triage the new review result and implement a follow-up only for evidence-backed accepted findings.

## Decisions

- The npm release was published with automatic sibling sync disabled because the shared Murph checkout contains unrelated unresolved conflicts.
- The dependency bump remains a normal isolated PR even though the post-merge PR 712 audit uses a detached exact-head checkout.
- Preserve ReviewGPT's existing locked `incur@0.4.5` dependency rather than accepting unrelated transitive refreshes from `pnpm up`; a frozen install proves the minimal lockfile edit is complete.
- Update the existing CLI release-flow ratchet so Murph verifies both the approved package version and the packaged sub-10-minute failure guard.

## Verification

- Commands to run: `pnpm install --frozen-lockfile`, `pnpm deps:guard`, `pnpm deps:audit`, `pnpm deps:ignored-builds`, `pnpm test:diff package.json pnpm-lock.yaml pnpm-workspace.yaml packages/cli/test/release-script-coverage-audit.test.ts agent-docs/operations/pr-reviewgpt-loop.md`, package-version readback, PR checks, exact-head preflight, and the guarded `pnpm review:gpt pr-review` equivalent using the installed `0.5.109` executable.
- Expected outcomes: clean install, scoped checks pass, manifest/lock/package agree on `0.5.109`, ingestion PR is green, and the new PR 712 review either fails closed before 10 minutes or returns a valid evidence-backed result after the minimum duration.

## Results

- Published `@cobuild/review-gpt@0.5.109` passed its 169-test release check, npm publication workflow, and registry version readback.
- Murph's frozen install, dependency policy guard, ignored-build review, docs drift guard, and focused CLI release-flow audit passed. The focused audit finished with 36 tests passed and 1 skipped after the required coverage-write pass added direct 37-second, below-boundary, exact-boundary, diagnostic-only, no-attestation, and nonzero-exit proof.
- The full diff-aware lane passed 368 tests before its unrelated real-release-tarball smoke surfaced the fixed 240-second timeout after `npm pack` took 668 seconds under concurrent machine load. The npm dependency audit was externally blocked because both registry audit endpoints returned HTTP 410 through pnpm.
- Murph PR 744 reached green required CI on the implementation head. Its final plan-close commit must rerun the ordinary PR checks before merge.
- PR 712 exact-head preflight passed at `b84b1bb8e47c1f8fe3d5ac285df543ae07650cba`. A Phlebas attachment-staging retry failed before send and did not count. The Mountain retry used ReviewGPT `0.5.109`, attached the guarded ZIP, completed after the 10-minute floor, attested `gpt-5.6-sol` through response slug `gpt-5-6-pro`, and returned `ROUND_OUTCOME: PASS` with no qualifying findings.
- The earlier 37-second result was not reused for findings, evidence, or merge readiness.
Completed: 2026-07-16
