# PR 465 ReviewGPT Round 50

## Goal

Prevent a malformed optional Telegram API base URL from terminalizing a usage-limit
notice before any provider request, using the existing shared configuration owner.

## Constraints

- Validate the existing `TELEGRAM_API_BASE_URL` resolver once for all Telegram callers.
- Preserve valid absolute HTTP(S) overrides and the existing default Telegram endpoint.
- Add no retry classifier branch, state, queue, manager, or transport abstraction.
- Keep the fix scoped to shared URL resolution and production-path regression coverage.

## Working Set

- `packages/operator-config/src/telegram-runtime.ts`
- `packages/operator-config/test/runtime-helpers.test.ts`
- `packages/assistant-runtime/test/hosted-provider-effects.test.ts`

## Verification Plan

- Reproduce malformed URL classification through the hosted provider effect.
- Verify invalid and non-HTTP(S) overrides resolve to the existing default endpoint.
- Run focused package tests, workspace typecheck, diff-aware verification, required
  completion audits, and another exact-head Mountain ReviewGPT round.

## Progress

- Round 50 reproduced `ASSISTANT_TELEGRAM_DELIVERY_AMBIGUOUS` with
  `deliveryMayHaveSucceeded=true` before URL parsing could reach a provider.
- The shared resolver now accepts only absolute HTTP(S) overrides; malformed or
  unsupported values return `null`, so every existing caller uses the canonical
  Telegram endpoint it already owns.
- Focused resolver and hosted provider-effect tests passed, including native URL
  parsing at the production fetch boundary.
- Coverage-write, security/privacy, and simplification audits found no remaining
  actionable issue. Adding consumer-specific parser cases was rejected as duplicate
  coverage and unnecessary complexity.
- Full serialized `pnpm verify:acceptance` passed in 627 seconds, including package
  coverage, workspace typecheck, web build/tests/lint/smoke, Cloudflare verification,
  package boundaries, and fixture coverage.

Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
