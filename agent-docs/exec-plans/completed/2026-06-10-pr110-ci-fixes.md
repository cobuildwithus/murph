Goal (incl. success criteria):
- Fix PR 110 failing Murph Host Support package coverage shards with the smallest durable changes.
- Success means the current CLI, assistant, and platform coverage failures are corrected without weakening CI guard intent or adding speculative provider/runtime complexity.

Constraints/Assumptions:
- Preserve unrelated active ledger rows and worktree changes.
- Prefer test/guard updates where behavior already intentionally changed; update production code only if an invariant failure proves source behavior is wrong.
- Keep hosted mailbox consume route exported intentionally.
- Garmin currently needs a shared provider descriptor for identity/query planning, not a full importer adapter.
- `@murphai/hosted-execution/vault-share` is already a package export; workspace TypeScript needs the same explicit source path mapping pattern as the other stable hosted-execution subpaths.

Key decisions:
- Use the existing PR worktree on `hosted-reply-replay-durability`.
- Avoid editing `.github/workflows/cloudflare-hosted-e2e.yml` because another active lane already owns that workflow and the current failure is stale guard parsing.

State:
- Active.

Done:
- Read required repo routing/verification docs.
- Pulled failing CI logs for PR 110.
- Full CLI, hosted-execution, and importers package coverage commands pass locally after guard updates.
- Fixed the workspace typecheck subpath mapping for `@murphai/hosted-execution/vault-share`.
- Coverage-write audit added direct proof for the mailbox consume route value.
- Re-ran hosted-execution coverage and workspace typecheck after the coverage-write change; both passed.

Now:
- Run final completion review.

Next:
- Run focused package coverage commands and complete required review/commit flow.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `packages/cli/test/release-workflow-cache-guards.test.ts`
- `packages/cli/test/cloudflare-hosted-e2e-workflow-guards.test.ts`
- `packages/hosted-execution/test/hosted-execution.test.ts`
- `packages/importers/test/provider-descriptors.test.ts`
- `tsconfig.base.json`
- CI run `27294578310`, PR `110`
Status: completed
Updated: 2026-06-10
Completed: 2026-06-10
