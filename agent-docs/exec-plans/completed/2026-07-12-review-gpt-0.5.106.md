# Adopt Published ReviewGPT 0.5.106

Status: completed
Created: 2026-07-12
Updated: 2026-07-12

## Goal

- Make ordinary Murph `pnpm review:gpt` runs consume the published 0.5.106 release that selects the current visible Pro model and uses the 120-minute response timeout.

## Success criteria

- The root manifest and lockfile resolve `@cobuild/review-gpt` 0.5.106 from the public registry.
- No local patch, alias, or non-registry dependency path is introduced.
- The installed package matches the published registry integrity and exposes the expected version.
- Dependency guards, focused release/package coverage, install reproducibility, and scoped repo verification pass.

## Scope

- In scope: the root dependency declaration, its existing exact-version minimum-age exception, lockfile resolution, and the focused release-policy assertion.
- Out of scope: changing ReviewGPT behavior in Murph or carrying another local patch.

## Constraints

- Preserve the one-attempt-per-exact-head rule and the substantive ten-minute UNKNOWN-attestation fallback.
- Preserve concurrent CI/ReviewGPT launch ordering and the 120-minute response wait.
- Keep the change to the smallest public-registry dependency bump.

## Evidence

- Murph currently declares and locks 0.5.104, so ordinary worktree installs do not inherit the released Pro-selector and timeout corrections.
- The public 0.5.106 package is published with registry integrity `sha512-Vqn1NsVowcRnR5f70TUnxKzfZTBTCp/u/4Y9ytbpWUrppfVwsf3yO2fBxpHh1oKbzz3/kUFXQY6iEtU+8TkABA==`.
- Upstream tags 0.5.105 and 0.5.106 contain the timeout extension and current-Pro selector fixes.

## Tasks

1. Update the manifest, narrow exact-version minimum-age exception, and lockfile to the public 0.5.106 package.
2. Verify installed version, registry integrity, package graph, ignored scripts, and dependency policy.
3. Run focused release/package tests plus the routed scoped verification.
4. Complete final review, close the plan with a scoped commit, push, and open the PR.

## Verification

- `pnpm install --frozen-lockfile --offline`: passed; the installed graph contains one `@cobuild/review-gpt` 0.5.106 package with the published registry integrity.
- `pnpm --filter @murphai/cli test -- release-script-coverage-audit.test.ts`: ReviewGPT policy assertions passed; one unrelated slow release-coverage case timed out in the full file and passed in isolation in 54 seconds.
- `pnpm --filter @murphai/cloudflare-runner test -- runner-dependency-coverage.test.ts`: passed (17 tests).
- `pnpm deps:guard`: passed (31 workspace manifests).
- `pnpm test:diff`: all guards, CLI checks (19 files / 364 tests), and 25 affected-package typechecks passed. The broad run then reached 59 hosted-local-harness failures because the isolated worktree had not built `@murphai/assistant-runtime/dist`.
- `pnpm --filter @murphai/assistant-runtime build`: passed.
- `pnpm --filter @murphai/hosted-local-harness test`: passed after the required dependency build (21 files / 386 passed / 1 skipped), confirming the broad-run failures were isolated-worktree setup rather than this dependency bump.
- `git diff --check`: passed.
Completed: 2026-07-12
