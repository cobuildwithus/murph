# PR 465 ReviewGPT Round 49

## Goal

Fix the reproduced marker/delivery ownership race without adding another owner
or lifecycle layer, then rerun the final review and deployment gates.

## Constraints

- Keep the existing delivery row as the sole executable send owner.
- Keep the period marker only as the temporary mixed-release compatibility fence.
- Make delivery-row and marker acquisition all-or-nothing in one transaction.
- Clear the marker only when the exact delivery generation is proven retryable
  before provider delivery; never clear it after ambiguous provider dispatch.
- Do not add a queue, scheduler, manager, table, or permanent rollout service.

## Working Set

- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `apps/web/src/lib/hosted-onboarding/webhook-transport.ts`
- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- matching hosted usage-limit tests and contract documentation

## Verification Plan

- Reproduce the stale Linq dispatch-start versus Telegram claim race.
- Cover marker clearing for exact pre-provider and explicit retry-after failures.
- Cover marker preservation for ambiguous post-dispatch outcomes.
- Run focused tests, workspace typecheck, production web build, required audits,
  exact-head CI, and another Mountain ReviewGPT round.

## Progress

- Replaced marker-first acquisition plus a separate dispatch-start mutation with
  one transaction that acquires the delivery row directly as dispatch-started,
  then claims the marker or rolls the row change back.
- Moved Linq ownership after local route/configuration checks and Telegram
  ownership into the existing post-OIDC, pre-fetch callback.
- Added exact-generation retry release and deleted stale Telegram
  terminalization writes.
- Accepted the final review's deletion-only simplification: the early notice
  claim is now a boolean because its former `in_flight` state moved into the
  send phase and became unreachable.
- Coverage-write and security/privacy audits found no actionable gaps and made
  no edits. The final logic audit found no correctness issue after the dead
  state was removed.
- Five focused suites pass (200 tests), app typecheck and lint pass, and the
  diff-aware app verifier passes all 4,084 runnable web tests, dev smoke, and
  the production build.
- Full acceptance passed workspace typecheck, guards, both app verifiers, and
  every changed package lane. Its parallel package-coverage fanout hit an
  unrelated contracts artifact race and a CLI lock-test timeout; rerun the
  canonical gate after reconciling the latest `main`.
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
