# PR 240 App Lint CI Follow-Up

## Goal

Fix the hosted-web lint failure on PR 240 and keep the PR review/CI loop moving until the final pushed head is green.

## Constraints

- Keep the change narrow and proportional to the CI failure.
- Preserve unrelated worktree and ledger edits.
- Do not touch the inbox media retention implementation unless CI or ReviewGPT proves it is still broken.
- Keep ReviewGPT polling/reruns on pushed PR heads only.

## Plan

1. Confirm the current PR and CI state.
2. Fix the `apps/web` computer handoff floating-island lint failure with the smallest component-local change.
3. Run focused app verification, then broader checks as needed for confidence.
4. Commit through `scripts/finish-task`, push, and poll CI.
5. Continue the ReviewGPT loop on the final pushed head.

## Verification

- Passed: `pnpm --dir apps/web lint`.
- Passed: `pnpm --dir apps/web verify`.
- Passed: `pnpm typecheck`.
- Passed: `git diff --check`.
- Pending: CI check polling after push.
- Pending: ReviewGPT round completion/rerun as needed.
Status: completed
Updated: 2026-06-22
Completed: 2026-06-22
