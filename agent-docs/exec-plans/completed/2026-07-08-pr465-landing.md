# PR 465 Landing

## Goal

Land PR 465 after taking over the existing usage-limit notice durable-send work:
resolve the current merge conflict, fix any real CI or verification failures,
push the final head, run the PR-lane ReviewGPT loop to zero accepted findings,
and merge once required checks are green.

## Constraints

- Keep the landing pass scoped to the existing hosted usage-limit notice,
  Linq delivery, and reconciliation surfaces already touched by the PR.
- Preserve retryability for real failed sends without allowing duplicate
  once-per-period notices after ambiguous provider outcomes.
- Preserve hosted ingress, billing, usage gating, and Linq deliverability
  invariants.
- Do not add new durable owners or broad lifecycle machinery unless production
  proof shows the existing owner boundary cannot preserve the invariant.

## Working Set

- `apps/web/src/lib/hosted-execution/usage-allowance.ts`
- `apps/web/src/lib/hosted-execution/usage-limit-notice.ts`
- `apps/web/src/lib/hosted-execution/usage.ts`
- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `apps/web/src/lib/hosted-onboarding/webhook-provider-linq.ts`
- `apps/web/src/lib/hosted-onboarding/webhook-service.ts`
- `apps/web/src/lib/hosted-onboarding/webhook-transport.ts`
- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- Matching `apps/web/test/*` coverage for hosted usage, Linq dispatch,
  observability, transport, usage reset, and reconciliation facts.

## Verification Plan

- Inspect and resolve the base-branch conflict against current `origin/main`.
- Run focused hosted-web/Linq/usage tests for any touched conflict-resolution or
  CI-fix surface.
- Run the required apps/web verification lane, using truthful scoped verification
  only if a repo-wide required command is already credibly red for unrelated
  reasons.
- Push the final PR head and run the PR ReviewGPT loop to zero accepted findings.
- Confirm final PR CI is green before merge.
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
