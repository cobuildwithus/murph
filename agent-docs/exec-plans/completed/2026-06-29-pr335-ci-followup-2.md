# PR 335 CI Follow-Up 2

## Goal

Make the Linq scheduled reminder E2E green by preventing post-delivery assistant wakes from being checkpointed with too little future runway.

## Constraints

- Keep the hosted wake ownership model small: no new queue, scheduler, state owner, or reconciliation loop.
- Preserve post-delivery cron/outbox/system/provider wake selection.
- Preserve the app verification fix already pushed in the previous commit.

## Current Evidence

- Commit `f4363d7` fixed the release app verification shard.
- The fresh Linq scheduled reminder artifact still shows `hostedOutboxPendingDeliveryEffects: 0` and `hostedOutboxDeliverySent: 1`, but checkpoints `nextWakeAt` in the past relative to `checkpointedAt`.
- The first fix only handled a base wake echo. The failing head proves the invariant must be enforced on the final selected post-delivery assistant wake.

## Plan

1. Normalize the selected post-delivery assistant wake so it has checkpoint-safe future runway.
2. Update focused assistant-runtime tests for due and near-due post-delivery wakes.
3. Run focused tests, scoped verification, push, and recheck CI.
Status: completed
Updated: 2026-06-29
Completed: 2026-06-29
