# Frog Autofix Round 10 Follow-up

## Goal

Close the production-reachable defects reported by the invalid round-ten
diagnostic without adding another authority owner or retry system.

## Accepted findings

- Restore a trusted exact or ancestor handoff before worktree synchronization,
  and permit the parent to repair the exact foreign-edited presentation it
  revalidated.
- Put the one exact `origin/main` friction task blob, source path, and digest in
  both implementation and canonical-review archives.
- Revalidate the exact PR body after each long ReviewGPT wait and immediately
  before metadata writes; preserve an operator handoff instead of overwriting
  it.
- Reject GitHub full-URL closing relationships as well as short closing forms.
- Recover a verified merged-but-open issue closure, while treating a
  post-merge reopen as human-owned.
- Replace broad policy, skill, documentation, and successor-task auto-merge
  authority with one canonical parent-rendered completed-plan exception bound
  to the current issue and phase.

## Boundaries

- Keep Git, GitHub, ReviewGPT, merge, and issue-close authority in the existing
  non-model parent.
- Do not mutate or synchronize a repair worktree before honoring trusted
  handoff metadata.
- Trust only the exact committed friction blob from `origin/main`; do not add
  mutable issue content or the whole friction tree to model context.
- Add no queue, database, daemon, credential, or model turn.

## Verification

- Focused Frog tests, including production-shaped lifecycle and archive cases.
- Diff-aware repository tools and release packager tests.
- Workspace typecheck, documentation drift, shell syntax, permission smoke,
  read-only scan, privacy scan, diff check, and current-base merge tree.
- Exact-head CI and a valid ReviewGPT round-ten PASS.

## Progress

- [x] Reproduce and accept the five diagnostic findings.
- [x] Implement the corrections inside existing owners.
- [x] Run focused and broad local verification for the corrected source.
- [x] Push the first corrected exact head.
- [x] Reproduce and correct the valid exact-head auto-merge allowlist finding.
- [ ] Verify and push the narrowed exact head.
- [ ] Obtain valid exact-head ReviewGPT PASS and green CI.
- [ ] Merge, install, and verify the two-hour service.
