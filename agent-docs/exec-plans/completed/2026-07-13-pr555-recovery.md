# PR #555 Recovery

## Goal

Bring PR #555 to a conflict-free, fully verified, audit-complete final head while
preserving the intended private assistant-style behavior and the accepted hosted
email durability and authority fixes already present in the branch.

## Scope

- Reconcile the PR branch with current `main` through ordinary Git history.
- Finish and verify the existing uncommitted review fixes in the task worktree.
- Preserve private-direct style authority, audience isolation, and accurate
  style-setting outcome guidance.
- Run the routed prompt, security/privacy, coverage, ReviewGPT, CI, and final
  merge-readiness gates required by the resulting mixed prompt/runtime patch.

## Invariants

- Private style controls are available only to the exact authorized private
  provider conversation turn.
- Group, indeterminate, notification, cron, signed-alias, and unauthenticated
  email turns cannot discover or mutate private style settings.
- Group email fanout remains recipient-scoped, replay-safe before provider
  entry, and fail-closed after ambiguous provider entry.
- Unrelated working-tree and coordination-ledger work is preserved.

## State

- PR #555's remote head remains
  `214f6fb136b42fe34372238b5933221991efaf68`; the guarded local branch now
  contains ordinary merges through current `main` head
  `1529dfefa1a1b208721fe7a8efe9b90c0e85a592`.
- Both base merges were resolved semantically. Exact-turn style causal ordering
  and the independently merged assistant-model configuration tool coexist in
  the hosted execution and dynamic-tool contexts.
- The final dirty patch contains deleted-group authority handling, replay-safe
  partial fanout-intent persistence, a private-surface prompt guard, and two
  parent-audit corrections.

## Verification

- Focused assistant-engine tests: 132 passed across style prompts, style tool,
  stale CLI-contract scoping, route planning, and the merged assistant-model
  configuration tool.
- Focused hosted-runtime tests: 587 passed across callback/outbox behavior,
  workspace assistant assembly, and workspace entrypoint causal binding.
- Cloudflare hosted-email transport: 23 passed. Signed web email callbacks: 28
  passed.
- Typechecks passed serially for runtime-state, operator-config, inboxd,
  hosted-execution, vault-usecases, hosted web, assistant-engine,
  assistant-cli, assistant-runtime, CLI, and Cloudflare.
- `pnpm build:workspace:incremental` and CLI config/schema/skill-hash generation
  passed. `git diff --check` passed.
- The routed diff verifier passed repository guards, workspace boundaries,
  cycle checks, generated-artifact preparation, and 364/366 CLI tests. Its two
  release-packaging cases completed their commands and assertions but exceeded
  their fixed 120-second local Vitest deadline under shared load. A serialized
  retry of the lean/full audit-bundle case again completed without an
  assertion failure in about 155 seconds, confirming the timeout; this patch
  does not change the test or packaging scripts.
- Parent-owned prompt review checked the prompt slice against the current
  official GPT-5.6 outcome-first, contextual tool-routing, explicit-value, and
  stopping-condition guidance. The style prompt remains compact and preserves
  exact outcome semantics.
- Parent-owned security/privacy and coverage-write reviews found no remaining
  accepted finding. Accepted corrections now remove retired style commands
  from both full and compact stale CLI contracts and keep the transient email
  authority regression under the suite's mock reset hooks.
- Exact pushed-head ReviewGPT, hosted CI, unresolved-thread verification, and
  final local/remote/PR alignment remain pending after the scoped finish
  commit.
Status: completed
Updated: 2026-07-13
Completed: 2026-07-13
