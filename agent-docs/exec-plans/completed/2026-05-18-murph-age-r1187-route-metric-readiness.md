# Murph Age R1187 Route Metric Readiness

## Goal

Bridge the average-submitter safe confirmation packet to the real lab/wearable route-metric ladder, so the current loop can see the exact next post-confirmation research step without accepting private rows, paths, headers, identifiers, predictions, coefficients, source text, or small cells.

## Scope

- Add an aggregate-only R1187 packet that reads R1186 plus existing route-metric artifacts.
- Surface the current route-metric ladder: safe confirmation first, row-owner private config, private runner, aggregate metric intake, then ReviewGPT only for real aggregate deltas.
- Keep commands allowlisted so malformed upstream command strings are never echoed.
- Add focused tests for the current blocked state, post-confirmation private-config state, ReviewGPT-ready aggregate-delta state, command redaction, and CLI summary.

## Non-Goals

- No row parsing, private config execution, source downloads, scoring, predictions, model training, or metric fabrication.
- No product display, dashboard copy, recommendations, clinical claims, or age-like score promotion.
- No ReviewGPT call unless a real aggregate metric delta is present or a scientific/architecture route choice changes.

## Verification

- Focused R1187 Vitest test.
- Route-metric/safe-submission slice tests.
- Full Murph Age script suite.
- `pnpm typecheck`.
- `pnpm test:diff` when practical.
- Scoped diff, identifier, credential, and aggregate-egress checks.
Status: completed
Updated: 2026-06-03
Completed: 2026-06-03
