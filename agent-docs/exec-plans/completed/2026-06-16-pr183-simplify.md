# PR 183 Simplification

## Goal

Simplify PR 183's hosted reply ordering implementation while preserving the production fix: later same-turn replies must not overtake earlier retryable replies on the same delivery boundary, and prepared but unsent work must remain retryable after aborts.

## Constraints

- Default to deletion and the smallest durable primitive.
- Preserve existing hosted outbox ordering, retry, idempotency, and boundary tests unless a test is proving unnecessary implementation detail.
- Do not broaden package exports, persisted state, or runtime surfaces.

## State

Simplification implemented. Accepted one deep-review finding in the same owner
path: repeated terminal `sent` repair now only runs for the same delivery,
leaving mismatched terminal rows unchanged for the caller to treat as a mismatch.
Pulled the latest PR deep-review prompt preset from `main`.

## Verification Plan

- Focused assistant-engine/runtime tests for changed paths.
- `pnpm typecheck`
- `pnpm test:diff` for touched files, or owner coverage if diff coverage is not truthful.

## Verification So Far

- `pnpm --dir packages/assistant-runtime exec vitest run --config vitest.config.ts --isolate=true --no-coverage test/hosted-runtime-callbacks.test.ts`
- `pnpm --dir packages/assistant-engine exec vitest run --config vitest.config.ts --isolate=true --no-coverage test/outbox-dispatch-state.test.ts test/assistant-outbox-runtime.test.ts`
Status: completed
Updated: 2026-06-16
Completed: 2026-06-16
