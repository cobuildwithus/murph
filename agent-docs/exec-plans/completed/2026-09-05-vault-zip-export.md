# Vault ZIP export guidance

## Outcome

Restore reliable fulfillment of personal vault archive requests through the existing file-send owner.

## Product UX — Patch

- Outcome: prepare the requested original files as an archive instead of merely offering to help.
- Reaches: private iMessage file exports, both ordinary vault wording and broad workspace wording; existing approval and delivery ownership remain unchanged.
- Proof: production-composed prompt assertions, generated-file boundary tests, and a focused synthetic Terra journey checking archive contents, preserved originals, one send, and truthful approval copy.

## Evidence and scope

The ZIP sender and staging guidance predate the recent behavior report. Current instructions prohibit copying existing files into disposable staging but do not distinguish creating a derived archive from relocating the originals. Inspect that ambiguity with synthetic runtime evidence before changing the prompt. No production data, member transcripts, or credentials belong in fixtures or this plan.

## Steps

1. Trace current prompt, tool, permission, and delivery owners; exercise synthetic archive creation.
2. Clarify the smallest existing prompt/tool owner and add focused deterministic proof.
3. Run focused tests, package typecheck, and the real-Codex journey; review the actual reply and effects.
4. Record changelog, parent review and PR evidence; close the plan with a scoped commit and open the PR.

## Validation

- Ready: the production prompt and tool description now distinguish reading original vault files into a new ZIP from relocating them into disposable staging. Existing permission, approval, attachment, and cleanup owners are unchanged.
- Focused deterministic proof: 86 prompt/generated-delivery tests and 27 vault-file-send tests passed. The existing prompt-size ceiling was preserved.
- Real Terra journey passed on the current-main candidate: three synthetic source files, exact archive contents and folder paths, originals unchanged, one send intent, one pending approval, and truthful approval-required output. No actual member message was sent. This proves archive preparation through pending approval, not subsequent external attachment delivery.
- Assistant-engine typecheck passed. Changelog rendering tests passed (9). Web typecheck passed after building the missing local device-syncd artifact.
- Complexity diff passed with unchanged debt. The existing stable-route (30) and hosted-group-guidance (25) hotspots do not gain branching or state; a refactor would broaden this text clarification without improving its ownership.
- Full first provider-visible input measured through the real pinned app-server and local scripted Responses stub, with identical direct/group fixtures, current-main base instructions reconstructed by reversing this patch, normalized temporary roots and message IDs, and transport/cache metadata excluded. Model gpt-5.6-terra; gpt-tokenizer 3.4.0, o200k_base. Individual: 27,733 to 27,752 tokens (+19, +0.069%), 128,227 to 128,333 bytes (+106). Group: 23,548 tokens and 109,097 bytes unchanged. Temporary capture instrumentation was removed.
- Parent review found no new permission boundary, state owner, provider call, database call, or awaited work. ReviewGPT is exempt under the prompt-primary route. Product UX is Ready: requested originals remain available and the existing pending approval owns the next action.
- Historical limit: ZIP support and the ambiguous staging prohibition predate the reported regression. A specific introducing release and the original production turn were not established; the fix addresses the demonstrated instruction ambiguity without claiming a transport failure.
- Implementation and parent review are complete in PR #2873; the changelog links that PR. Required CI gates the final pushed head. Merge and deployment are separate actions.
Status: completed
Updated: 2026-09-04
Completed: 2026-09-04
