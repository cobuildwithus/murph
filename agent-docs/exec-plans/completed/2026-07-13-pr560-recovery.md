# PR 560 recovery

Status: completed
Created: 2026-07-13
Updated: 2026-07-14

## Goal

- Reconcile PR #560 with current `main`, preserve deferred Linq group reaction context, and leave the exact pushed head conflict-free with green CI and zero accepted ReviewGPT findings.

## Success criteria

- The branch contains current `origin/main` through ordinary Git history.
- The lost-active-operation E2E observes the already-proven two replies without a harness counting race.
- Required owner verification, security/privacy review, coverage review, parent final review, CI, and ReviewGPT pass.
- The scoped recovery commit is pushed to PR #560 and the PR description remains current.

## Scope

- In scope: current-main reconciliation, manual conflict resolution limited to PR #560 behavior, the Linq lost-active-operation E2E observation path, focused owner tests/docs required by conflict resolution, PR metadata, CI, and ReviewGPT.
- Out of scope: broad reaction-context redesign, unrelated active ledger work, PR #542, and PR #573.

## Constraints

- Preserve reaction rows as non-waking context paired to the next natural same-group message.
- Preserve unrelated work and use only this checkout.
- Prove product versus harness failures before changing code.
- Add no new state owner, retry loop, or compatibility machinery without production-path evidence.

## Evidence and decisions

- The starting checkout was clean and exactly matched pushed PR head `bd260ddc`.
- CI recorded both expected Linq message POSTs, zero mailbox lag, and one successful outbox delivery in the final invocation while the test timed out waiting for a second matcher-relative count. The helper applies a total-send baseline after filtering by exact reply text, so the second exact-text wait incorrectly requires two matching second replies. This is a harness observation defect, not a lost runtime operation.
- Current `origin/main` through `c0936956` was reconciled through ordinary merge commits, most recently `db7820d6`. The original six conflicts retained current-main foreground priority and mailbox authority changes while preserving PR #560's bounded reaction-context contract. The final two conflicts retained main's deletion of obsolete usage-notice wake routing and the PR's causal-context import.
- The pre-dispatch classifier now checks only fresh conversation inputs observed by the current runner invocation. It excludes the current turn's inputs, fails closed when a fresh or current event record is missing, invalidates only causally matched Linq context, and schedules a later same-route message through the canonical delivery-route identity. It no longer scans or compacts unrelated global pending backlog.
- The focused unit suite proves initial, foreground, and pre-auto-reply reaction admission; current-input exclusion; terminal-evidence exclusion; missing-event failure closure; direct-thread rekeying; later same-route turns; and a 513-item unrelated pending backlog that remains untouched.
- Repeated current-head E2E attempts expired in `beforeAll` at exactly 300 seconds while the canonical cold full-stack setup was still running. Neighboring full-stack scenarios already budget 600 seconds, so this scenario now uses that setup-only budget. No product/runtime timeout changed.
- The canonical non-skipped runner bundle repeatedly reached the full CLI manifest generator and exceeded its fixed 60-second child-process budget under host load. The same generator completed standalone in 34.71 seconds. Prebuilt generation now supplies a build-only 120-second budget while compact and runtime manifest reads retain the 60-second default; its focused test asserts that boundary.
- The canonical runner bundle completed with a real, non-skipped manifest and passed deploy smoke, CLI parity probes, and size budgets. The PR patch on the immediately preceding current-main reconciliation then passed the real lost-active-operation scenario 1/1 in 665.18 seconds with both exact replies and zero final mailbox lag. During that run the local R2 sidecar transiently refused a checkpoint connection, the owned harness restarted it, and the scenario still recovered and converged. The final main merge touched the same runner for durable usage-notice ordering. Its automatic replay duplicated an existing delivery-route import; the current-head focused suite proved the parse failure, the redundant replayed import was deleted while retaining main's existing import location, and the final scoped patch is `+564/-103`.

## Tasks

1. Merge current `origin/main` and resolve only real conflicts while preserving the PR contract.
2. Correct the exact-reply observation semantics and add or retain focused proof.
3. Run focused and routed verification plus required specialist audits.
4. Close the plan with the scoped commit, push, update the PR body, and start ReviewGPT concurrently with CI.
5. Resolve accepted ReviewGPT or CI findings until the PR is merge-ready or a proven external blocker remains.

## Verification

- `pnpm exec vitest run packages/assistant-runtime/test/hosted-runtime-workspace-runner.test.ts packages/assistant-runtime/test/hosted-runtime-pending-input-index.test.ts` — 116/116 passed.
- `pnpm exec vitest run --maxWorkers=1 --no-file-parallelism packages/assistant-runtime/test/hosted-runtime-workspace-runner.test.ts packages/assistant-runtime/test/hosted-runtime-pending-input-index.test.ts` — 123/123 passed on the final current-main reconciliation.
- `pnpm exec vitest run packages/assistant-runtime/test/hosted-runtime-turn-input.test.ts packages/assistant-runtime/test/hosted-runtime-mailbox-import.test.ts` — 57/57 passed after current-main reconciliation.
- `pnpm --dir packages/assistant-runtime typecheck` — passed.
- `pnpm exec vitest run packages/assistant-engine/test/assistant-cli-surface-bootstrap.test.ts` — 21/21 passed.
- `pnpm --dir packages/assistant-engine typecheck` — passed.
- `MURPH_RUNNER_BUNDLE_BUILD_CONCURRENCY=1 MURPH_RUNNER_BUNDLE_SKIP_PACK_PREFLIGHTS=1 pnpm --dir apps/cloudflare runner:bundle:hosted-local` — passed with a non-skipped canonical bundle.
- `MURPH_HOSTED_WEB_PRISMA_GENERATED_PREPARED=1 MURPH_HEALTH_COMMONS_GENERATED_PREPARED=1 MURPH_E2E_STREAM_DEV_LOGS=1 pnpm hosted-local e2e linq-lost-active-operation --profile e2e:stub --no-bundle` — 1/1 passed in 665.18 seconds before the final usage-notice-only main reconciliation.
- `git diff --check` — passed.

## Audit resolution

- Security/privacy re-audit found zero critical, high, or medium findings. The reviewed boundaries were signed ingress, mailbox ownership, context-only non-waking reaction import, same-account/group causality, fail-closed classification, canonical route identity, and exact terminal evidence.
- Coverage-write review accepted the focused owner tests. Parent review accepted the bounded architecture and deletion of the global pending-index scan; the reconciled current-main tests and exact E2E provide final owner proof.
- Rollout remains fail closed: the reaction-context path must stay disabled until a compatible runner head is deployed and verified.
Completed: 2026-07-14
