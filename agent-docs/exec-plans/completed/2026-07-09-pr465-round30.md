Goal (incl. success criteria):
- Resolve PR 465 ReviewGPT round-30 accepted findings without adding new delivery state.
- Success means Linq AI usage-limit provider sends stamp provider-dispatch-start before the external send, same-period spend accounting never erases a real sent marker, focused tests/typecheck/verify pass, ReviewGPT returns no accepted findings, and PR CI stays green.

Constraints/Assumptions:
- Keep the fix narrow and reuse existing hosted_linq_delivery status primitives.
- Do not weaken retryability for failures before provider dispatch starts.
- Preserve `limitNoticeSentAt` as a post-send fact until period rollover or an explicit reset/repair path.

Key decisions:
- Use existing `markHostedLinqDeliveryProviderDispatchStartedTx` for webhook-side Linq usage-limit sends.
- Remove spend-time clearing of `limit_notice_sent_at`; period creation/rollover and explicit reset paths own clearing.

State:
- Round-30 findings accepted; implementation pending.

Done:
- Inspected ReviewGPT artifact and affected code paths.

Now:
- Patch transport/spend logic and add regression coverage.

Next:
- Run focused tests, typecheck, verify, commit, push, rerun ReviewGPT and CI.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `apps/web/src/lib/hosted-onboarding/webhook-transport.ts`
- `apps/web/src/lib/hosted-execution/usage-allowance.ts`
- `apps/web/test/hosted-onboarding-linq-transport.test.ts`
- `apps/web/test/hosted-execution-usage-allowance.test.ts`
- `audit-packages/pr-465-round-30.md`
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
