# PR 465 ReviewGPT Round 28

## Goal

Fix the accepted ReviewGPT round-28 finding for PR 465: stale legacy AI
usage-limit delivery rows must own the period and must not be bypassed by a
new current-period provider idempotency key.

## Constraints

- Keep the fix inside the existing Linq delivery-row ownership boundary.
- Preserve current-key reclaim behavior for stale pre-provider rows.
- Preserve provider-correlated, provider-started, terminal Telegram, and
  retry-after semantics.
- Do not add tables, schedulers, queues, or new durable ownership state.

## Working Set

- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `apps/web/src/lib/hosted-onboarding/webhook-transport.ts`
- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- Focused hosted Linq/runtime delivery ownership tests.

## Verification Plan

- Add focused coverage proving stale legacy rows do not send under the new key.
- Add runtime coverage for legacy ownership before Telegram current-key claims.
- Rerun focused hosted tests, app typecheck/verify as needed, then push and rerun
  ReviewGPT on the final synced PR head.
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
