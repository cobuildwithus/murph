Goal (incl. success criteria):
- Resolve PR 465 ReviewGPT round-31 accepted findings in the Linq usage-limit delivery claim state machine.
- Success means stale non-Telegram `provider_dispatch_started` usage notice rows converge, test-only fresh-claim seams are removed, focused tests/typecheck/verify pass, ReviewGPT has no accepted findings, and CI stays green.

Constraints/Assumptions:
- Keep ownership decisions in the store claim primitive, not caller-side probes.
- Fresh provider-dispatch-started rows remain in-flight; stale non-Telegram rows may reclaim the same idempotency key because Linq sends are idempotency-keyed.
- Telegram stale dispatch-start rows keep their existing terminal unknown outcome behavior.

Key decisions:
- Delete the unused `reclaimFreshPreProviderAttempt` branch and the test-only correlated-or-fresh export.
- Let stale non-Telegram pre-provider rows include both `attempted` and `provider_dispatch_started` in the reclaim predicate.

State:
- Round-31 findings accepted; implementation pending.

Done:
- Inspected ReviewGPT artifact and current delivery-store claim logic.

Now:
- Patch store logic and tests.

Next:
- Run focused tests, typecheck, verify, commit, push, rerun ReviewGPT and CI.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `apps/web/test/hosted-onboarding-linq-observability-store.test.ts`
- `apps/web/test/hosted-onboarding-linq-transport.test.ts`
- `audit-packages/pr-465-round-31.md`
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
