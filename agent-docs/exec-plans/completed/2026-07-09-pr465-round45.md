# PR 465 ReviewGPT Round 45

Goal: close the accepted ReviewGPT round-45 invariant findings for PR 465.

Constraints:
- Collapse quota-notice ownership instead of adding another branch.
- Keep one executable selected idempotency key per notice claim.
- Keep legacy rows as compatibility inputs, not newly created guard rows.
- Preserve Worker-mediated Telegram egress, terminal unknown after possible provider dispatch, retryAfterAt, and post-send `limitNoticeSentAt`.
- Render deterministic family usage-limit notices through the seeded variant bank.

State:
- ReviewGPT round 45 found no Critical/High bugs but blocked merge on Minimal Mechanism Bias and family usage-limit fixed copy.

Plan:
- Remove `guardIdempotencyKeys` and guard-row reservation helpers/callers/tests.
- Keep current/legacy claim resolution as the single owner selector.
- Add `linq.ai_usage.family_limit_reached` variants and render the family notice through `renderUserFacingMessage`.
- Update focused tests and PR body.
- Rebase/merge-refresh after the PR conflict is resolved locally.

Verification:
- `pnpm --dir apps/web test -- hosted-orchestration-reconciliation-facts.test.ts hosted-onboarding-linq-observability-store.test.ts hosted-onboarding-linq-transport.test.ts hosted-execution-usage.test.ts user-facing-messages.test.ts` passed.
- `pnpm test:apps` passed (apps/cloudflare 94 files/1,678 tests; apps/web 367 files/4,045 tests, 9 skipped; existing lint/build warnings only).
- `pnpm --dir apps/web test -- hosted-orchestration-reconciliation-facts.test.ts hosted-onboarding-linq-transport.test.ts` passed after final import cleanup.
- `pnpm typecheck` passed after final import cleanup.
- `pnpm --dir apps/web lint` passed with 9 existing warnings.
- `git diff --check` passed.
- Identifier scan passed.

Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
