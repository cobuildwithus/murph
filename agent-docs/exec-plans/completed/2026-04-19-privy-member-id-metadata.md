## Goal

Write the stable Murph hosted member id into Privy custom metadata, read it back from the verified identity token, and use that trusted id as the fast-path member auth key.

## Scope

- `apps/web/src/lib/hosted-onboarding/{privy,hosted-session,request-auth,page-auth,authentication-service}.ts`
- `apps/web/src/components/hosted-onboarding/hosted-auth-completion.ts`
- `apps/web/src/lib/hosted-onboarding/{env,runtime}.ts`
- focused `apps/web/test/**` coverage for Privy session/auth/completion behavior
- hosted env/docs touchpoints only if the new Privy app-secret requirement needs documenting

## Constraints

- Keep the existing DB-backed Privy identity lookup as the fallback while old or stale identity tokens do not yet carry `murph_member_id`.
- Do not require browsing or printing any live env values.
- Avoid unrelated hosted onboarding, billing, webhook, or UI changes outside the session refresh seam.

## Verification

- Focused `apps/web` tests for Privy token parsing, request/page auth fast path, metadata sync, and post-completion refresh
- `apps/web` verification lane per repo policy after the implementation is stable

## Outcome

- Switched server-side identity-token verification to `PrivyClient.users().get({ id_token })` while retaining the existing DB fallback for missing/stale token member ids.
- Confirmed the trusted Murph member id is our existing hosted DB primary key (`HostedMember.id`), generated by `generateHostedMemberId()`.
- Kept `murph_member_id` sync in Privy custom metadata and the post-completion client refresh seam.

## Final Verification

- Passed: `pnpm --dir apps/web exec vitest run --config vitest.workspace.ts hosted-onboarding-privy.test.ts`
- Passed: `pnpm --dir apps/web exec vitest run --config vitest.workspace.ts hosted-session.test.ts hosted-onboarding-privy-complete-route.test.ts`
- Passed: required `coverage-write` audit lane on the current diff with the same 3-file command above (`29` tests green).
- Passed: required `task-finish-review` audit pass with no findings.
- Failed for pre-existing workspace-resolution reasons: `pnpm --dir apps/web typecheck`
  - unresolved module/type resolution in this workspace for `@privy-io/react-auth`, `@privy-io/node`, and `@cobuild/wire`

## Residual Risk

- No live end-to-end browser proof was run for real Privy token refresh timing after completion.
- Metadata-sync write failures remain intentionally non-fatal and therefore need runtime observation rather than test-only confidence.
Status: completed
Updated: 2026-04-19
Completed: 2026-04-19
