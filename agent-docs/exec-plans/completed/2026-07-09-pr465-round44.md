# PR 465 ReviewGPT Round 44

Goal: close the accepted ReviewGPT round-44 High finding for PR 465.

Constraints:
- Pre-control local failures must remain retryable and must not enter `provider_dispatch_started`.
- Once hosted control may have attempted provider egress, stale Telegram rows must not be retried.
- Preserve round-43 atomic guard reservation and orphan-guard recovery behavior.
- Keep the fix narrow; no new queue, scheduler, table, or broad delivery rewrite.

State:
- Hosted-control request-boundary callbacks are awaited before `fetch`.
- Telegram authority signing now happens before `provider_dispatch_started`.
- Runtime marks `provider_dispatch_started` inside the awaited hosted-control request boundary.
- Stale Telegram `provider_dispatch_started` rows terminalize unknown again instead of retrying.

Plan:
- Update PR body and rerun ReviewGPT after commit and push.

Verification:
- `pnpm --dir packages/cloudflare-hosted-control test`
- `pnpm --dir apps/web test -- hosted-orchestration-reconciliation-facts.test.ts hosted-onboarding-linq-observability-store.test.ts`
- `pnpm typecheck`
- `pnpm test:apps`

Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
