Goal (incl. success criteria):
- Upgrade the hosted Codex CLI pin from 0.143.0 to the npm latest 0.144.0.
- Success means the Docker runner base, image-tag contract, root dependency lock, and pnpm minimum-release exception all point at 0.144.0 and the dependency/update checks pass.

Constraints/Assumptions:
- Keep the change narrowly scoped to the Codex CLI version bump and generated lockfile changes.
- Preserve the existing Dockerfile install shape and native binary symlink behavior.
- Do not alter hosted runtime architecture, model catalog patching, or deploy workflow logic unless the new package shape proves it necessary.
- Dependency changes require the lockfile, dependency guard, audit, and ignored-build-script review before handoff.

Key decisions:
- Use 0.144.0 because npm reports it as the `latest` dist-tag for `@openai/codex`.
- Keep CI/deploy workflows deriving the Codex version from `Dockerfile.cloudflare-hosted-runner-base`.

State:
- Ready for scoped commit.

Done:
- Read repo routing, architecture, invariant, security, reliability, completion, and verification docs.
- Confirmed current pins are 0.143.0 in the Dockerfile, lockfile, and pnpm minimum-release exception.
- Confirmed npm latest for `@openai/codex` is 0.144.0 and platform optional packages exist for that release.
- Updated the Dockerfile pins, assistant-engine dev dependency, pnpm lockfile, pnpm minimum-release exception, hosted runner image-tag contract, and version-specific docs/tests/comments.
- Passed lockfile validation, dependency policy, ignored-build review, CLI binary smoke, full typecheck, broadened `test:diff` package coverage through the failed Cloudflare step, focused hosted-local rerun after building `@murphai/assistant-runtime/dist`, and focused Cloudflare verify after fixing the final-image base tag.
- `pnpm deps:audit` remains blocked by existing high advisories also present on `main`; reported high paths are outside `@openai/codex`.

Now:
- Commit, push, and open the PR.

Next:
- Watch PR checks after push.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `Dockerfile.cloudflare-hosted-runner-base`
- `Dockerfile.cloudflare-hosted-runner`
- `.github/workflows/deploy-cloudflare-hosted.yml`
- `apps/cloudflare/scripts/runner-base-image-contract.ts`
- `apps/cloudflare/test/container-image-contract.test.ts`
- `apps/cloudflare/test/helpers/hosted-local-e2e-support.ts`
- `apps/cloudflare/DEPLOY.md`
- `packages/assistant-engine/package.json`
- `packages/assistant-engine/src/assistant-codex/failures.ts`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`
- `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`
- `pnpm install --lockfile-only --frozen-lockfile` passed.
- `pnpm deps:guard` passed.
- `pnpm deps:audit` failed with pre-existing high advisories on `main`: `form-data`, `vite`, `undici`, `ws`.
- `pnpm deps:ignored-builds` passed; ignored scripts are existing workspace packages, not `@openai/codex`.
- `pnpm --dir packages/assistant-engine exec codex --version` reported `codex-cli 0.144.0`.
- `pnpm --dir packages/assistant-engine exec codex app-server --help` passed.
- `pnpm --dir packages/assistant-engine exec codex doctor --help` passed.
- `pnpm typecheck` passed after preparing generated runtime artifacts in the fresh worktree.
- `pnpm test:diff ...` broadened due root lock/workspace config; package coverage passed until `apps/cloudflare verify` exposed stale `Dockerfile.cloudflare-hosted-runner` tag.
- `pnpm --dir packages/hosted-local-harness test` passed after building `packages/assistant-runtime/dist`.
- `pnpm --dir apps/cloudflare verify` passed after updating `Dockerfile.cloudflare-hosted-runner`.
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
