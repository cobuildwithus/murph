# PR 337 ReviewGPT round 3 fixes

## Goal

Address accepted ReviewGPT round 3 findings for PR 337.

Success criteria:

- The contact-card after-outbound web callback revalidates durable Linq thread
  route authority before provider sharing.
- Contact-card state uses one atomic 48-hour attempt reservation and no claim
  lease fields.
- Focused tests cover unauthorized callbacks, duplicate reservation races, and
  failure/unknown-success throttling.
- Required verification passes and ReviewGPT is rerun.

## Scope

- In: hosted-execution callback contract, assistant-runtime callback payload,
  web route/helper/schema/migration, Cloudflare effects port, focused tests.
- Out: unrelated Linq delivery behavior, standalone contact-card jobs,
  provider-side contact-card setup.

## Plan

1. Add durable Linq thread-route authority to the after-outbound callback.
2. Assert authority in the web route before attempting provider sharing.
3. Delete claim lease fields and implement atomic attempt reservation.
4. Update focused tests, schema/migration, and docs.
5. Run verification, finish-task commit, push, and rerun ReviewGPT.

## Outcome

- Added durable Linq thread-route authority to the contact-card after-outbound
  callback contract and asserted it in the web route before provider sharing.
- Removed contact-card claim lease fields and use the 48-hour attempted
  timestamp as the atomic retry gate.
- Added focused route/helper/runtime/cloudflare coverage.

## Verification

- `pnpm --dir apps/web prisma:generate`
- Focused web, assistant-runtime, hosted-execution, and Cloudflare tests
- `pnpm --dir apps/web typecheck`
- `pnpm typecheck`
- `MURPH_VERIFY_STEP_PARALLEL=0 bash scripts/workspace-verify.sh test:diff ...`
- `git diff --check`
Status: completed
Updated: 2026-06-27
Completed: 2026-06-27
