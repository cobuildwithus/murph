Goal (incl. success criteria):
- Upgrade Murph's hosted Codex CLI/runtime pin from 0.144.0 to the current official npm `latest` release, 0.145.0.
- Success means every live production pin, package dependency, lockfile entry, minimum-release-age exception, runner image contract, and version-specific assertion agrees on 0.145.0; the real packaged binary and app-server surface pass direct smoke checks; required dependency and scoped repository verification pass or any unrelated baseline failure is proved explicitly.

Constraints/Assumptions:
- Treat Codex as an external immutable dependency; do not edit the sibling Codex source checkout or patch the published package.
- Keep the change narrowly scoped to the exact official public-registry release and generated lockfile consequences.
- Preserve the existing Docker install shape, native binary and sandbox-resource validation, warm App Server lifecycle, model-catalog patching, and supply-chain controls.
- Do not merge the resulting PR.

Key decisions:
- Use 0.145.0 because the npm registry currently reports it as the stable `latest` dist-tag and publishes all six supported platform packages.
- Keep the minimum-release-age exception exact and version-scoped because the stable release is newer than the workspace release-age floor.
- Use the existing real-app-server scripted test lane and image-contract tests as compatibility proof instead of adding an upgrade-specific abstraction.

State:
- Implementation and exact-head verification complete; closing the execution plan before the final PR gate.

Done:
- Read the repository routing, architecture, invariant, security, completion, verification, and PR-gate guidance.
- Confirmed the worktree is isolated and clean on `agent/codex-0-145-upgrade`.
- Confirmed npm `latest` is 0.145.0, with matching platform optional dependencies and the standard `codex` binary entrypoint.
- Located all live 0.144.0 pins and compatibility assertions outside immutable completed plans.
- Updated the exact Docker, package, lockfile, minimum-release-age, image-contract, documentation, and version-specific compatibility pins to 0.145.0.
- Passed `pnpm install --frozen-lockfile`, `pnpm deps:guard`, and `pnpm deps:ignored-builds`.
- Confirmed `pnpm --dir packages/assistant-engine exec codex --version` reports `codex-cli 0.145.0`; `codex app-server --help` and `codex doctor --help` both pass.
- Passed the 13-test real Codex app-server scripted-provider suite and the 11-test hosted runner image-contract suite.
- The canonical root-broadened `pnpm test:diff ...` passed global guards, 30 affected owner typechecks, 374 CLI tests, 217 contracts tests, and all 2,607 Assistant Engine tests. It then stopped on three pre-existing Assistant Runtime diagnostics-fixture failures: the failing test and implementation are byte-identical to `origin/main`, and the focused test reproduces the same stale pending-input schema error without touching Codex.
- `pnpm deps:audit` remains red on existing dependency paths outside `@openai/codex`; the lockfile diff changes only the Codex package family.
- Committed and pushed the candidate, then opened draft PR #877 at `29f835594599780abac67f4fbc113f0e321de72b`.
- The preliminary coverage specialist requested native Linux final-image execution. The exact-head `Runner permission sandbox gate` built the `linux/amd64` base and final images, reported `codexVersion=codex-cli 0.145.0`, exercised App Server, and passed every native sandbox permission-profile probe, resolving the finding without a code change.
- Cancelled the additional focused Cloudflare waiter after more than ten minutes without shared-host admission; it never executed, while the protected exact-head native runner smoke completed successfully.
- Completed the parent full-diff review with no additional findings.

Now:
- Close and archive this plan with the scoped final commit, then push the resulting PR head.

Next:
- Record the immutable final ReviewGPT baseline, run the final gate alongside exact-head CI, and report any unrelated CI retry separately.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `Dockerfile.cloudflare-hosted-runner-base`
- `Dockerfile.cloudflare-hosted-runner`
- `packages/assistant-engine/package.json`
- `pnpm-lock.yaml`
- `pnpm-workspace.yaml`
- `apps/cloudflare/scripts/runner-base-image-contract.ts`
- `apps/cloudflare/test/container-image-contract.test.ts`
- `apps/cloudflare/test/helpers/hosted-local-e2e-support.ts`
- `apps/cloudflare/DEPLOY.md`
- `packages/assistant-engine/src/assistant-codex/failures.ts`
- `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`
Status: completed
Updated: 2026-07-22
Completed: 2026-07-22
