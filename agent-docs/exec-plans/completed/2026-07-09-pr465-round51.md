# PR 465 ReviewGPT Round 51

## Goal

Preserve a definitive delayed Telegram rate-limit response through the existing
provider, web control, Temporal HTTP, and Activity boundaries.

## Constraints

- Keep the provider's existing 30-second delivery deadline.
- Enforce strictly nested existing deadlines with operational slack:
  provider < web control < reconciliation HTTP < Activity Start-To-Close.
- Add no delivery state, retry mechanism, classifier branch, queue, manager, or
  workflow compatibility branch.
- Delete reconciliation timeout configuration and duplicate parsing. Preserve
  the already-published shared option field and slack symbol only as fixed-value
  API compatibility surfaces; the private workflow must not consume or carry
  the old option.
- Prove the delayed typed-429 path without a real multi-second test sleep.

## Working Set

- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- `apps/web/test/hosted-orchestration-reconciliation-facts.test.ts`
- `packages/hosted-execution/src/temporal-env.ts`
- `packages/hosted-execution/test/temporal-env.test.ts`
- `packages/hosted-orchestrator-temporal/src/activities/http-client.ts`
- `packages/hosted-orchestrator-temporal/src/workflows/hosted-user-runtime.ts`
- `packages/hosted-orchestrator-temporal/test/**`
- `packages/hosted-orchestrator-temporal/README.md`
- `render.yaml`

## Verification Plan

- First reproduce the current inverted deadline ordering with a failing contract
  assertion and a deterministic delayed-429 transport test.
- Run focused web, hosted-control, hosted-execution, and Temporal tests/typechecks.
- Run required security/privacy and coverage-write audits, full serialized
  acceptance, exact-head CI, and another Mountain ReviewGPT round.

## Progress

- Round 51 checked PR #465 at `d2a104aea6a868e3e79a0bca6e0e1f23a8a37728`
  with GPT-5.6 Pro and returned one High finding.
- Static tracing confirms the shipped chain is currently 30-second provider,
  15-second web control, 10-second Temporal HTTP, and 10-second Activity.
- Production configuration selects the 10-second deprecated fallback value, so
  the failure path is deploy-reachable rather than speculative.
- A deterministic delayed-429 test reproduced the outer-abort failure and now
  passes through the real hosted-control client with a 40-second control budget.
- The final chain is 30-second provider, 40-second web control, 55-second
  reconciliation HTTP, and 60-second Activity Start-To-Close.
- Both reconciliation timeout environment variables, their duplicate parsers,
  the unused aggregate Temporal environment reader, Render configuration, and
  the private workflow option/Continue-As-New propagation are deleted.
- Private web and Temporal adapters project the obsolete published option away;
  only the fixed-value public compatibility field and old slack symbol remain.
- Continue-As-New reuses the already-normalized options, fixed reconciliation
  timing no longer travels as environment data, and device-only timeout parsing
  no longer couples into per-user reconciliation.
- Pinned Temporal Core replay matching compares scheduled Activity id/type, not
  timeout attributes. A synthetic replay with old 10-second history against the
  new 60-second workflow passed, so no patch marker, reset, or version branch is
  required.
- Focused tests pass across web, hosted execution, and Temporal (129 tests).
  Owner typechecks and the production Temporal workflow bundle also pass.
- Required coverage, security/privacy, simplification, and task-finish reviews
  completed with all accepted findings fixed and no unresolved action items.
- The serialized acceptance run passed the PR-owned hosted-execution and
  Temporal coverage lanes. Three unrelated timing-sensitive package lanes
  failed while multiple worktrees saturated the machine; the existing remote
  PR head passed the assistant coverage shard, while current `main` independently
  failed that shard after all tests passed because a Vitest worker exited.
- The final Round 51 diff removes 103 more lines than it adds.

Status: completed
Updated: 2026-07-10
Completed: 2026-07-10
