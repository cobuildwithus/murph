Goal (incl. success criteria):
- Resolve PR 465 ReviewGPT round-34 accepted findings.
- Success means Telegram usage-limit notices have a production-safe Worker-mediated send path from hosted web, retry state no longer overloads `HostedLinqDelivery.attemptedAt`, focused tests/typecheck/verify pass, ReviewGPT has no accepted findings, and CI stays green.

Constraints/Assumptions:
- Web remains the allowance/accounting and delivery-claim owner.
- Provider egress must stay Worker-mediated; web must not require raw Telegram provider credentials.
- `HostedLinqDelivery.attemptedAt` must mean the actual attempt timestamp only.
- Prefer a narrow route/port over queues or new lifecycle machinery.

Key decisions:
- Add a narrow hosted-control `telegram/send` route instead of exposing Telegram provider credentials to hosted web.
- Hosted web owns usage-limit notice claim/accounting, then asks the Worker-owned provider egress path to send the Telegram notice.
- Keep `HostedLinqDelivery.attemptedAt` as the real attempt timestamp and derive retry eligibility from `failedAt + HOSTED_LINQ_PROVIDER_DISPATCH_STALE_ATTEMPT_MS`.

State:
- Implemented and locally verified. Ready for commit, push, ReviewGPT round 35, CI, and merge.

Done:
- Round-34 ReviewGPT completed on `6c17621b9f`.
- Added Cloudflare hosted-control `telegram/send` client/route contract and Worker handler.
- Replaced hosted-web direct Telegram Bot API usage-limit notice send with Worker-mediated hosted-control send.
- Removed `markHostedLinqDeliverySendFailedTx.nextAttemptAt`; retryable Telegram notice retry state is now derived from `failedAt`.
- Updated hosted-control, Cloudflare, and hosted-web tests.
- Verification passed:
  - `pnpm --dir packages/cloudflare-hosted-control test`
  - `pnpm --dir packages/cloudflare-hosted-control typecheck`
  - `pnpm --dir apps/web test -- hosted-orchestration-reconciliation-facts.test.ts hosted-onboarding-linq-observability-store.test.ts`
  - `pnpm --dir apps/web typecheck`
  - `pnpm --dir apps/cloudflare test -- index.test.ts worker-telegram-send-route.test.ts`
  - `pnpm --dir apps/web verify`
  - `pnpm --dir apps/cloudflare verify`
  - `git diff --check`

Now:
- Commit scoped PR 465 round-35 fix with `scripts/finish-task`.

Next:
- Push the branch, run PR preflight, rerun ReviewGPT, wait for CI, then merge if clean.

Open questions (UNCONFIRMED if needed):
- None.

Working set (files/ids/commands):
- `apps/web/src/lib/hosted-orchestration/runtime-reconciliation-facts.ts`
- `apps/web/src/lib/hosted-onboarding/linq-delivery-store.ts`
- `packages/cloudflare-hosted-control/src/client.ts`
- `packages/cloudflare-hosted-control/src/routes.ts`
- `apps/cloudflare/src/worker/route-handlers/telegram-send.ts`
- `apps/cloudflare/src/worker/internal-routes.ts`
- `apps/cloudflare/test/worker-telegram-send-route.test.ts`
- `audit-packages/pr-465-round-34.md`
Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
