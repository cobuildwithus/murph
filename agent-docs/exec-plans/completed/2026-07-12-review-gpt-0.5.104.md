# Use published ReviewGPT 0.5.104

Status: completed
Created: 2026-07-12
Updated: 2026-07-12

## Goal

- Replace Murph's package-manager patch for ReviewGPT with the published upstream 0.5.104 package while preserving the merged review and attestation behavior.

## Success criteria

- The root dependency and lockfile resolve `@cobuild/review-gpt` 0.5.104.
- No ReviewGPT patched-dependency entry or patch file remains.
- Dependency-install and release-package coverage tests pass against the published package.
- Required typecheck, diff, privacy, and dependency verification pass.

## Scope

- In scope: root dependency metadata, workspace dependency policy, lockfile, obsolete patch deletion, directly affected tests/docs if required.
- Out of scope: changing ReviewGPT behavior beyond the published 0.5.104 release.

## Constraints

- Use the public npm registry package; do not replace it with a local path or compatibility shim.
- Preserve the one-run-per-exact-head and ten-minute UNKNOWN fallback policy.
- Keep the dependency update minimal and auditable.

## Risks and mitigations

1. Risk: the published tarball differs from the reviewed local patch.
   Mitigation: verify the registry artifact, install it frozen, and run the exact package coverage tests.
2. Risk: removing the patch changes hosted runner packaging.
   Mitigation: run the runner dependency-install tests and inspect the final lockfile/package graph.

## Tasks

1. Wait for and verify the 0.5.104 registry publication.
2. Update dependency metadata and lockfile; delete the obsolete patch.
3. Run focused dependency, release coverage, typecheck, diff, and privacy checks.
4. Complete required audits, finish the plan, push, and open a draft PR.

## Decisions

- The upstream package is the only source of ReviewGPT behavior; Murph will not retain a package-manager patch.
- The installed public-registry artifact reports 0.5.104 and its driver SHA-256 is `36239db84f69d192616933660380b51a322e97914d4a212f6a5a59a031670e20`.

## Verification

- Passed: `pnpm install --frozen-lockfile`.
- Passed: runner dependency-install coverage (17/17) and CLI release coverage (33/33).
- Passed after preparing the fresh worktree's workspace build artifacts: `pnpm typecheck`.
- Passed: root-manifest `pnpm test:diff` fanout across affected packages, web, and Cloudflare.
- Passed: `git diff --check` and the direct-identifier/credential diff scan.
- Passed: coverage-write audit with zero actionable findings and no edits; existing focused tests directly exercise the installed package behavior.
- Passed: security/privacy audit with zero proven medium-or-higher findings and no edits.
- Passed: parent final review, final dependency readback, `git diff --check`, and privacy scan.
Completed: 2026-07-12
