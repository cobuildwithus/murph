# Reliable complete comparison cards

Status: completed
Created: 2026-09-08
Updated: 2026-09-08

## Outcome

Reliably author complete generic comparison cards or give full truthful text
when no card fits.

## Reaches and owner

Private-direct generic table authoring in native-capable and code-only routes.
The supplied actual App Server captures lose generic bounds in both modes.
Add one generic-only paragraph at `MURPH_ATTACH_RESPONSE_CARD_TOOL`, deriving
limits from public contracts. No group, nutrition, workout, Telegram, authority,
schema, envelope, canonical-write, or visual changes; no new state or runtime retry logic.
Keep adjacent wearable-card work separate. Add one independent changelog fragment.

## Proof recorded for this correction

Local execution reported both provider-contract tests passing the candidate and
failing with the exact guidance hunk reversed; three argument-admission cases,
package typecheck, 16 changelog tests, and complexity checks passed (maximum 12,
unchanged). Terra/local-subscription nine-separate-items/native: baseline made
one invalid card call before complete text; candidate returned all nine items
in order in complete text with zero tool attempts, invalid calls, writes, or
attachment claims. All four Terra/local-subscription concise/nine-item journeys
(native/code-only) passed with prose/actions Ready: small cards exactly one
success and no duplicate text; nine items complete and ordered in text with
zero attempts; zero invalid calls or writes throughout. Three nearby
card/nutrition/discovery checks also passed.

The measurement assertion accepts top-level `tools` and `input` items of
type `additional_tools`. Local raw UTF-8 totals (only `prompt_cache_key` excluded)
had zero delta: direct/native 147039 -> 147039; direct/code-only 139773 -> 139773;
group/native 125618 -> 125618; group/code-only 115712 -> 115712. Raw requests were
not identical. Equality alone now normalizes observed `input[0..6].id`, the five
named client IDs, and the six IDs plus `turn_started_at_unix_ms` inside decoded
`x-codex-turn-metadata`; static metadata and all other fields remain compared.
Raw capture/metrics and production-prompt checks are unchanged.

## Acceptance requirements

Rerun correction checks and all four private/group, native/code-only first-input
fixtures; require identity-normalized equality and zero raw UTF-8 byte delta,
reporting raw totals/digests separately from normalized equality. No exact
supported tokenizer is available; do not claim absolute counts or use fake usage.
Preserve complete truthful text or quiet successful cards, zero invalid attempts,
and no writes in the focused Terra/local-subscription journeys.

Keep PR #3059 draft through focused local proof and parent candidate review.
With the intended pushed candidate and no known PR-specific edits, mark Ready;
admission starts exact-head CI, with final ReviewGPT launched concurrently.
Require both before merge readiness. No merge or deployment is authorized.
Completed: 2026-09-08
