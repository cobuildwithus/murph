# Get repo checks and typechecks green

Status: completed
Created: 2026-05-03
Updated: 2026-05-03

## Goal

- Restore the current checkout to green repo verification by fixing failures from `pnpm typecheck` and the canonical repo acceptance/check lanes.

## Success criteria

- `pnpm typecheck` passes.
- `pnpm verify:acceptance` passes, or any remaining failure is proved unrelated and documented with a concrete failing target.
- Any fixes stay scoped to the failing owners surfaced by verification.

## Scope

- In scope: failing repo checks, typecheck failures, and directly coupled tests or config required to make those checks pass.
- Out of scope: unrelated feature work, broad refactors, product behavior changes unrelated to verification failures, and active ledger rows owned by other tasks.

## Constraints

- Technical constraints: preserve package boundaries, avoid weakening runtime/auth/privacy invariants for tests, and prefer the repo's existing verification scripts.
- Product/process constraints: keep privacy guardrails intact, preserve unrelated working-tree edits if they appear, and close this plan through the repo commit workflow if files change.

## Risks and mitigations

1. Risk: Repo-wide acceptance may be expensive or fail in a broad area.
   Mitigation: run `pnpm typecheck` first, inspect precise failures, then use focused owner checks during repair before rerunning the full acceptance lane.
2. Risk: Existing active ledger rows indicate many stale or concurrent areas.
   Mitigation: avoid touching files listed in active rows unless verification proves they are the current blocker, then keep edits minimal and note the overlap.

## Tasks

1. Run `pnpm typecheck` and capture failures. Done: passed.
2. Fix typecheck failures at their owning source. Done: no typecheck failures.
3. Run the relevant focused tests for touched owners. Done: isolated Cloudflare container-entrypoint test and `apps/cloudflare verify` passed.
4. Run `pnpm verify:acceptance`. Done: first run failed in Cloudflare app verification, final rerun passed.
5. Address any remaining acceptance failures and rerun required checks. Done: hardened the Cloudflare container health tests against ambient `fetch` stubs.
6. Complete required reviews and close with a scoped commit if files changed. Now: final review in progress.

## Decisions

- Use the full acceptance lane because the user asked for repo checks fully green.
- The observed acceptance failure was test isolation, not production container health behavior: the health endpoint tests now use a local `node:http` GET helper rather than ambient `globalThis.fetch`, matching the existing POST helper style and avoiding concurrent Cloudflare fetch stubs.

## Verification

- `pnpm exec vitest run --config apps/cloudflare/vitest.node.workspace.ts apps/cloudflare/test/container-entrypoint.test.ts --reporter=verbose`: passed.
- `pnpm --dir apps/cloudflare verify`: passed.
- `pnpm typecheck`: passed.
- `pnpm verify:acceptance`: passed.
- `git diff --check`: passed.
Completed: 2026-05-03
