# Validate assistant command artifacts before built CLI tests

Status: completed
Created: 2026-09-05
Updated: 2026-09-05

## Goal

Built CLI tests must repair missing or stale assistant command artifacts before using them, while preserving explicitly prepared runtime reuse.

## Scope

The existing CLI test helper and isolated synthetic regression fixtures. No production command behavior, dependency, or runtime configuration changes.

## Tasks

1. Revalidate recovered patch ownership and committed issue authority; preserve owned edits before ordinary base reconciliation.
2. Prove stale command reuse through the actual helper and real synthetic artifact imports, then correct the existing manifest and freshness checks.
3. Complete focused tests and typecheck, canonical full ReviewGPT and required exact-head CI, then guarded landing and issue closure.

## Decisions

- The recovered patch's under-lock prepared-build bypass accepts stale output. Remove it; only the explicit prepared-runtime environment shortcut may trust the prepared build.
- Keep the assistant freshness check in the current helper; remove the generic exported freshness manifest and structural assertion.
- Existing Frog issue #2639 covers the encountered friction; no duplicate report.

## Verification

- Actual-helper synthetic fixture: recovered patch fails four cases (stale result, forced revalidation, later edit, compiler failure) and passes two missing-artifact cases.
- Current-main baseline fails six of seven isolated cases; the current-artifact control passes. Fixed owner plus existing helper suite passes 13 tests. CLI typecheck and whitespace checks pass. Complexity guard passes with test scaffolding excluded from its production-source metric.
- Canonical full ReviewGPT and required exact-head CI remain landing gates. Issue closure waits for the coordinator's Frog reconciliation barrier.
Completed: 2026-09-05
