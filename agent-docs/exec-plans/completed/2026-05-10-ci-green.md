# Get release CI green

Status: completed
Created: 2026-05-10
Updated: 2026-05-10

## Goal

- Restore green status for GitHub Actions run 25621250129 on `main` by fixing the failing release fixture/package coverage checks without touching unrelated hosted runner worktree edits.

## Success criteria

- The stale scenario-manifest coverage check passes locally.
- The affected importer/device-provider deletion artifact tests pass locally.
- The affected CLI workflow guard tests pass locally.
- Required scoped typecheck/coverage verification passes, or any unrelated blocker is explicitly named.
- The fix is committed with a scoped plan-closing commit when safe.

## Scope

- In scope: stale scenario command fixtures, release/cloudflare workflow guard expectations, and the importer deletion artifact normalization contract behind the failing package shard.
- Out of scope: unrelated existing dirty `apps/cloudflare/src/runner-container.ts` and `apps/cloudflare/test/runner-container.test.ts` edits, live deployment, and broad workflow refactors.

## Constraints

- Technical constraints: preserve package-boundary rules, avoid widening CI surfaces, keep fixture/docs baselines truthful.
- Product/process constraints: preserve unrelated working-tree edits, avoid leaking personal identifiers in docs/commits/log excerpts.

## Risks and mitigations

1. Risk: Deletion artifact tests may encode either stale expectations or a real contract regression.
   Mitigation: inspect the normalizer implementation and recent commit before choosing whether to repair production code or tests.
2. Risk: The working tree has unrelated hosted runner edits.
   Mitigation: do not edit those files and use a scoped commit path.

## Tasks

1. Inspect failing logs and local files. Done.
2. Patch stale scenario/workflow guard expectations. Done.
3. Patch deletion artifact root cause. Done: tests now match the hashed/de-identified artifact-role contract from the current normalizer.
4. Run focused verification and required audits. Done.
5. Close the plan and commit scoped changes if safe.

## Decisions

- Treat the aggregate release-checks job as downstream of the failed fixture/package shards.

## Verification

- Commands to run: targeted Vitest/scenario checks first, then `pnpm typecheck` and the relevant diff/package coverage lane per verification policy.
- Expected outcomes: targeted failures from the run are gone; any remaining failures are unrelated and documented.
- Passed: `pnpm exec tsx e2e/smoke/verify-scenario-integrity.ts --coverage`.
- Passed: `pnpm exec vitest run packages/cli/test/release-workflow-guards.test.ts packages/cli/test/cloudflare-hosted-e2e-workflow-guards.test.ts --no-coverage`.
- Passed: `pnpm --dir packages/importers exec vitest run test/device-providers/deletion-normalization.test.ts test/device-providers.test.ts test/oura-coverage.test.ts test/strava.test.ts --no-coverage`.
- Passed: `pnpm typecheck`.
- Passed: `pnpm test:smoke`.
- Passed: `pnpm --dir packages/importers test:coverage`.
- Passed: `pnpm build:test-runtime:prepared`.
- Passed: `env MURPH_PREPARED_CLI_RUNTIME_ARTIFACTS=1 MURPH_VITEST_MAX_WORKERS=50% pnpm exec vitest run --config packages/cli/vitest.workspace.ts --coverage`.
Completed: 2026-05-10
