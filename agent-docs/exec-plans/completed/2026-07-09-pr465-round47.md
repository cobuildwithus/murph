# PR 465 ReviewGPT Round 47

Goal: close ReviewGPT round-47 blockers for PR 465.

Constraints:
- Preserve one durable period notice obligation.
- Do not duplicate Telegram provider sends after hosted web has handed control to the Worker.
- Keep failures before the hosted-control request boundary retryable.
- Keep legacy notice-code rows as compatibility inputs unless code proves active writers still need atomic reservation.
- Avoid new tables, queues, schedulers, or broad ownership machinery.

State:
- ReviewGPT round 47 reported:
  - Telegram notices can resend after the Worker/provider sends but hosted web loses the response because the row stays `attempted`;
  - docs/contracts still claim current-key claims reserve legacy keys, while the implementation now only selects one executable row;
  - the repeated fix loop should collapse toward one owner and one dispatch-start primitive.

Plan:
- Reinspect hosted-control send boundary, existing Linq dispatch-start primitive, and legacy-key callers.
- Restore a minimal dispatch-start stamp immediately before hosted-control can send to the Worker, while keeping earlier config/signing failures retryable.
- Align deploy-skew docs/PR body with the current single executable selected-key model unless active legacy writers require reservation.
- Add focused regressions for post-boundary unknown, pre-boundary retryable, and legacy-key compatibility wording/behavior as applicable.
- Run focused tests, typecheck, app verification, commit, push, and rerun ReviewGPT.

Verification:
- `pnpm --dir apps/web test -- hosted-orchestration-reconciliation-facts.test.ts hosted-onboarding-linq-observability-store.test.ts hosted-onboarding-linq-transport.test.ts` passed.
- `pnpm typecheck` passed.
- `pnpm test:apps` passed.
- `git diff --check` passed.
- Identifier scan over the current diff passed.

Status: completed
Updated: 2026-07-09
Completed: 2026-07-09
