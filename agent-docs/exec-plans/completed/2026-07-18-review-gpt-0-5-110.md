# Ingest ReviewGPT 0.5.110

Status: completed
Created: 2026-07-18
Updated: 2026-07-18

## Goal

- Consume the published `@cobuild/review-gpt@0.5.110` model-picker compatibility fix in Murph without changing any existing ReviewGPT audit lineage.

## Success criteria

- Murph resolves the published package at `0.5.110` in the manifest and lockfile.
- The exact release-age exception advances from `0.5.109` to `0.5.110` without broadening.
- The CLI release audit proves the installed driver recognizes the explicit `GPT-5.6 Sol` model while rejecting Effort and wrong-version evidence.
- Dependency policy checks, focused tests, and the diff-aware verification lane pass or any unrelated external blocker is recorded precisely.

## Scope

- In scope: root package metadata, `pnpm-lock.yaml`, the CLI release-flow ratchet test, and this execution plan.
- Out of scope: PR-specific ReviewGPT rounds, browser-lane changes, application behavior, and unrelated dependency refreshes.

## Constraints

- Use only the npm-published package and preserve pnpm dependency verification.
- Keep the release-age bypass exact and limited to the newly published version.
- Preserve existing PR review baselines and artifacts.

## Tasks

1. Update ReviewGPT to `0.5.110` and refresh only its lockfile resolution.
2. Ratchet the installed-driver release audit for the split Model/Effort picker behavior.
3. Run dependency, focused, and diff-aware verification.
4. Run the required coverage-write pass and parent-owned final review.
5. Finish the scoped commit and open a standalone Murph PR.

## Verification

- Verify npm version/integrity metadata, frozen install, installed CLI version, dependency guards/audit/ignored-builds, focused CLI release audit, `pnpm test:diff`, and `git diff --check`.

## Decisions

- Keep the package range consistent with the existing patch-update policy while pinning the published `0.5.110` resolution and its exact release-age exception in the lock/workspace metadata.
- Keep this as a standalone low-risk developer-tooling PR. It does not change production behavior or a product-critical flow, so it uses the documented ReviewGPT exemption while retaining the required coverage-write and parent final review.
- Do not widen the bump to remediate unrelated registry advisories or refresh other transitive dependencies.

## Results

- ReviewGPT `0.5.110` passed its upstream typecheck, 171-test suite, release check, dry run, Trusted Publishing workflow, and npm version/integrity readback.
- Murph's frozen install, installed CLI version/list, dependency policy guard, ignored-build inspection, and focused release audit passed. The focused audit finished with 37 tests passed and 1 intentionally skipped; the coverage-write pass added exact singleton proof for the release-age exception and the rerun stayed green.
- The diff-aware lane passed global syntax/architecture/privacy guards, repo and affected-package typechecks, prepared runtime/package-shape checks, and the 371-test CLI workspace suite. Its root-manifest fanout later failed in the unrelated `@murphai/assistant-engine` package when one existing local-service-runtime worker exhausted the default 4 GB heap after 2,478 tests passed. A standalone run reproduced the same OOM; an 8 GB, two-worker retry then stalled at 0% CPU for more than 13 minutes and the session-owned command was stopped. No assistant-engine or dependency-path files changed in this task.
- `pnpm deps:audit` reported existing advisories only on non-ReviewGPT dependency paths; the lockfile diff changes only the three ReviewGPT resolution blocks and introduces no transitive churn.
- Parent final review found the manifest, lock, exact release-age exception, installed-driver proof, plan scope, and privacy boundaries coherent, with no unresolved accepted or actionable finding.
Completed: 2026-07-18
