# Upgrade Codex CLI to 0.155.1 in GPT-6 launch PR

Status: completed
Created: 2026-09-22
Updated: 2026-09-22

## Goal

Update PR #3649 to the latest stable Codex CLI, 0.155.1, across dependency, native runner, and verification owners.

## Scope and constraints

Keep the requested GPT-6 model and pricing changes. Preserve historical benchmark and completed-plan evidence. Do not broaden egress policy or synthesize missing native model metadata.

## Tasks

1. Update manifest, lockfile, release-age exemption, native base image, and current documentation references.
2. Review the exact upstream provider source and refresh the route inventory provenance.
3. Run real App Server contract/runtime tests, egress guards, catalog validation, and relevant typechecks.
4. Record results, commit, and push to the existing draft PR.

## Decisions

- npm latest and the official upstream stable release both resolve to 0.155.1.
- Upstream removed the responses/compact client; remove it from the current binary inventory while retaining the existing policy for older supported binaries.
- Preserve recorded 0.153.4 benchmark measurements as historical evidence.

## Verification

- Frozen lockfile install and `pnpm deps:guard`: pass; lockfile changes only Codex and its platform packages.
- `pnpm --dir apps/cloudflare verify:codex-upstream-source`: pass for the release tag, commit, provider tree, and nine reviewed source paths.
- Canonical CLI-upgrade suite plus protocol, failure, and WebSocket-stall tests: 485 pass, 18 opt-in skips across 11 files, using real Codex 0.155.1 against loopback providers.
- Cloudflare egress conformance: 11 pass after explicit classification of the new printable linker fragments; Linux x64 artifact inventory also has zero unclassified routes.
- Runner-base and custom-inference focused tests pass. Container contract: 11 pass, one existing launch blocker because the installed native catalog still lacks GPT-6 Sol/Luna. Combined Cloudflare selection has 47 passing tests and that one failure after the inventory correction.
- Assistant-engine and Cloudflare typechecks: pass.
- Dependency policy, docs drift, complexity diff, and whitespace checks: pass.
- Full Linux image/continuity proof, live GPT-6 journey, final review, and Ready-only CI remain launch gates. This upgrade does not make the draft merge-ready.

## Outcome

Dependency, supply-chain exemption, native runner base, lockfile, and current documentation now pin Codex CLI 0.155.1. The exact upstream source inventory is refreshed without broadening provider egress. Existing 0.153.4 measurements remain explicitly historical. Add the scoped commit to draft PR #3649; no deployment or merge.
Completed: 2026-09-22
