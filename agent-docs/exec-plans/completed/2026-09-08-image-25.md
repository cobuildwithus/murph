# GPT Image 2.5 upgrade

Status: completed
Created: 2026-09-08

## Outcome and scope

Use GPT Image 2.5 Flare for existing image generation and reference edits.
Preserve quality, timeout, credential, destination, and retry contracts.
Existing GPT Image 2 captures remain usable in exact replies and retained delivery evidence.

## Product UX

- Outcome: Existing image requests use GPT Image 2.5.
- Reaches: New generation, reference edits, exact replies to old/new captures, and hosted usage accounting.
- Proof: Request-body, generated-media, reply provenance, retention, and token pricing regressions; relevant typechecks.

## Decisions and risks

- Official model: https://developers.openai.com/api/docs/models/gpt-image-2.5-flare
- OpenAI documents matching GPT Image 2 token rates; keep existing token accounting.
- Deploy the compatible Web usage reader before the runner starts producing new model usage.
- Preserve old model recognition for persisted captures and usage records.
- Live provider access and production deployment are separate from local proof.

## Tasks

1. Update the image model and exact old/new capture recognition.
2. Extend usage-model recognition and focused regressions; add the release note.
3. Run focused tests/typechecks, inspect the diff, and commit the scoped change.

## Verification

- Passed: 309 assistant-engine tests covering image requests, saved captures, retry behavior, response media, exact native replies, and outbox retention.
- Passed: 38 hosted runtime-control tests and 138 Web usage/changelog tests (485 focused deterministic tests total).
- Passed: direct edit-request model regression after adding the explicit GPT Image 2.5 assertion.
- Passed: typechecks for assistant-engine, hosted-execution, Web, and Cloudflare.
- Passed: `pnpm complexity:diff`, `pnpm docs:drift`, and `git diff --check`.
- Live command: `pnpm test:assistant:live -- --test "resumes a gpt-image-2.5-flare capture and uses its exact ref only after a later group-avatar request"`.
- Live model/auth: `gpt-5.6-terra`, local subscription. The corrected journey passed all generation, delivery, no-premature-mutation, and exact-avatar-reuse assertions once. A later run intended to expose successful reply text failed at initial generation-tool selection before any image request. Live UX verdict: Hold; passing reply text was suppressed on the successful run, so prose review remains incomplete. The initial tool-choice failure also appeared with the legacy fixture, before model-specific image behavior.
- Initial live failures exposed a stale fixture that put dynamic completion context into immutable thread instructions. Replaced it with the production completion-context and provider-prompt builders; recorded the fixture issue in the task-owned Frog entry.
- No image-provider credential access, real Image API generation, hosted E2E environment, CI, or production deployment was used for local proof.
- Parent review: scoped changes preserve the existing API request and authority contracts. Existing complexity hotspots are unchanged and outside this upgrade.
- Changelog: `2026-09-08 / gpt-image-25`.


## Handoff

Implementation and deterministic proof are complete in the local task branch.
Live assistant consistency and reply review remain a reported verification gap.
No merge, push, or deployment was performed.
Updated: 2026-09-08
Completed: 2026-09-08
