# Update Murph to ReviewGPT 0.5.114

Status: completed
Created: 2026-07-22
Updated: 2026-07-22

## Goal

- Consume the ReviewGPT patch release that lowers the marked concrete-model
  response minimum from ten minutes to seven and a half minutes.

## Success criteria

- Murph resolves `@cobuild/review-gpt` 0.5.114 from the public registry.
- The manifest, lockfile, narrow minimum-release-age exception, and ReviewGPT
  workflow docs agree.
- Dependency guards, blocked-build review, diff-aware verification, CI, and the
  scoped PR commit path pass; pre-existing audit failures are documented.

## Scope

- Root ReviewGPT dependency metadata, lockfile, current workflow docs, and the
  existing CLI release-contract test that pins the package and threshold.
- This active plan and its coordination-ledger row.

## Constraints

- Use only the public npm release; do not use a git, URL, file, link, portal, or
  alias dependency.
- Keep the minimum-release-age exception exact to the released version.
- Preserve unrelated work in the primary checkout.

## Tasks

1. Publish ReviewGPT 0.5.114 with the new 7.5-minute threshold and aligned CLI
   help.
2. Update Murph's manifest, lockfile, and exact release-age exception.
3. Run dependency supply-chain checks and diff-aware verification.
4. Complete required review, close the plan, commit, push, and open the PR.

## Evidence

- Before this task, ReviewGPT 0.5.112 owned both the ten-minute marked-response
  gate and Murph's exact dependency/release-age entries.
- ReviewGPT 0.5.114 passed its typecheck, all 174 tests, package dry run, and
  release check before the npm publish workflow completed successfully.
- Murph resolves the public 0.5.114 package and its published integrity; the
  lockfile changes only the ReviewGPT package version and integrity.
- Murph dependency policy, ignored-build review, docs drift, package smoke,
  and the focused 38-test CLI release-contract lane pass.
- The dedicated coverage-write audit found the installed-driver proof complete
  at both `7.5m - 1ms` and exactly `7.5m`; it made no test changes. Its stale
  upstream help-text finding was corrected and regression-tested in 0.5.114.
- Final `pnpm test:diff` passes the full root-manifest lane after building the
  hosted-local harness's documented assistant-runtime prerequisite.
- Final `pnpm verify:acceptance` passes workspace coverage, package boundaries,
  artifact hygiene, web verification, and Cloudflare verification.
- `pnpm deps:audit` remains blocked by pre-existing workspace advisories. The
  lockfile diff does not change any transitive resolution, including the
  reported ReviewGPT-to-Repomix paths.
Completed: 2026-07-22
