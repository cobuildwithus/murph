# Update ReviewGPT to 0.5.143

Status: completed
Created: 2026-09-02
Updated: 2026-09-02

## Goal

- Adopt the ReviewGPT release that keeps normal repository reviews on regular Chat instead of ChatGPT Work.

## Success criteria

- The root dependency, supply-chain exception, retained package patch, and lockfile resolve `@cobuild/review-gpt` 0.5.143.
- Murph's exact-version regression test and durable ReviewGPT operating guidance describe the regular-Chat boundary.
- Focused dependency, repository-tooling, typecheck, exact-head CI, and final ReviewGPT proof pass.

## Scope

- In scope: ReviewGPT dependency metadata, its existing Murph-only patch binding, exact-version tests, durable review guidance, and dependency verification.
- Out of scope: Murph production behavior, browser-lane configuration, review prompts, hosted UI, and deployment configuration.

## Constraints

- Use the public-registry 0.5.143 package and retain Murph's two existing package-level compatibility fixes.
- Keep lockfile movement limited to the ReviewGPT resolution and patch identity.
- This is internal review tooling with no member-visible Product UX or changelog change.

## Risks and mitigations

1. Risk: a new package release or patch rebind changes unrelated dependency resolutions.
   Mitigation: update the exact dependency seams, regenerate the lockfile once, and inspect the full manifest and lockfile diff.
2. Risk: Murph's review workflow still permits Work usage despite the package fix.
   Mitigation: document the package-owned regular-Chat guard and run the final exact-head review through the updated dependency.

## Tasks

1. Completed: update ReviewGPT dependency metadata, the retained patch binding, and exact-version assertions.
2. Completed: update durable ReviewGPT operating guidance.
3. Completed: run focused dependency and tooling verification.
4. In progress: commit, push, open the PR, and complete exact-head CI and ReviewGPT gates.

## Decisions

- Keep Chat/Work detection and enforcement package-owned; Murph consumes the behavior without duplicating browser DOM logic.

## Verification

- Passed: frozen install, installed package/version/source inspection, 22 focused ReviewGPT tooling tests, the two directly affected release-audit tests, `pnpm deps:guard`, `pnpm deps:ignored-builds`, `pnpm docs:drift`, `pnpm typecheck`, and `git diff --check`.
- Local environment limitation: the complete release-audit file passed 48 tests and skipped one, but its isolated global-wrapper test could not finish under the local temporary-home package-manager setup. The directly affected wrapper and lifecycle tests pass when targeted; exact-head CI remains the authoritative full-file proof.
- Existing repository exposure: `pnpm deps:audit` reports the current workspace advisory set. ReviewGPT 0.5.143 has the same dependency graph as 0.5.142, so this update adds no dependency surface.
- Pending: exact-head CI, the final ReviewGPT gate, and global CLI version proof.
Completed: 2026-09-02
