# CI run 26846075760 fixes

Status: completed
Created: 2026-06-02
Updated: 2026-06-02

## Goal

- Fix the failing GitHub Actions run `26846075760` on `main` without disturbing unrelated in-progress work in the checkout.

## Success criteria

- The hosted-local E2E script test typechecks.
- The `packages/parsers` attachment parse worker coverage shard passes.
- Focused local verification covers the touched scripts/package surface.
- Completion workflow audits required for a standard repo CI fix are complete, or any blocker is reported with exact scope.

## Scope

- In scope: `scripts/hosted-local-e2e.test.ts`, parser attachment job/runtime code and focused parser tests required by the failing CI logs.
- Out of scope: unrelated hosted-runtime, device-sync, CLI attachment simplification, and Murph Age dirty work already present in the checkout.

## Constraints

- Technical constraints: preserve parser runtime state semantics, keep derived parser artifacts rebuildable, and avoid broad abstractions.
- Product/process constraints: protect unrelated dirty work, do not create/switch branches or worktrees, and follow repo verification/commit workflow.

## Risks and mitigations

1. Risk: parser changes accidentally alter raw/canonical storage behavior.
   Mitigation: keep changes confined to attachment parse job scheduling/finalization and run focused parser coverage.
2. Risk: local dirty files make repo-wide verification or scoped commit unsafe.
   Mitigation: use focused checks first and report exact overlapping-work blockers if they prevent completion.

## Tasks

1. Inspect failing CI logs and reproduce relevant failures locally where possible.
2. Fix hosted-local E2E call typing.
3. Fix parser attachment parse job enqueue/claim/finalize behavior.
4. Run focused verification and required completion audits.
5. Archive the plan and create a scoped commit if the dirty checkout permits it safely.

## Decisions

- Treat the GitHub run as push-run context, not PR context; there is no PR patch to fetch.
- Keep the parser coverage fix aligned to the current media parser invariant: auto-enqueued attachment parser jobs are audio/video, and audio parser output is stored as transcript text.
- Do not rewrite the earlier mixed-scope commit; close this plan with only scoped CI plan/ledger cleanup and the parser-test formatting cleanup.

## Verification

- `pnpm --dir packages/parsers test:coverage`: passed, 5 test files and 68 tests.
- `pnpm typecheck`: passed.
- `pnpm test:smoke`: passed.
- `coverage-write` audit: no findings, no edits.
- `task-finish-review` audit: no scoped code correctness, coverage, privacy, or security findings; noted that an earlier commit also included unrelated hosted-runtime changes, so this plan closure must stay scoped.
Completed: 2026-06-02
