# Simplify the opening signup reply

Status: completed
Created: 2026-09-23
Updated: 2026-09-23

## Outcome and invariants

Keep the direct opening conversational and reduce work before the aspiration question. Preserve accepted identity facts through the existing canonical child save, truthful save claims, immediate-request priority, and finite later recovery. No new state, tools, services, or scheduler.

## Evidence and smallest change

The opening instruction requires a foreground automation save in addition to identity delegation. This requires tool discovery and an additional provider continuation for an optional early nudge. Delete that recipe and its redundant references. Keep identity persistence and existing managed recovery. The separate canary formatting and recreated-account eligibility fixes retain their own PRs.

## Product UX

Entry: a new direct member answers or skips the bundled identity question. Outcome: the next health aspiration question arrives without scheduling a first-minutes nudge. Supplied facts are saved through the existing leaf; skips do not fabricate facts. Immediate requests and later-stage resume keep their owners. Existing already-saved one-shots retain their normal finite execution; no production data migration is added.

## Proof and delivery

1. Update the injected owner, routed skill references, durable product spec, and deterministic assertions.
2. Keep the automation tool available in the real synthetic journey and prove zero calls, one identity child, canonical readback, and an honest reply.
3. Run focused tests and typecheck; capture full provider input where the local provider lane permits it.
4. Scoped PR, required exact-head CI and review, managed release, then production canary.

## Risks and status

Provider timing is stochastic. A local run cannot prove hosted delivery latency. Implementation and focused verification are complete; PR #3673 owns remaining exact-head CI, managed rollout, and production canary evidence. Existing scheduled records are intentionally left to finish under their original finite instructions.

## Focused evidence

- 113 deterministic prompt and skill tests passed; seven existing unrelated tests skipped. Assistant-engine project-reference typecheck passed.
- Refreshed credential-free real App Server request capture against main 98406cbe6a67, identical synthetic direct/group fixtures with the shipped GPT-6 Sol catalog: direct input 158,605 to 156,425 UTF-8 bytes (-2,180; -1.37%); group input unchanged at 133,133 bytes. All provider fields except transport cache identity included. Exact model tokenizer unavailable, so no token count is claimed. Temporary capture fixture removed after proof.
- Focused real-model journey passed with GPT-6 Sol and local subscription after access was restored. One identity child saved and read back the exact synthetic facts; zero automation calls and no saved check-in. The root asked the aspiration question without claiming the child had finished. Reply 10,219 ms; canonical writes verified at 23,629 ms. Actual synthetic reply reviewed: Ready. These are local observations, not a hosted latency guarantee.
- Canary formatting acceptance shipped separately in #3677; recreated-account fast-reply eligibility was fixed separately in #3679. Neither is attributed to this deletion.

- PR #3673 carries the final candidate and release evidence. Its narrow release note describes the removed nudge without claiming a measured speedup. No UI renderer changes or visual are needed for this copy-only note.
Completed: 2026-09-23
