# Testing And CI Map

Last verified: 2026-04-08

## Current Repo Checks

| Command | Purpose | Current coverage |
| --- | --- | --- |
| `pnpm typecheck` | Shell syntax validation, root release-helper syntax checks, the workspace-boundary audit for sibling build-artifact aliases and relative cross-package imports, the workspace package-cycle guard, repo-owned TS tools typecheck, package/app-local no-emit typecheck scripts, and the solution-style TypeScript build. The `apps/web` typecheck script bootstraps the exact `next-env.d.ts` route-type stub it references so a clean clone does not depend on leftover Next output. The root verify harness now also prints per-step timing. | `scripts/*.{sh,mjs,ts}`, `e2e/smoke/verify-fixtures.ts`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**`, `packages/operator-config/**`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/setup-cli/**`, `packages/cli/**`, `packages/openclaw-plugin/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistantd/**`, `packages/assistant-runtime/**`, `packages/query/**`, `apps/web/**`, `apps/cloudflare/**`, `config/workspace-source-resolution.ts` |
| `pnpm test:diff` | Diff-aware agent/local iteration lane. It inspects the current worktree diff against `HEAD` plus untracked files, maps touched files to workspace owners, expands to reverse workspace dependents, then runs the matching package/app-local `typecheck`, `test`, `verify`, and built-CLI verification steps for that subset. Docs/process/verification-tooling-only diffs, and other no-owner non-workspace diffs, stay on the narrow tooling checks (`shell syntax`, `node --check`, repo TS tools typecheck, and dependency policy) instead of fanning out to package/app lanes or running unrelated workspace-boundary audits, while root workspace-manifest changes such as `pnpm-lock.yaml` or `pnpm-workspace.yaml` broaden to the whole workspace. Use `bash scripts/workspace-verify.sh test:diff <path ...>` when the branch already has unrelated edits and the lane should target only the current task files. | Affected workspace owners plus reverse dependents under `packages/**` and `apps/**`, repo-internal tooling fast-path files under `agent-docs/**`, `docs/**`, `scripts/**`, `AGENTS.md`, `ARCHITECTURE.md`, `README.md`, `package.json`, `vitest.config.ts`, and root `tsconfig*.json`, plus whole-workspace fan-out when root workspace manifests change, and built-CLI verification when the affected subset reaches `packages/cli` |
| `pnpm test` | The workspace-boundary audit, the workspace package-cycle guard, the tracked-artifact hygiene guard, the package-only Vitest lane, the dedicated heavier app verify lane, and fixture/scenario integrity validation. Local runs now prepare the shared package-test artifacts once, overlap the root package Vitest lane with `pnpm test:apps` by default, overlap CLI runtime-artifact prep with the app verify lane, also run `pnpm no-js` alongside `packages/contracts test` and `packages/openclaw-plugin test`, and now overlap smoke verification through `e2e/smoke/verify-fixtures.ts`; CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it. The hygiene guard now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files before failing on remaining blocked artifacts, including non-default Next artifact dirs such as `apps/web/.next-dev/` and `apps/web/.next-smoke/`. Retries are now opt-in through `MURPH_VERIFY_RETRY_COUNT`, and the root harness skips the heavy app verify tail automatically when the current changed files are limited to the repo-internal docs/process/verification-tooling fast path. Keep this as the faster non-coverage repo loop; it is no longer the durable acceptance requirement. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `packages/contracts/**`, `packages/hosted-execution/**` including hosted execution auth/env/client coverage, `packages/runtime-state/**` including hosted workspace-snapshot and artifact-externalization coverage plus hosted verified-email env helper coverage and pending assistant-usage record helpers, `packages/operator-config/**` including setup/runtime-env/config persistence coverage, `packages/assistant-engine/**` including the local assistant runtime, provider-turn, tool-catalog, outbox, and owner-boundary coverage, `packages/assistant-cli/**` including CLI-only assistant wrappers, terminal logging, assistant command routing, and Ink chat UI coverage, `packages/setup-cli/**` including onboarding, host setup, and setup-wizard coverage, `packages/cli/**` including the published shell, command-schema coverage, CLI `.env` loading coverage, compatibility-wrapper coverage, and assistantd client routing coverage, `packages/openclaw-plugin/**` including the published OpenClaw bundle metadata and Murph skill guidance package-local test, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including the Oura and WHOOP config/provider/service tests plus HTTP control-plane auth/listener coverage, `packages/inboxd/**` including AgentMail email connector tests plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistantd/**` including loopback host validation, bearer-authenticated control-plane routing, single-vault request enforcement, and the direct owner-package boundary regressions, `packages/assistant-runtime/**` including hosted assistant profile seeding/adoption coverage, fail-closed hosted automation gating, hosted verified-email self-target reconciliation coverage, the direct owner-package boundary checks for hosted consumers, selective hosted artifact materialization, preserved-artifact snapshot behavior, post-commit hosted AI usage export coverage, scrubbed isolated-child env launch, stripped supervisor proxy vars, per-run writable cache/temp roots, launch-time supervisor-`HOME` avoidance, and child-process reaping on abort, `packages/query/**`, `apps/web/**` including hosted web Vitest coverage plus the dedicated `verify` lane for typecheck, smoke, and build checks when the heavier app surface is required, `apps/cloudflare/**` including signed-dispatch verification, `/health` plus internal route alias coverage, per-user runner coordination with retry/poison state, explicit activation bootstrap coverage, durable hosted assistant env/profile propagation plus fail-closed configuration errors, encrypted workspace snapshot storage plus selective artifact-object routing, separate per-user runner env control-route and persistence coverage, committed hosted side-effect journaling plus post-commit assistant-delivery resume coverage, best-effort cleanup of orphaned per-user artifact objects, keyring-aware hosted ciphertext reads by stored `keyId`, lifecycle-friendly transient journal key coverage, isolated child-process hosted execution, container-request abort propagation into the runner bridge, one-shot hosted runner tests, smoke-helper status polling coverage, and the Workers-runtime Vitest lane for dispatch auth, direct Durable Object RPC, Durable Object alarms, bundle journaling, artifact-object routing, and runner control flows, `config/workspace-source-resolution.ts`, `fixtures/**`, `e2e/smoke/scenarios/**` |
| `pnpm docs:drift` | Manual durable-doc drift check. Use this when you intentionally change durable repo docs and want the old index/truthfulness enforcement without making the default `pnpm test` lane sensitive to unrelated dirty-tree doc work. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `README.md`, `package.json` |
| `pnpm test:coverage` | The durable repo acceptance lane: it repeats the dependency-policy and workspace-boundary/package-cycle guards, runs doc-gardening validation, then runs the package-only Vitest smoke/orchestration lane plus standalone owner-package and rollout-package coverage commands, the dedicated heavier app verify lane, command-surface smoke coverage, and the source-artifact guard that rejects handwritten JS-like source files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`, while still allowlisting the exact Next.js `apps/web/next-env.d.ts` stub, the explicit `apps/web/{postcss,eslint}.config.mjs` framework config paths, plus tracked `.env.example` files. Local runs now overlap package coverage, app verification, and smoke verification by default; CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it, and the tag-driven release workflow now opts into that override explicitly. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before the guard runs. Retries are opt-in through `MURPH_VERIFY_RETRY_COUNT`, and the root harness now skips the heavy app verify tail automatically when the current changed files are limited to the repo-internal docs/process/verification-tooling fast path. | `agent-docs/**`, `ARCHITECTURE.md`, `README.md`, `docs/contracts/03-command-surface.md`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**`, `packages/operator-config/**`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/setup-cli/**`, `packages/cli/**`, `packages/openclaw-plugin/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistantd/**`, `packages/assistant-runtime/**`, `packages/query/**`, `apps/web/**`, `apps/cloudflare/**`, `fixtures/**`, `e2e/smoke/**` |
| `pnpm test:packages` | Package-only runtime verification plus built CLI verification without the worktree-sensitive doc-drift wrapper. Starts with the tracked-artifact hygiene guard, runs `packages/contracts test` plus `packages/openclaw-plugin test`, then performs the prepared `pnpm build:test-runtime:prepared` graph plus the root multi-project Vitest package suite. That root package lane now runs the full package-local test surfaces for the repo-owned Vitest packages already wired into `vitest.config.ts` rather than the older narrower subset, while still leaving the heavier local/hosted web and Cloudflare verify work to `pnpm test:apps`. The CLI portion also now runs the full six serial-safe workspace buckets instead of a narrower hand-picked subset. Vitest concurrency still defaults to `MURPH_VITEST_MAX_WORKERS=75%` locally or `50%` in CI unless the environment overrides it. File-level Vitest parallelism is enabled locally by default, disabled in CI by default, in-file suite concurrency is opt-in across repo-owned Vitest suites unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and `MURPH_VITEST_FILE_PARALLELISM`, `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` provide explicit overrides. The prepared graph now also verifies the built `packages/assistant-cli/dist/index.js`, `packages/setup-cli/dist/index.js`, and `packages/cli/dist/cli-entry.js` artifacts, and package-local CLI tests can reuse a persistent subprocess harness for non-stdin commands unless `MURPH_CLI_TEST_PERSISTENT_HARNESS=0` forces isolated per-command execution again. Hosted web and Cloudflare Node lanes still keep their dedicated workspace configs outside this command. | `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**` including hosted verified-email env helpers plus assistant usage pending-record helpers, `packages/operator-config/**` including setup/runtime-env/config coverage, `packages/assistant-engine/**` including local assistant runtime coverage, `packages/assistant-cli/**` including assistant wrapper, terminal logging, and Ink chat UI coverage, `packages/setup-cli/**` including onboarding and host-setup coverage, `packages/cli/**` including the published shell, compatibility wrappers, assistant command coverage, CLI `.env` loading, assistantd client routing coverage, and the persistent non-stdin CLI harness path, `packages/openclaw-plugin/**` including the published OpenClaw bundle metadata and Murph skill guidance package-local test, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including Oura and WHOOP provider/config coverage plus control-plane auth and public-listener route coverage, `packages/inboxd/**` including AgentMail email connector coverage plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistantd/**` including loopback/bearer HTTP control-plane coverage, `packages/assistant-runtime/**` including hosted verified-email self-target reconciliation plus hosted AI usage export coverage, scrubbed isolated-child env launch, stripped supervisor proxy vars, per-run writable cache/temp roots, launch-time supervisor-`HOME` avoidance, and child-process reaping on abort, `packages/query/**` |
| `pnpm test:apps` | Dedicated heavier app verification lane. Runs `apps/web verify` and `apps/cloudflare verify`, defaulting to parallel local execution and sequential CI execution unless `MURPH_APP_VERIFY_PARALLEL` overrides it, so app-local `test` commands can stay fast Vitest-only loops while repo acceptance still covers typecheck, lint, smoke, and build-sensitive behavior. Those app-local `verify` wrappers now also overlap their own substeps locally by default unless `MURPH_VERIFY_STEP_PARALLEL` overrides them; the tag-driven release workflow now opts into both CI overrides explicitly. The hosted-web suite is now split into five workspace buckets and the Cloudflare Node suite into three, both with app-local worker caps defaulting to `MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI. Those app lanes enable file-level Vitest parallelism locally by default, disable it in CI by default, keep in-file suite concurrency opt-in unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and accept `MURPH_VITEST_FILE_PARALLELISM` plus `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` as explicit overrides. `apps/web verify` now runs `pnpm lint` after the shared `prisma generate` plus prepared typecheck step, then overlaps `pnpm test` with the heavier smoke/build work while keeping `pnpm dev:smoke` and `next build` sequential so Next does not see a competing build, and the dev-smoke helper polls readiness every 250ms to reduce avoidable idle time once the server is already up. For `apps/web`-only work using scoped verification, `pnpm --dir apps/web lint` is also a required package-level check even when the broader `apps/web verify` superset is not run. | `apps/web/**` including hosted-app typecheck, explicit ESLint CLI coverage, one shared Prisma client generate before verify's typecheck/build steps, cold-boot `next dev` smoke under `apps/web/.next-smoke`, production build under `apps/web/.next`, and hosted browser-auth/session/onboarding/share/Linq-ingress/RevNet Vitest coverage, plus `apps/cloudflare/**` including hosted-runner typecheck, fast Node tests, and Workers-runtime tests |
| `pnpm test:packages:coverage` | Package-only runtime verification with the same broadened package-oriented Vitest suite under coverage. Starts with the tracked-artifact hygiene guard, runs `packages/contracts test` plus `packages/openclaw-plugin test`, then performs the prepared `pnpm build:test-runtime:prepared` graph, the root multi-project package smoke lane through `vitest.config.ts`, the standalone owner-package coverage commands for `core`, `hosted-execution`, `importers`, and `query`, and standalone package coverage commands for `assistant-engine`, `assistant-runtime`, `assistantd`, `cli`, `contracts`, `device-syncd`, `inboxd`, `messaging-ingress`, and `openclaw-plugin`. The dedicated `test:apps` verify lane stays out of this command and is composed back in at the full `pnpm test:coverage` layer. | Legacy curated coverage files under `packages/{core,hosted-execution,importers,query}/src/**`, standalone package coverage under `packages/{assistant-engine,assistant-runtime,assistantd,cli,contracts,device-syncd,inboxd,messaging-ingress,openclaw-plugin}/src/**/*.ts`, plus the root package test files exercised through `vitest.config.ts` |
| `pnpm test:smoke` | Standalone fixture/scenario integrity verification. | `fixtures/**`, `e2e/smoke/**`, `docs/contracts/03-command-surface.md` |

## Current CI Workflows

- `.github/workflows/host-support.yml` runs a host-support matrix on `ubuntu-latest` and `macos-latest`, installing with `pnpm install --frozen-lockfile`, building the workspace, and exercising the focused CLI host-support suite (`packages/cli/test/setup-cli.test.ts` plus `packages/cli/test/inbox-cli.test.ts`) on both hosts.
- The same workflow also runs `pnpm release:check` on `ubuntu-latest` so Linux bootstrap and release packaging stay exercised in CI in addition to the local repo commands above.
- `.github/workflows/release.yml` installs once, runs `pnpm release:check` with `MURPH_TEST_LANES_PARALLEL=1`, `MURPH_APP_VERIFY_PARALLEL=1`, and `MURPH_VERIFY_STEP_PARALLEL=1` so the release verification lane overlaps package coverage, app verification, and smoke checks even in CI, then packs the publishable tarballs once for upload/publication.

## Current Gaps

- Repo-level automation still does not run full end-to-end CLI scenario flows; it typechecks/builds the published shell plus the extracted `assistant-cli` and `setup-cli` packages, now includes inbox service/runtime tests plus parser-worker/runtime tests, and the smoke verifier still covers fixture/scenario integrity separately.
- Fixture smoke still validates manifests and command-surface coverage, not end-to-end package orchestration.
- No automated check hits a live AI Gateway or OpenAI-compatible endpoint; inbox model routing is currently verified through type/build/test coverage around the CLI surface and static smoke-manifest coverage only.
- No automated check hits a live Linq endpoint; Linq webhook delivery and outbound reply behavior are currently verified through mocked CLI, inboxd, and hosted `apps/web` tests only.
- No automated check hits a live AgentMail endpoint; email provisioning, polling, and in-thread reply behavior are currently verified through mocked CLI and inboxd tests only.
- No automated check hits a live WHOOP or other wearable OAuth provider; device-syncd auth/webhook behavior is currently verified through local service tests, route tests, and stubbed control-plane callers.
- No routine repo verification command validates a real Cloudflare Worker deploy or a real native-container rollout. `apps/cloudflare` tests now cover the in-repo worker, direct Durable Object RPC and alarms in the Workers runtime, the Durable Object/container boundary, configurable container idle-timeout wiring, selective artifact materialization plus preserved-artifact snapshot behavior, keyring-aware hosted ciphertext reads by stored `keyId`, bundle/artifact cleanup on successful transitions, and Node container-image seams, and the manual workflow `.github/workflows/deploy-cloudflare-hosted.yml` now uploads versions, creates gradual deployments, validates the required GitHub environment wiring up front including `CF_PUBLIC_BASE_URL` for smoke runs, renders explicit Wrangler observability config, declares the required hosted runtime secrets through Wrangler's experimental `secrets.required` support, and pairs the deploy docs with a checked-in transient R2 lifecycle config/helper. Live deployment still depends on operator-supplied Cloudflare credentials, GitHub environment wiring, first-time container provisioning in Cloudflare, and an operator applying the bucket lifecycle rules to the real R2 buckets.
- The tag-driven release workflow is present, uses npm trusted publishing for package publication, runs a slimmer `release:check` guard path that now validates release metadata plus `pnpm typecheck` and `pnpm test:coverage` without re-installing/re-building/re-packing inside the script, and is only exercised on real `v*.*.*` tag pushes rather than during ordinary repo verification. npm trust is package-level rather than repo-level, so this monorepo also ships `pnpm release:trust:github` for the one-time bootstrap that binds every publishable `@murphai/*` package to `cobuildwithus/murph` and `.github/workflows/release.yml`; if a package already has the wrong trusted publisher entry, that npm-side state still needs manual revoke-and-recreate repair, which local repo checks cannot fully prove.

## Update Rule

When real source code, CI, or deployment automation is added, update this file and `agent-docs/operations/verification-and-runtime.md` in the same change.
