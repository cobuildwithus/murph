# Testing And CI Map

Last verified: 2026-03-29

## Current Repo Checks

| Command | Purpose | Current coverage |
| --- | --- | --- |
| `pnpm typecheck` | Shell syntax validation, root release-helper syntax checks, the workspace-boundary audit for sibling build-artifact aliases and relative cross-package imports, repo-owned TS tools typecheck, package/app-local no-emit typecheck scripts, and the solution-style TypeScript build. The `packages/web` and `apps/web` typecheck scripts now bootstrap the exact `next-env.d.ts` route-type stub they reference so a clean clone does not depend on leftover Next output. | `scripts/*.{sh,mjs,ts}`, `e2e/smoke/verify-fixtures.ts`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**`, `packages/cli/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistant-runtime/**`, `packages/query/**`, `packages/web/**`, `apps/web/**`, `apps/cloudflare/**`, `config/workspace-source-resolution.ts` |
| `pnpm test` | Agent-docs drift checks, the workspace-boundary audit, the tracked-artifact hygiene guard, plus package/app verification and fixture/scenario integrity validation. The hygiene guard now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files before failing on remaining blocked artifacts, including non-default Next artifact dirs such as `apps/web/.next-dev/` and `apps/web/.next-smoke/`. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `packages/contracts/**`, `packages/hosted-execution/**` including hosted execution auth/env/client coverage, `packages/runtime-state/**` including hosted workspace-snapshot and artifact-externalization coverage plus hosted verified-email env helper coverage and pending assistant-usage record helpers, `packages/cli/**` including assistant state scratchpad coverage plus assistant cron and assistant status/doctor/stop resilience CLI/runtime tests, command-schema coverage, CLI `.env` loading coverage, failover/outbox/fault-injection robustness coverage, Linq outbound/setup coverage, AgentMail email outbound tests, setup email-channel discovery/reuse coverage, and provider usage extraction coverage for Codex plus OpenAI-compatible runs, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including the Oura and WHOOP config/provider/service tests plus HTTP control-plane auth/listener coverage, `packages/inboxd/**` including AgentMail email connector tests plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistant-runtime/**` including hosted verified-email self-target reconciliation coverage plus selective hosted artifact materialization, preserved-artifact snapshot behavior, and post-commit hosted AI usage export coverage, `packages/query/**`, `packages/web/**`, `apps/web/**` including hosted device-sync auth/session coverage, durable hosted execution outbox coverage for device-sync/share/onboarding dispatches, hosted AI usage import and optional Stripe token-meter coverage, Privy-backed hosted onboarding/share plus verified-email sync route coverage, the hosted cold-boot `next dev` smoke under `apps/web/.next-smoke`, hosted onboarding wallet-forwarding plus RevNet webhook idempotency coverage, and hosted Linq ingress route/control-plane coverage, `apps/cloudflare/**` including signed-dispatch verification, `/health` plus internal route alias coverage, per-user runner coordination with retry/poison state, explicit activation bootstrap coverage, encrypted workspace snapshot storage plus selective artifact-object routing, separate per-user runner env control-route and persistence coverage, committed hosted side-effect journaling plus post-commit assistant-delivery resume coverage, best-effort cleanup of orphaned per-user artifact objects, keyring-aware hosted ciphertext reads by stored `keyId`, lifecycle-friendly transient journal key coverage, isolated child-process hosted execution, one-shot hosted runner tests, smoke-helper status polling coverage, and the Workers-runtime Vitest lane for dispatch auth, direct Durable Object RPC, Durable Object alarms, bundle journaling, artifact-object routing, and runner control flows, `config/workspace-source-resolution.ts`, `fixtures/**`, `e2e/smoke/scenarios/**` |
| `pnpm test:coverage` | Doc inventory/doc-gardening enforcement plus package/app verification, Vitest V8 per-file coverage gates on the directly exercised runtime modules, command-surface smoke coverage, and the source-artifact guard that rejects handwritten JS-like source files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`, while still allowlisting the exact Next.js `packages/web/next-env.d.ts` and `apps/web/next-env.d.ts` stubs, the explicit `packages/web/postcss.config.mjs` and `apps/web/postcss.config.mjs` framework config paths, plus tracked `.env.example` files. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before the guard runs. | `agent-docs/**`, `ARCHITECTURE.md`, `README.md`, `docs/contracts/03-command-surface.md`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**`, `packages/cli/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistant-runtime/**`, `packages/query/**`, `packages/web/**`, `apps/web/**`, `apps/cloudflare/**`, `fixtures/**`, `e2e/smoke/**` |
| `pnpm test:packages` | Direct runtime verification for executable packages/apps plus built CLI verification without the worktree-sensitive doc-drift wrapper. Starts with the tracked-artifact hygiene guard, which now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files, then runs the local web package test/build path, the hosted control-plane app test/build path, and the hosted Cloudflare runner app tests before the root Vitest suite. The hosted app path now proves interactive dev isolation under `apps/web/.next-dev`, cold `next dev` smoke isolation under `apps/web/.next-smoke`, and the production build under `apps/web/.next`. | `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**` including hosted verified-email env helpers plus assistant usage pending-record helpers, `packages/cli/**` including assistant state scratchpad runtime and CLI command coverage plus assistant cron and assistant status/doctor/stop resilience runtime and CLI command coverage, CLI `.env` loading, failover/outbox/fault-injection robustness coverage, Linq delivery/setup coverage, AgentMail email delivery/setup discovery coverage, and provider usage extraction coverage, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including Oura and WHOOP provider/config coverage plus control-plane auth and public-listener route coverage, `packages/inboxd/**` including AgentMail email connector coverage plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistant-runtime/**` including hosted verified-email self-target reconciliation plus hosted AI usage export coverage, `packages/query/**`, `packages/web/**`, `apps/web/**` including hosted device-sync auth/session tests, durable execution-outbox coverage for hosted share/onboarding/device-sync dispatches, hosted AI usage import and Stripe token-meter tests, hosted verified-email sync route tests, hosted onboarding wallet-forwarding plus RevNet webhook idempotency tests, hosted Linq ingress tests, and the hosted dev smoke, `apps/cloudflare/**` including signed dispatch, health/alias route coverage, runner coordination, explicit activation bootstrap, separate per-user env control/persistence handling, hosted assistant post-commit delivery reconciliation, hosted share import handling, broader hosted bundle restore tests through the `@murph/assistant-runtime` surface, and the smaller Workers-runtime Vitest lane |
| `pnpm test:packages:coverage` | Package/app runtime verification with the same Vitest suite plus deterministic single-file execution and V8 coverage thresholds (`perFile`, `lines/functions/statements=85`, `branches=80`) on the targeted source coverage surface while device-syncd/inbox/parser/assistant-runtime tests still execute under coverage. Starts with the tracked-artifact hygiene guard, which now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files, and the local and hosted web apps plus the hosted Cloudflare runner are exercised through `packages/web test`, `apps/web test`, and `apps/cloudflare verify` so the Cloudflare app typechecks once and then runs both its Node and Workers-runtime suites, but those app tests are not yet part of the thresholded V8 include list. | `packages/core/src/{constants,ids,jsonl,mutations,raw,vault}.ts`, `packages/hosted-execution/src/{auth,client,contracts,env,routes}.ts`, `packages/importers/src/{csv-sample-importer,document-importer,meal-importer}.ts`, `packages/query/src/{export-pack,model,search,summaries,timeline}.ts`, `packages/web/test/**/*.test.ts`, `apps/web/test/**/*.test.ts`, `apps/cloudflare/test/**/*.test.ts`, `packages/*/test/**/*.test.ts` |
| `pnpm test:smoke` | Standalone fixture/scenario integrity verification. | `fixtures/**`, `e2e/smoke/**`, `docs/contracts/03-command-surface.md` |

## Current CI Workflows

- `.github/workflows/host-support.yml` runs a host-support matrix on `ubuntu-latest` and `macos-latest`, installing with `pnpm install --frozen-lockfile`, building the workspace, and exercising the focused CLI host-support suite (`packages/cli/test/setup-cli.test.ts` plus `packages/cli/test/inbox-cli.test.ts`) on both hosts.
- The same workflow also runs `pnpm release:check` on `ubuntu-latest` so Linux bootstrap and release packaging stay exercised in CI in addition to the local repo commands above.

## Current Gaps

- Repo-level automation still does not run full end-to-end CLI scenario flows; it typechecks/builds the CLI package, now includes inbox service/runtime tests plus parser-worker/runtime tests, and the smoke verifier still covers fixture/scenario integrity separately.
- Fixture smoke still validates manifests and command-surface coverage, not end-to-end package orchestration.
- No automated check hits a live AI Gateway or OpenAI-compatible endpoint; inbox model routing is currently verified through type/build/test coverage around the CLI surface and static smoke-manifest coverage only.
- No automated check hits a live Linq endpoint; Linq webhook delivery and outbound reply behavior are currently verified through mocked CLI, inboxd, and hosted `apps/web` tests only.
- No automated check hits a live AgentMail endpoint; email provisioning, polling, and in-thread reply behavior are currently verified through mocked CLI and inboxd tests only.
- No automated check hits a live WHOOP or other wearable OAuth provider; device-syncd auth/webhook behavior is currently verified through local service tests, route tests, and stubbed control-plane callers.
- No routine repo verification command validates a real Cloudflare Worker deploy or a real native-container rollout. `apps/cloudflare` tests now cover the in-repo worker, direct Durable Object RPC and alarms in the Workers runtime, the Durable Object/container boundary, configurable container idle-timeout wiring, selective artifact materialization plus preserved-artifact snapshot behavior, keyring-aware hosted ciphertext reads by stored `keyId`, bundle/artifact cleanup on successful transitions, and Node container-image seams, and the manual workflow `.github/workflows/deploy-cloudflare-hosted.yml` now uploads versions, creates gradual deployments, validates the required GitHub environment wiring up front including `CF_PUBLIC_BASE_URL` for smoke runs, renders explicit Wrangler observability config, declares the required hosted runtime secrets through Wrangler's experimental `secrets.required` support, and pairs the deploy docs with a checked-in transient R2 lifecycle config/helper. Live deployment still depends on operator-supplied Cloudflare credentials, GitHub environment wiring, first-time container provisioning in Cloudflare, and an operator applying the bucket lifecycle rules to the real R2 buckets.
- The tag-driven release workflow is present, but it is only exercised on real `v*.*.*` tag pushes and does not run a live npm publish during ordinary repo verification.

## Update Rule

When real source code, CI, or deployment automation is added, update this file and `agent-docs/operations/verification-and-runtime.md` in the same change.
