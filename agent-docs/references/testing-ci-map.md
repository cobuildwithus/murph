# Testing And CI Map

Last verified: 2026-03-26

## Current Repo Checks

| Command | Purpose | Current coverage |
| --- | --- | --- |
| `pnpm typecheck` | Shell syntax validation, root release-helper syntax checks, the workspace-boundary audit for sibling-`dist` aliases and relative cross-package imports, repo-owned TS tools typecheck, package/app-local no-emit typecheck scripts, and the solution-style TypeScript build. | `scripts/*.{sh,mjs,ts}`, `e2e/smoke/verify-fixtures.ts`, `packages/contracts/**`, `packages/runtime-state/**`, `packages/cli/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/query/**`, `packages/web/**`, `apps/web/**`, `apps/cloudflare/**`, `config/workspace-source-resolution.ts` |
| `pnpm test` | Agent-docs drift checks, the workspace-boundary audit, the tracked-artifact hygiene guard, plus package/app verification and fixture/scenario integrity validation. The hygiene guard now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files before failing on remaining blocked artifacts. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `packages/contracts/**`, `packages/runtime-state/**`, `packages/cli/**` including assistant cron plus assistant status/doctor/stop resilience CLI/runtime tests, command-schema coverage, CLI `.env` loading coverage, failover/outbox/fault-injection robustness coverage, Linq outbound/setup coverage, AgentMail email outbound tests, and setup email-channel discovery/reuse coverage, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including the Oura and WHOOP config/provider/service tests plus HTTP control-plane auth/listener coverage, `packages/inboxd/**` including AgentMail email connector tests plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/query/**`, `packages/web/**`, `apps/web/**` including hosted device-sync auth/session coverage plus hosted Linq ingress route/control-plane coverage, `apps/cloudflare/**` including signed-dispatch verification, `/health` plus internal route alias coverage, per-user runner coordination with retry/poison state, encrypted bundle storage, and one-shot hosted runner tests, `config/workspace-source-resolution.ts`, `fixtures/**`, `e2e/smoke/scenarios/**` |
| `pnpm test:coverage` | Doc inventory/doc-gardening enforcement plus package/app verification, Vitest V8 per-file coverage gates on the directly exercised runtime modules, command-surface smoke coverage, and the source-artifact guard that rejects handwritten JS-like source files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.test-dist/`, and `*.tsbuildinfo`, while still allowlisting the exact Next.js `packages/web/next-env.d.ts` and `apps/web/next-env.d.ts` stubs plus tracked `.env.example` files. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before the guard runs. | `agent-docs/**`, `ARCHITECTURE.md`, `README.md`, `docs/contracts/03-command-surface.md`, `packages/contracts/**`, `packages/runtime-state/**`, `packages/cli/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/query/**`, `packages/web/**`, `apps/web/**`, `apps/cloudflare/**`, `fixtures/**`, `e2e/smoke/**` |
| `pnpm test:packages` | Direct runtime verification for executable packages/apps plus built CLI verification without the worktree-sensitive doc-drift wrapper. Starts with the tracked-artifact hygiene guard, which now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files, then runs the local web package test/build path, the hosted control-plane app test/build path, and the hosted Cloudflare runner app tests before the root Vitest suite. | `packages/contracts/**`, `packages/runtime-state/**`, `packages/cli/**` including assistant cron plus assistant status/doctor/stop resilience runtime and CLI command coverage, CLI `.env` loading, failover/outbox/fault-injection robustness coverage, Linq delivery/setup coverage, and AgentMail email delivery/setup discovery coverage, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including Oura and WHOOP provider/config coverage plus control-plane auth and public-listener route coverage, `packages/inboxd/**` including AgentMail email connector coverage plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/query/**`, `packages/web/**`, `apps/web/**` including hosted device-sync auth/session tests plus hosted Linq ingress tests, `apps/cloudflare/**` including signed dispatch, health/alias route coverage, runner coordination, and broader hosted bundle restore tests |
| `pnpm test:packages:coverage` | Package/app runtime verification with the same Vitest suite plus deterministic single-file execution and V8 coverage thresholds (`perFile`, `lines/functions/statements=85`, `branches=80`) on the targeted source coverage surface while device-syncd/inbox/parser tests still execute under coverage. Starts with the tracked-artifact hygiene guard, which now prunes untracked generated JS/declaration sidecars adjacent to tracked TypeScript source files, and the local and hosted web apps plus the hosted Cloudflare runner are exercised through `packages/web test`, `apps/web test`, and `apps/cloudflare test` but are not yet part of the thresholded V8 include list. | `packages/core/src/{constants,ids,jsonl,mutations,raw,vault}.ts`, `packages/importers/src/{csv-sample-importer,document-importer,meal-importer}.ts`, `packages/query/src/{export-pack,model,search,search-sqlite,summaries,timeline}.ts`, `packages/web/test/**/*.test.ts`, `apps/web/test/**/*.test.ts`, `apps/cloudflare/test/**/*.test.ts`, `packages/*/test/**/*.test.ts` |
| `pnpm test:smoke` | Standalone fixture/scenario integrity verification. | `fixtures/**`, `e2e/smoke/**`, `docs/contracts/03-command-surface.md` |

## Current CI Workflows

- `.github/workflows/host-support.yml` runs a host-support matrix on `ubuntu-latest` and `macos-latest`, installing with `pnpm install --frozen-lockfile`, building the workspace, and exercising the focused CLI host-support suite (`packages/cli/test/setup-cli.test.ts` plus `packages/cli/test/inbox-cli.test.ts`) on both hosts.
- The same workflow also runs `pnpm release:check` on `ubuntu-latest` so Linux bootstrap and release packaging stay exercised in CI in addition to the local repo commands above.

## Current Gaps

- Repo-level automation still does not run full end-to-end CLI scenario flows; it typechecks/builds the CLI package, now includes inbox service/runtime tests plus parser-worker/runtime tests, and the smoke verifier still covers fixture/scenario integrity separately.
- Fixture smoke still validates manifests and command-surface coverage, not end-to-end package orchestration.
- No automated check hits a live AI Gateway or OpenAI-compatible endpoint; inbox model routing is currently verified through type/build/test coverage around the CLI surface and static smoke-manifest coverage only.
- No automated check hits a live Linq endpoint; Linq webhook delivery and outbound reply behavior are currently verified through mocked CLI, inboxd, and hosted `apps/web` tests only.
- No automated check hits a live AgentMail endpoint; email provisioning, polling, and in-thread reply behavior are currently verified through mocked CLI and inboxd tests only.
- No automated check hits a live WHOOP or other wearable OAuth provider; device-syncd auth/webhook behavior is currently verified through local service tests, route tests, and stubbed control-plane callers.
- No automated check validates a real Cloudflare Worker deploy or the hosted runner container image. `apps/cloudflare` tests cover the in-repo worker and Node runner seams only; `apps/cloudflare/wrangler.jsonc`, `apps/cloudflare/.dev.vars.example`, `apps/cloudflare/.runner.env.example`, and `Dockerfile.cloudflare-hosted-runner` remain manual scaffolds.
- The tag-driven release workflow is present, but it is only exercised on real `v*.*.*` tag pushes and does not run a live npm publish during ordinary repo verification.

## Update Rule

When real source code, CI, or deployment automation is added, update this file and `agent-docs/operations/verification-and-runtime.md` in the same change.
