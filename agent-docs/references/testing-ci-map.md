# Testing And CI Map

Last verified: 2026-04-29

## Current Repo Checks

| Command | Purpose | Current coverage |
| --- | --- | --- |
| `pnpm typecheck` | Shell syntax validation, root release-helper syntax checks, the workspace-boundary audit for sibling build-artifact aliases and relative cross-package imports, the workspace package-cycle guard, the raw health/model/vault payload logging guard, repo-owned TS tools typecheck, `packages/contracts` build, and package/app-local no-emit typecheck scripts. The independent preflight checks now run concurrently by default, and the package/app fanout starts as soon as the contracts build prerequisite succeeds instead of waiting for every independent guard. The package/app fanout defaults to up to six workers locally based on logical CPU count, or two in CI. The full workspace solution build now lives on the explicit `pnpm build:workspace:clean` / `pnpm build:workspace:incremental` lanes instead of the default local typecheck loop, and the build graph's `tsbuildinfo` files now live outside `dist/` so warm incremental runs can reuse them. The `apps/web` typecheck script bootstraps the exact `next-env.d.ts` route-type stub it references and regenerates the ignored Health Commons catalog before importing it, so a clean clone does not depend on leftover Next or catalog output. The root verify harness now also prints per-step timing and keeps the per-worktree workspace-artifact lock only on the artifact-sensitive top-level commands, so `typecheck` still serializes correctly around shared generated outputs without forcing ordinary `test:diff` source lanes to wait behind it. | `scripts/*.{sh,mjs,ts}`, `e2e/smoke/{verify-scenario-integrity.ts,verify-fixtures.ts}`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/runtime-state/**`, `packages/operator-config/**`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/setup-cli/**`, `packages/cli/**`, `packages/openclaw-plugin/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistantd/**`, `packages/assistant-runtime/**`, `packages/query/**`, `apps/web/**`, `apps/cloudflare/**`, `config/workspace-source-resolution.ts` |
| `pnpm test:diff` | Diff-aware agent/local iteration lane. It inspects the current worktree diff against `HEAD` plus untracked files, maps touched files to workspace owners, expands to reverse workspace dependents, then runs the matching package/app-local `typecheck`, `test`, and `verify` steps for that subset. Docs/process/verification-tooling-only diffs, and other no-owner non-workspace diffs, stay on the narrow tooling checks (`shell syntax`, `node --check`, repo TS tools typecheck, and dependency policy) instead of fanning out to package/app lanes or running unrelated workspace-boundary audits, while root workspace-manifest changes such as `pnpm-lock.yaml` or `pnpm-workspace.yaml` broaden to the whole workspace. Reverse-dependent `packages/cli` fanout now stays on the package's source-first `typecheck` and `test` scripts by default; the built-runtime/package-shape `verify:cli` lane only runs when the diff directly touches CLI artifact-sensitive surfaces such as the CLI package manifest/build/package-shape config, the CLI workspace Vitest configs, the prepared-runtime helper, or the root workspace manifests. Use `bash scripts/workspace-verify.sh test:diff <path ...>` when the branch already has unrelated edits and the lane should target only the current task files. | Affected workspace owners plus reverse dependents under `packages/**` and `apps/**`, repo-internal tooling fast-path files under `agent-docs/**`, `docs/**`, `scripts/**`, `AGENTS.md`, `ARCHITECTURE.md`, `README.md`, `package.json`, `vitest.config.ts`, and root `tsconfig*.json`, plus whole-workspace fan-out when root workspace manifests change, and the nested built-CLI verification lane only for CLI artifact-sensitive diffs |
| `pnpm test` | Fast deterministic local behavior loop only. It runs `packages/contracts test`, `packages/openclaw-plugin test`, the root multi-project Vitest lane, and fixture/scenario-manifest verification without coverage. It does not run dependency-policy or workspace-boundary/package-cycle checks, tracked-artifact hygiene, app verification, prepared-runtime/package-shape acceptance gates, or any git/worktree-sensitive skip behavior. Local runs still overlap repo Vitest with scenario-manifest verification by default; CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it. Retries remain opt-in through `MURPH_VERIFY_RETRY_COUNT`. | `packages/contracts/**`, `packages/hosted-execution/**` including hosted execution auth/env/client coverage plus `vault.sync.import` contract/parser coverage, `packages/runtime-state/**` including hosted workspace-snapshot and artifact-externalization coverage plus hosted verified-email env helper coverage and pending assistant-usage record helpers, `packages/operator-config/**` including setup/runtime-env/config persistence coverage, `packages/assistant-engine/**` including the local assistant runtime, provider-turn, tool-catalog, outbox, and owner-boundary coverage, `packages/assistant-cli/**` including CLI-only assistant wrappers, terminal logging, assistant command routing, and Ink chat UI coverage, `packages/setup-cli/**` including onboarding, host setup, and setup-wizard coverage, `packages/cli/**` including the published shell, command-schema coverage, CLI `.env` loading coverage, compatibility-wrapper coverage, and assistantd client routing coverage, `packages/openclaw-plugin/**` including the published OpenClaw bundle metadata and Murph skill guidance package-local test, `packages/core/**` including canonical-only vault-sync import pack and additive non-clobbering merge coverage, `packages/importers/**`, `packages/device-syncd/**` including the Oura and WHOOP config/provider/service tests plus HTTP control-plane auth/listener coverage, `packages/inboxd/**` including AgentMail email connector tests plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistantd/**` including loopback host validation, bearer-authenticated control-plane routing, single-vault request enforcement, and the direct owner-package boundary regressions, `packages/assistant-runtime/**` including hosted assistant profile seeding/adoption coverage, fail-closed hosted automation gating, hosted verified-email self-target reconciliation coverage, the direct owner-package boundary checks for hosted consumers, selective hosted artifact materialization, preserved-artifact snapshot behavior, post-commit hosted AI usage export coverage, scrubbed isolated-child env launch, stripped supervisor proxy vars, per-run writable cache/temp roots, launch-time supervisor-`HOME` avoidance, hosted vault-sync import event handling, and child-process reaping on abort, `packages/query/**`, `fixtures/**`, `e2e/smoke/scenarios/**` |
| `pnpm verify:acceptance` | Canonical CI/release acceptance gate. It runs the root verifier's full typecheck surface first, then the coverage-heavy acceptance lane while skipping only work already proven inside the same acceptance process: repeated repo guards, Cloudflare app-local typecheck, and the contracts artifact rebuild. When acceptance parallelism is enabled, the Cloudflare app verification lane can overlap package coverage and scenario-integrity coverage, while hosted web verification still waits until package coverage has released shared Prisma and Next build resources. | The full `pnpm typecheck` surface plus the full `pnpm test:coverage` coverage/app/smoke surface below, without duplicate guard/typecheck/build repeats inside the same command |
| `pnpm docs:drift` | Manual durable-doc drift check. Use this when you intentionally change durable repo docs and want the old index/truthfulness enforcement without making the default `pnpm test` lane sensitive to unrelated dirty-tree doc work. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `README.md`, `package.json` |
| `pnpm test:coverage` | Coverage-focused acceptance lane, not the default local loop. Standalone `pnpm test:coverage` repeats the dependency-policy and workspace-boundary/package-cycle guards, runs doc-gardening validation, then runs every package-local `test:coverage` command under `packages/*`, the dedicated heavier app verify lane, scenario-integrity coverage, and the source-artifact guard that rejects handwritten JS-like source files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`, while still allowlisting the exact Next.js `apps/web/next-env.d.ts` stub, the explicit `apps/web/{postcss,eslint}.config.mjs` framework config paths, plus tracked `.env.example` files. Local runs now overlap package coverage with scenario-integrity verification by default; when `MURPH_ACCEPTANCE_EARLY_CLOUDFLARE_VERIFY=1` (defaulting to `MURPH_APP_VERIFY_PARALLEL`), Cloudflare app verification overlaps those branches too, while hosted web verification still waits until package coverage releases shared Prisma and Next build resources. Package coverage itself uses a bounded local outer fanout of `4` by default. CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it, and CI package coverage itself stays at one outer package at a time by default. The root acceptance harness plus the direct app/package-shape verification wrappers still take the same per-worktree workspace-artifact lock, and `pnpm verify:acceptance` now holds that lock once across typecheck and coverage so concurrent artifact-sensitive acceptance invocations on one clone queue instead of clobbering shared emitted artifacts. The tag-driven release workflow still opts into that outer-lane override explicitly. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before the guard runs. Retries are opt-in through `MURPH_VERIFY_RETRY_COUNT`. | `agent-docs/**`, `ARCHITECTURE.md`, `README.md`, `docs/contracts/03-command-surface.md`, `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cli,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,gateway-local,hosted-execution,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/**`, `apps/web/**`, `apps/cloudflare/**`, `fixtures/**`, `e2e/smoke/**` |
| `pnpm test:packages` | Package-only behavior verification without the heavier app or built-runtime/package-shape acceptance gates. It runs `packages/contracts test` plus `packages/openclaw-plugin test`, then executes the root multi-project Vitest package suite. That root package lane now runs the full package-local test surfaces for every repo-owned Vitest package wired into `vitest.config.ts`, including `inbox-services` and `vault-usecases`, while still leaving the heavier local/hosted web and Cloudflare verify work to `pnpm test:apps`. The CLI portion also now runs the full six serial-safe workspace buckets in source-first mode instead of a narrower hand-picked subset. Vitest concurrency still defaults to `MURPH_VITEST_MAX_WORKERS=75%` locally or `50%` in CI unless the environment overrides it. File-level Vitest parallelism is enabled locally by default, disabled in CI by default, in-file suite concurrency is opt-in across repo-owned Vitest suites unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and `MURPH_VITEST_FILE_PARALLELISM`, `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` provide explicit overrides. Built-runtime CLI proof now lives under `pnpm verify:cli` and the acceptance lanes instead of blocking this package behavior loop. | `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,gateway-local,hosted-execution,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/**`, plus `packages/cli/**` through its source-first workspace buckets |
| `pnpm test:apps` | Dedicated heavier app verification lane. Runs `apps/web verify` and `apps/cloudflare verify`, defaulting to parallel local execution and sequential CI execution unless `MURPH_APP_VERIFY_PARALLEL` overrides it, so app-local `test` commands can stay fast Vitest-only loops while repo acceptance still covers typecheck, lint, smoke, and build-sensitive behavior. `apps/web verify` now defaults locally to `health-commons:generate`, `prisma:generate`, `pnpm dev:smoke`, then parallel `next build` / `pnpm test` / `pnpm lint`; CI keeps hosted-web substeps serial unless `MURPH_VERIFY_STEP_PARALLEL=1` is set, and `apps/web verify:parallel` forces the same parallel path explicitly. `apps/cloudflare verify` still defaults to serial substeps locally, can skip its own typecheck only when `MURPH_CLOUDFLARE_VERIFY_SKIP_TYPECHECK=1` is set by the root acceptance wrapper after root typecheck has already covered the app, and `apps/cloudflare verify:parallel` opts back into overlapping its internal heavy work when desired. The tag-driven release workflow still opts into the outer CI overrides explicitly. The hosted-web suite is now split into five workspace buckets and the Cloudflare Node suite into three, both with app-local worker caps defaulting to `MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI. Those app lanes enable file-level Vitest parallelism locally by default, disable it in CI by default, keep in-file suite concurrency opt-in unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and accept `MURPH_VITEST_FILE_PARALLELISM` plus `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` as explicit overrides. The hosted-web dev-smoke helper checks route types and Turbopack cache paths directly, polls readiness every 250ms, and reuses its local Turbopack smoke cache by default; CI still prunes the cache, and `MURPH_HOSTED_WEB_SMOKE_PRUNE_CACHE=1` forces a cold local smoke when needed. For `apps/web`-only work using scoped verification, `pnpm --dir apps/web lint` is also a required package-level check even when the broader `apps/web verify` superset is not run. | `apps/web/**` including hosted-app typecheck, explicit ESLint CLI coverage, one Health Commons catalog generate plus one shared Prisma client generate before verify's typecheck/build/test steps, local cached `next dev` smoke under `apps/web/.next-smoke`, production build under `apps/web/.next`, and hosted browser-auth/session/onboarding/vault-sync/Linq-ingress Vitest coverage, plus `apps/cloudflare/**` including hosted-runner typecheck, fast Node tests, Workers-runtime tests, and hosted vault-sync side-input hydration coverage. Repo-local source resolution still flows through `config/workspace-source-resolution.ts`, but Vitest subpaths now resolve only through explicit workspace entries plus package-declared public exports instead of wildcarding arbitrary package internals. The separate `.github/workflows/cloudflare-hosted-e2e.yml` lane now adds focused local hosted E2E checks for the device-connect smoke plus Linq and Telegram delivery flows on relevant pull requests without broadening the default app verify surface to the full serial E2E bundle. For local manual proof, `pnpm --dir apps/cloudflare test:e2e:local` is broader than the focused CI slices: it runs the hosted-local full-stack E2E suite and then the Workers-runtime E2E lane. |
| `pnpm test:packages:coverage` | Package-only runtime verification under coverage. Starts with the tracked-artifact hygiene guard, performs the prepared `pnpm build:test-runtime:prepared` graph, then runs every package-local `test:coverage` command under `packages/*`; under `pnpm verify:acceptance`, the contracts package keeps the same Vitest coverage plus generated-schema verification but reuses the contracts build from the earlier root typecheck instead of rebuilding it. Local runs fan those package coverage commands out with bounded outer concurrency of `4` by default, while CI keeps the package coverage lane at one outer package at a time by default. The dedicated `test:apps` verify lane stays out of this command and is composed back in at the full `pnpm test:coverage` layer. Shared package coverage thresholds are enforced at the package aggregate level, not as per-file thresholds; packages can still set stricter package-local coverage config when a file-level gate is intentionally useful. | Package-wide coverage under `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cli,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,gateway-local,hosted-execution,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/src/**/*.ts` |
| `pnpm test:smoke` | Current root command for fixture/scenario-manifest integrity verification; this lane is not executable end-to-end smoke today. | `fixtures/**`, `e2e/smoke/**`, `docs/contracts/03-command-surface.md` |

## Current CI Workflows

- `.github/workflows/host-support.yml` runs a host-support matrix on `ubuntu-latest` and `macos-latest`, installing with `pnpm install --frozen-lockfile`, building the workspace, preparing `pnpm build:test-runtime:prepared`, and then exercising the focused built-runtime CLI host-support suite (`packages/cli/test/setup-cli.test.ts`, `packages/cli/test/cli-expansion-inbox-attachments.test.ts`, and `packages/cli/test/inbox-service-boundaries.test.ts`) with `MURPH_PREPARED_CLI_RUNTIME_ARTIFACTS=1` on both hosts. The macOS host leg serializes package-script workspace builds so sibling `tsc -b --force` package scripts do not rewrite shared project-reference declaration outputs concurrently while the Linux leg keeps the normal package-build fanout. The workflow also carries deterministic CI-only hosted-web build placeholders for `DATABASE_URL`, `DEVICE_SYNC_ENCRYPTION_KEY`, `DEVICE_SYNC_ENCRYPTION_KEY_VERSION`, contact-privacy, and the public Privy app id so its Linux release shards can finish `apps/web verify` without inheriting production secrets.
- The same workflow also preserves the Ubuntu `pnpm release:check` surface without running it as one long job: release metadata/build/typecheck, package coverage shards, app verification, and fixture coverage run as parallel jobs, then a final `Release checks (ubuntu)` aggregator preserves the required-check name. This keeps Linux bootstrap and release packaging exercised in CI while avoiding the serial package-coverage wall clock.
- `.github/workflows/cloudflare-hosted-e2e.yml` runs three focused hosted local E2E jobs on `ubuntu-latest` for pull requests that touch the hosted welcome path or its local harness (`apps/cloudflare/**`, `packages/assistant-engine/**`, `packages/assistant-runtime/**`, `packages/cloudflare-hosted-control/**`, `packages/hosted-execution/**`, and `scripts/dev-hosted-local/**`). Each job provisions a local `postgres` service container and points `DATABASE_URL` at `127.0.0.1:5432/murph_test`, while still using deterministic CI-only hosted-web placeholders for device-sync/contact-privacy/Privy values. The workflow then runs `pnpm --dir apps/cloudflare test:e2e:device-connect:local`, `pnpm --dir apps/cloudflare test:e2e:linq-delivery:local`, and `pnpm --dir apps/cloudflare test:e2e:telegram:local`, and always uploads per-job logs instead of broadening to the full serial hosted-local E2E suite. The local aggregate `pnpm --dir apps/cloudflare test:e2e:local` also runs the Workers-runtime E2E lane through `test:e2e:workers:local`; CI keeps that narrower Workers proof inside `apps/cloudflare verify` / `test:workers` rather than duplicating it in each hosted-local E2E job.
- `.github/workflows/release.yml` installs once, runs `pnpm release:check` with `MURPH_TEST_LANES_PARALLEL=1`, `MURPH_APP_VERIFY_PARALLEL=1`, and `MURPH_VERIFY_STEP_PARALLEL=1` so the release verification lane overlaps package coverage, Cloudflare app verification, and scenario-manifest checks even in CI before hosted web verification runs, while the same deterministic hosted-web build placeholders keep `apps/web verify` on its truthful build path without injecting production DB or device-sync secrets, then packs the publishable tarballs once for upload/publication.

## Current Gaps

- Repo-level automation still does not run full end-to-end CLI scenario flows; it typechecks/builds the published shell plus the extracted `assistant-cli` and `setup-cli` packages, now includes inbox service/runtime tests plus parser-worker/runtime tests, and the `test:smoke` lane still covers fixture/scenario-manifest integrity separately.
- The current fixture/scenario lane still validates manifests and command-surface coverage, not end-to-end package orchestration.
- No automated check launches a live Codex App Server or a live inbox model-routing backend by default; Codex assistant command surfaces and the audit-only inbox bundle helper are currently verified through type/build/test coverage around the CLI surface and static scenario-manifest coverage only. Codex App Server file/PDF inputs are not advertised as natively supported unless the app-server protocol grows a supported file input item.
- No automated check hits a live Linq endpoint; Linq webhook delivery and outbound reply behavior are currently verified through mocked CLI, inboxd, and hosted `apps/web` tests only.
- No automated check hits a live AgentMail endpoint; email provisioning, polling, and in-thread reply behavior are currently verified through mocked CLI and inboxd tests only.
- No automated check hits a live WHOOP or other wearable OAuth provider; device-syncd auth/webhook behavior is currently verified through local service tests, route tests, stubbed control-plane callers, and the hosted-local device-connect smoke that creates a signed WHOOP connect link against synthetic provider config.
- No routine repo verification command validates a real Cloudflare Worker deploy or a real Cloudflare-managed native-container rollout. `apps/cloudflare` tests now cover the in-repo worker, direct Durable Object RPC and alarms in the Workers runtime, the Durable Object/container boundary, configurable container idle-timeout wiring, selective artifact materialization plus preserved-artifact snapshot behavior, keyring-aware hosted ciphertext reads by stored `keyId`, bundle/artifact cleanup on successful transitions, and Node container-image seams. The repo now also ships `pnpm --dir apps/cloudflare runner:docker:smoke` as a local final-image smoke: it rebuilds the hosted-runner workspace closure, assembles a fresh runner bundle, overlays the smoke entrypoints into that bundle, prepares the cached native base image, builds the app image locally, restores a real fixture vault into an isolated child-run workspace inside the container, exercises the shared `@murphai/parsers` attachment pipeline, and records the selected provider ids so the proof explicitly covers the shipped `murph` / `vault-cli` bins plus native `pdftotext`, `whisper.cpp`, and ffmpeg-backed normalization/transcription behavior under the hosted runner's rebound `HOME` / `VAULT` model. The manual workflow `.github/workflows/deploy-cloudflare-hosted.yml` now renders the deploy artifacts, prepares the native runner base image with Docker Buildx/GitHub cache, runs the focused Cloudflare verify lane, executes a direct `wrangler deploy`, validates the required GitHub environment wiring up front including `CF_PUBLIC_BASE_URL` for smoke runs, declares the required hosted runtime secrets through Wrangler's experimental `secrets.required` support, and pairs the deploy docs with a checked-in transient R2 lifecycle config/helper. The lower-level version/deployment split remains helper-only for manual recovery. Live deployment still depends on operator-supplied Cloudflare credentials, GitHub environment wiring, first-time container provisioning in Cloudflare, and an operator applying the bucket lifecycle rules to the real R2 buckets.
- The tag-driven release workflow is present, uses npm trusted publishing for package publication, runs a slimmer `release:check` guard path that now validates release metadata plus `pnpm build:workspace:clean` and `pnpm verify:acceptance` without re-installing/re-building/re-packing inside the script, and is only exercised on real `v*.*.*` tag pushes rather than during ordinary repo verification. npm trust is package-level rather than repo-level, so this monorepo also ships `pnpm release:trust:github` for the one-time bootstrap that binds every publishable `@murphai/*` package to `cobuildwithus/murph` and `.github/workflows/release.yml`; if a package already has the wrong trusted publisher entry, that npm-side state still needs manual revoke-and-recreate repair, which local repo checks cannot fully prove.

## Update Rule

When real source code, CI, or deployment automation is added, update this file and `agent-docs/operations/verification-and-runtime.md` in the same change.
