# Testing And CI Map

Last verified: 2026-05-21

## Current Repo Checks

| Command | Purpose | Current coverage |
| --- | --- | --- |
| `pnpm typecheck` | Shell syntax validation, root release-helper syntax checks, the workspace-boundary audit for sibling build-artifact aliases and relative cross-package imports, the workspace package-cycle guard, the hosted Temporal orchestration architecture guard, the raw health/model/vault payload logging guard, repo-owned TS tools typecheck, `packages/contracts` build, and package/app-local no-emit typecheck scripts. The independent preflight checks now run concurrently by default, and the package/app fanout starts as soon as the contracts build prerequisite succeeds instead of waiting for every independent guard. The package/app fanout defaults to up to eight workers locally based on logical CPU count, or two in CI. The full workspace solution build now lives on the explicit `pnpm build:workspace:clean` / `pnpm build:workspace:incremental` lanes instead of the default local typecheck loop, and the build graph's `tsbuildinfo` files now live outside `dist/` so warm incremental runs can reuse them. The `apps/web` typecheck script bootstraps ignored `next-env.d.ts` and its route-type stub when needed, then regenerates the ignored Health Commons catalog before importing it, so a clean clone does not depend on leftover Next or catalog output. The root verify harness now also prints per-step timing and keeps the per-worktree workspace-artifact lock only on the artifact-sensitive top-level commands, so `typecheck` still serializes correctly around shared generated outputs without forcing ordinary `test:diff` source lanes to wait behind it. | `scripts/*.{sh,mjs,ts}`, `e2e/smoke/{verify-scenario-integrity.ts,verify-fixtures.ts}`, `packages/contracts/**`, `packages/hosted-execution/**`, `packages/hosted-orchestrator-temporal/**`, `packages/runtime-state/**`, `packages/operator-config/**`, `packages/assistant-engine/**`, `packages/assistant-cli/**`, `packages/setup-cli/**`, `packages/cli/**`, `packages/openclaw-plugin/**`, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**`, `packages/inboxd/**`, `packages/parsers/**`, `packages/assistantd/**`, `packages/assistant-runtime/**`, `packages/health-metrics/**`, `packages/query/**`, `apps/web/**`, `apps/cloudflare/**`, `config/workspace-source-resolution.ts` |
| `pnpm test:diff` | Diff-aware agent/local iteration lane. It inspects the current worktree diff against `HEAD` plus untracked files, maps touched files to workspace owners, expands to reverse workspace dependents, then runs the matching package/app-local `typecheck`, `test`, and `verify` steps for that subset. Docs/process/verification-tooling-only diffs, and other no-owner non-workspace diffs, stay on the narrow tooling checks (`shell syntax`, `node --check`, hosted Temporal orchestration guard, repo TS tools typecheck, and dependency policy) instead of fanning out to package/app lanes or running unrelated workspace-boundary audits, while root workspace-manifest changes such as `pnpm-lock.yaml` or `pnpm-workspace.yaml` broaden to the whole workspace. Reverse-dependent `packages/cli` fanout now stays on the package's source-first `typecheck` and `test` scripts by default; the built-runtime/package-shape `verify:cli` lane only runs when the diff directly touches CLI artifact-sensitive surfaces such as the CLI package manifest/build/package-shape config, the CLI workspace Vitest configs, the prepared-runtime helper, or the root workspace manifests. When `packages/inboxd` or `packages/messaging-ingress` are in the affected package test set, `test:diff` also runs their explicit `verify:package-boundary` scripts after the source-first package test so built subpath resolution is not skipped by the scoped lane. Use `bash scripts/workspace-verify.sh test:diff <path ...>` when the branch already has unrelated edits and the lane should target only the current task files. | Affected workspace owners plus reverse dependents under `packages/**` and `apps/**`, repo-internal tooling fast-path files under `agent-docs/**`, `docs/**`, `scripts/**`, `AGENTS.md`, `ARCHITECTURE.md`, `README.md`, `package.json`, `vitest.config.ts`, and root `tsconfig*.json`, plus whole-workspace fan-out when root workspace manifests change, the nested built-CLI verification lane only for CLI artifact-sensitive diffs, and explicit built package-boundary scripts for affected inboxd/messaging-ingress diffs |
| `pnpm test` | Fast deterministic local behavior loop only. It runs `packages/contracts test`, `packages/openclaw-plugin test`, the root multi-project Vitest lane, and fixture/scenario-manifest verification without coverage. It does not run dependency-policy or workspace-boundary/package-cycle checks, tracked-artifact hygiene, app verification, prepared-runtime/package-shape acceptance gates, or any git/worktree-sensitive skip behavior. Local runs still overlap repo Vitest with scenario-manifest verification by default; CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it. Retries remain opt-in through `MURPH_VERIFY_RETRY_COUNT`. | `packages/contracts/**`, `packages/hosted-execution/**` including hosted execution auth/env/client coverage, `packages/hosted-orchestrator-temporal/**` including Temporal workflow signal coalescing, timer, Activity boundary, and local env harness coverage, `packages/runtime-state/**` including hosted workspace-snapshot and artifact-externalization coverage plus hosted verified-email env helper coverage and assistant usage record parsing, `packages/operator-config/**` including setup/runtime-env/config persistence coverage, `packages/assistant-engine/**` including the local assistant runtime, provider-turn, tool-catalog, outbox, direct hosted usage recorder, and owner-boundary coverage, `packages/assistant-cli/**` including CLI-only assistant wrappers, terminal logging, assistant command routing, and Ink chat UI coverage, `packages/setup-cli/**` including onboarding, host setup, and setup-wizard coverage, `packages/cli/**` including the published shell, command-schema coverage, CLI `.env` loading coverage, compatibility-wrapper coverage, and assistantd client routing coverage, `packages/openclaw-plugin/**` including the published OpenClaw bundle metadata and Murph skill guidance package-local test, `packages/core/**`, `packages/importers/**`, `packages/device-syncd/**` including the Oura and WHOOP config/provider/service tests plus HTTP control-plane auth/listener coverage, `packages/inboxd/**` including AgentMail email connector tests plus shared Linq webhook verification coverage, `packages/parsers/**`, `packages/assistantd/**` including loopback host validation, bearer-authenticated control-plane routing, single-vault request enforcement, and the direct owner-package boundary regressions, `packages/assistant-runtime/**` including hosted assistant profile seeding/adoption coverage, fail-closed hosted automation gating, hosted verified-email self-target reconciliation coverage, the direct owner-package boundary checks for hosted consumers, selective hosted artifact materialization, preserved-artifact snapshot behavior, direct hosted AI usage recording, explicit runtime-env projection, Cloudflare-managed proxy env preservation, per-user warm workspace roots, invocation-local writable cache/temp roots, runtime wake coalescing, and container cleanup poisoning when process residue cannot be proven, `packages/health-metrics/**`, `packages/query/**`, `fixtures/**`, `e2e/smoke/scenarios/**` |
| `pnpm verify:acceptance` | Canonical CI/release acceptance gate. It runs the root verifier's full typecheck surface first, then the coverage-heavy acceptance lane while skipping only work already proven inside the same acceptance process: repeated repo guards, Cloudflare app-local typecheck, and the contracts artifact rebuild. Local acceptance now prepares shared runtime artifacts, runs package coverage cleanup plus tracked-artifact hygiene, then overlaps package coverage, scenario-integrity coverage, and both app verifiers by default, with app verification delayed 45s by `MURPH_ACCEPTANCE_APP_VERIFY_DELAY_SECONDS` so package coverage clears its heaviest early work first; CI keeps app/package overlap opt-in through `MURPH_ACCEPTANCE_APP_VERIFY_WITH_COVERAGE=1` and defaults that delay to 0 unless explicitly set. | The full `pnpm typecheck` surface plus the full `pnpm test:coverage` coverage/app/smoke surface below, without duplicate guard/typecheck/build repeats inside the same command |
| `pnpm docs:drift` | Manual durable-doc drift check. Use this when you intentionally change durable repo docs and want the old index/truthfulness enforcement without making the default `pnpm test` lane sensitive to unrelated dirty-tree doc work. | `AGENTS.md`, `ARCHITECTURE.md`, `agent-docs/**`, `README.md`, `package.json` |
| `pnpm --dir packages/health-commons verify` | Package-local Health Commons verification. Use this for authored Health Commons content, generator, schema, or package test changes. Root acceptance regenerates the ignored catalog for app/typecheck consumers, but does not replace this package-local check. | `packages/health-commons/**` |
| `pnpm test:coverage` | Coverage-focused acceptance lane, not the default local loop. Standalone `pnpm test:coverage` repeats the dependency-policy and workspace-boundary/package-cycle guards, runs doc-gardening validation, then runs every package-local `test:coverage` command under `packages/*`, the dedicated heavier app verify lane, scenario-integrity coverage, and the source-artifact guard that rejects handwritten JS-like source files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`, while still allowlisting the exact Next.js `apps/web/next-env.d.ts` stub, the explicit `apps/web/{postcss,eslint}.config.mjs` framework config paths, plus tracked `.env.example` files. Local runs prepare shared runtime artifacts, run package coverage cleanup plus tracked-artifact hygiene, then overlap package coverage, scenario-integrity verification, and both app verifiers by default through `MURPH_ACCEPTANCE_APP_VERIFY_WITH_COVERAGE=1`; the app verifier branch starts after the local `MURPH_ACCEPTANCE_APP_VERIFY_DELAY_SECONDS=45` delay by default to preserve package coverage throughput, while CI keeps app/package overlap opt-in and defaults that delay to 0. The legacy Cloudflare-only overlap remains available with `MURPH_ACCEPTANCE_EARLY_CLOUDFLARE_VERIFY=1` when full app overlap is disabled. Package coverage itself uses a bounded local outer fanout of `6` by default, drops to `MURPH_PACKAGE_COVERAGE_CLI_ACTIVE_CONCURRENCY=4` locally while CLI coverage is active, and refills freed package slots immediately. CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it, and CI package coverage itself stays at one outer package at a time by default. The root acceptance harness plus the direct app/package-shape verification wrappers still take the same per-worktree workspace-artifact lock, and `pnpm verify:acceptance` now holds that lock once across typecheck and coverage so concurrent artifact-sensitive acceptance invocations on one clone queue instead of clobbering shared emitted artifacts. Under `pnpm verify:acceptance`, the coverage phase reuses Health Commons generated output and hosted-web Prisma client generation from the immediately preceding root typecheck; standalone `pnpm test:coverage` remains self-contained and prepares those artifacts itself. The tag-driven release workflow still opts into the listed lane-parallelism variables explicitly, but acceptance app/package overlap remains opt-in in CI unless `MURPH_ACCEPTANCE_APP_VERIFY_WITH_COVERAGE=1` is also set. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before the guard runs. Retries are opt-in through `MURPH_VERIFY_RETRY_COUNT`. | `agent-docs/**`, `ARCHITECTURE.md`, `README.md`, `docs/contracts/03-command-surface.md`, `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cli,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,health-metrics,hosted-execution,hosted-orchestrator-temporal,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/**`, `apps/web/**`, `apps/cloudflare/**`, `fixtures/**`, `e2e/smoke/**` |
| `pnpm test:packages` | Package-only behavior verification without the heavier app or built-runtime/package-shape acceptance gates. It runs `packages/contracts test` plus `packages/openclaw-plugin test`, then executes the root multi-project Vitest package suite. That root package lane now runs the full package-local test surfaces for every repo-owned Vitest package wired into `vitest.config.ts`, including `inbox-services` and `vault-usecases`, while still leaving the heavier local/hosted web and Cloudflare verify work to `pnpm test:apps`. The CLI portion also now runs the full eight serial-safe workspace buckets in source-first mode instead of a narrower hand-picked subset, with the long device and release/audit smoke groups split out from the broader schema smoke bucket. Vitest concurrency still defaults to `MURPH_VITEST_MAX_WORKERS=75%` locally or `50%` in CI unless the environment overrides it. File-level Vitest parallelism is enabled locally by default, disabled in CI by default, in-file suite concurrency is opt-in across repo-owned Vitest suites unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and `MURPH_VITEST_FILE_PARALLELISM`, `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` provide explicit overrides. Built-runtime CLI proof now lives under `pnpm verify:cli` and the acceptance lanes instead of blocking this package behavior loop. | `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,health-metrics,hosted-execution,hosted-orchestrator-temporal,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/**`, plus `packages/cli/**` through its source-first workspace buckets |
| `pnpm test:apps` | Dedicated heavier app verification lane. Runs `apps/web verify` and `apps/cloudflare verify`, defaulting to parallel local execution and sequential CI execution unless `MURPH_APP_VERIFY_PARALLEL` overrides it, so app-local `test` commands can stay fast Vitest-only loops while repo acceptance still covers typecheck, lint, smoke, and build-sensitive behavior. `apps/web verify` now defaults locally to legal PDF generation, `prisma:generate`, one Health Commons generation, then starts `next build` before launching sibling `pnpm dev:smoke`, prepared Vitest, and `pnpm lint` jobs; CI keeps hosted-web substeps serial unless `MURPH_VERIFY_STEP_PARALLEL=1` is set, and `apps/web verify:parallel` forces the same parallel path explicitly. Its dev smoke and Vitest subcommands run through prepared entrypoints after that setup, avoiding repeated Health Commons/legal setup inside one verify run while keeping direct `pnpm --dir apps/web test` self-contained. `apps/cloudflare verify` now defaults locally to overlapping its Node and Workers lanes after the shared typecheck, can skip its own typecheck only when `MURPH_CLOUDFLARE_VERIFY_SKIP_TYPECHECK=1` is set by the root acceptance wrapper after root typecheck has already covered the app, and CI keeps Cloudflare substeps serial unless `MURPH_VERIFY_STEP_PARALLEL=1` is set. The tag-driven release workflow still opts into the outer CI overrides explicitly. The hosted-web suite is now split into five workspace buckets and the Cloudflare Node suite into three, both with app-local worker caps defaulting to `MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI. Those app lanes enable file-level Vitest parallelism locally by default except for Cloudflare's `cloudflare-node-platform` bucket, disable it in CI by default, keep in-file suite concurrency opt-in unless `MURPH_VITEST_SUITE_CONCURRENCY` explicitly enables it, and accept `MURPH_VITEST_FILE_PARALLELISM` plus `MURPH_VITEST_MAX_CONCURRENCY` / `MURPH_CLI_VITEST_MAX_CONCURRENCY` as explicit overrides where a bucket has not explicitly disabled file parallelism. The hosted-web dev-smoke helper checks route types and Turbopack cache paths directly, polls readiness every 250ms, and reuses its local Turbopack smoke cache by default; CI still prunes the cache, and `MURPH_HOSTED_WEB_SMOKE_PRUNE_CACHE=1` forces a cold local smoke when needed. For `apps/web`-only work using scoped verification, `pnpm --dir apps/web lint` is also a required package-level check even when the broader `apps/web verify` superset is not run. | `apps/web/**` including hosted-app typecheck, explicit ESLint CLI coverage, one Health Commons catalog generate plus one shared Prisma client generate before verify's build/test steps, local cached `next dev` smoke under `apps/web/.next-smoke`, production build under `apps/web/.next`, and hosted browser-auth/session/onboarding/Linq-ingress Vitest coverage, plus `apps/cloudflare/**` including hosted-runner typecheck, fast Node tests, Workers-runtime tests. Repo-local source resolution still flows through `config/workspace-source-resolution.ts`, but Vitest subpaths now resolve only through explicit workspace entries plus package-declared public exports instead of wildcarding arbitrary package internals. The separate `.github/workflows/cloudflare-hosted-e2e.yml` lane now adds focused root `pnpm hosted-local e2e <scenario>` checks for device-connect, Codex image media delivery, Linq delivery, Linq scheduled reminder, idle-checkpoint deferred progress, direct-R2 presigned upload, Temporal orchestration, and Telegram delivery flows on every pull request and `main` push without broadening the default app verify surface to the full serial E2E bundle. For local manual proof, `pnpm hosted-local e2e` runs the hosted-local full-stack E2E suite, `pnpm hosted-local e2e codex-gateway-prefix --profile e2e:live` runs the opt-in real Codex app-server Gateway-prefix diagnostic against a local recorder, `pnpm hosted-local e2e vault-persistence --profile e2e:live` runs the opt-in real Codex app-server vault persistence scenario across a hosted-local restart, and `pnpm --dir apps/cloudflare test:e2e:local` adds the Workers-runtime E2E lane. |
| `pnpm test:packages:coverage` | Package-only runtime verification under coverage. Starts with the tracked-artifact hygiene guard, performs the prepared `pnpm build:test-runtime:prepared` graph, then runs every package-local `test:coverage` command under `packages/*`; under `pnpm verify:acceptance`, the contracts package keeps the same Vitest coverage plus generated-schema verification but reuses the contracts build from the earlier root typecheck through `test:coverage:prepared` instead of rebuilding it. After the parallel package coverage fanout finishes, the lane runs the built package-boundary checks for `packages/messaging-ingress` and `packages/inboxd`; when prepared runtime artifacts are present, those checks use prepared variants that do not rebuild `dist`. Local runs fan the package coverage commands out with bounded outer concurrency of `6` by default, throttle to `4` while CLI coverage is active, and refill freed slots immediately instead of waiting for a whole batch to drain, while CI keeps the package coverage lane at one outer package at a time by default. The dedicated `test:apps` verify lane stays out of this command and is composed back in at the full `pnpm test:coverage` layer. Shared package coverage thresholds are enforced at the package aggregate level, not as per-file thresholds; packages can still set stricter package-local coverage config when a file-level gate is intentionally useful. | Package-wide coverage under `packages/{assistant-cli,assistant-engine,assistant-runtime,assistantd,cli,cloudflare-hosted-control,contracts,core,device-syncd,gateway-core,health-metrics,hosted-execution,hosted-orchestrator-temporal,importers,inbox-services,inboxd,messaging-ingress,openclaw-plugin,operator-config,parsers,query,runtime-state,setup-cli,vault-usecases}/src/**/*.ts`, plus sequential built package-boundary checks for `packages/messaging-ingress` and `packages/inboxd` |
| `pnpm test:smoke` | Current root command for fixture/scenario-manifest integrity verification; this lane is not executable end-to-end smoke today. | `fixtures/**`, `e2e/smoke/**`, `docs/contracts/03-command-surface.md` |

## Current CI Workflows

- `.github/workflows/repo-hygiene.yml` runs the tracked private/build artifact guard on GitHub-hosted `ubuntu-24.04`.
- `.github/workflows/host-support.yml` runs a host-support matrix on GitHub-hosted `ubuntu-24.04` and `macos-latest`, installing with `pnpm install --frozen-lockfile`, building the workspace, preparing `pnpm build:test-runtime:prepared`, and then exercising the focused built-runtime CLI host-support suite (`packages/cli/test/setup-cli.test.ts` and `packages/cli/test/inbox-service-boundaries.test.ts`) with `MURPH_PREPARED_CLI_RUNTIME_ARTIFACTS=1` on both hosts. The macOS host leg serializes package-script workspace builds so sibling `tsc -b --force` package scripts do not rewrite shared project-reference declarations at once while the Linux leg keeps the normal package-build fanout. The workflow also carries deterministic CI-only hosted-web build placeholders for `DATABASE_URL`, hosted device routing, contact privacy, hosted mailbox fingerprinting, and the public Privy app id so its Linux release shards can finish `apps/web verify` without inheriting production secrets.
- The same workflow also preserves the Ubuntu `pnpm release:check` surface without running it as one long job: release metadata/build/typecheck, package coverage shards, app verification, and fixture coverage run as parallel jobs, then a final `Release checks (ubuntu)` aggregator preserves the required-check name. This keeps Linux bootstrap and release packaging exercised in CI while avoiding the serial package-coverage wall clock.
- `.github/workflows/deploy-render-temporal-worker.yml` runs after successful `Murph Host Support` push runs on `main`, waits for both `Murph Host Support` and `Repo Hygiene` to be successful for the same current `main` commit, then calls the Render Temporal worker deploy hook with that exact commit `ref`. The hook URL lives only in the `RENDER_TEMPORAL_WORKER_DEPLOY_HOOK` GitHub Actions secret. This replaces Render's native `checksPass` auto-deploy gate for the worker so stale third-party check suites cannot block production deploys.
- `.github/workflows/cloudflare-runner-base-image.yml` runs only on protected `main` pushes or manual dispatches from protected `main`, publishes the pinned GHCR Whisper model image from `Dockerfile.cloudflare-whisper-model`, then publishes stable and source-fingerprinted GHCR native runner base image tags through `pnpm --dir apps/cloudflare runner:docker:base -- --push`. The workflow derives the model image tag from `Dockerfile.cloudflare-hosted-runner-base`, uses the model Dockerfile checksum for the Hugging Face fetch, grants `packages: write`, and deliberately has no pull-request trigger.
- `.github/workflows/cloudflare-hosted-e2e.yml` runs focused hosted local E2E jobs on GitHub-hosted `ubuntu-24.04` for every pull request and `main` push. Each job provisions a local `postgres` service container, runs `pnpm temporal:cli:setup` before the managed local Temporal harness starts, does not authenticate to GHCR before running PR-controlled code, uses anonymous pulls for the public fingerprinted native runner base and pinned model images on cold runners, and points `DATABASE_URL` at `127.0.0.1:5432/murph_test`, while still using deterministic CI-only hosted-web placeholders for hosted device routing, contact privacy, mailbox fingerprinting, and Privy values. The workflow then runs `pnpm hosted-local e2e device-connect`, `pnpm hosted-local e2e codex-image-media-delivery`, `pnpm hosted-local e2e linq-delivery`, `pnpm hosted-local e2e linq-scheduled-reminder`, `pnpm hosted-local e2e idle-checkpoint-deferred-progress`, `pnpm hosted-local e2e direct-r2-presigned-put`, `pnpm hosted-local e2e temporal-orchestration`, and `pnpm hosted-local e2e telegram`, and always uploads per-job logs plus redacted hosted-local `state.json` files instead of broadening to the full serial hosted-local E2E suite or uploading every harness artifact. The local aggregate `pnpm --dir apps/cloudflare test:e2e:local` also runs the Workers-runtime E2E lane through `test:e2e:workers:local`; CI keeps that narrower Workers proof inside `apps/cloudflare verify` / `test:workers` rather than duplicating it in each hosted-local E2E job.
- `.github/workflows/cloudflare-hosted-device-sync-e2e.yml` runs the Junction wearable direct-resource replay hosted local E2E job on every pull request and `main` push. The job uses the same loopback Postgres, Temporal CLI setup, anonymous runner-image pulls, and deterministic hosted-web placeholder env pattern as the hosted E2E workflow, then runs `pnpm hosted-local e2e device-sync-junction-wearable-direct-resource-replay` and always uploads the focused job log.
- `.github/workflows/release.yml` uses GitHub-hosted `ubuntu-24.04`, installs once, runs `pnpm release:check` with `MURPH_TEST_LANES_PARALLEL=1`, `MURPH_APP_VERIFY_PARALLEL=1`, and `MURPH_VERIFY_STEP_PARALLEL=1` so the release verification lane uses the parallel package/smoke branches and parallel app substeps without enabling full app/package overlap unless `MURPH_ACCEPTANCE_APP_VERIFY_WITH_COVERAGE=1` is set explicitly, while the same deterministic hosted-web build placeholders keep `apps/web verify` on its truthful build path without injecting production DB or production hosted device secrets, then packs the publishable tarballs once for upload/publication.
- Vercel deploys of `apps/web` use the checked-in Vercel build command `pnpm release:production:migrate && pnpm build`, so the guarded migration wrapper still runs automatically on main-branch production deploys while preview/non-main builds skip through the wrapper guard. The generic `pnpm --dir apps/web build` script is non-mutating and does not run production migrations. The guarded migration entrypoint uses `DIRECT_DATABASE_URL` when present, requires it in Vercel production, and rejects known pooled Postgres ports such as `6432` and `6543`. Production migrations must remain backward compatible with the currently deployed app because a successful migration can outlive a later build failure.

## Current Gaps

- Repo-level automation still does not run full end-to-end CLI scenario flows; it typechecks/builds the published shell plus the extracted `assistant-cli` and `setup-cli` packages, now includes inbox service/runtime tests plus parser-worker/runtime tests, and the `test:smoke` lane still covers fixture/scenario-manifest integrity separately.
- The current fixture/scenario lane still validates manifests and command-surface coverage, not end-to-end package orchestration.
- Hosted Temporal orchestration has package, route, focused web/Cloudflare
  coverage, a local Signal-With-Start smoke script, and a root Render
  Background Worker Blueprint for the worker process. The hosted-local E2E
  suite now includes `temporal-orchestration`, which starts managed local
  Temporal, signals through web, queries the workflow, and proves the worker
  reaches Cloudflare ensure-processing. Routine repo checks still do not prove
  that command-ordering edits to `hosted-user-runtime.ts` replay old histories;
  those edits require Worker Versioning/deployment pinning, `patched()` /
  `deprecatePatch()`, or a replay test against captured pre-change histories.
  Routine repo checks also do not validate a live Render deploy or a production
  Temporal Cloud namespace.
- No automated check launches a live Codex App Server by default; Codex assistant command surfaces are verified through type/build/test coverage around the CLI and assistant runtime surfaces. The opt-in `codex-gateway-prefix` hosted-local E2E scenario runs the real Codex app-server against a local Responses API recorder for cache-prefix diagnostics, fingerprints the first cacheable provider prompt prefix across repeated Linq wakes, and fails if those fingerprints diverge; it is excluded from the default `all` scenario set because it can intentionally fail while provider behavior is under investigation. Codex App Server file/PDF inputs are not advertised as natively supported unless the app-server protocol grows a supported file input item.
- Hosted Codex config must keep native Codex skill instructions disabled (`[skills] include_instructions = false` and `[skills.bundled] enabled = false`) unless the hosted prompt-cache invariant is deliberately redesigned. Re-enabling those instructions can embed per-wake runner-local skill paths in provider prompts, making otherwise resumed hosted turns diverge before the cacheable prefix floor. Murph-managed assistant skill assets are different: `packages/assistant-engine/skills/**` ships with the package, the stable prompt references them symbolically through `$MURPH_ASSISTANT_SKILLS_ROOT/<slug>/SKILL.md`, and hosted/local shell env stamps `MURPH_ASSISTANT_SKILLS_ROOT` to the canonical package-owned root for explicit reads. Keep `packages/assistant-engine/test/assistant-skill-assets.test.ts` and `packages/assistant-runtime/test/hosted-runtime-codex-config.test.ts` as the regression guards for this split.
- No automated check hits a live Linq endpoint; Linq webhook delivery and outbound reply behavior are currently verified through mocked CLI, inboxd, and hosted `apps/web` tests only.
- No automated check hits a live AgentMail endpoint; email provisioning, polling, and in-thread reply behavior are currently verified through mocked CLI and inboxd tests only.
- No automated check hits a live WHOOP or other wearable OAuth provider; device-syncd auth/webhook behavior is currently verified through local service tests, route tests, stubbed control-plane callers, and the hosted-local device-connect smoke that creates a signed WHOOP connect link against synthetic provider config.
- No routine repo verification command validates a real Cloudflare Worker deploy or a real Cloudflare-managed native-container rollout. `apps/cloudflare` tests now cover the in-repo worker, direct Durable Object RPC and alarms in the Workers runtime, the Durable Object/container boundary, configurable container idle-timeout wiring, container activity-expiry cleanup behavior, runtime-owned idle/scheduled-wake checkpointing, selective artifact materialization plus preserved-artifact snapshot behavior, keyring-aware hosted ciphertext reads by stored `keyId`, bundle/artifact cleanup on successful transitions, and Node container-image seams. The repo also ships `pnpm --dir apps/cloudflare test:e2e:runner-python:local` as a targeted final-image Python PATH E2E: it assembles a fresh runner bundle, prepares the cached native base image, builds the same `linux/amd64` app-layer Dockerfile used by the Cloudflare container, starts the image with its normal entrypoint, waits for `/health`, and checks as the non-root `runner` user from immutable `/app` with the baked runner PATH to prove `python` and `python3` resolve to Python 3. `pnpm --dir apps/cloudflare runner:docker:smoke` remains the broader local final-image smoke: it overlays smoke entrypoints into a derived bundle, restores a real fixture vault into an isolated smoke workspace inside the container, exercises `vault-cli` through Codex App Server `command/exec` for default vault reads, explicit raw `--vault`, measurement and scheduled-measurement writes, representative list commands, and hidden-vault schema/LLM metadata, exercises the shared `@murphai/parsers` attachment pipeline, and records metadata-only CLI proof counts plus the selected provider ids so the proof explicitly covers the shipped `murph` / `vault-cli` bins plus native `python` / `python3`, `pdftotext`, `whisper.cpp`, and ffmpeg-backed normalization/transcription behavior under the hosted runner's rebound `HOME` / `VAULT` model. The runner bundle packer uses runner-specific tarballs for the CLI shell and Health Commons so E2E and deploy bundles keep the same CLI/runtime/catalog surfaces without the public npm package's nested bundled workspace payload or web-only Health Commons artifacts. The manual workflow `.github/workflows/deploy-cloudflare-hosted.yml` intentionally keeps protected-main-only predeploy gates on Blacksmith: hosted-local E2E jobs start loopback Postgres containers, install Temporal CLI, and run `codex-gateway-prefix`, `linq-delivery`, and `linq-scheduled-reminder` with `MURPH_HOSTED_LOCAL_E2E_FAST_GATE=1`, and normal Worker deploy runs add a separate Blacksmith gate that prepares the runner bundle/base image before running the focused Cloudflare verify lane plus `runner:docker:smoke:prepared-base` from the same commit. `pnpm cf:deploy:immediate` remains the break-glass path that skips those E2E/smoke gates, but Worker deploys now still run a protected-main-only Blacksmith build-prep handoff that runs the hosted Codex auth guard, assembles the runner bundle, prepares the native base image, and uploads only those no-secret build artifacts. That immediate handoff intentionally trusts Blacksmith for production artifact integrity, while still denying it production secret access. The GitHub-hosted deploy job then attaches the production environment, validates and adopts the immediate handoff before any secret-bearing deploy preflight, renders deploy config and Worker secrets, rejects unsafe archive entries and symlink targets, dry-runs the generated Wrangler deploy bundle, executes a direct `wrangler deploy`, reads `wrangler deployments status --json` for the smoke version and final traffic summary, validates the required GitHub environment wiring up front including `CF_PUBLIC_BASE_URL` for smoke runs, declares the required hosted runtime secrets through generated Wrangler config, and pairs the deploy docs with a checked-in transient R2 lifecycle config/helper. Gradual deploys run deployed managed-container runner-bundle and assistant CLI surface smoke with a longer retry window so Cloudflare has time to surface the new container application version; `container_rollout=immediate` adds the stricter direct-R2 managed-container smoke. Hosted prompt-cache prefix drift, core Linq delivery regressions, scheduled Linq reminder regressions, runner-image regressions, missing deployed assistant CLI hot-path schemas, or invalid generated deploy bundles therefore block `pnpm cf:deploy`-triggered deploys before or immediately after the real deploy step; the immediate path keeps the deploy job's own artifact validation, deploy, and strict managed-container smoke checks while moving its no-secret build prep to Blacksmith. Live deployment still depends on operator-supplied Cloudflare credentials, GitHub environment wiring, first-time container provisioning in Cloudflare, and an operator applying the bucket lifecycle rules to the real R2 buckets.
- The tag-driven release workflow is present, uses npm trusted publishing for package publication, runs a slimmer `release:check` guard path that now validates release metadata plus `pnpm build:workspace:clean` and `pnpm verify:acceptance` without re-installing/re-building/re-packing inside the script, and is only exercised on real `v*.*.*` tag pushes rather than during ordinary repo verification. npm trust is package-level rather than repo-level, so this monorepo also ships `pnpm release:trust:github` for the one-time bootstrap that binds every publishable `@murphai/*` package to `cobuildwithus/murph` and `.github/workflows/release.yml`; if a package already has the wrong trusted publisher entry, that npm-side state still needs manual revoke-and-recreate repair, which local repo checks cannot fully prove.

## Update Rule

When real source code, CI, or deployment automation is added, update this file and `agent-docs/operations/verification-and-runtime.md` in the same change.
