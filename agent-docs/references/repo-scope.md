# Repo Scope

Last verified: 2026-03-12

## Current Scope

- `murph` is a standalone repo bootstrap inside this workspace.
- Its precise product/runtime role is `UNCONFIRMED`.
- Use this repo for Murph-specific code and docs once that role is defined.

## Routing Rule

If a task clearly belongs to another established repo (`interface`, `v1-core`, `wire`, `chat-api`, `cli`, `indexer`), do not place it here just because this repo is new.

## Follow-Up Needed

- Replace this bootstrap scope doc with a concrete subsystem map once the repository purpose is documented.
