# Verification And Runtime

Last verified: 2026-03-31

## Verification Matrix

| Change scope | Required commands | Notes |
| --- | --- | --- |
| Vault-only data changes under `vault/**` | No repo-wide commands by default. | Read back the touched vault records plus any audit or ledger entries written by the mutation path. Do not touch the coordination ledger unless the task expands into repo code/docs/process work. |
| Text-only docs/process-only (`*.md` edits or deletions only) | No repo-wide commands by default. | Allowed only when the diff is limited to Markdown text changes or deletions in repo docs/process files and does not touch scripts, config, tests, generated docs, tracked artifacts, or workflow-enforcement files. Read back the touched docs, confirm any intended deletions directly, and check for obvious broken references when the removed or renamed doc might be linked elsewhere. |
| Docs/process-only with mechanics beyond text-only Markdown | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Applies when docs/process work touches anything beyond text-only `.md` edits/deletions, including scripts, config, tests, generated docs, tracked artifact inventories, or workflow-enforcement files. Includes package-runtime checks, built CLI verification, the tracked-artifact hygiene guard, and fixture/scenario scaffolding because those are part of repo truth now. When those repo-wide commands are already known red for unrelated reasons, the scoped verification mode below may be used instead. |
| Fixture/e2e/package-doc changes | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Verifies fixture corpus integrity, smoke-manifest wiring, package-runtime health, built CLI checks, command-surface coverage, and the source-artifact guard for handwritten JS-like files plus tracked `.env` / `.env.*` private files and generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`. |
| Changes under `packages/contracts`, `packages/hosted-execution`, `packages/runtime-state`, `packages/core`, `packages/importers`, `packages/inboxd`, `packages/parsers`, or `packages/query` | `pnpm typecheck`, `pnpm test:packages`, `pnpm test:smoke` | `pnpm test` remains required for full repo acceptance, but `pnpm test:packages` is the clean runtime signal when the doc-drift wrapper is blocked by an in-progress dirty worktree and now keeps the dedicated web/app verify lane out of the default package loop. The root Vitest leg still uses the curated repo acceptance surface, just expressed through project-mode `vitest.config.ts`, and `pnpm test:packages:coverage` keeps the same targeted V8 coverage gates without the heavier web/app smoke/build path. |
| Changes under `packages/device-syncd` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Repo checks typecheck/build the daemon in the workspace graph, execute its service/runtime tests plus Oura and WHOOP provider/config coverage through the root multi-project Vitest suite, and keep the local OAuth/token boundary documented alongside the CLI and web callers. |
| Changes under `packages/assistantd` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Repo checks typecheck/build the daemon in the workspace graph, execute its loopback-auth/routing tests plus the explicit `murph/assistant-core` boundary regression and CLI daemon-routing coverage through the root multi-project Vitest suite, and keep the single-vault loopback plus bearer-token trust boundary documented alongside the CLI runtime it fronts. |
| Changes under `packages/web` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | `packages/web test` is now the fast Vitest-only fixture lane for local iteration, while full repo acceptance reaches the heavier typecheck plus production-build path through `pnpm test:apps` and the package-local `packages/web verify` script. The web package still resolves workspace dependencies from source through the shared `config/workspace-source-resolution.ts` helper, preserves the original `pnpm` launch cwd for relative vault paths, and consumes the query read layer without write access. UI-affecting web changes also require browser inspection at desktop and mobile sizes before handoff because the current test suite is not a visual/layout substitute. |
| Changes under `apps/web` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | `apps/web test` is now the fast hosted-web Vitest lane for local iteration, split into five serial-safe workspace buckets with app-local worker caps defaulting to `MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI. File-level Vitest parallelism is enabled locally by default, disabled in CI by default, and can be forced either way with `MURPH_VITEST_FILE_PARALLELISM` or `MURPH_TEST_FILE_PARALLELISM`. Full repo acceptance still reaches the heavier app-local typecheck, cold-boot `next dev` smoke under `apps/web/.next-smoke`, and production build under `apps/web/.next` through `pnpm test:apps` and the package-local `apps/web verify` script. That hosted lane now performs one explicit `prisma generate` before the verify typecheck/build steps instead of generating the client twice, while still covering route-type stub bootstrap from the tracked `next-env.d.ts` import, focused Vitest coverage for browser-auth/session helpers, hosted AI usage import plus optional Stripe token-meter routes/helpers, the hosted verified-email sync route and Privy linked-account helpers, durable hosted execution outbox coverage across device-sync/share/onboarding dispatches, Privy-backed hosted onboarding/share routes, hosted onboarding wallet-forwarding plus RevNet webhook idempotency paths, hosted Linq ingress routes, interactive-dev isolation under `apps/web/.next-dev`, and source-based workspace package resolution through the shared `config/workspace-source-resolution.ts` helper. |
| Changes under `apps/cloudflare` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Repo checks now include hosted-runner app typecheck plus the focused hosted-runner verification surface under `apps/cloudflare verify`, which runs app-local typecheck once and then both the fast Node lane (`apps/cloudflare test`) and the smaller Workers-runtime Vitest lane (`apps/cloudflare test:workers`). The Node lane is now split into three serial-safe workspace buckets with the same app-local worker-cap fallback chain (`MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI) so local runs can overlap safely without fully serializing the app. File-level Vitest parallelism is enabled locally by default, disabled in CI by default, and can be forced either way with `MURPH_VITEST_FILE_PARALLELISM` or `MURPH_TEST_FILE_PARALLELISM`. That combined Cloudflare verification covers signed dispatch verification, `/health` plus internal route aliases, per-user runner retry/poison state transitions, explicit `member.activated` bootstrap behavior, one encrypted hosted workspace snapshot in the `vault` slot plus separately encrypted artifact objects, selective artifact materialization with preserved untouched artifact refs across later snapshots, separate per-user runner env control/persistence handling, committed hosted side-effect journaling plus post-commit assistant-delivery reconciliation/resume, hosted share acceptance import handling, best-effort cleanup of orphaned per-user artifact objects, keyring-aware hosted ciphertext reads by stored `keyId`, direct Durable Object RPC/alarm coverage inside workerd, and the one-shot Node runner path that restores the hosted workspace snapshot plus only the externalized artifacts the current run needs and runs inbox/parser/assistant/device-sync/share-import seams through `@murph/assistant-runtime`. The app-local no-emit typecheck now includes the Node runner/container-entrypoint path instead of excluding direct CLI bridge files. The repo does not yet verify a live `wrangler` deploy or native-container provisioning path. |
| Changes under `packages/assistant-runtime` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Repo checks include the package-local no-emit typecheck through the workspace scripts, plus package-local Vitest coverage through the root multi-project suite and the focused Cloudflare hosted-runner lanes that exercise the package through in-process and isolated child-process execution paths, including hosted verified-email self-target reconciliation, the direct `murph/assistant-core` boundary checks, scrubbed child-env launch, stripped supervisor proxy vars, per-run writable cache/temp roots, launch-time supervisor-`HOME` avoidance, and child-process reaping on abort. This package is the headless hosted execution surface for Cloudflare and should carry explicit runtime context rather than ambient process configuration. |
| Changes under `packages/cli` | `pnpm typecheck`, `pnpm test`, `pnpm test:coverage` | Repo checks now run `packages/cli` typecheck plus package-local verification through `pnpm verify:cli`, and package tests build the shared incremental CLI runtime graph through `pnpm build:test-runtime` before exercising the built CLI package. The CLI Vitest surface now runs through six serial-safe workspace buckets, with local worker caps defaulting to `MURPH_VITEST_MAX_WORKERS=75%` unless the environment overrides it and file-level Vitest parallelism enabled locally by default but disabled in CI by default. `MURPH_VITEST_FILE_PARALLELISM` and `MURPH_TEST_FILE_PARALLELISM` can force file parallelism on or off per run. That narrower build still prepares the required hosted-execution, runtime-state, core, importer, device-syncd, query, inboxd, parser, and CLI runtime artifacts while avoiding repeated one-package-at-a-time rebuilds, and the shared CLI runtime-artifact helper can now trust that prepared lane instead of falling back to its slow wait-and-reverify path on every run. CLI runtime tests still execute through the built package path, so source coverage remains focused on the in-process `core`/`importers`/`query` runtime modules. Assistant provider, assistant cron, assistant status/doctor/stop resilience observability, failover/outbox/fault-injection, and outbound channel tests use mocks/stubs and do not execute live Codex, Ollama, gateway, iMessage, Telegram, Linq, or AgentMail sends in repo automation. |
| User explicitly says to skip checks | Skip checks for that turn only. | User instruction takes precedence. |

## Scoped Verification Mode

The text-only docs/process fast path above is not a scoped-verification exception. It is the default verification rule for eligible Markdown-only docs work.

Scoped verification may replace the repo-wide baseline only when all of the following are true:

1. The change is narrow and bounded to one subsystem or one docs/process lane rather than a broad refactor.
2. `pnpm typecheck`, `pnpm test`, or `pnpm test:coverage` are already credibly known red for unrelated reasons in the current branch or working session.
3. You can name the exact failing command and failing target, and explain why your diff did not cause that failure.
4. You run the highest-signal scoped checks available for the touched surface and record the evidence in handoff.

Scoped verification is allowed for narrow changes such as:

- docs/process-only updates outside the text-only Markdown fast path when repo-wide checks are already known red and manual readback confirms the touched docs are internally consistent
- package-local or app-local fixes with a focused test, typecheck, verify, or scenario command that exercises the changed surface directly
- small config changes with a direct validation command or targeted test covering the changed contract

Scoped verification is not allowed when the change is broad, cross-cutting, or high-risk, including schema/storage changes, billing/auth/trust-boundary changes, deploy/runtime entrypoint changes, or refactors that touch multiple subsystems. Those changes still need the full repo-wide baseline unless the user explicitly says otherwise.

When using scoped verification, handoff must include:

- that scoped verification mode was used
- which repo-wide commands were omitted or left red
- the prior unrelated failing command(s) and target(s)
- the focused commands or direct scenario checks that were run instead

## Current Command Meaning

- `pnpm build`: executes the solution-style `tsc -b` build across the workspace package graph, with `packages/contracts` library emit ordered through `packages/contracts/tsconfig.build.json`, `packages/hosted-execution` built before `packages/runtime-state` and the hosted app callers, `packages/runtime-state` shared-runtime helpers built before query, device-syncd, inboxd, assistant-runtime, and CLI consumers, the CLI built before `packages/assistantd` so the daemon can consume the published `murph` surface, and downstream runtime packages, including `packages/parsers`, resolved through project references.
- `pnpm build:workspace:incremental`: clears the referenced runtime-package `dist/` outputs first, then runs the same root TypeScript project-reference graph as `pnpm build` through one `tsc -b tsconfig.json` invocation instead of sequential per-package shelling. This preserves clean-build semantics for built-runtime consumers while still avoiding the old per-package helper path.
- `pnpm build:test-runtime`: clears the narrower runtime-package `dist/` outputs first, then runs the focused TypeScript project-reference graph in `tsconfig.test-runtime.json`, building only the workspace runtime artifacts needed for built CLI consumers and the curated root package Vitest lane.
- `pnpm clean`: removes workspace build artifacts such as `dist/`, `.next/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo`, then prunes untracked generated JS/declaration sidecars that sit next to tracked TypeScript source files under `packages/`, `apps/`, and `e2e/`. Hosted `apps/web` interactive dev now uses `apps/web/.next-dev`, which intentionally stays outside the shared clean/build lane so repo build/test/clean commands do not tear down a running hosted dev server, while smoke output is disposable under `apps/web/.next-smoke`.
- `pnpm no-js`: first prunes untracked generated JS/declaration sidecars that sit next to tracked TypeScript source files, then fails if handwritten `.js`, `.mjs`, `.cjs`, or `.d.ts` source files remain under `packages/`, `apps/`, or `e2e/` outside the explicit Next.js stub allowlist plus the explicit `packages/web/postcss.config.mjs` and `apps/web/postcss.config.mjs` framework config paths, or if tracked `.env` / `.env.*` private files or generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, or `*.tsbuildinfo` is committed there.
- `pnpm typecheck`: validates shell syntax, syntax-checks the root `.mjs` release helpers, runs the workspace-boundary audit that blocks sibling build-artifact aliases including `.next-dev` and `.next-smoke` plus relative cross-package imports, typechecks repo-owned TS tools, runs each package/app's local no-emit typecheck script, bootstraps the exact Next route-type stubs referenced by `packages/web/next-env.d.ts` and `apps/web/next-env.d.ts`, and verifies the shared `tsc -b` build, including `packages/hosted-execution`, `packages/runtime-state`, `packages/device-syncd`, `packages/inboxd`, `packages/parsers`, `packages/assistantd`, `packages/assistant-runtime`, `packages/web`, `apps/web`, and `apps/cloudflare`.
- `pnpm test`: runs docs drift enforcement, repeats the workspace-boundary audit, runs the tracked-artifact hygiene guard, then executes the full acceptance stack by preparing the shared package-test artifacts once, running the curated root package Vitest lane and `pnpm test:apps` in parallel by default on local machines, and finishing with fixture/scenario integrity verification through `e2e/smoke/verify-fixtures.ts`. CI stays sequential unless `MURPH_TEST_LANES_PARALLEL` overrides it. That full stack still covers the Oura and WHOOP config/provider/service daemon tests, local assistantd loopback-auth plus `murph/assistant-core` boundary coverage and CLI routing coverage, shared hosted execution auth/env/client coverage, hosted verified-email env helper coverage, hosted control-plane auth/session/onboarding/share/Linq-ingress/build coverage plus the durable execution-outbox path for hosted share acceptance, hosted onboarding activation/Linq dispatches, hosted device-sync wakes, hosted AI usage import plus optional Stripe token-meter routes, the hosted verified-email sync route, hosted onboarding wallet-forwarding and RevNet webhook idempotency tests, hosted-runner signed-dispatch and one-shot runner coverage including explicit activation bootstrap, separately encrypted per-user env control/persistence handling, hosted verified-email self-target reconciliation, hosted AI usage export after commit, isolated child-process execution with scrubbed child env, stripped supervisor proxy vars, per-run writable cache/temp roots, launch-time supervisor-`HOME` avoidance, request-abort child-process reaping inside the container bridge, Workers-runtime dispatch/DO-alarm/runner-control coverage, hosted share acceptance import handling, the direct `murph/assistant-core` single-boundary checks for hosted consumers, assistant cron plus assistant status/doctor/stop resilience CLI/runtime tests, failover/outbox/fault-injection robustness coverage, Linq webhook/outbound adapter coverage, AgentMail email connector/delivery/setup discovery tests, and the dedicated web/app verify lanes. Its docs-drift phase still requires `agent-docs/index.md` for durable docs changes, but execution-plan churn under `agent-docs/exec-plans/**` and coordination-ledger-only updates are exempt from that specific index requirement.
- `pnpm test:packages`: runs the tracked-artifact hygiene guard first, runs `packages/contracts test`, performs the narrower `pnpm build:test-runtime` graph plus CLI package-shape verification for built-runtime consumers, then executes the curated root multi-project Vitest suite through `vitest.config.ts` with `--no-coverage`. The CLI portion of that curated root lane now runs through the same six serial-safe workspace buckets used by the package-local CLI suite, defaults to `MURPH_VITEST_MAX_WORKERS=75%` locally or `50%` in CI when the environment does not override it, enables file-level Vitest parallelism locally by default, and intentionally leaves the heavier web/app verify work to `pnpm test:apps`. `MURPH_VITEST_FILE_PARALLELISM` and `MURPH_TEST_FILE_PARALLELISM` can force file parallelism on or off per run. Package-local test commands still exercise each target's full owned suite rather than the curated root subset; the CLI, hosted web, and Cloudflare Node lanes now do that through dedicated workspace configs, while the remaining package-local loops keep their package-local `vitest.config.ts` surfaces.
- `pnpm test:apps`: runs the dedicated heavier web/app verification lane by executing `packages/web verify`, `apps/web verify`, and `apps/cloudflare verify`. Local runs default to parallel execution and CI defaults to sequential execution unless `MURPH_APP_VERIFY_PARALLEL` overrides the behavior. The hosted-web and Cloudflare Node Vitest lanes underneath those verify commands now use split workspace buckets with app-local worker caps defaulting to `MURPH_APP_VITEST_MAX_WORKERS`, then `MURPH_VITEST_MAX_WORKERS`, then `50%` locally or `25%` in CI. Those app lanes also enable file-level Vitest parallelism locally by default, disable it in CI by default, and accept `MURPH_VITEST_FILE_PARALLELISM` or `MURPH_TEST_FILE_PARALLELISM` as explicit overrides. That preserves the fast package-local `test` loops for the local and hosted web code while keeping typecheck, smoke, and build checks in repo acceptance.
- `pnpm test:packages:coverage`: runs the tracked-artifact hygiene guard first, runs `packages/contracts test`, performs the narrower `pnpm build:test-runtime` graph plus CLI package-shape verification for built-runtime consumers, then executes the curated root multi-project Vitest suite through `vitest.config.ts` with `--coverage`. The root package-coverage lane uses the same default `MURPH_VITEST_MAX_WORKERS=75%` locally or `50%` in CI override behavior, preserves the existing targeted V8 coverage surface and per-file thresholds, and still leaves the heavier web/app verify work to `pnpm test:apps`. Package-local `test` commands remain the full per-package loop and are not aliases for this curated coverage lane.
- `pnpm test:coverage`: runs doc-gardening validation, `pnpm test:packages:coverage`, `pnpm test:apps`, verifies every documented baseline command has smoke coverage plus a golden-output scaffold, and fails if handwritten `.js`, `.mjs`, `.cjs`, or `.d.ts` source files remain under `packages/`, `apps/`, or `e2e/` aside from the framework-generated `packages/web/next-env.d.ts` and `apps/web/next-env.d.ts` allowlist plus the explicit `packages/web/postcss.config.mjs` and `apps/web/postcss.config.mjs` framework config paths, or if tracked `.env` / `.env.*` private files or generated residue such as `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, or `*.tsbuildinfo` is committed there. Untracked generated sidecars adjacent to tracked TypeScript source files are pruned before that guard runs. Tracked `.env.example` files remain allowed.
- `pnpm zip:src` and `scripts/package-audit-context.sh`: shell through `pnpm no-js`, which first prunes untracked generated JS/declaration sidecars that sit next to tracked TypeScript source files and then runs the tracked-artifact hygiene guard, before building the source/review bundle from git-visible files while scanning `config/**` alongside app/package code and filtering blocked local residue such as `.env` / `.env.*`, `dist/`, `.next/`, `.next-dev/`, `.next-smoke/`, `.test-dist/`, and `*.tsbuildinfo` paths out of the manifest. This keeps ignored local artifacts out of the upload bundle without requiring a clean development worktree, while raw clone archives remain unsafe.
- `pnpm test:smoke`: runs only the fixture/scenario integrity verifier.
- `pnpm release:check`: installs with `--frozen-lockfile`, builds the workspace, runs `pnpm verify:repo`, validates the fixed-version monorepo release manifest plus publish metadata, and packs every publishable package into temporary tarballs with `pnpm pack`.

## UI Verification Addendum

- For UI-affecting `packages/web` changes, use browser inspection in addition to the scripted checks above.
- Inspect at least one desktop-width and one mobile-width state before handoff.
- Verify no horizontal overflow, clipped controls, broken wrapping, or obvious overlap in the default and changed interaction states.
- Treat the current web test suite as structural/regression coverage, not as proof of visual quality.

## Incur-Backed CLI Guardrails

- Model nested CLI verbs with real incur router groups. Do not use argv rewrites or synthetic action args to mimic nested commands, because `--schema`, `--llms`, `skills add`, and command-map typegen only stay truthful when the router tree itself is truthful.
- Treat incur-owned transport and discovery features as framework behavior: `--format`, `--json`, `--verbose`, `--schema`, `--llms`, `skills add`, and `--mcp`. Command-surface docs should describe Murph semantics and payloads, not restate incur defaults command-by-command unless the repo is deliberately constraining them.
- Keep `packages/cli/src/index.ts` default-exporting the root CLI and refresh `packages/cli/src/incur.generated.ts` whenever command topology changes. If `incur gen` is blocked by an unrelated build failure, record that explicitly in the handoff instead of silently leaving stale generated types.
- `packages/cli/test/cli-test-helpers.ts` executes `packages/cli/dist/bin.js`, so source checks like `pnpm exec tsx packages/cli/src/bin.ts ...` are only a diagnostic shortcut. Final verification still needs the built CLI path or a clearly documented unrelated blocker.

## Runtime Status

- No automated hosted deployment target is defined yet.
- Repo-level checks execute canonical write/read paths in `core`, `importers`, `inboxd`, `parsers`, and `query`, build the shared `hosted-execution` and `runtime-state` packages, and build the CLI package through the same TypeScript workspace toolchain used for local development.
- Shared `hosted-execution` helpers own the hosted control-plane auth/env/route/client seam between `apps/web` and `apps/cloudflare`, while `runtime-state` owns `.runtime` path resolution plus SQLite open defaults for query search, inboxd, and the CLI inbox layer.
- Query-owned lexical search state lives only at `.runtime/search.sqlite`; inbox-owned local state remains at `.runtime/inboxd.sqlite` plus `.runtime/inboxd/*.json`.
- Device sync state lives only at `.runtime/device-syncd.sqlite`; Murph's CLI-managed daemon launcher state, logs, and a separate `0600` local control-token file live under `.runtime/device-syncd/`, with the bearer kept out of ordinary `launcher.json`; provider OAuth sessions and encrypted tokens remain outside the canonical vault.
- `vault-cli inbox model bundle|route` can materialize capture-scoped audit artifacts under `derived/inbox/**/assistant/*.json`; those files are rebuildable and non-canonical.
- `vault-cli inbox model route` may call either the AI Gateway or an operator-supplied OpenAI-compatible endpoint. Automated repo checks do not execute live network model calls.
- `vault-cli assistant ask|chat|deliver|status|doctor|run|stop|session|state|memory|cron` persist or inspect local assistant state under `assistant-state/`, including explicit conversation bindings, timestamps/turn counts, provider session references, local transcript files, inbox-routing and channel auto-reply cursors, enabled auto-reply channels, assistant cron job definitions plus run history under `assistant-state/cron/`, generic JSON scratchpads under `assistant-state/state/`, coarse turn receipts under `assistant-state/receipts/`, replay-safe outbound intents under `assistant-state/outbox/`, pending provider-usage ledger rows under `assistant-state/usage/pending/`, diagnostics events plus snapshots under `assistant-state/diagnostics/`, persisted assistant status snapshots under `assistant-state/status.json`, provider failover cooldown state under `assistant-state/failover.json`, and Markdown assistant memory for naming, response preferences, standing instructions, selected health context, and recent project context; fresh sessions now inject only a small core memory block, recent notes are tool-retrieved on demand through `assistant memory search|get|forget`, assistant state scratchpads are managed through `assistant state list|show|put|patch|delete`, assistant cron management is exposed through the same bounded MCP-backed CLI path, failover attempts rebuild provider-specific execution context per route instead of reusing the primary-provider plan across provider families, assistant-written memory commits are bound to the real active user turn on the host side, health memory is private-context-gated, assistant runtime receipt/outbox/diagnostics/failover/status mutations are serialized under one shared assistant-runtime write lock, and provider-native transcript history plus channel-native send history may still stay external when adapters support them. Current outbound channel support covers iMessage, Telegram, Linq, and AgentMail-backed email. New prompt-backed cron jobs must bind an explicit outbound channel route and always deliver their generated response, while optional `stateDocId` bindings let the cron carry forward non-canonical scratch state only when the job opts in. Email setup can still reuse a discovered or explicit existing inbox when the API key cannot create new inboxes.
- Provider-backed Codex assistant turns default to `workspace-write` sandboxing plus `on-request` approvals so the CLI-backed MCP state/memory/cron tool paths and routine local commands are available without dropping directly to full-access defaults.
- When the built CLI artifact is present, assistant-memory operations are exposed to Codex as a bounded local MCP server rooted at the active vault/session context; CLI fallback remains available for direct operator use, and the live provider path should use that MCP tool surface rather than a separate localhost bridge.
- Assistant-memory writes now carry minimal provenance metadata (`writtenBy`, `sessionId`, `turnId`) and use a vault-scoped write lock so concurrent assistant processes or automations cannot interleave Markdown commits.
- `vault-cli` and `murph` load local `.env.local` first and then `.env` from the launch cwd before command dispatch, while preserving already-exported shell variables as higher precedence. This keeps repo-local operator credentials out of the canonical vault without requiring manual `export` commands each shell session.
- `vault-cli assistant run` can now run without `--model` when Murph only needs configured channel auto-reply such as iMessage, Telegram, Linq, or AgentMail email; when a model is supplied it also continues to perform canonical inbox triage, dedicated self-chat flows can opt into self-authored captures plus age-based session rollover, and due assistant cron jobs are processed while that loop stays active for the selected vault. Email auto-reply remains limited to direct threads and reuses the inbound AgentMail inbox id as the reply identity; Linq auto-reply remains limited to direct chats and reuses the inbound Linq chat id thread binding for replies.
- `vault-cli assistant status` and `vault-cli assistant doctor`, plus the root `vault-cli status` and `vault-cli doctor` shorthands, are read-only local diagnostics over assistant session files, transcripts, receipts, automation state, outbox intents, diagnostics snapshots, failover cooldown state, and persisted status snapshots; they must tolerate missing or partially corrupted assistant-state files without mutating vault data.
- `vault-cli assistant stop`, plus the root `vault-cli stop` shorthand, is the supported operator recovery path when `assistant run` / `murph run` is already active for the same vault. It targets the recorded run-lock PID with `SIGTERM`, escalates only if needed, and clears stale run-lock state when the recorded process is already gone.
- `vault-cli inbox source add email --id email:agentmail --provision ...` attempts to provision an AgentMail inbox when `AGENTMAIL_API_KEY` is available through the shell or the CLI-loaded local `.env.local` / `.env` files, automatically reuses the single discovered inbox when create permission is forbidden but discovery succeeds, stores only the AgentMail inbox id plus email address in local inbox config, and uses mocked AgentMail HTTP interactions in repo automation rather than live sends.
- `vault-cli device ...` targets the local device control plane, defaults to `DEVICE_SYNC_BASE_URL` or `http://localhost:8788`, authenticates with `DEVICE_SYNC_CONTROL_TOKEN`, rejects non-loopback base URLs whenever that bearer path is used, and can now start or reuse a Murph-managed local daemon for the selected vault when no explicit control-plane target is provided. `vault-cli device daemon start|status|stop` is the explicit lifecycle surface for that managed process, and managed bearer reuse comes from the separate local secret file rather than persisted launcher JSON.
- `vault-cli assistant chat` uses an Ink-based stderr UI and expects `react`, `ink`, and `ink-text-input` to be installed in the CLI workspace.
- The built `vault-cli` binary can be exercised locally with `node packages/cli/dist/bin.js ...` when a change requires an end-to-end runtime check beyond the standard repo scripts.
- A setup-specific entrypoint also exists at `node packages/cli/dist/bin.js onboard ...` (with `setup` kept as an alias); it is routed from `packages/cli/src/bin.ts` instead of the main `vault-cli` manifest so installer-style host provisioning can happen without reshaping the data-plane command graph.
- The built CLI package shape exposes a `murph` bin alias that targets the same built entrypoint as `vault-cli`; `murph`, `murph --help`, `murph onboard ...`, and `murph setup ...` route to the setup surface, while other commands continue through the main operator surface. Interactive TTY onboarding now opens a compact assistant/channel/wearable stepper with inline readiness badges for iMessage, Telegram, Linq, AgentMail email, Oura, and WHOOP, can prompt for missing runtime credentials for the current onboarding run without persisting them, can discover and reuse an existing AgentMail inbox or prompt for an explicit inbox id before provisioning when the API key is scoped, opens any selected wearable connect flow that is ready before handoff, defers scheduled-update preset installation until the operator later binds an explicit outbound destination, and then routes to `assistant run` when a configured auto-reply channel remains enabled. The repo's release flow now publishes `murph` plus `@murph/contracts`, `@murph/hosted-execution`, `@murph/runtime-state`, `@murph/core`, `@murph/query`, `@murph/importers`, `@murph/device-syncd`, `@murph/inboxd`, and `@murph/parsers` under one shared version and one git tag, while `@murph/web` stays private.
- Repo-local host bootstrap is handled by `scripts/setup-host.sh`, which delegates to the existing Homebrew-based `scripts/setup-macos.sh` path on macOS and can reuse or download Node 22+ locally on Linux before activating `pnpm` through corepack, installing workspace dependencies, building the workspace, and delegating to the built setup entrypoint. `scripts/setup-macos.sh` still hard-fails off macOS, `scripts/setup-linux.sh` hard-fails off non-Linux hosts, and `--dry-run` remains a wrapper-only planning mode for those shell entrypoints.
- GitHub Actions host-support CI now runs `.github/workflows/host-support.yml`, which exercises the focused CLI setup/inbox host-support suite on both `ubuntu-latest` and `macos-latest` and runs `pnpm release:check` on Ubuntu.
- Repo-local source-resolved workspace aliases are intentionally limited to the package allowlists exported from `config/workspace-source-resolution.ts`; packages outside that helper stay on their existing emitted-JS-shaped import conventions until a caller explicitly opts them into source resolution.
- `packages/device-syncd` exposes the local HTTP control plane for wearable OAuth/webhook/reconcile flows, binds `127.0.0.1` by default unless `DEVICE_SYNC_HOST` overrides it, rejects non-loopback control-route callers, requires a bearer token for `/providers/*`, `/accounts/*`, and other control routes, can expose only `/oauth/*/callback` plus `/webhooks/*` on a separate `DEVICE_SYNC_PUBLIC_HOST`/`PORT` listener when public ingress is needed, stores tokens outside the vault, serializes active jobs per account to avoid refresh-token races, and only allows cross-origin post-connect redirects when `DEVICE_SYNC_ALLOWED_RETURN_ORIGINS` includes the requesting origin. Murph's CLI-managed launcher may provide the default control token, state DB path, and loopback base URL for the selected vault, but it still talks to the daemon strictly over that localhost HTTP boundary.
- The hosted integration control-plane entrypoint lives under `apps/web`; Prisma CLI configuration now lives in `apps/web/prisma.config.ts` for Prisma 7, the hosted Next build now runs on Next.js' default Turbopack path with repo-local source-resolution compatibility configured in `next.config.ts`, interactive hosted `next dev` uses `apps/web/.next-dev`, cold-boot smoke uses `apps/web/.next-smoke`, route modules lazily construct Prisma so build-time route analysis does not require a live `DATABASE_URL`, package and app typecheck bootstrap the exact tracked Next route-type stub import before `tsc` so clean clones do not depend on leftover generated files, browser-authenticated device-sync routes trust only short-lived request-bound signed assertions from a trusted auth edge/proxy and consume each assertion nonce once to reject replay, hosted onboarding verifies Privy identity tokens server-side before issuing hosted session cookies and stores only invite/member/billing metadata plus embedded-wallet linkage rather than canonical health data, optional RevNet issuance requires full `HOSTED_ONBOARDING_REVNET_*` config plus `HOSTED_ONBOARDING_STRIPE_BILLING_MODE=subscription`, now funds the canonical co-build community terminal with native ETH using an operator-set `HOSTED_ONBOARDING_REVNET_WEI_PER_STRIPE_MINOR_UNIT` conversion knob, uses a treasury private key held only in deploy secrets, records invoice-level idempotency plus tx hashes in Postgres, ingests Stripe webhooks into a durable hosted Stripe fact queue plus internal cron reconciler instead of doing heavy work inline, imports successful hosted assistant usage rows into `hosted_ai_usage` after the hosted commit succeeds so the hosted DB is the canonical usage ledger, can optionally meter total tokens later through a separate cron that skips member-supplied API keys, hosted Linq ingress stores only recipient bindings plus queued webhook events rather than canonical inbox captures, and every Cloudflare-bound hosted execution intent now lands in the shared Postgres `execution_outbox` in the same transaction as the originating onboarding/share/device-sync state change so delivery retries and dedupe are uniform instead of inline and flow-specific. The hosted tier may still store encrypted one-time share packs plus preview metadata without becoming the canonical health store, and repo-local Next/Vitest resolution for workspace packages is centralized in `config/workspace-source-resolution.ts` and intentionally limited to the helper's explicit package lists.
- The hosted execution runner entrypoint lives under `apps/cloudflare`; it verifies signed internal dispatch from `apps/web`, coordinates per-user runs through Durable Objects, keeps queue/replay/poison state in Durable Object SQLite tables, stores one encrypted hosted workspace snapshot plus separately encrypted artifact objects in object storage, stores per-user runner env overrides in a separate encrypted hosted object, restores a temporary local execution context for one-shot runs, performs durable hosted bootstrap explicitly during `member.activated`, starts the native Cloudflare container attached to the per-user Durable Object, fails closed when either hosted control token is missing, routes worker-side control/status/env calls into direct Durable Object methods, keeps the per-user container warm until its configurable idle timeout expires with a checked-in `5m` default instead of forcing teardown after each drained batch, restores inline workspace files first and materializes only the externalized artifact paths the current run needs, preserves untouched artifact refs across later snapshots so old media does not churn through download/upload cycles just to stay referenced, collects due hosted side effects before the event commit, persists those effects on the committed journal, best-effort exports pending assistant usage after the commit succeeds, resumes post-commit sends from committed state when retries happen, records assistant-delivery reconciliation in the hosted side-effect journal, best-effort deletes only per-user artifact objects no longer referenced by the latest workspace snapshot after successful bundle transitions, commits only bundle-ref/version transitions inside Durable Object storage after any R2 bundle I/O succeeds, decrypts stored bundle/artifact/journal objects by their envelope `keyId` through the configured keyring so older ciphertext can remain readable during staged rotation, forwards only the canonical hosted runtime vars that the runner actually consumes while keeping supervisor-only secrets out of the isolated child env, launches each hosted job with per-run writable cache/temp roots plus a non-supervisor launch `HOME`, reaps the child process group when the container request aborts or times out, and otherwise reuses the existing Murph inbox, parser, assistant, device-sync, and hosted share-import seams instead of inventing a second hosted canonical state model.
- `apps/cloudflare/wrangler.jsonc` remains the checked-in worker scaffold, but the repo now also ships generated deploy-artifact helpers under `apps/cloudflare/scripts/**`, the `apps/cloudflare/src/deploy-automation.ts` helper module, the checked-in `apps/cloudflare/r2-bundles-lifecycle.json` transient-cleanup config plus `pnpm --dir apps/cloudflare r2:lifecycle:apply`, and the manual GitHub Actions workflow `.github/workflows/deploy-cloudflare-hosted.yml` for environment-driven config rendering, version uploads, gradual deployments, direct-deploy fallback for first deploys or Durable Object migrations, explicit `instance_type` pinning, and version-pinned smoke checks that poll operator status until the queue drains and durable bundle refs exist. The Wrangler scaffold now explicitly enables Workers Logs and Workers Traces, declares the four required hosted runtime secrets through Wrangler's experimental `secrets.required` support, requires `CF_PUBLIC_BASE_URL` for normal deploy-and-smoke workflow runs, expects operators to apply the checked-in transient R2 lifecycle rules to the real bundles buckets as part of deploy setup, treats `CF_BUNDLE_KEY_ID` as single-key metadata rather than active rotation support, the native container image is declared in Wrangler and is built as part of `wrangler deploy`, and the containerized one-shot execution core now lives in `packages/assistant-runtime`.
- `Dockerfile.cloudflare-hosted-runner` is the checked-in scaffold for the native Cloudflare container image. It starts the private `apps/cloudflare/src/container-entrypoint.ts` bridge from repo source, which serves `GET /health` plus `POST /__internal/run` inside the container only, installs the common Linux parser dependencies baked into that image, and delegates one-shot execution to `packages/assistant-runtime`. The default execution path now isolates each hosted job in its own child process so per-user encrypted runner env overrides do not force container-wide request serialization, gives that child a scrubbed env plus per-run writable cache/temp roots and a non-supervisor launch `HOME` instead of the full supervisor env, and reaps the child process group when the internal request disconnects or times out. The present expectation is Node `>=22.16.0`, workspace dependencies installed from this repo, writable temp storage for restore/snapshot work, `PORT`, required `HOSTED_EXECUTION_RUNNER_CONTROL_TOKEN`, optional `HOSTED_EXECUTION_RUNNER_COMMIT_TIMEOUT_MS`, and shared worker/container allowlist extension vars for encrypted per-user env overrides when additional key names must be permitted. Build-time image toggles such as `INSTALL_PADDLEOCR` now flow through Wrangler container `image_vars` instead of a separate GHCR image-publish path.
- The local assistant daemon entrypoint lives under `packages/assistantd`; `murph-assistantd` binds to one vault, rejects non-loopback hosts, requires a bearer token on every route, sets `MURPH_ASSISTANTD_DISABLE_CLIENT=1` in its own process so daemon-local calls do not recurse back through HTTP, and now fronts the steady-state assistant session/message/options flows plus session/status/outbox/cron inspection and serializable automation control whenever the CLI invocation does not need local-only hooks such as live provider events, foreground inbox events, abort propagation, or local session/transcript snapshots.
- The current runner scaffold is still intentionally repo-source-based rather than a slim production image. `apps/cloudflare/DEPLOY.md` is the durable guide for the current staged manual deploy path.
- The local web entrypoint lives under `packages/web`; `pnpm web:dev` starts the app through a wrapper that blocks framework `.env*` reads, preserves the original `pnpm` launch cwd for relative `VAULT` values, leaves bundler and host selection to Next or explicit CLI flags, prefers an explicit `VAULT` and otherwise falls back to the saved Murph CLI default vault, resolves internal workspace packages from source through `config/workspace-source-resolution.ts`, renders a read-only local vault surface with safe-field search over the query layer, and may initiate device auth/account actions only through the separate local `device-syncd` control plane configured by `DEVICE_SYNC_BASE_URL` plus `DEVICE_SYNC_CONTROL_TOKEN`, with bearer-backed control-plane access restricted to loopback URLs.
- Before adding a runtime target, document entrypoints, environment assumptions, and operational guardrails here.
