# Murph Agent Docs Index

Last verified: 2026-04-01 (verification docs still cover the prepared incremental CLI test-runtime build lane, the reusable built `cli-entry` artifact plus persistent non-stdin CLI test harness escape hatch, the forced rebuild fallback when the prepared smoke check sees concurrently-missing artifacts, bucketed CLI/hosted-web/Cloudflare Node Vitest lanes, local-parallel app verification defaults, local-fast verify wrapper scripts for `packages/local-web`, `apps/web`, and `apps/cloudflare`, repo and app Vitest worker fallback chains, env-controlled file-level Vitest parallelism plus opt-in suite-level concurrency overrides across all repo-owned suites, including the CLI helper's mirrored opt-in fallback and focused proof for its env overrides, overlapped local package-plus-app test execution, the prepared hosted-web Prisma generate flow plus local `apps/web verify` overlap of `test`, `dev:smoke`, and `next build`, the faster hosted-web smoke readiness poll, the new text-only Markdown docs fast path, the scoped-verification fallback for narrowly bounded non-fast-path changes when repo-wide checks are already known red, the completion workflow's new default final-review-only audit path with `simplify` reserved for massive non-patch changes, the explicit repo-policy rule that required audit subagent passes do not need separate user permission, the plan doc's local working-tree large-change-set exemption for parallel-agent dirty trees, the refreshed `finish-task` versus `committer` helper split that now does Bash-3-compatible preflight commit-target expansion before plan closure and accepts file-or-directory inputs while still resolving to exact changed file paths for dirty-tree-safe scoped commits, and the pnpm 10 supply-chain hardening lane covering exact pnpm engine matching, committed workspace trust-policy controls, blocked-build review helpers, and `corepack pnpm` guidance for dependency-sensitive flows; the index also reflects the refreshed architecture/reliability/security docs for bound assistant tool-runtime parity, bounded vault text inspection, no-replay tool-turn failover rules, explicit read-only/non-committing audit-subagent workflow rules, the device-sync metadata storage bounds, and the gateway-core owner-package hard-cut execution lane)

## Purpose

This index is the table of contents for durable, repository-local context that agents should use.

## Canonical Docs

| Path | Purpose | Source of truth | Owner | Review cadence | Criticality | Last verified |
| --- | --- | --- | --- | --- | --- | --- |
| `README.md` | Repo bootstrap summary, hosted/local runtime-package status, and verification entrypoint for humans. | Repository bootstrap decisions | Murph Maintainer | Per bootstrap/process change | Medium | 2026-03-26 |
| `ARCHITECTURE.md` | Top-level module map and trust-boundary summary, including the local `assistantd` loopback daemon boundary, the apps/web Postgres `execution_outbox` contract, immutable hosted AI usage rows imported after successful hosted commits, the Cloudflare hosted runner's SQLite-backed Durable Object queue state and direct Durable Object control path, the explicit `@murphai/assistant-core` headless boundary for non-CLI consumers, keyring-aware hosted ciphertext reads during staged bundle-key rotation, and the shared bounded assistant/vault tool-runtime surface now exposed across Codex-backed and OpenAI-compatible provider turns. | Product/runtime code plus repo decisions | Murph Maintainer | Per architecture change | High | 2026-03-31 |
| `docs/architecture.md` | One-page architecture summary and target repo shape for the baseline vault plus adjunct device-sync, inbox, parser, and web layers. | Baseline vault architecture decisions | Murph Maintainer | Per architecture change | High | 2026-03-17 |
| `docs/architecture-review-2026-04-01.md` | Point-in-time architecture review covering shared-model ownership, orchestration-heavy hosted seams, and incremental simplifications landed in the 2026-04-01 review patch. | 2026-04-01 architecture review snapshot | Murph Maintainer | As needed when review snapshots are added | Low | 2026-04-01 |
| `docs/architecture-review-2026-03-30.md` | Point-in-time architecture review covering current ownership boundaries, duplicated invariants, and incremental refactor recommendations, while also recording the landed shared registry-helper and health-history constant simplifications. | 2026-03-30 architecture review snapshot | Murph Maintainer | As needed when review snapshots are added | Low | 2026-03-30 |
| `docs/composability-review-2026-03-31.md` | Point-in-time composability review that identifies current multi-seam large-file split candidates, keeps canonical write and provider boundaries intact, and records incremental extraction plans rather than preemptive code motion. | 2026-03-31 composability review snapshot | Murph Maintainer | As needed when review snapshots are added | Low | 2026-03-31 |
| `docs/architecture-review-2026-03-31.md` | Point-in-time architecture review covering current shared-model ownership drift, hosted bundle-ref invariants, and incremental package-boundary refactors while preserving Murph's file-native canonical-write seams. | 2026-03-31 architecture review snapshot | Murph Maintainer | As needed when review snapshots are added | Low | 2026-03-31 |
| `docs/legacy-removal-audit-2026-03-31.md` | Point-in-time audit of compatibility behavior that can be hard-cut immediately versus legacy paths that still serve active contracts. | 2026-03-31 legacy-removal audit snapshot | Murph Maintainer | As needed when review snapshots are added | Low | 2026-04-01 |
| `docs/cloudflare-hosted-idempotency-followup.md` | Current status and durable-rule doc for eliminating duplicate externally visible side effects in hosted execution through the committed side-effect journal plus the hosted `apps/web` outbox, receipt, Stripe-fact, and issuance journals. | Hosted execution reliability follow-up | Murph Maintainer | Per hosted execution reliability change | Medium | 2026-03-28 |
| `docs/device-sync-hosted-control-plane.md` | Proposed hosted control-plane split for device sync, including the shared public-ingress seam and hosted/local responsibility boundary. | Device-sync architecture direction | Murph Maintainer | Per device-sync architecture change | Medium | 2026-03-26 |
| `docs/contracts/` | Frozen contract docs for vault layout, schemas, command surface, and invariants, including assistant `status|doctor|stop` aliases, assistant `state|memory|cron` surfaces, and observability/recovery surfaces. | Baseline vault interface decisions | Murph Maintainer | Per contract change | High | 2026-03-27 |
| `agent-docs/PLANS.md` | Execution-plan workflow and storage conventions, including when narrow supplied-patch landings may stay ledger-only and how the large-change-set guard stays strict for staged/CI checks while local working-tree runs ignore unrelated dirty-tree file count by default. | `agent-docs/exec-plans/**` | Murph Maintainer | Per process change | Medium | 2026-03-31 |
| `agent-docs/PRODUCT_SENSE.md` | Product-behavior guardrails for future implementation work. | Product specs + user-facing behavior | Murph Maintainer | Monthly | Medium | 2026-03-26 |
| `agent-docs/PRODUCT_CONSTITUTION.md` | Internal product constitution for behavioral direction, UX tone, and feature-tradeoff review. | Murph product principles | Murph Maintainer | Per product-direction change | High | 2026-03-28 |
| `agent-docs/FRONTEND.md` | Frontend rules for the current local operator-facing web surface and future UI work. | `packages/local-web/**`, theme/layout decisions | Murph Maintainer | Per web UI change | High | 2026-03-23 |
| `agent-docs/QUALITY_SCORE.md` | Current quality posture by area. | Docs, checks, audits, test posture | Murph Maintainer | Bi-weekly | Medium | 2026-03-12 |
| `agent-docs/RELIABILITY.md` | Reliability guardrails and failure-mode expectations, including restartable foreground connector policy, bounded network timeout/retry expectations, and the no-implicit-retry or cross-route replay rule for tool-enabled assistant turns. | Runtime behavior + recovery strategy | Murph Maintainer | Per reliability-affecting change | High | 2026-03-31 |
| `agent-docs/SECURITY.md` | Security constraints, trust boundaries, and escalation rules, including how CLI-loaded local `.env` files remain sensitive, how device-sync metadata writes stay bounded to shallow diagnostic state, how root pnpm supply-chain controls plus blocked-build review stay enabled, and how OpenAI-compatible tool-enabled assistant turns stay bound to the active vault without gaining arbitrary host access. | Auth/secrets/data boundaries/process | Murph Maintainer | Per security-affecting change | High | 2026-04-01 |
| `agent-docs/product-specs/index.md` | Index for product-spec documents. | `agent-docs/product-specs/**` | Murph Maintainer | Per behavior change | High | 2026-03-12 |
| `agent-docs/product-specs/repo-bootstrap.md` | Current repository state and bootstrap success criteria. | Repository bootstrap decisions | Murph Maintainer | Until first product feature lands | High | 2026-03-12 |
| `agent-docs/references/README.md` | Reference-pack overview and maintenance rules. | `agent-docs/references/**` | Murph Maintainer | Monthly | Medium | 2026-03-12 |
| `agent-docs/references/health-entity-taxonomy-seam.md` | Design note for keeping `packages/contracts/src/health-entities.ts` as the shared owner of health taxonomy, lookup aliases, scaffold templates, registry metadata, and the landed Goal payload/frontmatter/command metadata consumed by query and CLI. | `packages/contracts/src/health-entities.ts`, `packages/query/src/health/registries.ts`, `packages/cli/src/health-cli-descriptors.ts` | Murph Maintainer | Per taxonomy seam change | Medium | 2026-03-29 |
| `agent-docs/references/repo-scope.md` | Repo scope, ownership boundary, and unknowns. | Workspace routing + local decisions | Murph Maintainer | Per scope change | High | 2026-03-12 |
| `packages/hosted-execution/README.md` | Package-local overview for the shared hosted execution contract, auth, env, callback-host defaults, lifecycle helpers, and client seam used by `apps/web` and `apps/cloudflare`. | `packages/hosted-execution/**` | Murph Maintainer | Per hosted execution contract change | Medium | 2026-03-28 |
| `packages/runtime-state/README.md` | Package-local overview for the root-vs-`node` runtime-state boundary: hosted-safe bundle/email/env helpers at the root plus Node-only `.runtime` path, assistant-state, process, and SQLite helpers under `@murphai/runtime-state/node`. | `packages/runtime-state/**` | Murph Maintainer | Per runtime-state change | Medium | 2026-04-01 |
| `packages/device-syncd/README.md` | Package-local overview for the local wearable OAuth/webhook/reconcile daemon, its control-plane env contract, and the Murph-managed launcher path. | `packages/device-syncd/**` | Murph Maintainer | Per device-sync runtime change | Medium | 2026-03-18 |
| `packages/assistantd/README.md` | Package-local overview for the local assistant daemon, its single-vault loopback/bearer control plane, and the staged CLI routing seam for steady-state assistant runtime calls plus session/status/outbox/cron inspection and serializable automation control. | `packages/assistantd/**` | Murph Maintainer | Per assistant daemon/runtime change | Medium | 2026-03-30 |
| `apps/web/README.md` | App-local overview for the hosted integration control plane, its Prisma/Postgres contract including the durable `execution_outbox`, immutable hosted AI usage imports plus the optional token-meter cron, the hosted onboarding lane including optional RevNet issuance, the hosted share-link/import flow, and the browser/public/agent route split for device-sync plus hosted Linq ingress/dispatch. | `apps/web/**` | Murph Maintainer | Per hosted control-plane change | Medium | 2026-03-29 |
| `apps/cloudflare/README.md` | App-local overview for the Cloudflare-hosted execution plane, including signed dispatch, explicit `member.activated` bootstrap, the per-user Durable Object's SQLite-backed queue state, one encrypted hosted workspace snapshot in the `vault` slot plus separate encrypted artifact objects and per-user env storage, keyring-aware hosted ciphertext reads during staged rotation, the native Durable Object + Container execution path, direct Durable Object control methods, the fast Node `test` loop plus Workers-runtime `test:workers` lane under `verify`, the rollout guard for newer Durable Object migration tags, Wrangler `secrets.required` validation for the four hosted runtime secrets, the transient R2 lifecycle helper, and the `@murphai/assistant-runtime` one-shot runner boundary. | `apps/cloudflare/**` | Murph Maintainer | Per hosted execution change | Medium | 2026-03-29 |
| `packages/assistant-runtime/README.md` | Package-local overview for the headless hosted assistant runtime surface consumed by Cloudflare and future hosted executors. | `packages/assistant-runtime/**` | Murph Maintainer | Per hosted runtime change | Medium | 2026-03-27 |
| `packages/gateway-local/README.md` | Package-local overview for the local vault-backed gateway runtime, rebuildable `.runtime/gateway.sqlite` serving store, and local adapter seam layered on top of the transport-neutral `@murphai/gateway-core` contracts. | `packages/gateway-local/**` | Murph Maintainer | Per gateway-local runtime change | Medium | 2026-03-31 |
| `apps/cloudflare/DEPLOY.md` | Concrete staged deployment guide for the Cloudflare-hosted execution plane, including generated deploy artifacts, the manual GitHub Actions workflow, the `apps/cloudflare verify` acceptance command, version uploads plus gradual deployments, staged bundle-key rotation guidance, the rollout helper's direct-deploy guard for newer Durable Object migration tags, observability config, required `CF_PUBLIC_BASE_URL` smoke wiring, the transient R2 lifecycle helper, and version-pinned smoke steps. | `apps/cloudflare/**` | Murph Maintainer | Per hosted deploy-flow change | Medium | 2026-03-28 |
| `packages/local-web/README.md` | Package-local overview for the local-only Next.js observability app and its vault configuration contract. | `packages/local-web/**` | Murph Maintainer | Per web-surface change | Medium | 2026-03-26 |
| `packages/parsers/README.md` | Package-local overview for local-first attachment parsing, provider ranking, and derived artifact publication. | `packages/parsers/**` | Murph Maintainer | Per parser-layer change | Medium | 2026-03-13 |
| `agent-docs/references/testing-ci-map.md` | Current verification map for contracts/runtime packages including `@murphai/assistant-runtime`, hosted and local web app coverage, built CLI checks, the smoke harness, release-flow helper coverage, CLI `.env` loading coverage, the prepared CLI `cli-entry` artifact plus persistent non-stdin harness coverage, device-sync provider tests, the explicit `@murphai/assistant-core` single-boundary regressions, hosted share/onboarding plus verified-email sync regression coverage, hosted AI usage ledger/import plus optional Stripe token-meter coverage, explicit hosted activation-bootstrap and separate per-user env coverage, the Cloudflare fast Node `test` loop plus Workers-runtime `test:workers` lane under `verify`, AgentMail email connector/delivery/setup discovery/reuse coverage, repo and app Vitest worker fallback defaults plus file-parallelism env overrides and opt-in suite-concurrency defaults, the targeted Vitest/V8 package coverage surface including the audited `packages/hosted-execution/src/{auth,client,contracts,env,routes}.ts` set, keyring-aware Cloudflare ciphertext-read coverage, assistant cron plus assistant `status|doctor|stop` coverage, the workspace-boundary audit, the workspace package-cycle guard, the source-artifact guard including the explicit `packages/local-web/{postcss,eslint}.config.mjs` and `apps/web/{postcss,eslint}.config.mjs` allowlist plus hosted `.next-dev` and `.next-smoke` lanes, the clean-clone Next route-type stub bootstrap, the deploy-workflow `CF_PUBLIC_BASE_URL` preflight, the Wrangler `secrets.required` validation note, and the current hosted-runner deployment-validation gap. | `package.json`, `scripts/**`, `fixtures/**`, `e2e/**`, future CI | Murph Maintainer | Per verification change | High | 2026-04-01 |
| `agent-docs/operations/agent-workflow-routing.md` | Compact durable workflow router for task classes, doc-loading paths, ledger/plan expectations, audit defaults including the default final-review-only path with `simplify` reserved for massive non-patch changes, the explicit rule that required audit subagent passes are pre-authorized by repo policy, verification paths including the text-only Markdown docs fast path, and commit-helper routing so `AGENTS.md` can stay a short table of contents instead of a policy blob. | `AGENTS.md`, process docs, commit helpers | Murph Maintainer | Per process change | High | 2026-04-01 |
| `agent-docs/operations/verification-and-runtime.md` | Required checks, runtime-package verification matrix including the text-only Markdown docs fast path, scoped-verification fallback rules for narrowly bounded non-fast-path changes when repo-wide checks are already known red for unrelated reasons, built CLI behavior coverage, fixed-version monorepo release-check semantics, repo and app Vitest worker fallback defaults plus file-parallelism env overrides and opt-in suite-concurrency defaults, the reusable built CLI `cli-entry` artifact plus persistent non-stdin harness escape hatch, pnpm dependency-policy verification plus blocked-build review commands, CLI `.env.local` / `.env` loading assumptions with shell-env precedence, device-sync/web control-plane assumptions including localhost-default `device-syncd` host, the local `assistantd` loopback/bearer control-plane contract and staged CLI routing seam, the explicit `@murphai/assistant-core` single-boundary verification notes, bearer-authenticated control routes, hosted browser signed-assertion replay defense, hosted share-link runtime and import assumptions, optional public callback/webhook listener separation, per-account job serialization plus Oura and WHOOP daemon verification coverage, assistant binding/delivery runtime assumptions including AgentMail email plus inbox discovery/reuse setup behavior, explicit `assistant state`, `assistant memory`, `assistant cron`, and assistant `status|doctor|stop` CLI/runtime behavior including host-bound provenance and MCP tooling, hosted assistant usage capture under `assistant-state/usage/pending` plus post-commit import into `apps/web` and optional Stripe token metering, onboard entrypoint/wrapper semantics including the assistant/channel/wearable onboarding stepper plus current-run env prompts, package-local no-emit typecheck semantics including `@murphai/assistant-runtime`, the centralized workspace source-resolution helper with explicit package allowlists, the workspace-boundary audit, the workspace package dependency cycle guard, the source-artifact allowlist for the explicit `packages/local-web/{postcss,eslint}.config.mjs` and `apps/web/{postcss,eslint}.config.mjs` paths plus hosted `.next-dev` and `.next-smoke` lanes, the clean-clone Next route-type stub bootstrap, the thresholded `core`/audited `hosted-execution`/`importers`/`query` V8 coverage surface plus the rebuild-before-final-Vitest wrapper step, the hosted verified-email sync and self-target reconciliation checks, the hosted RevNet native-ETH terminal pricing/runtime assumptions, and the current manual Cloudflare worker/native-container scaffold assumptions including explicit hosted activation bootstrap, one encrypted hosted workspace snapshot in the `vault` slot plus separate encrypted artifact objects and per-user env storage, keyring-aware hosted ciphertext reads during staged rotation, the hosted runner's SQLite queue tables, direct Durable Object control methods, the fast Node `test` loop plus Workers-runtime `test:workers` lane under `apps/cloudflare verify`, the gradual-deploy migration guard, observability config, Wrangler `secrets.required` validation for the four hosted runtime secrets, the local `apps/web verify` overlap of `test`, `dev:smoke`, and `next build`, the faster hosted-web smoke readiness poll, required `CF_PUBLIC_BASE_URL` deploy-smoke wiring, the transient R2 lifecycle helper, and post-R2 Durable Object ref/version commits for the TypeScript-first workspace. | `AGENTS.md`, `package.json`, `fixtures/**`, `e2e/**`, `scripts/**` | Murph Maintainer | Per process/runtime change | High | 2026-04-01 |
| `agent-docs/operations/completion-workflow.md` | Required post-implementation audit workflow, including the default `task-finish-review` path with `simplify` reserved for massive non-patch changes, read-only/non-committing audit-agent rules, coverage/proof-gap checking folded into the final completion audit, the repo's standing authorization for required audit passes without separate user permission when the environment supports spawned agents, the ledger-first workflow for narrow supplied-patch landings, and the `scripts/finish-task` versus `scripts/committer` commit-helper split with preflight path resolution before plan closure. | Prompts + completion process | Murph Maintainer | Per process change | High | 2026-04-01 |
| `agent-docs/prompts/simplify.md` | Reusable simplification pass prompt for a dedicated spawned audit subagent, explicitly constrained to read-only/non-committing review output. | Completion workflow | Murph Maintainer | Per process change | Medium | 2026-03-31 |
| `agent-docs/prompts/frontend-quality-review.md` | Reusable browser-backed frontend review prompt for hierarchy, responsiveness, and polish, returning read-only textual findings plus recommended fixes. | Completion workflow | Murph Maintainer | Per process change | Medium | 2026-03-31 |
| `agent-docs/prompts/task-finish-review.md` | Reusable final completion-review prompt for a dedicated spawned audit subagent, explicitly constrained to read-only/non-committing review output plus coverage/proof-gap review. | Completion workflow | Murph Maintainer | Per process change | High | 2026-03-31 |
| `agent-docs/generated/README.md` | Generated doc artifacts produced by scripts. | `agent-docs/generated/**` | Murph Maintainer | Per script change | Medium | 2026-03-12 |
| `agent-docs/exec-plans/active/` | Active execution plans and in-flight coordination docs, including the shared coordination ledger plus current implementation/refactor plans such as the assistant canonical-write/workspace fixes lane, the direct `@murphai/assistant-core` assistant-boundary hard-cut lane, the final local-only assistant-core/headless review-fixes lane, the assistant model-config hard-cutover lane, the assistant provider/session seam-cleanup lane, the hosted execution-outbox minimization lane, the shared PDF-routing fallback plus runtime-state verification-unblock lane, the Cloudflare hosted-runner patch-landing lane, the Cloudflare hosted-email bridge lane, the hosted email final cleanup lane, the Cloudflare staging follow-up lane for required-secret validation plus deploy-smoke/lifecycle hardening, the CLI health simplification pass-two lane, the Goal shared-entity-definition lane, the hosted device-sync store-split lane, the hosted-runtime seam-decomposition lane, the gateway serving-store cutover lane, the gateway owner-package hard-cut lane, the delayed ChatGPT re-check, attachment-download, and wake-resume tooling lanes, and the six-refactor worker orchestration/integration plans; narrow supplied-patch landings may stay ledger-only while they remain bounded and single-turn. | Plan workflow | Murph Maintainer | Per process change | Medium | 2026-03-31 |
| `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` | Active task coordination ledger for safe concurrent work; rows are notices by default and exclusive only when explicitly marked. | Active coding sessions | Murph Maintainer | Continuous | High | 2026-03-29 |
| `agent-docs/exec-plans/completed/` | Immutable completed execution-plan snapshots that may intentionally retain pre-rename Murph references, including recently closed lanes such as the hosted webhook receipt CAS simplification snapshot and the Telegram webhook/file-boundary hardening snapshot. | Plan workflow | Murph Maintainer | Per process change | Medium | 2026-03-28 |
| `agent-docs/exec-plans/tech-debt-tracker.md` | Rolling debt register with owner/priority/status. | Audits, reviews, bootstrap follow-ups | Murph Maintainer | Bi-weekly | Medium | 2026-03-12 |

## Conventions

- Keep `AGENTS.md` short and route-oriented.
- Update this index whenever durable docs are added, removed, or moved. Execution-plan churn under `agent-docs/exec-plans/**` and coordination-ledger-only updates do not require index edits.
- Treat `UNCONFIRMED` product/domain assumptions as temporary and replace them with concrete specs before broad implementation work.
- For multi-file or high-risk work, add a plan in `agent-docs/exec-plans/active/`. Narrow supplied-patch landings may stay ledger-only while the work remains bounded and single-turn.
- Keep `agent-docs/exec-plans/active/COORDINATION_LEDGER.md` current during active coding work, and mark rows as exclusive only when overlap is genuinely unsafe.
- For `packages/cli` work, treat `ARCHITECTURE.md` and `agent-docs/operations/verification-and-runtime.md` as the durable home for incur-specific routing, typegen, and built-vs-source verification rules.
