# Murph Architecture

Last verified: 2026-04-08

## Module Map

Only five packages are published to npm: `@murphai/contracts`, `@murphai/hosted-execution`, `@murphai/gateway-core`, `@murphai/murph`, and `@murphai/openclaw-plugin`. All other `packages/*` entries remain workspace-private owner packages. When a public package still needs one of those private workspace packages at runtime, the release flow bundles that private dependency into the public tarball instead of publishing it as a standalone npm package.

## Package Boundary Rules

- Treat each workspace package `package.json` as a small public contract, not as a mirror of the internal file tree.
- Keep `exports` narrow and semantic. Prefer a few intentional entrypoints such as `"."`, `./runtime`, `./helpers`, or another owner-level seam over file-shaped paths like `./usecases/foo` or `./assistant/bar`.
- If a package starts needing many unrelated subpath exports, split ownership into a new package or a clearer owner surface instead of growing a long `exports` list.
- Compatibility shims are temporary migration tools only. Hard-cut them once callers move, and do not leave pass-through files or re-exports around as permanent API.
- Do not re-export another package's broad surface just to make imports shorter. Callers should depend on the real owner package when that owner already exists.
- Keep root barrels and public entrypoints behavior-oriented. Internal helper modules, test-only seams, and source-layout details should stay private unless there is a deliberate owner-level reason to publish them.
- When tightening a package boundary, update the matching package-shape or boundary guard tests in the same change so future drift fails mechanically instead of relying on memory.

- `packages/contracts`: canonical Zod contracts, shared event-envelope/lifecycle parse and revision-collapse helpers, TypeScript types, generated JSON Schema artifacts, and the shared vault-family registry/layout/query-source metadata consumed by core, query, and inboxd
- `packages/hosted-execution`: shared hosted control-plane contracts, HMAC signing/verification helpers, vendor-neutral env readers, route builders, and side-effect codecs; it no longer owns Cloudflare worker-host topology or proxy-client inference, and app-local adapters now own deployment-specific transport, hostname, and token policy
- `packages/runtime-state`: workspace-private shared hosted email/env/loopback/id helpers plus pure hosted bundle identity types/equality on the root package, with an explicit `@murphai/runtime-state/node` subpath for hosted bundle codec/materialization, explicit `.runtime` taxonomy/path resolution (`operations` vs `projections` vs `cache/tmp`), assistant runtime path/security helpers, process scoping, versioned JSON helpers, and SQLite-backed Node-only migration seams
- `packages/core`: workspace-private canonical mutation owner, including the reserved `vault upgrade` seam for live local-vault evolution, with current-format canonical reads/writes failing closed on older `formatVersion` values until an explicit upgrade step is registered; it also owns the shared raw-attachment staging/manifests and canonical event attachment metadata used by document, meal, workout, and measurement writes, the dedicated `addActivitySession` and `addBodyMeasurement` seams for workout-session and body-measurement persistence, and the shared event-spine envelope assembly used by generic events and history writes over the single `ledger/events` seam
- `packages/importers`: workspace-private ingestion adapters that parse external files or provider API snapshots, normalize them behind registry-based adapters, and delegate all writes to core
- `packages/device-syncd`: workspace-private local device OAuth/webhook/reconcile runtime with an authenticated localhost control plane, optional separate public callback/webhook ingress, a reusable shared public-ingress core for future hosted/tunneled callback surfaces, the canonical `@murphai/device-syncd/client` control-plane client/contracts surface for workspace or bundled callers, and durable local operational state under `.runtime/operations/device-sync/**` split explicitly into connection identity/config, credential authority state, and observation/reconcile state while normalized provider snapshot imports still flow through importers/core
- `packages/messaging-ingress`: workspace-private shared stateless messaging-provider ingress package that owns provider webhook parsing/verification, target grammar, supported-message extraction, summary helpers, and sparse raw minimization for transports such as Telegram and Linq without taking on polling drivers, hosted policy, or runtime persistence
- `packages/inboxd`: workspace-private inbox capture ingestion/runtime package that persists canonical raw inbox evidence plus a first-class append-only inbox-capture ledger while keeping inbox-only cursors, source-specific checkpoints, capture indexes, and attachment job state in a rebuildable local SQLite projection under `.runtime/projections/inboxd.sqlite`, with inbox daemon/config JSON state under `.runtime/operations/inbox/**`; it consumes `@murphai/messaging-ingress` for stateless Telegram/Linq ingress semantics while continuing to own polling connectors, local capture persistence, and the optional inbox-plus-parser daemon composition helpers layered on top of parser-owned runtime contracts
- `packages/parsers`: workspace-private local-first attachment parsing, parser-service helpers, parser-owned runtime/store contracts for attachment parsing, and derived artifact publication under `derived/inbox/**`; it no longer owns inbox daemon orchestration or depends upward on `@murphai/inboxd`
- `packages/query`: workspace-private read helpers, export-pack generation, the semantic wearable read model and day-summary layer over imported device evidence, the rebuildable local query projection over canonical vault data under `.runtime/projections/query.sqlite` that now backs both `readVault()` and lexical search, the stable reference-graph readers for `bank/library/**`, the pure parser/search/index helpers for derived knowledge pages under `derived/knowledge/**`, and the read-side adapters that now consume shared health registry projection metadata plus shared event lifecycle/revision collapse helpers from `@murphai/contracts` instead of maintaining duplicate query-local copies
- `packages/assistant-engine`: workspace-private headless assistant execution runtime that now also owns the canonical vault, inbox, knowledge, query/runtime-helper, and shared usecase surfaces consumed across the workspace; it owns provider-turn execution, tool/runtime assembly, assistant state/outbox/status/store surfaces, the integrated vault/inbox service factories, shared usecase/query helpers, and the local gateway adapter used by the daemon and hosted runtimes, while consuming provider-target normalization and hosted provider-preset/config helpers from `@murphai/operator-config` instead of owning duplicate copies
- `packages/operator-config`: workspace-private operator and setup configuration surface that owns persisted operator defaults, hosted assistant config, assistant backend target normalization, hosted provider-preset/config helpers, setup/runtime-env helpers, device/channel readiness helpers, and CLI/shared command contracts
- `packages/assistant-cli`: workspace-private CLI-only assistant surface that owns the daemon-aware assistant wrappers, assistant command registration, foreground terminal logging, and the Ink chat UI
- `packages/setup-cli`: workspace-private CLI-only onboarding and host-setup surface that owns the setup wizard, host provisioning helpers, AgentMail setup helpers, and assistant/channel/wearable onboarding flows
- `packages/gateway-core`: published transport-neutral gateway boundary package that owns the shared gateway contracts, route helpers, projection/snapshot logic, opaque ids, and event-log helpers used by both local and hosted adapters
- `packages/gateway-local`: workspace-private local vault-backed gateway runtime package that owns the rebuildable `.runtime/projections/gateway.sqlite` source-backed projection store plus the local gateway read/send/permission wrapper surface used by assistantd and restored hosted-runtime workspaces; it depends only on lower-layer gateway contracts, persists source-side gateway rows plus event metadata, derives conversation/message/attachment snapshots on demand, and receives assistant-backed projection/send adapters by injection from higher layers
- `packages/assistantd`: workspace-private local assistant daemon package with a bearer-authenticated loopback-only control plane bound to one vault; it fronts steady-state local assistant session/message/status/automation entrypoints directly through `@murphai/assistant-engine`, and now hosts the full local gateway conversation/message/attachment/send/event/permission API over `@murphai/gateway-local`, serving reads from the rebuildable local gateway source-backed projection store without moving canonical writes out of the CLI/core boundary
- `packages/assistant-runtime`: workspace-private headless hosted assistant execution surface that exposes one-shot inbox/bootstrap/assistant/outbox/device-sync/share-import runtime behavior behind explicit runtime context, consumes `@murphai/assistant-engine` and explicit `@murphai/operator-config/*` owner subpaths instead of the umbrella config root, now treats the durable operator `hostedAssistant` config as the only persisted hosted assistant source of truth, and consumes shared messaging ingress contracts from `@murphai/messaging-ingress` rather than defining provider semantics itself
- `apps/web`: hosted Next.js integration control plane for Vercel-style deployments, backed by Postgres/Prisma for device OAuth sessions, opaque public device-connection ids plus blind-index ownership mapping, typed durable connection summaries, webhook traces, sparse sync signals, token-audit history, a delivery-only `execution_outbox` for Cloudflare-bound hosted execution handoff persisted as immutable inline envelopes or staged Cloudflare-owned dispatch refs, with transport-local outbox status kept separate from the shared cross-boundary dispatch outcome union now durably persisted as `dispatchState`, immutable hosted AI usage rows imported after successful hosted commits, Linq recipient bindings, queued Linq webhook events, per-member hosted Linq daily message state, queued hosted Stripe receipt/retry rows, hosted onboarding webhook receipts for receipt-local side effects, Privy-backed hosted onboarding state, stable hosted Stripe customer/subscription refs for metering and reconciliation, dormant invoice-idempotent RevNet issuance records kept for future re-enable, hosted share-link tiny preview summaries plus lifecycle metadata in Postgres, Cloudflare-backed opaque share-pack objects used only at execution time, local-agent pairing, and the app-local Vercel OIDC bearer-token adapter used for hosted execution dispatch/control while keeping canonical health and inbox data out of the hosted tier; hosted onboarding identity, billing, and routing lookups now stay slice-owned and return nested matched-slice results instead of flattening back into a wide aggregate or forcing immediate rereads; hosted Telegram dispatches now use a narrow message envelope that keeps only routing ids, coarse top-level text or placeholders, and attachment fetch metadata rather than any minimized raw Telegram update copy; canonical decryptable hosted device-sync token escrow now lives in the Cloudflare runtime store, while ordinary hosted-web settings/control-plane reads stay on durable Prisma metadata plus typed connection summaries and live runtime inspection is reserved for explicit operational routes
- `apps/cloudflare`: hosted execution plane for Vercel OIDC-authenticated web control/dispatch, per-user coordination via container-enabled Durable Objects, encrypted hosted workspace snapshot storage plus separately encrypted artifact objects, a native Cloudflare container image, an encrypted hot per-user gateway projection plus short-retained gateway event log for internal control-plane reads, a short-lived warm per-user outer runner shell, and one-shot inbox/parser/assistant/device-sync/share-import child execution that materializes a temporary local workspace through `packages/assistant-runtime` rather than importing hosted seams from the operator CLI surface; the worker keeps the app-local Vercel OIDC validation and request-verification adapter that guards its internal routes, owns the leaf-artifact contract under `apps/cloudflare/.deploy/runner-bundle`, installs the real published-shape `@murphai/murph` package there as a leaf executable dependency so hosted `vault-cli` resolves from the normal package/bin layout while runtime behavior still stays in `packages/assistant-runtime`, and keeps `wrangler.generated.jsonc` plus `worker-secrets.json` as sibling deploy-only inputs outside the copied image
- `packages/cli`: the published `@murphai/murph` package plus the `murph` and `vault-cli` binaries, an incur-backed typed operator surface over core/importers/query/inboxd plus parser-toolchain queue controls, quick workout capture as a workflow façade over canonical `activity_session`, `body_measurement`, and `workout_format` primitives, inbox model-routing helpers, provider-backed assistant session orchestration, the transport-neutral `@murphai/gateway-core` headless conversation/message/event/send contract now shared directly by assistantd and hosted Cloudflare gateway adapters (with MCP still intentionally deferred), a local gateway projection store under `.runtime/projections/gateway.sqlite` now owned by `@murphai/gateway-local`, incrementally syncing inbox captures through inboxd's durable local capture mutation cursor while still syncing assistant sessions/outbox via their existing signatures, persisting only source-side gateway rows plus projection metadata/event state so empty projections do not rebuild forever, and serving conversation/message/attachment reads plus event polling from source-derived snapshots, optional env-routed client access to `packages/assistantd` for steady-state local assistant open/send/update flows plus session/status/outbox/runtime inspection and serializable automation control, CLI-owned command/UI/client helpers for the selected vault, one shared bound assistant/vault tool catalog with turn-scoped profiles for canonical memory, canonical automation, assistant runtime inspection, and derived knowledge, saved self-target inspection, explicitly enabled guarded public `web.fetch` and `web.pdf.read`, provider-agnostic `web.search` when a search backend is configured, bounded vault text reads, deterministic inbox document-preservation helpers, and vault query/write operations across provider turns, outbound iMessage/Telegram/Linq/AgentMail email channel adapters, an onboarding/setup wizard that can reuse or discover existing AgentMail inboxes before provisioning, and local host setup commands for macOS and Linux while keeping iMessage itself macOS-only. Programmatic assistant, vault/inbox, and setup surfaces stay in workspace-private owner packages such as `@murphai/assistant-engine`, `@murphai/operator-config`, `@murphai/assistant-cli`, and `@murphai/setup-cli`; the release flow bundles those private owners into the public CLI tarball when needed instead of publishing them as standalone npm products.
- `packages/openclaw-plugin`: published OpenClaw-compatible bundle package in the default Claude bundle layout (`skills/**`) that teaches OpenClaw to use Murph's existing `vault-cli` surface against the operator's configured vault via OpenClaw's built-in `exec` tool, keeping the integration skill-first, vault-first, and free of any second Murph assistant runtime inside OpenClaw
- `fixtures/` and `e2e/`: deterministic fixture corpus and end-to-end smoke flows

## Trust Boundaries

- Canonical vault storage is file-native under the vault root.
- Human-facing truth lives in Markdown documents such as `CORE.md`, journal pages, and experiment pages.
- Canonical markdown writes now reduce to one shared `packages/core` document seam with three target shapes only: singleton documents (for example `CORE.md` and `bank/memory.md`), slugged documents (for example `bank/automations/*.md`, `bank/experiments/*.md`, and registry-backed bank records), and dated documents (for example `journal/YYYY/YYYY-MM-DD.md`). Typed singleton JSON documents such as `bank/preferences.json` remain canonical too, but they stay out of the markdown seam on purpose.
- Machine-facing truth lives in append-only JSONL ledgers for inbox captures, events, samples, and audit records, with inbox capture intake canonicalized first through `ledger/inbox-captures/**`.
- Raw imported artifacts are immutable once copied into `raw/`, and they now live under owner-scoped directories derived from the owning canonical record or import session (`kind` + `id`, plus a partition only for batch families such as device/sample/workout imports). Each raw import directory keeps a `manifest.json` sidecar that records the same explicit owner metadata used to resolve the on-disk path, while normalized device/provider API snapshots continue to live under `raw/integrations/**`.
- Parser outputs under `derived/inbox/**` are rebuildable and never canonical health facts.
- `bank/library/**` is the stable health reference layer for durable shared entities such as biomarkers, domains, protocol variants, and source artifacts.
- Model-authored compiled knowledge pages under `derived/knowledge/**` are the separate non-canonical, rebuildable personal wiki layer that synthesizes local vault evidence and saved research notes without becoming a second source of truth. `derived/knowledge/index.md` is the content catalog, `derived/knowledge/log.md` is the append-only write log, and `derived/knowledge/pages/*.md` stores the assistant-authored pages themselves.
- Inbox model-routing bundles, plans, and execution results under `derived/inbox/**/assistant/*.json` are rebuildable audit artifacts and never canonical health facts.
- Inbox runtime state is split between a rebuildable local projection under `.runtime/projections/inboxd.sqlite` and durable daemon/config JSON state under `.runtime/operations/inbox/*.json`; the projection remains rebuildable from canonical vault evidence under `raw/inbox/**`.
- AgentMail inbox ids, outbound email thread bindings, and channel credentials remain non-canonical local/runtime concerns; AgentMail API keys stay in operator environment variables and never belong in the vault.
- Query runtime state is local-only under `.runtime/projections/query.sqlite`, is rebuildable from canonical vault evidence, and remains strictly read-only relative to canonical writes.
- Durable local runtime state is split explicitly: `.runtime/operations/**` holds non-canonical operational state, `.runtime/projections/**` holds rebuildable indexes/projections, and `.runtime/cache/**` plus `.runtime/tmp/**` stay ephemeral. Canonical vault evolution is a separate seam: `vault.json`, `CORE.md`, and any future canonical record-shape changes stay behind the `vault upgrade` seam in `packages/core`; today older `formatVersion` values fail closed until an explicit upgrade step exists, while `.runtime/projections/**` stores remain rebuildable and never carry canonical migration authority. `vault.json` itself stays minimal and instance-owned: it stores only `formatVersion`, `vaultId`, `title`, `timezone`, and `createdAt`, while layout paths, shard patterns, and id-prefix policy remain code-owned runtime contract details. Portability is a second explicit axis on top of that taxonomy: runtime paths are either `portable` or `machine_local`, and operational state defaults to `machine_local` unless a more specific classification says otherwise. Device sync runtime state is machine-local under `.runtime/operations/device-sync/state.sqlite`, and Murph's daemon launcher state/logs live under `.runtime/operations/device-sync/`; encrypted provider tokens, OAuth sessions, and webhook/reconcile cursors never belong in the canonical vault. Portable operational examples include canonical write-operation receipts and inbox promotion ledgers that must move with the vault's recovery/idempotency context. In the hosted lane, Cloudflare owns the canonical decryptable device-sync runtime and token escrow under the user root key, while `apps/web` Postgres keeps only integration/auth metadata, opaque connection ids plus blind-index mapping, typed durable connection summaries, token-audit history, and sparse webhook/signal state rather than canonical health payloads. Ordinary hosted-web settings reads must stay on that durable Postgres seam, with live Cloudflare runtime reads limited to explicit operational endpoints. Hosted onboarding billing refs, queued Stripe receipts, webhook receipts, the shared delivery-only `execution_outbox`, immutable hosted AI usage rows, and optional RevNet issuance rows in Postgres are operational/idempotency state only, not canonical health truth. Within that hosted outbox, inline payloads and staged Cloudflare dispatch refs are durable handoff facts only, transport-local outbox status is web-owned lease/retry state only, and the shared dispatch outcome union persisted as `dispatchState` is the sole cross-boundary product-facing outcome vocabulary; hosted Telegram handoff is intentionally reduced to stable routing ids, coarse placeholder text for non-text messages, and attachment fetch metadata, with names, usernames, phone numbers, locations, venue text, poll text, and raw Telegram envelopes excluded from durable hosted storage. After a dispatch is accepted by Cloudflare, retries, poison/backpressure handling, business-outcome callbacks, and completion all live in the Cloudflare queue and committed-result lane rather than on the Postgres row.
- Local assistant runtime state is non-canonical under `vault/.runtime/operations/assistant/**`, including sessions, transcripts, outbox/receipt artifacts, diagnostics, status, failover state, and other execution residue. Durable user-facing memory, typed preferences, compiled wiki pages, and scheduled prompt configuration do not live in assistant runtime state; they live under `bank/memory.md`, `bank/preferences.json`, `derived/knowledge/**`, and `bank/automations/*.md`. Session persistence still stores one canonical assistant model target plus separate resume-state metadata rather than duplicating provider config across multiple runtime records, hosted assistant profiles and hosted bootstrap stay constrained to explicit OpenAI-compatible targets even though local operator defaults may still prefer Codex, and turn execution now resolves boundary defaults, persisted session target, and per-turn overrides through one explicit execution-plan seam before provider-specific request shaping. Murph may replay recent raw transcript turns and rely on provider-native resume or compaction for continuity, but that runtime context still must never be treated as canonical health memory or vault truth.
- Storage-policy hard line: if a datum is user-facing, queryable, or something future product features will build on, it belongs in canonical vault records or explicit derived materializations, not in assistant runtime. `vault/.runtime/operations/assistant/**` is for execution residue, replay/continuity artifacts, and operator diagnostics only.
- The gateway plane is a derived operational model over inbox captures, assistant bindings, sent outbox deliveries, and approval state. Local daemon APIs and hosted Durable Objects may materialize hot gateway projections and short-retained event logs for transport-facing reads, but those projections are never canonical health truth and must remain rebuildable from canonical vault evidence plus non-canonical runtime state. The local projection in `.runtime/projections/gateway.sqlite` is owned by `@murphai/gateway-local`, persists only source-side gateway rows plus projection metadata and event state, consumes inboxd's durable local capture mutation cursor for incremental capture sync, derives conversation/message/attachment snapshots on demand, keeps stable empty-snapshot bootstrap metadata so no-op reads stay incremental, and keeps full rebuild behavior as the bootstrap or recovery path.
- Hosted execution state for `apps/cloudflare` may store one encrypted hosted workspace snapshot in the existing `vault` bundle slot plus separately encrypted artifact objects and per-user coordination metadata. That hosted workspace snapshot now includes canonical `vault/**`, only the `.runtime/**` paths explicitly classified `portable`, and the minimal operator-home config needed for explicit `member.activated` bootstrap, including the durable top-level `hostedAssistant` operator config while keeping raw provider credentials in Worker secrets or the separately encrypted per-user env object. `machine_local` operational state such as device-sync runtime state, daemon state/logs, inbox daemon config/state, parser toolchain overrides, rebuildable local projections under `vault/.runtime/projections/**`, ephemeral cache/tmp state, and local incur CLI defaults under `~/.config/murph/config.json` remain excluded from the hosted workspace snapshot because the hosted lane owns those equivalents separately, explicitly ignores local incur autodiscovery during hosted assistant/provider turns, or because they are host-bound by design. Large raw artifacts under `vault/raw/**` are externalized into their own encrypted objects. The runner restores inline workspace files first, materializes only the externalized artifact paths the current run actually needs, preserves untouched artifact refs across later snapshots so old media is not re-downloaded just to survive the next commit, and best-effort deletes only per-user artifact objects no longer referenced by the latest workspace snapshot. Hosted per-user env overrides still live in their own encrypted object keyed independently from the workspace snapshot. Those hosted bundles still represent canonical vault material and must be treated as sensitive data rather than a separate hosted source of truth. The worker-facing control routes are internal only, the per-user Durable Object now keeps its queue/process state in SQLite tables plus encrypted bundle refs rather than one serialized record blob, starts its native Cloudflare container and talks to the runner over the container TCP port, commits only small bundle-ref/version state changes inside Durable Object storage after any R2 I/O completes, persists committed execution journal records with the owning `userId` so recovery can still rebind the Durable Object after partial state loss, and also maintains an encrypted hot per-user gateway projection snapshot plus short-retained gateway event log derived from the durable commit plus returned final result rather than from a second hosted source of truth. Cloudflare now also owns the canonical decryptable device-sync runtime store under the user root key, including signed read/apply control routes plus metadata-only snapshot merges from `apps/web`, while Prisma retains only public connection metadata and token-audit history. The containerized runner now reads hosted device-sync runtime state, pending usage buffers, hydrated share packs, and other worker-owned runtime inputs only through worker-owned outbound proxy hosts or inline dispatch payloads that bind the request to the owning `userId`, keeps the broad web internal/control token out of the runner environment, reaches `apps/web` only for the narrow hosted wearable connect-link flow behind that same bound-user proxy seam, launches each hosted job in a scrubbed child env with per-run temp and cache roots instead of inheriting the full supervisor env, avoids the supervisor `HOME` at child launch while still restoring the persisted operator-home snapshot inside the workspace for runtime config, strips ambient supervisor proxy credentials from the isolated child env, reaps the whole child process group when the container request times out or disconnects, persists due hosted side effects on the committed journal, authenticates the outer container invoke/destroy wrapper with the runner control token header before any body is trusted, accepts hosted gateway send requests only through the same route-bound gateway seam, and now uses the worker's internal commit and side-effect routes while returning the fuller final result directly to the Durable Object so post-commit retries can resume outward delivery without rerunning compute. Hosted share acceptance now hands Cloudflare only a tiny inline share ref; the runner hydrates the opaque share pack from Cloudflare storage immediately before import, and if that pack is missing the worker releases the Postgres claim through the signed hosted-web callback instead of forcing a cross-system read during the original claim transaction. Exact consumed-event tombstones now block replayed hosted dispatch event ids for 24 hours; there is no secondary replay filter beyond that authoritative window. Hosted ciphertext envelopes still decrypt by envelope `keyId` through the configured keyring so operators can stage platform-envelope-key rotation without eagerly rewriting older objects. Writes still use one active platform envelope key at a time, and missing keyring entries still fail closed. This lane does not claim operator-blind privacy or TEE isolation.
- Any inbox-to-canonical promotion idempotency must be stored in or derivable from canonical vault evidence, not `.runtime/` alone.
- General assistant/session state belongs under `vault/.runtime/operations/assistant/**`, including local transcript files, per-turn decision receipts, replay-safe outbound intent journals, pending provider-usage records, diagnostics events plus snapshot counters, persisted assistant status snapshots, provider failover cooldown state, and runtime automation execution state plus run history. Durable user-facing memory belongs canonically in `bank/memory.md`, typed preferences belong canonically in `bank/preferences.json`, and durable scheduled prompt configuration belongs canonically in `bank/automations/*.md`; capture-scoped rebuildable audit artifacts stay under `derived/inbox/**`, while durable compiled knowledge dossiers live under `derived/knowledge/**`.
- Provider transcript history and channel-native delivery history should stay with upstream adapters when possible; Murph stores local assistant transcript copies, minimal manual aliases, explicit conversation bindings, runtime automation cursors, enabled auto-reply channel state, timestamps/turn counts, provider session references, runtime automation run history, compact system-emitted turn receipts, idempotent outbound intent state, diagnostics counters/warnings, persisted failover cooldown metadata, and persisted status snapshots under `vault/.runtime/operations/assistant/**`. Assistant runtime directories must stay private (`0700`) and assistant runtime files must stay private (`0600`). Secret-bearing provider headers for persisted sessions live only in private sidecars under `vault/.runtime/operations/assistant/secrets/**`; the general session JSON keeps only public headers, diagnostics/runtime-event writes redact inline secret material before persistence, and `assistant doctor --repair` can tighten permissive assistant runtime modes in place. Inline secret findings indicate stale local session data rather than a supported migration path. Fresh sessions may inject a small canonical memory block from `bank/memory.md`, and provider turns now use one shared CLI-first Murph runtime surface plus a small helper-tool layer across manual and message-triggered automation turns. OpenAI-compatible routes reach canonical Murph operations through the shared `murph.cli.run` executor plus a few bounded helper tools, while Codex remains a privileged local adapter that can call the same `vault-cli` surface through its native local CLI/filesystem/env authority. Optional provider-agnostic `web.search` plus explicitly opt-in `web.fetch` and `web.pdf.read` stay available as public read-only external-context tools rather than canonical data paths. These web-read tools remain HTTP(S) read surfaces only: they strip URL fragments, redact query-bearing URL details from returned metadata and errors, resolve and pin vetted public destination addresses before each request or redirect so the socket does not silently re-resolve to a different host, block private-network destinations, bound response and PDF-parse work, and return untrusted external content rather than canonical Murph data. Accepted inbound channel messages are therefore treated as operator-authorized actions for the bound vault and may use the assistant runtime, canonical `memory`, canonical `automation`, self-target, and vault query/write surface. Murph owns transcript policy, turn orchestration, and tool/runtime planning across providers, but Codex CLI is now an explicitly privileged local adapter rather than a boxed-in host-tool client: it keeps native local CLI/filesystem/env authority, defaults to unsandboxed execution plus no approval friction, and is trusted as a local operator path. Canonical vault records remain authoritative on conflicts.
- `vault-cli inbox model route` may send a normalized inbox bundle, with raw routing images attached when eligible, to either the AI Gateway or an operator-specified OpenAI-compatible endpoint.

## Control Flow

1. Operators, automations, and future agent layers call `vault-cli` or package APIs.
2. CLI commands stay thin, validate input, and delegate to internal CLI use-case modules that coordinate `packages/core`, `packages/importers`, `packages/query`, `packages/inboxd`, and parser-toolchain helpers from `packages/parsers`. Canonical mutation flows for experiments, journal pages, providers, events, vault summary updates, and inbox journal/experiment-note promotions must route through typed `packages/core` mutation ports; CLI may keep read-side lookup/orchestration, but it must not parse/stringify canonical frontmatter or assemble canonical write batches for those write paths.
3. Inbox capture persists raw evidence from iMessage, Telegram, Linq webhook chats, and AgentMail email connectors, appends typed `ledger/inbox-captures` facts as the canonical intake record, indexes attachments, and enqueues parse jobs in rebuildable local runtime state. Generic event/audit projections happen later only when a promotion or user-visible flow needs them.
4. Parser workers or parsed-pipeline wrappers consume those attachment jobs and publish only derived artifacts.
5. Inbox model routing can materialize a normalized bundle, attach supported stored images for multimodal routing when available, and write audited bundle/plan/result artifacts before any optional apply step.
6. Importers may parse and normalize external inputs but must never write canonical vault files directly. Provider connectors normalize upstream payloads into shared device-batch payloads and still rely on `packages/core` for canonical persistence.
7. `packages/device-syncd` owns provider OAuth state, reconnect/disconnect control, scheduled device backfills, and optional webhook fan-in; its control routes must stay loopback-only plus bearer-authenticated, any public callback/webhook ingress should stay isolated from `/accounts/*` and `/providers/*`, polling-first providers remain first-class citizens, provider credentials stay outside the vault, per-account jobs should be serialized to avoid rotating-refresh-token races, and canonical health writes still flow through `packages/importers` and `packages/core`. Its provider-agnostic public OAuth/webhook handling should live in a reusable shared ingress layer so future hosted control planes and local tunnel setups do not fork provider callback logic. `packages/cli` may start, reuse, and stop that daemon for the active vault, but it should treat the localhost HTTP control plane as the stable boundary rather than reaching through to provider state in-process.
8. Provider-backed assistant chat and outbound channel flows may persist local session metadata, local transcript files, explicit delivery bindings, auto-reply cursors/channel state, runtime automation execution state, and derived gateway conversation/message/event projections under `vault/.runtime/operations/assistant/**`, but they must not treat that state as canonical health truth or bypass canonical write boundaries for health data. Durable user-facing memory and scheduled prompt configuration belong in canonical vault records, not assistant runtime. Saved assistant session bindings are monotonic routing facts: lookup may enrich missing channel, identity, participant, or thread fields, but it must fail closed rather than silently rebinding an existing session to a different audience; one-off outbound retargeting belongs on the explicit delivery-target override path instead of mutating the saved binding. The local `packages/assistantd` daemon is now an allowed loopback-only bearer-authenticated control boundary for this runtime, but it stays bound to one vault and does not become a second canonical write owner. Current outbound adapters include iMessage, Telegram, Linq, and AgentMail-backed email; email auto-reply is intentionally limited to direct threads and must preserve the inbound AgentMail inbox identity for replies, while Linq replies reuse the inbound chat id thread binding for the local webhook-driven conversation. Assistant inbox automation now merges canonical-routing candidates and reply candidates into one ordered decision pass so a capture is reasoned over once per scan even though reply and routing progress still keep separate internal cursors, and that same pass preserves accepted stored document attachments as canonical documents before later model routing or reply behavior depends on them. Canonical prompt-backed automations must declare an explicit outbound channel route and always deliver their generated response instead of storing local-only undelivered summaries. Provider turns now bind the real current user prompt, session id, and turn id on the host side, share one CLI-first Murph runtime surface across provider families, and keep provider differences focused on transport/runtime behavior rather than Murph authority. OpenAI-compatible routes call the bound runtime through the shared `murph.cli.run` executor plus bounded helper tools, while Codex reaches the same canonical `vault-cli` surface through its native local CLI authority. Message-triggered assistant turns now use that same full Murph runtime surface rather than a bounded read-only profile, so any accepted inbound channel message can inspect runtime state and canonical vault records for the bound user and vault. The provider still changes the transport mechanism for those actions, but not whether the bound user/vault authority exists. Provider execution context is rebuilt per failover route instead of reusing the primary-provider plan across provider families, assistant runtime receipt/outbox/diagnostics/failover/status mutations stay serialized under one shared assistant-runtime write lock, and due canonical automations execute only while `vault-cli assistant run` is active for that vault.
9. Query/export paths are read-only and must not mutate canonical vault state.
11. The hosted `apps/web` control plane accepts only provider OAuth/webhook traffic plus authenticated browser/agent control traffic, keeps raw provider tokens away from browsers, keeps decryptable provider tokens out of browser-visible surfaces, stores only sparse Linq routing/event state instead of canonical captures, records durable token export/refresh audit rows, and should expose sparse sync hints/token export endpoints or queued Linq webhook events only to the paired local agent for the owning user. Hosted Linq onboarding now treats any valid first-contact inbound as an onboarding request: it creates or reuses the hosted member keyed by the blinded phone lookup, persists the Linq direct-chat binding on the member for post-payment follow-up, requires each direct `linqChatId` to belong to at most one hosted member at a time, sends at most one onboarding link per UTC day while the member is still inactive, counts echoed outbound Linq webhooks against the same per-member UTC-day state, and enforces a 100 inbound-message UTC-day cap for billing-active members before writing the hosted execution dispatch outbox row, with at most one quota reply per UTC day. The only human/browser wearable-management surface is `/api/settings/device-sync/**`; it relies on Privy-issued identity tokens from the `privy-id-token` cookie plus allowed `Origin` checks, keeps SMS login and embedded-wallet creation browser-initiated through Privy's SDK user state, centralizes local server-side cookie verification with Privy's verification key before issuing a hosted session cookie, and serves ordinary settings reads from durable Prisma metadata plus sparse hosted signals rather than live Cloudflare runtime snapshots. Hosted onboarding identity is anchored on the verified phone plus blind lookup keys in Postgres, and `apps/web` is now the sole durable owner of recoverable raw hosted-member identifiers: the owner tables keep encrypted phone numbers, Privy user ids, embedded-wallet addresses, signup phone state, Linq chat ids, Telegram user ids, Stripe customer ids, and Stripe subscription ids alongside one canonical blind lookup key per field so request-path writes stay purely transactional inside Prisma. Contact privacy keeps a future key-rotation seam at the keyring layer: one current key version writes new lookup keys, reads can derive candidates for every configured keyring version, and older key versions fail closed once they are removed from the configured keyring. If a deployed rotation is ever needed, it should ship as a targeted migration against the then-current runtime and queue behavior rather than as permanent launch-time backfill machinery. Embedded-wallet state may arrive later and only becomes mandatory if RevNet-backed billing or issuance is ever re-enabled. Hosted lifecycle truth is intentionally collapsed: `HostedMember.billingStatus` owns entitlement, suspension is a timestamp-only admin lane on the member row, invite rows keep only delivery and expiration metadata, Stripe Checkout Sessions are ephemeral Stripe-owned artifacts rather than durable local history, and the browser/API onboarding stage is derived on read instead of persisted across multiple enums. Lower-level assertion-authenticated browser bridge routes such as `POST /api/device-sync/agents/pair` must rely on short-lived, request-bound signed assertions from a trusted auth edge or proxy, reject stale assertions, and consume browser-assertion nonces once so captured assertions cannot be replayed across hosted control-plane routes. Remaining hosted web internal routes now split cleanly by principal: Vercel cron routes accept only `CRON_SECRET`, Cloudflare-owned callback routes accept only dedicated asymmetric callback signatures verified against the published Cloudflare callback public JWK set, and hosted share creation lives on the normal user-authenticated web boundary instead of a generic signed server-to-server lane. The worker-facing hosted execution dispatch/control edge is bearer-authenticated with Vercel OIDC workload identity instead of a shared web-to-Cloudflare secret, but that deployment-specific token acquisition stays in the `apps/web` auth adapter rather than the shared hosted-execution seam, while the runner hot path no longer reaches back into hosted web for device-sync runtime snapshots, share payload reads, direct usage-record writes, or hosted-member private-state lookups. `@murphai/hosted-execution` is now limited to the shared dispatch/transport/status seam; the Cloudflare-owned operational control contract between `apps/web` and `apps/cloudflare` lives in the private `@murphai/cloudflare-hosted-control` package, and hosted device-sync runtime snapshot/apply/connect-link contracts live in `@murphai/device-syncd/hosted-runtime`. `apps/web` now seeds and updates Cloudflare-owned canonical device-sync runtime state while later importing pending hosted usage by reading Cloudflare-owned storage, and live device-sync runtime reads from web stay limited to explicit operational routes such as agent token export/refresh, heartbeat reconciliation, disconnect, and runtime upkeep. Hosted assistant wearable connect-link creation remains the narrow exception: the runner still reaches `apps/web` only through the Cloudflare-owned `device-sync.worker` proxy host, and the worker adds the bound user header plus its own dedicated callback signature, so the runner never sees broad hosted-web credentials. Hosted email ingress is its own trust boundary: resolving the recipient alias is not sufficient, so the worker must re-authorize the sender before raw-message persistence or hosted dispatch. New outbound mail keeps the fixed public sender identity but uses one stable per-user reply alias, registered members may also initiate a conversation by emailing that fixed public sender address once their verified email has been synced into hosted execution, direct-public-mail lookup is allowed only through an encrypted verified-owner index that stores secret-derived sender hashes instead of raw verified emails, that direct-public path now requires matching envelope and `From` sender values before lookup, unresolved or unauthorized mail to the public sender address is accepted-and-dropped to reduce account-state probing, leaked aliases are never sufficient authority on their own, and hosted email ingress no longer accepts legacy per-thread reply aliases or legacy verified-owner route records. All Cloudflare-bound hosted execution from `apps/web`, including device-sync wake hints, hosted share acceptance imports, and hosted onboarding activation or Linq dispatches, must first be written to the shared Postgres `execution_outbox` in the same transaction as the originating state mutation; delivery to `apps/cloudflare` is then retried out of band, and hosted UI/API surfaces should read eventual execution state instead of depending on synchronous runner responses. Successful hosted assistant provider attempts now also land one immutable pending usage record under `vault/.runtime/operations/assistant/usage/pending/`; `apps/web` later imports those records from the Cloudflare-owned pending-usage store so hosted Postgres, not Stripe, remains the canonical hosted usage ledger. Hosted onboarding webhook receipts are now the retry journal only for receipt-local Linq or Telegram side effects; they durably persist the planned response plus queued side effects before any external send, keep a reclaimable processing lease, and mark hosted execution dispatch effects sent once the outbox row is durable rather than once Cloudflare transport returns inline. Stripe webhook ingress now verifies the event, stores a minimal Stripe receipt row, commits local billing state plus an inline `member.activated` outbox fact inside the reconciliation transaction, runs managed-user crypto provisioning only after that transaction commits, and only then marks the Stripe receipt completed before the best-effort outbox drain. The Stripe recovery cron handles failed or deferred receipts, optional hosted AI token metering, and dormant RevNet issuance repair-only follow-up out of band. Member activation dispatches now stay sparse in the shared outbox payload but remain self-contained for first-contact delivery: `apps/web` includes the minimal `firstContact` locator in the event, and hosted execution queues the deterministic Murph welcome as a normal assistant outbox intent in the existing conversation thread without reading member private state back from web. Stripe billing state must remain monotonic under out-of-order delivery, including same-second collisions, so member entitlement writes should depend on canonical Stripe subscription fetches instead of durable local event-order markers, and stale success webhooks must not reactivate access after later cancellation or delinquency. Normal subscription delinquency, cancellation, and pause states block access through `billingStatus` without consuming the explicit suspension timestamp lane, while refund/dispute/reversal outcomes still suspend hosted access, revoke live hosted sessions, and halt future activation or issuance until an operator clears suspension. For live hosted billing, `invoice.paid` is the only positive Stripe source and `customer.subscription.*` is negative/status-only. RevNet issuance is currently hard-disabled in runtime, so subscription activation no longer waits on confirmed issuance; existing issuance rows remain dormant repair/idempotency state kept in-tree for future re-enable. Once a broadcast tx hash exists, persistence failures fail closed for operator repair rather than classifying the attempt as a clean retry. This MVP still does not attempt onchain clawbacks.
12. The hosted `apps/cloudflare` execution plane accepts only Vercel OIDC-authenticated internal dispatch/control calls at `POST /internal/dispatch`, `GET /internal/users/:userId/status`, `POST /internal/users/:userId/run`, `GET|PUT|DELETE /internal/users/:userId/env`, `GET|POST /internal/users/:userId/device-sync/runtime`, `PUT /internal/users/:userId/device-sync/runtime/snapshot`, `PUT|DELETE /internal/users/:userId/dispatch-payload`, `POST /internal/users/:userId/dispatch-payload/dispatch`, `PUT /internal/users/:userId/crypto-context`, `PUT|DELETE /internal/users/:userId/shares/:shareId/pack`, `GET|DELETE /internal/users/:userId/usage/pending`, and the internal gateway control routes under `POST /internal/users/:userId/gateway/**`; the narrow exception is the Cloudflare-owned callback seam back into `apps/web`, which still uses HMAC signatures that bind method, path, query string, payload, bound user id, timestamp, and a single-use nonce. The Vercel workload-identity env parsing and bearer verification for that edge now live in the Cloudflare app-local auth adapter instead of the shared hosted-execution seam. Managed-hosted root-key provisioning stays internal to the Cloudflare runtime but now happens through an explicit user-bound provisioning path rather than as an implicit side effect of generic Durable Object bootstrap. Normal runtime access must fail closed when the envelope has not been provisioned yet. Managed provisioning creates automation plus recovery recipients today, may add tee-automation later, and must preserve any future browser-originated `user-unlock` recipients during reconciliation. It routes worker control calls into direct Durable Object methods instead of worker-local callback fetches, restores an ephemeral local execution context from encrypted hosted bundle snapshots, performs canonical hosted workspace bootstrap explicitly during `member.activated`, persists or adopts one explicit durable hosted assistant profile in operator config, injects one method-based hosted runtime platform into `packages/assistant-runtime`, and keeps all Cloudflare topology ownership app-local: only `apps/cloudflare` knows that `artifactStore`, `effectsPort`, `deviceSyncPort`, and `usageExportPort` are implemented as `artifacts.worker`, `results.worker`, `device-sync.worker`, and `usage.worker`. The outer native container shell may stay warm per user for a short idle TTL, but it authenticates the internal bridge with a supervisor-only runner control token, rejects concurrent `POST /__internal/run` requests, rotates the per-run outbound proxy token after each run, and destroys ambiguous warm state instead of attempting reuse. Those proxy hosts keep the runner untrusted, inject the Durable Object's bound `userId`, and preserve the fail-closed trust boundary even if one runner container is compromised. The only remaining share-pack reads are worker-local hydration reads immediately before `vault.share.accepted` import; web pages, status reads, and control-plane callers can write or delete a pack but cannot fetch a live Cloudflare pack object again. The isolated hosted child still does not inherit the full container supervisor env or its ambient proxy credentials, now uses per-run writable cache and temp roots while avoiding the supervisor `HOME` at launch, and is killed as a process group when the container request aborts or the run finishes so each hosted execution remains one-shot at the child-runtime boundary even when the outer shell stays warm; before the shell can stay warm again, the container entrypoint must detect and kill any other live processes and otherwise exit the shell fail-closed. Callback and control-plane base URLs must normalize to HTTPS unless they are explicit loopback exceptions on the hosted-web server boundary; shared runtime packages no longer maintain worker-host allowlists or default worker URLs. The per-user queue keeps queue/poison tracking in Durable Object SQLite tables and blocks replayed immutable event ids with 24-hour exact tombstones rather than an indefinite Bloom-style filter, persists only inline/reference dispatch envelopes in SQLite while externalizing sensitive Linq, Telegram, gateway-send, and device-sync payload bodies into separately encrypted transient blobs, writes updated encrypted bundles back to object storage through opaque bundle/artifact/env/journal locators, skips bundle rewrites when the content hash is unchanged, centralizes shared dispatch contracts and outbox payload helpers in `packages/hosted-execution` while keeping worker-host maps and proxy-token policy app-local, decrypts stored bundle/artifact/journal objects by envelope `keyId` through the configured keyring, best-effort deletes consumed transient queue payload blobs and hosted raw email bodies after successful finalize while short R2 lifecycle rules backstop any leftovers, runs any required hosted-web business-outcome callback before it consumes the committed queue event, commits only the final bundle-ref/version transition inside Durable Object storage after R2 I/O succeeds, and schedules the next per-user wake without inventing a second canonical persistence model. Hosted runner observability now stays in that same Durable Object lane: Durable Object storage persists only the queue-coordination surface needed for scheduling and recovery, including pending/poison/consumed state, next wake, activation/in-flight flags, and redaction-safe error codes/timestamps, while the latest run summary and bounded phase timeline stay volatile in-memory for the live runner instance and the worker, container, and runtime boundaries emit matching structured logs so Cloudflare remains the searchable debug surface instead of a second source of truth. Hosted platform-envelope keys must be configured as fixed 32-byte base64 or base64url values so malformed key material fails startup immediately. Those logs intentionally default to redacted operator-safe summaries and do not auto-attach the bound `userId`; callers must opt in explicitly if a user identifier is truly required.

## CLI Framework Notes

- `packages/cli` is built on incur. Model nested verbs with real mounted sub-CLIs such as `search -> query` and `query -> projection -> status|rebuild`; do not simulate nested commands with argv rewrites or positional action enums.
- Treat output/discovery transport such as `--format`, `--json`, `--verbose`, `--schema`, `--llms`, `skills add`, and `--mcp` as incur-owned global behavior. Murph command docs should focus on domain semantics unless the repo intentionally constrains that surface.
- Keep the root CLI default-exported from `packages/cli/src/index.ts` and keep `packages/cli/src/incur.generated.ts` aligned with command-topology changes so typed CTAs and generated skill metadata stay truthful.
- Source-only CLI checks are useful for triage, but repo acceptance still depends on the built CLI path because package tests execute `packages/cli/dist/bin.js`.

## Source Of Truth

- Routing and hard rules: `AGENTS.md`
- Durable docs index: `agent-docs/index.md`
- Detailed architecture summary: `docs/architecture.md`
- Frozen baseline contracts: `docs/contracts/*.md`

## Current Verification Posture

The repository uses the current verification commands described in `agent-docs/operations/verification-and-runtime.md`, and it also has a repo-owned parser setup path (`pnpm setup:inbox`), a cross-platform repo-local host setup path (`pnpm onboard` / `scripts/setup-host.sh`) for macOS and Linux, a fixed-version release manifest that publishes five public packages while bundling private workspace owners into the relevant tarballs, a local device-sync runtime with service/http tests, and inbox/parser package tests that exercise runtime rebuild, parser workers, parser-toolchain discovery, and parsed-pipeline flows inside the local TypeScript workspace.
