# @murphai/setup-cli

CLI-only onboarding and host setup surface for Murph.

This package owns the setup wizard, host provisioning helpers, AgentMail setup helpers,
and assistant/channel/wearable onboarding flows. It depends on `@murphai/assistant-core`
for setup contracts, runtime helpers, and shared assistant provider catalog discovery used
during setup.
