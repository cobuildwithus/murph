{
  "name": "@healthybob/inboxd",
  "version": "0.0.0",
  "private": false,
  "description": "Local inbox ingestion runtime for Healthy Bob.",
  "license": "GPL-3.0-only",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cobuildwithus/healthybob"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "build": "rimraf dist && tsc -b tsconfig.json",
    "typecheck": "pnpm --dir ../contracts build && pnpm --dir ../core build && tsc -p tsconfig.json --noEmit --pretty false",
    "test": "pnpm --dir ../.. exec vitest run --config packages/inboxd/vitest.config.ts --no-coverage"
  },
  "dependencies": {
    "@healthybob/contracts": "workspace:*",
    "@healthybob/core": "workspace:*",
    "@healthybob/runtime-state": "workspace:*",
    "@photon-ai/imessage-kit": "2.1.2",
    "grammy": "1.41.1"
  }
}
