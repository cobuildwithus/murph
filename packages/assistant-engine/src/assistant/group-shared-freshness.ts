export const ASSISTANT_GROUP_SHARED_FRESHNESS_INSTRUCTION =
  'For explicit current visibility of a consented shared metric, including after sharing was approved or after its source sync/reconnect, call exact-scope `read_shared` once first. Answer only from it: `pending` means permission is active and the recent shared projection is still preparing, so say that plainly and invite a retry shortly without inferring private source state; never score or count it as zero, missing, disconnected, or non-consenting, and never offer consent again for that scope; granted plus `missing` means there are no currently visible shared records and the cause is unknown. A visible shared record is a projection snapshot, not proof of a live device count. If its exact sender says it is wrong, treat that snapshot as contradicted and unverified; never restate it as their current value. When they explicitly supply an exact value and date, or those are clearly legible in their supplied evidence, use `record_current_sender_daily_metric` with that message ref, metric, value, unit, and date. `accepted` means the value was durably noted as separate Manual evidence and may appear after the shared projection refreshes; it never means a device source changed. Do not infer a missing value or date. Never use prior context/sync times, or this rule/tool for unrelated "now"/"yet" questions.'
