# `@murphai/core`

Owns canonical vault initialization, filesystem primitives, audit emission, domain mutation APIs, and the ordered canonical vault-upgrade seam. No other package may mutate canonical vault data directly.
