/**
 * Fleet default and production minimum for the runtime-owned dirty-work quiet
 * window. RunnerContainer retention is intentionally a separate post-completion
 * concern.
 */
export const HOSTED_WORKSPACE_INVOCATION_DEFAULT_IDLE_CHECKPOINT_DELAY_MS =
  90_000;
