{
  "name": "@healthybob/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "bin": {
    "vault-cli": "dist/bin.js"
  },
  "scripts": {
    "build": "rimraf dist && tsc -b tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.typecheck.json --pretty false",
    "test": "pnpm --dir ../contracts build && pnpm --dir ../core build && pnpm --dir ../importers build && pnpm --dir ../query build && pnpm build && tsx ./scripts/verify-package-shape.ts && pnpm --dir ../.. exec vitest run packages/cli/test/health-tail.test.ts packages/cli/test/incur-smoke.test.ts packages/cli/test/runtime.test.ts --no-coverage --maxWorkers 1"
  },
  "dependencies": {
    "@healthybob/core": "workspace:*",
    "@healthybob/importers": "workspace:*",
    "@healthybob/query": "workspace:*",
    "incur": "0.3.3"
  }
}
