{
  "name": "@healthybob/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-only",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "ensure:query": "pnpm --dir ../query build",
    "build:app": "pnpm --dir ../.. exec tsx packages/web/scripts/next-local.ts build",
    "dev": "pnpm ensure:query && pnpm --dir ../.. exec tsx packages/web/scripts/next-local.ts dev",
    "build": "pnpm ensure:query && pnpm build:app",
    "start": "pnpm --dir ../.. exec tsx packages/web/scripts/next-local.ts start",
    "typecheck": "pnpm ensure:query && tsc -p tsconfig.json --pretty false",
    "test": "pnpm typecheck && pnpm --dir ../.. exec vitest run --config packages/web/vitest.config.ts --no-coverage --maxWorkers 1 && pnpm build:app"
  },
  "dependencies": {
    "@healthybob/contracts": "workspace:*",
    "@healthybob/query": "workspace:*",
    "@healthybob/runtime-state": "workspace:*",
    "@tailwindcss/postcss": "^4.2.1",
    "next": "^16.1.6",
    "next-themes": "^0.4.6",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3"
  }
}
