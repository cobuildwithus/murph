# `@murphai/inboxd-imessage`

Optional iMessage connector ownership for Murph inbox ingestion.

This package owns the local iMessage poll driver, connector normalization, and the direct dependency on `@photon-ai/imessage-kit`.

Consumers that only need inbox core should depend on `@murphai/inboxd`.
