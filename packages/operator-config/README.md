# @murphai/operator-config

Workspace-private operator and setup configuration surface for Murph.

This package owns persisted operator defaults, hosted assistant config, assistant backend target normalization, hosted provider preset/config helpers, setup/runtime environment helpers, device and channel readiness helpers, and CLI/shared command contracts used by assistant and setup flows.
