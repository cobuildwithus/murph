# @murphai/operator-config

Operator and setup configuration surface for Murph.

This package owns persisted operator defaults, hosted assistant config, setup/runtime environment helpers, device and channel readiness helpers, CLI/shared command contracts, and provider-target normalization used by assistant and setup flows.
